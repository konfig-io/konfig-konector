/*
Copyright 2026.

Licensed under the Apache License, Version 2.0 (the "License");
you may not use this file except in compliance with the License.
You may obtain a copy of the License at

    http://www.apache.org/licenses/LICENSE-2.0

Unless required by applicable law or agreed to in writing, software
distributed under the License is distributed on an "AS IS" BASIS,
WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
See the License for the specific language governing permissions and
limitations under the License.
*/
// Code generated by hack/gen-cloudcontrol/gen.py from the CloudFormation schema registry. DO NOT EDIT.

package v1alpha1

import (
	apiextensionsv1 "k8s.io/apiextensions-apiserver/pkg/apis/apiextensions/v1"
	metav1 "k8s.io/apimachinery/pkg/apis/meta/v1"
)

var _ = apiextensionsv1.JSON{}

// ECSClusterCapacityProviderAssociationsSpec is the desired state of AWS::ECS::ClusterCapacityProviderAssociations.
type ECSClusterCapacityProviderAssociationsSpec struct {
	// ProviderRef selects the AWSProvider (account/region) this resource is
	// reconciled against. Defaults to the namespace annotation, then the
	// default provider, then the operator's own credentials.
	// +optional
	ProviderRef *ProviderRef `json:"providerRef,omitempty"`

	DefaultCapacityProviderStrategy []ECSClusterCapacityProviderAssociationsCapacityProviderStrategy `json:"defaultCapacityProviderStrategy" cfn:"DefaultCapacityProviderStrategy"`

	// +optional
	CapacityProviders []apiextensionsv1.JSON `json:"capacityProviders,omitempty" cfn:"CapacityProviders"`

	// +kubebuilder:validation:MinLength=1
	// +kubebuilder:validation:MaxLength=2048
	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="cluster is immutable"
	Cluster string `json:"cluster" cfn:"Cluster"`
}

// ECSClusterCapacityProviderAssociationsStatus is the observed state of AWS::ECS::ClusterCapacityProviderAssociations.
type ECSClusterCapacityProviderAssociationsStatus struct {
	CloudControlStatus `json:",inline"`
}

// ECSClusterCapacityProviderAssociations manages AWS::ECS::ClusterCapacityProviderAssociations through the AWS
// Cloud Control API. Associate a set of ECS Capacity Providers with a specified ECS Cluster
// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Identifier",type="string",JSONPath=".status.identifier"
// +kubebuilder:printcolumn:name="Ready",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp"
type ECSClusterCapacityProviderAssociations struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   ECSClusterCapacityProviderAssociationsSpec   `json:"spec,omitempty"`
	Status ECSClusterCapacityProviderAssociationsStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ECSClusterCapacityProviderAssociationsList contains a list of ECSClusterCapacityProviderAssociations.
type ECSClusterCapacityProviderAssociationsList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ECSClusterCapacityProviderAssociations `json:"items"`
}

func (o *ECSClusterCapacityProviderAssociations) GetProviderRef() *ProviderRef {
	return o.Spec.ProviderRef
}
func (o *ECSClusterCapacityProviderAssociations) CloudControlTypeName() string {
	return "AWS::ECS::ClusterCapacityProviderAssociations"
}
func (o *ECSClusterCapacityProviderAssociations) CloudControlSpec() interface{} { return &o.Spec }
func (o *ECSClusterCapacityProviderAssociations) CloudControlStatusRef() *CloudControlStatus {
	return &o.Status.CloudControlStatus
}
func (o *ECSClusterCapacityProviderAssociations) CloudControlObserved() interface{} { return &o.Status }
func (o *ECSClusterCapacityProviderAssociations) SetProviderStatus(p *ProviderStatus) {
	o.Status.AWSProvider = p
}

// ECSClusterCapacityProviderAssociationsCapacityProviderStrategy is a nested property type of AWS::ECS::ClusterCapacityProviderAssociations.
type ECSClusterCapacityProviderAssociationsCapacityProviderStrategy struct {
	// +kubebuilder:pruning:PreserveUnknownFields
	CapacityProvider apiextensionsv1.JSON `json:"capacityProvider" cfn:"CapacityProvider"`

	// +kubebuilder:validation:Minimum=0
	// +kubebuilder:validation:Maximum=100000
	// +optional
	Base *int64 `json:"base,omitempty" cfn:"Base"`

	// +kubebuilder:validation:Minimum=0
	// +kubebuilder:validation:Maximum=1000
	// +optional
	Weight *int64 `json:"weight,omitempty" cfn:"Weight"`
}

// ECSDaemonSpec is the desired state of AWS::ECS::Daemon.
type ECSDaemonSpec struct {
	// ProviderRef selects the AWSProvider (account/region) this resource is
	// reconciled against. Defaults to the namespace annotation, then the
	// default provider, then the operator's own credentials.
	// +optional
	ProviderRef *ProviderRef `json:"providerRef,omitempty"`

	// The Amazon Resource Name (ARN) of the cluster that the daemon is running in.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	ClusterArn string `json:"clusterArn,omitempty" cfn:"ClusterArn"`

	// The Amazon Resource Name (ARN) of the daemon task definition used by this revision.
	// +optional
	DaemonTaskDefinitionArn string `json:"daemonTaskDefinitionArn,omitempty" cfn:"DaemonTaskDefinitionArn"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	DaemonName string `json:"daemonName,omitempty" cfn:"DaemonName"`

	// Specifies whether Amazon ECS managed tags are turned on for the daemon tasks.
	// +optional
	EnableECSManagedTags *bool `json:"enableECSManagedTags,omitempty" cfn:"EnableECSManagedTags"`

	// Specifies whether the execute command functionality is turned on for the daemon tasks.
	// +optional
	EnableExecuteCommand *bool `json:"enableExecuteCommand,omitempty" cfn:"EnableExecuteCommand"`

	// Specifies whether tags are propagated from the daemon to the daemon tasks.
	// +kubebuilder:validation:Enum=DAEMON;NONE
	// +optional
	PropagateTags string `json:"propagateTags,omitempty" cfn:"PropagateTags"`

	// The Amazon Resource Names (ARNs) of the capacity providers associated with the daemon.
	// +optional
	CapacityProviderArns []string `json:"capacityProviderArns,omitempty" cfn:"CapacityProviderArns"`

	// The deployment configuration used for this daemon deployment.
	// +optional
	DeploymentConfiguration *ECSDaemonDaemonDeploymentConfiguration `json:"deploymentConfiguration,omitempty" cfn:"DeploymentConfiguration"`

	// +optional
	Tags []CFNTag `json:"tags,omitempty" cfn:"Tags"`
}

// ECSDaemonStatus is the observed state of AWS::ECS::Daemon.
type ECSDaemonStatus struct {
	CloudControlStatus `json:",inline"`

	// Read-only: reported by AWS after creation.
	// +kubebuilder:validation:Enum=ACTIVE;DELETE_IN_PROGRESS
	// +optional
	DaemonStatus string `json:"daemonStatus,omitempty" cfn:"DaemonStatus"`

	// Read-only: reported by AWS after creation.
	// +optional
	CreatedAt string `json:"createdAt,omitempty" cfn:"CreatedAt"`

	// Read-only: reported by AWS after creation.
	// +optional
	UpdatedAt string `json:"updatedAt,omitempty" cfn:"UpdatedAt"`

	// Read-only: reported by AWS after creation.
	// +optional
	DeploymentArn string `json:"deploymentArn,omitempty" cfn:"DeploymentArn"`

	// Read-only: reported by AWS after creation.
	// +optional
	DaemonArn string `json:"daemonArn,omitempty" cfn:"DaemonArn"`
}

// ECSDaemon manages AWS::ECS::Daemon through the AWS Cloud Control API. Information about a daemon resource.
// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Identifier",type="string",JSONPath=".status.identifier"
// +kubebuilder:printcolumn:name="Ready",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp"
type ECSDaemon struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   ECSDaemonSpec   `json:"spec,omitempty"`
	Status ECSDaemonStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ECSDaemonList contains a list of ECSDaemon.
type ECSDaemonList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ECSDaemon `json:"items"`
}

func (o *ECSDaemon) GetProviderRef() *ProviderRef               { return o.Spec.ProviderRef }
func (o *ECSDaemon) CloudControlTypeName() string               { return "AWS::ECS::Daemon" }
func (o *ECSDaemon) CloudControlSpec() interface{}              { return &o.Spec }
func (o *ECSDaemon) CloudControlStatusRef() *CloudControlStatus { return &o.Status.CloudControlStatus }
func (o *ECSDaemon) CloudControlObserved() interface{}          { return &o.Status }
func (o *ECSDaemon) SetProviderStatus(p *ProviderStatus)        { o.Status.AWSProvider = p }

// ECSDaemonDaemonDeploymentConfiguration is a nested property type of AWS::ECS::Daemon.
type ECSDaemonDaemonDeploymentConfiguration struct {
	// The percentage of container instances to drain simultaneously during a daemon deployment. Valid values are
	// between 0.0 and 100.0.
	// +optional
	DrainPercent *float64 `json:"drainPercent,omitempty" cfn:"DrainPercent"`

	// The amount of time (in minutes) to wait after a successful deployment step before proceeding. This allows time
	// to monitor for issues before continuing. The default value is 0.
	// +kubebuilder:validation:Minimum=0
	// +kubebuilder:validation:Maximum=1440
	// +optional
	BakeTimeInMinutes *int64 `json:"bakeTimeInMinutes,omitempty" cfn:"BakeTimeInMinutes"`

	// The CloudWatch alarm configuration for the daemon deployment. When alarms are triggered during a deployment,
	// the deployment can be automatically rolled back.
	// +optional
	Alarms *ECSDaemonDaemonAlarmConfiguration `json:"alarms,omitempty" cfn:"Alarms"`
}

// ECSDaemonDaemonAlarmConfiguration is a nested property type of AWS::ECS::Daemon.
type ECSDaemonDaemonAlarmConfiguration struct {
	// The CloudWatch alarm names to monitor during a daemon deployment.
	// +optional
	AlarmNames []string `json:"alarmNames,omitempty" cfn:"AlarmNames"`

	// Determines whether to use the CloudWatch alarm option in the daemon deployment process. The default value is
	// ``false``.
	// +optional
	Enable *bool `json:"enable,omitempty" cfn:"Enable"`
}

// ECSDaemonTaskDefinitionSpec is the desired state of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionSpec struct {
	// ProviderRef selects the AWSProvider (account/region) this resource is
	// reconciled against. Defaults to the namespace annotation, then the
	// default provider, then the operator's own credentials.
	// +optional
	ProviderRef *ProviderRef `json:"providerRef,omitempty"`

	// The Amazon Resource Name (ARN) of the task execution role that grants the Amazon ECS container agent
	// permission to make Amazon Web Services API calls on your behalf.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	ExecutionRoleArn string `json:"executionRoleArn,omitempty" cfn:"ExecutionRoleArn"`

	// The short name or full Amazon Resource Name (ARN) of the IAM role that grants containers in the daemon task
	// permission to call Amazon Web Services APIs on your behalf.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	TaskRoleArn string `json:"taskRoleArn,omitempty" cfn:"TaskRoleArn"`

	// The IPC namespace mode for the daemon. The valid values are ``none`` and ``shared``. The default is ``none``.
	// If ``none`` is specified or no value is provided, the daemon runs with its own IPC namespace, isolated from
	// other tasks. If ``shared`` is specified, the daemon joins the host IPC namespace, making it accessible to
	// non-daemon tasks that use ``ipcMode: "host"`` or other daemons that use `...
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	IpcMode string `json:"ipcMode,omitempty" cfn:"IpcMode"`

	// The list of data volume definitions for the daemon task.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	Volumes []ECSDaemonTaskDefinitionVolume `json:"volumes,omitempty" cfn:"Volumes"`

	// The amount of memory (in MiB) used by the daemon task.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	Memory string `json:"memory,omitempty" cfn:"Memory"`

	// A list of container definitions in JSON format that describe the containers that make up the daemon task.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	ContainerDefinitions []ECSDaemonTaskDefinitionDaemonContainerDefinition `json:"containerDefinitions,omitempty" cfn:"ContainerDefinitions"`

	// The name of a family that this daemon task definition is registered to.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	Family string `json:"family,omitempty" cfn:"Family"`

	// The number of CPU units used by the daemon task.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	Cpu string `json:"cpu,omitempty" cfn:"Cpu"`

	// The PID namespace mode for the daemon. The valid values are ``none`` and ``shared``. The default is ``none``.
	// If ``none`` is specified or no value is provided, the daemon runs with its own PID namespace, isolated from
	// other tasks. If ``shared`` is specified, the daemon joins the host PID namespace, making it accessible to
	// non-daemon tasks that use ``pidMode: "host"`` or other daemons that use `...
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	PidMode string `json:"pidMode,omitempty" cfn:"PidMode"`

	// +optional
	Tags []CFNTag `json:"tags,omitempty" cfn:"Tags"`
}

// ECSDaemonTaskDefinitionStatus is the observed state of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionStatus struct {
	CloudControlStatus `json:",inline"`

	// Read-only: reported by AWS after creation.
	// +optional
	DaemonTaskDefinitionArn string `json:"daemonTaskDefinitionArn,omitempty" cfn:"DaemonTaskDefinitionArn"`
}

// ECSDaemonTaskDefinition manages AWS::ECS::DaemonTaskDefinition through the AWS Cloud Control API. The details
// of a daemon task definition. A daemon task definition is a template that describes the containers that form a
// daemon. Daemons deploy cross-cutting software agents independently across your Amazon ECS infrastructure.
// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Identifier",type="string",JSONPath=".status.identifier"
// +kubebuilder:printcolumn:name="Ready",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp"
type ECSDaemonTaskDefinition struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   ECSDaemonTaskDefinitionSpec   `json:"spec,omitempty"`
	Status ECSDaemonTaskDefinitionStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ECSDaemonTaskDefinitionList contains a list of ECSDaemonTaskDefinition.
type ECSDaemonTaskDefinitionList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ECSDaemonTaskDefinition `json:"items"`
}

func (o *ECSDaemonTaskDefinition) GetProviderRef() *ProviderRef { return o.Spec.ProviderRef }
func (o *ECSDaemonTaskDefinition) CloudControlTypeName() string {
	return "AWS::ECS::DaemonTaskDefinition"
}
func (o *ECSDaemonTaskDefinition) CloudControlSpec() interface{} { return &o.Spec }
func (o *ECSDaemonTaskDefinition) CloudControlStatusRef() *CloudControlStatus {
	return &o.Status.CloudControlStatus
}
func (o *ECSDaemonTaskDefinition) CloudControlObserved() interface{}   { return &o.Status }
func (o *ECSDaemonTaskDefinition) SetProviderStatus(p *ProviderStatus) { o.Status.AWSProvider = p }

// ECSDaemonTaskDefinitionVolume is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionVolume struct {
	// This parameter is specified when you use bind mount host volumes. The contents of the ``host`` parameter
	// determine whether your bind mount host volume persists on the host container instance and where it's stored.
	// If the ``host`` parameter is empty, then the Docker daemon assigns a host path for your data volume. However,
	// the data isn't guaranteed to persist after the containers that are associ...
	// +optional
	Host *ECSDaemonTaskDefinitionHostVolumeProperties `json:"host,omitempty" cfn:"Host"`

	// The name of the volume. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are
	// allowed. When using a volume configured at launch, the ``name`` is required and must also be specified as the
	// volume name in the ``ServiceVolumeConfiguration`` or ``TaskVolumeConfiguration`` parameter when creating your
	// service or standalone task. For all other types of volumes, this name ...
	// +optional
	Name string `json:"name,omitempty" cfn:"Name"`
}

// ECSDaemonTaskDefinitionHostVolumeProperties is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionHostVolumeProperties struct {
	// When the ``host`` parameter is used, specify a ``sourcePath`` to declare the path on the host container
	// instance that's presented to the container. If this parameter is empty, then the Docker daemon has assigned a
	// host path for you. If the ``host`` parameter contains a ``sourcePath`` file location, then the data volume
	// persists at the specified location on the host container instance until you ...
	// +optional
	SourcePath string `json:"sourcePath,omitempty" cfn:"SourcePath"`
}

// ECSDaemonTaskDefinitionDaemonContainerDefinition is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionDaemonContainerDefinition struct {
	// The user to use inside the container.
	// +optional
	User string `json:"user,omitempty" cfn:"User"`

	// The secrets to pass to the container.
	// +optional
	Secrets []ECSDaemonTaskDefinitionSecret `json:"secrets,omitempty" cfn:"Secrets"`

	// The amount (in MiB) of memory to present to the container. If the container attempts to exceed the memory
	// specified here, the container is killed.
	// +optional
	Memory *int64 `json:"memory,omitempty" cfn:"Memory"`

	// When this parameter is true, the container is given elevated privileges on the host container instance
	// (similar to the ``root`` user).
	// +optional
	Privileged *bool `json:"privileged,omitempty" cfn:"Privileged"`

	// Time duration (in seconds) to wait before giving up on resolving dependencies for a container.
	// +optional
	StartTimeout *int64 `json:"startTimeout,omitempty" cfn:"StartTimeout"`

	// The container health check command and associated configuration parameters for the container.
	// +optional
	HealthCheck *ECSDaemonTaskDefinitionHealthCheck `json:"healthCheck,omitempty" cfn:"HealthCheck"`

	// The number of ``cpu`` units reserved for the container.
	// +optional
	Cpu *int64 `json:"cpu,omitempty" cfn:"Cpu"`

	// The entry point that's passed to the container.
	// +optional
	EntryPoint []string `json:"entryPoint,omitempty" cfn:"EntryPoint"`

	// When this parameter is true, the container is given read-only access to its root file system.
	// +optional
	ReadonlyRootFilesystem *bool `json:"readonlyRootFilesystem,omitempty" cfn:"ReadonlyRootFilesystem"`

	// The image used to start the container. This string is passed directly to the Docker daemon. Images in the
	// Docker Hub registry are available by default. Other repositories are specified with either
	// ``repository-url/image:tag`` or ``repository-url/image@digest``.
	Image string `json:"image" cfn:"Image"`

	// If the ``essential`` parameter of a container is marked as ``true``, and that container fails or stops for any
	// reason, all other containers that are part of the task are stopped.
	// +optional
	Essential *bool `json:"essential,omitempty" cfn:"Essential"`

	// The log configuration specification for the container.
	// +optional
	LogConfiguration *ECSDaemonTaskDefinitionLogConfiguration `json:"logConfiguration,omitempty" cfn:"LogConfiguration"`

	// A list of files containing the environment variables to pass to a container.
	// +optional
	EnvironmentFiles []ECSDaemonTaskDefinitionEnvironmentFile `json:"environmentFiles,omitempty" cfn:"EnvironmentFiles"`

	// The name of the container. Up to 255 letters (uppercase and lowercase), numbers, underscores, and hyphens are
	// allowed.
	Name string `json:"name" cfn:"Name"`

	// The FireLens configuration for the container. This is used to specify and configure a log router for container
	// logs.
	// +optional
	FirelensConfiguration *ECSDaemonTaskDefinitionFirelensConfiguration `json:"firelensConfiguration,omitempty" cfn:"FirelensConfiguration"`

	// A list of namespaced kernel parameters to set in the container.
	// +optional
	SystemControls []ECSDaemonTaskDefinitionSystemControl `json:"systemControls,omitempty" cfn:"SystemControls"`

	// When this parameter is ``true``, you can deploy containerized applications that require ``stdin`` or a ``tty``
	// to be allocated.
	// +optional
	Interactive *bool `json:"interactive,omitempty" cfn:"Interactive"`

	// A list of ``ulimits`` to set in the container.
	// +optional
	Ulimits []ECSDaemonTaskDefinitionUlimit `json:"ulimits,omitempty" cfn:"Ulimits"`

	// Time duration (in seconds) to wait before the container is forcefully killed if it doesn't exit normally on
	// its own.
	// +optional
	StopTimeout *int64 `json:"stopTimeout,omitempty" cfn:"StopTimeout"`

	// The working directory to run commands inside the container in.
	// +optional
	WorkingDirectory string `json:"workingDirectory,omitempty" cfn:"WorkingDirectory"`

	// The soft limit (in MiB) of memory to reserve for the container.
	// +optional
	MemoryReservation *int64 `json:"memoryReservation,omitempty" cfn:"MemoryReservation"`

	// The private repository authentication credentials to use.
	// +optional
	RepositoryCredentials *ECSDaemonTaskDefinitionRepositoryCredentials `json:"repositoryCredentials,omitempty" cfn:"RepositoryCredentials"`

	// Linux-specific modifications that are applied to the container configuration, such as Linux kernel
	// capabilities.
	// +optional
	LinuxParameters *ECSDaemonTaskDefinitionLinuxParameters `json:"linuxParameters,omitempty" cfn:"LinuxParameters"`

	// The restart policy for the container. When you set up a restart policy, Amazon ECS can restart the container
	// without needing to replace the task.
	// +optional
	RestartPolicy *ECSDaemonTaskDefinitionRestartPolicy `json:"restartPolicy,omitempty" cfn:"RestartPolicy"`

	// When this parameter is ``true``, a TTY is allocated.
	// +optional
	PseudoTerminal *bool `json:"pseudoTerminal,omitempty" cfn:"PseudoTerminal"`

	// The mount points for data volumes in your container.
	// +optional
	MountPoints []ECSDaemonTaskDefinitionMountPoint `json:"mountPoints,omitempty" cfn:"MountPoints"`

	// The dependencies defined for container startup and shutdown. A container can contain multiple dependencies on
	// other containers in a task definition.
	// +optional
	DependsOn []ECSDaemonTaskDefinitionContainerDependency `json:"dependsOn,omitempty" cfn:"DependsOn"`

	// The command that's passed to the container.
	// +optional
	Command []string `json:"command,omitempty" cfn:"Command"`

	// The environment variables to pass to a container.
	// +optional
	Environment []ECSDaemonTaskDefinitionKeyValuePair `json:"environment,omitempty" cfn:"Environment"`
}

// ECSDaemonTaskDefinitionSecret is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionSecret struct {
	// The secret to expose to the container. The supported values are either the full ARN of the ASMlong secret or
	// the full ARN of the parameter in the SSM Parameter Store. For information about the require IAMlong
	// permissions, see [Required IAM permissions for Amazon ECS
	// secrets](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/specifying-sensitive-data-secrets.html#secrets-iam)
	// (for Secr...
	ValueFrom string `json:"valueFrom" cfn:"ValueFrom"`

	// The name of the secret.
	Name string `json:"name" cfn:"Name"`
}

// ECSDaemonTaskDefinitionHealthCheck is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionHealthCheck struct {
	// A string array representing the command that the container runs to determine if it is healthy. The string
	// array must start with ``CMD`` to run the command arguments directly, or ``CMD-SHELL`` to run the command with
	// the container's default shell. When you use the AWS Management Console JSON panel, the CLIlong, or the APIs,
	// enclose the list of commands in double quotes and brackets. ``[ "CMD-SHE...
	// +optional
	Command []string `json:"command,omitempty" cfn:"Command"`

	// The time period in seconds to wait for a health check to succeed before it is considered a failure. You may
	// specify between 2 and 60 seconds. The default value is 5. This value applies only when you specify a
	// ``command``.
	// +optional
	Timeout *int64 `json:"timeout,omitempty" cfn:"Timeout"`

	// The number of times to retry a failed health check before the container is considered unhealthy. You may
	// specify between 1 and 10 retries. The default value is 3. This value applies only when you specify a
	// ``command``.
	// +optional
	Retries *int64 `json:"retries,omitempty" cfn:"Retries"`

	// The time period in seconds between each health check execution. You may specify between 5 and 300 seconds. The
	// default value is 30 seconds. This value applies only when you specify a ``command``.
	// +optional
	Interval *int64 `json:"interval,omitempty" cfn:"Interval"`

	// The optional grace period to provide containers time to bootstrap before failed health checks count towards
	// the maximum number of retries. You can specify between 0 and 300 seconds. By default, the ``startPeriod`` is
	// off. This value applies only when you specify a ``command``. If a health check succeeds within the
	// ``startPeriod``, then the container is considered healthy and any subsequent fail...
	// +optional
	StartPeriod *int64 `json:"startPeriod,omitempty" cfn:"StartPeriod"`
}

// ECSDaemonTaskDefinitionLogConfiguration is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionLogConfiguration struct {
	// The secrets to pass to the log configuration. For more information, see [Specifying sensitive
	// data](https://docs.aws.amazon.com/AmazonECS/latest/developerguide/specifying-sensitive-data.html) in the
	// *Amazon Elastic Container Service Developer Guide*.
	// +optional
	SecretOptions []ECSDaemonTaskDefinitionSecret `json:"secretOptions,omitempty" cfn:"SecretOptions"`

	// The configuration options to send to the log driver. The options you can specify depend on the log driver.
	// Some of the options you can specify when you use the ``awslogs`` log driver to route logs to Amazon CloudWatch
	// include the following: + awslogs-create-group Required: No Specify whether you want the log group to be
	// created automatically. If this option isn't specified, it defaults to false...
	// +optional
	Options map[string]string `json:"options,omitempty" cfn:"Options"`

	// The log driver to use for the container. For tasks on FARGATElong, the supported log drivers are ``awslogs``,
	// ``splunk``, and ``awsfirelens``. For tasks hosted on Amazon EC2 instances, the supported log drivers are
	// ``awslogs``, ``fluentd``, ``gelf``, ``json-file``, ``journald``, ``syslog``, ``splunk``, and ``awsfirelens``.
	// For more information about using the ``awslogs`` log driver, see [Send A...
	LogDriver string `json:"logDriver" cfn:"LogDriver"`
}

// ECSDaemonTaskDefinitionEnvironmentFile is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionEnvironmentFile struct {
	// The file type to use. Environment files are objects in Amazon S3. The only supported value is ``s3``.
	// +optional
	Type string `json:"type,omitempty" cfn:"Type"`

	// The Amazon Resource Name (ARN) of the Amazon S3 object containing the environment variable file.
	// +optional
	Value string `json:"value,omitempty" cfn:"Value"`
}

// ECSDaemonTaskDefinitionFirelensConfiguration is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionFirelensConfiguration struct {
	// The options to use when configuring the log router. This field is optional and can be used to specify a custom
	// configuration file or to add additional metadata, such as the task, task definition, cluster, and container
	// instance details to the log event. If specified, the syntax to use is
	// ``"options":{"enable-ecs-log-metadata":"true|false","config-file-type:"s3|file","config-file-value":"arn:aws...
	// +optional
	Options map[string]string `json:"options,omitempty" cfn:"Options"`

	// The log router to use. The valid values are ``fluentd`` or ``fluentbit``.
	// +optional
	Type string `json:"type,omitempty" cfn:"Type"`
}

// ECSDaemonTaskDefinitionSystemControl is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionSystemControl struct {
	// The namespaced kernel parameter to set a ``value`` for. Valid IPC namespace values: ``"kernel.msgmax" |
	// "kernel.msgmnb" | "kernel.msgmni" | "kernel.sem" | "kernel.shmall" | "kernel.shmmax" | "kernel.shmmni" |
	// "kernel.shm_rmid_forced"``, and ``Sysctls`` that start with ``"fs.mqueue.*"`` Valid network namespace values:
	// ``Sysctls`` that start with ``"net.*"``. Only namespaced ``Sysctls`` that exis...
	// +optional
	Value string `json:"value,omitempty" cfn:"Value"`

	// The namespaced kernel parameter to set a ``value`` for.
	// +optional
	Namespace string `json:"namespace,omitempty" cfn:"Namespace"`
}

// ECSDaemonTaskDefinitionUlimit is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionUlimit struct {
	// The soft limit for the ``ulimit`` type. The value can be specified in bytes, seconds, or as a count, depending
	// on the ``type`` of the ``ulimit``.
	SoftLimit int64 `json:"softLimit" cfn:"SoftLimit"`

	// The hard limit for the ``ulimit`` type. The value can be specified in bytes, seconds, or as a count, depending
	// on the ``type`` of the ``ulimit``.
	HardLimit int64 `json:"hardLimit" cfn:"HardLimit"`

	// The ``type`` of the ``ulimit``.
	Name string `json:"name" cfn:"Name"`
}

// ECSDaemonTaskDefinitionRepositoryCredentials is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionRepositoryCredentials struct {
	// The Amazon Resource Name (ARN) of the secret containing the private repository credentials. When you use the
	// Amazon ECS API, CLI, or AWS SDK, if the secret exists in the same Region as the task that you're launching
	// then you can use either the full ARN or the name of the secret. When you use the AWS Management Console, you
	// must specify the full ARN of the secret.
	// +optional
	CredentialsParameter string `json:"credentialsParameter,omitempty" cfn:"CredentialsParameter"`
}

// ECSDaemonTaskDefinitionLinuxParameters is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionLinuxParameters struct {
	// The Linux capabilities for the container that are added to or dropped from the default configuration provided
	// by Docker. For tasks that use the Fargate launch type, ``capabilities`` is supported for all platform versions
	// but the ``add`` parameter is only supported if using platform version 1.4.0 or later.
	// +optional
	Capabilities *ECSDaemonTaskDefinitionKernelCapabilities `json:"capabilities,omitempty" cfn:"Capabilities"`

	// The container path, mount options, and size (in MiB) of the tmpfs mount. This parameter maps to the
	// ``--tmpfs`` option to docker run.
	// +optional
	Tmpfs []ECSDaemonTaskDefinitionTmpfs `json:"tmpfs,omitempty" cfn:"Tmpfs"`

	// Any host devices to expose to the container. This parameter maps to ``Devices`` in the docker container create
	// command and the ``--device`` option to docker run. If you're using tasks that use the Fargate launch type, the
	// ``devices`` parameter isn't supported.
	// +optional
	Devices []ECSDaemonTaskDefinitionDevice `json:"devices,omitempty" cfn:"Devices"`

	// Run an ``init`` process inside the container that forwards signals and reaps processes. This parameter maps to
	// the ``--init`` option to docker run. This parameter requires version 1.25 of the Docker Remote API or greater
	// on your container instance. To check the Docker Remote API version on your container instance, log in to your
	// container instance and run the following command: ``sudo docker ve...
	// +optional
	InitProcessEnabled *bool `json:"initProcessEnabled,omitempty" cfn:"InitProcessEnabled"`
}

// ECSDaemonTaskDefinitionKernelCapabilities is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionKernelCapabilities struct {
	// The Linux capabilities for the container that have been added to the default configuration provided by Docker.
	// This parameter maps to ``CapAdd`` in the docker container create command and the ``--cap-add`` option to
	// docker run. Tasks launched on FARGATElong only support adding the ``SYS_PTRACE`` kernel capability. Valid
	// values: ``"ALL" | "AUDIT_CONTROL" | "AUDIT_WRITE" | "BLOCK_SUSPEND" | "CHOW...
	// +optional
	Add []string `json:"add,omitempty" cfn:"Add"`

	// The Linux capabilities for the container that have been removed from the default configuration provided by
	// Docker. This parameter maps to ``CapDrop`` in the docker container create command and the ``--cap-drop``
	// option to docker run. Valid values: ``"ALL" | "AUDIT_CONTROL" | "AUDIT_WRITE" | "BLOCK_SUSPEND" | "CHOWN" |
	// "DAC_OVERRIDE" | "DAC_READ_SEARCH" | "FOWNER" | "FSETID" | "IPC_LOCK" | "IPC_...
	// +optional
	Drop []string `json:"drop,omitempty" cfn:"Drop"`
}

// ECSDaemonTaskDefinitionTmpfs is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionTmpfs struct {
	// The maximum size (in MiB) of the tmpfs volume.
	Size int64 `json:"size" cfn:"Size"`

	// The absolute file path where the tmpfs volume is to be mounted.
	// +optional
	ContainerPath string `json:"containerPath,omitempty" cfn:"ContainerPath"`

	// The list of tmpfs volume mount options. Valid values: ``"defaults" | "ro" | "rw" | "suid" | "nosuid" | "dev" |
	// "nodev" | "exec" | "noexec" | "sync" | "async" | "dirsync" | "remount" | "mand" | "nomand" | "atime" |
	// "noatime" | "diratime" | "nodiratime" | "bind" | "rbind" | "unbindable" | "runbindable" | "private" |
	// "rprivate" | "shared" | "rshared" | "slave" | "rslave" | "relatime" | "norelatime...
	// +optional
	MountOptions []string `json:"mountOptions,omitempty" cfn:"MountOptions"`
}

// ECSDaemonTaskDefinitionDevice is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionDevice struct {
	// The path for the device on the host container instance.
	// +optional
	HostPath string `json:"hostPath,omitempty" cfn:"HostPath"`

	// The explicit permissions to provide to the container for the device. By default, the container has permissions
	// for ``read``, ``write``, and ``mknod`` for the device.
	// +optional
	Permissions []string `json:"permissions,omitempty" cfn:"Permissions"`

	// The path inside the container at which to expose the host device.
	// +optional
	ContainerPath string `json:"containerPath,omitempty" cfn:"ContainerPath"`
}

// ECSDaemonTaskDefinitionRestartPolicy is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionRestartPolicy struct {
	// +optional
	IgnoredExitCodes []int64 `json:"ignoredExitCodes,omitempty" cfn:"IgnoredExitCodes"`

	// +optional
	RestartAttemptPeriod *int64 `json:"restartAttemptPeriod,omitempty" cfn:"RestartAttemptPeriod"`

	// +optional
	Enabled *bool `json:"enabled,omitempty" cfn:"Enabled"`
}

// ECSDaemonTaskDefinitionMountPoint is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionMountPoint struct {
	// If this value is ``true``, the container has read-only access to the volume. If this value is ``false``, then
	// the container can write to the volume. The default value is ``false``.
	// +optional
	ReadOnly *bool `json:"readOnly,omitempty" cfn:"ReadOnly"`

	// The name of the volume to mount. Must be a volume name referenced in the ``name`` parameter of task definition
	// ``volume``.
	// +optional
	SourceVolume string `json:"sourceVolume,omitempty" cfn:"SourceVolume"`

	// The path on the container to mount the host volume at.
	// +optional
	ContainerPath string `json:"containerPath,omitempty" cfn:"ContainerPath"`
}

// ECSDaemonTaskDefinitionContainerDependency is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionContainerDependency struct {
	// The dependency condition of the container. The following are the available conditions and their behavior: +
	// ``START`` - This condition emulates the behavior of links and volumes today. It validates that a dependent
	// container is started before permitting other containers to start. + ``COMPLETE`` - This condition validates
	// that a dependent container runs to completion (exits) before permitting ot...
	// +optional
	Condition string `json:"condition,omitempty" cfn:"Condition"`

	// The name of a container.
	// +optional
	ContainerName string `json:"containerName,omitempty" cfn:"ContainerName"`
}

// ECSDaemonTaskDefinitionKeyValuePair is a nested property type of AWS::ECS::DaemonTaskDefinition.
type ECSDaemonTaskDefinitionKeyValuePair struct {
	// The value of the key-value pair. For environment variables, this is the value of the environment variable.
	// +optional
	Value string `json:"value,omitempty" cfn:"Value"`

	// The name of the key-value pair. For environment variables, this is the name of the environment variable.
	// +optional
	Name string `json:"name,omitempty" cfn:"Name"`
}

// ECSExpressGatewayServiceSpec is the desired state of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceSpec struct {
	// ProviderRef selects the AWSProvider (account/region) this resource is
	// reconciled against. Defaults to the namespace annotation, then the
	// default provider, then the operator's own credentials.
	// +optional
	ProviderRef *ProviderRef `json:"providerRef,omitempty"`

	// +optional
	TaskRoleArn string `json:"taskRoleArn,omitempty" cfn:"TaskRoleArn"`

	// +optional
	PrimaryContainer *ECSExpressGatewayServiceExpressGatewayContainer `json:"primaryContainer,omitempty" cfn:"PrimaryContainer"`

	// +optional
	Memory string `json:"memory,omitempty" cfn:"Memory"`

	// +optional
	HealthCheckPath string `json:"healthCheckPath,omitempty" cfn:"HealthCheckPath"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	Cluster string `json:"cluster,omitempty" cfn:"Cluster"`

	// +optional
	Cpu string `json:"cpu,omitempty" cfn:"Cpu"`

	// +optional
	ExecutionRoleArn string `json:"executionRoleArn,omitempty" cfn:"ExecutionRoleArn"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="infrastructureRoleArn is immutable"
	InfrastructureRoleArn string `json:"infrastructureRoleArn" cfn:"InfrastructureRoleArn"`

	// +optional
	ScalingTarget *ECSExpressGatewayServiceExpressGatewayScalingTarget `json:"scalingTarget,omitempty" cfn:"ScalingTarget"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	ServiceName string `json:"serviceName,omitempty" cfn:"ServiceName"`

	// +optional
	NetworkConfiguration *ECSExpressGatewayServiceExpressGatewayServiceNetworkConfiguration `json:"networkConfiguration,omitempty" cfn:"NetworkConfiguration"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	Tags []CFNTag `json:"tags,omitempty" cfn:"Tags"`

	// +optional
	TaskDefinitionArn string `json:"taskDefinitionArn,omitempty" cfn:"TaskDefinitionArn"`
}

// ECSExpressGatewayServiceStatus is the observed state of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceStatus struct {
	CloudControlStatus `json:",inline"`

	// Read-only: reported by AWS after creation.
	// +optional
	Status *ECSExpressGatewayServiceExpressGatewayServiceStatus `json:"status,omitempty" cfn:"Status"`

	// Read-only: reported by AWS after creation.
	// +optional
	CreatedAt string `json:"createdAt,omitempty" cfn:"CreatedAt"`

	// Read-only: reported by AWS after creation.
	// +optional
	ServiceArn string `json:"serviceArn,omitempty" cfn:"ServiceArn"`

	// Read-only: reported by AWS after creation.
	// +optional
	ECSManagedResourceArns *ECSExpressGatewayServiceECSManagedResourceArns `json:"ecsManagedResourceArns,omitempty" cfn:"ECSManagedResourceArns"`

	// Read-only: reported by AWS after creation.
	// +optional
	UpdatedAt string `json:"updatedAt,omitempty" cfn:"UpdatedAt"`

	// Read-only: reported by AWS after creation.
	// +optional
	ActiveConfigurations []ECSExpressGatewayServiceExpressGatewayServiceConfiguration `json:"activeConfigurations,omitempty" cfn:"ActiveConfigurations"`

	// Read-only: reported by AWS after creation.
	// +optional
	Endpoint string `json:"endpoint,omitempty" cfn:"Endpoint"`
}

// ECSExpressGatewayService manages AWS::ECS::ExpressGatewayService through the AWS Cloud Control API. Resource
// Type definition for AWS::ECS::ExpressGatewayService
// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Identifier",type="string",JSONPath=".status.identifier"
// +kubebuilder:printcolumn:name="Ready",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp"
type ECSExpressGatewayService struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   ECSExpressGatewayServiceSpec   `json:"spec,omitempty"`
	Status ECSExpressGatewayServiceStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ECSExpressGatewayServiceList contains a list of ECSExpressGatewayService.
type ECSExpressGatewayServiceList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ECSExpressGatewayService `json:"items"`
}

func (o *ECSExpressGatewayService) GetProviderRef() *ProviderRef { return o.Spec.ProviderRef }
func (o *ECSExpressGatewayService) CloudControlTypeName() string {
	return "AWS::ECS::ExpressGatewayService"
}
func (o *ECSExpressGatewayService) CloudControlSpec() interface{} { return &o.Spec }
func (o *ECSExpressGatewayService) CloudControlStatusRef() *CloudControlStatus {
	return &o.Status.CloudControlStatus
}
func (o *ECSExpressGatewayService) CloudControlObserved() interface{}   { return &o.Status }
func (o *ECSExpressGatewayService) SetProviderStatus(p *ProviderStatus) { o.Status.AWSProvider = p }

// ECSExpressGatewayServiceExpressGatewayServiceStatus is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayServiceStatus struct {
	// +kubebuilder:validation:Enum=ACTIVE;DRAINING;INACTIVE
	// +optional
	StatusCode string `json:"statusCode,omitempty" cfn:"StatusCode"`
}

// ECSExpressGatewayServiceExpressGatewayContainer is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayContainer struct {
	// +optional
	RepositoryCredentials *ECSExpressGatewayServiceExpressGatewayRepositoryCredentials `json:"repositoryCredentials,omitempty" cfn:"RepositoryCredentials"`

	// +optional
	Secrets []ECSExpressGatewayServiceSecret `json:"secrets,omitempty" cfn:"Secrets"`

	// +optional
	Command []string `json:"command,omitempty" cfn:"Command"`

	// +optional
	AwsLogsConfiguration *ECSExpressGatewayServiceExpressGatewayServiceAwsLogsConfiguration `json:"awsLogsConfiguration,omitempty" cfn:"AwsLogsConfiguration"`

	// +optional
	ContainerPort *int64 `json:"containerPort,omitempty" cfn:"ContainerPort"`

	// +optional
	Environment []ECSExpressGatewayServiceKeyValuePair `json:"environment,omitempty" cfn:"Environment"`

	Image string `json:"image" cfn:"Image"`
}

// ECSExpressGatewayServiceExpressGatewayRepositoryCredentials is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayRepositoryCredentials struct {
	CredentialsParameter string `json:"credentialsParameter" cfn:"CredentialsParameter"`
}

// ECSExpressGatewayServiceSecret is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceSecret struct {
	ValueFrom string `json:"valueFrom" cfn:"ValueFrom"`

	Name string `json:"name" cfn:"Name"`
}

// ECSExpressGatewayServiceExpressGatewayServiceAwsLogsConfiguration is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayServiceAwsLogsConfiguration struct {
	LogStreamPrefix string `json:"logStreamPrefix" cfn:"LogStreamPrefix"`

	LogGroup string `json:"logGroup" cfn:"LogGroup"`
}

// ECSExpressGatewayServiceKeyValuePair is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceKeyValuePair struct {
	Value string `json:"value" cfn:"Value"`

	Name string `json:"name" cfn:"Name"`
}

// ECSExpressGatewayServiceECSManagedResourceArns is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceECSManagedResourceArns struct {
	// +optional
	LogGroups []string `json:"logGroups,omitempty" cfn:"LogGroups"`

	// +optional
	ServiceSecurityGroups []string `json:"serviceSecurityGroups,omitempty" cfn:"ServiceSecurityGroups"`

	// +optional
	MetricAlarms []string `json:"metricAlarms,omitempty" cfn:"MetricAlarms"`

	// +optional
	IngressPath *ECSExpressGatewayServiceIngressPathArns `json:"ingressPath,omitempty" cfn:"IngressPath"`

	// +optional
	AutoScaling *ECSExpressGatewayServiceAutoScalingArns `json:"autoScaling,omitempty" cfn:"AutoScaling"`
}

// ECSExpressGatewayServiceIngressPathArns is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceIngressPathArns struct {
	// +optional
	ListenerArn string `json:"listenerArn,omitempty" cfn:"ListenerArn"`

	// +optional
	LoadBalancerArn string `json:"loadBalancerArn,omitempty" cfn:"LoadBalancerArn"`

	// +optional
	TargetGroupArns []string `json:"targetGroupArns,omitempty" cfn:"TargetGroupArns"`

	// +optional
	ListenerRuleArn string `json:"listenerRuleArn,omitempty" cfn:"ListenerRuleArn"`

	// +optional
	LoadBalancerSecurityGroups []string `json:"loadBalancerSecurityGroups,omitempty" cfn:"LoadBalancerSecurityGroups"`

	// +optional
	CertificateArn string `json:"certificateArn,omitempty" cfn:"CertificateArn"`
}

// ECSExpressGatewayServiceAutoScalingArns is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceAutoScalingArns struct {
	// +optional
	ScalableTarget string `json:"scalableTarget,omitempty" cfn:"ScalableTarget"`

	// +optional
	ApplicationAutoScalingPolicies []string `json:"applicationAutoScalingPolicies,omitempty" cfn:"ApplicationAutoScalingPolicies"`
}

// ECSExpressGatewayServiceExpressGatewayScalingTarget is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayScalingTarget struct {
	// +optional
	MinTaskCount *int64 `json:"minTaskCount,omitempty" cfn:"MinTaskCount"`

	// +optional
	MaxTaskCount *int64 `json:"maxTaskCount,omitempty" cfn:"MaxTaskCount"`

	// +kubebuilder:validation:Enum=AVERAGE_CPU;AVERAGE_MEMORY;REQUEST_COUNT_PER_TARGET
	// +optional
	AutoScalingMetric string `json:"autoScalingMetric,omitempty" cfn:"AutoScalingMetric"`

	// +optional
	AutoScalingTargetValue *int64 `json:"autoScalingTargetValue,omitempty" cfn:"AutoScalingTargetValue"`
}

// ECSExpressGatewayServiceExpressGatewayServiceConfiguration is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayServiceConfiguration struct {
	// +optional
	ServiceRevisionArn string `json:"serviceRevisionArn,omitempty" cfn:"ServiceRevisionArn"`

	// +optional
	ExecutionRoleArn string `json:"executionRoleArn,omitempty" cfn:"ExecutionRoleArn"`

	// +optional
	TaskRoleArn string `json:"taskRoleArn,omitempty" cfn:"TaskRoleArn"`

	// +optional
	ScalingTarget *ECSExpressGatewayServiceExpressGatewayScalingTarget `json:"scalingTarget,omitempty" cfn:"ScalingTarget"`

	// +optional
	IngressPaths []ECSExpressGatewayServiceIngressPathSummary `json:"ingressPaths,omitempty" cfn:"IngressPaths"`

	// +optional
	PrimaryContainer *ECSExpressGatewayServiceExpressGatewayContainer `json:"primaryContainer,omitempty" cfn:"PrimaryContainer"`

	// +optional
	Memory string `json:"memory,omitempty" cfn:"Memory"`

	// +optional
	HealthCheckPath string `json:"healthCheckPath,omitempty" cfn:"HealthCheckPath"`

	// +optional
	CreatedAt string `json:"createdAt,omitempty" cfn:"CreatedAt"`

	// +optional
	Cpu string `json:"cpu,omitempty" cfn:"Cpu"`

	// +optional
	NetworkConfiguration *ECSExpressGatewayServiceExpressGatewayServiceNetworkConfiguration `json:"networkConfiguration,omitempty" cfn:"NetworkConfiguration"`

	// +optional
	TaskDefinitionArn string `json:"taskDefinitionArn,omitempty" cfn:"TaskDefinitionArn"`
}

// ECSExpressGatewayServiceIngressPathSummary is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceIngressPathSummary struct {
	// +optional
	Endpoint string `json:"endpoint,omitempty" cfn:"Endpoint"`

	// +kubebuilder:validation:Enum=PUBLIC;PRIVATE
	// +optional
	AccessType string `json:"accessType,omitempty" cfn:"AccessType"`
}

// ECSExpressGatewayServiceExpressGatewayServiceNetworkConfiguration is a nested property type of AWS::ECS::ExpressGatewayService.
type ECSExpressGatewayServiceExpressGatewayServiceNetworkConfiguration struct {
	// +optional
	SecurityGroups []string `json:"securityGroups,omitempty" cfn:"SecurityGroups"`

	// +optional
	Subnets []string `json:"subnets,omitempty" cfn:"Subnets"`
}

// ECSPrimaryTaskSetSpec is the desired state of AWS::ECS::PrimaryTaskSet.
type ECSPrimaryTaskSetSpec struct {
	// ProviderRef selects the AWSProvider (account/region) this resource is
	// reconciled against. Defaults to the namespace annotation, then the
	// default provider, then the operator's own credentials.
	// +optional
	ProviderRef *ProviderRef `json:"providerRef,omitempty"`

	// The ID or full Amazon Resource Name (ARN) of the task set.
	TaskSetId string `json:"taskSetId" cfn:"TaskSetId"`

	// The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to create the task set
	// in.
	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="cluster is immutable"
	Cluster string `json:"cluster" cfn:"Cluster"`

	// The short name or full Amazon Resource Name (ARN) of the service to create the task set in.
	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="service is immutable"
	Service string `json:"service" cfn:"Service"`
}

// ECSPrimaryTaskSetStatus is the observed state of AWS::ECS::PrimaryTaskSet.
type ECSPrimaryTaskSetStatus struct {
	CloudControlStatus `json:",inline"`
}

// ECSPrimaryTaskSet manages AWS::ECS::PrimaryTaskSet through the AWS Cloud Control API. A pseudo-resource that
// manages which of your ECS task sets is primary.
// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Identifier",type="string",JSONPath=".status.identifier"
// +kubebuilder:printcolumn:name="Ready",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp"
type ECSPrimaryTaskSet struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   ECSPrimaryTaskSetSpec   `json:"spec,omitempty"`
	Status ECSPrimaryTaskSetStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ECSPrimaryTaskSetList contains a list of ECSPrimaryTaskSet.
type ECSPrimaryTaskSetList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ECSPrimaryTaskSet `json:"items"`
}

func (o *ECSPrimaryTaskSet) GetProviderRef() *ProviderRef  { return o.Spec.ProviderRef }
func (o *ECSPrimaryTaskSet) CloudControlTypeName() string  { return "AWS::ECS::PrimaryTaskSet" }
func (o *ECSPrimaryTaskSet) CloudControlSpec() interface{} { return &o.Spec }
func (o *ECSPrimaryTaskSet) CloudControlStatusRef() *CloudControlStatus {
	return &o.Status.CloudControlStatus
}
func (o *ECSPrimaryTaskSet) CloudControlObserved() interface{}   { return &o.Status }
func (o *ECSPrimaryTaskSet) SetProviderStatus(p *ProviderStatus) { o.Status.AWSProvider = p }

// ECSTaskSetSpec is the desired state of AWS::ECS::TaskSet.
type ECSTaskSetSpec struct {
	// ProviderRef selects the AWSProvider (account/region) this resource is
	// reconciled against. Defaults to the namespace annotation, then the
	// default provider, then the operator's own credentials.
	// +optional
	ProviderRef *ProviderRef `json:"providerRef,omitempty"`

	// The platform version that the tasks in the task set should use. A platform version is specified only for tasks
	// using the Fargate launch type. If one isn't specified, the LATEST platform version is used by default.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	PlatformVersion string `json:"platformVersion,omitempty" cfn:"PlatformVersion"`

	// An optional non-unique tag that identifies this task set in external systems. If the task set is associated
	// with a service discovery registry, the tasks in this task set will have the ECS_TASK_SET_EXTERNAL_ID AWS Cloud
	// Map attribute set to the provided value.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	ExternalId string `json:"externalId,omitempty" cfn:"ExternalId"`

	// The short name or full Amazon Resource Name (ARN) of the cluster that hosts the service to create the task set
	// in.
	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="cluster is immutable"
	Cluster string `json:"cluster" cfn:"Cluster"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	LoadBalancers []ECSTaskSetLoadBalancer `json:"loadBalancers,omitempty" cfn:"LoadBalancers"`

	// The short name or full Amazon Resource Name (ARN) of the service to create the task set in.
	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="service is immutable"
	Service string `json:"service" cfn:"Service"`

	// A floating-point percentage of the desired number of tasks to place and keep running in the task set.
	// +optional
	Scale *ECSTaskSetScale `json:"scale,omitempty" cfn:"Scale"`

	// The details of the service discovery registries to assign to this task set. For more information, see
	// https://docs.aws.amazon.com/AmazonECS/latest/developerguide/service-discovery.html.
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	ServiceRegistries []ECSTaskSetServiceRegistry `json:"serviceRegistries,omitempty" cfn:"ServiceRegistries"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	CapacityProviderStrategy []ECSTaskSetCapacityProviderStrategyItem `json:"capacityProviderStrategy,omitempty" cfn:"CapacityProviderStrategy"`

	// The launch type that new tasks in the task set will use. For more information, see
	// https://docs.aws.amazon.com/AmazonECS/latest/developerguide/launch_types.html in the Amazon Elastic Container
	// Service Developer Guide.
	// +kubebuilder:validation:Enum=EC2;FARGATE
	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	LaunchType string `json:"launchType,omitempty" cfn:"LaunchType"`

	// The short name or full Amazon Resource Name (ARN) of the task definition for the tasks in the task set to use.
	// Create-only in AWS: changing it requires replacing the resource.
	// +kubebuilder:validation:XValidation:rule="self == oldSelf",message="taskDefinition is immutable"
	TaskDefinition string `json:"taskDefinition" cfn:"TaskDefinition"`

	// Create-only in AWS: changing it requires replacing the resource.
	// +optional
	NetworkConfiguration *ECSTaskSetNetworkConfiguration `json:"networkConfiguration,omitempty" cfn:"NetworkConfiguration"`

	// +optional
	Tags []CFNTag `json:"tags,omitempty" cfn:"Tags"`
}

// ECSTaskSetStatus is the observed state of AWS::ECS::TaskSet.
type ECSTaskSetStatus struct {
	CloudControlStatus `json:",inline"`

	// The ID of the task set.
	// Read-only: reported by AWS after creation.
	// +optional
	Id string `json:"id,omitempty" cfn:"Id"`
}

// ECSTaskSet manages AWS::ECS::TaskSet through the AWS Cloud Control API. Create a task set in the specified
// cluster and service. This is used when a service uses the EXTERNAL deployment controller type. For more
// information, see https://docs.aws.amazon.com/AmazonECS/latest/developerguide/deployment-types.htmlin the
// Amazon Elastic Container Service Developer Guide.
// +kubebuilder:object:root=true
// +kubebuilder:subresource:status
// +kubebuilder:printcolumn:name="Identifier",type="string",JSONPath=".status.identifier"
// +kubebuilder:printcolumn:name="Ready",type="string",JSONPath=".status.conditions[?(@.type=='Ready')].status"
// +kubebuilder:printcolumn:name="Age",type="date",JSONPath=".metadata.creationTimestamp"
type ECSTaskSet struct {
	metav1.TypeMeta   `json:",inline"`
	metav1.ObjectMeta `json:"metadata,omitempty"`

	Spec   ECSTaskSetSpec   `json:"spec,omitempty"`
	Status ECSTaskSetStatus `json:"status,omitempty"`
}

// +kubebuilder:object:root=true

// ECSTaskSetList contains a list of ECSTaskSet.
type ECSTaskSetList struct {
	metav1.TypeMeta `json:",inline"`
	metav1.ListMeta `json:"metadata,omitempty"`
	Items           []ECSTaskSet `json:"items"`
}

func (o *ECSTaskSet) GetProviderRef() *ProviderRef               { return o.Spec.ProviderRef }
func (o *ECSTaskSet) CloudControlTypeName() string               { return "AWS::ECS::TaskSet" }
func (o *ECSTaskSet) CloudControlSpec() interface{}              { return &o.Spec }
func (o *ECSTaskSet) CloudControlStatusRef() *CloudControlStatus { return &o.Status.CloudControlStatus }
func (o *ECSTaskSet) CloudControlObserved() interface{}          { return &o.Status }
func (o *ECSTaskSet) SetProviderStatus(p *ProviderStatus)        { o.Status.AWSProvider = p }

// ECSTaskSetLoadBalancer is a nested property type of AWS::ECS::TaskSet.
type ECSTaskSetLoadBalancer struct {
	// The full Amazon Resource Name (ARN) of the Elastic Load Balancing target group or groups associated with a
	// service or task set. A target group ARN is only specified when using an Application Load Balancer or Network
	// Load Balancer. If you are using a Classic Load Balancer this should be omitted. For services using the ECS
	// deployment controller, you can specify one or multiple target groups. For ...
	// +optional
	TargetGroupArn string `json:"targetGroupArn,omitempty" cfn:"TargetGroupArn"`

	// The name of the container (as it appears in a container definition) to associate with the load balancer.
	// +optional
	ContainerName string `json:"containerName,omitempty" cfn:"ContainerName"`

	// The port on the container to associate with the load balancer. This port must correspond to a containerPort in
	// the task definition the tasks in the service are using. For tasks that use the EC2 launch type, the container
	// instance they are launched on must allow ingress traffic on the hostPort of the port mapping.
	// +optional
	ContainerPort *int64 `json:"containerPort,omitempty" cfn:"ContainerPort"`
}

// ECSTaskSetScale is a nested property type of AWS::ECS::TaskSet.
type ECSTaskSetScale struct {
	// The value, specified as a percent total of a service's desiredCount, to scale the task set. Accepted values
	// are numbers between 0 and 100.
	// +optional
	Value *float64 `json:"value,omitempty" cfn:"Value"`

	// The unit of measure for the scale value.
	// +kubebuilder:validation:Enum=PERCENT
	// +optional
	Unit string `json:"unit,omitempty" cfn:"Unit"`
}

// ECSTaskSetServiceRegistry is a nested property type of AWS::ECS::TaskSet.
type ECSTaskSetServiceRegistry struct {
	// The container name value, already specified in the task definition, to be used for your service discovery
	// service. If the task definition that your service task specifies uses the bridge or host network mode, you
	// must specify a containerName and containerPort combination from the task definition. If the task definition
	// that your service task specifies uses the awsvpc network mode and a type SRV...
	// +optional
	ContainerName string `json:"containerName,omitempty" cfn:"ContainerName"`

	// The port value used if your service discovery service specified an SRV record. This field may be used if both
	// the awsvpc network mode and SRV records are used.
	// +optional
	Port *int64 `json:"port,omitempty" cfn:"Port"`

	// The port value, already specified in the task definition, to be used for your service discovery service. If
	// the task definition your service task specifies uses the bridge or host network mode, you must specify a
	// containerName and containerPort combination from the task definition. If the task definition your service task
	// specifies uses the awsvpc network mode and a type SRV DNS record is used,...
	// +optional
	ContainerPort *int64 `json:"containerPort,omitempty" cfn:"ContainerPort"`

	// The Amazon Resource Name (ARN) of the service registry. The currently supported service registry is AWS Cloud
	// Map. For more information, see https://docs.aws.amazon.com/cloud-map/latest/api/API_CreateService.html
	// +optional
	RegistryArn string `json:"registryArn,omitempty" cfn:"RegistryArn"`
}

// ECSTaskSetCapacityProviderStrategyItem is a nested property type of AWS::ECS::TaskSet.
type ECSTaskSetCapacityProviderStrategyItem struct {
	// +optional
	CapacityProvider string `json:"capacityProvider,omitempty" cfn:"CapacityProvider"`

	// +optional
	Base *int64 `json:"base,omitempty" cfn:"Base"`

	// +optional
	Weight *int64 `json:"weight,omitempty" cfn:"Weight"`
}

// ECSTaskSetNetworkConfiguration is a nested property type of AWS::ECS::TaskSet.
type ECSTaskSetNetworkConfiguration struct {
	// +optional
	AwsVpcConfiguration *ECSTaskSetAwsVpcConfiguration `json:"awsVpcConfiguration,omitempty" cfn:"AwsVpcConfiguration"`
}

// ECSTaskSetAwsVpcConfiguration is a nested property type of AWS::ECS::TaskSet.
type ECSTaskSetAwsVpcConfiguration struct {
	// The security groups associated with the task or service. If you do not specify a security group, the default
	// security group for the VPC is used. There is a limit of 5 security groups that can be specified per
	// AwsVpcConfiguration.
	// +optional
	SecurityGroups []string `json:"securityGroups,omitempty" cfn:"SecurityGroups"`

	// The subnets associated with the task or service. There is a limit of 16 subnets that can be specified per
	// AwsVpcConfiguration.
	Subnets []string `json:"subnets" cfn:"Subnets"`

	// Whether the task's elastic network interface receives a public IP address. The default value is DISABLED.
	// +kubebuilder:validation:Enum=DISABLED;ENABLED
	// +optional
	AssignPublicIp string `json:"assignPublicIp,omitempty" cfn:"AssignPublicIp"`
}

func init() {
	SchemeBuilder.Register(&ECSClusterCapacityProviderAssociations{}, &ECSClusterCapacityProviderAssociationsList{})
	SchemeBuilder.Register(&ECSDaemon{}, &ECSDaemonList{})
	SchemeBuilder.Register(&ECSDaemonTaskDefinition{}, &ECSDaemonTaskDefinitionList{})
	SchemeBuilder.Register(&ECSExpressGatewayService{}, &ECSExpressGatewayServiceList{})
	SchemeBuilder.Register(&ECSPrimaryTaskSet{}, &ECSPrimaryTaskSetList{})
	SchemeBuilder.Register(&ECSTaskSet{}, &ECSTaskSetList{})
}
