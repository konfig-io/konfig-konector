# OpenSearchServerlessIndex — OpenSearch Serverless
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/opensearchserverless.html#opensearchserverlessindex
apiVersion: aws.konfig.io/v1alpha1
kind: OpenSearchServerlessIndex
metadata:
  name: example-opensearchserverlessindex
  namespace: default
spec:
  collectionEndpoint: example
  indexName: example-opensearchserverlessindex
  mappings:
    properties: {}
  providerRef:
    name: example-opensearchserverlessindex
    region: us-east-1
  settings:
    analysis:
      analyzer: {}
    index:
      knn: false
      knnAlgoParamEfSearch: 1
      refreshInterval: example
