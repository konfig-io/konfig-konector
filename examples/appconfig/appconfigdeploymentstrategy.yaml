# AppConfigDeploymentStrategy — AppConfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigdeploymentstrategy
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigDeploymentStrategy
metadata:
  name: example-appconfigdeploymentstrategy
  namespace: default
spec:
  deploymentDurationInMinutes: 1
  description: Managed by konfig-konector
  finalBakeTimeInMinutes: 1
  growthFactor: 1
  growthType: EXPONENTIAL
  name: example-appconfigdeploymentstrategy
  providerRef:
    name: example-appconfigdeploymentstrategy
    region: us-east-1
  replicateTo: NONE
  tags:
  - key: example
    value: example
