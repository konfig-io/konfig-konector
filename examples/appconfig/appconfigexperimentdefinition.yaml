# AppConfigExperimentDefinition — appconfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigexperimentdefinition
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigExperimentDefinition
metadata:
  name: example-appconfigexperimentdefinition
  namespace: default
spec:
  applicationIdentifier: example
  audienceDescription: Managed by konfig-konector
  audienceRule: example
  configurationProfileIdentifier: example
  control:
    attributeValues: {}
    description: Managed by konfig-konector
    enabled: true
    key: example
    weight: 1
  environmentIdentifier: example
  flagKey: example
  hypothesis: example
  launchCriteria: example
  name: example-appconfigexperimentdefinition
  providerRef:
    name: example-appconfigexperimentdefinition
    region: us-east-1
  tags:
  - key: example
    value: example
  treatments:
  - attributeValues: {}
    description: Managed by konfig-konector
    enabled: true
    key: example
    weight: 1
