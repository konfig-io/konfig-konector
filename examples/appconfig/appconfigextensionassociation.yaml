# AppConfigExtensionAssociation — AppConfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigextensionassociation
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigExtensionAssociation
metadata:
  name: example-appconfigextensionassociation
  namespace: default
spec:
  extensionIdentifier: example
  extensionVersionNumber: 1
  parameters:
    key: value
  providerRef:
    name: example-appconfigextensionassociation
    region: us-east-1
  resourceIdentifier: example
  tags:
  - key: example
    value: example
