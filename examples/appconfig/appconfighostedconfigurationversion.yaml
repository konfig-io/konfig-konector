# AppConfigHostedConfigurationVersion — AppConfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfighostedconfigurationversion
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigHostedConfigurationVersion
metadata:
  name: example-appconfighostedconfigurationversion
  namespace: default
spec:
  applicationId: example-id
  configurationProfileId: example-id
  content: example
  contentType: example
  description: Managed by konfig-konector
  latestVersionNumber: 1
  providerRef:
    name: example-appconfighostedconfigurationversion
    region: us-east-1
  versionLabel: example
