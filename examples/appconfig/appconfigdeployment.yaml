# AppConfigDeployment — AppConfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigdeployment
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigDeployment
metadata:
  name: example-appconfigdeployment
  namespace: default
spec:
  applicationId: example-id
  configurationProfileId: example-id
  configurationVersion: '1.0'
  deploymentStrategyId: example-id
  description: Managed by konfig-konector
  dynamicExtensionParameters:
  - extensionReference: example
    parameterName: example-appconfigdeployment
    parameterValue: example
  environmentId: example-id
  kmsKeyIdentifier: example
  providerRef:
    name: example-appconfigdeployment
    region: us-east-1
  tags:
  - key: example
    value: example
