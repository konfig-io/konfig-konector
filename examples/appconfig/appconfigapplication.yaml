# AppConfigApplication — appconfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigapplication
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigApplication
metadata:
  name: example-appconfigapplication
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-appconfigapplication
  providerRef:
    name: example-appconfigapplication
    region: us-east-1
  tags:
  - key: example
    value: example
