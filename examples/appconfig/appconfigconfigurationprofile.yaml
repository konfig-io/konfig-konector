# AppConfigConfigurationProfile — appconfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigconfigurationprofile
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigConfigurationProfile
metadata:
  name: example-appconfigconfigurationprofile
  namespace: default
spec:
  applicationId: example-id
  deletionProtectionCheck: ACCOUNT_DEFAULT
  description: Managed by konfig-konector
  kmsKeyIdentifier: example
  locationUri: example
  name: example-appconfigconfigurationprofile
  providerRef:
    name: example-appconfigconfigurationprofile
    region: us-east-1
  retrievalRoleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  type: example
  validators:
  - content: example
    type: example
