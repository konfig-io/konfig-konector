# AppConfigExperimentRun — appconfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigexperimentrun
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigExperimentRun
metadata:
  name: example-appconfigexperimentrun
  namespace: default
spec:
  applicationIdentifier: example
  description: Managed by konfig-konector
  experimentDefinitionIdentifier: example
  exposurePercentage: 1
  providerRef:
    name: example-appconfigexperimentrun
    region: us-east-1
  tags:
  - key: example
    value: example
  treatmentOverrides:
    inline:
      key: value
