# AppConfigExtension — appconfig
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appconfig.html#appconfigextension
apiVersion: aws.konfig.io/v1alpha1
kind: AppConfigExtension
metadata:
  name: example-appconfigextension
  namespace: default
spec:
  description: Managed by konfig-konector
  latestVersionNumber: 1
  name: example-appconfigextension
  parameters: {}
  providerRef:
    name: example-appconfigextension
    region: us-east-1
  tags:
  - key: example
    value: example
