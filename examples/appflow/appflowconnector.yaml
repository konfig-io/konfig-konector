# AppFlowConnector — AppFlow
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appflow.html#appflowconnector
apiVersion: aws.konfig.io/v1alpha1
kind: AppFlowConnector
metadata:
  name: example-appflowconnector
  namespace: default
spec:
  connectorLabel: example
  connectorProvisioningConfig:
    lambda:
      lambdaArn: arn:aws:iam::123456789012:role/example-role
  connectorProvisioningType: example
  description: Managed by konfig-konector
  providerRef:
    name: example-appflowconnector
    region: us-east-1
