# CodeGuruProfilerProfilingGroup — CodeGuru Profiler
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codeguruprofiler.html#codeguruprofilerprofilinggroup
apiVersion: aws.konfig.io/v1alpha1
kind: CodeGuruProfilerProfilingGroup
metadata:
  name: example-codeguruprofilerprofilinggroup
  namespace: default
spec:
  agentPermissions:
    principals:
    - example
  anomalyDetectionNotificationConfiguration:
  - channelUri: example
  computePlatform: Default
  profilingGroupName: example-codeguruprofilerprofilinggroup
  providerRef:
    name: example-codeguruprofilerprofilinggroup
    region: us-east-1
  tags:
  - key: example
    value: example
