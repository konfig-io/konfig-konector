# EC2TransitGatewayRouteTablePropagation — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewayroutetablepropagation
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayRouteTablePropagation
metadata:
  name: example-ec2transitgatewayroutetablepropagation
  namespace: default
spec:
  providerRef:
    name: example-ec2transitgatewayroutetablepropagation
    region: us-east-1
  transitGatewayAttachmentId: example-id
  transitGatewayRouteTableId: example-id
