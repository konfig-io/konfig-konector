# EC2SnapshotBlockPublicAccess — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2snapshotblockpublicaccess
apiVersion: aws.konfig.io/v1alpha1
kind: EC2SnapshotBlockPublicAccess
metadata:
  name: example-ec2snapshotblockpublicaccess
  namespace: default
spec:
  providerRef:
    name: example-ec2snapshotblockpublicaccess
    region: us-east-1
  state: block-all-sharing
