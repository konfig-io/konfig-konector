# EC2NetworkPerformanceMetricSubscription — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2networkperformancemetricsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: EC2NetworkPerformanceMetricSubscription
metadata:
  name: example-ec2networkperformancemetricsubscription
  namespace: default
spec:
  destination: example
  metric: example
  providerRef:
    name: example-ec2networkperformancemetricsubscription
    region: us-east-1
  source: example
  statistic: example
