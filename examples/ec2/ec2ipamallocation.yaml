# EC2IPAMAllocation — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipamallocation
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAMAllocation
metadata:
  name: example-ec2ipamallocation
  namespace: default
spec:
  cidr: 10.0.0.0/16
  description: Managed by konfig-konector
  ipamPoolId: example-id
  netmaskLength: 1
  providerRef:
    name: example-ec2ipamallocation
    region: us-east-1
