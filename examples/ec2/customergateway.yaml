# CustomerGateway — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#customergateway
apiVersion: aws.konfig.io/v1alpha1
kind: CustomerGateway
metadata:
  name: example-customergateway
  namespace: default
spec:
  bgpAsn: 1
  deviceName: example-customergateway
  ipAddress: example
  providerRef:
    name: example-customergateway
    region: us-east-1
  tags:
    environment: dev
    team: platform
  type: ipsec.1
