# EC2RouteServerPropagation — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2routeserverpropagation
apiVersion: aws.konfig.io/v1alpha1
kind: EC2RouteServerPropagation
metadata:
  name: example-ec2routeserverpropagation
  namespace: default
spec:
  providerRef:
    name: example-ec2routeserverpropagation
    region: us-east-1
  routeServerId: example-id
  routeTableId: example-id
