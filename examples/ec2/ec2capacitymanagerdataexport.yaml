# EC2CapacityManagerDataExport — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2capacitymanagerdataexport
apiVersion: aws.konfig.io/v1alpha1
kind: EC2CapacityManagerDataExport
metadata:
  name: example-ec2capacitymanagerdataexport
  namespace: default
spec:
  outputFormat: csv
  providerRef:
    name: example-ec2capacitymanagerdataexport
    region: us-east-1
  s3BucketName: example-ec2capacitymanagerdataexport
  s3BucketPrefix: example
  schedule: hourly
  tags:
  - key: example
    value: example
