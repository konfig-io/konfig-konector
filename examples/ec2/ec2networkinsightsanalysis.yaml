# EC2NetworkInsightsAnalysis — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2networkinsightsanalysis
apiVersion: aws.konfig.io/v1alpha1
kind: EC2NetworkInsightsAnalysis
metadata:
  name: example-ec2networkinsightsanalysis
  namespace: default
spec:
  additionalAccounts:
  - example
  filterInArns:
  - example
  filterOutArns:
  - example
  networkInsightsPathId: example-id
  providerRef:
    name: example-ec2networkinsightsanalysis
    region: us-east-1
  tags:
  - key: example
    value: example
