# EC2VerifiedAccessInstance — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2verifiedaccessinstance
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VerifiedAccessInstance
metadata:
  name: example-ec2verifiedaccessinstance
  namespace: default
spec:
  cidrEndpointsCustomSubDomain: 10.0.0.0/16
  description: Managed by konfig-konector
  fipsEnabled: true
  loggingConfigurations:
    cloudWatchLogs:
      enabled: true
      logGroup: example
    includeTrustContext: false
    kinesisDataFirehose:
      deliveryStream: example
      enabled: true
    logVersion: '1.0'
    s3:
      bucketName: example-ec2verifiedaccessinstance
      bucketOwner: example
      enabled: true
      prefix: example
  providerRef:
    name: example-ec2verifiedaccessinstance
    region: us-east-1
  tags:
  - key: example
    value: example
  verifiedAccessTrustProviderIds:
  - example
  verifiedAccessTrustProviders:
  - description: Managed by konfig-konector
    deviceTrustProviderType: example
    trustProviderType: example
    userTrustProviderType: example
