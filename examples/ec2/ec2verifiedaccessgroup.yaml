# EC2VerifiedAccessGroup — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2verifiedaccessgroup
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VerifiedAccessGroup
metadata:
  name: example-ec2verifiedaccessgroup
  namespace: default
spec:
  description: Managed by konfig-konector
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  policyEnabled: true
  providerRef:
    name: example-ec2verifiedaccessgroup
    region: us-east-1
  sseSpecification:
    customerManagedKeyEnabled: true
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  verifiedAccessInstanceId: i-0abc1234def567890
