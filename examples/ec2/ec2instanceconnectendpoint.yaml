# EC2InstanceConnectEndpoint — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2instanceconnectendpoint
apiVersion: aws.konfig.io/v1alpha1
kind: EC2InstanceConnectEndpoint
metadata:
  name: example-ec2instanceconnectendpoint
  namespace: default
spec:
  clientToken: example
  preserveClientIp: false
  providerRef:
    name: example-ec2instanceconnectendpoint
    region: us-east-1
  securityGroupIds:
  - example
  subnetId: subnet-0abc12def3456789
  tags:
  - key: example
    value: example
