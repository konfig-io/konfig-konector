# EC2TransitGatewayMulticastDomainAssociation — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewaymulticastdomainassociation
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayMulticastDomainAssociation
metadata:
  name: example-ec2transitgatewaymulticastdomainassociation
  namespace: default
spec:
  providerRef:
    name: example-ec2transitgatewaymulticastdomainassociation
    region: us-east-1
  subnetId: subnet-0abc12def3456789
  transitGatewayAttachmentId: example-id
  transitGatewayMulticastDomainId: example-id
