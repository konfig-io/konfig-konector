# EC2LocalGatewayRouteTable — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2localgatewayroutetable
apiVersion: aws.konfig.io/v1alpha1
kind: EC2LocalGatewayRouteTable
metadata:
  name: example-ec2localgatewayroutetable
  namespace: default
spec:
  localGatewayId: example-id
  mode: example
  providerRef:
    name: example-ec2localgatewayroutetable
    region: us-east-1
  tags:
  - key: example
    value: example
