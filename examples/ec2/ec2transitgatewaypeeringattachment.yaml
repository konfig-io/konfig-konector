# EC2TransitGatewayPeeringAttachment — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewaypeeringattachment
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayPeeringAttachment
metadata:
  name: example-ec2transitgatewaypeeringattachment
  namespace: default
spec:
  peerAccountId: '123456789012'
  peerRegion: example
  peerTransitGatewayId: example-id
  providerRef:
    name: example-ec2transitgatewaypeeringattachment
    region: us-east-1
  tags:
  - key: example
    value: example
  transitGatewayId: example-id
