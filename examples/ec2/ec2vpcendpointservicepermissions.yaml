# EC2VPCEndpointServicePermissions — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2vpcendpointservicepermissions
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VPCEndpointServicePermissions
metadata:
  name: example-ec2vpcendpointservicepermissions
  namespace: default
spec:
  allowedPrincipals:
  - example
  providerRef:
    name: example-ec2vpcendpointservicepermissions
    region: us-east-1
  serviceId: example-id
