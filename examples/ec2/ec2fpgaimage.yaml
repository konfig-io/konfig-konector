# EC2FpgaImage — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2fpgaimage
apiVersion: aws.konfig.io/v1alpha1
kind: EC2FpgaImage
metadata:
  name: example-ec2fpgaimage
  namespace: default
spec:
  description: Managed by konfig-konector
  inputStorageLocation:
    bucket: example
    key: example
  logsStorageLocation:
    bucket: example
    key: example
  name: example-ec2fpgaimage
  providerRef:
    name: example-ec2fpgaimage
    region: us-east-1
  tags:
  - key: example
    value: example
