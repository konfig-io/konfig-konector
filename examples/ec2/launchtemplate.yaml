# LaunchTemplate — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#launchtemplate
apiVersion: aws.konfig.io/v1alpha1
kind: LaunchTemplate
metadata:
  name: example-launchtemplate
  namespace: default
spec:
  blockDeviceMappings:
  - deviceName: example-launchtemplate
    encrypted: true
    volumeSize: 2
    volumeType: example
  iamInstanceProfile: example
  imageId: ami-0abc1234def567890
  instanceType: example
  keyName: example-launchtemplate
  launchTemplateName: example-launchtemplate
  providerRef:
    name: example-launchtemplate
    region: us-east-1
  securityGroupRefs:
  - name: example-securitygrouprefs
  tags:
    environment: dev
    team: platform
  userData: example
