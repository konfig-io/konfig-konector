# EC2VPCBlockPublicAccessOptions — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2vpcblockpublicaccessoptions
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VPCBlockPublicAccessOptions
metadata:
  name: example-ec2vpcblockpublicaccessoptions
  namespace: default
spec:
  internetGatewayBlockMode: block-bidirectional
  providerRef:
    name: example-ec2vpcblockpublicaccessoptions
    region: us-east-1
