# TransitGatewayVpcAttachment — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#transitgatewayvpcattachment
apiVersion: aws.konfig.io/v1alpha1
kind: TransitGatewayVpcAttachment
metadata:
  name: example-transitgatewayvpcattachment
  namespace: default
spec:
  accepterProviderRef:
    name: example-transitgatewayvpcattachment
    region: us-east-1
  applianceModeSupport: false
  dnsSupport: false
  ipv6Support: false
  providerRef:
    name: example-transitgatewayvpcattachment
    region: us-east-1
  subnetRefs:
  - name: example-subnetrefs
  tags:
    environment: dev
    team: platform
  transitGatewayRef:
    name: example-transitgatewayvpcattachment
  vpcRef:
    name: example-vpc
