# EC2IpamExternalResourceVerificationToken — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipamexternalresourceverificationtoken
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IpamExternalResourceVerificationToken
metadata:
  name: example-ec2ipamexternalresourceverificationtoken
  namespace: default
spec:
  ipamId: example-id
  providerRef:
    name: example-ec2ipamexternalresourceverificationtoken
    region: us-east-1
  tags:
  - key: example
    value: example
