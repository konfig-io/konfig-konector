# EC2IPAMPrefixListResolverTarget — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipamprefixlistresolvertarget
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAMPrefixListResolverTarget
metadata:
  name: example-ec2ipamprefixlistresolvertarget
  namespace: default
spec:
  desiredVersion: 1
  ipamPrefixListResolverId: example-id
  prefixListId: example-id
  prefixListRegion: example
  providerRef:
    name: example-ec2ipamprefixlistresolvertarget
    region: us-east-1
  tags:
  - key: example
    value: example
  trackLatestVersion: false
