# EC2TransitGatewayMulticastGroupSource — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewaymulticastgroupsource
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayMulticastGroupSource
metadata:
  name: example-ec2transitgatewaymulticastgroupsource
  namespace: default
spec:
  groupIpAddress: example
  networkInterfaceId: example-id
  providerRef:
    name: example-ec2transitgatewaymulticastgroupsource
    region: us-east-1
  transitGatewayMulticastDomainId: example-id
