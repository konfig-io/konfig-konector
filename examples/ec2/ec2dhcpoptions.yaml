# EC2DHCPOptions — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2dhcpoptions
apiVersion: aws.konfig.io/v1alpha1
kind: EC2DHCPOptions
metadata:
  name: example-ec2dhcpoptions
  namespace: default
spec:
  domainName: example-ec2dhcpoptions
  domainNameServers:
  - example
  ipv6AddressPreferredLeaseTime: 1
  netbiosNameServers:
  - example
  netbiosNodeType: 1
  ntpServers:
  - example
  providerRef:
    name: example-ec2dhcpoptions
    region: us-east-1
  tags:
  - key: example
    value: example
