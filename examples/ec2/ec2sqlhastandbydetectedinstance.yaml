# EC2SqlHaStandbyDetectedInstance — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2sqlhastandbydetectedinstance
apiVersion: aws.konfig.io/v1alpha1
kind: EC2SqlHaStandbyDetectedInstance
metadata:
  name: example-ec2sqlhastandbydetectedinstance
  namespace: default
spec:
  instanceId: i-0abc1234def567890
  providerRef:
    name: example-ec2sqlhastandbydetectedinstance
    region: us-east-1
  sqlServerCredentials: example
