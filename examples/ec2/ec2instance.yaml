# EC2Instance — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2instance
apiVersion: aws.konfig.io/v1alpha1
kind: EC2Instance
metadata:
  name: example-ec2instance
  namespace: default
spec:
  associatePublicIpAddress: false
  iamInstanceProfile: example
  imageId: ami-0abc1234def567890
  instanceType: example
  keyName: example-ec2instance
  providerRef:
    name: example-ec2instance
    region: us-east-1
  securityGroupRefs:
  - name: example-securitygrouprefs
  subnetRef: example
  tags:
    environment: dev
    team: platform
  userData: example
