# EC2CapacityReservationFleet — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2capacityreservationfleet
apiVersion: aws.konfig.io/v1alpha1
kind: EC2CapacityReservationFleet
metadata:
  name: example-ec2capacityreservationfleet
  namespace: default
spec:
  allocationStrategy: example
  endDate: example
  instanceMatchCriteria: open
  instanceTypeSpecifications:
  - availabilityZone: us-east-1a
    availabilityZoneId: us-east-1a
    ebsOptimized: false
    instancePlatform: example
    instanceType: example
    priority: 1
    weight: 1
  noRemoveEndDate: false
  providerRef:
    name: example-ec2capacityreservationfleet
    region: us-east-1
  removeEndDate: false
  tagSpecifications:
  - resourceType: example
    tags:
    - key: example
      value: example
  tenancy: default
  totalTargetCapacity: 1
