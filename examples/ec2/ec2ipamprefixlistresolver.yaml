# EC2IPAMPrefixListResolver — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipamprefixlistresolver
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAMPrefixListResolver
metadata:
  name: example-ec2ipamprefixlistresolver
  namespace: default
spec:
  addressFamily: example
  description: Managed by konfig-konector
  providerRef:
    name: example-ec2ipamprefixlistresolver
    region: us-east-1
  rules:
  - conditions:
    - cidr: 10.0.0.0/16
      operation: equals
      resourceOwner: example
      resourceRegion: example
    resourceType: vpc
    ruleType: static-cidr
    staticCidr: 10.0.0.0/16
  tags:
  - key: example
    value: example
