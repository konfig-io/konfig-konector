# EC2TrafficMirrorSession — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2trafficmirrorsession
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TrafficMirrorSession
metadata:
  name: example-ec2trafficmirrorsession
  namespace: default
spec:
  description: Managed by konfig-konector
  networkInterfaceId: example-id
  packetLength: 1
  providerRef:
    name: example-ec2trafficmirrorsession
    region: us-east-1
  sessionNumber: 1
  tags:
  - key: example
    value: example
  trafficMirrorFilterId: example-id
  trafficMirrorTargetId: example-id
  virtualNetworkId: 1
