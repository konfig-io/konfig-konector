# EC2IPAMResourceDiscoveryAssociation — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipamresourcediscoveryassociation
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAMResourceDiscoveryAssociation
metadata:
  name: example-ec2ipamresourcediscoveryassociation
  namespace: default
spec:
  ipamId: example-id
  ipamResourceDiscoveryId: example-id
  providerRef:
    name: example-ec2ipamresourcediscoveryassociation
    region: us-east-1
  tags:
  - key: example
    value: example
