# EC2LocalGatewayVirtualInterfaceGroup — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2localgatewayvirtualinterfacegroup
apiVersion: aws.konfig.io/v1alpha1
kind: EC2LocalGatewayVirtualInterfaceGroup
metadata:
  name: example-ec2localgatewayvirtualinterfacegroup
  namespace: default
spec:
  localBgpAsn: 1
  localBgpAsnExtended: 1
  localGatewayId: example-id
  providerRef:
    name: example-ec2localgatewayvirtualinterfacegroup
    region: us-east-1
  tags:
  - key: example
    value: example
