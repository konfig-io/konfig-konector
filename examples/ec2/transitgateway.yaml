# TransitGateway — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#transitgateway
apiVersion: aws.konfig.io/v1alpha1
kind: TransitGateway
metadata:
  name: example-transitgateway
  namespace: default
spec:
  amazonSideAsn: 64512
  autoAcceptSharedAttachments: false
  defaultRouteTableAssociation: false
  defaultRouteTablePropagation: false
  description: Managed by konfig-konector
  dnsSupport: false
  providerRef:
    name: example-transitgateway
    region: us-east-1
  tags:
    environment: dev
    team: platform
  vpnEcmpSupport: false
