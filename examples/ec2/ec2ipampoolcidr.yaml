# EC2IPAMPoolCidr — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipampoolcidr
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAMPoolCidr
metadata:
  name: example-ec2ipampoolcidr
  namespace: default
spec:
  cidr: 10.0.0.0/16
  ipamPoolId: example-id
  netmaskLength: 1
  providerRef:
    name: example-ec2ipampoolcidr
    region: us-east-1
