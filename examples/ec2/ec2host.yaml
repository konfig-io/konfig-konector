# EC2Host — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2host
apiVersion: aws.konfig.io/v1alpha1
kind: EC2Host
metadata:
  name: example-ec2host
  namespace: default
spec:
  autoPlacement: 'on'
  availabilityZone: us-east-1a
  hostMaintenance: 'on'
  hostRecovery: 'on'
  instanceFamily: example
  instanceType: example
  outpostArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-ec2host
    region: us-east-1
  tags:
  - key: example
    value: example
