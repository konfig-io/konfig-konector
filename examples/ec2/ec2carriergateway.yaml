# EC2CarrierGateway — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2carriergateway
apiVersion: aws.konfig.io/v1alpha1
kind: EC2CarrierGateway
metadata:
  name: example-ec2carriergateway
  namespace: default
spec:
  providerRef:
    name: example-ec2carriergateway
    region: us-east-1
  tags:
  - key: example
    value: example
  vpcId: vpc-0abc12def3456789
