# EC2VerifiedAccessTrustProvider — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2verifiedaccesstrustprovider
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VerifiedAccessTrustProvider
metadata:
  name: example-ec2verifiedaccesstrustprovider
  namespace: default
spec:
  description: Managed by konfig-konector
  deviceOptions:
    publicSigningKeyUrl: example
  deviceTrustProviderType: example
  nativeApplicationOidcOptions:
    authorizationEndpoint: example
    clientSecret: example
    issuer: example
    publicSigningKeyEndpoint: example
    scope: example
    tokenEndpoint: example
    userInfoEndpoint: example
  oidcOptions:
    authorizationEndpoint: example
    clientSecret: example
    issuer: example
    scope: example
    tokenEndpoint: example
    userInfoEndpoint: example
  policyReferenceName: example-ec2verifiedaccesstrustprovider
  providerRef:
    name: example-ec2verifiedaccesstrustprovider
    region: us-east-1
  sseSpecification:
    customerManagedKeyEnabled: true
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  trustProviderType: example
  userTrustProviderType: example
