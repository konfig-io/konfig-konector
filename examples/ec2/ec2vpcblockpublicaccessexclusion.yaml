# EC2VPCBlockPublicAccessExclusion — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2vpcblockpublicaccessexclusion
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VPCBlockPublicAccessExclusion
metadata:
  name: example-ec2vpcblockpublicaccessexclusion
  namespace: default
spec:
  internetGatewayExclusionMode: allow-bidirectional
  providerRef:
    name: example-ec2vpcblockpublicaccessexclusion
    region: us-east-1
  tags:
  - key: example
    value: example
