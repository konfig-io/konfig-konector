# EC2NetworkInsightsPath — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2networkinsightspath
apiVersion: aws.konfig.io/v1alpha1
kind: EC2NetworkInsightsPath
metadata:
  name: example-ec2networkinsightspath
  namespace: default
spec:
  destination: example
  destinationIp: example
  destinationPort: 443
  filterAtDestination:
    destinationAddress: example
    destinationPortRange:
      fromPort: 443
      toPort: 443
    sourceAddress: example
    sourcePortRange:
      fromPort: 443
      toPort: 443
  filterAtSource:
    destinationAddress: example
    destinationPortRange:
      fromPort: 443
      toPort: 443
    sourceAddress: example
    sourcePortRange:
      fromPort: 443
      toPort: 443
  protocol: tcp
  providerRef:
    name: example-ec2networkinsightspath
    region: us-east-1
  source: example
  sourceIp: example
  tags:
  - key: example
    value: example
