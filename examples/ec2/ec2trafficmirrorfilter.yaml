# EC2TrafficMirrorFilter — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2trafficmirrorfilter
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TrafficMirrorFilter
metadata:
  name: example-ec2trafficmirrorfilter
  namespace: default
spec:
  description: Managed by konfig-konector
  networkServices:
  - example
  providerRef:
    name: example-ec2trafficmirrorfilter
    region: us-east-1
  tags:
  - key: example
    value: example
