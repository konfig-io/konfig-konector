# EC2TransitGatewayMeteringPolicy — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewaymeteringpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayMeteringPolicy
metadata:
  name: example-ec2transitgatewaymeteringpolicy
  namespace: default
spec:
  middleboxAttachmentIds:
  - example
  providerRef:
    name: example-ec2transitgatewaymeteringpolicy
    region: us-east-1
  tags:
  - key: example
    value: example
  transitGatewayId: example-id
