# EC2TransitGatewayPolicyTableEntry — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewaypolicytableentry
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayPolicyTableEntry
metadata:
  name: example-ec2transitgatewaypolicytableentry
  namespace: default
spec:
  policyRule:
    destinationCidrBlock: 10.0.0.0/16
    destinationPortRange: example
    protocol: example
    sourceCidrBlock: 10.0.0.0/16
    sourcePortRange: example
  policyRuleNumber: example
  providerRef:
    name: example-ec2transitgatewaypolicytableentry
    region: us-east-1
  targetRouteTableId: example-id
  transitGatewayPolicyTableId: example-id
