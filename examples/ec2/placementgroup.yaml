# PlacementGroup — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#placementgroup
apiVersion: aws.konfig.io/v1alpha1
kind: PlacementGroup
metadata:
  name: example-placementgroup
  namespace: default
spec:
  groupName: example-placementgroup
  partitionCount: 1
  providerRef:
    name: example-placementgroup
    region: us-east-1
  spreadLevel: host
  strategy: cluster
  tags:
    environment: dev
    team: platform
