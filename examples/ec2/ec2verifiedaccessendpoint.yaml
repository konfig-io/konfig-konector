# EC2VerifiedAccessEndpoint — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2verifiedaccessendpoint
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VerifiedAccessEndpoint
metadata:
  name: example-ec2verifiedaccessendpoint
  namespace: default
spec:
  applicationDomain: example
  attachmentType: example
  cidrOptions:
    cidr: 10.0.0.0/16
    portRanges:
    - fromPort: 1
      toPort: 1
    protocol: example
    subnetIds:
    - example
  description: Managed by konfig-konector
  domainCertificateArn: arn:aws:iam::123456789012:role/example-role
  endpointDomainPrefix: example
  endpointType: example
  loadBalancerOptions:
    loadBalancerArn: arn:aws:iam::123456789012:role/example-role
    port: 1
    portRanges:
    - fromPort: 1
      toPort: 1
    protocol: example
    subnetIds:
    - example
  networkInterfaceOptions:
    port: 1
    portRanges:
    - fromPort: 1
      toPort: 1
    protocol: example
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  policyEnabled: true
  providerRef:
    name: example-ec2verifiedaccessendpoint
    region: us-east-1
  rdsOptions:
    port: 1
    protocol: example
    rdsDbClusterArn: arn:aws:iam::123456789012:role/example-role
    rdsDbInstanceArn: arn:aws:iam::123456789012:role/example-role
    rdsDbProxyArn: arn:aws:iam::123456789012:role/example-role
    rdsEndpoint: example
    subnetIds:
    - example
  securityGroupIds:
  - example
  sseSpecification:
    customerManagedKeyEnabled: true
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  verifiedAccessGroupId: example-id
