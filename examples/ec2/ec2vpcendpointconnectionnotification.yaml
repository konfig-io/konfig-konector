# EC2VPCEndpointConnectionNotification — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2vpcendpointconnectionnotification
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VPCEndpointConnectionNotification
metadata:
  name: example-ec2vpcendpointconnectionnotification
  namespace: default
spec:
  connectionEvents:
  - example
  connectionNotificationArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-ec2vpcendpointconnectionnotification
    region: us-east-1
