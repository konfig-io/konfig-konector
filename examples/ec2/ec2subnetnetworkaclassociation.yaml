# EC2SubnetNetworkAclAssociation — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2subnetnetworkaclassociation
apiVersion: aws.konfig.io/v1alpha1
kind: EC2SubnetNetworkAclAssociation
metadata:
  name: example-ec2subnetnetworkaclassociation
  namespace: default
spec:
  networkAclId: example-id
  providerRef:
    name: example-ec2subnetnetworkaclassociation
    region: us-east-1
  subnetId: subnet-0abc12def3456789
