# EC2LocalGatewayVirtualInterface — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2localgatewayvirtualinterface
apiVersion: aws.konfig.io/v1alpha1
kind: EC2LocalGatewayVirtualInterface
metadata:
  name: example-ec2localgatewayvirtualinterface
  namespace: default
spec:
  localAddress: example
  localGatewayVirtualInterfaceGroupId: example-id
  outpostLagId: example-id
  peerAddress: example
  peerBgpAsn: 1
  peerBgpAsnExtended: 1
  providerRef:
    name: example-ec2localgatewayvirtualinterface
    region: us-east-1
  tags:
  - key: example
    value: example
  vlan: 1
