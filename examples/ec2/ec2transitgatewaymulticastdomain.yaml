# EC2TransitGatewayMulticastDomain — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewaymulticastdomain
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayMulticastDomain
metadata:
  name: example-ec2transitgatewaymulticastdomain
  namespace: default
spec:
  options:
    autoAcceptSharedAssociations: example
    igmpv2Support: example
    staticSourcesSupport: example
  providerRef:
    name: example-ec2transitgatewaymulticastdomain
    region: us-east-1
  tags:
  - key: example
    value: example
  transitGatewayId: example-id
