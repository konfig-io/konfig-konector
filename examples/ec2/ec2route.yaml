# EC2Route — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2route
apiVersion: aws.konfig.io/v1alpha1
kind: EC2Route
metadata:
  name: example-ec2route
  namespace: default
spec:
  coreNetworkArn: arn:aws:iam::123456789012:role/example-role
  destinationCidrBlock: 10.0.0.0/16
  destinationIpv6CidrBlock: 2001:db8::/56
  odbNetworkArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-ec2route
    region: us-east-1
  routeTableId: example-id
