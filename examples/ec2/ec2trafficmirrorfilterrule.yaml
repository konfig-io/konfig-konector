# EC2TrafficMirrorFilterRule — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2trafficmirrorfilterrule
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TrafficMirrorFilterRule
metadata:
  name: example-ec2trafficmirrorfilterrule
  namespace: default
spec:
  description: Managed by konfig-konector
  destinationCidrBlock: 10.0.0.0/16
  destinationPortRange:
    fromPort: 443
    toPort: 443
  protocol: 1
  providerRef:
    name: example-ec2trafficmirrorfilterrule
    region: us-east-1
  ruleAction: example
  ruleNumber: 1
  sourceCidrBlock: 10.0.0.0/16
  sourcePortRange:
    fromPort: 443
    toPort: 443
  tags:
  - key: example
    value: example
  trafficDirection: example
  trafficMirrorFilterId: example-id
