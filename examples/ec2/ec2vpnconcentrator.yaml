# EC2VPNConcentrator — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2vpnconcentrator
apiVersion: aws.konfig.io/v1alpha1
kind: EC2VPNConcentrator
metadata:
  name: example-ec2vpnconcentrator
  namespace: default
spec:
  providerRef:
    name: example-ec2vpnconcentrator
    region: us-east-1
  tags:
  - key: example
    value: example
  transitGatewayId: example-id
  type: example
