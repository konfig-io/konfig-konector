# VPNGateway — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#vpngateway
apiVersion: aws.konfig.io/v1alpha1
kind: VPNGateway
metadata:
  name: example-vpngateway
  namespace: default
spec:
  amazonSideAsn: 1
  providerRef:
    name: example-vpngateway
    region: us-east-1
  tags:
    environment: dev
    team: platform
  type: ipsec.1
  vpcRef:
    name: example-vpc
