# EC2TransitGatewayConnect — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewayconnect
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayConnect
metadata:
  name: example-ec2transitgatewayconnect
  namespace: default
spec:
  options:
    protocol: example
  providerRef:
    name: example-ec2transitgatewayconnect
    region: us-east-1
  tags:
  - key: example
    value: example
  transportTransitGatewayAttachmentId: example-id
