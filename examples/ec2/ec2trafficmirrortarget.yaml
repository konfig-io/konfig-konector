# EC2TrafficMirrorTarget — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2trafficmirrortarget
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TrafficMirrorTarget
metadata:
  name: example-ec2trafficmirrortarget
  namespace: default
spec:
  description: Managed by konfig-konector
  networkLoadBalancerArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-ec2trafficmirrortarget
    region: us-east-1
  tags:
  - key: example
    value: example
