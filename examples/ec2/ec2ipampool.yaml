# EC2IPAMPool — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipampool
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAMPool
metadata:
  name: example-ec2ipampool
  namespace: default
spec:
  addressFamily: example
  allocationDefaultNetmaskLength: 1
  allocationMaxNetmaskLength: 1
  allocationMinNetmaskLength: 1
  allocationResourceTags:
  - key: example
    value: example
  autoImport: false
  awsService: ec2
  description: Managed by konfig-konector
  ipamScopeId: example-id
  locale: example
  providerRef:
    name: example-ec2ipampool
    region: us-east-1
  provisionedCidrs:
  - cidr: 10.0.0.0/16
  publicIpSource: byoip
  publiclyAdvertisable: false
  sourceResource:
    resourceId: example-id
    resourceOwner: example
    resourceRegion: example
    resourceType: example
  tags:
  - key: example
    value: example
