# EC2RouteServerEndpoint — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2routeserverendpoint
apiVersion: aws.konfig.io/v1alpha1
kind: EC2RouteServerEndpoint
metadata:
  name: example-ec2routeserverendpoint
  namespace: default
spec:
  providerRef:
    name: example-ec2routeserverendpoint
    region: us-east-1
  routeServerId: example-id
  subnetId: subnet-0abc12def3456789
  tags:
  - key: example
    value: example
