# EC2IPAM — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2ipam
apiVersion: aws.konfig.io/v1alpha1
kind: EC2IPAM
metadata:
  name: example-ec2ipam
  namespace: default
spec:
  defaultResourceDiscoveryOrganizationalUnitExclusions:
  - organizationsEntityPath: example
  description: Managed by konfig-konector
  enablePrivateGua: true
  meteredAccount: ipam-owner
  operatingRegions:
  - regionName: example-ec2ipam
  providerRef:
    name: example-ec2ipam
    region: us-east-1
  tags:
  - key: example
    value: example
  tier: free
