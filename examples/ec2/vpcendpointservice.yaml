# VPCEndpointService — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#vpcendpointservice
apiVersion: aws.konfig.io/v1alpha1
kind: VPCEndpointService
metadata:
  name: example-vpcendpointservice
  namespace: default
spec:
  acceptanceRequired: false
  allowedPrincipals:
  - example
  autoAcceptConnections: false
  gatewayLoadBalancerArns:
  - example
  networkLoadBalancerRefs:
  - name: example-networkloadbalancerrefs
  privateDnsName: example-vpcendpointservice
  providerRef:
    name: example-vpcendpointservice
    region: us-east-1
  supportedIpAddressTypes:
  - example
  tags:
    environment: dev
    team: platform
