# EC2RouteServerPeer — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2routeserverpeer
apiVersion: aws.konfig.io/v1alpha1
kind: EC2RouteServerPeer
metadata:
  name: example-ec2routeserverpeer
  namespace: default
spec:
  bgpOptions:
    peerAsn: 1
    peerLivenessDetection: bfd
  peerAddress: example
  providerRef:
    name: example-ec2routeserverpeer
    region: us-east-1
  routeServerEndpointId: example-id
  tags:
  - key: example
    value: example
