# EC2SubnetCidrBlock — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2subnetcidrblock
apiVersion: aws.konfig.io/v1alpha1
kind: EC2SubnetCidrBlock
metadata:
  name: example-ec2subnetcidrblock
  namespace: default
spec:
  ipv6CidrBlock: 2001:db8::/56
  ipv6NetmaskLength: 1
  providerRef:
    name: example-ec2subnetcidrblock
    region: us-east-1
  subnetId: subnet-0abc12def3456789
