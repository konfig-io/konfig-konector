# EC2TransitGatewayConnectPeer — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2transitgatewayconnectpeer
apiVersion: aws.konfig.io/v1alpha1
kind: EC2TransitGatewayConnectPeer
metadata:
  name: example-ec2transitgatewayconnectpeer
  namespace: default
spec:
  connectPeerConfiguration:
    bgpConfigurations:
    - bgpStatus: example
      peerAddress: example
      peerAsn: 1
      transitGatewayAddress: example
      transitGatewayAsn: 1
    insideCidrBlocks:
    - 10.0.0.0/16
    peerAddress: example
    protocol: example
    transitGatewayAddress: example
  providerRef:
    name: example-ec2transitgatewayconnectpeer
    region: us-east-1
  tags:
  - key: example
    value: example
  transitGatewayAttachmentId: example-id
