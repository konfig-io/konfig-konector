# ManagedPrefixList — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#managedprefixlist
apiVersion: aws.konfig.io/v1alpha1
kind: ManagedPrefixList
metadata:
  name: example-managedprefixlist
  namespace: default
spec:
  addressFamily: IPv4
  entries:
  - cidr: 10.0.0.0/16
    description: Managed by konfig-konector
  maxEntries: 1
  name: example-managedprefixlist
  tags:
    environment: dev
    team: platform
