# EC2NetworkInsightsAccessScope — EC2 & VPC
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ec2.html#ec2networkinsightsaccessscope
apiVersion: aws.konfig.io/v1alpha1
kind: EC2NetworkInsightsAccessScope
metadata:
  name: example-ec2networkinsightsaccessscope
  namespace: default
spec:
  providerRef:
    name: example-ec2networkinsightsaccessscope
    region: us-east-1
  tags:
  - key: example
    value: example
