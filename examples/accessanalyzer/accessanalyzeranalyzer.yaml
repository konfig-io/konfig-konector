# AccessAnalyzerAnalyzer — accessanalyzer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/accessanalyzer.html#accessanalyzeranalyzer
apiVersion: aws.konfig.io/v1alpha1
kind: AccessAnalyzerAnalyzer
metadata:
  name: example-accessanalyzeranalyzer
  namespace: default
spec:
  analyzerConfiguration:
    internalAccessConfiguration:
      internalAccessAnalysisRule:
        inclusions:
        - accountIds:
          - example
          resourceArns:
          - example
          resourceTypes:
          - example
    unusedAccessConfiguration:
      analysisRule:
        exclusions:
        - accountIds:
          - example
          resourceTags:
          - - key: example
              value: example
      unusedAccessAge: 1
  analyzerName: example-accessanalyzeranalyzer
  archiveRules:
  - filter:
    - contains:
      - example
      eq:
      - example
      exists: false
      neq:
      - example
      property: example
    ruleName: example-accessanalyzeranalyzer
  providerRef:
    name: example-accessanalyzeranalyzer
    region: us-east-1
  tags:
  - key: example
    value: example
  type: example
