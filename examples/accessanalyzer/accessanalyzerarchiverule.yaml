# AccessAnalyzerArchiveRule — accessanalyzer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/accessanalyzer.html#accessanalyzerarchiverule
apiVersion: aws.konfig.io/v1alpha1
kind: AccessAnalyzerArchiveRule
metadata:
  name: example-accessanalyzerarchiverule
  namespace: default
spec:
  analyzerName: example-accessanalyzerarchiverule
  filter: {}
  providerRef:
    name: example-accessanalyzerarchiverule
    region: us-east-1
  ruleName: example-accessanalyzerarchiverule
