# Route53ProfilesProfile — Route 53 Profiles
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/route53profiles.html#route53profilesprofile
apiVersion: aws.konfig.io/v1alpha1
kind: Route53ProfilesProfile
metadata:
  name: example-route53profilesprofile
  namespace: default
spec:
  name: example-route53profilesprofile
  providerRef:
    name: example-route53profilesprofile
    region: us-east-1
  tags:
  - key: example
    value: example
