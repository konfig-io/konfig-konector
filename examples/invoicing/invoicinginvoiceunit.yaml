# InvoicingInvoiceUnit — invoicing
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/invoicing.html#invoicinginvoiceunit
apiVersion: aws.konfig.io/v1alpha1
kind: InvoicingInvoiceUnit
metadata:
  name: example-invoicinginvoiceunit
  namespace: default
spec:
  description: Managed by konfig-konector
  invoiceReceiver: example-xxxx
  name: example-invoicinginvoiceunit
  providerRef:
    name: example-invoicinginvoiceunit
    region: us-east-1
  resourceTags:
  - key: example
    value: example
  rule:
    linkedAccounts:
    - example
  taxInheritanceDisabled: false
