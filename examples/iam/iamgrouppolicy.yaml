# IAMGroupPolicy — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamgrouppolicy
apiVersion: aws.konfig.io/v1alpha1
kind: IAMGroupPolicy
metadata:
  name: example-iamgrouppolicy
  namespace: default
spec:
  groupName: example-iamgrouppolicy
  policyName: example-iamgrouppolicy
  providerRef:
    name: example-iamgrouppolicy
    region: us-east-1
