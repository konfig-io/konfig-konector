# IAMUser — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamuser
apiVersion: aws.konfig.io/v1alpha1
kind: IAMUser
metadata:
  name: example-iamuser
  namespace: default
spec:
  path: example
  permissionsBoundary: example
  providerRef:
    name: example-iamuser
    region: us-east-1
  tags:
    environment: dev
    team: platform
  userName: example-iamuser
