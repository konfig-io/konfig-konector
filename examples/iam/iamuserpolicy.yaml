# IAMUserPolicy — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamuserpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: IAMUserPolicy
metadata:
  name: example-iamuserpolicy
  namespace: default
spec:
  policyName: example-iamuserpolicy
  providerRef:
    name: example-iamuserpolicy
    region: us-east-1
  userName: example-iamuserpolicy
