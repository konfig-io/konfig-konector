# IAMPolicy — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iampolicy
apiVersion: aws.konfig.io/v1alpha1
kind: IAMPolicy
metadata:
  name: example-iampolicy
  namespace: default
spec:
  description: Managed by konfig-konector
  path: example
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  policyName: example-iampolicy
  providerRef:
    name: example-iampolicy
    region: us-east-1
  tags:
    environment: dev
    team: platform
