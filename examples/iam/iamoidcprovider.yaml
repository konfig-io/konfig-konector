# IAMOIDCProvider — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamoidcprovider
apiVersion: aws.konfig.io/v1alpha1
kind: IAMOIDCProvider
metadata:
  name: example-iamoidcprovider
  namespace: default
spec:
  clientIDList:
  - example
  providerRef:
    name: example-iamoidcprovider
    region: us-east-1
  tags:
    environment: dev
    team: platform
  thumbprintList:
  - example
  url: example
