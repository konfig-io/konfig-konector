# IAMGroupMembership — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamgroupmembership
apiVersion: aws.konfig.io/v1alpha1
kind: IAMGroupMembership
metadata:
  name: example-iamgroupmembership
  namespace: default
spec:
  groupRef:
    groupName: example-iamgroupmembership
    name: example-iamgroupmembership
  userRef:
    name: example-iamgroupmembership
    userName: example-iamgroupmembership
