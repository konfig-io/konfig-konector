# IAMInstanceProfile — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iaminstanceprofile
apiVersion: aws.konfig.io/v1alpha1
kind: IAMInstanceProfile
metadata:
  name: example-iaminstanceprofile
  namespace: default
spec:
  instanceProfileName: example-iaminstanceprofile
  path: example
  providerRef:
    name: example-iaminstanceprofile
    region: us-east-1
  roleRef:
    name: example-role
  tags:
    environment: dev
    team: platform
