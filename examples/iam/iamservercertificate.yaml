# IAMServerCertificate — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamservercertificate
apiVersion: aws.konfig.io/v1alpha1
kind: IAMServerCertificate
metadata:
  name: example-iamservercertificate
  namespace: default
spec:
  certificateBody: example
  certificateChain: example
  path: example
  privateKey: example
  providerRef:
    name: example-iamservercertificate
    region: us-east-1
  serverCertificateName: example-iamservercertificate
  tags:
  - key: example
    value: example
