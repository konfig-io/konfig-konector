# IAMVirtualMFADevice — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamvirtualmfadevice
apiVersion: aws.konfig.io/v1alpha1
kind: IAMVirtualMFADevice
metadata:
  name: example-iamvirtualmfadevice
  namespace: default
spec:
  path: example
  providerRef:
    name: example-iamvirtualmfadevice
    region: us-east-1
  tags:
  - key: example
    value: example
  users:
  - example
  virtualMfaDeviceName: example-iamvirtualmfadevice
