# IAMRole — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamrole
apiVersion: aws.konfig.io/v1alpha1
kind: IAMRole
metadata:
  name: example-iamrole
  namespace: default
spec:
  assumeRolePolicyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  description: Managed by konfig-konector
  maxSessionDuration: 3600
  path: example
  permissionsBoundary: example
  roleName: example-iamrole
  tags:
    environment: dev
    team: platform
