# IAMGroupPolicyAttachment — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamgrouppolicyattachment
apiVersion: aws.konfig.io/v1alpha1
kind: IAMGroupPolicyAttachment
metadata:
  name: example-iamgrouppolicyattachment
  namespace: default
spec:
  groupRef:
    groupName: example-iamgrouppolicyattachment
    name: example-iamgrouppolicyattachment
  policyRef:
    name: example-policy
  providerRef:
    name: example-iamgrouppolicyattachment
    region: us-east-1
