# IAMServiceLinkedRole — IAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/iam.html#iamservicelinkedrole
apiVersion: aws.konfig.io/v1alpha1
kind: IAMServiceLinkedRole
metadata:
  name: example-iamservicelinkedrole
  namespace: default
spec:
  awsServiceName: example-iamservicelinkedrole
  customSuffix: example
  description: Managed by konfig-konector
  providerRef:
    name: example-iamservicelinkedrole
    region: us-east-1
