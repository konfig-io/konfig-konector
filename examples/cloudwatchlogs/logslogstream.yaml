# LogsLogStream — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logslogstream
apiVersion: aws.konfig.io/v1alpha1
kind: LogsLogStream
metadata:
  name: example-logslogstream
  namespace: default
spec:
  logGroupName: example-logslogstream
  logStreamName: example-logslogstream
  providerRef:
    name: example-logslogstream
    region: us-east-1
