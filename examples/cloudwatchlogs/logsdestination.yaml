# LogsDestination — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsdestination
apiVersion: aws.konfig.io/v1alpha1
kind: LogsDestination
metadata:
  name: example-logsdestination
  namespace: default
spec:
  destinationName: example-logsdestination
  destinationPolicy: example
  providerRef:
    name: example-logsdestination
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  targetArn: arn:aws:iam::123456789012:role/example-role
