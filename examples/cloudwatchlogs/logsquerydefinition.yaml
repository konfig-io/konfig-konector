# LogsQueryDefinition — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsquerydefinition
apiVersion: aws.konfig.io/v1alpha1
kind: LogsQueryDefinition
metadata:
  name: example-logsquerydefinition
  namespace: default
spec:
  logGroupNames:
  - example
  name: example-logsquerydefinition
  parameters:
  - defaultValue: example
    description: Managed by konfig-konector
    name: example-logsquerydefinition
  providerRef:
    name: example-logsquerydefinition
    region: us-east-1
  queryLanguage: CWLI
  queryString: example
