# MetricFilter — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#metricfilter
apiVersion: aws.konfig.io/v1alpha1
kind: MetricFilter
metadata:
  name: example-metricfilter
  namespace: default
spec:
  filterName: example-metricfilter
  filterPattern: example
  logGroupRef:
    logGroupName: example-metricfilter
    name: example-metricfilter
  metricTransformations:
  - defaultValue: 1
    metricName: example-metricfilter
    metricNamespace: example
    metricValue: example
