# LogsAccountPolicy — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsaccountpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: LogsAccountPolicy
metadata:
  name: example-logsaccountpolicy
  namespace: default
spec:
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  policyName: example-logsaccountpolicy
  policyType: DATA_PROTECTION_POLICY
  providerRef:
    name: example-logsaccountpolicy
    region: us-east-1
  scope: ALL
  selectionCriteria: example
