# SubscriptionFilter — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#subscriptionfilter
apiVersion: aws.konfig.io/v1alpha1
kind: SubscriptionFilter
metadata:
  name: example-subscriptionfilter
  namespace: default
spec:
  destinationArn: arn:aws:iam::123456789012:role/example-role
  filterName: example-subscriptionfilter
  filterPattern: example
  logGroupRef:
    logGroupName: example-subscriptionfilter
    name: example-subscriptionfilter
  roleArn: arn:aws:iam::123456789012:role/example-role
