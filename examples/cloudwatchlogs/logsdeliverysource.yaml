# LogsDeliverySource — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsdeliverysource
apiVersion: aws.konfig.io/v1alpha1
kind: LogsDeliverySource
metadata:
  name: example-logsdeliverysource
  namespace: default
spec:
  deliverySourceConfiguration:
    key: value
  logType: example
  name: example-logsdeliverysource
  providerRef:
    name: example-logsdeliverysource
    region: us-east-1
  resourceArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
