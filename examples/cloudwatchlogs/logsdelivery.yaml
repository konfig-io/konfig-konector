# LogsDelivery — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsdelivery
apiVersion: aws.konfig.io/v1alpha1
kind: LogsDelivery
metadata:
  name: example-logsdelivery
  namespace: default
spec:
  deliveryDestinationArn: arn:aws:iam::123456789012:role/example-role
  deliverySourceName: example-logsdelivery
  fieldDelimiter: examp
  providerRef:
    name: example-logsdelivery
    region: us-east-1
  recordFields:
  - example
  s3EnableHiveCompatiblePath: true
  s3SuffixPath: example
  tags:
  - key: example
    value: example
