# LogsStorageTierPolicy — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsstoragetierpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: LogsStorageTierPolicy
metadata:
  name: example-logsstoragetierpolicy
  namespace: default
spec:
  providerRef:
    name: example-logsstoragetierpolicy
    region: us-east-1
  storageTier: INTELLIGENT_TIERING
