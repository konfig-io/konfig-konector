# LogsResourcePolicy — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: LogsResourcePolicy
metadata:
  name: example-logsresourcepolicy
  namespace: default
spec:
  policyName: example-logsresourcepolicy
  providerRef:
    name: example-logsresourcepolicy
    region: us-east-1
