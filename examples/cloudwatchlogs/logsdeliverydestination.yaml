# LogsDeliveryDestination — CloudWatch Logs
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatchlogs.html#logsdeliverydestination
apiVersion: aws.konfig.io/v1alpha1
kind: LogsDeliveryDestination
metadata:
  name: example-logsdeliverydestination
  namespace: default
spec:
  deliveryDestinationPolicy:
    deliveryDestinationName: example-logsdeliverydestination
  deliveryDestinationType: example
  destinationResourceArn: arn:aws:iam::123456789012:role/example-role
  name: example-logsdeliverydestination
  outputFormat: example
  providerRef:
    name: example-logsdeliverydestination
    region: us-east-1
  tags:
  - key: example
    value: example
