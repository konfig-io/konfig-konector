# DynamoDBGlobalTable — DynamoDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dynamodb.html#dynamodbglobaltable
apiVersion: aws.konfig.io/v1alpha1
kind: DynamoDBGlobalTable
metadata:
  name: example-dynamodbglobaltable
  namespace: default
spec:
  replicationGroup:
  - regionName: example-dynamodbglobaltable
  tableName: example-dynamodbglobaltable
