# DynamoDBBackup — DynamoDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dynamodb.html#dynamodbbackup
apiVersion: aws.konfig.io/v1alpha1
kind: DynamoDBBackup
metadata:
  name: example-dynamodbbackup
  namespace: default
spec:
  backupName: example-dynamodbbackup
  tableName: example-dynamodbbackup
