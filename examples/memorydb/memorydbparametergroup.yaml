# MemoryDBParameterGroup — MemoryDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/memorydb.html#memorydbparametergroup
apiVersion: aws.konfig.io/v1alpha1
kind: MemoryDBParameterGroup
metadata:
  name: example-memorydbparametergroup
  namespace: default
spec:
  description: Managed by konfig-konector
  family: example
  parameterGroupName: example-memorydbparametergroup
  providerRef:
    name: example-memorydbparametergroup
    region: us-east-1
  tags:
  - key: example
    value: example
