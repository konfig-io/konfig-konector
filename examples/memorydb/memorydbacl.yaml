# MemoryDBACL — MemoryDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/memorydb.html#memorydbacl
apiVersion: aws.konfig.io/v1alpha1
kind: MemoryDBACL
metadata:
  name: example-memorydbacl
  namespace: default
spec:
  aclName: example-memorydbacl
  providerRef:
    name: example-memorydbacl
    region: us-east-1
  tags:
  - key: example
    value: example
  userNames:
  - example
