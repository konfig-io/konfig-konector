# MemoryDBMultiRegionCluster — MemoryDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/memorydb.html#memorydbmultiregioncluster
apiVersion: aws.konfig.io/v1alpha1
kind: MemoryDBMultiRegionCluster
metadata:
  name: example-memorydbmultiregioncluster
  namespace: default
spec:
  description: Managed by konfig-konector
  engine: example
  engineVersion: example
  multiRegionClusterNameSuffix: example
  multiRegionParameterGroupName: example-memorydbmultiregioncluster
  nodeType: example
  numShards: 1
  providerRef:
    name: example-memorydbmultiregioncluster
    region: us-east-1
  tags:
  - key: example
    value: example
  tlsEnabled: true
  updateStrategy: COORDINATED
