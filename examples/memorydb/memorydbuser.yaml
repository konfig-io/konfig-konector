# MemoryDBUser — MemoryDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/memorydb.html#memorydbuser
apiVersion: aws.konfig.io/v1alpha1
kind: MemoryDBUser
metadata:
  name: example-memorydbuser
  namespace: default
spec:
  accessString: example
  authenticationMode:
    passwords:
    - example
    type: password
  providerRef:
    name: example-memorydbuser
    region: us-east-1
  tags:
  - key: example
    value: example
  userName: example-memorydbuser
