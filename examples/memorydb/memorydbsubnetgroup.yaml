# MemoryDBSubnetGroup — MemoryDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/memorydb.html#memorydbsubnetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: MemoryDBSubnetGroup
metadata:
  name: example-memorydbsubnetgroup
  namespace: default
spec:
  description: Managed by konfig-konector
  providerRef:
    name: example-memorydbsubnetgroup
    region: us-east-1
  subnetGroupName: example-memorydbsubnetgroup
  subnetIds:
  - example
  tags:
  - key: example
    value: example
