# BackupLogicallyAirGappedBackupVault — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backuplogicallyairgappedbackupvault
apiVersion: aws.konfig.io/v1alpha1
kind: BackupLogicallyAirGappedBackupVault
metadata:
  name: example-backuplogicallyairgappedbackupvault
  namespace: default
spec:
  backupVaultName: example-backuplogicallyairgappedbackupvault
  backupVaultTags:
    environment: dev
    team: platform
  encryptionKeyArn: arn:aws:iam::123456789012:role/example-role
  maxRetentionDays: 7
  minRetentionDays: 7
  mpaApprovalTeamArn: arn:aws:iam::123456789012:role/example-role
  notifications:
    backupVaultEvents:
    - example
    snsTopicArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-backuplogicallyairgappedbackupvault
    region: us-east-1
