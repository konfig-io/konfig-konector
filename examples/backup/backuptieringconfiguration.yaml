# BackupTieringConfiguration — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backuptieringconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: BackupTieringConfiguration
metadata:
  name: example-backuptieringconfiguration
  namespace: default
spec:
  backupVaultName: example-backuptieringconfiguration
  providerRef:
    name: example-backuptieringconfiguration
    region: us-east-1
  resourceSelection:
  - resourceType: example
    resources:
    - example
    tieringDownSettingsInDays: 7
  tieringConfigurationName: example-backuptieringconfiguration
  tieringConfigurationTags:
    environment: dev
    team: platform
