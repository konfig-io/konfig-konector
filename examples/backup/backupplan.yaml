# BackupPlan — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backupplan
apiVersion: aws.konfig.io/v1alpha1
kind: BackupPlan
metadata:
  name: example-backupplan
  namespace: default
spec:
  planName: example-backupplan
  providerRef:
    name: example-backupplan
    region: us-east-1
  rules:
  - completionWindowMinutes: 1
    lifecycle:
      deleteAfterDays: 7
      moveToColdStorageAfterDays: 7
    ruleName: example-backupplan
    scheduleExpression: example
    startWindowMinutes: 1
    targetBackupVaultName: example-backupplan
    targetBackupVaultRef: example
  tags:
    environment: dev
    team: platform
