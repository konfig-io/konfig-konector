# BackupRestoreTestingSelection — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backuprestoretestingselection
apiVersion: aws.konfig.io/v1alpha1
kind: BackupRestoreTestingSelection
metadata:
  name: example-backuprestoretestingselection
  namespace: default
spec:
  iamRoleArn: arn:aws:iam::123456789012:role/example-role
  protectedResourceArns:
  - example
  protectedResourceConditions:
    stringEquals:
    - key: example
      value: example
    stringNotEquals:
    - key: example
      value: example
  protectedResourceType: example
  providerRef:
    name: example-backuprestoretestingselection
    region: us-east-1
  restoreMetadataOverrides:
    key: value
  restoreTestingPlanName: example-backuprestoretestingselection
  restoreTestingSelectionName: example-backuprestoretestingselection
  validationWindowHours: 1
