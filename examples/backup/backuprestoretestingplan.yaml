# BackupRestoreTestingPlan — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backuprestoretestingplan
apiVersion: aws.konfig.io/v1alpha1
kind: BackupRestoreTestingPlan
metadata:
  name: example-backuprestoretestingplan
  namespace: default
spec:
  providerRef:
    name: example-backuprestoretestingplan
    region: us-east-1
  recoveryPointSelection:
    algorithm: LATEST_WITHIN_WINDOW
    excludeVaults:
    - example
    includeVaults:
    - example
    recoveryPointTypes:
    - example
    selectionWindowDays: 7
  restoreTestingPlanName: example-backuprestoretestingplan
  scheduleExpression: example
  scheduleExpressionTimezone: example
  startWindowHours: 1
  tags:
  - key: example
    value: example
