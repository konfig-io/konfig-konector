# BackupReportPlan — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backupreportplan
apiVersion: aws.konfig.io/v1alpha1
kind: BackupReportPlan
metadata:
  name: example-backupreportplan
  namespace: default
spec:
  providerRef:
    name: example-backupreportplan
    region: us-east-1
  reportDeliveryChannel:
    formats:
    - example
    s3BucketName: example-backupreportplan
    s3KeyPrefix: example
  reportPlanDescription: Managed by konfig-konector
  reportPlanName: example-backupreportplan
  reportPlanTags:
  - key: example
    value: example
  reportSetting:
    accounts:
    - example
    frameworkArns:
    - example
    organizationUnits:
    - example
    regions:
    - example
    reportTemplate: example
