# BackupLegalHold — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backuplegalhold
apiVersion: aws.konfig.io/v1alpha1
kind: BackupLegalHold
metadata:
  name: example-backuplegalhold
  namespace: default
spec:
  description: Managed by konfig-konector
  providerRef:
    name: example-backuplegalhold
    region: us-east-1
  recoveryPointSelection:
    dateRange:
      fromDate: example
      toDate: example
    resourceIdentifiers:
    - example
    vaultNames:
    - example
  tags:
  - key: example
    value: example
  title: example
