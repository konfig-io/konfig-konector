# BackupFramework — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backupframework
apiVersion: aws.konfig.io/v1alpha1
kind: BackupFramework
metadata:
  name: example-backupframework
  namespace: default
spec:
  frameworkControls:
  - controlInputParameters:
    - parameterName: example-backupframework
      parameterValue: example
    controlName: example-backupframework
    controlScope:
      complianceResourceIds:
      - example
      complianceResourceTypes:
      - example
      tags:
      - key: example
        value: example
  frameworkDescription: Managed by konfig-konector
  frameworkName: example-backupframework
  frameworkTags:
  - key: example
    value: example
  providerRef:
    name: example-backupframework
    region: us-east-1
