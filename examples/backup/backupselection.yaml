# BackupSelection — Backup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/backup.html#backupselection
apiVersion: aws.konfig.io/v1alpha1
kind: BackupSelection
metadata:
  name: example-backupselection
  namespace: default
spec:
  iamRoleArn: arn:aws:iam::123456789012:role/example-role
  iamRoleRef:
    name: example-iamrole
  listOfTags:
  - key: example
    value: example
  planRef: example
  resources:
  - example
  selectionName: example-backupselection
