# BatchServiceEnvironment — Batch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/batch.html#batchserviceenvironment
apiVersion: aws.konfig.io/v1alpha1
kind: BatchServiceEnvironment
metadata:
  name: example-batchserviceenvironment
  namespace: default
spec:
  capacityLimits:
  - capacityUnit: example
    maxCapacity: 2
  providerRef:
    name: example-batchserviceenvironment
    region: us-east-1
  serviceEnvironmentName: example-batchserviceenvironment
  serviceEnvironmentType: example
  state: example
  tags:
    environment: dev
    team: platform
