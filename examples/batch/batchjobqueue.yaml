# BatchJobQueue — Batch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/batch.html#batchjobqueue
apiVersion: aws.konfig.io/v1alpha1
kind: BatchJobQueue
metadata:
  name: example-batchjobqueue
  namespace: default
spec:
  computeEnvironmentOrder:
  - computeEnvironmentRef:
      name: example-computeenvironment
    order: 1
  name: example-batchjobqueue
  priority: 1
  state: ENABLED
  tags:
    environment: dev
    team: platform
