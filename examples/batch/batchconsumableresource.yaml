# BatchConsumableResource — Batch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/batch.html#batchconsumableresource
apiVersion: aws.konfig.io/v1alpha1
kind: BatchConsumableResource
metadata:
  name: example-batchconsumableresource
  namespace: default
spec:
  consumableResourceName: example-batchconsumableresource
  providerRef:
    name: example-batchconsumableresource
    region: us-east-1
  resourceType: REPLENISHABLE
  tags:
    environment: dev
    team: platform
  totalQuantity: 1
