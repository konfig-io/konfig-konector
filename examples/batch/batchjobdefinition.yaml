# BatchJobDefinition — Batch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/batch.html#batchjobdefinition
apiVersion: aws.konfig.io/v1alpha1
kind: BatchJobDefinition
metadata:
  name: example-batchjobdefinition
  namespace: default
spec:
  containerProperties:
    command:
    - example
    environment:
      key: value
    executionRoleArn: arn:aws:iam::123456789012:role/example-role
    executionRoleRef:
      name: example-executionrole
    image: example
    jobRoleArn: arn:aws:iam::123456789012:role/example-role
    jobRoleRef:
      name: example-jobrole
    resourceRequirements:
    - type: VCPU
      value: example
  name: example-batchjobdefinition
  platformCapabilities:
  - example
  retryStrategy:
    attempts: 1
  tags:
    environment: dev
    team: platform
  timeout:
    attemptDurationSeconds: 60
  type: container
