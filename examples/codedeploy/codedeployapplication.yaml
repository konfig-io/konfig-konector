# CodeDeployApplication — CodeDeploy
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codedeploy.html#codedeployapplication
apiVersion: aws.konfig.io/v1alpha1
kind: CodeDeployApplication
metadata:
  name: example-codedeployapplication
  namespace: default
spec:
  applicationName: example-codedeployapplication
  computePlatform: Server
  tags:
    environment: dev
    team: platform
