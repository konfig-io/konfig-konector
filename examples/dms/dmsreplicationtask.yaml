# DMSReplicationTask — DMS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dms.html#dmsreplicationtask
apiVersion: aws.konfig.io/v1alpha1
kind: DMSReplicationTask
metadata:
  name: example-dmsreplicationtask
  namespace: default
spec:
  cdcStartPosition: example
  cdcStartTime: 1
  cdcStopPosition: example
  migrationType: full-load
  providerRef:
    name: example-dmsreplicationtask
    region: us-east-1
  replicationInstanceArn: arn:aws:iam::123456789012:role/example-role
  replicationTaskIdentifier: example
  replicationTaskSettings: example
  resourceIdentifier: example
  sourceEndpointArn: arn:aws:iam::123456789012:role/example-role
  tableMappings: example
  tags:
  - key: example
    value: example
  targetEndpointArn: arn:aws:iam::123456789012:role/example-role
  taskData: example
