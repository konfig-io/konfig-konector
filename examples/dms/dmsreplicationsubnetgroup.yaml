# DMSReplicationSubnetGroup — dms
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dms.html#dmsreplicationsubnetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: DMSReplicationSubnetGroup
metadata:
  name: example-dmsreplicationsubnetgroup
  namespace: default
spec:
  providerRef:
    name: example-dmsreplicationsubnetgroup
    region: us-east-1
  replicationSubnetGroupDescription: Managed by konfig-konector
  replicationSubnetGroupIdentifier: example
  subnetIds:
  - example
  tags:
  - key: example
    value: example
