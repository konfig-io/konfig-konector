# DMSEventSubscription — dms
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dms.html#dmseventsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: DMSEventSubscription
metadata:
  name: example-dmseventsubscription
  namespace: default
spec:
  enabled: true
  eventCategories:
  - example
  providerRef:
    name: example-dmseventsubscription
    region: us-east-1
  snsTopicArn: arn:aws:iam::123456789012:role/example-role
  sourceIds:
  - example
  sourceType: example
  subscriptionName: example-dmseventsubscription
  tags:
  - key: example
    value: example
