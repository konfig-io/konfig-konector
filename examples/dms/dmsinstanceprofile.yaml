# DMSInstanceProfile — dms
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dms.html#dmsinstanceprofile
apiVersion: aws.konfig.io/v1alpha1
kind: DMSInstanceProfile
metadata:
  name: example-dmsinstanceprofile
  namespace: default
spec:
  availabilityZone: us-east-1a
  description: Managed by konfig-konector
  instanceProfileIdentifier: example
  instanceProfileName: example-dmsinstanceprofile
  kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  networkType: IPV4
  providerRef:
    name: example-dmsinstanceprofile
    region: us-east-1
  publiclyAccessible: false
  subnetGroupIdentifier: example
  tags:
  - key: example
    value: example
  vpcSecurityGroups:
  - example
