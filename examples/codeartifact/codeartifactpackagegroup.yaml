# CodeArtifactPackageGroup — CodeArtifact
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codeartifact.html#codeartifactpackagegroup
apiVersion: aws.konfig.io/v1alpha1
kind: CodeArtifactPackageGroup
metadata:
  name: example-codeartifactpackagegroup
  namespace: default
spec:
  contactInfo: example
  description: Managed by konfig-konector
  domainName: example-codeartifactpackagegroup
  domainOwner: example
  originConfiguration:
    restrictions:
      externalUpstream:
        repositories:
        - example
        restrictionMode: ALLOW
      internalUpstream:
        repositories:
        - example
        restrictionMode: ALLOW
      publish:
        repositories:
        - example
        restrictionMode: ALLOW
  pattern: example
  providerRef:
    name: example-codeartifactpackagegroup
    region: us-east-1
  tags:
  - key: example
    value: example
