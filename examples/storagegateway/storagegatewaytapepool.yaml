# StorageGatewayTapePool — Storage Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/storagegateway.html#storagegatewaytapepool
apiVersion: aws.konfig.io/v1alpha1
kind: StorageGatewayTapePool
metadata:
  name: example-storagegatewaytapepool
  namespace: default
spec:
  poolName: example-storagegatewaytapepool
  providerRef:
    name: example-storagegatewaytapepool
    region: us-east-1
  retentionLockTimeInDays: 1
  retentionLockType: COMPLIANCE
  storageClass: DEEP_ARCHIVE
  tags:
  - key: example
    value: example
