# MQConfiguration — Amazon MQ
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/mq.html#mqconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: MQConfiguration
metadata:
  name: example-mqconfiguration
  namespace: default
spec:
  data: example
  engineType: ACTIVEMQ
  engineVersion: example
  name: example-mqconfiguration
  providerRef:
    name: example-mqconfiguration
    region: us-east-1
  tags:
    environment: dev
    team: platform
