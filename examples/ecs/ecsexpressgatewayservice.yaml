# ECSExpressGatewayService — ECS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecs.html#ecsexpressgatewayservice
apiVersion: aws.konfig.io/v1alpha1
kind: ECSExpressGatewayService
metadata:
  name: example-ecsexpressgatewayservice
  namespace: default
spec:
  cluster: example
  cpu: example
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  healthCheckPath: example
  infrastructureRoleArn: arn:aws:iam::123456789012:role/example-role
  memory: example
  networkConfiguration:
    securityGroups:
    - example
    subnets:
    - example
  primaryContainer:
    awsLogsConfiguration:
      logGroup: example
      logStreamPrefix: example
    command:
    - example
    containerPort: 443
    environment:
    - name: example-ecsexpressgatewayservice
      value: example
    image: example
    repositoryCredentials:
      credentialsParameter: example
    secrets:
    - name: example-ecsexpressgatewayservice
      valueFrom: example
  providerRef:
    name: example-ecsexpressgatewayservice
    region: us-east-1
  scalingTarget:
    autoScalingMetric: AVERAGE_CPU
    autoScalingTargetValue: 1
    maxTaskCount: 2
    minTaskCount: 2
  serviceName: example-ecsexpressgatewayservice
  tags:
  - key: example
    value: example
  taskDefinitionArn: arn:aws:iam::123456789012:role/example-role
  taskRoleArn: arn:aws:iam::123456789012:role/example-role
