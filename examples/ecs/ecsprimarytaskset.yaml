# ECSPrimaryTaskSet — ECS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecs.html#ecsprimarytaskset
apiVersion: aws.konfig.io/v1alpha1
kind: ECSPrimaryTaskSet
metadata:
  name: example-ecsprimarytaskset
  namespace: default
spec:
  cluster: example
  providerRef:
    name: example-ecsprimarytaskset
    region: us-east-1
  service: example
  taskSetId: example-id
