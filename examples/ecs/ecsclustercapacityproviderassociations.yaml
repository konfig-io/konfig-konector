# ECSClusterCapacityProviderAssociations — ECS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecs.html#ecsclustercapacityproviderassociations
apiVersion: aws.konfig.io/v1alpha1
kind: ECSClusterCapacityProviderAssociations
metadata:
  name: example-ecsclustercapacityproviderassociations
  namespace: default
spec:
  cluster: example
  defaultCapacityProviderStrategy:
  - base: 1
    weight: 1
  providerRef:
    name: example-ecsclustercapacityproviderassociations
    region: us-east-1
