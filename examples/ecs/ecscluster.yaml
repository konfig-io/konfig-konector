# ECSCluster — ECS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecs.html#ecscluster
apiVersion: aws.konfig.io/v1alpha1
kind: ECSCluster
metadata:
  name: example-ecscluster
  namespace: default
spec:
  capacityProviders:
  - example
  clusterName: example-ecscluster
  containerInsights: false
  tags:
    environment: dev
    team: platform
