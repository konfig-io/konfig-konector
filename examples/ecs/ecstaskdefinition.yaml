# ECSTaskDefinition — ECS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecs.html#ecstaskdefinition
apiVersion: aws.konfig.io/v1alpha1
kind: ECSTaskDefinition
metadata:
  name: example-ecstaskdefinition
  namespace: default
spec:
  containerDefinitions:
  - command:
    - example
    cpu: 1
    entryPoint:
    - example
    environment:
      key: value
    essential: false
    image: example
    logGroup: example
    memory: 1
    name: example-ecstaskdefinition
    portMappings:
    - containerPort: 443
      protocol: tcp
  cpu: example
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  executionRoleRef:
    name: example-executionrole
  family: example
  memory: example
  networkMode: awsvpc
  providerRef:
    name: example-ecstaskdefinition
    region: us-east-1
  tags:
    environment: dev
    team: platform
  taskRoleArn: arn:aws:iam::123456789012:role/example-role
  taskRoleRef:
    name: example-taskrole
