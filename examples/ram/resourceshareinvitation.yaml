# ResourceShareInvitation — RAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ram.html#resourceshareinvitation
apiVersion: aws.konfig.io/v1alpha1
kind: ResourceShareInvitation
metadata:
  name: example-resourceshareinvitation
  namespace: default
spec:
  providerRef:
    name: example-resourceshareinvitation
    region: us-east-1
  resourceShareRef:
    name: example-resourceshare
