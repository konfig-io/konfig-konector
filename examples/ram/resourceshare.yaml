# ResourceShare — RAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ram.html#resourceshare
apiVersion: aws.konfig.io/v1alpha1
kind: ResourceShare
metadata:
  name: example-resourceshare
  namespace: default
spec:
  allowExternalPrincipals: false
  name: example-resourceshare
  permissionArns:
  - example
  principals:
  - example
  resourceArns:
  - example
  tags:
    environment: dev
    team: platform
