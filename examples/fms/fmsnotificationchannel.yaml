# FMSNotificationChannel — fms
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/fms.html#fmsnotificationchannel
apiVersion: aws.konfig.io/v1alpha1
kind: FMSNotificationChannel
metadata:
  name: example-fmsnotificationchannel
  namespace: default
spec:
  providerRef:
    name: example-fmsnotificationchannel
    region: us-east-1
  snsRoleName: example-fmsnotificationchannel
  snsTopicArn: arn:aws:iam::123456789012:role/example-role
