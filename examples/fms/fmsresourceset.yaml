# FMSResourceSet — Firewall Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/fms.html#fmsresourceset
apiVersion: aws.konfig.io/v1alpha1
kind: FMSResourceSet
metadata:
  name: example-fmsresourceset
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-fmsresourceset
  providerRef:
    name: example-fmsresourceset
    region: us-east-1
  resourceTypeList:
  - example
  resources:
  - example
  tags:
  - key: example
    value: example
