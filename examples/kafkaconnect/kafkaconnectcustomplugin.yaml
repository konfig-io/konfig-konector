# KafkaConnectCustomPlugin — kafkaconnect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kafkaconnect.html#kafkaconnectcustomplugin
apiVersion: aws.konfig.io/v1alpha1
kind: KafkaConnectCustomPlugin
metadata:
  name: example-kafkaconnectcustomplugin
  namespace: default
spec:
  contentType: JAR
  description: Managed by konfig-konector
  location:
    s3Location:
      bucketArn: arn:aws:iam::123456789012:role/example-role
      fileKey: example
      objectVersion: '1.0'
  name: example-kafkaconnectcustomplugin
  providerRef:
    name: example-kafkaconnectcustomplugin
    region: us-east-1
  tags:
  - key: example
    value: example
