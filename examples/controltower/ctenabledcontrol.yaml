# CTEnabledControl — Control Tower
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/controltower.html#ctenabledcontrol
apiVersion: aws.konfig.io/v1alpha1
kind: CTEnabledControl
metadata:
  name: example-ctenabledcontrol
  namespace: default
spec:
  controlIdentifier: example-xx
  providerRef:
    name: example-ctenabledcontrol
    region: us-east-1
  tags:
    environment: dev
    team: platform
  targetIdentifier: example-xx
