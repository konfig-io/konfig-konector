# ControlTowerLandingZone — Control Tower
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/controltower.html#controltowerlandingzone
apiVersion: aws.konfig.io/v1alpha1
kind: ControlTowerLandingZone
metadata:
  name: example-controltowerlandingzone
  namespace: default
spec:
  providerRef:
    name: example-controltowerlandingzone
    region: us-east-1
  remediationTypes:
  - example
  tags:
  - key: example
    value: example
  version: '1.0'
