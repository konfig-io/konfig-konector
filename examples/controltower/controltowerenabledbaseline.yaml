# ControlTowerEnabledBaseline — Control Tower
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/controltower.html#controltowerenabledbaseline
apiVersion: aws.konfig.io/v1alpha1
kind: ControlTowerEnabledBaseline
metadata:
  name: example-controltowerenabledbaseline
  namespace: default
spec:
  baselineIdentifier: example-xxxxxxxxxxxx
  baselineVersion: '1.0'
  parameters:
  - key: example
  providerRef:
    name: example-controltowerenabledbaseline
    region: us-east-1
  tags:
  - key: example
    value: example
  targetIdentifier: example-xxxxxxxxxxxx
