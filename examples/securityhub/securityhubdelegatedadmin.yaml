# SecurityHubDelegatedAdmin — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubdelegatedadmin
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubDelegatedAdmin
metadata:
  name: example-securityhubdelegatedadmin
  namespace: default
spec:
  adminAccountId: '123456789012'
  providerRef:
    name: example-securityhubdelegatedadmin
    region: us-east-1
