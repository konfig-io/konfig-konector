# SecurityHubConnector — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubconnector
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubConnector
metadata:
  name: example-securityhubconnector
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-securityhubconnector
  provider:
    azure:
      awsConfigConnectorArn: arn:aws:iam::123456789012:role/example-role
      azureRegions:
      - example
      scopeConfiguration:
        scopeType: TENANT
        scopeValues:
        - example
  providerRef:
    name: example-securityhubconnector
    region: us-east-1
  tags:
    environment: dev
    team: platform
