# SecurityHubProductSubscription — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubproductsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubProductSubscription
metadata:
  name: example-securityhubproductsubscription
  namespace: default
spec:
  productArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-securityhubproductsubscription
    region: us-east-1
