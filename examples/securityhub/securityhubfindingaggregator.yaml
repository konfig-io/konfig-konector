# SecurityHubFindingAggregator — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubfindingaggregator
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubFindingAggregator
metadata:
  name: example-securityhubfindingaggregator
  namespace: default
spec:
  providerRef:
    name: example-securityhubfindingaggregator
    region: us-east-1
  regionLinkingMode: ALL_REGIONS
  regions:
  - example
