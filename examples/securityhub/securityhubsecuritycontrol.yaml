# SecurityHubSecurityControl — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubsecuritycontrol
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubSecurityControl
metadata:
  name: example-securityhubsecuritycontrol
  namespace: default
spec:
  lastUpdateReason: example
  parameters: {}
  providerRef:
    name: example-securityhubsecuritycontrol
    region: us-east-1
  securityControlArn: arn:aws:iam::123456789012:role/example-role
