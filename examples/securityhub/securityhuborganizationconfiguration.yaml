# SecurityHubOrganizationConfiguration — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhuborganizationconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubOrganizationConfiguration
metadata:
  name: example-securityhuborganizationconfiguration
  namespace: default
spec:
  autoEnable: true
  autoEnableStandards: DEFAULT
  configurationType: CENTRAL
  providerRef:
    name: example-securityhuborganizationconfiguration
    region: us-east-1
