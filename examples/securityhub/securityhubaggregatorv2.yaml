# SecurityHubAggregatorV2 — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubaggregatorv2
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubAggregatorV2
metadata:
  name: example-securityhubaggregatorv2
  namespace: default
spec:
  linkedRegions:
  - example
  providerRef:
    name: example-securityhubaggregatorv2
    region: us-east-1
  regionLinkingMode: SPECIFIED_REGIONS
  tags:
    environment: dev
    team: platform
