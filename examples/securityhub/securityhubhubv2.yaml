# SecurityHubHubV2 — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubhubv2
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubHubV2
metadata:
  name: example-securityhubhubv2
  namespace: default
spec:
  providerRef:
    name: example-securityhubhubv2
    region: us-east-1
  tags:
    environment: dev
    team: platform
