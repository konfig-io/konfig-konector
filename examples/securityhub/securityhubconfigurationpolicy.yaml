# SecurityHubConfigurationPolicy — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubconfigurationpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubConfigurationPolicy
metadata:
  name: example-securityhubconfigurationpolicy
  namespace: default
spec:
  configurationPolicy:
    securityHub:
      enabledStandardIdentifiers:
      - example
      securityControlsConfiguration:
        disabledSecurityControlIdentifiers:
        - example
        enabledSecurityControlIdentifiers:
        - example
      serviceEnabled: true
  description: Managed by konfig-konector
  name: example-securityhubconfigurationpolicy
  providerRef:
    name: example-securityhubconfigurationpolicy
    region: us-east-1
  tags:
    environment: dev
    team: platform
