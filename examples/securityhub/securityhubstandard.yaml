# SecurityHubStandard — Security Hub
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securityhub.html#securityhubstandard
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityHubStandard
metadata:
  name: example-securityhubstandard
  namespace: default
spec:
  providerRef:
    name: example-securityhubstandard
    region: us-east-1
  standardsArn: arn:aws:iam::123456789012:role/example-role
