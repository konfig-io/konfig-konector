# TimestreamDatabase — Timestream
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/timestream.html#timestreamdatabase
apiVersion: aws.konfig.io/v1alpha1
kind: TimestreamDatabase
metadata:
  name: example-timestreamdatabase
  namespace: default
spec:
  databaseName: example-timestreamdatabase
  kmsKeyId: arn:aws:kms:us-east-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab
  providerRef:
    name: example-timestreamdatabase
    region: us-east-1
  tags:
  - key: example
    value: example
