# InspectorV2CisScanConfiguration — Inspector
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/inspector2.html#inspectorv2cisscanconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: InspectorV2CisScanConfiguration
metadata:
  name: example-inspectorv2cisscanconfiguration
  namespace: default
spec:
  providerRef:
    name: example-inspectorv2cisscanconfiguration
    region: us-east-1
  scanName: example-inspectorv2cisscanconfiguration
  securityLevel: LEVEL_1
  tags:
    environment: dev
    team: platform
