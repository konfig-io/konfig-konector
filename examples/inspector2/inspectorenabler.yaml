# InspectorEnabler — Inspector
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/inspector2.html#inspectorenabler
apiVersion: aws.konfig.io/v1alpha1
kind: InspectorEnabler
metadata:
  name: example-inspectorenabler
  namespace: default
spec:
  resourceTypes:
  - EC2
