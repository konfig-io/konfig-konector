# InspectorV2CodeSecurityScanConfiguration — Inspector
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/inspector2.html#inspectorv2codesecurityscanconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: InspectorV2CodeSecurityScanConfiguration
metadata:
  name: example-inspectorv2codesecurityscanconfiguration
  namespace: default
spec:
  configuration:
    continuousIntegrationScanConfiguration:
      supportedEvents:
      - example
    periodicScanConfiguration:
      frequency: WEEKLY
      frequencyExpression: example
    ruleSetCategories:
    - example
  level: ORGANIZATION
  name: example-inspectorv2codesecurityscanconfiguration
  providerRef:
    name: example-inspectorv2codesecurityscanconfiguration
    region: us-east-1
  scopeSettings:
    projectSelectionScope: ALL
  tags:
    environment: dev
    team: platform
