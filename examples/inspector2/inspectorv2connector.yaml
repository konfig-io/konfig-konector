# InspectorV2Connector — Inspector
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/inspector2.html#inspectorv2connector
apiVersion: aws.konfig.io/v1alpha1
kind: InspectorV2Connector
metadata:
  name: example-inspectorv2connector
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-inspectorv2connector
  provider: AZURE
  providerConfiguration:
    azure:
      autoInstallVMScanner: false
      awsConfigConnectorArn: arn:aws:iam::123456789012:role/example-role
      azureRegions:
      - example
  providerRef:
    name: example-inspectorv2connector
    region: us-east-1
  tags:
  - key: example
    value: example
