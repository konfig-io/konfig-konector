# SSMOpsItem — Systems Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmopsitem
apiVersion: aws.konfig.io/v1alpha1
kind: SSMOpsItem
metadata:
  name: example-ssmopsitem
  namespace: default
spec:
  category: example
  description: Managed by konfig-konector
  priority: 1
  providerRef:
    name: example-ssmopsitem
    region: us-east-1
  severity: example
  source: example
  tags:
  - key: example
    value: example
  title: example
