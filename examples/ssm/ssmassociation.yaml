# SSMAssociation — SSM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmassociation
apiVersion: aws.konfig.io/v1alpha1
kind: SSMAssociation
metadata:
  name: example-ssmassociation
  namespace: default
spec:
  associationName: example-ssmassociation
  documentRef:
    documentName: example-ssmassociation
    name: example-ssmassociation
  name: example-ssmassociation
  parameters: {}
  scheduleExpression: example
  targets:
  - key: example
    values:
    - example
