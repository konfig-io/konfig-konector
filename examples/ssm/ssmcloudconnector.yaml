# SSMCloudConnector — SSM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmcloudconnector
apiVersion: aws.konfig.io/v1alpha1
kind: SSMCloudConnector
metadata:
  name: example-ssmcloudconnector
  namespace: default
spec:
  configConnectorArn: arn:aws:iam::123456789012:role/example-role
  configuration:
    azureConfiguration:
      applicationDisplayName: example-ssmcloudconnector
      applicationId: example-id
      targets:
        subscriptions:
        - displayName: example-ssmcloudconnector
          id: example-id
      tenantDisplayName: example-ssmcloudconnector
      tenantId: example-id
  description: Managed by konfig-konector
  displayName: example-ssmcloudconnector
  providerRef:
    name: example-ssmcloudconnector
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
