# SSMMaintenanceWindowTarget — SSM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmmaintenancewindowtarget
apiVersion: aws.konfig.io/v1alpha1
kind: SSMMaintenanceWindowTarget
metadata:
  name: example-ssmmaintenancewindowtarget
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-ssmmaintenancewindowtarget
  ownerInformation: example
  providerRef:
    name: example-ssmmaintenancewindowtarget
    region: us-east-1
  resourceType: example
  targets:
  - key: example
    values:
    - example
  windowId: example-id
