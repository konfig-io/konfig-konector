# SSMResourceDataSync — Systems Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmresourcedatasync
apiVersion: aws.konfig.io/v1alpha1
kind: SSMResourceDataSync
metadata:
  name: example-ssmresourcedatasync
  namespace: default
spec:
  bucketName: example-ssmresourcedatasync
  bucketPrefix: example
  bucketRegion: example
  kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-ssmresourcedatasync
    region: us-east-1
  s3Destination:
    bucketName: example-ssmresourcedatasync
    bucketPrefix: example
    bucketRegion: example
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
    syncFormat: example
  syncFormat: example
  syncName: example-ssmresourcedatasync
  syncSource:
    awsOrganizationsSource:
      organizationSourceType: example
      organizationalUnits:
      - example
    includeFutureRegions: false
    sourceRegions:
    - example
    sourceType: example
  syncType: example
