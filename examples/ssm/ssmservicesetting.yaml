# SSMServiceSetting — SSM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmservicesetting
apiVersion: aws.konfig.io/v1alpha1
kind: SSMServiceSetting
metadata:
  name: example-ssmservicesetting
  namespace: default
spec:
  providerRef:
    name: example-ssmservicesetting
    region: us-east-1
  settingId: example-id
  settingValue: example
