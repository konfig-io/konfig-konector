# SSMDocument — SSM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssm.html#ssmdocument
apiVersion: aws.konfig.io/v1alpha1
kind: SSMDocument
metadata:
  name: example-ssmdocument
  namespace: default
spec:
  content: example
  documentFormat: JSON
  documentType: Command
  name: example-ssmdocument
  tags:
    environment: dev
    team: platform
  versionName: example-ssmdocument
