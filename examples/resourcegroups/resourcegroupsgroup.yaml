# ResourceGroupsGroup — resourcegroups
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/resourcegroups.html#resourcegroupsgroup
apiVersion: aws.konfig.io/v1alpha1
kind: ResourceGroupsGroup
metadata:
  name: example-resourcegroupsgroup
  namespace: default
spec:
  configuration:
  - parameters:
    - name: example-resourcegroupsgroup
      values:
      - example
    type: example
  description: Managed by konfig-konector
  name: example-resourcegroupsgroup
  providerRef:
    name: example-resourcegroupsgroup
    region: us-east-1
  resourceQuery:
    query:
      resourceTypeFilters:
      - example
      stackIdentifier: example
      tagFilters:
      - key: example
        values:
        - example
    type: TAG_FILTERS_1_0
  resources:
  - example
  tags:
  - key: example
    value: example
