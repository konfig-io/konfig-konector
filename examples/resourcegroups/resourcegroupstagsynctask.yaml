# ResourceGroupsTagSyncTask — Resource Groups
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/resourcegroups.html#resourcegroupstagsynctask
apiVersion: aws.konfig.io/v1alpha1
kind: ResourceGroupsTagSyncTask
metadata:
  name: example-resourcegroupstagsynctask
  namespace: default
spec:
  group: example-xxxx
  providerRef:
    name: example-resourcegroupstagsynctask
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tagKey: example
  tagValue: example
