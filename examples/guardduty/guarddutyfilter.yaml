# GuardDutyFilter — GuardDuty
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/guardduty.html#guarddutyfilter
apiVersion: aws.konfig.io/v1alpha1
kind: GuardDutyFilter
metadata:
  name: example-guarddutyfilter
  namespace: default
spec:
  action: example
  description: Managed by konfig-konector
  detectorId: example-id
  findingCriteria:
    criterion: {}
  name: example-guarddutyfilter
  providerRef:
    name: example-guarddutyfilter
    region: us-east-1
  rank: 1
  tags:
  - key: example
    value: example
