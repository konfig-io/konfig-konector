# GuardDutyMaster — GuardDuty
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/guardduty.html#guarddutymaster
apiVersion: aws.konfig.io/v1alpha1
kind: GuardDutyMaster
metadata:
  name: example-guarddutymaster
  namespace: default
spec:
  detectorId: example-id
  masterId: example-id
  providerRef:
    name: example-guarddutymaster
    region: us-east-1
