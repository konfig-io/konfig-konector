# GuardDutyPublishingDestination — GuardDuty
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/guardduty.html#guarddutypublishingdestination
apiVersion: aws.konfig.io/v1alpha1
kind: GuardDutyPublishingDestination
metadata:
  name: example-guarddutypublishingdestination
  namespace: default
spec:
  destinationProperties:
    destinationArn: arn:aws:iam::123456789012:role/example-role
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  destinationType: example
  detectorId: example-id
  providerRef:
    name: example-guarddutypublishingdestination
    region: us-east-1
  tags:
  - key: example
    value: example
