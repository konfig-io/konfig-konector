# S3TablesTablePolicy — s3tables
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3tables.html#s3tablestablepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: S3TablesTablePolicy
metadata:
  name: example-s3tablestablepolicy
  namespace: default
spec:
  providerRef:
    name: example-s3tablestablepolicy
    region: us-east-1
  tableARN: arn:aws:iam::123456789012:role/example-role
