# S3TablesTableBucketPolicy — S3 Tables
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3tables.html#s3tablestablebucketpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: S3TablesTableBucketPolicy
metadata:
  name: example-s3tablestablebucketpolicy
  namespace: default
spec:
  providerRef:
    name: example-s3tablestablebucketpolicy
    region: us-east-1
  tableBucketARN: arn:aws:iam::123456789012:role/example-role
