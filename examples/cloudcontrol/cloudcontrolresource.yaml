# CloudControlResource — Cloud Control API
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudcontrol.html#cloudcontrolresource
apiVersion: aws.konfig.io/v1alpha1
kind: CloudControlResource
metadata:
  name: example-cloudcontrolresource
  namespace: default
spec:
  identifier: example
  providerRef:
    name: example-cloudcontrolresource
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  typeName: example-cloudcontrolresource
