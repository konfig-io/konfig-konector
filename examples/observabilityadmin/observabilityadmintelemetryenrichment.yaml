# ObservabilityAdminTelemetryEnrichment — Observability Admin
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/observabilityadmin.html#observabilityadmintelemetryenrichment
apiVersion: aws.konfig.io/v1alpha1
kind: ObservabilityAdminTelemetryEnrichment
metadata:
  name: example-observabilityadmintelemetryenrichment
  namespace: default
spec:
  providerRef:
    name: example-observabilityadmintelemetryenrichment
    region: us-east-1
  scope: ACCOUNT
