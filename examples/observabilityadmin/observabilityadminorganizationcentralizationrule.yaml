# ObservabilityAdminOrganizationCentralizationRule — Observability Admin
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/observabilityadmin.html#observabilityadminorganizationcentralizationrule
apiVersion: aws.konfig.io/v1alpha1
kind: ObservabilityAdminOrganizationCentralizationRule
metadata:
  name: example-observabilityadminorganizationcentralizationrule
  namespace: default
spec:
  providerRef:
    name: example-observabilityadminorganizationcentralizationrule
    region: us-east-1
  rule:
    destination:
      account: example
      region: us-east-1
    source:
      regions:
      - example
      scope: example
      sourceLogsConfiguration:
        dataSourceSelectionCriteria: example
        encryptedLogGroupStrategy: ALLOW
        logGroupSelectionCriteria: example
      sourceMetricsConfiguration:
        metricsSelectionCriteria: example
  ruleName: example-observabilityadminorganizationcentralizationrule
  tags:
  - key: example
    value: example
