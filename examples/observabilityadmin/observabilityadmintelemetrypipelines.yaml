# ObservabilityAdminTelemetryPipelines — Observability Admin
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/observabilityadmin.html#observabilityadmintelemetrypipelines
apiVersion: aws.konfig.io/v1alpha1
kind: ObservabilityAdminTelemetryPipelines
metadata:
  name: example-observabilityadmintelemetrypipelines
  namespace: default
spec:
  configuration:
    body: example
  name: example-observabilityadminte
  providerRef:
    name: example-observabilityadmintelemetrypipelines
    region: us-east-1
  tags:
  - key: example
    value: example
