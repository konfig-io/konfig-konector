# EventSchemasRegistryPolicy — EventBridge Schemas
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventschemas.html#eventschemasregistrypolicy
apiVersion: aws.konfig.io/v1alpha1
kind: EventSchemasRegistryPolicy
metadata:
  name: example-eventschemasregistrypolicy
  namespace: default
spec:
  providerRef:
    name: example-eventschemasregistrypolicy
    region: us-east-1
  registryName: example-eventschemasregistrypolicy
