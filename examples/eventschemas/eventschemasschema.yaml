# EventSchemasSchema — eventschemas
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventschemas.html#eventschemasschema
apiVersion: aws.konfig.io/v1alpha1
kind: EventSchemasSchema
metadata:
  name: example-eventschemasschema
  namespace: default
spec:
  content: example
  description: Managed by konfig-konector
  providerRef:
    name: example-eventschemasschema
    region: us-east-1
  registryName: example-eventschemasschema
  schemaName: example-eventschemasschema
  tags:
  - key: example
    value: example
  type: example
