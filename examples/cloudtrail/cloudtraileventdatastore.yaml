# CloudTrailEventDataStore — CloudTrail
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudtrail.html#cloudtraileventdatastore
apiVersion: aws.konfig.io/v1alpha1
kind: CloudTrailEventDataStore
metadata:
  name: example-cloudtraileventdatastore
  namespace: default
spec:
  advancedEventSelectors:
  - fieldSelectors:
    - endsWith:
      - example
      equals:
      - example
      field: example
      notEndsWith:
      - example
      notEquals:
      - example
      notStartsWith:
      - example
      startsWith:
      - example
    name: example-cloudtraileventdatastore
  billingMode: example
  contextKeySelectors:
  - equals:
    - example
    type: RequestContext
  federationEnabled: true
  federationRoleArn: arn:aws:iam::123456789012:role/example-role
  ingestionEnabled: true
  insightSelectors:
  - insightType: example
  insightsDestination: example
  kmsKeyId: arn:aws:kms:us-east-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab
  maxEventSize: Standard
  multiRegionEnabled: true
  name: example-cloudtraileventdatastore
  organizationEnabled: true
  providerRef:
    name: example-cloudtraileventdatastore
    region: us-east-1
  retentionPeriod: 7
  tags:
  - key: example
    value: example
  terminationProtectionEnabled: true
