# CloudTrailChannel — CloudTrail
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudtrail.html#cloudtrailchannel
apiVersion: aws.konfig.io/v1alpha1
kind: CloudTrailChannel
metadata:
  name: example-cloudtrailchannel
  namespace: default
spec:
  destinations:
  - location: example
    type: EVENT_DATA_STORE
  name: example-cloudtrailchannel
  providerRef:
    name: example-cloudtrailchannel
    region: us-east-1
  source: example
  tags:
  - key: example
    value: example
