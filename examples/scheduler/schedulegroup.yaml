# ScheduleGroup — EventBridge Scheduler
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/scheduler.html#schedulegroup
apiVersion: aws.konfig.io/v1alpha1
kind: ScheduleGroup
metadata:
  name: example-schedulegroup
  namespace: default
spec:
  name: example-schedulegroup
  tags:
    environment: dev
    team: platform
