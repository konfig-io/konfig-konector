# SSMIncidentsReplicationSet — ssmincidents
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssmincidents.html#ssmincidentsreplicationset
apiVersion: aws.konfig.io/v1alpha1
kind: SSMIncidentsReplicationSet
metadata:
  name: example-ssmincidentsreplicationset
  namespace: default
spec:
  deletionProtected: true
  providerRef:
    name: example-ssmincidentsreplicationset
    region: us-east-1
  regions:
  - regionConfiguration:
      sseKmsKeyId: arn:aws:kms:us-east-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab
    regionName: example-ssmincidents
  tags:
  - key: example
    value: example
