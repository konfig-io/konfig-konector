# SSMIncidentsResponsePlan — ssmincidents
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssmincidents.html#ssmincidentsresponseplan
apiVersion: aws.konfig.io/v1alpha1
kind: SSMIncidentsResponsePlan
metadata:
  name: example-ssmincidentsresponseplan
  namespace: default
spec:
  actions:
  - ssmAutomation:
      documentName: example-ssmincidentsresponseplan
      documentVersion: '1.0'
      dynamicParameters:
      - key: example
        value:
          variable: INCIDENT_RECORD_ARN
      parameters:
      - key: example
        values:
        - example
      roleArn: arn:aws:iam::123456789012:role/example-role
      targetAccount: IMPACTED_ACCOUNT
  chatChannel:
    chatbotSns:
    - example
  displayName: example-ssmincidentsresponseplan
  engagements:
  - example
  incidentTemplate:
    dedupeString: example
    impact: 1
    incidentTags:
    - key: example
      value: example
    notificationTargets:
    - snsTopicArn: arn:aws:iam::123456789012:role/example-role
    summary: example
    title: example
  integrations:
  - pagerDutyConfiguration:
      name: example-ssmincidentsresponseplan
      pagerDutyIncidentConfiguration:
        serviceId: example-id
      secretId: example-id
  name: example-ssmincidentsresponseplan
  providerRef:
    name: example-ssmincidentsresponseplan
    region: us-east-1
  tags:
  - key: example
    value: example
