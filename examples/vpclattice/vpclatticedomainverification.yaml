# VpcLatticeDomainVerification — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#vpclatticedomainverification
apiVersion: aws.konfig.io/v1alpha1
kind: VpcLatticeDomainVerification
metadata:
  name: example-vpclatticedomainverification
  namespace: default
spec:
  domainName: example-vpclatticedomainverification
  providerRef:
    name: example-vpclatticedomainverification
    region: us-east-1
  tags:
  - key: example
    value: example
