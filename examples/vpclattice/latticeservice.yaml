# LatticeService — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#latticeservice
apiVersion: aws.konfig.io/v1alpha1
kind: LatticeService
metadata:
  name: example-latticeservice
  namespace: default
spec:
  authType: NONE
  certificateArn: arn:aws:iam::123456789012:role/example-role
  customDomainName: example-latticeservice
  name: example-latticeservice
  tags:
    environment: dev
    team: platform
