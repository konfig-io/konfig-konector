# VpcLatticeRule — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#vpclatticerule
apiVersion: aws.konfig.io/v1alpha1
kind: VpcLatticeRule
metadata:
  name: example-vpclatticerule
  namespace: default
spec:
  action:
    fixedResponse:
      statusCode: 100
    forward:
      targetGroups:
      - targetGroupIdentifier: example-xxxxxxxxxxxx
        weight: 1
  listenerIdentifier: example
  match:
    httpMatch:
      headerMatches:
      - caseSensitive: false
        match:
          contains: example
          exact: example
          prefix: example
        name: example-vpclatticerule
      method: CONNECT
      pathMatch:
        caseSensitive: false
        match:
          exact: example
          prefix: example
  name: example-vpclatticerule
  priority: 1
  providerRef:
    name: example-vpclatticerule
    region: us-east-1
  serviceIdentifier: example
  tags:
  - key: example
    value: example
