# LatticeServiceNetworkVpcAssociation — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#latticeservicenetworkvpcassociation
apiVersion: aws.konfig.io/v1alpha1
kind: LatticeServiceNetworkVpcAssociation
metadata:
  name: example-latticeservicenetworkvpcassociation
  namespace: default
spec:
  securityGroupRefs:
  - name: example-securitygrouprefs
  serviceNetworkRef:
    name: example-servicenetwork
  tags:
    environment: dev
    team: platform
  vpcRef:
    name: example-vpc
