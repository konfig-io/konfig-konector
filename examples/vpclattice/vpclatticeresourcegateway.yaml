# VpcLatticeResourceGateway — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#vpclatticeresourcegateway
apiVersion: aws.konfig.io/v1alpha1
kind: VpcLatticeResourceGateway
metadata:
  name: example-vpclatticeresourcegateway
  namespace: default
spec:
  ipAddressType: IPV4
  ipv4AddressesPerEni: 1
  name: example-vpclatticeresourcegateway
  providerRef:
    name: example-vpclatticeresourcegateway
    region: us-east-1
  resourceConfigDnsResolution: IN_VPC
  subnetIds:
  - example
  tags:
  - key: example
    value: example
  vpcIdentifier: example
