# VpcLatticeAuthPolicy — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#vpclatticeauthpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: VpcLatticeAuthPolicy
metadata:
  name: example-vpclatticeauthpolicy
  namespace: default
spec:
  providerRef:
    name: example-vpclatticeauthpolicy
    region: us-east-1
  resourceIdentifier: example-xxxxxxxxx
