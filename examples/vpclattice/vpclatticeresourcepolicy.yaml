# VpcLatticeResourcePolicy — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#vpclatticeresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: VpcLatticeResourcePolicy
metadata:
  name: example-vpclatticeresourcepolicy
  namespace: default
spec:
  providerRef:
    name: example-vpclatticeresourcepolicy
    region: us-east-1
  resourceArn: arn:aws:iam::123456789012:role/example-role
