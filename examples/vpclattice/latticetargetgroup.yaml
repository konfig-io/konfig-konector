# LatticeTargetGroup — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#latticetargetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: LatticeTargetGroup
metadata:
  name: example-latticetargetgroup
  namespace: default
spec:
  config:
    healthCheck:
      path: example
      protocol: HTTP
    port: 1
    protocol: HTTP
    vpcRef:
      name: example-vpc
  name: example-latticetargetgroup
  tags:
    environment: dev
    team: platform
  type: INSTANCE
