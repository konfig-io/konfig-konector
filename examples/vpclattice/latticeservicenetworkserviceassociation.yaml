# LatticeServiceNetworkServiceAssociation — VPC Lattice
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/vpclattice.html#latticeservicenetworkserviceassociation
apiVersion: aws.konfig.io/v1alpha1
kind: LatticeServiceNetworkServiceAssociation
metadata:
  name: example-latticeservicenetworkserviceassociation
  namespace: default
spec:
  serviceNetworkRef:
    name: example-servicenetwork
  serviceRef:
    name: example-service
  tags:
    environment: dev
    team: platform
