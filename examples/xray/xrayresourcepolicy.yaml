# XRayResourcePolicy — X-Ray
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/xray.html#xrayresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: XRayResourcePolicy
metadata:
  name: example-xrayresourcepolicy
  namespace: default
spec:
  bypassPolicyLockoutCheck: false
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  policyName: example-xrayresourcepolicy
  providerRef:
    name: example-xrayresourcepolicy
    region: us-east-1
