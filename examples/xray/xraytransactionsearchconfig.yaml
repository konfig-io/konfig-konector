# XRayTransactionSearchConfig — X-Ray
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/xray.html#xraytransactionsearchconfig
apiVersion: aws.konfig.io/v1alpha1
kind: XRayTransactionSearchConfig
metadata:
  name: example-xraytransactionsearchconfig
  namespace: default
spec:
  indexingPercentage: 1
  providerRef:
    name: example-xraytransactionsearchconfig
    region: us-east-1
