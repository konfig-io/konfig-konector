# CECostCategory — Cost Explorer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/costexplorer.html#cecostcategory
apiVersion: aws.konfig.io/v1alpha1
kind: CECostCategory
metadata:
  name: example-cecostcategory
  namespace: default
spec:
  defaultValue: example
  name: example-cecostcategory
  providerRef:
    name: example-cecostcategory
    region: us-east-1
  ruleVersion: CostCategoryExpression.v1
  rules: example
  splitChargeRules: example
  tags:
  - key: example
    value: example
