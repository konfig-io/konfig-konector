# Route53RecoveryControlSafetyRule — Route 53 ARC Control
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/route53recoverycontrol.html#route53recoverycontrolsafetyrule
apiVersion: aws.konfig.io/v1alpha1
kind: Route53RecoveryControlSafetyRule
metadata:
  name: example-route53recoverycontrolsafetyrule
  namespace: default
spec:
  assertionRule:
    assertedControls:
    - example
    waitPeriodMs: 1
  controlPanelArn: arn:aws:iam::123456789012:role/example-role
  gatingRule:
    gatingControls:
    - example
    targetControls:
    - example
    waitPeriodMs: 1
  name: example-route53recoverycontrolsafetyrule
  providerRef:
    name: example-route53recoverycontrolsafetyrule
    region: us-east-1
  ruleConfig:
    inverted: false
    threshold: 1
    type: AND
  tags:
  - key: example
    value: example
