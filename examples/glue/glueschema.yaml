# GlueSchema — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#glueschema
apiVersion: aws.konfig.io/v1alpha1
kind: GlueSchema
metadata:
  name: example-glueschema
  namespace: default
spec:
  checkpointVersion:
    isLatest: false
    versionNumber: 1
  compatibility: NONE
  dataFormat: AVRO
  description: Managed by konfig-konector
  name: example-glueschema
  providerRef:
    name: example-glueschema
    region: us-east-1
  registry:
    name: example-registry
  schemaDefinition: example
  tags:
  - key: example
    value: example
