# GlueJob — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#gluejob
apiVersion: aws.konfig.io/v1alpha1
kind: GlueJob
metadata:
  name: example-gluejob
  namespace: default
spec:
  command:
    name: example-gluejob
    pythonVersion: '1.0'
    scriptLocation: example
  defaultArguments:
    key: value
  description: Managed by konfig-konector
  glueVersion: '1.0'
  maxRetries: 1
  name: example-gluejob
  numberOfWorkers: 1
  providerRef:
    name: example-gluejob
    region: us-east-1
  roleRef:
    name: example-role
  tags:
    environment: dev
    team: platform
  timeout: 1
  workerType: Standard
