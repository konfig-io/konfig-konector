# GlueSchemaVersionMetadata — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#glueschemaversionmetadata
apiVersion: aws.konfig.io/v1alpha1
kind: GlueSchemaVersionMetadata
metadata:
  name: example-glueschemaversionmetadata
  namespace: default
spec:
  key: example
  providerRef:
    name: example-glueschemaversionmetadata
    region: us-east-1
  schemaVersionId: example-id
  value: example
