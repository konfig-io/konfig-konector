# GlueCustomEntityType — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#gluecustomentitytype
apiVersion: aws.konfig.io/v1alpha1
kind: GlueCustomEntityType
metadata:
  name: example-gluecustomentitytype
  namespace: default
spec:
  contextWords:
  - example
  name: example-gluecustomentitytype
  providerRef:
    name: example-gluecustomentitytype
    region: us-east-1
  regexString: example
