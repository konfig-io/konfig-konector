# GlueCrawler — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#gluecrawler
apiVersion: aws.konfig.io/v1alpha1
kind: GlueCrawler
metadata:
  name: example-gluecrawler
  namespace: default
spec:
  configuration: example
  databaseName: example-gluecrawler
  databaseRef: example
  description: Managed by konfig-konector
  name: example-gluecrawler
  roleRef:
    name: example-role
  schedule: example
  schemaChangePolicy:
    deleteBehavior: LOG
    updateBehavior: LOG
  tablePrefix: example
  tags:
    environment: dev
    team: platform
  targets:
    jdbcTargets:
    - connectionName: example-gluecrawler
      exclusions:
      - example
      path: example
    s3Targets:
    - exclusions:
      - example
      path: example
