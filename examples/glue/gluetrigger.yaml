# GlueTrigger — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#gluetrigger
apiVersion: aws.konfig.io/v1alpha1
kind: GlueTrigger
metadata:
  name: example-gluetrigger
  namespace: default
spec:
  actions:
  - arguments:
      key: value
    jobName: example-gluetrigger
  description: Managed by konfig-konector
  name: example-gluetrigger
  predicate:
    conditions:
    - crawlerName: example-gluetrigger
      jobName: example-gluetrigger
      logicalOperator: EQUALS
      state: example
    logical: AND
  schedule: example
  startOnCreation: false
  tags:
    environment: dev
    team: platform
  type: SCHEDULED
