# GlueIdentityCenterConfiguration — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#glueidentitycenterconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: GlueIdentityCenterConfiguration
metadata:
  name: example-glueidentitycenterconfiguration
  namespace: default
spec:
  instanceArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-glueidentitycenterconfiguration
    region: us-east-1
  scopes:
  - example
  userBackgroundSessionsEnabled: true
