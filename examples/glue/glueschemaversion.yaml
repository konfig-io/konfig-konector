# GlueSchemaVersion — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#glueschemaversion
apiVersion: aws.konfig.io/v1alpha1
kind: GlueSchemaVersion
metadata:
  name: example-glueschemaversion
  namespace: default
spec:
  providerRef:
    name: example-glueschemaversion
    region: us-east-1
  schema:
    registryName: example-glueschemaversion
    schemaArn: arn:aws:iam::123456789012:role/example-role
    schemaName: example-glueschemaversion
  schemaDefinition: example
