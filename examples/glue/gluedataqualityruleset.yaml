# GlueDataQualityRuleset — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#gluedataqualityruleset
apiVersion: aws.konfig.io/v1alpha1
kind: GlueDataQualityRuleset
metadata:
  name: example-gluedataqualityruleset
  namespace: default
spec:
  clientToken: example
  description: Managed by konfig-konector
  name: example-gluedataqualityruleset
  providerRef:
    name: example-gluedataqualityruleset
    region: us-east-1
  ruleset: example
  tags:
    environment: dev
    team: platform
  targetTable:
    databaseName: example-gluedataqualityruleset
    tableName: example-gluedataqualityruleset
