# GlueBlueprint — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#glueblueprint
apiVersion: aws.konfig.io/v1alpha1
kind: GlueBlueprint
metadata:
  name: example-glueblueprint
  namespace: default
spec:
  blueprintLocation: example
  description: Managed by konfig-konector
  name: example-glueblueprint
  providerRef:
    name: example-glueblueprint
    region: us-east-1
  tags:
  - key: example
    value: example
