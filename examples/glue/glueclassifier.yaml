# GlueClassifier — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#glueclassifier
apiVersion: aws.konfig.io/v1alpha1
kind: GlueClassifier
metadata:
  name: example-glueclassifier
  namespace: default
spec:
  csvClassifier:
    allowSingleColumn: false
    containsCustomDatatype:
    - example
    containsHeader: example
    customDatatypeConfigured: false
    delimiter: example
    disableValueTrimming: false
    header:
    - example
    name: example-glueclassifier
    quoteSymbol: example
  grokClassifier:
    classification: example
    customPatterns: example
    grokPattern: example
    name: example-glueclassifier
  jsonClassifier:
    jsonPath: example
    name: example-glueclassifier
  providerRef:
    name: example-glueclassifier
    region: us-east-1
  xmlClassifier:
    classification: example
    name: example-glueclassifier
    rowTag: example
