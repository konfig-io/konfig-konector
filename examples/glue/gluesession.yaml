# GlueSession — Glue
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/glue.html#gluesession
apiVersion: aws.konfig.io/v1alpha1
kind: GlueSession
metadata:
  name: example-gluesession
  namespace: default
spec:
  command:
    name: example-gluesession
    pythonVersion: '1.0'
  connections:
    connections:
    - example
  defaultArguments:
    key: value
  description: Managed by konfig-konector
  glueVersion: '1.0'
  id: example-id
  idleTimeout: 300
  maxCapacity: 2
  numberOfWorkers: 1
  providerRef:
    name: example-gluesession
    region: us-east-1
  requestOrigin: example
  role: example-xxxxxxxxxxxx
  securityConfiguration: example
  tags:
  - key: example
    value: example
  timeout: 1
  workerType: Standard
