# InspectorResourceGroup — inspector
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/inspector.html#inspectorresourcegroup
apiVersion: aws.konfig.io/v1alpha1
kind: InspectorResourceGroup
metadata:
  name: example-inspectorresourcegroup
  namespace: default
spec:
  providerRef:
    name: example-inspectorresourcegroup
    region: us-east-1
  resourceGroupTags:
  - key: example
    value: example
