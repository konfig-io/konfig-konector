# SecretRotation — Secrets Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/secretsmanager.html#secretrotation
apiVersion: aws.konfig.io/v1alpha1
kind: SecretRotation
metadata:
  name: example-secretrotation
  namespace: default
spec:
  automaticallyAfterDays: 1
  providerRef:
    name: example-secretrotation
    region: us-east-1
  rotationLambdaArn: arn:aws:iam::123456789012:role/example-role
  secretRef:
    name: example-secretrotation
    secretArn: arn:aws:iam::123456789012:role/example-role
