# AutoScalingScheduledAction — Auto Scaling
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/autoscaling.html#autoscalingscheduledaction
apiVersion: aws.konfig.io/v1alpha1
kind: AutoScalingScheduledAction
metadata:
  name: example-autoscalingscheduledaction
  namespace: default
spec:
  autoScalingGroupName: example-autoscalingscheduledaction
  desiredCapacity: 2
  endTime: '2026-01-01T00:00:00Z'
  maxSize: 2
  minSize: 2
  providerRef:
    name: example-autoscalingscheduledaction
    region: us-east-1
  recurrence: example
  startTime: '2026-01-01T00:00:00Z'
  timeZone: example
