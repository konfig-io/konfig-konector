# DocDBEventSubscription — DocumentDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/docdb.html#docdbeventsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: DocDBEventSubscription
metadata:
  name: example-docdbeventsubscription
  namespace: default
spec:
  enabled: true
  eventCategories:
  - example
  providerRef:
    name: example-docdbeventsubscription
    region: us-east-1
  snsTopicArn: arn:aws:iam::123456789012:role/example-role
  sourceIds:
  - example
  sourceType: db-instance
  subscriptionName: example-docdbeventsubscription
