# DocDBDBSubnetGroup — DocumentDB
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/docdb.html#docdbdbsubnetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: DocDBDBSubnetGroup
metadata:
  name: example-docdbdbsubnetgroup
  namespace: default
spec:
  dbSubnetGroupDescription: Managed by konfig-konector
  dbSubnetGroupName: example-docdbdbsubnetgroup
  providerRef:
    name: example-docdbdbsubnetgroup
    region: us-east-1
  subnetIds:
  - example
  tags:
  - key: example
    value: example
