# SESReceiptRule — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesreceiptrule
apiVersion: aws.konfig.io/v1alpha1
kind: SESReceiptRule
metadata:
  name: example-sesreceiptrule
  namespace: default
spec:
  after: example
  providerRef:
    name: example-sesreceiptrule
    region: us-east-1
  rule:
    enabled: true
    name: example-sesreceiptrule
    recipients:
    - example
    scanEnabled: true
    tlsPolicy: example
  ruleSetName: example-sesreceiptrule
