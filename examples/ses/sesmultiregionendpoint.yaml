# SESMultiRegionEndpoint — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmultiregionendpoint
apiVersion: aws.konfig.io/v1alpha1
kind: SESMultiRegionEndpoint
metadata:
  name: example-sesmultiregionendpoint
  namespace: default
spec:
  details:
    routeDetails:
    - region: us-east-1
  endpointName: example-sesmultiregionendpoint
  providerRef:
    name: example-sesmultiregionendpoint
    region: us-east-1
  tags:
  - key: example
    value: example
