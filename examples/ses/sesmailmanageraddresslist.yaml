# SESMailManagerAddressList — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanageraddresslist
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerAddressList
metadata:
  name: example-sesmailmanageraddresslist
  namespace: default
spec:
  addressListName: example-sesmailmanageraddresslist
  providerRef:
    name: example-sesmailmanageraddresslist
    region: us-east-1
  tags:
  - key: example
    value: example
