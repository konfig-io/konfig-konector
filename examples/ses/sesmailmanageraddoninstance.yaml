# SESMailManagerAddonInstance — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanageraddoninstance
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerAddonInstance
metadata:
  name: example-sesmailmanageraddoninstance
  namespace: default
spec:
  addonSubscriptionId: example-id
  providerRef:
    name: example-sesmailmanageraddoninstance
    region: us-east-1
  tags:
  - key: example
    value: example
