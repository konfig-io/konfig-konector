# SESTemplate — ses
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sestemplate
apiVersion: aws.konfig.io/v1alpha1
kind: SESTemplate
metadata:
  name: example-sestemplate
  namespace: default
spec:
  providerRef:
    name: example-sestemplate
    region: us-east-1
  tags:
  - key: example
    value: example
  template:
    htmlPart: example
    subjectPart: example
    templateName: example-sestemplate
    textPart: example
