# SESContactList — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sescontactlist
apiVersion: aws.konfig.io/v1alpha1
kind: SESContactList
metadata:
  name: example-sescontactlist
  namespace: default
spec:
  contactListName: example-sescontactlist
  description: Managed by konfig-konector
  providerRef:
    name: example-sescontactlist
    region: us-east-1
  tags:
  - key: example
    value: example
  topics:
  - defaultSubscriptionStatus: example
    description: Managed by konfig-konector
    displayName: example-sescontactlist
    topicName: example-sescontactlist
