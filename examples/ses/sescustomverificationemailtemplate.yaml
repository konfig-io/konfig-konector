# SESCustomVerificationEmailTemplate — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sescustomverificationemailtemplate
apiVersion: aws.konfig.io/v1alpha1
kind: SESCustomVerificationEmailTemplate
metadata:
  name: example-sescustomverificationemailtemplate
  namespace: default
spec:
  failureRedirectionURL: example
  fromEmailAddress: ops@example.com
  providerRef:
    name: example-sescustomverificationemailtemplate
    region: us-east-1
  successRedirectionURL: example
  tags:
  - key: example
    value: example
  templateContent: example
  templateName: example-sescustomverificationemailtemplate
  templateSubject: example
