# SESMailManagerRuleSet — ses
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanagerruleset
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerRuleSet
metadata:
  name: example-sesmailmanagerruleset
  namespace: default
spec:
  providerRef:
    name: example-sesmailmanagerruleset
    region: us-east-1
  ruleSetName: example-sesmailmanagerruleset
  rules:
  - name: example-sesmailmanagerruleset
  tags:
  - key: example
    value: example
