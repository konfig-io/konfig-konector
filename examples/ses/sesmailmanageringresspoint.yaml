# SESMailManagerIngressPoint — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanageringresspoint
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerIngressPoint
metadata:
  name: example-sesmailmanageringresspoint
  namespace: default
spec:
  ingressPointName: example-sesmailmanageringresspoint
  providerRef:
    name: example-sesmailmanageringresspoint
    region: us-east-1
  ruleSetId: example-id
  statusToUpdate: ACTIVE
  tags:
  - key: example
    value: example
  tlsPolicy: REQUIRED
  trafficPolicyId: example-id
  type: OPEN
