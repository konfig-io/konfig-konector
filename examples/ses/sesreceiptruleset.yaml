# SESReceiptRuleSet — ses
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesreceiptruleset
apiVersion: aws.konfig.io/v1alpha1
kind: SESReceiptRuleSet
metadata:
  name: example-sesreceiptruleset
  namespace: default
spec:
  providerRef:
    name: example-sesreceiptruleset
    region: us-east-1
  ruleSetName: example-sesreceiptruleset
