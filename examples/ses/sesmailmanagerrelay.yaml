# SESMailManagerRelay — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanagerrelay
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerRelay
metadata:
  name: example-sesmailmanagerrelay
  namespace: default
spec:
  providerRef:
    name: example-sesmailmanagerrelay
    region: us-east-1
  relayName: example-sesmailmanagerrelay
  serverName: example-sesmailmanagerrelay
  serverPort: 443
  tags:
  - key: example
    value: example
