# SESDedicatedIpPool — ses
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesdedicatedippool
apiVersion: aws.konfig.io/v1alpha1
kind: SESDedicatedIpPool
metadata:
  name: example-sesdedicatedippool
  namespace: default
spec:
  poolName: example-sesdedicatedippool
  providerRef:
    name: example-sesdedicatedippool
    region: us-east-1
  scalingMode: example
  tags:
  - key: example
    value: example
