# SESMailManagerAddonSubscription — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanageraddonsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerAddonSubscription
metadata:
  name: example-sesmailmanageraddonsubscription
  namespace: default
spec:
  addonName: example-sesmailmanageraddonsubscription
  providerRef:
    name: example-sesmailmanageraddonsubscription
    region: us-east-1
  tags:
  - key: example
    value: example
