# SESMailManagerTrafficPolicy — ses
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ses.html#sesmailmanagertrafficpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: SESMailManagerTrafficPolicy
metadata:
  name: example-sesmailmanagertrafficpolicy
  namespace: default
spec:
  defaultAction: ALLOW
  maxMessageSizeBytes: 2
  policyStatements:
  - action: ALLOW
  providerRef:
    name: example-sesmailmanagertrafficpolicy
    region: us-east-1
  tags:
  - key: example
    value: example
  trafficPolicyName: example-sesmailmanagertrafficpolicy
