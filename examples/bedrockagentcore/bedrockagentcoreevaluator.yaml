# BedrockAgentCoreEvaluator — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoreevaluator
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreEvaluator
metadata:
  name: example-bedrockagentcoreevaluator
  namespace: default
spec:
  description: Managed by konfig-konector
  evaluatorConfig:
    codeBased:
      lambdaConfig:
        lambdaArn: arn:aws:iam::123456789012:role/example-role
        lambdaTimeoutInSeconds: 1
    llmAsAJudge:
      instructions: example
      ratingScale:
        categorical:
        - definition: example
          label: example
        numerical:
        - definition: example
          label: example
          value: 1
  evaluatorName: example-bedrockagentcoreevaluator
  kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  level: TOOL_CALL
  providerRef:
    name: example-bedrockagentcoreevaluator
    region: us-east-1
  tags:
  - key: example
    value: example
