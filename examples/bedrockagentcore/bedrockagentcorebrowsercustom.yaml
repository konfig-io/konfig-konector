# BedrockAgentCoreBrowserCustom — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcorebrowsercustom
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreBrowserCustom
metadata:
  name: example-bedrockagentcorebrowsercustom
  namespace: default
spec:
  browserSigning:
    enabled: true
  certificates:
  - certificateLocation:
      secretArn: arn:aws:iam::123456789012:role/example-role
  description: Managed by konfig-konector
  enterprisePolicies:
  - location:
      bucket: example
      prefix: example
    type: MANAGED
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  name: example-bedrockagentcorebrowsercustom
  networkConfiguration:
    networkMode: PUBLIC
    vpcConfig:
      securityGroups:
      - example
      subnets:
      - example
  providerRef:
    name: example-bedrockagentcorebrowsercustom
    region: us-east-1
  recordingConfig:
    enabled: true
    s3Location:
      bucket: example
      prefix: example
  tags:
    environment: dev
    team: platform
