# BedrockAgentCoreGatewayRule — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoregatewayrule
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreGatewayRule
metadata:
  name: example-bedrockagentcoregatewayrule
  namespace: default
spec:
  description: Managed by konfig-konector
  gatewayIdentifier: example
  priority: 1
  providerRef:
    name: example-bedrockagentcoregatewayrule
    region: us-east-1
