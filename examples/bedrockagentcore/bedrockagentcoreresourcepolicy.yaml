# BedrockAgentCoreResourcePolicy — Bedrock AgentCore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoreresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreResourcePolicy
metadata:
  name: example-bedrockagentcoreresourcepolicy
  namespace: default
spec:
  policy: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"], "Resource":
    "*"}]}'
  providerRef:
    name: example-bedrockagentcoreresourcepolicy
    region: us-east-1
  resourceArn: arn:aws:iam::123456789012:role/example-role
