# BedrockAgentCoreCodeInterpreterCustom — Bedrock AgentCore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcorecodeinterpretercustom
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreCodeInterpreterCustom
metadata:
  name: example-bedrockagentcorecodeinterpretercustom
  namespace: default
spec:
  certificates:
  - certificateLocation:
      secretArn: arn:aws:iam::123456789012:role/example-role
  description: Managed by konfig-konector
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  name: example-bedrockagentcorecodeinterpretercustom
  networkConfiguration:
    networkMode: PUBLIC
    vpcConfig:
      securityGroups:
      - example
      subnets:
      - example
  providerRef:
    name: example-bedrockagentcorecodeinterpretercustom
    region: us-east-1
  tags:
    environment: dev
    team: platform
