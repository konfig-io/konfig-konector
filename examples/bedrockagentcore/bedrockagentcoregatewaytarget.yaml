# BedrockAgentCoreGatewayTarget — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoregatewaytarget
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreGatewayTarget
metadata:
  name: example-bedrockagentcoregatewaytarget
  namespace: default
spec:
  credentialProviderConfigurations:
  - credentialProviderType: GATEWAY_IAM_ROLE
  description: Managed by konfig-konector
  gatewayIdentifier: example
  metadataConfiguration:
    allowedQueryParameters:
    - example
    allowedRequestHeaders:
    - example
    allowedResponseHeaders:
    - example
  name: example-bedrockagentcoregatewaytarget
  providerRef:
    name: example-bedrockagentcoregatewaytarget
    region: us-east-1
