# BedrockAgentCoreGatewayRateLimit — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoregatewayratelimit
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreGatewayRateLimit
metadata:
  name: example-bedrockagentcoregatewayratelimit
  namespace: default
spec:
  description: Managed by konfig-konector
  dimensionKeys:
  - example
  entries:
  - connections:
    - period: second
      rate: 1
    dimensions:
      key: value
    requests:
    - period: second
      rate: 1
    tokens:
    - period: second
      rate: 1
  gatewayIdentifier: example
  providerRef:
    name: example-bedrockagentcoregatewayratelimit
    region: us-east-1
