# BedrockAgentCoreApiKeyCredentialProvider — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoreapikeycredentialprovider
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreApiKeyCredentialProvider
metadata:
  name: example-bedrockagentcoreapikeycredentialprovider
  namespace: default
spec:
  apiKey: example
  apiKeySecretConfig:
    jsonKey: example
    secretId: example-id
  apiKeySecretSource: MANAGED
  name: example-bedrockagentcoreapikeycredentialprovider
  providerRef:
    name: example-bedrockagentcoreapikeycredentialprovider
    region: us-east-1
  tags:
  - key: example
    value: example
