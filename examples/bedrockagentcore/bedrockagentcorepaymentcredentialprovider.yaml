# BedrockAgentCorePaymentCredentialProvider — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcorepaymentcredentialprovider
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCorePaymentCredentialProvider
metadata:
  name: example-bedrockagentcorepaymentcredentialprovider
  namespace: default
spec:
  credentialProviderVendor: CoinbaseCDP
  name: example-bedrockagentcorepaymentcredentialprovider
  providerConfigurationInput:
    coinbaseCdpConfiguration:
      apiKeyId: example-id
      apiKeySecret: example
      apiKeySecretConfig:
        jsonKey: example
        secretId: example-id
      apiKeySecretSource: MANAGED
      walletSecret: example
      walletSecretConfig:
        jsonKey: example
        secretId: example-id
      walletSecretSource: MANAGED
    stripePrivyConfiguration:
      appId: example-id
      appSecret: example
      appSecretConfig:
        jsonKey: example
        secretId: example-id
      appSecretSource: MANAGED
      authorizationId: example-id
      authorizationPrivateKey: example
      authorizationPrivateKeyConfig:
        jsonKey: example
        secretId: example-id
      authorizationPrivateKeySource: MANAGED
  providerRef:
    name: example-bedrockagentcorepaymentcredentialprovider
    region: us-east-1
  tags:
  - key: example
    value: example
