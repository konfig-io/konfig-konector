# BedrockAgentCoreGateway — bedrockagentcore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoregateway
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreGateway
metadata:
  name: example-bedrockagentcoregateway
  namespace: default
spec:
  authorizerType: CUSTOM_JWT
  description: Managed by konfig-konector
  exceptionLevel: DEBUG
  interceptorConfigurations:
  - inputConfiguration:
      passRequestHeaders: false
    interceptionPoints:
    - example
  kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  name: example-bedrockagentcoregateway
  policyEngineConfiguration:
    arn: arn:aws:iam::123456789012:role/example-role
    mode: LOG_ONLY
  providerRef:
    name: example-bedrockagentcoregateway
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
    environment: dev
    team: platform
  wafConfiguration:
    failureMode: FAIL_CLOSE
