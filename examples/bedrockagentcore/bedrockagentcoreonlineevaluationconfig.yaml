# BedrockAgentCoreOnlineEvaluationConfig — Bedrock AgentCore
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrockagentcore.html#bedrockagentcoreonlineevaluationconfig
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentCoreOnlineEvaluationConfig
metadata:
  name: example-bedrockagentcoreonlineevaluationconfig
  namespace: default
spec:
  clusteringConfig:
    frequencies:
    - example
  dataSourceConfig:
    cloudWatchLogs:
      logGroupNames:
      - example
      serviceNames:
      - example
  description: Managed by konfig-konector
  evaluationExecutionRoleArn: arn:aws:iam::123456789012:role/example-role
  evaluators:
  - evaluatorId: example-id
  executionStatus: ENABLED
  insights:
  - insightId: example-id
  onlineEvaluationConfigName: example-bedrockagentcoreonlineevaluationconfig
  providerRef:
    name: example-bedrockagentcoreonlineevaluationconfig
    region: us-east-1
  rule:
    filters:
    - key: example
      operator: Equals
      value:
        booleanValue: false
        doubleValue: 1
        stringValue: example
    samplingConfig:
      samplingPercentage: 1
    sessionConfig:
      sessionTimeoutMinutes: 1
  tags:
  - key: example
    value: example
