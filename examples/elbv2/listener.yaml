# Listener — Elastic Load Balancing
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elbv2.html#listener
apiVersion: aws.konfig.io/v1alpha1
kind: Listener
metadata:
  name: example-listener
  namespace: default
spec:
  certificateArns:
  - example
  defaultActions:
  - fixedResponseConfig:
      contentType: example
      messageBody: example
      statusCode: example
    redirectConfig:
      host: example
      path: example
      port: example
      protocol: example
      statusCode: HTTP_301
    targetGroupRef:
      name: example-targetgroup
    type: forward
  loadBalancerRef:
    name: example-loadbalancer
  port: 1
  protocol: HTTP
  providerRef:
    name: example-listener
    region: us-east-1
  sslPolicy: example
  tags:
    environment: dev
    team: platform
