# ListenerRule — Load Balancing
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elbv2.html#listenerrule
apiVersion: aws.konfig.io/v1alpha1
kind: ListenerRule
metadata:
  name: example-listenerrule
  namespace: default
spec:
  actions:
  - fixedResponseConfig:
      contentType: example
      messageBody: example
      statusCode: example
    redirectConfig:
      host: example
      path: example
      port: example
      protocol: example
      statusCode: HTTP_301
    targetGroupRef:
      name: example-targetgroup
    type: forward
  conditions:
  - field: example
    values:
    - example
  listenerRef:
    name: example-listener
  priority: 1
  providerRef:
    name: example-listenerrule
    region: us-east-1
  tags:
    environment: dev
    team: platform
