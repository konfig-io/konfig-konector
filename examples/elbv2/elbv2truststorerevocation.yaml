# ELBv2TrustStoreRevocation — Elastic Load Balancing
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elbv2.html#elbv2truststorerevocation
apiVersion: aws.konfig.io/v1alpha1
kind: ELBv2TrustStoreRevocation
metadata:
  name: example-elbv2truststorerevocation
  namespace: default
spec:
  providerRef:
    name: example-elbv2truststorerevocation
    region: us-east-1
  revocationContents:
  - revocationType: example
    s3Bucket: example
    s3Key: example
    s3ObjectVersion: '1.0'
  trustStoreArn: arn:aws:iam::123456789012:role/example-role
