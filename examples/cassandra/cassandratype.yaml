# CassandraType — Keyspaces
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cassandra.html#cassandratype
apiVersion: aws.konfig.io/v1alpha1
kind: CassandraType
metadata:
  name: example-cassandratype
  namespace: default
spec:
  fields:
  - fieldName: example-cassandratype
    fieldType: example
  keyspaceName: example-cassandratype
  providerRef:
    name: example-cassandratype
    region: us-east-1
  typeName: example-cassandratype
