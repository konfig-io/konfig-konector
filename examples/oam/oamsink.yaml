# OAMSink — CloudWatch OAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/oam.html#oamsink
apiVersion: aws.konfig.io/v1alpha1
kind: OAMSink
metadata:
  name: example-oamsink
  namespace: default
spec:
  name: example-oamsink
  providerRef:
    name: example-oamsink
    region: us-east-1
  tags:
    environment: dev
    team: platform
