# OAMLink — CloudWatch OAM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/oam.html#oamlink
apiVersion: aws.konfig.io/v1alpha1
kind: OAMLink
metadata:
  name: example-oamlink
  namespace: default
spec:
  labelTemplate: example
  linkConfiguration:
    logGroupConfiguration:
      filter: example
    metricConfiguration:
      filter: example
  providerRef:
    name: example-oamlink
    region: us-east-1
  resourceTypes:
  - example
  sinkIdentifier: example
  tags:
    environment: dev
    team: platform
