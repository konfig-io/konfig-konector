# CloudFrontCloudFrontOriginAccessIdentity — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontcloudfrontoriginaccessidentity
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontCloudFrontOriginAccessIdentity
metadata:
  name: example-cloudfrontcloudfrontoriginaccessidentity
  namespace: default
spec:
  cloudFrontOriginAccessIdentityConfig:
    comment: Managed by konfig-konector
  providerRef:
    name: example-cloudfrontcloudfrontoriginaccessidentity
    region: us-east-1
