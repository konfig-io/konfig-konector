# CloudFrontConnectionGroup — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontconnectiongroup
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontConnectionGroup
metadata:
  name: example-cloudfrontconnectiongroup
  namespace: default
spec:
  enabled: true
  ipv6Enabled: true
  name: example-cloudfrontconnectiongroup
  providerRef:
    name: example-cloudfrontconnectiongroup
    region: us-east-1
  tags:
  - key: example
    value: example
