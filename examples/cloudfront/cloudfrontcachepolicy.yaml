# CloudFrontCachePolicy — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontcachepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontCachePolicy
metadata:
  name: example-cloudfrontcachepolicy
  namespace: default
spec:
  comment: Managed by konfig-konector
  defaultTtl: 1
  maxTtl: 1
  minTtl: 1
  name: example-cloudfrontcachepolicy
  providerRef:
    name: example-cloudfrontcachepolicy
    region: us-east-1
