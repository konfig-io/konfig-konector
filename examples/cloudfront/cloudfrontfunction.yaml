# CloudFrontFunction — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontfunction
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontFunction
metadata:
  name: example-cloudfrontfunction
  namespace: default
spec:
  comment: Managed by konfig-konector
  functionCode: example
  name: example-cloudfrontfunction
  providerRef:
    name: example-cloudfrontfunction
    region: us-east-1
  runtime: cloudfront-js-1.0
