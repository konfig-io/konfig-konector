# CloudFrontOriginRequestPolicy — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontoriginrequestpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontOriginRequestPolicy
metadata:
  name: example-cloudfrontoriginrequestpolicy
  namespace: default
spec:
  originRequestPolicyConfig:
    comment: Managed by konfig-konector
    cookiesConfig:
      cookieBehavior: example
      cookies:
      - example
    headersConfig:
      headerBehavior: example
      headers:
      - example
    name: example-cloudfrontoriginrequestpolicy
    queryStringsConfig:
      queryStringBehavior: example
      queryStrings:
      - example
  providerRef:
    name: example-cloudfrontoriginrequestpolicy
    region: us-east-1
