# CloudFrontConnectionFunction — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontconnectionfunction
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontConnectionFunction
metadata:
  name: example-cloudfrontconnectionfunction
  namespace: default
spec:
  autoPublish: false
  connectionFunctionCode: example
  connectionFunctionConfig:
    comment: Managed by konfig-konector
    keyValueStoreAssociations:
    - keyValueStoreARN: arn:aws:iam::123456789012:role/example-role
    runtime: cloudfront-js-2.0
  name: example-cloudfrontconnectionfunction
  providerRef:
    name: example-cloudfrontconnectionfunction
    region: us-east-1
  tags:
  - key: example
    value: example
