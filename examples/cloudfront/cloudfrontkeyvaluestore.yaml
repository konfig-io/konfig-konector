# CloudFrontKeyValueStore — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontkeyvaluestore
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontKeyValueStore
metadata:
  name: example-cloudfrontkeyvaluestore
  namespace: default
spec:
  comment: Managed by konfig-konector
  importSource:
    sourceArn: arn:aws:iam::123456789012:role/example-role
    sourceType: example
  name: example-cloudfrontkeyvaluestore
  providerRef:
    name: example-cloudfrontkeyvaluestore
    region: us-east-1
  tags:
  - key: example
    value: example
