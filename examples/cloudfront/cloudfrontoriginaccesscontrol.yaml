# CloudFrontOriginAccessControl — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontoriginaccesscontrol
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontOriginAccessControl
metadata:
  name: example-cloudfrontoriginaccesscontrol
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-cloudfrontoriginaccesscontrol
  originAccessControlOriginType: s3
  providerRef:
    name: example-cloudfrontoriginaccesscontrol
    region: us-east-1
  signingBehavior: always
  signingProtocol: sigv4
