# CloudFrontRealtimeLogConfig — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontrealtimelogconfig
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontRealtimeLogConfig
metadata:
  name: example-cloudfrontrealtimelogconfig
  namespace: default
spec:
  endPoints:
  - kinesisStreamConfig:
      roleArn: arn:aws:iam::123456789012:role/example-role
      streamArn: arn:aws:iam::123456789012:role/example-role
    streamType: example
  fields:
  - example
  name: example-cloudfrontrealtimelogconfig
  providerRef:
    name: example-cloudfrontrealtimelogconfig
    region: us-east-1
  samplingRate: 1
