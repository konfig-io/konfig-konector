# CloudFrontAnycastIpList — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontanycastiplist
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontAnycastIpList
metadata:
  name: example-cloudfrontanycastiplist
  namespace: default
spec:
  ipAddressType: ipv4
  ipCount: 2
  ipamCidrConfigs:
  - cidr: 10.0.0.0/16
    ipamPoolArn: arn:aws:iam::123456789012:role/example-role
  name: example-cloudfrontanycastiplist
  providerRef:
    name: example-cloudfrontanycastiplist
    region: us-east-1
  tags:
    items:
    - key: example
      value: example
