# CloudFrontPublicKey — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontpublickey
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontPublicKey
metadata:
  name: example-cloudfrontpublickey
  namespace: default
spec:
  providerRef:
    name: example-cloudfrontpublickey
    region: us-east-1
  publicKeyConfig:
    callerReference: example
    comment: Managed by konfig-konector
    encodedKey: example
    name: example-cloudfrontpublickey
