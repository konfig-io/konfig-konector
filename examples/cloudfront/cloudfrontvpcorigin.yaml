# CloudFrontVpcOrigin — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontvpcorigin
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontVpcOrigin
metadata:
  name: example-cloudfrontvpcorigin
  namespace: default
spec:
  providerRef:
    name: example-cloudfrontvpcorigin
    region: us-east-1
  tags:
  - key: example
    value: example
  vpcOriginEndpointConfig:
    arn: arn:aws:iam::123456789012:role/example-role
    httpPort: 443
    httpsPort: 443
    ipAddressType: ipv4
    name: example-cloudfrontvpcorigin
    originProtocolPolicy: example
    originSSLProtocols:
    - example
