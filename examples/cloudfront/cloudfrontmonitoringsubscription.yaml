# CloudFrontMonitoringSubscription — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontmonitoringsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontMonitoringSubscription
metadata:
  name: example-cloudfrontmonitoringsubscription
  namespace: default
spec:
  distributionId: example-id
  monitoringSubscription:
    realtimeMetricsSubscriptionConfig:
      realtimeMetricsSubscriptionStatus: Enabled
  providerRef:
    name: example-cloudfrontmonitoringsubscription
    region: us-east-1
