# CloudFrontKeyGroup — CloudFront
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudfront.html#cloudfrontkeygroup
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFrontKeyGroup
metadata:
  name: example-cloudfrontkeygroup
  namespace: default
spec:
  keyGroupConfig:
    comment: Managed by konfig-konector
    items:
    - example
    name: example-cloudfrontkeygroup
  providerRef:
    name: example-cloudfrontkeygroup
    region: us-east-1
