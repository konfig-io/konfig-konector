# AthenaPreparedStatement — Athena
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/athena.html#athenapreparedstatement
apiVersion: aws.konfig.io/v1alpha1
kind: AthenaPreparedStatement
metadata:
  name: example-athenapreparedstatement
  namespace: default
spec:
  description: Managed by konfig-konector
  providerRef:
    name: example-athenapreparedstatement
    region: us-east-1
  queryStatement: example
  statementName: example-athenapreparedstatement
  workGroup: example
