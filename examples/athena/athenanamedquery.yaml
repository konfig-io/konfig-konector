# AthenaNamedQuery — Athena
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/athena.html#athenanamedquery
apiVersion: aws.konfig.io/v1alpha1
kind: AthenaNamedQuery
metadata:
  name: example-athenanamedquery
  namespace: default
spec:
  database: example
  description: Managed by konfig-konector
  name: example-athenanamedquery
  queryString: example
  workGroup: example
