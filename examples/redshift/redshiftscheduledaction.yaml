# RedshiftScheduledAction — Redshift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/redshift.html#redshiftscheduledaction
apiVersion: aws.konfig.io/v1alpha1
kind: RedshiftScheduledAction
metadata:
  name: example-redshiftscheduledaction
  namespace: default
spec:
  enable: true
  endTime: '2026-01-01T00:00:00Z'
  iamRole: example
  providerRef:
    name: example-redshiftscheduledaction
    region: us-east-1
  schedule: example
  scheduledActionDescription: Managed by konfig-konector
  scheduledActionName: example-redshiftscheduledaction
  startTime: '2026-01-01T00:00:00Z'
