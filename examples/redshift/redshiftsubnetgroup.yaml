# RedshiftSubnetGroup — Redshift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/redshift.html#redshiftsubnetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: RedshiftSubnetGroup
metadata:
  name: example-redshiftsubnetgroup
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-redshiftsubnetgroup
  subnetRefs:
  - name: example-subnetrefs
  tags:
    environment: dev
    team: platform
