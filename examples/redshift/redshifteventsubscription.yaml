# RedshiftEventSubscription — Redshift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/redshift.html#redshifteventsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: RedshiftEventSubscription
metadata:
  name: example-redshifteventsubscription
  namespace: default
spec:
  enabled: true
  eventCategories:
  - example
  providerRef:
    name: example-redshifteventsubscription
    region: us-east-1
  severity: ERROR
  snsTopicArn: arn:aws:iam::123456789012:role/example-role
  sourceIds:
  - example
  sourceType: cluster
  subscriptionName: example-redshifteventsubscription
  tags:
  - key: example
    value: example
