# RedshiftEndpointAuthorization — Redshift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/redshift.html#redshiftendpointauthorization
apiVersion: aws.konfig.io/v1alpha1
kind: RedshiftEndpointAuthorization
metadata:
  name: example-redshiftendpointauthorization
  namespace: default
spec:
  account: example
  clusterIdentifier: example
  force: false
  providerRef:
    name: example-redshiftendpointauthorization
    region: us-east-1
  vpcIds:
  - example
