# RedshiftSnapshotSchedule — Redshift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/redshift.html#redshiftsnapshotschedule
apiVersion: aws.konfig.io/v1alpha1
kind: RedshiftSnapshotSchedule
metadata:
  name: example-redshiftsnapshotschedule
  namespace: default
spec:
  providerRef:
    name: example-redshiftsnapshotschedule
    region: us-east-1
  scheduleDefinitions:
  - example
  scheduleDescription: Managed by konfig-konector
  scheduleIdentifier: example
  tags:
  - key: example
    value: example
