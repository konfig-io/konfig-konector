# Route53RecoveryReadinessCell — route53recoveryreadiness
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/route53recoveryreadiness.html#route53recoveryreadinesscell
apiVersion: aws.konfig.io/v1alpha1
kind: Route53RecoveryReadinessCell
metadata:
  name: example-route53recoveryreadinesscell
  namespace: default
spec:
  cellName: example-route53recoveryreadinesscell
  cells:
  - example
  providerRef:
    name: example-route53recoveryreadinesscell
    region: us-east-1
  tags:
  - key: example
    value: example
