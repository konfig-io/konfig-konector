# DirectConnectDirectConnectGateway — directconnect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnectdirectconnectgateway
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectDirectConnectGateway
metadata:
  name: example-directconnectdirectconnectgateway
  namespace: default
spec:
  amazonSideAsn: example
  directConnectGatewayName: example-directconnectdirectconnectgateway
  providerRef:
    name: example-directconnectdirectconnectgateway
    region: us-east-1
  tags:
  - key: example
    value: example
