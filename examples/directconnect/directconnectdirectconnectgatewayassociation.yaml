# DirectConnectDirectConnectGatewayAssociation — Direct Connect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnectdirectconnectgatewayassociation
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectDirectConnectGatewayAssociation
metadata:
  name: example-directconnectdirectconnectgatewayassociation
  namespace: default
spec:
  acceptDirectConnectGatewayAssociationProposalRoleArn: arn:aws:iam::123456789012:role/example-role
  allowedPrefixesToDirectConnectGateway:
  - example
  providerRef:
    name: example-directconnectdirectconnectgatewayassociation
    region: us-east-1
