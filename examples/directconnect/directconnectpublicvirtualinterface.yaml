# DirectConnectPublicVirtualInterface — Direct Connect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnectpublicvirtualinterface
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectPublicVirtualInterface
metadata:
  name: example-directconnectpublicvirtualinterface
  namespace: default
spec:
  allocatePublicVirtualInterfaceRoleArn: arn:aws:iam::123456789012:role/example-role
  bgpPeers:
  - addressFamily: example
    amazonAddress: example
    asn: example
    authKey: example
    customerAddress: example
  providerRef:
    name: example-directconnectpublicvirtualinterface
    region: us-east-1
  rateLimit: example
  routeFilterPrefixes:
  - example
  tags:
  - key: example
    value: example
  virtualInterfaceName: example-directconnectpublicvirtualinterface
  vlan: 1
