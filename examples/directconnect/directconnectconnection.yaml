# DirectConnectConnection — Direct Connect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnectconnection
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectConnection
metadata:
  name: example-directconnectconnection
  namespace: default
spec:
  bandwidth: example
  connectionName: example-directconnectconnection
  location: example
  providerName: example-directconnectconnection
  providerRef:
    name: example-directconnectconnection
    region: us-east-1
  requestMACSec: false
  tags:
  - key: example
    value: example
