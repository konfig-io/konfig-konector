# DirectConnectLag — Direct Connect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnectlag
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectLag
metadata:
  name: example-directconnectlag
  namespace: default
spec:
  connectionsBandwidth: example
  lagName: example-directconnectlag
  location: example
  minimumLinks: 1
  providerName: example-directconnectlag
  providerRef:
    name: example-directconnectlag
    region: us-east-1
  requestMACSec: false
  tags:
  - key: example
    value: example
