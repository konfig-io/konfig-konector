# DirectConnectPrivateVirtualInterface — directconnect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnectprivatevirtualinterface
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectPrivateVirtualInterface
metadata:
  name: example-directconnectprivatevirtualinterface
  namespace: default
spec:
  allocatePrivateVirtualInterfaceRoleArn: arn:aws:iam::123456789012:role/example-role
  bgpPeers:
  - addressFamily: example
    amazonAddress: example
    asn: example
    authKey: example
    customerAddress: example
  enableSiteLink: true
  mtu: 1
  providerRef:
    name: example-directconnectprivatevirtualinterface
    region: us-east-1
  rateLimit: example
  tags:
  - key: example
    value: example
  virtualInterfaceName: example-directconnectprivatevirtualinterface
  vlan: 1
