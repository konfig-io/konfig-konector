# DirectConnectTransitVirtualInterface — Direct Connect
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/directconnect.html#directconnecttransitvirtualinterface
apiVersion: aws.konfig.io/v1alpha1
kind: DirectConnectTransitVirtualInterface
metadata:
  name: example-directconnecttransitvirtualinterface
  namespace: default
spec:
  allocateTransitVirtualInterfaceRoleArn: arn:aws:iam::123456789012:role/example-role
  bgpPeers:
  - addressFamily: example
    amazonAddress: example
    asn: example
    authKey: example
    customerAddress: example
  enableSiteLink: true
  mtu: 1
  providerRef:
    name: example-directconnecttransitvirtualinterface
    region: us-east-1
  rateLimit: example
  tags:
  - key: example
    value: example
  virtualInterfaceName: example-directconnecttransitvirtualinterface
  vlan: 1
