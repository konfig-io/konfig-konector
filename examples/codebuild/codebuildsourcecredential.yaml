# CodeBuildSourceCredential — CodeBuild
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codebuild.html#codebuildsourcecredential
apiVersion: aws.konfig.io/v1alpha1
kind: CodeBuildSourceCredential
metadata:
  name: example-codebuildsourcecredential
  namespace: default
spec:
  authType: example
  providerRef:
    name: example-codebuildsourcecredential
    region: us-east-1
  serverType: example
  token: example
  username: example-codebuildsourcecredential
