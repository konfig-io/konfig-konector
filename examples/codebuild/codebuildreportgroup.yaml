# CodeBuildReportGroup — CodeBuild
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codebuild.html#codebuildreportgroup
apiVersion: aws.konfig.io/v1alpha1
kind: CodeBuildReportGroup
metadata:
  name: example-codebuildreportgroup
  namespace: default
spec:
  deleteReports: false
  exportConfig:
    exportConfigType: S3
    s3Destination:
      bucket: example
      bucketOwner: example
      encryptionDisabled: true
      encryptionKey: example
      packaging: NONE
      path: example
  name: example-codebuildreportgroup
  providerRef:
    name: example-codebuildreportgroup
    region: us-east-1
  tags:
  - key: example
    value: example
  type: TEST
