# AccountAccessEntitlement — accountaccess
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/accountaccess.html#accountaccessentitlement
apiVersion: aws.konfig.io/v1alpha1
kind: AccountAccessEntitlement
metadata:
  name: example-accountaccessentitlement
  namespace: default
spec:
  applicationArn: arn:aws:iam::123456789012:role/example-role
  entitlement:
    principalRole:
      account: example
      roleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-accountaccessentitlement
    region: us-east-1
