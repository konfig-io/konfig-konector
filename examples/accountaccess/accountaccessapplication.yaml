# AccountAccessApplication — accountaccess
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/accountaccess.html#accountaccessapplication
apiVersion: aws.konfig.io/v1alpha1
kind: AccountAccessApplication
metadata:
  name: example-accountaccessapplication
  namespace: default
spec:
  identitySource:
    identityCenter:
      applicationArn: arn:aws:iam::123456789012:role/example-role
      instanceArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-accountaccessapplication
    region: us-east-1
  tags:
  - key: example
    value: example
