# OpenSearchApplication — OpenSearch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/opensearch.html#opensearchapplication
apiVersion: aws.konfig.io/v1alpha1
kind: OpenSearchApplication
metadata:
  name: example-opensearchapplication
  namespace: default
spec:
  appConfigs:
  - key: opensearchDashboards.dashboardAdmin.users
    value: example
  dataSources:
  - dataSourceDescription: Managed by konfig-konector
  endpoint: example
  iamIdentityCenterOptions:
    enabled: true
    iamRoleForIdentityCenterApplicationArn: arn:aws:iam::123456789012:role/example-role
  kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  name: example-opensearchapplication
  providerRef:
    name: example-opensearchapplication
    region: us-east-1
  tags:
  - key: example
    value: example
