# OpenSearchDataSource — OpenSearch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/opensearch.html#opensearchdatasource
apiVersion: aws.konfig.io/v1alpha1
kind: OpenSearchDataSource
metadata:
  name: example-opensearchdatasource
  namespace: default
spec:
  dataSourceType:
    s3GlueDataCatalog:
      roleArn: arn:aws:iam::123456789012:role/example-role
  description: Managed by konfig-konector
  domainName: example-opensearchdatasource
  name: example-opensearchdatasource
  providerRef:
    name: example-opensearchdatasource
    region: us-east-1
