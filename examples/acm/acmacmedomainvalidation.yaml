# ACMAcmeDomainValidation — ACM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/acm.html#acmacmedomainvalidation
apiVersion: aws.konfig.io/v1alpha1
kind: ACMAcmeDomainValidation
metadata:
  name: example-acmacmedomainvalidation
  namespace: default
spec:
  acmeEndpointArn: arn:aws:iam::123456789012:role/example-role
  domainName: example-acmacmedomainvalidation
  prevalidationOptions:
    dnsPrevalidation:
      domainScope:
        exactDomain: example
        subdomains: example
        wildcards: example
  providerRef:
    name: example-acmacmedomainvalidation
    region: us-east-1
  tags:
  - key: example
    value: example
