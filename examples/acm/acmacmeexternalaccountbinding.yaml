# ACMAcmeExternalAccountBinding — ACM
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/acm.html#acmacmeexternalaccountbinding
apiVersion: aws.konfig.io/v1alpha1
kind: ACMAcmeExternalAccountBinding
metadata:
  name: example-acmacmeexternalaccountbinding
  namespace: default
spec:
  acmeEndpointArn: arn:aws:iam::123456789012:role/example-role
  expiration:
    type: example
    value: 1
  providerRef:
    name: example-acmacmeexternalaccountbinding
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
