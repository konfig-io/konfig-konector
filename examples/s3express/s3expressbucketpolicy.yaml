# S3ExpressBucketPolicy — s3express
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3express.html#s3expressbucketpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: S3ExpressBucketPolicy
metadata:
  name: example-s3expressbucketpolicy
  namespace: default
spec:
  bucket: example
  providerRef:
    name: example-s3expressbucketpolicy
    region: us-east-1
