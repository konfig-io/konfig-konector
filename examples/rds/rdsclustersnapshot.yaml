# RDSClusterSnapshot — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#rdsclustersnapshot
apiVersion: aws.konfig.io/v1alpha1
kind: RDSClusterSnapshot
metadata:
  name: example-rdsclustersnapshot
  namespace: default
spec:
  dbClusterIdentifier: example
  dbClusterSnapshotIdentifier: example
  providerRef:
    name: example-rdsclustersnapshot
    region: us-east-1
  tags:
  - key: example
    value: example
