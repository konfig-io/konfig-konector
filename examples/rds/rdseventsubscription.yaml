# RDSEventSubscription — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#rdseventsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: RDSEventSubscription
metadata:
  name: example-rdseventsubscription
  namespace: default
spec:
  enabled: true
  eventCategories:
  - example
  snsTopicRef:
    name: example-snstopic
  sourceIds:
  - example
  sourceType: example
  subscriptionName: example-rdseventsubscription
