# RDSDBProxyTargetGroup — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#rdsdbproxytargetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: RDSDBProxyTargetGroup
metadata:
  name: example-rdsdbproxytargetgroup
  namespace: default
spec:
  connectionPoolConfigurationInfo:
    connectionBorrowTimeout: 300
    initQuery: example
    maxConnectionsPercent: 1
    maxIdleConnectionsPercent: 1
    sessionPinningFilters:
    - example
  dbClusterIdentifiers:
  - example
  dbInstanceIdentifiers:
  - example
  dbProxyName: example-rdsdbproxytargetgroup
  providerRef:
    name: example-rdsdbproxytargetgroup
    region: us-east-1
  targetGroupName: default
