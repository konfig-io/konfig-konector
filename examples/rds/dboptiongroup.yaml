# DBOptionGroup — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#dboptiongroup
apiVersion: aws.konfig.io/v1alpha1
kind: DBOptionGroup
metadata:
  name: example-dboptiongroup
  namespace: default
spec:
  engineName: example-dboptiongroup
  majorEngineVersion: example
  optionGroupDescription: Managed by konfig-konector
  optionGroupName: example-dboptiongroup
  options:
  - optionName: example-dboptiongroup
    optionSettings:
    - name: example-dboptiongroup
      value: example
    port: 443
  tags:
    environment: dev
    team: platform
