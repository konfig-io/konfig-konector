# RDSDBProxyEndpoint — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#rdsdbproxyendpoint
apiVersion: aws.konfig.io/v1alpha1
kind: RDSDBProxyEndpoint
metadata:
  name: example-rdsdbproxyendpoint
  namespace: default
spec:
  dbProxyEndpointName: example-rdsdbproxyendpoint
  dbProxyName: example-rdsdbproxyendpoint
  endpointNetworkType: IPV4
  providerRef:
    name: example-rdsdbproxyendpoint
    region: us-east-1
  tags:
  - key: example
    value: example
  targetRole: READ_WRITE
  vpcSecurityGroupIds:
  - example
  vpcSubnetIds:
  - example
