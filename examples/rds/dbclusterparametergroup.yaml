# DBClusterParameterGroup — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#dbclusterparametergroup
apiVersion: aws.konfig.io/v1alpha1
kind: DBClusterParameterGroup
metadata:
  name: example-dbclusterparametergroup
  namespace: default
spec:
  dbClusterParameterGroupName: example-dbclusterparametergroup
  dbParameterGroupFamily: example
  description: Managed by konfig-konector
  parameters:
  - applyMethod: immediate
    parameterName: example-dbclusterparametergroup
    parameterValue: example
  tags:
    environment: dev
    team: platform
