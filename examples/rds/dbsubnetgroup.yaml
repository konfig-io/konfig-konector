# DBSubnetGroup — RDS & Aurora
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rds.html#dbsubnetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: DBSubnetGroup
metadata:
  name: example-dbsubnetgroup
  namespace: default
spec:
  dbSubnetGroupName: example-dbsubnetgroup
  description: Managed by konfig-konector
  providerRef:
    name: example-dbsubnetgroup
    region: us-east-1
  subnetRefs:
  - name: example-subnetrefs
  - name: example-subnetrefs
  tags:
    environment: dev
    team: platform
