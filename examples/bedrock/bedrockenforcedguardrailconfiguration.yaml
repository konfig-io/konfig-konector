# BedrockEnforcedGuardrailConfiguration — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockenforcedguardrailconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockEnforcedGuardrailConfiguration
metadata:
  name: example-bedrockenforcedguardrailconfiguration
  namespace: default
spec:
  guardrailIdentifier: example
  guardrailVersion: '1.0'
  modelEnforcement:
    excludedModels:
    - example
    includedModels:
    - example
  providerRef:
    name: example-bedrockenforcedguardrailconfiguration
    region: us-east-1
  selectiveContentGuarding:
    messages: SELECTIVE
    system: SELECTIVE
