# BedrockFlow — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockflow
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockFlow
metadata:
  name: example-bedrockflow
  namespace: default
spec:
  customerEncryptionKeyArn: arn:aws:iam::123456789012:role/example-role
  definition:
    connections:
    - name: example-bedrockflow
      source: example
      target: example
      type: Data
    nodes:
    - inputs:
      - category: LoopCondition
        expression: example
        name: example-bedrockflow
        type: String
      name: example-bedrockflow
      outputs:
      - name: example-bedrockflow
        type: String
      type: Input
  definitionS3Location:
    bucket: example
    key: example
    version: '1.0'
  definitionString: example
  definitionSubstitutions: {}
  description: Managed by konfig-konector
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  name: example-bedrockflow
  providerRef:
    name: example-bedrockflow
    region: us-east-1
  tags:
    environment: dev
    team: platform
  testAliasTags:
    environment: dev
    team: platform
