# BedrockAgentAlias — Bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockagentalias
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAgentAlias
metadata:
  name: example-bedrockagentalias
  namespace: default
spec:
  agentAliasName: example-bedrockagentalias
  agentId: example-id
  description: Managed by konfig-konector
  providerRef:
    name: example-bedrockagentalias
    region: us-east-1
  routingConfiguration:
  - agentVersion: '1.0'
  tags:
    environment: dev
    team: platform
