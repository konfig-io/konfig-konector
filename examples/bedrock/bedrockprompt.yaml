# BedrockPrompt — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockprompt
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockPrompt
metadata:
  name: example-bedrockprompt
  namespace: default
spec:
  customerEncryptionKeyArn: arn:aws:iam::123456789012:role/example-role
  defaultVariant: example
  description: Managed by konfig-konector
  name: example-bedrockprompt
  providerRef:
    name: example-bedrockprompt
    region: us-east-1
  tags:
    environment: dev
    team: platform
  variants:
  - metadata:
    - key: example
      value: example
    name: example-bedrockprompt
    templateType: TEXT
