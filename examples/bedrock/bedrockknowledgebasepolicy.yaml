# BedrockKnowledgeBasePolicy — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockknowledgebasepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockKnowledgeBasePolicy
metadata:
  name: example-bedrockknowledgebasepolicy
  namespace: default
spec:
  knowledgeBaseId: example-id
  providerRef:
    name: example-bedrockknowledgebasepolicy
    region: us-east-1
