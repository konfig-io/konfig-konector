# BedrockGuardrailVersion — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockguardrailversion
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockGuardrailVersion
metadata:
  name: example-bedrockguardrailversion
  namespace: default
spec:
  description: Managed by konfig-konector
  guardrailIdentifier: example
  providerRef:
    name: example-bedrockguardrailversion
    region: us-east-1
