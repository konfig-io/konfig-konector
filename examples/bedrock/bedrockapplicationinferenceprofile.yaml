# BedrockApplicationInferenceProfile — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockapplicationinferenceprofile
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockApplicationInferenceProfile
metadata:
  name: example-bedrockapplicationinferenceprofile
  namespace: default
spec:
  description: Managed by konfig-konector
  inferenceProfileName: example-bedrockapplicationinferenceprofile
  providerRef:
    name: example-bedrockapplicationinferenceprofile
    region: us-east-1
  tags:
  - key: example
    value: example
