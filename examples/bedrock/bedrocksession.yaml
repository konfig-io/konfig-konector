# BedrockSession — Bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrocksession
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockSession
metadata:
  name: example-bedrocksession
  namespace: default
spec:
  encryptionKeyArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-bedrocksession
    region: us-east-1
  sessionMetadata:
    key: value
  tags:
  - key: example
    value: example
