# BedrockPromptVersion — Bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockpromptversion
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockPromptVersion
metadata:
  name: example-bedrockpromptversion
  namespace: default
spec:
  description: Managed by konfig-konector
  promptArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-bedrockpromptversion
    region: us-east-1
  tags:
    environment: dev
    team: platform
