# BedrockAutomatedReasoningPolicyVersion — bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockautomatedreasoningpolicyversion
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockAutomatedReasoningPolicyVersion
metadata:
  name: example-bedrockautomatedreasoningpolicyversion
  namespace: default
spec:
  lastUpdatedDefinitionHash: example
  policyArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-bedrockautomatedreasoningpolicyversion
    region: us-east-1
  tags:
  - key: example
    value: example
