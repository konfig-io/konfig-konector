# BedrockFlowAlias — Bedrock
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bedrock.html#bedrockflowalias
apiVersion: aws.konfig.io/v1alpha1
kind: BedrockFlowAlias
metadata:
  name: example-bedrockflowalias
  namespace: default
spec:
  concurrencyConfiguration:
    maxConcurrency: 1
    type: Automatic
  description: Managed by konfig-konector
  flowArn: arn:aws:iam::123456789012:role/example-role
  name: example-bedrockflowalias
  providerRef:
    name: example-bedrockflowalias
    region: us-east-1
  routingConfiguration:
  - flowVersion: '1.0'
  tags:
    environment: dev
    team: platform
