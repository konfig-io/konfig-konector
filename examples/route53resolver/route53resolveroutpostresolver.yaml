# Route53ResolverOutpostResolver — Route 53 Resolver
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/route53resolver.html#route53resolveroutpostresolver
apiVersion: aws.konfig.io/v1alpha1
kind: Route53ResolverOutpostResolver
metadata:
  name: example-route53resolveroutpostresolver
  namespace: default
spec:
  instanceCount: 4
  name: example-route53resolveroutpostresolver
  outpostArn: arn:aws:iam::123456789012:role/example-role
  preferredInstanceType: example
  providerRef:
    name: example-route53resolveroutpostresolver
    region: us-east-1
  tags:
  - key: example
    value: example
