# Route53ResolverResolverConfig — Route 53 Resolver
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/route53resolver.html#route53resolverresolverconfig
apiVersion: aws.konfig.io/v1alpha1
kind: Route53ResolverResolverConfig
metadata:
  name: example-route53resolverresolverconfig
  namespace: default
spec:
  autodefinedReverseFlag: DISABLE
  providerRef:
    name: example-route53resolverresolverconfig
    region: us-east-1
  resourceId: example-id
