# CloudMapNamespace — Cloud Map
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicediscovery.html#cloudmapnamespace
apiVersion: aws.konfig.io/v1alpha1
kind: CloudMapNamespace
metadata:
  name: example-cloudmapnamespace
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-cloudmapnamespace
  providerRef:
    name: example-cloudmapnamespace
    region: us-east-1
  tags:
    environment: dev
    team: platform
  type: PRIVATE_DNS
  vpcRef:
    name: example-vpc
