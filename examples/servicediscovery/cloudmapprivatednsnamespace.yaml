# CloudMapPrivateDnsNamespace — Cloud Map
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicediscovery.html#cloudmapprivatednsnamespace
apiVersion: aws.konfig.io/v1alpha1
kind: CloudMapPrivateDnsNamespace
metadata:
  name: example-cloudmapprivatednsnamespace
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-cloudmapprivatednsnamespace
  properties:
    dnsProperties:
      soa:
        ttl: 1
  providerRef:
    name: example-cloudmapprivatednsnamespace
    region: us-east-1
  tags:
  - key: example
    value: example
  vpc: example
