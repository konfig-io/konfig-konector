# EMRServerlessApplication — emrserverless
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/emrserverless.html#emrserverlessapplication
apiVersion: aws.konfig.io/v1alpha1
kind: EMRServerlessApplication
metadata:
  name: example-emrserverlessapplication
  namespace: default
spec:
  architecture: ARM64
  autoStartConfiguration:
    enabled: true
  autoStopConfiguration:
    enabled: true
    idleTimeoutMinutes: 300
  identityCenterConfiguration:
    identityCenterInstanceArn: arn:aws:iam::123456789012:role/example-role
  imageConfiguration:
    imageUri: example
  initialCapacity:
  - key: example
    value:
      workerConfiguration:
        cpu: example
        disk: example
        diskType: example
        memory: example
      workerCount: 1
  interactiveConfiguration:
    livyEndpointEnabled: true
    sessionEnabled: true
    studioEnabled: true
  maximumCapacity:
    cpu: example
    disk: example
    memory: example
  name: example-emrserverlessapplication
  networkConfiguration:
    securityGroupIds:
    - example
    subnetIds:
    - example
  providerRef:
    name: example-emrserverlessapplication
    region: us-east-1
  releaseLabel: example
  runtimeConfiguration:
  - classification: example
    properties:
      key: value
  schedulerConfiguration:
    maxConcurrentRuns: 1
    queueTimeoutMinutes: 300
  tags:
  - key: example
    value: example
  type: example
  workerTypeSpecifications: {}
