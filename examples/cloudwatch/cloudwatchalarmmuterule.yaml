# CloudWatchAlarmMuteRule — CloudWatch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatch.html#cloudwatchalarmmuterule
apiVersion: aws.konfig.io/v1alpha1
kind: CloudWatchAlarmMuteRule
metadata:
  name: example-cloudwatchalarmmuterule
  namespace: default
spec:
  description: Managed by konfig-konector
  expireDate: example
  muteTargets:
    alarmNames:
    - example
  name: example-cloudwatchalarmmuterule
  providerRef:
    name: example-cloudwatchalarmmuterule
    region: us-east-1
  rule:
    schedule:
      duration: example
      expression: example
      timezone: example
  startDate: example
  tags:
  - key: example
    value: example
