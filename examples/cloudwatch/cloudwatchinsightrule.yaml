# CloudWatchInsightRule — CloudWatch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatch.html#cloudwatchinsightrule
apiVersion: aws.konfig.io/v1alpha1
kind: CloudWatchInsightRule
metadata:
  name: example-cloudwatchinsightrule
  namespace: default
spec:
  applyOnTransformedLogs: false
  providerRef:
    name: example-cloudwatchinsightrule
    region: us-east-1
  ruleBody: example
  ruleName: example-cloudwatchinsightrule
  ruleState: ENABLED
  tags:
  - key: example
    value: example
