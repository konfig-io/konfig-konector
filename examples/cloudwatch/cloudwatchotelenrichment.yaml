# CloudWatchOTelEnrichment — CloudWatch
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudwatch.html#cloudwatchotelenrichment
apiVersion: aws.konfig.io/v1alpha1
kind: CloudWatchOTelEnrichment
metadata:
  name: example-cloudwatchotelenrichment
  namespace: default
spec:
  providerRef:
    name: example-cloudwatchotelenrichment
    region: us-east-1
