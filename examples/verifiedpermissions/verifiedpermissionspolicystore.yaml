# VerifiedPermissionsPolicyStore — Verified Permissions
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/verifiedpermissions.html#verifiedpermissionspolicystore
apiVersion: aws.konfig.io/v1alpha1
kind: VerifiedPermissionsPolicyStore
metadata:
  name: example-verifiedpermissionspolicystore
  namespace: default
spec:
  deletionProtection:
    mode: ENABLED
  description: Managed by konfig-konector
  providerRef:
    name: example-verifiedpermissionspolicystore
    region: us-east-1
  schema:
    cedarJson: example
  tags:
  - key: example
    value: example
  validationSettings:
    mode: 'OFF'
