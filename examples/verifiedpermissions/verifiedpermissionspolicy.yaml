# VerifiedPermissionsPolicy — Verified Permissions
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/verifiedpermissions.html#verifiedpermissionspolicy
apiVersion: aws.konfig.io/v1alpha1
kind: VerifiedPermissionsPolicy
metadata:
  name: example-verifiedpermissionspolicy
  namespace: default
spec:
  name: example-verifiedpermissionspolicy
  policyStoreId: example-id
  providerRef:
    name: example-verifiedpermissionspolicy
    region: us-east-1
