# VerifiedPermissionsIdentitySource — verifiedpermissions
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/verifiedpermissions.html#verifiedpermissionsidentitysource
apiVersion: aws.konfig.io/v1alpha1
kind: VerifiedPermissionsIdentitySource
metadata:
  name: example-verifiedpermissionsidentitysource
  namespace: default
spec:
  policyStoreId: example-id
  principalEntityType: example
  providerRef:
    name: example-verifiedpermissionsidentitysource
    region: us-east-1
