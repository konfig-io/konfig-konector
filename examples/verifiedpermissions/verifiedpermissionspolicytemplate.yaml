# VerifiedPermissionsPolicyTemplate — verifiedpermissions
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/verifiedpermissions.html#verifiedpermissionspolicytemplate
apiVersion: aws.konfig.io/v1alpha1
kind: VerifiedPermissionsPolicyTemplate
metadata:
  name: example-verifiedpermissionspolicytemplate
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-verifiedpermissionspolicytemplate
  policyStoreId: example-id
  providerRef:
    name: example-verifiedpermissionspolicytemplate
    region: us-east-1
  statement: example
