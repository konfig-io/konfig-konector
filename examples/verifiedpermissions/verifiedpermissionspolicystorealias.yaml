# VerifiedPermissionsPolicyStoreAlias — Verified Permissions
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/verifiedpermissions.html#verifiedpermissionspolicystorealias
apiVersion: aws.konfig.io/v1alpha1
kind: VerifiedPermissionsPolicyStoreAlias
metadata:
  name: example-verifiedpermissionspolicystorealias
  namespace: default
spec:
  aliasName: example-verifiedpermissionspolicystorealias
  policyStoreId: example-id
  providerRef:
    name: example-verifiedpermissionspolicystorealias
    region: us-east-1
