# BCMDataExportsExport — bcmdataexports
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/bcmdataexports.html#bcmdataexportsexport
apiVersion: aws.konfig.io/v1alpha1
kind: BCMDataExportsExport
metadata:
  name: example-bcmdataexportsexport
  namespace: default
spec:
  export:
    dataQuery:
      queryStatement: example
    description: Managed by konfig-konector
    destinationConfigurations:
      s3Destination:
        s3Bucket: example
        s3BucketOwner: example
        s3OutputConfigurations:
          compression: GZIP
          format: TEXT_OR_CSV
          outputType: CUSTOM
          overwrite: CREATE_NEW_REPORT
        s3Prefix: example
        s3Region: example
    exportArn: arn:aws:iam::123456789012:role/example-role
    name: example-bcmdataexportsexport
    refreshCadence:
      frequency: SYNCHRONOUS
  providerRef:
    name: example-bcmdataexportsexport
    region: us-east-1
  tags:
  - key: example
    value: example
