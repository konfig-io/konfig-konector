# SignerProfilePermission — Signer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/signer.html#signerprofilepermission
apiVersion: aws.konfig.io/v1alpha1
kind: SignerProfilePermission
metadata:
  name: example-signerprofilepermission
  namespace: default
spec:
  action: example
  principal: example
  profileName: example-signerprofilepermission
  profileVersion: '1.0'
  providerRef:
    name: example-signerprofilepermission
    region: us-east-1
  statementId: example-id
