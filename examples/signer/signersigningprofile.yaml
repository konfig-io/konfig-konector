# SignerSigningProfile — signer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/signer.html#signersigningprofile
apiVersion: aws.konfig.io/v1alpha1
kind: SignerSigningProfile
metadata:
  name: example-signersigningprofile
  namespace: default
spec:
  platformId: AWSLambda-SHA384-ECDSA
  providerRef:
    name: example-signersigningprofile
    region: us-east-1
  signatureValidityPeriod:
    type: DAYS
    value: 1
  tags:
  - key: example
    value: example
