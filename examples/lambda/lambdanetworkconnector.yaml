# LambdaNetworkConnector — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdanetworkconnector
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaNetworkConnector
metadata:
  name: example-lambdanetworkconnector
  namespace: default
spec:
  configuration:
    vpcEgressConfiguration:
      associatedComputeResourceTypes:
      - example
      networkProtocol: IPv4
      securityGroupIds:
      - example
      subnetIds:
      - example
  name: example-lambdanetworkconnector
  operatorRole: example
  providerRef:
    name: example-lambdanetworkconnector
    region: us-east-1
  tags:
  - key: example
    value: example
