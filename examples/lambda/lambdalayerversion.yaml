# LambdaLayerVersion — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdalayerversion
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaLayerVersion
metadata:
  name: example-lambdalayerversion
  namespace: default
spec:
  compatibleArchitectures:
  - example
  compatibleRuntimes:
  - '2026-01-01T00:00:00Z'
  content:
    s3Bucket: example
    s3Key: example
    s3ObjectVersion: '1.0'
  description: Managed by konfig-konector
  layerName: example-lambdalayerversion
  licenseInfo: example
  providerRef:
    name: example-lambdalayerversion
    region: us-east-1
