# LambdaResourcePolicy — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdaresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaResourcePolicy
metadata:
  name: example-lambdaresourcepolicy
  namespace: default
spec:
  providerRef:
    name: example-lambdaresourcepolicy
    region: us-east-1
  resourceArn: arn:aws:iam::123456789012:role/example-role
