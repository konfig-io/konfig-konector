# LambdaEventInvokeConfig — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdaeventinvokeconfig
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaEventInvokeConfig
metadata:
  name: example-lambdaeventinvokeconfig
  namespace: default
spec:
  functionName: example-lambdaeventinvokeconfig
  functionRef:
    functionName: example-lambdaeventinvokeconfig
    name: example-lambdaeventinvokeconfig
  maximumEventAgeInSeconds: 60
  maximumRetryAttempts: 1
  onFailureDestinationArn: arn:aws:iam::123456789012:role/example-role
  onSuccessDestinationArn: arn:aws:iam::123456789012:role/example-role
  qualifier: example
