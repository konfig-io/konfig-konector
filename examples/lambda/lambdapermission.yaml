# LambdaPermission — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdapermission
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaPermission
metadata:
  name: example-lambdapermission
  namespace: default
spec:
  action: example
  functionArn: arn:aws:iam::123456789012:role/example-role
  functionRef:
    functionName: example-lambdapermission
    name: example-lambdapermission
  principal: example
  providerRef:
    name: example-lambdapermission
    region: us-east-1
  sourceAccount: example
  sourceArn: arn:aws:iam::123456789012:role/example-role
  statementId: example-id
