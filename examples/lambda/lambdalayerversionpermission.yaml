# LambdaLayerVersionPermission — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdalayerversionpermission
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaLayerVersionPermission
metadata:
  name: example-lambdalayerversionpermission
  namespace: default
spec:
  action: example
  layerVersionArn: arn:aws:iam::123456789012:role/example-role
  principal: example
  providerRef:
    name: example-lambdalayerversionpermission
    region: us-east-1
