# LambdaCodeSigningConfig — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdacodesigningconfig
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaCodeSigningConfig
metadata:
  name: example-lambdacodesigningconfig
  namespace: default
spec:
  allowedPublisherArns:
  - example
  description: Managed by konfig-konector
  untrustedArtifactOnDeployment: Warn
