# LambdaFunctionURL — Lambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/lambda.html#lambdafunctionurl
apiVersion: aws.konfig.io/v1alpha1
kind: LambdaFunctionURL
metadata:
  name: example-lambdafunctionurl
  namespace: default
spec:
  authType: NONE
  cors:
    allowCredentials: false
    allowHeaders:
    - example
    allowMethods:
    - example
    allowOrigins:
    - example
    exposeHeaders:
    - example
    maxAge: 1
  functionName: example-lambdafunctionurl
  invokeMode: BUFFERED
  providerRef:
    name: example-lambdafunctionurl
    region: us-east-1
  qualifier: example
