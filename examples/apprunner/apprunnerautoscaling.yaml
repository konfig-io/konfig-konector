# AppRunnerAutoScaling — App Runner
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apprunner.html#apprunnerautoscaling
apiVersion: aws.konfig.io/v1alpha1
kind: AppRunnerAutoScaling
metadata:
  name: example-apprunnerautoscaling
  namespace: default
spec:
  maxConcurrency: 1
  maxSize: 1
  minSize: 1
  name: example-apprunnerautoscaling
  providerRef:
    name: example-apprunnerautoscaling
    region: us-east-1
  tags:
    environment: dev
    team: platform
