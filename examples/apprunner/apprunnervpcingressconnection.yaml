# AppRunnerVpcIngressConnection — App Runner
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apprunner.html#apprunnervpcingressconnection
apiVersion: aws.konfig.io/v1alpha1
kind: AppRunnerVpcIngressConnection
metadata:
  name: example-apprunnervpcingressconnection
  namespace: default
spec:
  ingressVpcConfiguration:
    vpcEndpointId: example-id
    vpcId: vpc-0abc12def3456789
  providerRef:
    name: example-apprunnervpcingressconnection
    region: us-east-1
  serviceArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  vpcIngressConnectionName: example-apprunnervpcingressconnection
