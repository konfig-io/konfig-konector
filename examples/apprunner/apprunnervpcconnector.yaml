# AppRunnerVpcConnector — App Runner
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apprunner.html#apprunnervpcconnector
apiVersion: aws.konfig.io/v1alpha1
kind: AppRunnerVpcConnector
metadata:
  name: example-apprunnervpcconnector
  namespace: default
spec:
  providerRef:
    name: example-apprunnervpcconnector
    region: us-east-1
  securityGroups:
  - example
  subnets:
  - example
  tags:
  - key: example
    value: example
  vpcConnectorName: example-apprunnervpcconnector
