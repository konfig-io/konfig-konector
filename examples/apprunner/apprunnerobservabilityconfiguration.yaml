# AppRunnerObservabilityConfiguration — App Runner
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apprunner.html#apprunnerobservabilityconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: AppRunnerObservabilityConfiguration
metadata:
  name: example-apprunnerobservabilityconfiguration
  namespace: default
spec:
  observabilityConfigurationName: example-apprunnerobservabilityco
  providerRef:
    name: example-apprunnerobservabilityconfiguration
    region: us-east-1
  tags:
  - key: example
    value: example
  traceConfiguration:
    vendor: AWSXRAY
