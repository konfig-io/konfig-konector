# FirewallPolicy — Network Firewall
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkfirewall.html#firewallpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: FirewallPolicy
metadata:
  name: example-firewallpolicy
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-firewallpolicy
  providerRef:
    name: example-firewallpolicy
    region: us-east-1
  statefulRuleGroupRefs:
  - name: example-statefulrulegrouprefs
  statelessDefaultActions:
  - example
  statelessFragmentDefaultActions:
  - example
  statelessRuleGroupRefs:
  - arn: arn:aws:iam::123456789012:role/example-role
    name: example-firewallpolicy
    priority: 1
  tags:
    environment: dev
    team: platform
