# NetworkFirewallTLSInspectionConfiguration — Network Firewall
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkfirewall.html#networkfirewalltlsinspectionconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkFirewallTLSInspectionConfiguration
metadata:
  name: example-networkfirewalltlsinspectionconfiguration
  namespace: default
spec:
  description: Managed by konfig-konector
  providerRef:
    name: example-networkfirewalltlsinspectionconfiguration
    region: us-east-1
  tags:
  - key: example
    value: example
  tlsInspectionConfiguration:
    serverCertificateConfigurations:
    - certificateAuthorityArn: arn:aws:iam::123456789012:role/example-role
      scopes:
      - destinationPorts:
        - fromPort: 1
          toPort: 1
        destinations:
        - addressDefinition: example
        protocols:
        - 1
        sourcePorts:
        - fromPort: 1
          toPort: 1
        sources:
        - addressDefinition: example
      serverCertificates:
      - resourceArn: arn:aws:iam::123456789012:role/example-role
  tlsInspectionConfigurationName: example-networkfirewalltlsinspectionconfiguration
