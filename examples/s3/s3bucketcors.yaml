# S3BucketCORS — S3
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3.html#s3bucketcors
apiVersion: aws.konfig.io/v1alpha1
kind: S3BucketCORS
metadata:
  name: example-s3bucketcors
  namespace: default
spec:
  bucketName: example-s3bucketcors
  corsRules:
  - allowedHeaders:
    - example
    allowedMethods:
    - example
    allowedOrigins:
    - example
    exposeHeaders:
    - example
    maxAgeSeconds: 300
  providerRef:
    name: example-s3bucketcors
    region: us-east-1
