# S3BucketReplication — S3
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3.html#s3bucketreplication
apiVersion: aws.konfig.io/v1alpha1
kind: S3BucketReplication
metadata:
  name: example-s3bucketreplication
  namespace: default
spec:
  bucketName: example-s3bucketreplication
  providerRef:
    name: example-s3bucketreplication
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  rules:
  - destination:
      account: example
      bucket: example
      storageClass: example
    prefix: example
    priority: 1
    status: Enabled
