# S3AccessGrant — S3
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3.html#s3accessgrant
apiVersion: aws.konfig.io/v1alpha1
kind: S3AccessGrant
metadata:
  name: example-s3accessgrant
  namespace: default
spec:
  accessGrantsLocationConfiguration:
    s3SubPrefix: example
  accessGrantsLocationId: example-id
  applicationArn: arn:aws:iam::123456789012:role/example-role
  grantee:
    granteeIdentifier: example
    granteeType: IAM
  permission: READ
  providerRef:
    name: example-s3accessgrant
    region: us-east-1
  s3PrefixType: Object
  tags:
  - key: example
    value: example
