# S3AccessGrantsLocation — S3
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3.html#s3accessgrantslocation
apiVersion: aws.konfig.io/v1alpha1
kind: S3AccessGrantsLocation
metadata:
  name: example-s3accessgrantslocation
  namespace: default
spec:
  iamRoleArn: arn:aws:iam::123456789012:role/example-role
  locationScope: example
  providerRef:
    name: example-s3accessgrantslocation
    region: us-east-1
  tags:
  - key: example
    value: example
