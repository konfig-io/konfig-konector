# S3AccessGrantsInstance — S3
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3.html#s3accessgrantsinstance
apiVersion: aws.konfig.io/v1alpha1
kind: S3AccessGrantsInstance
metadata:
  name: example-s3accessgrantsinstance
  namespace: default
spec:
  identityCenterArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-s3accessgrantsinstance
    region: us-east-1
  tags:
  - key: example
    value: example
