# S3MultiRegionAccessPointPolicy — S3
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3.html#s3multiregionaccesspointpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: S3MultiRegionAccessPointPolicy
metadata:
  name: example-s3multiregionaccesspointpolicy
  namespace: default
spec:
  mrapName: example-s3multiregionaccesspointpolicy
  providerRef:
    name: example-s3multiregionaccesspointpolicy
    region: us-east-1
