# CognitoUserPoolIdentityProvider — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitouserpoolidentityprovider
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoUserPoolIdentityProvider
metadata:
  name: example-cognitouserpoolidentityprovider
  namespace: default
spec:
  attributeMapping:
    key: value
  idpIdentifiers:
  - example
  providerDetails:
    key: value
  providerName: example-cognitouserpoolidentityprovider
  providerRef:
    name: example-cognitouserpoolidentityprovider
    region: us-east-1
  providerType: example
  userPoolId: example-id
