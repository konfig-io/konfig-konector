# IdentityProvider — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#identityprovider
apiVersion: aws.konfig.io/v1alpha1
kind: IdentityProvider
metadata:
  name: example-identityprovider
  namespace: default
spec:
  attributeMapping:
    key: value
  idpIdentifiers:
  - example
  providerDetails:
    key: value
  providerName: example-identityprovider
  providerRef:
    name: example-identityprovider
    region: us-east-1
  providerType: SAML
  userPoolRef:
    name: example-identityprovider
