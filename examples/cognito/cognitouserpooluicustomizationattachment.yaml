# CognitoUserPoolUICustomizationAttachment — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitouserpooluicustomizationattachment
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoUserPoolUICustomizationAttachment
metadata:
  name: example-cognitouserpooluicustomizationattachment
  namespace: default
spec:
  clientId: example-id
  css: example
  providerRef:
    name: example-cognitouserpooluicustomizationattachment
    region: us-east-1
  userPoolId: example-id
