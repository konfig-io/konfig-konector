# CognitoLogDeliveryConfiguration — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitologdeliveryconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoLogDeliveryConfiguration
metadata:
  name: example-cognitologdeliveryconfiguration
  namespace: default
spec:
  logConfigurations:
  - cloudWatchLogsConfiguration:
      logGroupArn: arn:aws:iam::123456789012:role/example-role
    eventSource: example
    firehoseConfiguration:
      streamArn: arn:aws:iam::123456789012:role/example-role
    logLevel: example
    s3Configuration:
      bucketArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-cognitologdeliveryconfiguration
    region: us-east-1
  userPoolId: example-id
