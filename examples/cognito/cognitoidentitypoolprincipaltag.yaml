# CognitoIdentityPoolPrincipalTag — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitoidentitypoolprincipaltag
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoIdentityPoolPrincipalTag
metadata:
  name: example-cognitoidentitypoolprincipaltag
  namespace: default
spec:
  identityPoolId: example-id
  identityProviderName: example-cognitoidentitypoolprincipaltag
  providerRef:
    name: example-cognitoidentitypoolprincipaltag
    region: us-east-1
  useDefaults: false
