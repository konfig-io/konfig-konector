# CognitoTerms — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitoterms
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoTerms
metadata:
  name: example-cognitoterms
  namespace: default
spec:
  enforcement: NONE
  links:
    key: value
  providerRef:
    name: example-cognitoterms
    region: us-east-1
  termsName: example-cognitoterms
  termsSource: LINK
  userPoolId: example-id
