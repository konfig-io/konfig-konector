# CognitoManagedLoginBranding — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitomanagedloginbranding
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoManagedLoginBranding
metadata:
  name: example-cognitomanagedloginbranding
  namespace: default
spec:
  assets:
  - bytes: example
    category: FAVICON_ICO
    colorMode: LIGHT
    extension: ICO
  providerRef:
    name: example-cognitomanagedloginbranding
    region: us-east-1
  returnMergedResources: false
  useCognitoProvidedValues: false
  userPoolId: example-id
