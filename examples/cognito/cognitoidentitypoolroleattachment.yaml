# CognitoIdentityPoolRoleAttachment — Cognito
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cognito.html#cognitoidentitypoolroleattachment
apiVersion: aws.konfig.io/v1alpha1
kind: CognitoIdentityPoolRoleAttachment
metadata:
  name: example-cognitoidentitypoolroleattachment
  namespace: default
spec:
  identityPoolId: example-id
  providerRef:
    name: example-cognitoidentitypoolroleattachment
    region: us-east-1
