# IdentityStoreGroup — IAM Identity Store
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/identitystore.html#identitystoregroup
apiVersion: aws.konfig.io/v1alpha1
kind: IdentityStoreGroup
metadata:
  name: example-identitystoregroup
  namespace: default
spec:
  description: Managed by konfig-konector
  displayName: example-identitystoregroup
  identityStoreId: example-id
  providerRef:
    name: example-identitystoregroup
    region: us-east-1
