# SecurityLakeSubscriber — Security Lake
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securitylake.html#securitylakesubscriber
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityLakeSubscriber
metadata:
  name: example-securitylakesubscriber
  namespace: default
spec:
  accessTypes:
  - example
  dataLakeArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-securitylakesubscriber
    region: us-east-1
  sources:
  - awsLogSource:
      sourceName: example-securitylakesubscriber
      sourceVersion: '1.0'
    customLogSource:
      sourceName: example-securitylakesubscriber
      sourceVersion: '1.0'
  subscriberDescription: Managed by konfig-konector
  subscriberIdentity:
    externalId: example-id
    principal: example
  subscriberName: example-securitylakesubscriber
  tags:
  - key: example
    value: example
