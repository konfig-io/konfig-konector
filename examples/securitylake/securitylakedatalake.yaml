# SecurityLakeDataLake — Security Lake
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securitylake.html#securitylakedatalake
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityLakeDataLake
metadata:
  name: example-securitylakedatalake
  namespace: default
spec:
  encryptionConfiguration:
    kmsKeyId: arn:aws:kms:us-east-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab
  lifecycleConfiguration:
    expiration:
      days: 1
    transitions:
    - days: 1
      storageClass: example
  metaStoreManagerRoleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-securitylakedatalake
    region: us-east-1
  replicationConfiguration:
    regions:
    - example
    roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
