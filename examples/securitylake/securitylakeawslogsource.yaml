# SecurityLakeAwsLogSource — securitylake
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securitylake.html#securitylakeawslogsource
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityLakeAwsLogSource
metadata:
  name: example-securitylakeawslogsource
  namespace: default
spec:
  accounts:
  - example
  dataLakeArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-securitylakeawslogsource
    region: us-east-1
  sourceName: example-securitylakeawslogsource
  sourceVersion: '1.0'
