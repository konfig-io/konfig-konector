# SecurityLakeSubscriberNotification — Security Lake
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/securitylake.html#securitylakesubscribernotification
apiVersion: aws.konfig.io/v1alpha1
kind: SecurityLakeSubscriberNotification
metadata:
  name: example-securitylakesubscribernotification
  namespace: default
spec:
  notificationConfiguration:
    httpsNotificationConfiguration:
      authorizationApiKeyName: example-securitylakesubscribernotification
      authorizationApiKeyValue: example
      endpoint: example
      httpMethod: POST
      targetRoleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-securitylakesubscribernotification
    region: us-east-1
  subscriberArn: arn:aws:iam::123456789012:role/example-role
