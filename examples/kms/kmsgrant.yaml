# KMSGrant — KMS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kms.html#kmsgrant
apiVersion: aws.konfig.io/v1alpha1
kind: KMSGrant
metadata:
  name: example-kmsgrant
  namespace: default
spec:
  granteePrincipalArn: arn:aws:iam::123456789012:role/example-role
  keyId: example-id
  name: example-kmsgrant
  operations:
  - example
  providerRef:
    name: example-kmsgrant
    region: us-east-1
  retiringPrincipalArn: arn:aws:iam::123456789012:role/example-role
