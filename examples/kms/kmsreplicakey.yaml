# KMSReplicaKey — KMS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kms.html#kmsreplicakey
apiVersion: aws.konfig.io/v1alpha1
kind: KMSReplicaKey
metadata:
  name: example-kmsreplicakey
  namespace: default
spec:
  description: Managed by konfig-konector
  enabled: true
  pendingWindowInDays: 7
  primaryKeyArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-kmsreplicakey
    region: us-east-1
  tags:
  - key: example
    value: example
