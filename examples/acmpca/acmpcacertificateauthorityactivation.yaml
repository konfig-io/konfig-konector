# ACMPCACertificateAuthorityActivation — ACM PCA
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/acmpca.html#acmpcacertificateauthorityactivation
apiVersion: aws.konfig.io/v1alpha1
kind: ACMPCACertificateAuthorityActivation
metadata:
  name: example-acmpcacertificateauthorityactivation
  namespace: default
spec:
  certificate: example
  certificateAuthorityArn: arn:aws:iam::123456789012:role/example-role
  certificateChain: example
  providerRef:
    name: example-acmpcacertificateauthorityactivation
    region: us-east-1
  status: example
