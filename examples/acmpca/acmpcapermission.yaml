# ACMPCAPermission — ACM PCA
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/acmpca.html#acmpcapermission
apiVersion: aws.konfig.io/v1alpha1
kind: ACMPCAPermission
metadata:
  name: example-acmpcapermission
  namespace: default
spec:
  actions:
  - example
  certificateAuthorityArn: arn:aws:iam::123456789012:role/example-role
  principal: example
  providerRef:
    name: example-acmpcapermission
    region: us-east-1
  sourceAccount: example
