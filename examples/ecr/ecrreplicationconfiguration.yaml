# ECRReplicationConfiguration — ECR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecr.html#ecrreplicationconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: ECRReplicationConfiguration
metadata:
  name: example-ecrreplicationconfiguration
  namespace: default
spec:
  providerRef:
    name: example-ecrreplicationconfiguration
    region: us-east-1
  replicationConfiguration:
    rules:
    - destinations:
      - region: us-east-1
        registryId: example-id
      repositoryFilters:
      - filter: example
        filterType: PREFIX_MATCH
