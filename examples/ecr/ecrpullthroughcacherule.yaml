# ECRPullThroughCacheRule — ECR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecr.html#ecrpullthroughcacherule
apiVersion: aws.konfig.io/v1alpha1
kind: ECRPullThroughCacheRule
metadata:
  name: example-ecrpullthroughcacherule
  namespace: default
spec:
  credentialArn: arn:aws:iam::123456789012:role/example-role
  customRoleArn: arn:aws:iam::123456789012:role/example-role
  ecrRepositoryPrefix: example
  providerRef:
    name: example-ecrpullthroughcacherule
    region: us-east-1
  upstreamRegistry: example
  upstreamRegistryUrl: example
  upstreamRepositoryPrefix: example
