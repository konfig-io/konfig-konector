# ECRRegistryPolicy — ECR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecr.html#ecrregistrypolicy
apiVersion: aws.konfig.io/v1alpha1
kind: ECRRegistryPolicy
metadata:
  name: example-ecrregistrypolicy
  namespace: default
spec:
  providerRef:
    name: example-ecrregistrypolicy
    region: us-east-1
