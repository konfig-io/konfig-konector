# ECRRepositoryPolicy — ECR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecr.html#ecrrepositorypolicy
apiVersion: aws.konfig.io/v1alpha1
kind: ECRRepositoryPolicy
metadata:
  name: example-ecrrepositorypolicy
  namespace: default
spec:
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  providerRef:
    name: example-ecrrepositorypolicy
    region: us-east-1
  repositoryRef:
    name: example-ecrrepositorypolicy
    repositoryName: example-ecrrepositorypolicy
