# ECRLifecyclePolicy — ECR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecr.html#ecrlifecyclepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: ECRLifecyclePolicy
metadata:
  name: example-ecrlifecyclepolicy
  namespace: default
spec:
  lifecyclePolicyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  providerRef:
    name: example-ecrlifecyclepolicy
    region: us-east-1
  repositoryRef:
    name: example-ecrlifecyclepolicy
    repositoryName: example-ecrlifecyclepolicy
