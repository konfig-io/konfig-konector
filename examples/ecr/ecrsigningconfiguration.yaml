# ECRSigningConfiguration — ECR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ecr.html#ecrsigningconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: ECRSigningConfiguration
metadata:
  name: example-ecrsigningconfiguration
  namespace: default
spec:
  providerRef:
    name: example-ecrsigningconfiguration
    region: us-east-1
  rules:
  - repositoryFilters:
    - filter: example
      filterType: WILDCARD_MATCH
    signingProfileArn: arn:aws:iam::123456789012:role/example-role
