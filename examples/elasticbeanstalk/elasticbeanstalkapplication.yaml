# ElasticBeanstalkApplication — Elastic Beanstalk
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticbeanstalk.html#elasticbeanstalkapplication
apiVersion: aws.konfig.io/v1alpha1
kind: ElasticBeanstalkApplication
metadata:
  name: example-elasticbeanstalkapplication
  namespace: default
spec:
  applicationName: example-elasticbeanstalkapplication
  description: Managed by konfig-konector
  providerRef:
    name: example-elasticbeanstalkapplication
    region: us-east-1
  resourceLifecycleConfig:
    serviceRole: example
    versionLifecycleConfig:
      maxAgeRule:
        deleteSourceFromS3: false
        enabled: true
        maxAgeInDays: 7
      maxCountRule:
        deleteSourceFromS3: false
        enabled: true
        maxCount: 2
