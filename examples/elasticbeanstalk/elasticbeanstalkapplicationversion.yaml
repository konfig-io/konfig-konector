# ElasticBeanstalkApplicationVersion — Elastic Beanstalk
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticbeanstalk.html#elasticbeanstalkapplicationversion
apiVersion: aws.konfig.io/v1alpha1
kind: ElasticBeanstalkApplicationVersion
metadata:
  name: example-elasticbeanstalkapplicationversion
  namespace: default
spec:
  applicationName: example-elasticbeanstalkapplicationversion
  description: Managed by konfig-konector
  providerRef:
    name: example-elasticbeanstalkapplicationversion
    region: us-east-1
  sourceBundle:
    s3Bucket: example
    s3Key: example
