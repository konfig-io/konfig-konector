# PodIdentityAssociation — EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eks.html#podidentityassociation
apiVersion: aws.konfig.io/v1alpha1
kind: PodIdentityAssociation
metadata:
  name: example-podidentityassociation
  namespace: default
spec:
  annotateServiceAccount: false
  clusterName: example-podidentityassociation
  roleRef:
    name: example-role
  serviceAccountName: example-podidentityassociation
  tags:
    environment: dev
    team: platform
  targetNamespace: example
