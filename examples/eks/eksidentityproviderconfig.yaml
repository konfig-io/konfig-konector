# EKSIdentityProviderConfig — EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eks.html#eksidentityproviderconfig
apiVersion: aws.konfig.io/v1alpha1
kind: EKSIdentityProviderConfig
metadata:
  name: example-eksidentityproviderconfig
  namespace: default
spec:
  clusterName: example-eksidentityproviderconfig
  identityProviderConfigName: example-eksidentityproviderconfig
  oidc:
    clientId: example-id
    groupsClaim: example
    groupsPrefix: example
    issuerUrl: example
    requiredClaims:
      key: value
    usernameClaim: example
    usernamePrefix: example
  tags:
    environment: dev
    team: platform
