# EKSAddon — EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eks.html#eksaddon
apiVersion: aws.konfig.io/v1alpha1
kind: EKSAddon
metadata:
  name: example-eksaddon
  namespace: default
spec:
  addonName: example-eksaddon
  addonVersion: '1.0'
  clusterName: example-eksaddon
  clusterRef:
    clusterName: example-eksaddon
    name: example-eksaddon
  configurationValues: example
  providerRef:
    name: example-eksaddon
    region: us-east-1
  resolveConflicts: OVERWRITE
  serviceAccountRoleArn: arn:aws:iam::123456789012:role/example-role
  serviceAccountRoleRef:
    name: example-serviceaccountrole
  tags:
    environment: dev
    team: platform
