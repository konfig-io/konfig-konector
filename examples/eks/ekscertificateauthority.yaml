# EKSCertificateAuthority — EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eks.html#ekscertificateauthority
apiVersion: aws.konfig.io/v1alpha1
kind: EKSCertificateAuthority
metadata:
  name: example-ekscertificateauthority
  namespace: default
spec:
  clusterName: example-ekscertificateauthority
  providerRef:
    name: example-ekscertificateauthority
    region: us-east-1
