# EKSAccessEntry — EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eks.html#eksaccessentry
apiVersion: aws.konfig.io/v1alpha1
kind: EKSAccessEntry
metadata:
  name: example-eksaccessentry
  namespace: default
spec:
  accessPolicies:
  - accessScope:
      namespaces:
      - example
      type: cluster
    policyArn: arn:aws:iam::123456789012:role/example-role
  clusterName: example-eksaccessentry
  clusterRef:
    clusterName: example-eksaccessentry
    name: example-eksaccessentry
  kubernetesGroups:
  - example
  principalArn: arn:aws:iam::123456789012:role/example-role
  principalRef:
    name: example-principal
  tags:
    environment: dev
    team: platform
  type: STANDARD
  username: example-eksaccessentry
