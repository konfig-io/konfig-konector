# EKSFargateProfile — EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eks.html#eksfargateprofile
apiVersion: aws.konfig.io/v1alpha1
kind: EKSFargateProfile
metadata:
  name: example-eksfargateprofile
  namespace: default
spec:
  clusterName: example-eksfargateprofile
  clusterRef:
    clusterName: example-eksfargateprofile
    name: example-eksfargateprofile
  fargateProfileName: example-eksfargateprofile
  podExecutionRoleArn: arn:aws:iam::123456789012:role/example-role
  podExecutionRoleRef:
    name: example-podexecutionrole
  selectors:
  - labels:
      key: value
    namespace: example
  subnetRefs:
  - name: example-subnetrefs
  tags:
    environment: dev
    team: platform
