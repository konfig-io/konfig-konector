# PrometheusAlertManagerDefinition — Managed Prometheus
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/amp.html#prometheusalertmanagerdefinition
apiVersion: aws.konfig.io/v1alpha1
kind: PrometheusAlertManagerDefinition
metadata:
  name: example-prometheusalertmanagerdefinition
  namespace: default
spec:
  definition: example
  workspaceRef:
    name: example-prometheusalertmanagerdefinition
