# PrometheusRuleGroupsNamespace — Managed Prometheus
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/amp.html#prometheusrulegroupsnamespace
apiVersion: aws.konfig.io/v1alpha1
kind: PrometheusRuleGroupsNamespace
metadata:
  name: example-prometheusrulegroupsnamespace
  namespace: default
spec:
  data: example
  name: example-prometheusrulegroupsnamespace
  providerRef:
    name: example-prometheusrulegroupsnamespace
    region: us-east-1
  tags:
    environment: dev
    team: platform
  workspaceRef:
    name: example-prometheusrulegroupsnamespace
