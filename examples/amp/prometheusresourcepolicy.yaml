# PrometheusResourcePolicy — Managed Prometheus
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/amp.html#prometheusresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: PrometheusResourcePolicy
metadata:
  name: example-prometheusresourcepolicy
  namespace: default
spec:
  policyDocument: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"],
    "Resource": "*"}]}'
  providerRef:
    name: example-prometheusresourcepolicy
    region: us-east-1
  workspaceArn: arn:aws:iam::123456789012:role/example-role
