# PrometheusWorkspace — Managed Prometheus
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/amp.html#prometheusworkspace
apiVersion: aws.konfig.io/v1alpha1
kind: PrometheusWorkspace
metadata:
  name: example-prometheusworkspace
  namespace: default
spec:
  alias: example
  kmsKeyArn: arn:aws:iam::123456789012:role/example-role
  tags:
    environment: dev
    team: platform
