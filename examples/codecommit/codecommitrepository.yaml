# CodeCommitRepository — CodeCommit
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codecommit.html#codecommitrepository
apiVersion: aws.konfig.io/v1alpha1
kind: CodeCommitRepository
metadata:
  name: example-codecommitrepository
  namespace: default
spec:
  repositoryDescription: Managed by konfig-konector
  repositoryName: example-codecommitrepository
  tags:
    environment: dev
    team: platform
