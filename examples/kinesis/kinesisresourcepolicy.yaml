# KinesisResourcePolicy — Kinesis
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kinesis.html#kinesisresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: KinesisResourcePolicy
metadata:
  name: example-kinesisresourcepolicy
  namespace: default
spec:
  providerRef:
    name: example-kinesisresourcepolicy
    region: us-east-1
