# ServiceCatalogServiceActionAssociation — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecatalogserviceactionassociation
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogServiceActionAssociation
metadata:
  name: example-servicecatalogserviceactionassociation
  namespace: default
spec:
  productId: example-id
  providerRef:
    name: example-servicecatalogserviceactionassociation
    region: us-east-1
  provisioningArtifactId: example-id
  serviceActionId: example-id
