# ServiceCatalogLaunchTemplateConstraint — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecataloglaunchtemplateconstraint
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogLaunchTemplateConstraint
metadata:
  name: example-servicecataloglaunchtemplateconstraint
  namespace: default
spec:
  acceptLanguage: example
  description: Managed by konfig-konector
  portfolioId: example-id
  productId: example-id
  providerRef:
    name: example-servicecataloglaunchtemplateconstraint
    region: us-east-1
  rules: example
