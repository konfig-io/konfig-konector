# ServiceCatalogStackSetConstraint — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecatalogstacksetconstraint
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogStackSetConstraint
metadata:
  name: example-servicecatalogstacksetconstraint
  namespace: default
spec:
  acceptLanguage: example
  accountList:
  - example
  adminRole: example
  description: Managed by konfig-konector
  executionRole: example
  portfolioId: example-id
  productId: example-id
  providerRef:
    name: example-servicecatalogstacksetconstraint
    region: us-east-1
  regionList:
  - example
  stackInstanceControl: example
