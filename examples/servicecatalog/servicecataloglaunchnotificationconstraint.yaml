# ServiceCatalogLaunchNotificationConstraint — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecataloglaunchnotificationconstraint
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogLaunchNotificationConstraint
metadata:
  name: example-servicecataloglaunchnotificationconstraint
  namespace: default
spec:
  acceptLanguage: example
  description: Managed by konfig-konector
  notificationArns:
  - example
  portfolioId: example-id
  productId: example-id
  providerRef:
    name: example-servicecataloglaunchnotificationconstraint
    region: us-east-1
