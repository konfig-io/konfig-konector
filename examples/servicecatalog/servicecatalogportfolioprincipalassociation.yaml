# ServiceCatalogPortfolioPrincipalAssociation — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecatalogportfolioprincipalassociation
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogPortfolioPrincipalAssociation
metadata:
  name: example-servicecatalogportfolioprincipalassociation
  namespace: default
spec:
  acceptLanguage: example
  principalARN: arn:aws:iam::123456789012:role/example-role
  principalType: example
  providerRef:
    name: example-servicecatalogportfolioprincipalassociation
    region: us-east-1
