# ServiceCatalogResourceUpdateConstraint — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecatalogresourceupdateconstraint
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogResourceUpdateConstraint
metadata:
  name: example-servicecatalogresourceupdateconstraint
  namespace: default
spec:
  acceptLanguage: example
  description: Managed by konfig-konector
  portfolioId: example-id
  productId: example-id
  providerRef:
    name: example-servicecatalogresourceupdateconstraint
    region: us-east-1
  tagUpdateOnProvisionedProduct: example
