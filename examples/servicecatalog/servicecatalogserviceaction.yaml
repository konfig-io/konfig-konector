# ServiceCatalogServiceAction — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecatalogserviceaction
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogServiceAction
metadata:
  name: example-servicecatalogserviceaction
  namespace: default
spec:
  acceptLanguage: en
  definition:
  - key: example
    value: example
  definitionType: SSM_AUTOMATION
  description: Managed by konfig-konector
  name: example-servicecatalogserviceaction
  providerRef:
    name: example-servicecatalogserviceaction
    region: us-east-1
