# SCPortfolio — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#scportfolio
apiVersion: aws.konfig.io/v1alpha1
kind: SCPortfolio
metadata:
  name: example-scportfolio
  namespace: default
spec:
  description: Managed by konfig-konector
  displayName: example-scportfolio
  providerName: example-scportfolio
  tags:
    environment: dev
    team: platform
