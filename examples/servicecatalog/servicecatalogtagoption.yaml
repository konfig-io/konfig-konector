# ServiceCatalogTagOption — Service Catalog
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/servicecatalog.html#servicecatalogtagoption
apiVersion: aws.konfig.io/v1alpha1
kind: ServiceCatalogTagOption
metadata:
  name: example-servicecatalogtagoption
  namespace: default
spec:
  active: false
  key: example
  providerRef:
    name: example-servicecatalogtagoption
    region: us-east-1
  value: example
