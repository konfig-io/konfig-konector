# PermissionSet — IAM Identity Center
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssoadmin.html#permissionset
apiVersion: aws.konfig.io/v1alpha1
kind: PermissionSet
metadata:
  name: example-permissionset
  namespace: default
spec:
  description: Managed by konfig-konector
  inlinePolicy: example
  instanceArn: arn:aws:iam::123456789012:role/example-role
  managedPolicies:
  - example
  name: example-permissionset
  providerRef:
    name: example-permissionset
    region: us-east-1
  relayState: example
  sessionDuration: example
  tags:
    environment: dev
    team: platform
