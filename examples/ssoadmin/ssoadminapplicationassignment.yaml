# SSOAdminApplicationAssignment — IAM Identity Center
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssoadmin.html#ssoadminapplicationassignment
apiVersion: aws.konfig.io/v1alpha1
kind: SSOAdminApplicationAssignment
metadata:
  name: example-ssoadminapplicationassignment
  namespace: default
spec:
  applicationArn: arn:aws:iam::123456789012:role/example-role
  principalId: example-id
  principalType: USER
  providerRef:
    name: example-ssoadminapplicationassignment
    region: us-east-1
