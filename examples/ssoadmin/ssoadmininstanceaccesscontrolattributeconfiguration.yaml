# SSOAdminInstanceAccessControlAttributeConfiguration — IAM Identity Center
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssoadmin.html#ssoadmininstanceaccesscontrolattributeconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: SSOAdminInstanceAccessControlAttributeConfiguration
metadata:
  name: example-ssoadmininstanceaccesscontrolattributeconfiguration
  namespace: default
spec:
  accessControlAttributes:
  - key: example
    value:
      source:
      - example
  instanceAccessControlAttributeConfiguration:
    accessControlAttributes:
    - key: example
      value:
        source:
        - example
  instanceArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-ssoadmininstanceaccesscontrolattributeconfiguration
    region: us-east-1
