# SSOAssignment — IAM Identity Center
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssoadmin.html#ssoassignment
apiVersion: aws.konfig.io/v1alpha1
kind: SSOAssignment
metadata:
  name: example-ssoassignment
  namespace: default
spec:
  instanceArn: arn:aws:iam::123456789012:role/example-role
  permissionSetRef:
    name: example-permissionset
  principalId: example-id
  principalType: USER
  providerRef:
    name: example-ssoassignment
    region: us-east-1
  targetId: example-id-x
  targetType: AWS_ACCOUNT
