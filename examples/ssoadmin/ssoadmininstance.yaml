# SSOAdminInstance — IAM Identity Center
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssoadmin.html#ssoadmininstance
apiVersion: aws.konfig.io/v1alpha1
kind: SSOAdminInstance
metadata:
  name: example-ssoadmininstance
  namespace: default
spec:
  name: example-ssoadmininstance
  providerRef:
    name: example-ssoadmininstance
    region: us-east-1
  tags:
  - key: example
    value: example
