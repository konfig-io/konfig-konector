# CloudFormationResourceVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationresourceversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationResourceVersion
metadata:
  name: example-cloudformationresourceversion
  namespace: default
spec:
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  loggingConfig:
    logGroupName: example-cloudformationresourceversion
    logRoleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-cloudformationresourceversion
    region: us-east-1
  schemaHandlerPackage: example
  typeName: example-cloudformationresourceversion
