# CloudFormationStack — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationstack
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationStack
metadata:
  name: example-cloudformationstack
  namespace: default
spec:
  capabilities:
  - example
  parameters:
  - parameterKey: example
    parameterValue: example
  stackName: example-cloudformationstack
  tags:
    environment: dev
    team: platform
  templateBody: example
  templateURL: example
