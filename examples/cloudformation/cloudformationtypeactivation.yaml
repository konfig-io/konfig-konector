# CloudFormationTypeActivation — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationtypeactivation
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationTypeActivation
metadata:
  name: example-cloudformationtypeactivation
  namespace: default
spec:
  autoUpdate: false
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  loggingConfig:
    logGroupName: example-cloudformationtypeactivation
    logRoleArn: arn:aws:iam::123456789012:role/example-role
  majorVersion: '1.0'
  providerRef:
    name: example-cloudformationtypeactivation
    region: us-east-1
  publicTypeArn: arn:aws:iam::123456789012:role/example-role
  type: RESOURCE
  typeName: example-cloudformationtypeactivation
  typeNameAlias: example
  versionBump: MAJOR
