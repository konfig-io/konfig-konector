# CloudFormationHookDefaultVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationhookdefaultversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationHookDefaultVersion
metadata:
  name: example-cloudformationhookdefaultversion
  namespace: default
spec:
  providerRef:
    name: example-cloudformationhookdefaultversion
    region: us-east-1
  typeName: example-cloudformationhookdefaultversion
  typeVersionArn: arn:aws:iam::123456789012:role/example-role
