# CloudFormationResourceDefaultVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationresourcedefaultversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationResourceDefaultVersion
metadata:
  name: example-cloudformationresourcedefaultversion
  namespace: default
spec:
  providerRef:
    name: example-cloudformationresourcedefaultversion
    region: us-east-1
  typeName: example-cloudformationresourcedefaultversion
  typeVersionArn: arn:aws:iam::123456789012:role/example-role
