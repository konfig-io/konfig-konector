# CloudFormationHookTypeConfig — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationhooktypeconfig
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationHookTypeConfig
metadata:
  name: example-cloudformationhooktypeconfig
  namespace: default
spec:
  configuration: example
  configurationAlias: default
  providerRef:
    name: example-cloudformationhooktypeconfig
    region: us-east-1
  typeArn: arn:aws:iam::123456789012:role/example-role
  typeName: example-cloudformationhooktypeconfig
