# CloudFormationModuleVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationmoduleversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationModuleVersion
metadata:
  name: example-cloudformationmoduleversion
  namespace: default
spec:
  moduleName: example-cloudformationmoduleversion
  modulePackage: example
  providerRef:
    name: example-cloudformationmoduleversion
    region: us-east-1
