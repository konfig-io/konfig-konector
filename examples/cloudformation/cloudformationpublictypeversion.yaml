# CloudFormationPublicTypeVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationpublictypeversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationPublicTypeVersion
metadata:
  name: example-cloudformationpublictypeversion
  namespace: default
spec:
  arn: arn:aws:iam::123456789012:role/example-role
  logDeliveryBucket: example
  providerRef:
    name: example-cloudformationpublictypeversion
    region: us-east-1
  publicVersionNumber: example
  type: RESOURCE
  typeName: example-cloudformationpublictypeversion
