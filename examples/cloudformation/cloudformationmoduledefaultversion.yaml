# CloudFormationModuleDefaultVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationmoduledefaultversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationModuleDefaultVersion
metadata:
  name: example-cloudformationmoduledefaultversion
  namespace: default
spec:
  arn: arn:aws:iam::123456789012:role/example-role
  moduleName: example-cloudformationmoduledefaultversion
  providerRef:
    name: example-cloudformationmoduledefaultversion
    region: us-east-1
