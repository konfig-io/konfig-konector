# CloudFormationHookVersion — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationhookversion
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationHookVersion
metadata:
  name: example-cloudformationhookversion
  namespace: default
spec:
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  loggingConfig:
    logGroupName: example-cloudformationhookversion
    logRoleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-cloudformationhookversion
    region: us-east-1
  schemaHandlerPackage: example
  typeName: example-cloudformationhookversion
