# CloudFormationStackSet — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationstackset
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationStackSet
metadata:
  name: example-cloudformationstackset
  namespace: default
spec:
  capabilities:
  - example
  description: Managed by konfig-konector
  parameters:
  - parameterKey: example
    parameterValue: example
  stackSetName: example-cloudformationstackset
  tags:
    environment: dev
    team: platform
  templateBody: example
  templateURL: example
