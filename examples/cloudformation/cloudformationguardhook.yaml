# CloudFormationGuardHook — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationguardhook
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationGuardHook
metadata:
  name: example-cloudformationguardhook
  namespace: default
spec:
  alias: example
  executionRole: example
  failureMode: FAIL
  hookStatus: ENABLED
  logBucket: example
  providerRef:
    name: example-cloudformationguardhook
    region: us-east-1
  ruleLocation:
    uri: example
  stackFilters:
    filteringCriteria: ALL
    stackNames:
      exclude:
      - example
      include:
      - example
  targetOperations:
  - example
