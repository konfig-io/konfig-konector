# CloudFormationPublisher — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationpublisher
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationPublisher
metadata:
  name: example-cloudformationpublisher
  namespace: default
spec:
  acceptTermsAndConditions: false
  connectionArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-cloudformationpublisher
    region: us-east-1
