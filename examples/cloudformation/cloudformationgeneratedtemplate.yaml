# CloudFormationGeneratedTemplate — CloudFormation
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/cloudformation.html#cloudformationgeneratedtemplate
apiVersion: aws.konfig.io/v1alpha1
kind: CloudFormationGeneratedTemplate
metadata:
  name: example-cloudformationgeneratedtemplate
  namespace: default
spec:
  generatedTemplateName: example-cloudformationgeneratedtemplate
  providerRef:
    name: example-cloudformationgeneratedtemplate
    region: us-east-1
  templateConfiguration:
    deletionPolicy: DELETE
    updateReplacePolicy: DELETE
