# SESConfigurationSet — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sesv2.html#sesconfigurationset
apiVersion: aws.konfig.io/v1alpha1
kind: SESConfigurationSet
metadata:
  name: example-sesconfigurationset
  namespace: default
spec:
  configurationSetName: example-sesconfigurationset
  reputationMetricsEnabled: true
  sendingEnabled: true
  tags:
    environment: dev
    team: platform
