# SESEmailIdentity — SES
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sesv2.html#sesemailidentity
apiVersion: aws.konfig.io/v1alpha1
kind: SESEmailIdentity
metadata:
  name: example-sesemailidentity
  namespace: default
spec:
  configurationSetName: example-sesemailidentity
  emailIdentity: ops@example.com
  tags:
    environment: dev
    team: platform
