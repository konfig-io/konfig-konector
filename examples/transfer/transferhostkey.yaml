# TransferHostKey — transfer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transferhostkey
apiVersion: aws.konfig.io/v1alpha1
kind: TransferHostKey
metadata:
  name: example-transferhostkey
  namespace: default
spec:
  description: Managed by konfig-konector
  hostKeyBody: example
  providerRef:
    name: example-transferhostkey
    region: us-east-1
  serverId: example-id-xxxxxxxx
  tags:
  - key: example
    value: example
