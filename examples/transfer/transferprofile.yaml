# TransferProfile — transfer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transferprofile
apiVersion: aws.konfig.io/v1alpha1
kind: TransferProfile
metadata:
  name: example-transferprofile
  namespace: default
spec:
  as2Id: example-id
  certificateIds:
  - example
  profileType: LOCAL
  providerRef:
    name: example-transferprofile
    region: us-east-1
  tags:
  - key: example
    value: example
