# TransferAgreement — Transfer Family
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transferagreement
apiVersion: aws.konfig.io/v1alpha1
kind: TransferAgreement
metadata:
  name: example-transferagreement
  namespace: default
spec:
  accessRole: example-xxxxxxxxxxxx
  baseDirectory: example
  customDirectories:
    failedFilesDirectory: example
    mdnFilesDirectory: example
    payloadFilesDirectory: example
    statusFilesDirectory: example
    temporaryFilesDirectory: example
  description: Managed by konfig-konector
  enforceMessageSigning: ENABLED
  localProfileId: example-id-xxxxxxxx
  partnerProfileId: example-id-xxxxxxxx
  preserveFilename: ENABLED
  providerRef:
    name: example-transferagreement
    region: us-east-1
  serverId: example-id-xxxxxxxx
  status: ACTIVE
  tags:
  - key: example
    value: example
