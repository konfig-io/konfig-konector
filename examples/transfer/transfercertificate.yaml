# TransferCertificate — Transfer Family
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transfercertificate
apiVersion: aws.konfig.io/v1alpha1
kind: TransferCertificate
metadata:
  name: example-transfercertificate
  namespace: default
spec:
  activeDate: example
  certificate: example
  certificateChain: example
  description: Managed by konfig-konector
  inactiveDate: example
  privateKey: example
  providerRef:
    name: example-transfercertificate
    region: us-east-1
  tags:
  - key: example
    value: example
  usage: SIGNING
