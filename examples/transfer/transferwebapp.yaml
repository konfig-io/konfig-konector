# TransferWebApp — transfer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transferwebapp
apiVersion: aws.konfig.io/v1alpha1
kind: TransferWebApp
metadata:
  name: example-transferwebapp
  namespace: default
spec:
  accessEndpoint: example
  endpointDetails:
    vpc:
      ipAddressType: IPV4
      securityGroupIds:
      - example
      subnetIds:
      - example
  identityProviderDetails:
    applicationArn: arn:aws:iam::123456789012:role/example-role
    instanceArn: arn:aws:iam::123456789012:role/example-role
    role: example
  providerRef:
    name: example-transferwebapp
    region: us-east-1
  tags:
  - key: example
    value: example
  webAppCustomization:
    faviconFile: example
    logoFile: example
    title: example
  webAppEndpointPolicy: STANDARD
