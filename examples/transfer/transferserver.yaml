# TransferServer — Transfer Family
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transferserver
apiVersion: aws.konfig.io/v1alpha1
kind: TransferServer
metadata:
  name: example-transferserver
  namespace: default
spec:
  certificate: example
  domain: S3
  endpointDetails:
    addressAllocationIds:
    - example
    securityGroupIds:
    - example
    subnetIds:
    - example
  endpointType: PUBLIC
  identityProviderDetails:
    function: example
    invocationRole: example
    sftpAuthenticationMethods: PASSWORD
    url: example
  identityProviderType: SERVICE_MANAGED
  ipAddressType: IPV4
  loggingRole: example
  postAuthenticationLoginBanner: example
  preAuthenticationLoginBanner: example
  protocolDetails:
    as2Transports:
    - example
    passiveIp: example
    setStatOption: DEFAULT
    tlsSessionResumptionMode: DISABLED
  protocols:
  - example
  providerRef:
    name: example-transferserver
    region: us-east-1
  s3StorageOptions:
    directoryListingOptimization: ENABLED
  securityPolicyName: example-transferserver
  structuredLogDestinations:
  - example
  tags:
  - key: example
    value: example
  workflowDetails:
    onPartialUpload:
    - executionRole: example-xxxxxxxxxxxx
      workflowId: example-id-xxxxxxxx
    onUpload:
    - executionRole: example-xxxxxxxxxxxx
      workflowId: example-id-xxxxxxxx
