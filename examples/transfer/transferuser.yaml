# TransferUser — Transfer Family
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/transfer.html#transferuser
apiVersion: aws.konfig.io/v1alpha1
kind: TransferUser
metadata:
  name: example-transferuser
  namespace: default
spec:
  homeDirectory: example
  homeDirectoryMappings:
  - entry: example
    target: example
    type: FILE
  homeDirectoryType: PATH
  policy: '{"Version": "2012-10-17", "Statement": [{"Effect": "Allow", "Action": ["s3:GetObject"], "Resource":
    "*"}]}'
  posixProfile:
    gid: 1
    secondaryGids:
    - 1
    uid: 1
  providerRef:
    name: example-transferuser
    region: us-east-1
  role: example-xxxxxxxxxxxx
  serverId: example-id-xxxxxxxx
  sshPublicKeys:
  - example
  tags:
  - key: example
    value: example
  userName: example-transferuser
