# ARCZonalShiftAutoshiftObserverNotificationStatus — arczonalshift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/arczonalshift.html#arczonalshiftautoshiftobservernotificationstatus
apiVersion: aws.konfig.io/v1alpha1
kind: ARCZonalShiftAutoshiftObserverNotificationStatus
metadata:
  name: example-arczonalshiftautoshiftobservernotificationstatus
  namespace: default
spec:
  providerRef:
    name: example-arczonalshiftautoshiftobservernotificationstatus
    region: us-east-1
  status: ENABLED
