# ARCZonalShiftZonalAutoshiftConfiguration — ARC Zonal Shift
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/arczonalshift.html#arczonalshiftzonalautoshiftconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: ARCZonalShiftZonalAutoshiftConfiguration
metadata:
  name: example-arczonalshiftzonalautoshiftconfiguration
  namespace: default
spec:
  practiceRunConfiguration:
    blockedDates:
    - example
    blockedWindows:
    - example
    blockingAlarms:
    - alarmIdentifier: example-
      type: example-
    outcomeAlarms:
    - alarmIdentifier: example-
      type: example-
  providerRef:
    name: example-arczonalshiftzonalautoshiftconfiguration
    region: us-east-1
  resourceIdentifier: example
  zonalAutoshiftStatus: ENABLED
