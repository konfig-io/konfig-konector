# DataSyncLocationFSxLustre — datasync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationfsxlustre
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationFSxLustre
metadata:
  name: example-datasynclocationfsxlustre
  namespace: default
spec:
  fsxFilesystemArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-datasynclocationfsxlustre
    region: us-east-1
  securityGroupArns:
  - example
  subdirectory: example
  tags:
  - key: example
    value: example
