# DataSyncLocationSMB — DataSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationsmb
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationSMB
metadata:
  name: example-datasynclocationsmb
  namespace: default
spec:
  agentArns:
  - example
  authenticationType: NTLM
  cmkSecretConfig:
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  customSecretConfig:
    secretAccessRoleArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  dnsIpAddresses:
  - example
  domain: example
  kerberosKeytab: example
  kerberosKrb5Conf: example
  kerberosPrincipal: example
  mountOptions:
    version: AUTOMATIC
  password: example
  providerRef:
    name: example-datasynclocationsmb
    region: us-east-1
  serverHostname: example-datasynclocationsmb
  subdirectory: example
  tags:
  - key: example
    value: example
  user: example
