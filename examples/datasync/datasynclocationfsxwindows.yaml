# DataSyncLocationFSxWindows — DataSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationfsxwindows
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationFSxWindows
metadata:
  name: example-datasynclocationfsxwindows
  namespace: default
spec:
  cmkSecretConfig:
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  customSecretConfig:
    secretAccessRoleArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  domain: example
  fsxFilesystemArn: arn:aws:iam::123456789012:role/example-role
  password: example
  providerRef:
    name: example-datasynclocationfsxwindows
    region: us-east-1
  securityGroupArns:
  - example
  subdirectory: example
  tags:
  - key: example
    value: example
  user: example
