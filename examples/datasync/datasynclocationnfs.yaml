# DataSyncLocationNFS — DataSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationnfs
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationNFS
metadata:
  name: example-datasynclocationnfs
  namespace: default
spec:
  mountOptions:
    version: AUTOMATIC
  onPremConfig:
    agentArns:
    - example
  providerRef:
    name: example-datasynclocationnfs
    region: us-east-1
  serverHostname: example-datasynclocationnfs
  subdirectory: example
  tags:
  - key: example
    value: example
