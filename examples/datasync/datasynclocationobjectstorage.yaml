# DataSyncLocationObjectStorage — datasync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationobjectstorage
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationObjectStorage
metadata:
  name: example-datasynclocationobjectstorage
  namespace: default
spec:
  accessKey: example
  agentArns:
  - example
  bucketName: example-datasynclocationobjectstorage
  cmkSecretConfig:
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  customSecretConfig:
    secretAccessRoleArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-datasynclocationobjectstorage
    region: us-east-1
  secretKey: example
  serverCertificate: example
  serverHostname: example-datasynclocationobjectstorage
  serverPort: 1
  serverProtocol: HTTPS
  subdirectory: example
  tags:
  - key: example
    value: example
