# DataSyncAgent — datasync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasyncagent
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncAgent
metadata:
  name: example-datasyncagent
  namespace: default
spec:
  activationKey: example
  agentName: example-datasyncagent
  providerRef:
    name: example-datasyncagent
    region: us-east-1
  securityGroupArns:
  - example
  subnetArns:
  - example
  tags:
  - key: example
    value: example
