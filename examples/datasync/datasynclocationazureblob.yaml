# DataSyncLocationAzureBlob — DataSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationazureblob
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationAzureBlob
metadata:
  name: example-datasynclocationazureblob
  namespace: default
spec:
  agentArns:
  - example
  azureAccessTier: HOT
  azureBlobAuthenticationType: SAS
  azureBlobContainerUrl: example
  azureBlobSasConfiguration:
    azureBlobSasToken: example
  azureBlobType: BLOCK
  cmkSecretConfig:
    kmsKeyArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  customSecretConfig:
    secretAccessRoleArn: arn:aws:iam::123456789012:role/example-role
    secretArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-datasynclocationazureblob
    region: us-east-1
  subdirectory: example
  tags:
  - key: example
    value: example
