# DataSyncLocationFSxOpenZFS — DataSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datasync.html#datasynclocationfsxopenzfs
apiVersion: aws.konfig.io/v1alpha1
kind: DataSyncLocationFSxOpenZFS
metadata:
  name: example-datasynclocationfsxopenzfs
  namespace: default
spec:
  fsxFilesystemArn: arn:aws:iam::123456789012:role/example-role
  protocol:
    nfs:
      mountOptions:
        version: AUTOMATIC
  providerRef:
    name: example-datasynclocationfsxopenzfs
    region: us-east-1
  securityGroupArns:
  - example
  subdirectory: example
  tags:
  - key: example
    value: example
