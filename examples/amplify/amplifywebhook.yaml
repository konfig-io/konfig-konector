# AmplifyWebhook — amplify
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/amplify.html#amplifywebhook
apiVersion: aws.konfig.io/v1alpha1
kind: AmplifyWebhook
metadata:
  name: example-amplifywebhook
  namespace: default
spec:
  branchName: example-amplifywebhook
  description: Managed by konfig-konector
  providerRef:
    name: example-amplifywebhook
    region: us-east-1
  tags:
  - key: example
    value: example
