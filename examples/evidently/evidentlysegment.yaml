# EvidentlySegment — CloudWatch Evidently
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/evidently.html#evidentlysegment
apiVersion: aws.konfig.io/v1alpha1
kind: EvidentlySegment
metadata:
  name: example-evidentlysegment
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-evidentlysegment
  pattern: example
  providerRef:
    name: example-evidentlysegment
    region: us-east-1
  tags:
  - key: example
    value: example
