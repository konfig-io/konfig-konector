# EvidentlyFeature — CloudWatch Evidently
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/evidently.html#evidentlyfeature
apiVersion: aws.konfig.io/v1alpha1
kind: EvidentlyFeature
metadata:
  name: example-evidentlyfeature
  namespace: default
spec:
  defaultVariation: example
  description: Managed by konfig-konector
  entityOverrides:
  - variation: example
  evaluationStrategy: ALL_RULES
  name: example-evidentlyfeature
  project: example
  providerRef:
    name: example-evidentlyfeature
    region: us-east-1
  tags:
  - key: example
    value: example
  variations:
  - booleanValue: false
    doubleValue: 1
    longValue: 1
    stringValue: example
    variationName: example-evidentlyfeature
