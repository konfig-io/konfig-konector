# ElastiCacheUserGroup — ElastiCache
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticache.html#elasticacheusergroup
apiVersion: aws.konfig.io/v1alpha1
kind: ElastiCacheUserGroup
metadata:
  name: example-elasticacheusergroup
  namespace: default
spec:
  engine: redis
  providerRef:
    name: example-elasticacheusergroup
    region: us-east-1
  tags:
  - key: example
    value: example
  userGroupId: example-id
  userIds:
  - example
