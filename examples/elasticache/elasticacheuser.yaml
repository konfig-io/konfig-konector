# ElastiCacheUser — ElastiCache
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticache.html#elasticacheuser
apiVersion: aws.konfig.io/v1alpha1
kind: ElastiCacheUser
metadata:
  name: example-elasticacheuser
  namespace: default
spec:
  accessString: example
  authenticationMode:
    passwords:
    - example
    type: password
  engine: redis
  noPasswordRequired: false
  passwords:
  - example
  providerRef:
    name: example-elasticacheuser
    region: us-east-1
  tags:
  - key: example
    value: example
  userId: example-id
  userName: example-elasticacheuser
