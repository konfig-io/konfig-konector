# ElastiCacheSubnetGroup — ElastiCache
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticache.html#elasticachesubnetgroup
apiVersion: aws.konfig.io/v1alpha1
kind: ElastiCacheSubnetGroup
metadata:
  name: example-elasticachesubnetgroup
  namespace: default
spec:
  description: Managed by konfig-konector
  providerRef:
    name: example-elasticachesubnetgroup
    region: us-east-1
  subnetGroupName: example-elasticachesubnetgroup
  subnetRefs:
  - name: example-subnetrefs
  tags:
    environment: dev
    team: platform
