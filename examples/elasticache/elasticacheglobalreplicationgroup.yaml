# ElastiCacheGlobalReplicationGroup — ElastiCache
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticache.html#elasticacheglobalreplicationgroup
apiVersion: aws.konfig.io/v1alpha1
kind: ElastiCacheGlobalReplicationGroup
metadata:
  name: example-elasticacheglobalreplicationgroup
  namespace: default
spec:
  automaticFailoverEnabled: true
  cacheNodeType: example
  cacheParameterGroupName: example-elasticacheglobalreplicationgroup
  engine: example
  engineVersion: example
  globalNodeGroupCount: 2
  globalReplicationGroupDescription: Managed by konfig-konector
  globalReplicationGroupIdSuffix: example
  members:
  - replicationGroupRegion: example
    role: PRIMARY
  providerRef:
    name: example-elasticacheglobalreplicationgroup
    region: us-east-1
  regionalConfigurations:
  - replicationGroupRegion: example
    reshardingConfigurations:
    - preferredAvailabilityZones:
      - us-east-1a
