# ElastiCacheReplicationGroup — ElastiCache
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticache.html#elasticachereplicationgroup
apiVersion: aws.konfig.io/v1alpha1
kind: ElastiCacheReplicationGroup
metadata:
  name: example-elasticachereplicationgroup
  namespace: default
spec:
  atRestEncryption: true
  authToken: example
  authTokenRef:
    key: example
    name: example-elasticachereplicationgroup
  automaticFailover: false
  cacheNodeType: example
  description: Managed by konfig-konector
  engine: redis
  engineVersion: example
  numCacheClusters: 1
  providerRef:
    name: example-elasticachereplicationgroup
    region: us-east-1
  replicationGroupId: example-id
  securityGroupRefs:
  - name: example-securitygrouprefs
  snapshotRetentionLimit: 1
  subnetGroupRef: example
  tags:
    environment: dev
    team: platform
  transitEncryption: true
