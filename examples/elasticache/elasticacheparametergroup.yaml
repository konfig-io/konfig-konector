# ElastiCacheParameterGroup — ElastiCache
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/elasticache.html#elasticacheparametergroup
apiVersion: aws.konfig.io/v1alpha1
kind: ElastiCacheParameterGroup
metadata:
  name: example-elasticacheparametergroup
  namespace: default
spec:
  cacheParameterGroupFamily: example
  cacheParameterGroupName: example-elasticacheparametergroup
  description: Managed by konfig-konector
  parameters:
  - name: example-elasticacheparametergroup
    value: example
  providerRef:
    name: example-elasticacheparametergroup
    region: us-east-1
  tags:
    environment: dev
    team: platform
