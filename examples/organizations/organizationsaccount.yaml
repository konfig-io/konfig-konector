# OrganizationsAccount — Organizations
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/organizations.html#organizationsaccount
apiVersion: aws.konfig.io/v1alpha1
kind: OrganizationsAccount
metadata:
  name: example-organizationsaccount
  namespace: default
spec:
  accountName: example-organizationsaccount
  closeOnDelete: false
  email: ops@example.com
  iamUserAccessToBilling: ALLOW
  parentRef:
    name: example-parent
  providerRef:
    name: example-organizationsaccount
    region: us-east-1
  roleName: example-organizationsaccount
  tags:
    environment: dev
    team: platform
