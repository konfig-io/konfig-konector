# OrganizationsResourcePolicy — Organizations
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/organizations.html#organizationsresourcepolicy
apiVersion: aws.konfig.io/v1alpha1
kind: OrganizationsResourcePolicy
metadata:
  name: example-organizationsresourcepolicy
  namespace: default
spec:
  providerRef:
    name: example-organizationsresourcepolicy
    region: us-east-1
  tags:
  - key: example
    value: example
