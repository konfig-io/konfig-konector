# OrganizationsOU — Organizations
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/organizations.html#organizationsou
apiVersion: aws.konfig.io/v1alpha1
kind: OrganizationsOU
metadata:
  name: example-organizationsou
  namespace: default
spec:
  name: example-organizationsou
  parentRef:
    name: example-parent
  tags:
    environment: dev
    team: platform
