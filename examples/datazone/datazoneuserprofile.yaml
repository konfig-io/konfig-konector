# DataZoneUserProfile — DataZone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazoneuserprofile
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneUserProfile
metadata:
  name: example-datazoneuserprofile
  namespace: default
spec:
  domainIdentifier: example
  providerRef:
    name: example-datazoneuserprofile
    region: us-east-1
  sessionName: example-datazoneuserprofile
  status: ASSIGNED
  userIdentifier: example
  userType: IAM_USER
