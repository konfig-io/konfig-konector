# DataZoneGroupProfile — DataZone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazonegroupprofile
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneGroupProfile
metadata:
  name: example-datazonegroupprofile
  namespace: default
spec:
  domainIdentifier: example
  groupIdentifier: example
  groupType: DATAZONE_SSO_GROUP
  providerRef:
    name: example-datazonegroupprofile
    region: us-east-1
  rolePrincipalArn: arn:aws:iam::123456789012:role/example-role
  status: ASSIGNED
