# DataZoneEnvironmentActions — DataZone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazoneenvironmentactions
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneEnvironmentActions
metadata:
  name: example-datazoneenvironmentactions
  namespace: default
spec:
  description: Managed by konfig-konector
  domainIdentifier: example
  environmentIdentifier: example
  identifier: example
  name: example-datazoneenvironmentactions
  parameters:
    uri: example
  providerRef:
    name: example-datazoneenvironmentactions
    region: us-east-1
