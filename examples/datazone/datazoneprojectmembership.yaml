# DataZoneProjectMembership — datazone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazoneprojectmembership
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneProjectMembership
metadata:
  name: example-datazoneprojectmembership
  namespace: default
spec:
  designation: PROJECT_OWNER
  domainIdentifier: example
  projectIdentifier: example
  providerRef:
    name: example-datazoneprojectmembership
    region: us-east-1
