# DataZonePolicyGrant — datazone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazonepolicygrant
apiVersion: aws.konfig.io/v1alpha1
kind: DataZonePolicyGrant
metadata:
  name: example-datazonepolicygrant
  namespace: default
spec:
  domainIdentifier: example
  entityIdentifier: example
  entityType: DOMAIN_UNIT
  policyType: CREATE_DOMAIN_UNIT
  providerRef:
    name: example-datazonepolicygrant
    region: us-east-1
