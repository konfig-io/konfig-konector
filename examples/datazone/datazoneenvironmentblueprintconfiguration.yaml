# DataZoneEnvironmentBlueprintConfiguration — datazone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazoneenvironmentblueprintconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneEnvironmentBlueprintConfiguration
metadata:
  name: example-datazoneenvironmentblueprintconfiguration
  namespace: default
spec:
  domainIdentifier: example
  enabledRegions:
  - example
  environmentBlueprintIdentifier: example
  environmentRolePermissionBoundary: example
  globalParameters:
    key: value
  manageAccessRoleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-datazoneenvironmentblueprintconfiguration
    region: us-east-1
  provisioningRoleArn: arn:aws:iam::123456789012:role/example-role
  regionalParameters:
  - parameters:
      key: value
    region: us-east-1
