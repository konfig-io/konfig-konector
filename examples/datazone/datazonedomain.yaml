# DataZoneDomain — DataZone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazonedomain
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneDomain
metadata:
  name: example-datazonedomain
  namespace: default
spec:
  description: Managed by konfig-konector
  domainExecutionRole: example
  domainVersion: V1
  kmsKeyIdentifier: example
  name: example-datazonedomain
  providerRef:
    name: example-datazonedomain
    region: us-east-1
  serviceRole: example
  singleSignOn:
    idcInstanceArn: arn:aws:iam::123456789012:role/example-role
    type: IAM_IDC
    userAssignment: AUTOMATIC
  tags:
  - key: example
    value: example
