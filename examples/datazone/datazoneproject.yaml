# DataZoneProject — datazone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazoneproject
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneProject
metadata:
  name: example-datazoneproject
  namespace: default
spec:
  description: Managed by konfig-konector
  domainIdentifier: example
  glossaryTerms:
  - example
  membershipAssignments:
  - designation: PROJECT_OWNER
    member:
      groupIdentifier: example
      userIdentifier: example
  name: example-datazoneproject
  projectCategory: example
  projectExecutionRole: example
  projectProfileVersion: '1.0'
  providerRef:
    name: example-datazoneproject
    region: us-east-1
  resourceTags:
  - key: example
    value: example
  userParameters:
  - environmentConfigurationName: example-datazoneproject
    environmentParameters:
    - name: example-datazoneproject
      value: example
