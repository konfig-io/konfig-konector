# DataZoneDataSource — datazone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazonedatasource
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneDataSource
metadata:
  name: example-datazonedatasource
  namespace: default
spec:
  assetFormsInput:
  - content: example
    formName: example-datazonedatasource
    typeIdentifier: example
    typeRevision: example
  connectionIdentifier: example
  description: Managed by konfig-konector
  domainIdentifier: example
  enableSetting: ENABLED
  environmentIdentifier: example
  name: example-datazonedatasource
  projectIdentifier: example
  providerRef:
    name: example-datazonedatasource
    region: us-east-1
  publishOnImport: false
  recommendation:
    enableBusinessNameGeneration: true
  schedule:
    schedule: example
    timezone: example
  type: example
