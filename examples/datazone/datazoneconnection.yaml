# DataZoneConnection — DataZone
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/datazone.html#datazoneconnection
apiVersion: aws.konfig.io/v1alpha1
kind: DataZoneConnection
metadata:
  name: example-datazoneconnection
  namespace: default
spec:
  awsLocation:
    accessRole: example
    awsAccountId: '123456789012'
    awsRegion: example
  configurations:
  - classification: example
    properties:
      key: value
  description: Managed by konfig-konector
  domainIdentifier: example
  enableTrustedIdentityPropagation: true
  environmentIdentifier: example
  name: example-datazoneconnection
  projectIdentifier: example
  providerRef:
    name: example-datazoneconnection
    region: us-east-1
  scope: DOMAIN
