# BillingBillingView — billing
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/billing.html#billingbillingview
apiVersion: aws.konfig.io/v1alpha1
kind: BillingBillingView
metadata:
  name: example-billingbillingview
  namespace: default
spec:
  dataFilterExpression:
    dimensions:
      key: LINKED_ACCOUNT
      values:
      - example
    tags:
      key: example
      values:
      - example
    timeRange:
      beginDateInclusive: example
      endDateInclusive: example
  description: Managed by konfig-konector
  name: example-billingbillingview
  providerRef:
    name: example-billingbillingview
    region: us-east-1
  sourceViews:
  - example
  tags:
  - key: example
    value: example
