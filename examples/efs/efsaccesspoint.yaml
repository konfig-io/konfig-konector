# EFSAccessPoint — EFS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/efs.html#efsaccesspoint
apiVersion: aws.konfig.io/v1alpha1
kind: EFSAccessPoint
metadata:
  name: example-efsaccesspoint
  namespace: default
spec:
  fileSystemId: fs-0abc1234def567890
  posixUser:
    gid: 1
    secondaryGids:
    - 1
    uid: 1
  providerRef:
    name: example-efsaccesspoint
    region: us-east-1
  rootDirectory:
    creationInfo:
      ownerGid: 1
      ownerUid: 1
      permissions: example
    path: example
  tags:
    environment: dev
    team: platform
