# EFSMountTarget — EFS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/efs.html#efsmounttarget
apiVersion: aws.konfig.io/v1alpha1
kind: EFSMountTarget
metadata:
  name: example-efsmounttarget
  namespace: default
spec:
  fileSystemId: fs-0abc1234def567890
  ipAddress: example
  providerRef:
    name: example-efsmounttarget
    region: us-east-1
  securityGroups:
  - example
  subnetId: subnet-0abc12def3456789
