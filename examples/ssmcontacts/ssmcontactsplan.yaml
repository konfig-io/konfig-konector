# SSMContactsPlan — ssmcontacts
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssmcontacts.html#ssmcontactsplan
apiVersion: aws.konfig.io/v1alpha1
kind: SSMContactsPlan
metadata:
  name: example-ssmcontactsplan
  namespace: default
spec:
  providerRef:
    name: example-ssmcontactsplan
    region: us-east-1
  rotationIds:
  - example
  stages:
  - durationInMinutes: 1
