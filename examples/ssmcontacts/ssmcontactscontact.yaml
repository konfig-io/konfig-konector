# SSMContactsContact — Incident Manager Contacts
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssmcontacts.html#ssmcontactscontact
apiVersion: aws.konfig.io/v1alpha1
kind: SSMContactsContact
metadata:
  name: example-ssmcontactscontact
  namespace: default
spec:
  alias: example
  displayName: example-ssmcontactscontact
  plan:
  - durationInMinutes: 1
    rotationIds:
    - example
  providerRef:
    name: example-ssmcontactscontact
    region: us-east-1
  tags:
  - key: example
    value: example
  type: PERSONAL
