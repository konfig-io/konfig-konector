# CodeStarNotificationsNotificationRule — codestarnotifications
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codestarnotifications.html#codestarnotificationsnotificationrule
apiVersion: aws.konfig.io/v1alpha1
kind: CodeStarNotificationsNotificationRule
metadata:
  name: example-codestarnotificationsnotificationrule
  namespace: default
spec:
  createdBy: example
  detailType: BASIC
  eventTypeIds:
  - example
  name: example-codestarnotificationsnotificationrule
  providerRef:
    name: example-codestarnotificationsnotificationrule
    region: us-east-1
  resource: example
  status: ENABLED
  tags:
    environment: dev
    team: platform
  targetAddress: example
  targets:
  - targetAddress: example
    targetType: example
