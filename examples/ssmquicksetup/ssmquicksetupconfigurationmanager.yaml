# SSMQuickSetupConfigurationManager — ssmquicksetup
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/ssmquicksetup.html#ssmquicksetupconfigurationmanager
apiVersion: aws.konfig.io/v1alpha1
kind: SSMQuickSetupConfigurationManager
metadata:
  name: example-ssmquicksetupconfigurationmanager
  namespace: default
spec:
  configurationDefinitions:
  - localDeploymentAdministrationRoleArn: arn:aws:iam::123456789012:role/example-role
    localDeploymentExecutionRoleName: example-ssmquicksetupconfigurationmanager
    parameters:
      key: value
    type: example
    typeVersion: '1.0'
  description: Managed by konfig-konector
  name: example-ssmquicksetupconfigurationmanager
  providerRef:
    name: example-ssmquicksetupconfigurationmanager
    region: us-east-1
  tags:
    environment: dev
    team: platform
