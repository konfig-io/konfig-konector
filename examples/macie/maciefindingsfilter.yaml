# MacieFindingsFilter — macie
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/macie.html#maciefindingsfilter
apiVersion: aws.konfig.io/v1alpha1
kind: MacieFindingsFilter
metadata:
  name: example-maciefindingsfilter
  namespace: default
spec:
  action: ARCHIVE
  description: Managed by konfig-konector
  findingCriteria:
    criterion: {}
  name: example-maciefindingsfilter
  position: 1
  providerRef:
    name: example-maciefindingsfilter
    region: us-east-1
  tags:
  - key: example
    value: example
