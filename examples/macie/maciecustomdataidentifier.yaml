# MacieCustomDataIdentifier — macie
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/macie.html#maciecustomdataidentifier
apiVersion: aws.konfig.io/v1alpha1
kind: MacieCustomDataIdentifier
metadata:
  name: example-maciecustomdataidentifier
  namespace: default
spec:
  description: Managed by konfig-konector
  ignoreWords:
  - example
  keywords:
  - example
  maximumMatchDistance: 1
  name: example-maciecustomdataidentifier
  providerRef:
    name: example-maciecustomdataidentifier
    region: us-east-1
  regex: example
  tags:
  - key: example
    value: example
