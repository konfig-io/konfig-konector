# FISTargetAccountConfiguration — fis
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/fis.html#fistargetaccountconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: FISTargetAccountConfiguration
metadata:
  name: example-fistargetaccountconfiguration
  namespace: default
spec:
  accountId: '123456789012'
  description: Managed by konfig-konector
  experimentTemplateId: example-id
  providerRef:
    name: example-fistargetaccountconfiguration
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
