# FISExperimentTemplate — Fault Injection Service
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/fis.html#fisexperimenttemplate
apiVersion: aws.konfig.io/v1alpha1
kind: FISExperimentTemplate
metadata:
  name: example-fisexperimenttemplate
  namespace: default
spec:
  actions: {}
  description: Managed by konfig-konector
  experimentOptions:
    accountTargeting: multi-account
    emptyTargetResolutionMode: fail
  experimentReportConfiguration:
    dataSources:
      cloudWatchDashboards:
      - dashboardIdentifier: example
    outputs:
      experimentReportS3Configuration:
        bucketName: example-fisexperimenttemplate
        prefix: example
    postExperimentDuration: example
    preExperimentDuration: example
  logConfiguration:
    cloudWatchLogsConfiguration:
      logGroupArn: arn:aws:iam::123456789012:role/example-role
    logSchemaVersion: 1
    s3Configuration:
      bucketName: example-fisexperimenttemplate
      prefix: example
  providerRef:
    name: example-fisexperimenttemplate
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  stopConditions:
  - source: example
    value: example
  tags:
    environment: dev
    team: platform
  targets: {}
