# GlobalAcceleratorListener — globalaccelerator
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/globalaccelerator.html#globalacceleratorlistener
apiVersion: aws.konfig.io/v1alpha1
kind: GlobalAcceleratorListener
metadata:
  name: example-globalacceleratorlistener
  namespace: default
spec:
  acceleratorArn: arn:aws:iam::123456789012:role/example-role
  clientAffinity: NONE
  portRanges:
  - fromPort: 1
    toPort: 1
  protocol: TCP
  providerRef:
    name: example-globalacceleratorlistener
    region: us-east-1
