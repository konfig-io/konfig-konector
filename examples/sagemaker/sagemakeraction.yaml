# SageMakerAction — sagemaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakeraction
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerAction
metadata:
  name: example-sagemakeraction
  namespace: default
spec:
  actionName: example-sagemakeraction
  actionType: example
  description: Managed by konfig-konector
  metadataProperties:
    generatedBy: example
    repository: example
  properties:
    key: value
  providerRef:
    name: example-sagemakeraction
    region: us-east-1
  source:
    sourceType: example
    sourceUri: example
  status: Unknown
  tags:
  - key: example
    value: example
