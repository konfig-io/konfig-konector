# SageMakerImageVersion — SageMaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerimageversion
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerImageVersion
metadata:
  name: example-sagemakerimageversion
  namespace: default
spec:
  alias: example
  aliases:
  - example
  baseImage: example
  horovod: false
  imageName: example-sagemakerimageversion
  jobType: TRAINING
  mlFramework: example
  processor: CPU
  programmingLang: example
  providerRef:
    name: example-sagemakerimageversion
    region: us-east-1
  releaseNotes: example
  vendorGuidance: NOT_PROVIDED
