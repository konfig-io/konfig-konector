# SageMakerContext — SageMaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakercontext
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerContext
metadata:
  name: example-sagemakercontext
  namespace: default
spec:
  contextName: example-sagemakercontext
  contextType: example
  description: Managed by konfig-konector
  properties:
    key: value
  providerRef:
    name: example-sagemakercontext
    region: us-east-1
  source:
    sourceType: example
    sourceUri: example
  tags:
  - key: example
    value: example
