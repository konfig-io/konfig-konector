# SageMakerMlflowApp — SageMaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakermlflowapp
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerMlflowApp
metadata:
  name: example-sagemakermlflowapp
  namespace: default
spec:
  artifactStoreUri: example
  modelRegistrationMode: AutoModelRegistrationEnabled
  name: example-sagemakermlflowapp
  providerRef:
    name: example-sagemakermlflowapp
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  weeklyMaintenanceWindowStart: example
