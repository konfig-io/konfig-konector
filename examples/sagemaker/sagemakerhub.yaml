# SageMakerHub — SageMaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerhub
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerHub
metadata:
  name: example-sagemakerhub
  namespace: default
spec:
  hubDescription: Managed by konfig-konector
  hubDisplayName: example-sagemakerhub
  hubName: example-sagemakerhub
  hubSearchKeywords:
  - example
  providerRef:
    name: example-sagemakerhub
    region: us-east-1
  s3StorageConfig:
    s3OutputPath: example
  tags:
  - key: example
    value: example
