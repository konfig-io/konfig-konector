# SageMakerDevice — sagemaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerdevice
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerDevice
metadata:
  name: example-sagemakerdevice
  namespace: default
spec:
  device:
    description: Managed by konfig-konector
    deviceName: example-sagemakerdevice
    iotThingName: example-sagemakerdevice
  deviceFleetName: example-sagemakerdevice
  providerRef:
    name: example-sagemakerdevice
    region: us-east-1
  tags:
  - key: example
    value: example
