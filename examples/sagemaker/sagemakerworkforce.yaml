# SageMakerWorkforce — sagemaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerworkforce
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerWorkforce
metadata:
  name: example-sagemakerworkforce
  namespace: default
spec:
  cognitoConfig:
    clientId: example-id
    userPool: example
  ipAddressType: ipv4
  oidcConfig:
    authenticationRequestExtraParams:
      key: value
    authorizationEndpoint: example
    clientId: example-id
    clientSecret: example
    issuer: example
    jwksUri: example
    logoutEndpoint: example
    scope: example
    tokenEndpoint: example
    userInfoEndpoint: example
  providerRef:
    name: example-sagemakerworkforce
    region: us-east-1
  sourceIpConfig:
    cidrs:
    - 10.0.0.0/16
  tags:
  - key: example
    value: example
  workforceName: example-sagemakerworkforce
  workforceVpcConfig:
    securityGroupIds:
    - example
    subnets:
    - example
