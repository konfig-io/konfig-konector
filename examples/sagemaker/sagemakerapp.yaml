# SageMakerApp — sagemaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerapp
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerApp
metadata:
  name: example-sagemakerapp
  namespace: default
spec:
  appName: example-sagemakerapp
  appType: JupyterServer
  domainId: example-id
  providerRef:
    name: example-sagemakerapp
    region: us-east-1
  recoveryMode: false
  resourceSpec:
    instanceType: example
    lifecycleConfigArn: arn:aws:iam::123456789012:role/example-role
    sageMakerImageArn: arn:aws:iam::123456789012:role/example-role
    sageMakerImageVersionArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
  userProfileName: example-sagemakerapp
