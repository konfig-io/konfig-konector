# SageMakerPipeline — SageMaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerpipeline
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerPipeline
metadata:
  name: example-sagemakerpipeline
  namespace: default
spec:
  parallelismConfiguration:
    maxParallelExecutionSteps: 1
  pipelineDescription: Managed by konfig-konector
  pipelineDisplayName: example-sagemakerpipeline
  pipelineName: example-sagemakerpipeline
  providerRef:
    name: example-sagemakerpipeline
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
