# SageMakerProject — sagemaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerproject
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerProject
metadata:
  name: example-sagemakerproject
  namespace: default
spec:
  projectDescription: Managed by konfig-konector
  projectName: example-sagemakerproject
  providerRef:
    name: example-sagemakerproject
    region: us-east-1
  serviceCatalogProvisionedProductDetails:
    provisionedProductStatusMessage: example
  serviceCatalogProvisioningDetails:
    productId: example-id
    provisioningParameters:
    - key: example
      value: example
  tags:
  - key: example
    value: example
  templateProviderDetails:
  - cfnTemplateProviderDetail:
      parameters:
      - key: example
        value: example
      roleARN: arn:aws:iam::123456789012:role/example-role
      templateName: example-sagemakerproject
      templateURL: example
