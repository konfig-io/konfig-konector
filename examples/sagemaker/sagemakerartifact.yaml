# SageMakerArtifact — sagemaker
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/sagemaker.html#sagemakerartifact
apiVersion: aws.konfig.io/v1alpha1
kind: SageMakerArtifact
metadata:
  name: example-sagemakerartifact
  namespace: default
spec:
  artifactName: example-sagemakerartifact
  artifactType: example
  metadataProperties:
    generatedBy: example
    repository: example
  properties:
    key: value
  providerRef:
    name: example-sagemakerartifact
    region: us-east-1
  source:
    sourceTypes:
    - sourceIdType: MD5Hash
      value: example
    sourceUri: example
  tags:
  - key: example
    value: example
