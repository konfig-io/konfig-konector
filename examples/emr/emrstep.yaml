# EMRStep — EMR
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/emr.html#emrstep
apiVersion: aws.konfig.io/v1alpha1
kind: EMRStep
metadata:
  name: example-emrstep
  namespace: default
spec:
  actionOnFailure: example
  encryptionKeyArn: arn:aws:iam::123456789012:role/example-role
  hadoopJarStep:
    args:
    - example
    jar: example
    mainClass: example
    stepProperties:
    - key: example
      value: example
  jobFlowId: example-id
  logUri: example
  name: example-emrstep
  providerRef:
    name: example-emrstep
    region: us-east-1
