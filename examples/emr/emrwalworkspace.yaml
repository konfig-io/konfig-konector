# EMRWALWorkspace — emr
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/emr.html#emrwalworkspace
apiVersion: aws.konfig.io/v1alpha1
kind: EMRWALWorkspace
metadata:
  name: example-emrwalworkspace
  namespace: default
spec:
  providerRef:
    name: example-emrwalworkspace
    region: us-east-1
  tags:
  - key: example
    value: example
  walWorkspaceName: example-emrwalworkspace
