# EMRStudioSessionMapping — emr
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/emr.html#emrstudiosessionmapping
apiVersion: aws.konfig.io/v1alpha1
kind: EMRStudioSessionMapping
metadata:
  name: example-emrstudiosessionmapping
  namespace: default
spec:
  identityName: example-emrstudiosessionmapping
  identityType: USER
  providerRef:
    name: example-emrstudiosessionmapping
    region: us-east-1
  sessionPolicyArn: arn:aws:iam::123456789012:role/example-role
  studioId: example-id
