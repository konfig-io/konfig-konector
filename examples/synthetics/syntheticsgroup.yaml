# SyntheticsGroup — CloudWatch Synthetics
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/synthetics.html#syntheticsgroup
apiVersion: aws.konfig.io/v1alpha1
kind: SyntheticsGroup
metadata:
  name: example-syntheticsgroup
  namespace: default
spec:
  name: example-syntheticsgroup
  providerRef:
    name: example-syntheticsgroup
    region: us-east-1
  resourceArns:
  - example
  tags:
  - key: example
    value: example
