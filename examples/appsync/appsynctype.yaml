# AppSyncType — AppSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appsync.html#appsynctype
apiVersion: aws.konfig.io/v1alpha1
kind: AppSyncType
metadata:
  name: example-appsynctype
  namespace: default
spec:
  apiId: example-id
  definition: example
  format: SDL
  providerRef:
    name: example-appsynctype
    region: us-east-1
