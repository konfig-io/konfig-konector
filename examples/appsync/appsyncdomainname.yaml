# AppSyncDomainName — AppSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appsync.html#appsyncdomainname
apiVersion: aws.konfig.io/v1alpha1
kind: AppSyncDomainName
metadata:
  name: example-appsyncdomainname
  namespace: default
spec:
  certificateArn: arn:aws:iam::123456789012:role/example-role
  description: Managed by konfig-konector
  domainName: example-appsyncdomainname
  providerRef:
    name: example-appsyncdomainname
    region: us-east-1
  tags:
  - key: example
    value: example
