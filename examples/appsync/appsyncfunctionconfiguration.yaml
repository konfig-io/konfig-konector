# AppSyncFunctionConfiguration — appsync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appsync.html#appsyncfunctionconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: AppSyncFunctionConfiguration
metadata:
  name: example-appsyncfunctionconfiguration
  namespace: default
spec:
  apiId: example-id
  code: example
  codeS3Location: example
  dataSourceName: example-appsyncfunctionconfiguration
  description: Managed by konfig-konector
  functionVersion: '1.0'
  maxBatchSize: 2
  name: example-appsyncfunctionconfiguration
  providerRef:
    name: example-appsyncfunctionconfiguration
    region: us-east-1
  requestMappingTemplate: example
  requestMappingTemplateS3Location: example
  responseMappingTemplate: example
  responseMappingTemplateS3Location: example
  runtime:
    name: example-appsyncfunctionconfiguration
    runtimeVersion: '1.0'
  syncConfig:
    conflictDetection: example
    conflictHandler: example
    lambdaConflictHandlerConfig:
      lambdaConflictHandlerArn: arn:aws:iam::123456789012:role/example-role
