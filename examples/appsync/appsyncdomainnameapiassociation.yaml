# AppSyncDomainNameApiAssociation — AppSync
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/appsync.html#appsyncdomainnameapiassociation
apiVersion: aws.konfig.io/v1alpha1
kind: AppSyncDomainNameApiAssociation
metadata:
  name: example-appsyncdomainnameapiassociation
  namespace: default
spec:
  apiId: example-id
  domainName: example-appsyncdomainnameapiassociation
  providerRef:
    name: example-appsyncdomainnameapiassociation
    region: us-east-1
