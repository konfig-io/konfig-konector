# CodePipelineCustomActionType — CodePipeline
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codepipeline.html#codepipelinecustomactiontype
apiVersion: aws.konfig.io/v1alpha1
kind: CodePipelineCustomActionType
metadata:
  name: example-codepipelinecustomactiontype
  namespace: default
spec:
  category: example
  configurationProperties:
  - description: Managed by konfig-konector
    key: false
    name: example-codepipelinecustomactiontype
    queryable: false
    required: false
    secret: false
    type: example
  inputArtifactDetails:
    maximumCount: 2
    minimumCount: 2
  outputArtifactDetails:
    maximumCount: 2
    minimumCount: 2
  provider: example
  providerRef:
    name: example-codepipelinecustomactiontype
    region: us-east-1
  settings:
    entityUrlTemplate: example
    executionUrlTemplate: example
    revisionUrlTemplate: example
    thirdPartyConfigurationUrl: example
  tags:
  - key: example
    value: example
  version: '1.0'
