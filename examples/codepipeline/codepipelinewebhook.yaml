# CodePipelineWebhook — CodePipeline
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codepipeline.html#codepipelinewebhook
apiVersion: aws.konfig.io/v1alpha1
kind: CodePipelineWebhook
metadata:
  name: example-codepipelinewebhook
  namespace: default
spec:
  authentication: GITHUB_HMAC
  authenticationConfiguration:
    allowedIPRange: example
    secretToken: example
  filters:
  - jsonPath: example
    matchEquals: example
  name: example-codepipelinewebhook
  providerRef:
    name: example-codepipelinewebhook
    region: us-east-1
  registerWithThirdParty: false
  tags:
  - key: example
    value: example
  targetAction: example
  targetPipeline: example
  targetPipelineVersion: 1
