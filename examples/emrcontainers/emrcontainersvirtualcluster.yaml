# EMRContainersVirtualCluster — EMR on EKS
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/emrcontainers.html#emrcontainersvirtualcluster
apiVersion: aws.konfig.io/v1alpha1
kind: EMRContainersVirtualCluster
metadata:
  name: example-emrcontainersvirtualcluster
  namespace: default
spec:
  containerProvider:
    id: example-id
    info:
      eksInfo:
        name: example-eksinfo
    type: example
  name: example-emrcontainersvirtualcluster
  providerRef:
    name: example-emrcontainersvirtualcluster
    region: us-east-1
  sessionEnabled: true
  tags:
  - key: example
    value: example
