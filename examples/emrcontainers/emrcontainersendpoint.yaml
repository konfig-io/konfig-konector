# EMRContainersEndpoint — emrcontainers
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/emrcontainers.html#emrcontainersendpoint
apiVersion: aws.konfig.io/v1alpha1
kind: EMRContainersEndpoint
metadata:
  name: example-emrcontainersendpoint
  namespace: default
spec:
  configurationOverrides:
    applicationConfiguration:
    - classification: example
    monitoringConfiguration:
      cloudWatchMonitoringConfiguration:
        logGroupName: example-emrcontainersendpoint
        logStreamNamePrefix: example
      containerLogRotationConfiguration:
        maxFilesToKeep: 1
        rotationSize: example
      persistentAppUI: ENABLED
      s3MonitoringConfiguration:
        logUri: example
  executionRoleArn: arn:aws:iam::123456789012:role/example-role
  name: example-emrcontainersendpoint
  providerRef:
    name: example-emrcontainersendpoint
    region: us-east-1
  releaseLabel: example
  sessionIdleTimeoutInMinutes: 1
  tags:
  - key: example
    value: example
  type: example
  virtualClusterId: example-id
