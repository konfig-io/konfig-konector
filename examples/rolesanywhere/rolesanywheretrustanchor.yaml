# RolesAnywhereTrustAnchor — IAM Roles Anywhere
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rolesanywhere.html#rolesanywheretrustanchor
apiVersion: aws.konfig.io/v1alpha1
kind: RolesAnywhereTrustAnchor
metadata:
  name: example-rolesanywheretrustanchor
  namespace: default
spec:
  enabled: true
  name: example-rolesanywheretrustanchor
  notificationSettings:
  - channel: ALL
    enabled: true
    event: CA_CERTIFICATE_EXPIRY
    threshold: 1
  providerRef:
    name: example-rolesanywheretrustanchor
    region: us-east-1
  source:
    sourceType: AWS_ACM_PCA
  tags:
  - key: example
    value: example
