# RolesAnywhereCRL — rolesanywhere
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/rolesanywhere.html#rolesanywherecrl
apiVersion: aws.konfig.io/v1alpha1
kind: RolesAnywhereCRL
metadata:
  name: example-rolesanywherecrl
  namespace: default
spec:
  crlData: example
  enabled: true
  name: example-rolesanywherecrl
  providerRef:
    name: example-rolesanywherecrl
    region: us-east-1
  tags:
  - key: example
    value: example
  trustAnchorArn: arn:aws:iam::123456789012:role/example-role
