# MSKVpcConnection — MSK
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kafka.html#mskvpcconnection
apiVersion: aws.konfig.io/v1alpha1
kind: MSKVpcConnection
metadata:
  name: example-mskvpcconnection
  namespace: default
spec:
  authentication: SASL_IAM
  clientSubnets:
  - example
  providerRef:
    name: example-mskvpcconnection
    region: us-east-1
  securityGroups:
  - example
  tags:
    environment: dev
    team: platform
  targetClusterArn: arn:aws:iam::123456789012:role/example-role
  vpcId: vpc-0abc12def3456789
