# MSKServerlessCluster — MSK
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kafka.html#mskserverlesscluster
apiVersion: aws.konfig.io/v1alpha1
kind: MSKServerlessCluster
metadata:
  name: example-mskserverlesscluster
  namespace: default
spec:
  clusterName: example-mskserverlesscluster
  providerRef:
    name: example-mskserverlesscluster
    region: us-east-1
  tags:
    environment: dev
    team: platform
  vpcConfigs:
  - securityGroupIDs:
    - example
    subnetIDs:
    - example
