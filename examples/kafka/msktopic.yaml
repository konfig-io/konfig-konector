# MSKTopic — MSK
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/kafka.html#msktopic
apiVersion: aws.konfig.io/v1alpha1
kind: MSKTopic
metadata:
  name: example-msktopic
  namespace: default
spec:
  clusterArn: arn:aws:iam::123456789012:role/example-role
  configs: example
  partitionCount: 1
  providerRef:
    name: example-msktopic
    region: us-east-1
  replicationFactor: 1
  topicName: example-msktopic
