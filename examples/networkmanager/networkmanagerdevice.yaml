# NetworkManagerDevice — networkmanager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkmanager.html#networkmanagerdevice
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkManagerDevice
metadata:
  name: example-networkmanagerdevice
  namespace: default
spec:
  awsLocation:
    subnetArn: arn:aws:iam::123456789012:role/example-role
    zone: example
  description: Managed by konfig-konector
  globalNetworkId: example-id
  location:
    address: example
    latitude: example
    longitude: example
  model: example
  providerRef:
    name: example-networkmanagerdevice
    region: us-east-1
  serialNumber: example
  tags:
  - key: example
    value: example
  type: example
  vendor: example
