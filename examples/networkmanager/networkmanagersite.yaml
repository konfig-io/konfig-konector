# NetworkManagerSite — Network Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkmanager.html#networkmanagersite
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkManagerSite
metadata:
  name: example-networkmanagersite
  namespace: default
spec:
  description: Managed by konfig-konector
  globalNetworkId: example-id
  location:
    address: example
    latitude: example
    longitude: example
  providerRef:
    name: example-networkmanagersite
    region: us-east-1
  tags:
  - key: example
    value: example
