# NetworkManagerConnectPeer — Network Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkmanager.html#networkmanagerconnectpeer
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkManagerConnectPeer
metadata:
  name: example-networkmanagerconnectpeer
  namespace: default
spec:
  bgpOptions:
    peerAsn: 1
  connectAttachmentId: example-id
  coreNetworkAddress: example
  insideCidrBlocks:
  - 10.0.0.0/16
  peerAddress: example
  providerRef:
    name: example-networkmanagerconnectpeer
    region: us-east-1
  subnetArn: arn:aws:iam::123456789012:role/example-role
  tags:
  - key: example
    value: example
