# NetworkManagerLinkAssociation — Network Manager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkmanager.html#networkmanagerlinkassociation
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkManagerLinkAssociation
metadata:
  name: example-networkmanagerlinkassociation
  namespace: default
spec:
  deviceId: example-id
  globalNetworkId: example-id
  linkId: example-id
  providerRef:
    name: example-networkmanagerlinkassociation
    region: us-east-1
