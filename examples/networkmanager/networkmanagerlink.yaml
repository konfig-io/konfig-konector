# NetworkManagerLink — networkmanager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkmanager.html#networkmanagerlink
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkManagerLink
metadata:
  name: example-networkmanagerlink
  namespace: default
spec:
  bandwidth:
    downloadSpeed: 1
    uploadSpeed: 1
  description: Managed by konfig-konector
  globalNetworkId: example-id
  provider: example
  providerRef:
    name: example-networkmanagerlink
    region: us-east-1
  siteId: example-id
  tags:
  - key: example
    value: example
  type: example
