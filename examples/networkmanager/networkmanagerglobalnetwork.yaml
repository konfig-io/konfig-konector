# NetworkManagerGlobalNetwork — networkmanager
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/networkmanager.html#networkmanagerglobalnetwork
apiVersion: aws.konfig.io/v1alpha1
kind: NetworkManagerGlobalNetwork
metadata:
  name: example-networkmanagerglobalnetwork
  namespace: default
spec:
  createdAt: example
  description: Managed by konfig-konector
  providerRef:
    name: example-networkmanagerglobalnetwork
    region: us-east-1
  state: example
  tags:
  - key: example
    value: example
