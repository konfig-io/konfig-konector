# EventRule — EventBridge
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventbridge.html#eventrule
apiVersion: aws.konfig.io/v1alpha1
kind: EventRule
metadata:
  name: example-eventrule
  namespace: default
spec:
  description: Managed by konfig-konector
  eventBusRef:
    eventBusName: example-eventrule
    name: example-eventrule
  eventPattern: example
  providerRef:
    name: example-eventrule
    region: us-east-1
  roleArn: arn:aws:iam::123456789012:role/example-role
  ruleName: example-eventrule
  scheduleExpression: example
  state: ENABLED
  tags:
    environment: dev
    team: platform
