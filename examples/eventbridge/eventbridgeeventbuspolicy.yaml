# EventBridgeEventBusPolicy — EventBridge
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventbridge.html#eventbridgeeventbuspolicy
apiVersion: aws.konfig.io/v1alpha1
kind: EventBridgeEventBusPolicy
metadata:
  name: example-eventbridgeeventbuspolicy
  namespace: default
spec:
  action: example
  condition:
    key: example
    type: example
    value: example
  eventBusName: example-eventbridgeeventbuspolicy
  principal: example
  providerRef:
    name: example-eventbridgeeventbuspolicy
    region: us-east-1
  statementId: example-id
