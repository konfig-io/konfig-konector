# EventTarget — EventBridge
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventbridge.html#eventtarget
apiVersion: aws.konfig.io/v1alpha1
kind: EventTarget
metadata:
  name: example-eventtarget
  namespace: default
spec:
  eventBusRef:
    eventBusName: example-eventtarget
    name: example-eventtarget
  eventRuleRef:
    name: example-eventtarget
    ruleName: example-eventtarget
  providerRef:
    name: example-eventtarget
    region: us-east-1
  targets:
  - arn: arn:aws:iam::123456789012:role/example-role
    id: example-id
    input: example
    inputPath: example
    roleArn: arn:aws:iam::123456789012:role/example-role
