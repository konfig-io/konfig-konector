# EventBridgeApiDestination — EventBridge
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventbridge.html#eventbridgeapidestination
apiVersion: aws.konfig.io/v1alpha1
kind: EventBridgeApiDestination
metadata:
  name: example-eventbridgeapidestination
  namespace: default
spec:
  connectionArn: arn:aws:iam::123456789012:role/example-role
  description: Managed by konfig-konector
  httpMethod: GET
  invocationEndpoint: example
  invocationRateLimitPerSecond: 1
  name: example-eventbridgeapidestination
  providerRef:
    name: example-eventbridgeapidestination
    region: us-east-1
