# ECSScheduledTask — EventBridge
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/eventbridge.html#ecsscheduledtask
apiVersion: aws.konfig.io/v1alpha1
kind: ECSScheduledTask
metadata:
  name: example-ecsscheduledtask
  namespace: default
spec:
  description: Managed by konfig-konector
  eventBusName: example-ecsscheduledtask
  roleArn: arn:aws:iam::123456789012:role/example-role
  ruleName: example-ecsscheduledtask
  scheduleExpression: example
  tags:
    environment: dev
    team: platform
  target:
    clusterArn: arn:aws:iam::123456789012:role/example-role
    launchType: FARGATE
    networkConfiguration:
      assignPublicIp: ENABLED
      securityGroups:
      - example
      subnets:
      - example
    platformVersion: '1.0'
    taskCount: 2
    taskDefinitionArn: arn:aws:iam::123456789012:role/example-role
