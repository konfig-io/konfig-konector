# ApiGatewayVpcLink — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayvpclink
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayVpcLink
metadata:
  name: example-apigatewayvpclink
  namespace: default
spec:
  description: Managed by konfig-konector
  name: example-apigatewayvpclink
  providerRef:
    name: example-apigatewayvpclink
    region: us-east-1
  tags:
  - key: example
    value: example
  targetArns:
  - example
