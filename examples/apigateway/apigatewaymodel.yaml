# ApiGatewayModel — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaymodel
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayModel
metadata:
  name: example-apigatewaymodel
  namespace: default
spec:
  contentType: example
  description: Managed by konfig-konector
  name: example-apigatewaymodel
  providerRef:
    name: example-apigatewaymodel
    region: us-east-1
  restApiId: example-id
