# ApiGatewayUsagePlanKey — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayusageplankey
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayUsagePlanKey
metadata:
  name: example-apigatewayusageplankey
  namespace: default
spec:
  keyId: example-id
  keyType: API_KEY
  providerRef:
    name: example-apigatewayusageplankey
    region: us-east-1
  usagePlanId: example-id
