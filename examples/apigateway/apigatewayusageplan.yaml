# ApiGatewayUsagePlan — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayusageplan
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayUsagePlan
metadata:
  name: example-apigatewayusageplan
  namespace: default
spec:
  apiStages:
  - stage: example
    throttle: {}
  description: Managed by konfig-konector
  providerRef:
    name: example-apigatewayusageplan
    region: us-east-1
  quota:
    limit: 1
    offset: 1
    period: example
  tags:
  - key: example
    value: example
  throttle:
    burstLimit: 1
    rateLimit: 1
  usagePlanName: example-apigatewayusageplan
