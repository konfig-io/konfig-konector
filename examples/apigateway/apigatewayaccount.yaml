# ApiGatewayAccount — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayaccount
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayAccount
metadata:
  name: example-apigatewayaccount
  namespace: default
spec:
  cloudWatchRoleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-apigatewayaccount
    region: us-east-1
