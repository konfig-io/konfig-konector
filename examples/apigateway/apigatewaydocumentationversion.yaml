# ApiGatewayDocumentationVersion — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaydocumentationversion
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayDocumentationVersion
metadata:
  name: example-apigatewaydocumentationversion
  namespace: default
spec:
  description: Managed by konfig-konector
  documentationVersion: '1.0'
  providerRef:
    name: example-apigatewaydocumentationversion
    region: us-east-1
  restApiId: example-id
