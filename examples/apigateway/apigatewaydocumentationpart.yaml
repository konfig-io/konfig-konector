# ApiGatewayDocumentationPart — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaydocumentationpart
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayDocumentationPart
metadata:
  name: example-apigatewaydocumentationpart
  namespace: default
spec:
  location:
    method: example
    name: example-apigatewaydocumentationpart
    path: example
    statusCode: example
    type: API
  properties: example
  providerRef:
    name: example-apigatewaydocumentationpart
    region: us-east-1
  restApiId: example-id
