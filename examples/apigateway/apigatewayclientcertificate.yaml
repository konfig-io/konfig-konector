# ApiGatewayClientCertificate — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayclientcertificate
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayClientCertificate
metadata:
  name: example-apigatewayclientcertificate
  namespace: default
spec:
  description: Managed by konfig-konector
  providerRef:
    name: example-apigatewayclientcertificate
    region: us-east-1
  tags:
  - key: example
    value: example
