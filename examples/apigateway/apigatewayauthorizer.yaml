# ApiGatewayAuthorizer — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayauthorizer
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayAuthorizer
metadata:
  name: example-apigatewayauthorizer
  namespace: default
spec:
  authType: example
  authorizerCredentials: example
  authorizerResultTtlInSeconds: 300
  authorizerUri: example
  identitySource: example
  identityValidationExpression: example
  name: example-apigatewayauthorizer
  providerARNs:
  - example
  providerRef:
    name: example-apigatewayauthorizer
    region: us-east-1
  restApiId: example-id
  type: example
