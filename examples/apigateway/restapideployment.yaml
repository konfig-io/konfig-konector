# RestAPIDeployment — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#restapideployment
apiVersion: aws.konfig.io/v1alpha1
kind: RestAPIDeployment
metadata:
  name: example-restapideployment
  namespace: default
spec:
  description: Managed by konfig-konector
  restApiRef:
    name: example-restapideployment
  stageName: example-restapideployment
