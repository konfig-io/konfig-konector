# ApiGatewayBasePathMapping — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaybasepathmapping
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayBasePathMapping
metadata:
  name: example-apigatewaybasepathmapping
  namespace: default
spec:
  basePath: example
  domainName: example-apigatewaybasepathmapping
  providerRef:
    name: example-apigatewaybasepathmapping
    region: us-east-1
  stage: example
