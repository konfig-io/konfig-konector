# RestAPIStage — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#restapistage
apiVersion: aws.konfig.io/v1alpha1
kind: RestAPIStage
metadata:
  name: example-restapistage
  namespace: default
spec:
  deploymentRef:
    name: example-restapistage
  description: Managed by konfig-konector
  restApiRef:
    name: example-restapistage
  stageName: example-restapistage
  tags:
    environment: dev
    team: platform
  tracingEnabled: true
  variables:
    key: value
