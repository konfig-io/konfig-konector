# ApiGatewayResource — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayresource
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayResource
metadata:
  name: example-apigatewayresource
  namespace: default
spec:
  parentId: example-id
  pathPart: example
  providerRef:
    name: example-apigatewayresource
    region: us-east-1
  restApiId: example-id
