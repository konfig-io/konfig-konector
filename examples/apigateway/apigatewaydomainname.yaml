# ApiGatewayDomainName — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaydomainname
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayDomainName
metadata:
  name: example-apigatewaydomainname
  namespace: default
spec:
  certificateArn: arn:aws:iam::123456789012:role/example-role
  domainName: example-apigatewaydomainname
  endpointAccessMode: example
  endpointConfiguration:
    ipAddressType: example
    types:
    - example
  mutualTlsAuthentication:
    truststoreUri: example
    truststoreVersion: '1.0'
  ownershipVerificationCertificateArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-apigatewaydomainname
    region: us-east-1
  regionalCertificateArn: arn:aws:iam::123456789012:role/example-role
  routingMode: BASE_PATH_MAPPING_ONLY
  securityPolicy: example
  tags:
  - key: example
    value: example
