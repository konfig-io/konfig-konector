# ApiGatewayGatewayResponse — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaygatewayresponse
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayGatewayResponse
metadata:
  name: example-apigatewaygatewayresponse
  namespace: default
spec:
  providerRef:
    name: example-apigatewaygatewayresponse
    region: us-east-1
  responseParameters:
    key: value
  responseTemplates:
    key: value
  responseType: example
  restApiId: example-id
  statusCode: example
