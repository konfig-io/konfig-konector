# ApiGatewayDomainNameV2 — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewaydomainnamev2
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayDomainNameV2
metadata:
  name: example-apigatewaydomainnamev2
  namespace: default
spec:
  certificateArn: arn:aws:iam::123456789012:role/example-role
  domainName: example-apigatewaydomainnamev2
  endpointAccessMode: example
  endpointConfiguration:
    ipAddressType: example
    types:
    - example
  providerRef:
    name: example-apigatewaydomainnamev2
    region: us-east-1
  routingMode: BASE_PATH_MAPPING_ONLY
  securityPolicy: example
  tags:
  - key: example
    value: example
