# ApiGatewayRequestValidator — API Gateway
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigateway.html#apigatewayrequestvalidator
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayRequestValidator
metadata:
  name: example-apigatewayrequestvalidator
  namespace: default
spec:
  name: example-apigatewayrequestvalidator
  providerRef:
    name: example-apigatewayrequestvalidator
    region: us-east-1
  restApiId: example-id
  validateRequestBody: false
  validateRequestParameters: false
