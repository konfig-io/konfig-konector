# NeptuneEventSubscription — neptune
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/neptune.html#neptuneeventsubscription
apiVersion: aws.konfig.io/v1alpha1
kind: NeptuneEventSubscription
metadata:
  name: example-neptuneeventsubscription
  namespace: default
spec:
  enabled: true
  eventCategories:
  - example
  providerRef:
    name: example-neptuneeventsubscription
    region: us-east-1
  snsTopicArn: arn:aws:iam::123456789012:role/example-role
  sourceIds:
  - example
  sourceType: example
  subscriptionName: example-neptuneeventsubscription
  tags:
  - key: example
    value: example
