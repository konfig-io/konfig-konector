# NeptuneGlobalCluster — neptune
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/neptune.html#neptuneglobalcluster
apiVersion: aws.konfig.io/v1alpha1
kind: NeptuneGlobalCluster
metadata:
  name: example-neptuneglobalcluster
  namespace: default
spec:
  deletionProtection: true
  engine: neptune
  engineVersion: example
  globalClusterIdentifier: example
  providerRef:
    name: example-neptuneglobalcluster
    region: us-east-1
  sourceDBClusterIdentifier: example
  storageEncrypted: true
  tags:
  - key: example
    value: example
