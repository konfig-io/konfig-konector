# NeptuneDBParameterGroup — neptune
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/neptune.html#neptunedbparametergroup
apiVersion: aws.konfig.io/v1alpha1
kind: NeptuneDBParameterGroup
metadata:
  name: example-neptunedbparametergroup
  namespace: default
spec:
  description: Managed by konfig-konector
  family: example
  name: example-neptunedbparametergroup
  providerRef:
    name: example-neptunedbparametergroup
    region: us-east-1
  tags:
  - key: example
    value: example
