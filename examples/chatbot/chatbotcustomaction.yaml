# ChatbotCustomAction — chatbot
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/chatbot.html#chatbotcustomaction
apiVersion: aws.konfig.io/v1alpha1
kind: ChatbotCustomAction
metadata:
  name: example-chatbotcustomaction
  namespace: default
spec:
  actionName: example-chatbotcustomaction
  aliasName: example-chatbotcustomaction
  attachments:
  - buttonText: example
    criteria:
    - operator: HAS_VALUE
      value: example
      variableName: example-chatbotcustomaction
    notificationType: example
    variables:
      key: value
  definition:
    commandText: example
  providerRef:
    name: example-chatbotcustomaction
    region: us-east-1
  tags:
  - key: example
    value: example
