# ChatbotSlackChannelConfiguration — Chatbot
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/chatbot.html#chatbotslackchannelconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: ChatbotSlackChannelConfiguration
metadata:
  name: example-chatbotslackchannelconfiguration
  namespace: default
spec:
  configurationName: example-chatbotslackchannelconfiguration
  customizationResourceArns:
  - example
  guardrailPolicies:
  - example
  iamRoleArn: arn:aws:iam::123456789012:role/example-role
  loggingLevel: example
  providerRef:
    name: example-chatbotslackchannelconfiguration
    region: us-east-1
  slackChannelId: example-id
  slackWorkspaceId: example-id
  snsTopicArns:
  - example
  tags:
  - key: example
    value: example
  userRoleRequired: false
