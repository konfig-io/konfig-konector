# ResourceExplorer2Index — Resource Explorer
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/resourceexplorer2.html#resourceexplorer2index
apiVersion: aws.konfig.io/v1alpha1
kind: ResourceExplorer2Index
metadata:
  name: example-resourceexplorer2index
  namespace: default
spec:
  providerRef:
    name: example-resourceexplorer2index
    region: us-east-1
  tags:
    environment: dev
    team: platform
  type: LOCAL
