# ResourceExplorer2View — resourceexplorer2
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/resourceexplorer2.html#resourceexplorer2view
apiVersion: aws.konfig.io/v1alpha1
kind: ResourceExplorer2View
metadata:
  name: example-resourceexplorer2view
  namespace: default
spec:
  filters:
    filterString: example
  includedProperties:
  - name: example-includedproperties
  providerRef:
    name: example-resourceexplorer2view
    region: us-east-1
  scope: example
  tags:
    environment: dev
    team: platform
  viewName: example-resourceexplorer2view
