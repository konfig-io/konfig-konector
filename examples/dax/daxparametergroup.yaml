# DAXParameterGroup — DAX
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dax.html#daxparametergroup
apiVersion: aws.konfig.io/v1alpha1
kind: DAXParameterGroup
metadata:
  name: example-daxparametergroup
  namespace: default
spec:
  description: Managed by konfig-konector
  parameterGroupName: example-daxparametergroup
  providerRef:
    name: example-daxparametergroup
    region: us-east-1
