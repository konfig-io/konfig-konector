# DAXCluster — DAX
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/dax.html#daxcluster
apiVersion: aws.konfig.io/v1alpha1
kind: DAXCluster
metadata:
  name: example-daxcluster
  namespace: default
spec:
  availabilityZones:
  - us-east-1a
  clusterName: example-daxcluster
  iamRoleArn: arn:aws:iam::123456789012:role/example-role
  nodeType: example
  parameterGroupName: example-daxcluster
  providerRef:
    name: example-daxcluster
    region: us-east-1
  replicationFactor: 1
  securityGroupIds:
  - example
  subnetGroupName: example-daxcluster
  tags:
    environment: dev
    team: platform
