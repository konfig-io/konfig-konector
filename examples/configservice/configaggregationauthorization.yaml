# ConfigAggregationAuthorization — AWS Config
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/configservice.html#configaggregationauthorization
apiVersion: aws.konfig.io/v1alpha1
kind: ConfigAggregationAuthorization
metadata:
  name: example-configaggregationauthorization
  namespace: default
spec:
  authorizedAccountId: '123456789012'
  authorizedAwsRegion: example
  providerRef:
    name: example-configaggregationauthorization
    region: us-east-1
  tags:
  - key: example
    value: example
