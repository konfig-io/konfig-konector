# ConfigDeliveryChannel — AWS Config
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/configservice.html#configdeliverychannel
apiVersion: aws.konfig.io/v1alpha1
kind: ConfigDeliveryChannel
metadata:
  name: example-configdeliverychannel
  namespace: default
spec:
  channelName: example-configdeliverychannel
  deliveryFrequency: One_Hour
  s3BucketName: example-configdeliverychannel
  s3KeyPrefix: example
  snsTopicARN: arn:aws:iam::123456789012:role/example-role
