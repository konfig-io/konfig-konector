# ConfigStoredQuery — AWS Config
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/configservice.html#configstoredquery
apiVersion: aws.konfig.io/v1alpha1
kind: ConfigStoredQuery
metadata:
  name: example-configstoredquery
  namespace: default
spec:
  providerRef:
    name: example-configstoredquery
    region: us-east-1
  queryDescription: Managed by konfig-konector
  queryExpression: example
  queryName: example-configstoredquery
  tags:
  - key: example
    value: example
