# ConfigConfigurationAggregator — AWS Config
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/configservice.html#configconfigurationaggregator
apiVersion: aws.konfig.io/v1alpha1
kind: ConfigConfigurationAggregator
metadata:
  name: example-configconfigurationaggregator
  namespace: default
spec:
  accountAggregationSources:
  - accountIds:
    - example
    allAwsRegions: false
    awsRegions:
    - example
  configurationAggregatorName: example-configconfigurationaggregator
  organizationAggregationSource:
    allAwsRegions: false
    awsRegions:
    - example
    roleArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-configconfigurationaggregator
    region: us-east-1
  tags:
  - key: example
    value: example
