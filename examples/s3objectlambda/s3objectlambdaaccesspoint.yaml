# S3ObjectLambdaAccessPoint — s3objectlambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3objectlambda.html#s3objectlambdaaccesspoint
apiVersion: aws.konfig.io/v1alpha1
kind: S3ObjectLambdaAccessPoint
metadata:
  name: example-s3objectlambdaaccesspoint
  namespace: default
spec:
  name: example-s3objectlambdaaccesspoint
  objectLambdaConfiguration:
    allowedFeatures:
    - example
    cloudWatchMetricsEnabled: true
    supportingAccessPoint: example
    transformationConfigurations:
    - actions:
      - example
  providerRef:
    name: example-s3objectlambdaaccesspoint
    region: us-east-1
