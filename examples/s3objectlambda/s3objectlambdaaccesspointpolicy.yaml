# S3ObjectLambdaAccessPointPolicy — s3objectlambda
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/s3objectlambda.html#s3objectlambdaaccesspointpolicy
apiVersion: aws.konfig.io/v1alpha1
kind: S3ObjectLambdaAccessPointPolicy
metadata:
  name: example-s3objectlambdaaccesspointpolicy
  namespace: default
spec:
  objectLambdaAccessPoint: example
  providerRef:
    name: example-s3objectlambdaaccesspointpolicy
    region: us-east-1
