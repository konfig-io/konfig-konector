# ApplicationSignalsGroupingConfiguration — applicationsignals
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/applicationsignals.html#applicationsignalsgroupingconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: ApplicationSignalsGroupingConfiguration
metadata:
  name: example-applicationsignalsgroupingconfiguration
  namespace: default
spec:
  groupingAttributeDefinitions:
  - defaultGroupingValue: example
    groupingName: example-applicationsignalsgroupingconfiguration
    groupingSourceKeys:
    - example
  providerRef:
    name: example-applicationsignalsgroupingconfiguration
    region: us-east-1
