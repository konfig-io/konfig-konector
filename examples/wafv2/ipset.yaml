# IPSet — WAFv2
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/wafv2.html#ipset
apiVersion: aws.konfig.io/v1alpha1
kind: IPSet
metadata:
  name: example-ipset
  namespace: default
spec:
  addresses:
  - example
  description: Managed by konfig-konector
  ipAddressVersion: IPV4
  name: example-ipset
  providerRef:
    name: example-ipset
    region: us-east-1
  scope: CLOUDFRONT
  tags:
    environment: dev
    team: platform
