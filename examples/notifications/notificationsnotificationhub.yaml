# NotificationsNotificationHub — User Notifications
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/notifications.html#notificationsnotificationhub
apiVersion: aws.konfig.io/v1alpha1
kind: NotificationsNotificationHub
metadata:
  name: example-notificationsnotificationhub
  namespace: default
spec:
  providerRef:
    name: example-notificationsnotificationhub
    region: us-east-1
  region: us-east-1
