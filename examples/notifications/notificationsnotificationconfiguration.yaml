# NotificationsNotificationConfiguration — User Notifications
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/notifications.html#notificationsnotificationconfiguration
apiVersion: aws.konfig.io/v1alpha1
kind: NotificationsNotificationConfiguration
metadata:
  name: example-notificationsnotificationconfiguration
  namespace: default
spec:
  aggregationDuration: LONG
  description: Managed by konfig-konector
  name: example-notificationsnotificationconfiguration
  providerRef:
    name: example-notificationsnotificationconfiguration
    region: us-east-1
  tags:
  - key: example
    value: example
