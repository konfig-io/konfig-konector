# NotificationsEventRule — notifications
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/notifications.html#notificationseventrule
apiVersion: aws.konfig.io/v1alpha1
kind: NotificationsEventRule
metadata:
  name: example-notificationseventrule
  namespace: default
spec:
  eventPattern: example
  eventType: example
  notificationConfigurationArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-notificationseventrule
    region: us-east-1
  regions:
  - example
  source: example
