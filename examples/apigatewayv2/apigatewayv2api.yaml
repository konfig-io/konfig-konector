# APIGatewayV2API — API Gateway v2
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigatewayv2.html#apigatewayv2api
apiVersion: aws.konfig.io/v1alpha1
kind: APIGatewayV2API
metadata:
  name: example-apigatewayv2api
  namespace: default
spec:
  apiKeySelectionExpression: example
  corsConfiguration:
    allowCredentials: false
    allowHeaders:
    - example
    allowMethods:
    - example
    allowOrigins:
    - example
    exposeHeaders:
    - example
    maxAge: 1
  description: Managed by konfig-konector
  disableExecuteApiEndpoint: false
  name: example-apigatewayv2api
  protocolType: HTTP
  routeSelectionExpression: example
  tags:
    environment: dev
    team: platform
