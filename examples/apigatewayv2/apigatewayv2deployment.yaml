# ApiGatewayV2Deployment — API Gateway v2
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigatewayv2.html#apigatewayv2deployment
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayV2Deployment
metadata:
  name: example-apigatewayv2deployment
  namespace: default
spec:
  apiId: example-id
  description: Managed by konfig-konector
  providerRef:
    name: example-apigatewayv2deployment
    region: us-east-1
  stageName: example-apigatewayv2deployment
