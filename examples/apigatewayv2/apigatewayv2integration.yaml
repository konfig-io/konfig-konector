# APIGatewayV2Integration — API Gateway v2
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigatewayv2.html#apigatewayv2integration
apiVersion: aws.konfig.io/v1alpha1
kind: APIGatewayV2Integration
metadata:
  name: example-apigatewayv2integration
  namespace: default
spec:
  apiRef:
    name: example-apigatewayv2integration
  connectionType: INTERNET
  credentialsArn: arn:aws:iam::123456789012:role/example-role
  description: Managed by konfig-konector
  functionRef:
    functionName: example-apigatewayv2integration
    name: example-apigatewayv2integration
  integrationMethod: example
  integrationType: AWS
  integrationUri: example
  payloadFormatVersion: '1.0'
  requestParameters:
    key: value
  timeoutInMillis: 50
  vpcLinkRef:
    name: example-vpclink
