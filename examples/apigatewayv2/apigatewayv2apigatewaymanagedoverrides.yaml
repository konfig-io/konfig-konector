# ApiGatewayV2ApiGatewayManagedOverrides — API Gateway v2
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/apigatewayv2.html#apigatewayv2apigatewaymanagedoverrides
apiVersion: aws.konfig.io/v1alpha1
kind: ApiGatewayV2ApiGatewayManagedOverrides
metadata:
  name: example-apigatewayv2apigatewaymanagedoverrides
  namespace: default
spec:
  apiId: example-id
  integration:
    description: Managed by konfig-konector
    integrationMethod: example
    payloadFormatVersion: '1.0'
    timeoutInMillis: 300
  providerRef:
    name: example-apigatewayv2apigatewaymanagedoverrides
    region: us-east-1
  route:
    authorizationScopes:
    - example
    authorizationType: example
    operationName: example-apigatewayv2apigatewaymanagedoverrides
    target: example
  stage:
    accessLogSettings:
      destinationArn: arn:aws:iam::123456789012:role/example-role
      format: example
    autoDeploy: false
    defaultRouteSettings:
      detailedMetricsEnabled: true
      throttlingBurstLimit: 1
      throttlingRateLimit: 1
    description: Managed by konfig-konector
    routeSettings: {}
    stageVariables:
      environment: dev
      team: platform
