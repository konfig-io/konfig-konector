# CodeConnectionsConnection — CodeConnections
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codeconnections.html#codeconnectionsconnection
apiVersion: aws.konfig.io/v1alpha1
kind: CodeConnectionsConnection
metadata:
  name: example-codeconnectionsconnection
  namespace: default
spec:
  connectionName: example-codeconnectionsconnectio
  hostArn: arn:aws:iam::123456789012:role/example-role
  providerRef:
    name: example-codeconnectionsconnection
    region: us-east-1
  providerType: example
  tags:
  - key: example
    value: example
