# CodeConnectionsHost — codeconnections
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/codeconnections.html#codeconnectionshost
apiVersion: aws.konfig.io/v1alpha1
kind: CodeConnectionsHost
metadata:
  name: example-codeconnectionshost
  namespace: default
spec:
  name: example-codeconnectionshost
  providerEndpoint: example
  providerRef:
    name: example-codeconnectionshost
    region: us-east-1
  providerType: Bitbucket
  tags:
  - key: example
    value: example
  vpcConfiguration:
    securityGroupIds:
    - example
    subnetIds:
    - example
    tlsCertificate: example
    vpcId: vpc-0abc12def3456789
