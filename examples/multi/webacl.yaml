# WebACL — multi
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/multi.html#webacl
apiVersion: aws.konfig.io/v1alpha1
kind: WebACL
metadata:
  name: example-webacl
  namespace: default
spec:
  defaultAction:
    allow: false
    block: false
  description: Managed by konfig-konector
  name: example-webacl
  providerRef:
    name: example-webacl
    region: us-east-1
  scope: CLOUDFRONT
  tags:
    environment: dev
    team: platform
