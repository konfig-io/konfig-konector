# OpenSearchDomain — multi
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/multi.html#opensearchdomain
apiVersion: aws.konfig.io/v1alpha1
kind: OpenSearchDomain
metadata:
  name: example-opensearchdomain
  namespace: default
spec:
  clusterConfig:
    dedicatedMasterEnabled: true
    instanceCount: 2
    instanceType: example
  domainName: example-opensearchdomain
  engineVersion: example
  providerRef:
    name: example-opensearchdomain
    region: us-east-1
  tags:
    environment: dev
    team: platform
