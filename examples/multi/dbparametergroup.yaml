# DBParameterGroup — multi
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/multi.html#dbparametergroup
apiVersion: aws.konfig.io/v1alpha1
kind: DBParameterGroup
metadata:
  name: example-dbparametergroup
  namespace: default
spec:
  dbParameterGroupFamily: example
  dbParameterGroupName: example-dbparametergroup
  description: Managed by konfig-konector
  parameters:
  - applyMethod: immediate
    parameterName: example-dbparametergroup
    parameterValue: example
  providerRef:
    name: example-dbparametergroup
    region: us-east-1
  tags:
    environment: dev
    team: platform
