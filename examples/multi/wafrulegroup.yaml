# WAFRuleGroup — multi
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/multi.html#wafrulegroup
apiVersion: aws.konfig.io/v1alpha1
kind: WAFRuleGroup
metadata:
  name: example-wafrulegroup
  namespace: default
spec:
  capacity: 2
  description: Managed by konfig-konector
  name: example-wafrulegroup
  providerRef:
    name: example-wafrulegroup
    region: us-east-1
  scope: CLOUDFRONT
  tags:
    environment: dev
    team: platform
