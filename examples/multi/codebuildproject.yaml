# CodeBuildProject — multi
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/multi.html#codebuildproject
apiVersion: aws.konfig.io/v1alpha1
kind: CodeBuildProject
metadata:
  name: example-codebuildproject
  namespace: default
spec:
  artifacts:
    location: example
    type: CODEPIPELINE
  description: Managed by konfig-konector
  environment:
    computeType: BUILD_GENERAL1_SMALL
    image: example
    privilegedMode: false
    type: LINUX_CONTAINER
  name: example-codebuildproject
  providerRef:
    name: example-codebuildproject
    region: us-east-1
  serviceRoleARN: arn:aws:iam::123456789012:role/example-role
  source:
    buildspec: example
    location: example
    type: CODECOMMIT
  tags:
    environment: dev
    team: platform
