# ResolverRule — multi
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/multi.html#resolverrule
apiVersion: aws.konfig.io/v1alpha1
kind: ResolverRule
metadata:
  name: example-resolverrule
  namespace: default
spec:
  domainName: example-resolverrule
  name: example-resolverrule
  providerRef:
    name: example-resolverrule
    region: us-east-1
  ruleType: FORWARD
  tags:
    environment: dev
    team: platform
  targetIPs:
  - ip: example
    port: 443
