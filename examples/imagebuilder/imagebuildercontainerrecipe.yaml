# ImageBuilderContainerRecipe — imagebuilder
# Full-options example generated from the CRD schema. Optional fields are
# included with sample values; delete what you don't need. Field reference:
# https://konfig-konector.io/docs/imagebuilder.html#imagebuildercontainerrecipe
apiVersion: aws.konfig.io/v1alpha1
kind: ImageBuilderContainerRecipe
metadata:
  name: example-imagebuildercontainerrecipe
  namespace: default
spec:
  components:
  - componentArn: arn:aws:iam::123456789012:role/example-role
    parameters:
    - name: example-imagebuildercontainerrecipe
      value:
      - example
  containerType: DOCKER
  description: Managed by konfig-konector
  dockerfileTemplateData: example
  dockerfileTemplateUri: example
  imageOsVersionOverride: example
  instanceConfiguration:
    blockDeviceMappings:
    - deviceName: example-imagebuildercontainerrecipe
      noDevice: example
      virtualName: example-imagebuildercontainerrecipe
    image: example
  kmsKeyId: arn:aws:kms:us-east-1:123456789012:key/1234abcd-12ab-34cd-56ef-1234567890ab
  name: example-imagebuildercontainerrecipe
  parentImage: example
  platformOverride: Windows
  providerRef:
    name: example-imagebuildercontainerrecipe
    region: us-east-1
  tags:
    environment: dev
    team: platform
  targetRepository:
    repositoryName: example-imagebuildercontainerrecipe
    service: ECR
  version: '1.0'
  workingDirectory: example
