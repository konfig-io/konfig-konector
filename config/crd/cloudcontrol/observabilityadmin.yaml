---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: observabilityadminorganizationcentralizationrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ObservabilityAdminOrganizationCentralizationRule
    listKind: ObservabilityAdminOrganizationCentralizationRuleList
    plural: observabilityadminorganizationcentralizationrules
    singular: observabilityadminorganizationcentralizationrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObservabilityAdminOrganizationCentralizationRule manages
          AWS::ObservabilityAdmin::OrganizationCentralizationRule through the AWS Cloud Control API. CAUTION,
          DESTRUCTIVE SCOPE: organization-wide telemetry rule. This kind changes shared account, region or organization
          state (or carries credential material); it is provided for completeness and should not be used from
          application namespaces. Restri...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAdminOrganizationCentralizationRuleSpec is the
              desired state of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              rule:
                description: ObservabilityAdminOrganizationCentralizationRuleCentralizationRule
                  is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                properties:
                  destination:
                    description: ObservabilityAdminOrganizationCentralizationRuleCentralizationRuleDestination
                      is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                    properties:
                      account:
                        maxLength: 12
                        type: string
                      destinationLogsConfiguration:
                        description: ObservabilityAdminOrganizationCentralizationRuleDestinationLogsConfiguration
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                        properties:
                          backupConfiguration:
                            description: ObservabilityAdminOrganizationCentralizationRuleLogsBackupConfiguration
                              is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                            properties:
                              kmsKeyArn:
                                maxLength: 1011
                                type: string
                              region:
                                minLength: 1
                                type: string
                            required:
                            - region
                            type: object
                          logGroupNameConfiguration:
                            description: ObservabilityAdminOrganizationCentralizationRuleLogGroupNameConfiguration
                              is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                            properties:
                              logGroupNamePattern:
                                maxLength: 512
                                minLength: 1
                                type: string
                            required:
                            - logGroupNamePattern
                            type: object
                          logsEncryptionConfiguration:
                            description: ObservabilityAdminOrganizationCentralizationRuleLogsEncryptionConfiguration
                              is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                            properties:
                              encryptionConflictResolutionStrategy:
                                enum:
                                - ALLOW
                                - SKIP
                                type: string
                              encryptionScope:
                                enum:
                                - ENCRYPTED_SOURCE_ONLY
                                - NEW_DESTINATION_LOG_GROUPS
                                type: string
                              encryptionStrategy:
                                enum:
                                - CUSTOMER_MANAGED
                                - AWS_OWNED
                                type: string
                              kmsKeyArn:
                                maxLength: 1011
                                type: string
                            required:
                            - encryptionStrategy
                            type: object
                          tagPropagationConfiguration:
                            description: ObservabilityAdminOrganizationCentralizationRuleTagPropagationConfiguration
                              is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                            properties:
                              destinationRoleArn:
                                description: The ARN of the destination account IAM
                                  role used for tag propagation.
                                maxLength: 2048
                                minLength: 20
                                type: string
                              tagConflictResolutionStrategy:
                                description: The strategy to resolve tag conflicts
                                  during propagation.
                                enum:
                                - IN_SYNC
                                - ADD_ONLY
                                - UPDATE_SYNC
                                type: string
                            required:
                            - destinationRoleArn
                            type: object
                        type: object
                      destinationMetricsConfiguration:
                        description: ObservabilityAdminOrganizationCentralizationRuleDestinationMetricsConfiguration
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                        properties:
                          backupConfiguration:
                            description: ObservabilityAdminOrganizationCentralizationRuleMetricsBackupConfiguration
                              is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                            properties:
                              region:
                                minLength: 1
                                type: string
                            required:
                            - region
                            type: object
                        type: object
                      region:
                        minLength: 1
                        type: string
                    required:
                    - region
                    type: object
                  source:
                    description: ObservabilityAdminOrganizationCentralizationRuleCentralizationRuleSource
                      is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                    properties:
                      regions:
                        items:
                          type: string
                        type: array
                      scope:
                        maxLength: 2000
                        type: string
                      sourceLogsConfiguration:
                        description: ObservabilityAdminOrganizationCentralizationRuleSourceLogsConfiguration
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                        properties:
                          dataSourceSelectionCriteria:
                            maxLength: 2000
                            type: string
                          encryptedLogGroupStrategy:
                            enum:
                            - ALLOW
                            - SKIP
                            type: string
                          logGroupSelectionCriteria:
                            maxLength: 2000
                            type: string
                        required:
                        - encryptedLogGroupStrategy
                        type: object
                      sourceMetricsConfiguration:
                        description: ObservabilityAdminOrganizationCentralizationRuleSourceMetricsConfiguration
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
                        properties:
                          metricsSelectionCriteria:
                            maxLength: 2000
                            type: string
                        type: object
                    required:
                    - regions
                    type: object
                required:
                - destination
                - source
                type: object
              ruleName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 100
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: ruleName is immutable
                  rule: self == oldSelf
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - rule
            - ruleName
            type: object
          status:
            description: ObservabilityAdminOrganizationCentralizationRuleStatus is
              the observed state of AWS::ObservabilityAdmin::OrganizationCentralizationRule.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 1011
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: observabilityadminorganizationtelemetryrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ObservabilityAdminOrganizationTelemetryRule
    listKind: ObservabilityAdminOrganizationTelemetryRuleList
    plural: observabilityadminorganizationtelemetryrules
    singular: observabilityadminorganizationtelemetryrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObservabilityAdminOrganizationTelemetryRule manages AWS::ObservabilityAdmin::OrganizationTelemetryRule through
          the AWS Cloud Control API. CAUTION, DESTRUCTIVE SCOPE: organization-wide telemetry rule. This kind changes
          shared account, region or organization state (or carries credential material); it is provided for completeness
          and should not be used from application namespaces. Restrict it with...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAdminOrganizationTelemetryRuleSpec is the desired
              state of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              rule:
                description: ObservabilityAdminOrganizationTelemetryRuleTelemetryRule
                  is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                properties:
                  allRegions:
                    description: When true, the rule is replicated to all supported
                      regions
                    type: boolean
                  allowFieldUpdates:
                    type: boolean
                  destinationConfiguration:
                    description: ObservabilityAdminOrganizationTelemetryRuleTelemetryDestinationConfiguration
                      is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                    properties:
                      cloudtrailParameters:
                        description: ObservabilityAdminOrganizationTelemetryRuleCloudtrailParameters
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                        properties:
                          advancedEventSelectors:
                            description: Create fine-grained selectors for AWS CloudTrail
                              management and data.
                            items:
                              description: ObservabilityAdminOrganizationTelemetryRuleAdvancedEventSelector
                                is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                              properties:
                                fieldSelectors:
                                  description: Contains all selector statements in
                                    an advanced event selector.
                                  items:
                                    description: ObservabilityAdminOrganizationTelemetryRuleAdvancedFieldSelector
                                      is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                                    properties:
                                      endsWith:
                                        description: |-
                                          An operator that includes events that match the last few characters of the event record field specified as the
                                          value of Field.
                                        items:
                                          type: string
                                        type: array
                                      equals:
                                        description: |-
                                          An operator that includes events that match the exact value of the event record field specified as the value
                                          of Field.
                                        items:
                                          type: string
                                        type: array
                                      field:
                                        description: A field in a CloudTrail event
                                          record on which to filter events to be logged
                                        maxLength: 1000
                                        type: string
                                      notEndsWith:
                                        description: |-
                                          An operator that excludes events that match the last few characters of the event record field specified as the
                                          value of Field.
                                        items:
                                          type: string
                                        type: array
                                      notEquals:
                                        description: |-
                                          An operator that excludes events that match the exact value of the event record field specified as the value
                                          of Field.
                                        items:
                                          type: string
                                        type: array
                                      notStartsWith:
                                        description: |-
                                          An operator that excludes events that match the first few characters of the event record field specified as
                                          the value of Field.
                                        items:
                                          type: string
                                        type: array
                                      startsWith:
                                        description: |-
                                          An operator that includes events that match the first few characters of the event record field specified as
                                          the value of Field.
                                        items:
                                          type: string
                                        type: array
                                    type: object
                                  type: array
                                name:
                                  description: An optional descriptive name for the
                                    advanced event selector
                                  type: string
                              required:
                              - fieldSelectors
                              type: object
                            type: array
                        required:
                        - advancedEventSelectors
                        type: object
                      destinationPattern:
                        type: string
                      destinationType:
                        enum:
                        - cloud-watch-logs
                        type: string
                      elbLoadBalancerLoggingParameters:
                        description: ObservabilityAdminOrganizationTelemetryRuleELBLoadBalancerLoggingParameters
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                        properties:
                          fieldDelimiter:
                            description: A delimiter to delineate log fields
                            type: string
                          outputFormat:
                            enum:
                            - plain
                            - json
                            type: string
                        type: object
                      kmsKeyArn:
                        maxLength: 2048
                        type: string
                      logDeliveryParameters:
                        description: Parameters for log delivery configuration
                        properties:
                          logTypes:
                            description: Types of logs to deliver
                            items:
                              type: string
                            type: array
                        type: object
                      retentionInDays:
                        format: int64
                        type: integer
                      vpcFlowLogParameters:
                        description: ObservabilityAdminOrganizationTelemetryRuleVPCFlowLogParameters
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                        properties:
                          logFormat:
                            description: |-
                              The fields to include in the flow log record. If you omit this parameter, the flow log is created using the
                              default format.
                            type: string
                          maxAggregationInterval:
                            description: |-
                              The maximum interval of time, in seconds, during which a flow of packets is captured and aggregated into a
                              flow log record. Default is 600s.
                            format: int64
                            type: integer
                          trafficType:
                            description: The type of traffic captured for the flow
                              log. Default is ALL
                            type: string
                        type: object
                      wafLoggingParameters:
                        description: ObservabilityAdminOrganizationTelemetryRuleWAFLoggingParameters
                          is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                        properties:
                          logType:
                            enum:
                            - WAF_LOGS
                            type: string
                          loggingFilter:
                            description: ObservabilityAdminOrganizationTelemetryRuleLoggingFilter
                              is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                            properties:
                              defaultBehavior:
                                enum:
                                - KEEP
                                - DROP
                                type: string
                              filters:
                                items:
                                  description: ObservabilityAdminOrganizationTelemetryRuleFilter
                                    is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                                  properties:
                                    behavior:
                                      enum:
                                      - KEEP
                                      - DROP
                                      type: string
                                    conditions:
                                      items:
                                        description: ObservabilityAdminOrganizationTelemetryRuleCondition
                                          is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                                        properties:
                                          actionCondition:
                                            description: ObservabilityAdminOrganizationTelemetryRuleActionCondition
                                              is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                                            properties:
                                              action:
                                                enum:
                                                - ALLOW
                                                - BLOCK
                                                - COUNT
                                                - CAPTCHA
                                                - CHALLENGE
                                                - EXCLUDED_AS_COUNT
                                                type: string
                                            type: object
                                          labelNameCondition:
                                            description: ObservabilityAdminOrganizationTelemetryRuleLabelNameCondition
                                              is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                                            properties:
                                              labelName:
                                                description: The label name of the
                                                  condition.
                                                maxLength: 1024
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                    requirement:
                                      enum:
                                      - MEETS_ALL
                                      - MEETS_ANY
                                      type: string
                                  type: object
                                type: array
                            type: object
                          redactedFields:
                            description: Fields not to be included in the logs.
                            items:
                              description: ObservabilityAdminOrganizationTelemetryRuleFieldToMatch
                                is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                              properties:
                                method:
                                  description: The method with which to match this
                                    rule.
                                  type: string
                                queryString:
                                  description: The query string to find the resource
                                    to match this field to.
                                  type: string
                                singleHeader:
                                  description: ObservabilityAdminOrganizationTelemetryRuleSingleHeader
                                    is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                                  properties:
                                    name:
                                      description: The name of the header
                                      maxLength: 64
                                      minLength: 1
                                      type: string
                                  required:
                                  - name
                                  type: object
                                uriPath:
                                  description: This is the URI path to match this
                                    rule to.
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  regions:
                    description: List of AWS region codes where the rule should be
                      replicated
                    items:
                      type: string
                    type: array
                  resourceType:
                    type: string
                  scope:
                    type: string
                  selectionCriteria:
                    type: string
                  telemetrySourceTypes:
                    items:
                      type: string
                    type: array
                  telemetryType:
                    enum:
                    - Logs
                    - Metrics
                    type: string
                required:
                - resourceType
                - telemetryType
                type: object
              ruleName:
                description: |-
                  The name of the organization telemetry rule
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 100
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: ruleName is immutable
                  rule: self == oldSelf
              tags:
                description: An array of key-value pairs to apply to this resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - rule
            - ruleName
            type: object
          status:
            description: ObservabilityAdminOrganizationTelemetryRuleStatus is the
              observed state of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              regionStatuses:
                description: |-
                  Per-region replication status of the rule
                  Read-only: reported by AWS after creation.
                items:
                  description: ObservabilityAdminOrganizationTelemetryRuleRegionStatus
                    is a nested property type of AWS::ObservabilityAdmin::OrganizationTelemetryRule.
                  properties:
                    region:
                      description: The AWS region code
                      type: string
                    ruleArn:
                      description: The ARN of the rule in this region
                      type: string
                    status:
                      description: The replication status of the rule in this region
                      type: string
                  type: object
                type: array
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleArn:
                description: |-
                  The arn of the organization telemetry rule
                  Read-only: reported by AWS after creation.
                maxLength: 1011
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: observabilityadmins3tableintegrations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ObservabilityAdminS3TableIntegration
    listKind: ObservabilityAdminS3TableIntegrationList
    plural: observabilityadmins3tableintegrations
    singular: observabilityadmins3tableintegration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObservabilityAdminS3TableIntegration manages AWS::ObservabilityAdmin::S3TableIntegration through the AWS Cloud
          Control API. Resource Type definition for a CloudWatch Observability Admin S3 Table Integration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAdminS3TableIntegrationSpec is the desired state
              of AWS::ObservabilityAdmin::S3TableIntegration.
            properties:
              encryption:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyArn:
                    description: The ARN of the KMS key used to encrypt the S3 Table
                      Integration
                    maxLength: 1011
                    type: string
                  sseAlgorithm:
                    description: The server-side encryption algorithm used to encrypt
                      the S3 Table(s) data
                    type: string
                required:
                - sseAlgorithm
                type: object
              logSources:
                description: The CloudWatch Logs data sources to associate with the
                  S3 Table Integration
                items:
                  description: ObservabilityAdminS3TableIntegrationLogSource is a
                    nested property type of AWS::ObservabilityAdmin::S3TableIntegration.
                  properties:
                    identifier:
                      description: The ID of the CloudWatch Logs data source association
                      maxLength: 256
                      type: string
                    name:
                      description: The name of the CloudWatch Logs data source
                      maxLength: 256
                      minLength: 1
                      type: string
                    type:
                      description: The type of the CloudWatch Logs data source
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - name
                  - type
                  type: object
                type: array
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The ARN of the role used to access the S3 Table Integration
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 1011
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              tags:
                description: An array of key-value pairs to apply to this resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - encryption
            - roleArn
            type: object
          status:
            description: ObservabilityAdminS3TableIntegrationStatus is the observed
              state of AWS::ObservabilityAdmin::S3TableIntegration.
            properties:
              arn:
                description: |-
                  The ARN of the S3 Table Integration
                  Read-only: reported by AWS after creation.
                maxLength: 1011
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: observabilityadmintelemetryenrichments.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ObservabilityAdminTelemetryEnrichment
    listKind: ObservabilityAdminTelemetryEnrichmentList
    plural: observabilityadmintelemetryenrichments
    singular: observabilityadmintelemetryenrichment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObservabilityAdminTelemetryEnrichment manages AWS::ObservabilityAdmin::TelemetryEnrichment through the AWS
          Cloud Control API. AWS::ObservabilityAdmin::TelemetryEnrichment cloudformation resource enables the resource
          tags for telemetry feature in CloudWatch to enrich infrastructure metrics with AWS resource tags. For more
          details: https://docs.aws.amazon.com/AmazonCloudWatch/latest/monitoring/re...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAdminTelemetryEnrichmentSpec is the desired
              state of AWS::ObservabilityAdmin::TelemetryEnrichment.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              scope:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                enum:
                - ACCOUNT
                type: string
                x-kubernetes-validations:
                - message: scope is immutable
                  rule: self == oldSelf
            required:
            - scope
            type: object
          status:
            description: ObservabilityAdminTelemetryEnrichmentStatus is the observed
              state of AWS::ObservabilityAdmin::TelemetryEnrichment.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - RUNNING
                - STOPPED
                - IMPAIRED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: observabilityadmintelemetrypipelines.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ObservabilityAdminTelemetryPipelines
    listKind: ObservabilityAdminTelemetryPipelinesList
    plural: observabilityadmintelemetrypipelines
    singular: observabilityadmintelemetrypipelines
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObservabilityAdminTelemetryPipelines manages AWS::ObservabilityAdmin::TelemetryPipelines through the AWS Cloud
          Control API. Resource Type definition for AWS::ObservabilityAdmin::TelemetryPipelines
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAdminTelemetryPipelinesSpec is the desired state
              of AWS::ObservabilityAdmin::TelemetryPipelines.
            properties:
              configuration:
                description: ObservabilityAdminTelemetryPipelinesTelemetryPipelineConfiguration
                  is a nested property type of AWS::ObservabilityAdmin::TelemetryPipelines.
                properties:
                  body:
                    maxLength: 24000
                    minLength: 1
                    type: string
                required:
                - body
                type: object
              name:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 28
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - configuration
            type: object
          status:
            description: ObservabilityAdminTelemetryPipelinesStatus is the observed
              state of AWS::ObservabilityAdmin::TelemetryPipelines.
            properties:
              arn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 1011
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              pipeline:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  arn:
                    maxLength: 1011
                    type: string
                  configuration:
                    description: ObservabilityAdminTelemetryPipelinesTelemetryPipelineConfiguration
                      is a nested property type of AWS::ObservabilityAdmin::TelemetryPipelines.
                    properties:
                      body:
                        maxLength: 24000
                        minLength: 1
                        type: string
                    required:
                    - body
                    type: object
                  createdTimeStamp:
                    type: number
                  lastUpdateTimeStamp:
                    type: number
                  name:
                    maxLength: 28
                    type: string
                  status:
                    enum:
                    - CREATING
                    - ACTIVE
                    - UPDATING
                    - DELETING
                    - CREATE_FAILED
                    - UPDATE_FAILED
                    type: string
                  statusReason:
                    description: ObservabilityAdminTelemetryPipelinesTelemetryPipelineStatusReason
                      is a nested property type of AWS::ObservabilityAdmin::TelemetryPipelines.
                    properties:
                      description:
                        type: string
                    type: object
                  tags:
                    description: An array of key-value pairs to apply to this resource
                    items:
                      description: CFNTag is the CloudFormation Tag shape shared by
                        generated kinds.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        value:
                          type: string
                      required:
                      - key
                      - value
                      type: object
                    type: array
                type: object
              pipelineIdentifier:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 512
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - ACTIVE
                - UPDATING
                - DELETING
                - CREATE_FAILED
                - UPDATE_FAILED
                type: string
              statusReason:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  description:
                    type: string
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: observabilityadmintelemetryrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ObservabilityAdminTelemetryRule
    listKind: ObservabilityAdminTelemetryRuleList
    plural: observabilityadmintelemetryrules
    singular: observabilityadmintelemetryrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ObservabilityAdminTelemetryRule manages AWS::ObservabilityAdmin::TelemetryRule through the AWS Cloud Control
          API. CAUTION, DESTRUCTIVE SCOPE: account-wide telemetry rule. This kind changes shared account, region or
          organization state (or carries credential material); it is provided for completeness and should not be used
          from application namespaces. Restrict it with AWSProvider.allowedNamespace...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ObservabilityAdminTelemetryRuleSpec is the desired state
              of AWS::ObservabilityAdmin::TelemetryRule.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              rule:
                description: ObservabilityAdminTelemetryRuleTelemetryRule is a nested
                  property type of AWS::ObservabilityAdmin::TelemetryRule.
                properties:
                  allRegions:
                    description: When true, the rule is replicated to all supported
                      regions
                    type: boolean
                  allowFieldUpdates:
                    type: boolean
                  destinationConfiguration:
                    description: ObservabilityAdminTelemetryRuleTelemetryDestinationConfiguration
                      is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                    properties:
                      cloudtrailParameters:
                        description: ObservabilityAdminTelemetryRuleCloudtrailParameters
                          is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                        properties:
                          advancedEventSelectors:
                            description: Create fine-grained selectors for AWS CloudTrail
                              management and data.
                            items:
                              description: ObservabilityAdminTelemetryRuleAdvancedEventSelector
                                is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                              properties:
                                fieldSelectors:
                                  description: Contains all selector statements in
                                    an advanced event selector.
                                  items:
                                    description: ObservabilityAdminTelemetryRuleAdvancedFieldSelector
                                      is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                                    properties:
                                      endsWith:
                                        description: |-
                                          An operator that includes events that match the last few characters of the event record field specified as the
                                          value of Field.
                                        items:
                                          type: string
                                        type: array
                                      equals:
                                        description: |-
                                          An operator that includes events that match the exact value of the event record field specified as the value
                                          of Field.
                                        items:
                                          type: string
                                        type: array
                                      field:
                                        description: A field in a CloudTrail event
                                          record on which to filter events to be logged
                                        maxLength: 1000
                                        type: string
                                      notEndsWith:
                                        description: |-
                                          An operator that excludes events that match the last few characters of the event record field specified as the
                                          value of Field.
                                        items:
                                          type: string
                                        type: array
                                      notEquals:
                                        description: |-
                                          An operator that excludes events that match the exact value of the event record field specified as the value
                                          of Field.
                                        items:
                                          type: string
                                        type: array
                                      notStartsWith:
                                        description: |-
                                          An operator that excludes events that match the first few characters of the event record field specified as
                                          the value of Field.
                                        items:
                                          type: string
                                        type: array
                                      startsWith:
                                        description: |-
                                          An operator that includes events that match the first few characters of the event record field specified as
                                          the value of Field.
                                        items:
                                          type: string
                                        type: array
                                    type: object
                                  type: array
                                name:
                                  description: An optional descriptive name for the
                                    advanced event selector
                                  type: string
                              required:
                              - fieldSelectors
                              type: object
                            type: array
                        required:
                        - advancedEventSelectors
                        type: object
                      destinationPattern:
                        type: string
                      destinationType:
                        enum:
                        - cloud-watch-logs
                        type: string
                      elbLoadBalancerLoggingParameters:
                        description: ObservabilityAdminTelemetryRuleELBLoadBalancerLoggingParameters
                          is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                        properties:
                          fieldDelimiter:
                            description: A delimiter to delineate log fields
                            type: string
                          outputFormat:
                            enum:
                            - plain
                            - json
                            type: string
                        type: object
                      kmsKeyArn:
                        maxLength: 2048
                        type: string
                      logDeliveryParameters:
                        description: Parameters for log delivery configuration
                        properties:
                          logTypes:
                            description: Types of logs to deliver
                            items:
                              type: string
                            type: array
                        type: object
                      retentionInDays:
                        format: int64
                        type: integer
                      vpcFlowLogParameters:
                        description: ObservabilityAdminTelemetryRuleVPCFlowLogParameters
                          is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                        properties:
                          logFormat:
                            description: |-
                              The fields to include in the flow log record. If you omit this parameter, the flow log is created using the
                              default format.
                            type: string
                          maxAggregationInterval:
                            description: |-
                              The maximum interval of time, in seconds, during which a flow of packets is captured and aggregated into a
                              flow log record. Default is 600s.
                            format: int64
                            type: integer
                          trafficType:
                            description: The type of traffic captured for the flow
                              log. Default is ALL
                            type: string
                        type: object
                      wafLoggingParameters:
                        description: ObservabilityAdminTelemetryRuleWAFLoggingParameters
                          is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                        properties:
                          logType:
                            enum:
                            - WAF_LOGS
                            type: string
                          loggingFilter:
                            description: ObservabilityAdminTelemetryRuleLoggingFilter
                              is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                            properties:
                              defaultBehavior:
                                enum:
                                - KEEP
                                - DROP
                                type: string
                              filters:
                                items:
                                  description: ObservabilityAdminTelemetryRuleFilter
                                    is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                                  properties:
                                    behavior:
                                      enum:
                                      - KEEP
                                      - DROP
                                      type: string
                                    conditions:
                                      items:
                                        description: ObservabilityAdminTelemetryRuleCondition
                                          is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                                        properties:
                                          actionCondition:
                                            description: ObservabilityAdminTelemetryRuleActionCondition
                                              is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                                            properties:
                                              action:
                                                enum:
                                                - ALLOW
                                                - BLOCK
                                                - COUNT
                                                - CAPTCHA
                                                - CHALLENGE
                                                - EXCLUDED_AS_COUNT
                                                type: string
                                            type: object
                                          labelNameCondition:
                                            description: ObservabilityAdminTelemetryRuleLabelNameCondition
                                              is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                                            properties:
                                              labelName:
                                                description: The label name of the
                                                  condition.
                                                maxLength: 1024
                                                type: string
                                            type: object
                                        type: object
                                      type: array
                                    requirement:
                                      enum:
                                      - MEETS_ALL
                                      - MEETS_ANY
                                      type: string
                                  type: object
                                type: array
                            type: object
                          redactedFields:
                            description: Fields not to be included in the logs.
                            items:
                              description: ObservabilityAdminTelemetryRuleFieldToMatch
                                is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                              properties:
                                method:
                                  description: The method with which to match this
                                    rule.
                                  type: string
                                queryString:
                                  description: The query string to find the resource
                                    to match this field to.
                                  type: string
                                singleHeader:
                                  description: ObservabilityAdminTelemetryRuleSingleHeader
                                    is a nested property type of AWS::ObservabilityAdmin::TelemetryRule.
                                  properties:
                                    name:
                                      description: The name of the header
                                      maxLength: 64
                                      minLength: 1
                                      type: string
                                  required:
                                  - name
                                  type: object
                                uriPath:
                                  description: This is the URI path to match this
                                    rule to.
                                  type: string
                              type: object
                            type: array
                        type: object
                    type: object
                  regions:
                    description: List of AWS region codes where the rule should be
                      replicated
                    items:
                      type: string
                    type: array
                  resourceType:
                    type: string
                  selectionCriteria:
                    type: string
                  telemetrySourceTypes:
                    items:
                      type: string
                    type: array
                  telemetryType:
                    enum:
                    - Logs
                    - Traces
                    - Metrics
                    type: string
                required:
                - resourceType
                - telemetryType
                type: object
              ruleName:
                description: |-
                  The name of the telemetry rule
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 100
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: ruleName is immutable
                  rule: self == oldSelf
              tags:
                description: An array of key-value pairs to apply to this resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - rule
            - ruleName
            type: object
          status:
            description: ObservabilityAdminTelemetryRuleStatus is the observed state
              of AWS::ObservabilityAdmin::TelemetryRule.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              regionStatuses:
                description: |-
                  Per-region replication status of the rule
                  Read-only: reported by AWS after creation.
                items:
                  description: ObservabilityAdminTelemetryRuleRegionStatus is a nested
                    property type of AWS::ObservabilityAdmin::TelemetryRule.
                  properties:
                    region:
                      description: The AWS region code
                      type: string
                    ruleArn:
                      description: The ARN of the rule in this region
                      type: string
                    status:
                      description: The replication status of the rule in this region
                      type: string
                  type: object
                type: array
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleArn:
                description: |-
                  The arn of the telemetry rule
                  Read-only: reported by AWS after creation.
                maxLength: 1011
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
