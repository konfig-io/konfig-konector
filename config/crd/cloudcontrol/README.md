# Cloud Control typed-kind CRD bundles (generated by make manifests)

| Service | Kinds | Install |
|---|---|---|
| accessanalyzer | 2 | `kubectl apply -f config/crd/cloudcontrol/accessanalyzer.yaml` |
| accountaccess | 2 | `kubectl apply -f config/crd/cloudcontrol/accountaccess.yaml` |
| acmpca | 3 | `kubectl apply -f config/crd/cloudcontrol/acmpca.yaml` |
| amplify | 4 | `kubectl apply -f config/crd/cloudcontrol/amplify.yaml` |
| apigateway | 19 | `kubectl apply -f config/crd/cloudcontrol/apigateway.yaml` |
| apigatewayv2 | 7 | `kubectl apply -f config/crd/cloudcontrol/apigatewayv2.yaml` |
| appconfig | 10 | `kubectl apply -f config/crd/cloudcontrol/appconfig.yaml` |
| appflow | 3 | `kubectl apply -f config/crd/cloudcontrol/appflow.yaml` |
| applicationinsights | 1 | `kubectl apply -f config/crd/cloudcontrol/applicationinsights.yaml` |
| applicationsignals | 3 | `kubectl apply -f config/crd/cloudcontrol/applicationsignals.yaml` |
| apprunner | 3 | `kubectl apply -f config/crd/cloudcontrol/apprunner.yaml` |
| appsync | 10 | `kubectl apply -f config/crd/cloudcontrol/appsync.yaml` |
| aps | 3 | `kubectl apply -f config/crd/cloudcontrol/aps.yaml` |
| arczonalshift | 2 | `kubectl apply -f config/crd/cloudcontrol/arczonalshift.yaml` |
| athena | 2 | `kubectl apply -f config/crd/cloudcontrol/athena.yaml` |
| auditmanager | 2 | `kubectl apply -f config/crd/cloudcontrol/auditmanager.yaml` |
| autoscaling | 4 | `kubectl apply -f config/crd/cloudcontrol/autoscaling.yaml` |
| backup | 7 | `kubectl apply -f config/crd/cloudcontrol/backup.yaml` |
| batch | 4 | `kubectl apply -f config/crd/cloudcontrol/batch.yaml` |
| bedrock | 22 | `kubectl apply -f config/crd/cloudcontrol/bedrock.yaml` |
| bedrockagentcore | 23 | `kubectl apply -f config/crd/cloudcontrol/bedrockagentcore.yaml` |
| cassandra | 3 | `kubectl apply -f config/crd/cloudcontrol/cassandra.yaml` |
| ce | 1 | `kubectl apply -f config/crd/cloudcontrol/ce.yaml` |
| certificatemanager | 4 | `kubectl apply -f config/crd/cloudcontrol/certificatemanager.yaml` |
| chatbot | 3 | `kubectl apply -f config/crd/cloudcontrol/chatbot.yaml` |
| cloudformation | 14 | `kubectl apply -f config/crd/cloudcontrol/cloudformation.yaml` |
| cloudfront | 15 | `kubectl apply -f config/crd/cloudcontrol/cloudfront.yaml` |
| cloudhsm | 1 | `kubectl apply -f config/crd/cloudcontrol/cloudhsm.yaml` |
| cloudtrail | 4 | `kubectl apply -f config/crd/cloudcontrol/cloudtrail.yaml` |
| cloudwatch | 5 | `kubectl apply -f config/crd/cloudcontrol/cloudwatch.yaml` |
| codeartifact | 1 | `kubectl apply -f config/crd/cloudcontrol/codeartifact.yaml` |
| codebuild | 3 | `kubectl apply -f config/crd/cloudcontrol/codebuild.yaml` |
| codeconnections | 2 | `kubectl apply -f config/crd/cloudcontrol/codeconnections.yaml` |
| codedeploy | 1 | `kubectl apply -f config/crd/cloudcontrol/codedeploy.yaml` |
| codeguruprofiler | 1 | `kubectl apply -f config/crd/cloudcontrol/codeguruprofiler.yaml` |
| codegurureviewer | 1 | `kubectl apply -f config/crd/cloudcontrol/codegurureviewer.yaml` |
| codepipeline | 2 | `kubectl apply -f config/crd/cloudcontrol/codepipeline.yaml` |
| codestarconnections | 3 | `kubectl apply -f config/crd/cloudcontrol/codestarconnections.yaml` |
| codestarnotifications | 1 | `kubectl apply -f config/crd/cloudcontrol/codestarnotifications.yaml` |
| cognito | 16 | `kubectl apply -f config/crd/cloudcontrol/cognito.yaml` |
| computeoptimizer | 1 | `kubectl apply -f config/crd/cloudcontrol/computeoptimizer.yaml` |
| config | 7 | `kubectl apply -f config/crd/cloudcontrol/config.yaml` |
| controltower | 2 | `kubectl apply -f config/crd/cloudcontrol/controltower.yaml` |
| datasync | 13 | `kubectl apply -f config/crd/cloudcontrol/datasync.yaml` |
| datazone | 17 | `kubectl apply -f config/crd/cloudcontrol/datazone.yaml` |
| dax | 1 | `kubectl apply -f config/crd/cloudcontrol/dax.yaml` |
| detective | 3 | `kubectl apply -f config/crd/cloudcontrol/detective.yaml` |
| directconnect | 7 | `kubectl apply -f config/crd/cloudcontrol/directconnect.yaml` |
| dms | 10 | `kubectl apply -f config/crd/cloudcontrol/dms.yaml` |
| docdb | 4 | `kubectl apply -f config/crd/cloudcontrol/docdb.yaml` |
| ec2 | 83 | `kubectl apply -f config/crd/cloudcontrol/ec2.yaml` |
| ecr | 8 | `kubectl apply -f config/crd/cloudcontrol/ecr.yaml` |
| ecs | 6 | `kubectl apply -f config/crd/cloudcontrol/ecs.yaml` |
| eks | 2 | `kubectl apply -f config/crd/cloudcontrol/eks.yaml` |
| elasticache | 5 | `kubectl apply -f config/crd/cloudcontrol/elasticache.yaml` |
| elasticbeanstalk | 4 | `kubectl apply -f config/crd/cloudcontrol/elasticbeanstalk.yaml` |
| elasticloadbalancing | 1 | `kubectl apply -f config/crd/cloudcontrol/elasticloadbalancing.yaml` |
| elasticloadbalancingv2 | 2 | `kubectl apply -f config/crd/cloudcontrol/elasticloadbalancingv2.yaml` |
| emr | 5 | `kubectl apply -f config/crd/cloudcontrol/emr.yaml` |
| emrcontainers | 3 | `kubectl apply -f config/crd/cloudcontrol/emrcontainers.yaml` |
| emrserverless | 1 | `kubectl apply -f config/crd/cloudcontrol/emrserverless.yaml` |
| events | 5 | `kubectl apply -f config/crd/cloudcontrol/events.yaml` |
| eventschemas | 4 | `kubectl apply -f config/crd/cloudcontrol/eventschemas.yaml` |
| evidently | 5 | `kubectl apply -f config/crd/cloudcontrol/evidently.yaml` |
| fis | 2 | `kubectl apply -f config/crd/cloudcontrol/fis.yaml` |
| fms | 3 | `kubectl apply -f config/crd/cloudcontrol/fms.yaml` |
| fsx | 3 | `kubectl apply -f config/crd/cloudcontrol/fsx.yaml` |
| globalaccelerator | 4 | `kubectl apply -f config/crd/cloudcontrol/globalaccelerator.yaml` |
| glue | 20 | `kubectl apply -f config/crd/cloudcontrol/glue.yaml` |
| guardduty | 10 | `kubectl apply -f config/crd/cloudcontrol/guardduty.yaml` |
| iam | 5 | `kubectl apply -f config/crd/cloudcontrol/iam.yaml` |
| identitystore | 3 | `kubectl apply -f config/crd/cloudcontrol/identitystore.yaml` |
| imagebuilder | 8 | `kubectl apply -f config/crd/cloudcontrol/imagebuilder.yaml` |
| inspector | 3 | `kubectl apply -f config/crd/cloudcontrol/inspector.yaml` |
| inspectorv2 | 5 | `kubectl apply -f config/crd/cloudcontrol/inspectorv2.yaml` |
| internetmonitor | 1 | `kubectl apply -f config/crd/cloudcontrol/internetmonitor.yaml` |
| kafkaconnect | 3 | `kubectl apply -f config/crd/cloudcontrol/kafkaconnect.yaml` |
| kinesis | 1 | `kubectl apply -f config/crd/cloudcontrol/kinesis.yaml` |
| kinesisanalyticsv2 | 1 | `kubectl apply -f config/crd/cloudcontrol/kinesisanalyticsv2.yaml` |
| kms | 1 | `kubectl apply -f config/crd/cloudcontrol/kms.yaml` |
| lakeformation | 4 | `kubectl apply -f config/crd/cloudcontrol/lakeformation.yaml` |
| lambda | 5 | `kubectl apply -f config/crd/cloudcontrol/lambda.yaml` |
| licensemanager | 3 | `kubectl apply -f config/crd/cloudcontrol/licensemanager.yaml` |
| logs | 13 | `kubectl apply -f config/crd/cloudcontrol/logs.yaml` |
| macie | 4 | `kubectl apply -f config/crd/cloudcontrol/macie.yaml` |
| memorydb | 5 | `kubectl apply -f config/crd/cloudcontrol/memorydb.yaml` |
| msk | 6 | `kubectl apply -f config/crd/cloudcontrol/msk.yaml` |
| mwaa | 1 | `kubectl apply -f config/crd/cloudcontrol/mwaa.yaml` |
| neptune | 7 | `kubectl apply -f config/crd/cloudcontrol/neptune.yaml` |
| networkfirewall | 3 | `kubectl apply -f config/crd/cloudcontrol/networkfirewall.yaml` |
| networkflowmonitor | 1 | `kubectl apply -f config/crd/cloudcontrol/networkflowmonitor.yaml` |
| networkmanager | 16 | `kubectl apply -f config/crd/cloudcontrol/networkmanager.yaml` |
| notifications | 7 | `kubectl apply -f config/crd/cloudcontrol/notifications.yaml` |
| notificationscontacts | 1 | `kubectl apply -f config/crd/cloudcontrol/notificationscontacts.yaml` |
| oam | 2 | `kubectl apply -f config/crd/cloudcontrol/oam.yaml` |
| observabilityadmin | 6 | `kubectl apply -f config/crd/cloudcontrol/observabilityadmin.yaml` |
| opensearch | 1 | `kubectl apply -f config/crd/cloudcontrol/opensearch.yaml` |
| opensearchserverless | 8 | `kubectl apply -f config/crd/cloudcontrol/opensearchserverless.yaml` |
| opensearchservice | 1 | `kubectl apply -f config/crd/cloudcontrol/opensearchservice.yaml` |
| organizations | 2 | `kubectl apply -f config/crd/cloudcontrol/organizations.yaml` |
| osis | 1 | `kubectl apply -f config/crd/cloudcontrol/osis.yaml` |
| ram | 1 | `kubectl apply -f config/crd/cloudcontrol/ram.yaml` |
| rbin | 1 | `kubectl apply -f config/crd/cloudcontrol/rbin.yaml` |
| rds | 7 | `kubectl apply -f config/crd/cloudcontrol/rds.yaml` |
| redshift | 6 | `kubectl apply -f config/crd/cloudcontrol/redshift.yaml` |
| redshiftserverless | 3 | `kubectl apply -f config/crd/cloudcontrol/redshiftserverless.yaml` |
| resiliencehub | 2 | `kubectl apply -f config/crd/cloudcontrol/resiliencehub.yaml` |
| resiliencehubv2 | 5 | `kubectl apply -f config/crd/cloudcontrol/resiliencehubv2.yaml` |
| resourceexplorer2 | 3 | `kubectl apply -f config/crd/cloudcontrol/resourceexplorer2.yaml` |
| resourcegroups | 2 | `kubectl apply -f config/crd/cloudcontrol/resourcegroups.yaml` |
| rolesanywhere | 3 | `kubectl apply -f config/crd/cloudcontrol/rolesanywhere.yaml` |
| route53 | 2 | `kubectl apply -f config/crd/cloudcontrol/route53.yaml` |
| route53profiles | 3 | `kubectl apply -f config/crd/cloudcontrol/route53profiles.yaml` |
| route53recoverycontrol | 4 | `kubectl apply -f config/crd/cloudcontrol/route53recoverycontrol.yaml` |
| route53recoveryreadiness | 4 | `kubectl apply -f config/crd/cloudcontrol/route53recoveryreadiness.yaml` |
| route53resolver | 9 | `kubectl apply -f config/crd/cloudcontrol/route53resolver.yaml` |
| rum | 1 | `kubectl apply -f config/crd/cloudcontrol/rum.yaml` |
| s3 | 7 | `kubectl apply -f config/crd/cloudcontrol/s3.yaml` |
| s3express | 3 | `kubectl apply -f config/crd/cloudcontrol/s3express.yaml` |
| s3objectlambda | 2 | `kubectl apply -f config/crd/cloudcontrol/s3objectlambda.yaml` |
| s3tables | 5 | `kubectl apply -f config/crd/cloudcontrol/s3tables.yaml` |
| sagemaker | 40 | `kubectl apply -f config/crd/cloudcontrol/sagemaker.yaml` |
| secretsmanager | 2 | `kubectl apply -f config/crd/cloudcontrol/secretsmanager.yaml` |
| securityhub | 14 | `kubectl apply -f config/crd/cloudcontrol/securityhub.yaml` |
| securitylake | 4 | `kubectl apply -f config/crd/cloudcontrol/securitylake.yaml` |
| servicecatalog | 13 | `kubectl apply -f config/crd/cloudcontrol/servicecatalog.yaml` |
| servicediscovery | 2 | `kubectl apply -f config/crd/cloudcontrol/servicediscovery.yaml` |
| ses | 19 | `kubectl apply -f config/crd/cloudcontrol/ses.yaml` |
| shield | 3 | `kubectl apply -f config/crd/cloudcontrol/shield.yaml` |
| signer | 2 | `kubectl apply -f config/crd/cloudcontrol/signer.yaml` |
| sns | 1 | `kubectl apply -f config/crd/cloudcontrol/sns.yaml` |
| sqs | 1 | `kubectl apply -f config/crd/cloudcontrol/sqs.yaml` |
| ssm | 7 | `kubectl apply -f config/crd/cloudcontrol/ssm.yaml` |
| ssmcontacts | 4 | `kubectl apply -f config/crd/cloudcontrol/ssmcontacts.yaml` |
| ssmincidents | 2 | `kubectl apply -f config/crd/cloudcontrol/ssmincidents.yaml` |
| ssmquicksetup | 2 | `kubectl apply -f config/crd/cloudcontrol/ssmquicksetup.yaml` |
| sso | 4 | `kubectl apply -f config/crd/cloudcontrol/sso.yaml` |
| stepfunctions | 2 | `kubectl apply -f config/crd/cloudcontrol/stepfunctions.yaml` |
| storagegateway | 1 | `kubectl apply -f config/crd/cloudcontrol/storagegateway.yaml` |
| synthetics | 2 | `kubectl apply -f config/crd/cloudcontrol/synthetics.yaml` |
| timestream | 5 | `kubectl apply -f config/crd/cloudcontrol/timestream.yaml` |
| transfer | 9 | `kubectl apply -f config/crd/cloudcontrol/transfer.yaml` |
| verifiedpermissions | 5 | `kubectl apply -f config/crd/cloudcontrol/verifiedpermissions.yaml` |
| vpclattice | 8 | `kubectl apply -f config/crd/cloudcontrol/vpclattice.yaml` |
| wafv2 | 2 | `kubectl apply -f config/crd/cloudcontrol/wafv2.yaml` |
| xray | 2 | `kubectl apply -f config/crd/cloudcontrol/xray.yaml` |
