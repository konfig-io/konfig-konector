---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: eventbridgeapidestinations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: EventBridgeApiDestination
    listKind: EventBridgeApiDestinationList
    plural: eventbridgeapidestinations
    singular: eventbridgeapidestination
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EventBridgeApiDestination manages AWS::Events::ApiDestination through the AWS Cloud Control API. Resource Type
          definition for AWS::Events::ApiDestination.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventBridgeApiDestinationSpec is the desired state of AWS::Events::ApiDestination.
            properties:
              connectionArn:
                description: The arn of the connection.
                type: string
              description:
                maxLength: 512
                type: string
              httpMethod:
                enum:
                - GET
                - HEAD
                - POST
                - OPTIONS
                - PUT
                - DELETE
                - PATCH
                type: string
              invocationEndpoint:
                description: Url endpoint to invoke.
                type: string
              invocationRateLimitPerSecond:
                format: int64
                minimum: 1
                type: integer
              name:
                description: |-
                  Name of the apiDestination.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - connectionArn
            - httpMethod
            - invocationEndpoint
            type: object
          status:
            description: EventBridgeApiDestinationStatus is the observed state of
              AWS::Events::ApiDestination.
            properties:
              arn:
                description: |-
                  The arn of the api destination.
                  Read-only: reported by AWS after creation.
                type: string
              arnForPolicy:
                description: |-
                  The arn of the api destination to be used in IAM policies.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: eventbridgearchives.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: EventBridgeArchive
    listKind: EventBridgeArchiveList
    plural: eventbridgearchives
    singular: eventbridgearchive
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EventBridgeArchive manages AWS::Events::Archive through the AWS Cloud Control API. Resource Type definition
          for AWS::Events::Archive
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventBridgeArchiveSpec is the desired state of AWS::Events::Archive.
            properties:
              archiveName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 48
                type: string
              description:
                type: string
              eventPattern:
                x-kubernetes-preserve-unknown-fields: true
              kmsKeyIdentifier:
                maxLength: 2048
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              retentionDays:
                format: int64
                type: integer
              sourceArn:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
                x-kubernetes-validations:
                - message: sourceArn is immutable
                  rule: self == oldSelf
            required:
            - sourceArn
            type: object
          status:
            description: EventBridgeArchiveStatus is the observed state of AWS::Events::Archive.
            properties:
              arn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: eventbridgeconnections.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: EventBridgeConnection
    listKind: EventBridgeConnectionList
    plural: eventbridgeconnections
    singular: eventbridgeconnection
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EventBridgeConnection manages AWS::Events::Connection through the AWS Cloud Control API. Resource Type
          definition for AWS::Events::Connection.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventBridgeConnectionSpec is the desired state of AWS::Events::Connection.
            properties:
              authParameters:
                description: EventBridgeConnectionAuthParameters is a nested property
                  type of AWS::Events::Connection.
                properties:
                  apiKeyAuthParameters:
                    description: EventBridgeConnectionApiKeyAuthParameters is a nested
                      property type of AWS::Events::Connection.
                    properties:
                      apiKeyName:
                        type: string
                      apiKeyValue:
                        type: string
                    required:
                    - apiKeyName
                    - apiKeyValue
                    type: object
                  basicAuthParameters:
                    description: EventBridgeConnectionBasicAuthParameters is a nested
                      property type of AWS::Events::Connection.
                    properties:
                      password:
                        type: string
                      username:
                        type: string
                    required:
                    - password
                    - username
                    type: object
                  connectivityParameters:
                    description: EventBridgeConnectionConnectivityParameters is a
                      nested property type of AWS::Events::Connection.
                    properties:
                      resourceParameters:
                        description: EventBridgeConnectionResourceParameters is a
                          nested property type of AWS::Events::Connection.
                        properties:
                          resourceAssociationArn:
                            maxLength: 2048
                            type: string
                          resourceConfigurationArn:
                            maxLength: 2048
                            type: string
                        required:
                        - resourceConfigurationArn
                        type: object
                    required:
                    - resourceParameters
                    type: object
                  invocationHttpParameters:
                    description: EventBridgeConnectionConnectionHttpParameters is
                      a nested property type of AWS::Events::Connection.
                    properties:
                      bodyParameters:
                        items:
                          description: EventBridgeConnectionParameter is a nested
                            property type of AWS::Events::Connection.
                          properties:
                            isValueSecret:
                              type: boolean
                            key:
                              type: string
                            value:
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                      headerParameters:
                        items:
                          description: EventBridgeConnectionParameter is a nested
                            property type of AWS::Events::Connection.
                          properties:
                            isValueSecret:
                              type: boolean
                            key:
                              type: string
                            value:
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                      queryStringParameters:
                        items:
                          description: EventBridgeConnectionParameter is a nested
                            property type of AWS::Events::Connection.
                          properties:
                            isValueSecret:
                              type: boolean
                            key:
                              type: string
                            value:
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                    type: object
                  oAuthParameters:
                    description: EventBridgeConnectionOAuthParameters is a nested
                      property type of AWS::Events::Connection.
                    properties:
                      authorizationEndpoint:
                        maxLength: 2048
                        minLength: 1
                        type: string
                      clientParameters:
                        description: EventBridgeConnectionClientParameters is a nested
                          property type of AWS::Events::Connection.
                        properties:
                          clientID:
                            type: string
                          clientSecret:
                            type: string
                        required:
                        - clientID
                        - clientSecret
                        type: object
                      httpMethod:
                        enum:
                        - GET
                        - POST
                        - PUT
                        type: string
                      oAuthHttpParameters:
                        description: EventBridgeConnectionConnectionHttpParameters
                          is a nested property type of AWS::Events::Connection.
                        properties:
                          bodyParameters:
                            items:
                              description: EventBridgeConnectionParameter is a nested
                                property type of AWS::Events::Connection.
                              properties:
                                isValueSecret:
                                  type: boolean
                                key:
                                  type: string
                                value:
                                  type: string
                              required:
                              - key
                              - value
                              type: object
                            type: array
                          headerParameters:
                            items:
                              description: EventBridgeConnectionParameter is a nested
                                property type of AWS::Events::Connection.
                              properties:
                                isValueSecret:
                                  type: boolean
                                key:
                                  type: string
                                value:
                                  type: string
                              required:
                              - key
                              - value
                              type: object
                            type: array
                          queryStringParameters:
                            items:
                              description: EventBridgeConnectionParameter is a nested
                                property type of AWS::Events::Connection.
                              properties:
                                isValueSecret:
                                  type: boolean
                                key:
                                  type: string
                                value:
                                  type: string
                              required:
                              - key
                              - value
                              type: object
                            type: array
                        type: object
                    required:
                    - authorizationEndpoint
                    - clientParameters
                    - httpMethod
                    type: object
                type: object
              authorizationType:
                enum:
                - API_KEY
                - BASIC
                - OAUTH_CLIENT_CREDENTIALS
                type: string
              description:
                description: Description of the connection.
                maxLength: 512
                type: string
              invocationConnectivityParameters:
                description: The private resource the HTTP request will be sent to.
                properties:
                  resourceParameters:
                    description: EventBridgeConnectionResourceParameters is a nested
                      property type of AWS::Events::Connection.
                    properties:
                      resourceAssociationArn:
                        maxLength: 2048
                        type: string
                      resourceConfigurationArn:
                        maxLength: 2048
                        type: string
                    required:
                    - resourceConfigurationArn
                    type: object
                required:
                - resourceParameters
                type: object
              kmsKeyIdentifier:
                maxLength: 2048
                type: string
              name:
                description: |-
                  Name of the connection.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            type: object
          status:
            description: EventBridgeConnectionStatus is the observed state of AWS::Events::Connection.
            properties:
              arn:
                description: |-
                  The arn of the connection resource.
                  Read-only: reported by AWS after creation.
                type: string
              arnForPolicy:
                description: |-
                  The arn of the connection resource to be used in IAM policies.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              secretArn:
                description: |-
                  The arn of the secrets manager secret created in the customer account.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: eventbridgeendpoints.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: EventBridgeEndpoint
    listKind: EventBridgeEndpointList
    plural: eventbridgeendpoints
    singular: eventbridgeendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EventBridgeEndpoint manages AWS::Events::Endpoint through the AWS Cloud Control API. Resource Type definition
          for AWS::Events::Endpoint.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventBridgeEndpointSpec is the desired state of AWS::Events::Endpoint.
            properties:
              description:
                maxLength: 512
                type: string
              eventBuses:
                items:
                  description: EventBridgeEndpointEndpointEventBus is a nested property
                    type of AWS::Events::Endpoint.
                  properties:
                    eventBusArn:
                      maxLength: 512
                      minLength: 1
                      type: string
                  required:
                  - eventBusArn
                  type: object
                type: array
              name:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 64
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              replicationConfig:
                description: EventBridgeEndpointReplicationConfig is a nested property
                  type of AWS::Events::Endpoint.
                properties:
                  state:
                    enum:
                    - ENABLED
                    - DISABLED
                    type: string
                required:
                - state
                type: object
              roleArn:
                maxLength: 256
                type: string
              routingConfig:
                description: EventBridgeEndpointRoutingConfig is a nested property
                  type of AWS::Events::Endpoint.
                properties:
                  failoverConfig:
                    description: EventBridgeEndpointFailoverConfig is a nested property
                      type of AWS::Events::Endpoint.
                    properties:
                      primary:
                        description: EventBridgeEndpointPrimary is a nested property
                          type of AWS::Events::Endpoint.
                        properties:
                          healthCheck:
                            maxLength: 1600
                            minLength: 1
                            type: string
                        required:
                        - healthCheck
                        type: object
                      secondary:
                        description: EventBridgeEndpointSecondary is a nested property
                          type of AWS::Events::Endpoint.
                        properties:
                          route:
                            maxLength: 20
                            minLength: 9
                            type: string
                        required:
                        - route
                        type: object
                    required:
                    - primary
                    - secondary
                    type: object
                required:
                - failoverConfig
                type: object
            required:
            - eventBuses
            - routingConfig
            type: object
          status:
            description: EventBridgeEndpointStatus is the observed state of AWS::Events::Endpoint.
            properties:
              arn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 1600
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpointId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 50
                type: string
              endpointUrl:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              state:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - ACTIVE
                - CREATING
                - UPDATING
                - DELETING
                - CREATE_FAILED
                - UPDATE_FAILED
                type: string
              stateReason:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 512
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: eventbridgeeventbuspolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: EventBridgeEventBusPolicy
    listKind: EventBridgeEventBusPolicyList
    plural: eventbridgeeventbuspolicies
    singular: eventbridgeeventbuspolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EventBridgeEventBusPolicy manages AWS::Events::EventBusPolicy through the AWS Cloud Control API. Resource Type
          definition for AWS::Events::EventBusPolicy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EventBridgeEventBusPolicySpec is the desired state of AWS::Events::EventBusPolicy.
            properties:
              action:
                description: The action that you are enabling the other account to
                  perform.
                maxLength: 64
                type: string
              condition:
                description: EventBridgeEventBusPolicyCondition is a nested property
                  type of AWS::Events::EventBusPolicy.
                properties:
                  key:
                    description: Specifies the value for the key. Currently, this
                      must be the ID of the organization.
                    type: string
                  type:
                    description: Specifies the type of condition. Currently the only
                      supported value is StringEquals.
                    type: string
                  value:
                    description: Specifies the key for the condition. Currently the
                      only supported key is aws:PrincipalOrgID.
                    type: string
                type: object
              eventBusName:
                description: |-
                  The name of the event bus associated with the rule. If you omit this, the default event bus is used.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                type: string
              principal:
                description: |-
                  The 12-digit AWS account ID that you are permitting to put events to your default event bus. Specify "*" to
                  permit any account to put events to your default event bus.
                maxLength: 12
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              statement:
                description: |-
                  A JSON string that describes the permission policy statement. You can include a Policy parameter in the
                  request instead of using the StatementId, Action, Principal, or Condition parameters.
                x-kubernetes-preserve-unknown-fields: true
              statementId:
                description: |-
                  An identifier string for the external account that you are granting permissions to
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: statementId is immutable
                  rule: self == oldSelf
            required:
            - statementId
            type: object
          status:
            description: EventBridgeEventBusPolicyStatus is the observed state of
              AWS::Events::EventBusPolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
