---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ssmincidentsreplicationsets.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SSMIncidentsReplicationSet
    listKind: SSMIncidentsReplicationSetList
    plural: ssmincidentsreplicationsets
    singular: ssmincidentsreplicationset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SSMIncidentsReplicationSet manages AWS::SSMIncidents::ReplicationSet through the AWS Cloud Control API.
          Resource type definition for AWS::SSMIncidents::ReplicationSet
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SSMIncidentsReplicationSetSpec is the desired state of AWS::SSMIncidents::ReplicationSet.
            properties:
              deletionProtected:
                type: boolean
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              regions:
                description: The ReplicationSet configuration.
                items:
                  description: SSMIncidentsReplicationSetReplicationRegion is a nested
                    property type of AWS::SSMIncidents::ReplicationSet.
                  properties:
                    regionConfiguration:
                      description: SSMIncidentsReplicationSetRegionConfiguration is
                        a nested property type of AWS::SSMIncidents::ReplicationSet.
                      properties:
                        sseKmsKeyId:
                          description: The AWS Key Management Service key ID or Key
                            Alias to use to encrypt your replication set.
                          maxLength: 2048
                          type: string
                      required:
                      - sseKmsKeyId
                      type: object
                    regionName:
                      maxLength: 20
                      type: string
                  type: object
                type: array
              tags:
                description: The tags to apply to the replication set.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - regions
            type: object
          status:
            description: SSMIncidentsReplicationSetStatus is the observed state of
              AWS::SSMIncidents::ReplicationSet.
            properties:
              arn:
                description: |-
                  The ARN of the ReplicationSet.
                  Read-only: reported by AWS after creation.
                maxLength: 1000
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: ssmincidentsresponseplans.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SSMIncidentsResponsePlan
    listKind: SSMIncidentsResponsePlanList
    plural: ssmincidentsresponseplans
    singular: ssmincidentsresponseplan
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SSMIncidentsResponsePlan manages AWS::SSMIncidents::ResponsePlan through the AWS Cloud Control API. Resource
          type definition for AWS::SSMIncidents::ResponsePlan
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SSMIncidentsResponsePlanSpec is the desired state of AWS::SSMIncidents::ResponsePlan.
            properties:
              actions:
                description: The list of actions.
                items:
                  description: SSMIncidentsResponsePlanAction is a nested property
                    type of AWS::SSMIncidents::ResponsePlan.
                  properties:
                    ssmAutomation:
                      description: SSMIncidentsResponsePlanSsmAutomation is a nested
                        property type of AWS::SSMIncidents::ResponsePlan.
                      properties:
                        documentName:
                          description: The document name to use when starting the
                            SSM automation document.
                          maxLength: 128
                          type: string
                        documentVersion:
                          description: The version of the document to use when starting
                            the SSM automation document.
                          maxLength: 128
                          type: string
                        dynamicParameters:
                          description: The parameters with dynamic values to set when
                            starting the SSM automation document.
                          items:
                            description: SSMIncidentsResponsePlanDynamicSsmParameter
                              is a nested property type of AWS::SSMIncidents::ResponsePlan.
                            properties:
                              key:
                                maxLength: 50
                                minLength: 1
                                type: string
                              value:
                                description: SSMIncidentsResponsePlanDynamicSsmParameterValue
                                  is a nested property type of AWS::SSMIncidents::ResponsePlan.
                                properties:
                                  variable:
                                    enum:
                                    - INCIDENT_RECORD_ARN
                                    - INVOLVED_RESOURCES
                                    type: string
                                type: object
                            required:
                            - key
                            - value
                            type: object
                          type: array
                        parameters:
                          description: The parameters to set when starting the SSM
                            automation document.
                          items:
                            description: SSMIncidentsResponsePlanSsmParameter is a
                              nested property type of AWS::SSMIncidents::ResponsePlan.
                            properties:
                              key:
                                maxLength: 50
                                minLength: 1
                                type: string
                              values:
                                items:
                                  type: string
                                type: array
                            required:
                            - key
                            - values
                            type: object
                          type: array
                        roleArn:
                          description: The role ARN to use when starting the SSM automation
                            document.
                          maxLength: 1000
                          type: string
                        targetAccount:
                          description: The account type to use when starting the SSM
                            automation document.
                          enum:
                          - IMPACTED_ACCOUNT
                          - RESPONSE_PLAN_OWNER_ACCOUNT
                          type: string
                      required:
                      - documentName
                      - roleArn
                      type: object
                  type: object
                type: array
              chatChannel:
                description: SSMIncidentsResponsePlanChatChannel is a nested property
                  type of AWS::SSMIncidents::ResponsePlan.
                properties:
                  chatbotSns:
                    items:
                      type: string
                    type: array
                type: object
              displayName:
                description: The display name of the response plan.
                maxLength: 200
                type: string
              engagements:
                description: The list of engagements to use.
                items:
                  type: string
                type: array
              incidentTemplate:
                description: SSMIncidentsResponsePlanIncidentTemplate is a nested
                  property type of AWS::SSMIncidents::ResponsePlan.
                properties:
                  dedupeString:
                    description: The deduplication string.
                    maxLength: 1000
                    type: string
                  impact:
                    description: The impact value.
                    format: int64
                    maximum: 5
                    minimum: 1
                    type: integer
                  incidentTags:
                    description: Tags that get applied to incidents created by the
                      StartIncident API action.
                    items:
                      description: CFNTag is the CloudFormation Tag shape shared by
                        generated kinds.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        value:
                          type: string
                      required:
                      - key
                      - value
                      type: object
                    type: array
                  notificationTargets:
                    description: The list of notification targets.
                    items:
                      description: SSMIncidentsResponsePlanNotificationTargetItem
                        is a nested property type of AWS::SSMIncidents::ResponsePlan.
                      properties:
                        snsTopicArn:
                          maxLength: 1000
                          type: string
                      type: object
                    type: array
                  summary:
                    description: The summary string.
                    maxLength: 4000
                    type: string
                  title:
                    description: The title string.
                    maxLength: 200
                    type: string
                required:
                - impact
                - title
                type: object
              integrations:
                description: The list of integrations.
                items:
                  description: SSMIncidentsResponsePlanIntegration is a nested property
                    type of AWS::SSMIncidents::ResponsePlan.
                  properties:
                    pagerDutyConfiguration:
                      description: SSMIncidentsResponsePlanPagerDutyConfiguration
                        is a nested property type of AWS::SSMIncidents::ResponsePlan.
                      properties:
                        name:
                          description: The name of the pagerDuty configuration.
                          maxLength: 200
                          minLength: 1
                          type: string
                        pagerDutyIncidentConfiguration:
                          description: SSMIncidentsResponsePlanPagerDutyIncidentConfiguration
                            is a nested property type of AWS::SSMIncidents::ResponsePlan.
                          properties:
                            serviceId:
                              description: The pagerDuty serviceId.
                              maxLength: 200
                              minLength: 1
                              type: string
                          required:
                          - serviceId
                          type: object
                        secretId:
                          description: The AWS secrets manager secretId storing the
                            pagerDuty token.
                          maxLength: 512
                          minLength: 1
                          type: string
                      required:
                      - name
                      - pagerDutyIncidentConfiguration
                      - secretId
                      type: object
                  type: object
                type: array
              name:
                description: |-
                  The name of the response plan.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 200
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: The tags to apply to the response plan.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - incidentTemplate
            - name
            type: object
          status:
            description: SSMIncidentsResponsePlanStatus is the observed state of AWS::SSMIncidents::ResponsePlan.
            properties:
              arn:
                description: |-
                  The ARN of the response plan.
                  Read-only: reported by AWS after creation.
                maxLength: 1000
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
