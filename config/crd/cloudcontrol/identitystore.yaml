---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: identitystoregroupmemberships.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: IdentityStoreGroupMembership
    listKind: IdentityStoreGroupMembershipList
    plural: identitystoregroupmemberships
    singular: identitystoregroupmembership
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IdentityStoreGroupMembership manages AWS::IdentityStore::GroupMembership through the AWS Cloud Control API.
          Resource Type Definition for AWS:IdentityStore::GroupMembership
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IdentityStoreGroupMembershipSpec is the desired state of
              AWS::IdentityStore::GroupMembership.
            properties:
              groupId:
                description: |-
                  The unique identifier for a group in the identity store.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 47
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: groupId is immutable
                  rule: self == oldSelf
              identityStoreId:
                description: |-
                  The globally unique identifier for the identity store.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 36
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: identityStoreId is immutable
                  rule: self == oldSelf
              memberId:
                description: |-
                  An object containing the identifier of a group member.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  userId:
                    description: The identifier for a user in the identity store.
                    maxLength: 47
                    minLength: 1
                    type: string
                required:
                - userId
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - groupId
            - identityStoreId
            - memberId
            type: object
          status:
            description: IdentityStoreGroupMembershipStatus is the observed state
              of AWS::IdentityStore::GroupMembership.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              membershipId:
                description: |-
                  The identifier for a GroupMembership in the identity store.
                  Read-only: reported by AWS after creation.
                maxLength: 47
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: identitystoregroups.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: IdentityStoreGroup
    listKind: IdentityStoreGroupList
    plural: identitystoregroups
    singular: identitystoregroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IdentityStoreGroup manages AWS::IdentityStore::Group through the AWS Cloud Control API. Resource Type
          definition for AWS::IdentityStore::Group
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IdentityStoreGroupSpec is the desired state of AWS::IdentityStore::Group.
            properties:
              description:
                description: A string containing the description of the group.
                maxLength: 1024
                type: string
              displayName:
                description: A string containing the name of the group. This value
                  is commonly displayed when the group is referenced.
                maxLength: 1024
                minLength: 1
                type: string
              identityStoreId:
                description: |-
                  The globally unique identifier for the identity store.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 36
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: identityStoreId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - displayName
            - identityStoreId
            type: object
          status:
            description: IdentityStoreGroupStatus is the observed state of AWS::IdentityStore::Group.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              groupId:
                description: |-
                  The unique identifier for a group in the identity store.
                  Read-only: reported by AWS after creation.
                maxLength: 47
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: identitystoreusers.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: IdentityStoreUser
    listKind: IdentityStoreUserList
    plural: identitystoreusers
    singular: identitystoreuser
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          IdentityStoreUser manages AWS::IdentityStore::User through the AWS Cloud Control API. Creates a user within
          the specified identity store.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IdentityStoreUserSpec is the desired state of AWS::IdentityStore::User.
            properties:
              addresses:
                description: |-
                  A list of addresses associated with the user.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: IdentityStoreUserAddressesItem is a nested property
                    type of AWS::IdentityStore::User.
                  properties:
                    country:
                      description: The country of the address.
                      maxLength: 1024
                      type: string
                    formatted:
                      description: A formatted version of the address for display.
                      maxLength: 1024
                      type: string
                    locality:
                      description: A string of the address locality.
                      maxLength: 1024
                      type: string
                    postalCode:
                      description: The postal code of the address.
                      maxLength: 1024
                      type: string
                    primary:
                      description: Whether this is the primary address.
                      type: boolean
                    region:
                      description: The region of the address.
                      maxLength: 1024
                      type: string
                    streetAddress:
                      description: The street of the address.
                      maxLength: 1024
                      type: string
                    type:
                      description: The type of address.
                      maxLength: 1024
                      type: string
                  type: object
                type: array
              birthdate:
                description: The user's birthdate in YYYY-MM-DD format.
                maxLength: 1024
                type: string
              displayName:
                description: A string containing the name of the user for display.
                maxLength: 1024
                type: string
              emails:
                description: |-
                  A list of email addresses associated with the user.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: IdentityStoreUserEmailsItem is a nested property type
                    of AWS::IdentityStore::User.
                  properties:
                    primary:
                      description: Whether this is the primary email address.
                      type: boolean
                    type:
                      description: The type of email address.
                      maxLength: 1024
                      type: string
                    value:
                      description: The email address.
                      maxLength: 1024
                      type: string
                  type: object
                type: array
              identityStoreId:
                description: |-
                  The globally unique identifier for the identity store.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 36
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: identityStoreId is immutable
                  rule: self == oldSelf
              locale:
                description: The geographical region or location of the user.
                maxLength: 1024
                type: string
              name:
                description: The name of the user.
                properties:
                  familyName:
                    description: The family name of the user.
                    maxLength: 1024
                    type: string
                  formatted:
                    description: A string containing a formatted version of the name
                      for display.
                    maxLength: 1024
                    type: string
                  givenName:
                    description: The given name of the user.
                    maxLength: 1024
                    type: string
                  honorificPrefix:
                    description: The honorific prefix of the user.
                    maxLength: 1024
                    type: string
                  honorificSuffix:
                    description: The honorific suffix of the user.
                    maxLength: 1024
                    type: string
                  middleName:
                    description: The middle name of the user.
                    maxLength: 1024
                    type: string
                type: object
              nickName:
                description: An alternate name for the user.
                maxLength: 1024
                type: string
              phoneNumbers:
                description: |-
                  A list of phone numbers associated with the user.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: IdentityStoreUserPhoneNumbersItem is a nested property
                    type of AWS::IdentityStore::User.
                  properties:
                    primary:
                      description: Whether this is the primary phone number.
                      type: boolean
                    type:
                      description: The type of phone number.
                      maxLength: 1024
                      type: string
                    value:
                      description: The phone number.
                      maxLength: 1024
                      type: string
                  type: object
                type: array
              photos:
                description: |-
                  A list of photos associated with the user.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: IdentityStoreUserPhotosItem is a nested property type
                    of AWS::IdentityStore::User.
                  properties:
                    display:
                      description: A display name for the photo.
                      maxLength: 1024
                      type: string
                    primary:
                      description: Whether this is the primary photo.
                      type: boolean
                    type:
                      description: The type of photo.
                      maxLength: 1024
                      type: string
                    value:
                      description: The photo data or URL.
                      maxLength: 1024
                      minLength: 1
                      type: string
                  required:
                  - value
                  type: object
                type: array
              preferredLanguage:
                description: The preferred language of the user.
                maxLength: 1024
                type: string
              profileUrl:
                description: A URL associated with the user.
                maxLength: 1024
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roles:
                description: |-
                  A list of roles associated with the user.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: IdentityStoreUserRolesItem is a nested property type
                    of AWS::IdentityStore::User.
                  properties:
                    primary:
                      description: Whether this is the primary role.
                      type: boolean
                    type:
                      description: The type of role.
                      maxLength: 1024
                      type: string
                    value:
                      description: The role name.
                      maxLength: 1024
                      type: string
                  type: object
                type: array
              timezone:
                description: The time zone for the user.
                maxLength: 1024
                type: string
              title:
                description: The title of the user.
                maxLength: 1024
                type: string
              userName:
                description: |-
                  A unique string used to identify the user.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                type: string
              userType:
                description: A string indicating the type of user.
                maxLength: 1024
                type: string
              website:
                description: The user's personal website or blog URL.
                maxLength: 1024
                type: string
            required:
            - identityStoreId
            type: object
          status:
            description: IdentityStoreUserStatus is the observed state of AWS::IdentityStore::User.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the user.
                  Read-only: reported by AWS after creation.
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The date and time the user was created.
                  Read-only: reported by AWS after creation.
                type: string
              createdBy:
                description: |-
                  The identifier of the user or system that created the user.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  The date and time the user was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              updatedBy:
                description: |-
                  The identifier of the user or system that last updated the user.
                  Read-only: reported by AWS after creation.
                type: string
              userId:
                description: |-
                  The identifier for a user in the identity store.
                  Read-only: reported by AWS after creation.
                maxLength: 47
                type: string
              userStatus:
                description: |-
                  The current status of the user account.
                  Read-only: reported by AWS after creation.
                enum:
                - ENABLED
                - DISABLED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
