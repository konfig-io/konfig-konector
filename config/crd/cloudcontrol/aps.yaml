---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: prometheusanomalydetectors.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: PrometheusAnomalyDetector
    listKind: PrometheusAnomalyDetectorList
    plural: prometheusanomalydetectors
    singular: prometheusanomalydetector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PrometheusAnomalyDetector manages AWS::APS::AnomalyDetector through the AWS Cloud Control API. AnomalyDetector
          schema for cloudformation.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrometheusAnomalyDetectorSpec is the desired state of AWS::APS::AnomalyDetector.
            properties:
              alias:
                description: |-
                  The AnomalyDetector alias.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: alias is immutable
                  rule: self == oldSelf
              configuration:
                description: Determines the anomaly detector's algorithm and its configuration.
                properties:
                  randomCutForest:
                    description: PrometheusAnomalyDetectorRandomCutForestConfiguration
                      is a nested property type of AWS::APS::AnomalyDetector.
                    properties:
                      ignoreNearExpectedFromAbove:
                        description: PrometheusAnomalyDetectorRandomCutForestConfigurationIgnoreNearExpectedFromAbove
                          is a nested property type of AWS::APS::AnomalyDetector.
                        properties:
                          amount:
                            type: number
                          ratio:
                            type: number
                        type: object
                      ignoreNearExpectedFromBelow:
                        description: PrometheusAnomalyDetectorRandomCutForestConfigurationIgnoreNearExpectedFromBelow
                          is a nested property type of AWS::APS::AnomalyDetector.
                        properties:
                          amount:
                            type: number
                          ratio:
                            type: number
                        type: object
                      query:
                        minLength: 1
                        type: string
                      sampleSize:
                        format: int64
                        maximum: 1024
                        minimum: 256
                        type: integer
                      shingleSize:
                        format: int64
                        maximum: 1024
                        minimum: 2
                        type: integer
                    required:
                    - query
                    type: object
                required:
                - randomCutForest
                type: object
              evaluationIntervalInSeconds:
                description: The AnomalyDetector period of detection and metric generation.
                format: int64
                type: integer
              labels:
                description: An array of key-value pairs to provide meta-data.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              missingDataAction:
                description: The action to perform when running the expression returns
                  no data.
                properties:
                  markAsAnomaly:
                    type: boolean
                  skip:
                    type: boolean
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              workspace:
                description: |-
                  Required to identify a specific APS Workspace associated with this Anomaly Detector.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: workspace is immutable
                  rule: self == oldSelf
            required:
            - alias
            - configuration
            - workspace
            type: object
          status:
            description: PrometheusAnomalyDetectorStatus is the observed state of
              AWS::APS::AnomalyDetector.
            properties:
              arn:
                description: |-
                  The AnomalyDetector ARN.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: prometheusresourcepolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: PrometheusResourcePolicy
    listKind: PrometheusResourcePolicyList
    plural: prometheusresourcepolicies
    singular: prometheusresourcepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PrometheusResourcePolicy manages AWS::APS::ResourcePolicy through the AWS Cloud Control API. Resource Type
          definition for AWS::APS::ResourcePolicy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrometheusResourcePolicySpec is the desired state of AWS::APS::ResourcePolicy.
            properties:
              policyDocument:
                description: The JSON to use as the Resource-based Policy.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              workspaceArn:
                description: |-
                  The Arn of an APS Workspace that the PolicyDocument will be attached to.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: workspaceArn is immutable
                  rule: self == oldSelf
            required:
            - policyDocument
            - workspaceArn
            type: object
          status:
            description: PrometheusResourcePolicyStatus is the observed state of AWS::APS::ResourcePolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: prometheusscrapers.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: PrometheusScraper
    listKind: PrometheusScraperList
    plural: prometheusscrapers
    singular: prometheusscraper
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          PrometheusScraper manages AWS::APS::Scraper through the AWS Cloud Control API. Resource Type definition for
          AWS::APS::Scraper
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PrometheusScraperSpec is the desired state of AWS::APS::Scraper.
            properties:
              alias:
                description: Scraper alias.
                maxLength: 100
                type: string
              destination:
                description: PrometheusScraperDestination is a nested property type
                  of AWS::APS::Scraper.
                properties:
                  ampConfiguration:
                    description: Configuration for Amazon Managed Prometheus metrics
                      destination
                    properties:
                      workspaceArn:
                        description: ARN of an Amazon Managed Prometheus workspace
                        type: string
                    required:
                    - workspaceArn
                    type: object
                  cloudWatchConfiguration:
                    description: Configuration for CloudWatch metrics destination
                    properties:
                      datasetArn:
                        description: ARN of a CloudWatch dataset
                        type: string
                    required:
                    - datasetArn
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleConfiguration:
                description: PrometheusScraperRoleConfiguration is a nested property
                  type of AWS::APS::Scraper.
                properties:
                  sourceRoleArn:
                    description: IAM Role in source account
                    type: string
                  targetRoleArn:
                    description: IAM Role in the target account
                    type: string
                type: object
              scrapeConfiguration:
                description: PrometheusScraperScrapeConfiguration is a nested property
                  type of AWS::APS::Scraper.
                properties:
                  configurationBlob:
                    description: Prometheus compatible scrape configuration in base64
                      encoded blob format
                    type: string
                type: object
              scraperLoggingConfiguration:
                description: PrometheusScraperScraperLoggingConfiguration is a nested
                  property type of AWS::APS::Scraper.
                properties:
                  loggingDestination:
                    description: PrometheusScraperScraperLoggingDestination is a nested
                      property type of AWS::APS::Scraper.
                    properties:
                      cloudWatchLogs:
                        description: PrometheusScraperCloudWatchLogDestination is
                          a nested property type of AWS::APS::Scraper.
                        properties:
                          logGroupArn:
                            description: ARN of the CloudWatch log group
                            maxLength: 512
                            type: string
                        type: object
                    type: object
                  scraperComponents:
                    items:
                      description: PrometheusScraperScraperComponent is a nested property
                        type of AWS::APS::Scraper.
                      properties:
                        config:
                          description: PrometheusScraperComponentConfig is a nested
                            property type of AWS::APS::Scraper.
                          properties:
                            options:
                              additionalProperties:
                                type: string
                              type: object
                          type: object
                        type:
                          enum:
                          - SERVICE_DISCOVERY
                          - COLLECTOR
                          - EXPORTER
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                required:
                - loggingDestination
                - scraperComponents
                type: object
              source:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  eksConfiguration:
                    description: Configuration for EKS metrics source
                    properties:
                      clusterArn:
                        description: ARN of an EKS cluster
                        type: string
                      securityGroupIds:
                        description: List of security group IDs
                        items:
                          type: string
                        type: array
                      subnetIds:
                        description: List of subnet IDs
                        items:
                          type: string
                        type: array
                    required:
                    - clusterArn
                    - subnetIds
                    type: object
                  vpcConfiguration:
                    description: Configuration for VPC metrics source
                    properties:
                      securityGroupIds:
                        description: List of security group IDs
                        items:
                          type: string
                        type: array
                      subnetIds:
                        description: List of subnet IDs
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroupIds
                    - subnetIds
                    type: object
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - destination
            - scrapeConfiguration
            - source
            type: object
          status:
            description: PrometheusScraperStatus is the observed state of AWS::APS::Scraper.
            properties:
              arn:
                description: |-
                  Scraper ARN.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              roleArn:
                description: |-
                  IAM role ARN for the scraper.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              scraperId:
                description: |-
                  Required to identify a specific scraper.
                  Read-only: reported by AWS after creation.
                maxLength: 64
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
