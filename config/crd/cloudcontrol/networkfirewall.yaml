---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: networkfirewallloggingconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: NetworkFirewallLoggingConfiguration
    listKind: NetworkFirewallLoggingConfigurationList
    plural: networkfirewallloggingconfigurations
    singular: networkfirewallloggingconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NetworkFirewallLoggingConfiguration manages AWS::NetworkFirewall::LoggingConfiguration through the AWS Cloud
          Control API. Resource type definition for AWS::NetworkFirewall::LoggingConfiguration
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkFirewallLoggingConfigurationSpec is the desired state
              of AWS::NetworkFirewall::LoggingConfiguration.
            properties:
              enableMonitoringDashboard:
                type: boolean
              firewallArn:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 256
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: firewallArn is immutable
                  rule: self == oldSelf
              firewallName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 128
                type: string
              loggingConfiguration:
                description: NetworkFirewallLoggingConfigurationLoggingConfiguration
                  is a nested property type of AWS::NetworkFirewall::LoggingConfiguration.
                properties:
                  logDestinationConfigs:
                    items:
                      description: NetworkFirewallLoggingConfigurationLogDestinationConfig
                        is a nested property type of AWS::NetworkFirewall::LoggingConfiguration.
                      properties:
                        logDestination:
                          additionalProperties:
                            type: string
                          description: A key-value pair to configure the logDestinations.
                          type: object
                        logDestinationType:
                          enum:
                          - S3
                          - CloudWatchLogs
                          - KinesisDataFirehose
                          type: string
                        logType:
                          enum:
                          - ALERT
                          - FLOW
                          - TLS
                          type: string
                      required:
                      - logDestination
                      - logDestinationType
                      - logType
                      type: object
                    type: array
                required:
                - logDestinationConfigs
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - firewallArn
            - loggingConfiguration
            type: object
          status:
            description: NetworkFirewallLoggingConfigurationStatus is the observed
              state of AWS::NetworkFirewall::LoggingConfiguration.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: networkfirewalltlsinspectionconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: NetworkFirewallTLSInspectionConfiguration
    listKind: NetworkFirewallTLSInspectionConfigurationList
    plural: networkfirewalltlsinspectionconfigurations
    singular: networkfirewalltlsinspectionconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NetworkFirewallTLSInspectionConfiguration manages AWS::NetworkFirewall::TLSInspectionConfiguration through the
          AWS Cloud Control API. Resource type definition for AWS::NetworkFirewall::TLSInspectionConfiguration
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkFirewallTLSInspectionConfigurationSpec is the desired
              state of AWS::NetworkFirewall::TLSInspectionConfiguration.
            properties:
              description:
                maxLength: 512
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              tlsInspectionConfiguration:
                description: NetworkFirewallTLSInspectionConfigurationTLSInspectionConfiguration
                  is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                properties:
                  serverCertificateConfigurations:
                    items:
                      description: NetworkFirewallTLSInspectionConfigurationServerCertificateConfiguration
                        is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                      properties:
                        certificateAuthorityArn:
                          maxLength: 256
                          type: string
                        checkCertificateRevocationStatus:
                          description: NetworkFirewallTLSInspectionConfigurationServerCertificateConfigurationCheckCertificateRevocationStatus
                            is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                          properties:
                            revokedStatusAction:
                              enum:
                              - PASS
                              - DROP
                              - REJECT
                              type: string
                            unknownStatusAction:
                              enum:
                              - PASS
                              - DROP
                              - REJECT
                              type: string
                          type: object
                        scopes:
                          items:
                            description: NetworkFirewallTLSInspectionConfigurationServerCertificateScope
                              is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                            properties:
                              destinationPorts:
                                items:
                                  description: NetworkFirewallTLSInspectionConfigurationPortRange
                                    is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                                  properties:
                                    fromPort:
                                      format: int64
                                      maximum: 65535
                                      minimum: 0
                                      type: integer
                                    toPort:
                                      format: int64
                                      maximum: 65535
                                      minimum: 0
                                      type: integer
                                  required:
                                  - fromPort
                                  - toPort
                                  type: object
                                type: array
                              destinations:
                                items:
                                  description: NetworkFirewallTLSInspectionConfigurationAddress
                                    is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                                  properties:
                                    addressDefinition:
                                      maxLength: 255
                                      minLength: 1
                                      type: string
                                  required:
                                  - addressDefinition
                                  type: object
                                type: array
                              protocols:
                                items:
                                  format: int64
                                  type: integer
                                type: array
                              sourcePorts:
                                items:
                                  description: NetworkFirewallTLSInspectionConfigurationPortRange
                                    is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                                  properties:
                                    fromPort:
                                      format: int64
                                      maximum: 65535
                                      minimum: 0
                                      type: integer
                                    toPort:
                                      format: int64
                                      maximum: 65535
                                      minimum: 0
                                      type: integer
                                  required:
                                  - fromPort
                                  - toPort
                                  type: object
                                type: array
                              sources:
                                items:
                                  description: NetworkFirewallTLSInspectionConfigurationAddress
                                    is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                                  properties:
                                    addressDefinition:
                                      maxLength: 255
                                      minLength: 1
                                      type: string
                                  required:
                                  - addressDefinition
                                  type: object
                                type: array
                            type: object
                          type: array
                        serverCertificates:
                          items:
                            description: NetworkFirewallTLSInspectionConfigurationServerCertificate
                              is a nested property type of AWS::NetworkFirewall::TLSInspectionConfiguration.
                            properties:
                              resourceArn:
                                maxLength: 256
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                type: object
              tlsInspectionConfigurationName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: tlsInspectionConfigurationName is immutable
                  rule: self == oldSelf
            required:
            - tlsInspectionConfiguration
            - tlsInspectionConfigurationName
            type: object
          status:
            description: NetworkFirewallTLSInspectionConfigurationStatus is the observed
              state of AWS::NetworkFirewall::TLSInspectionConfiguration.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              tlsInspectionConfigurationArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              tlsInspectionConfigurationId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 36
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: networkfirewallvpcendpointassociations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: NetworkFirewallVpcEndpointAssociation
    listKind: NetworkFirewallVpcEndpointAssociationList
    plural: networkfirewallvpcendpointassociations
    singular: networkfirewallvpcendpointassociation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          NetworkFirewallVpcEndpointAssociation manages AWS::NetworkFirewall::VpcEndpointAssociation through the AWS
          Cloud Control API. Resource type definition for AWS::NetworkFirewall::VpcEndpointAssociation
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NetworkFirewallVpcEndpointAssociationSpec is the desired
              state of AWS::NetworkFirewall::VpcEndpointAssociation.
            properties:
              description:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 512
                type: string
              firewallArn:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 256
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: firewallArn is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              subnetMapping:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  ipAddressType:
                    description: A IPAddressType
                    type: string
                  subnetId:
                    description: A SubnetId.
                    type: string
                required:
                - subnetId
                type: object
              tags:
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              vpcId:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: vpcId is immutable
                  rule: self == oldSelf
            required:
            - firewallArn
            - subnetMapping
            - vpcId
            type: object
          status:
            description: NetworkFirewallVpcEndpointAssociationStatus is the observed
              state of AWS::NetworkFirewall::VpcEndpointAssociation.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpointId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              vpcEndpointAssociationArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              vpcEndpointAssociationId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 36
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
