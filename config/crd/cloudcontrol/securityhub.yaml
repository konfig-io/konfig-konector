---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubaggregatorv2s.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubAggregatorV2
    listKind: SecurityHubAggregatorV2List
    plural: securityhubaggregatorv2s
    singular: securityhubaggregatorv2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubAggregatorV2 manages AWS::SecurityHub::AggregatorV2 through the AWS Cloud Control API. The
          AWS::SecurityHub::AggregatorV2 resource represents the AWS Security Hub AggregatorV2 in your account. One
          aggregatorv2 resource is created for each account in non opt-in region in which you configure region linking
          mode.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubAggregatorV2Spec is the desired state of AWS::SecurityHub::AggregatorV2.
            properties:
              linkedRegions:
                description: The list of included Regions
                items:
                  type: string
                type: array
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              regionLinkingMode:
                description: Indicates to link a list of included Regions
                enum:
                - SPECIFIED_REGIONS
                type: string
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - linkedRegions
            - regionLinkingMode
            type: object
          status:
            description: SecurityHubAggregatorV2Status is the observed state of AWS::SecurityHub::AggregatorV2.
            properties:
              aggregationRegion:
                description: |-
                  The aggregation Region of the AggregatorV2
                  Read-only: reported by AWS after creation.
                type: string
              aggregatorV2Arn:
                description: |-
                  The ARN of the AggregatorV2 being created and assigned as the unique identifier
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubautomationrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubAutomationRule
    listKind: SecurityHubAutomationRuleList
    plural: securityhubautomationrules
    singular: securityhubautomationrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubAutomationRule manages AWS::SecurityHub::AutomationRule through the AWS Cloud Control API. The
          “AWS::SecurityHub::AutomationRule“ resource specifies an automation rule based on input parameters. For more
          information, see [Automation
          rules](https://docs.aws.amazon.com/securityhub/latest/userguide/automation-rules.html) in the *User Guide*.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubAutomationRuleSpec is the desired state of AWS::SecurityHub::AutomationRule.
            properties:
              actions:
                description: One or more actions to update finding fields if a finding
                  matches the conditions specified in ``Criteria``.
                items:
                  description: SecurityHubAutomationRuleAutomationRulesAction is a
                    nested property type of AWS::SecurityHub::AutomationRule.
                  properties:
                    findingFieldsUpdate:
                      description: Specifies that the automation rule action is an
                        update to a finding field.
                      properties:
                        confidence:
                          description: The rule action updates the ``Confidence``
                            field of a finding.
                          format: int64
                          maximum: 100
                          minimum: 0
                          type: integer
                        criticality:
                          description: The rule action updates the ``Criticality``
                            field of a finding.
                          format: int64
                          maximum: 100
                          minimum: 0
                          type: integer
                        note:
                          description: The rule action will update the ``Note`` field
                            of a finding.
                          properties:
                            text:
                              description: The updated note text.
                              maxLength: 512
                              minLength: 1
                              type: string
                            updatedBy:
                              description: The principal that updated the note.
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - text
                          - updatedBy
                          type: object
                        relatedFindings:
                          description: The rule action will update the ``RelatedFindings``
                            field of a finding.
                          items:
                            description: SecurityHubAutomationRuleRelatedFinding is
                              a nested property type of AWS::SecurityHub::AutomationRule.
                            properties:
                              id:
                                description: |-
                                  The product-generated identifier for a related finding. Array Members: Minimum number of 1 item. Maximum
                                  number of 20 items.
                                x-kubernetes-preserve-unknown-fields: true
                              productArn:
                                description: The Amazon Resource Name (ARN) for the
                                  product that generated a related finding.
                                maxLength: 2048
                                minLength: 12
                                type: string
                            required:
                            - id
                            - productArn
                            type: object
                          type: array
                        severity:
                          description: The rule action will update the ``Severity``
                            field of a finding.
                          properties:
                            label:
                              description: |-
                                The severity value of the finding. The allowed values are the following. + ``INFORMATIONAL`` - No issue was
                                found. + ``LOW`` - The issue does not require action on its own. + ``MEDIUM`` - The issue must be addressed
                                but not urgently. + ``HIGH`` - The issue must be addressed as a priority. + ``CRITICAL`` - The issue must be
                                remediated immediately to avoid it escalating.
                              enum:
                              - INFORMATIONAL
                              - LOW
                              - MEDIUM
                              - HIGH
                              - CRITICAL
                              type: string
                            normalized:
                              description: |-
                                The normalized severity for the finding. This attribute is to be deprecated in favor of ``Label``. If you
                                provide ``Normalized`` and don't provide ``Label``, ``Label`` is set automatically as follows. + 0 -
                                ``INFORMATIONAL`` + 1–39 - ``LOW`` + 40–69 - ``MEDIUM`` + 70–89 - ``HIGH`` + 90–100 - ``CRITICAL``
                              format: int64
                              maximum: 100
                              minimum: 0
                              type: integer
                            product:
                              description: The native severity as defined by the AWS
                                service or integrated partner product that generated
                                the finding.
                              type: number
                          type: object
                        types:
                          description: The rule action updates the ``Types`` field
                            of a finding.
                          items:
                            type: string
                          type: array
                        userDefinedFields:
                          additionalProperties:
                            type: string
                          description: The rule action updates the ``UserDefinedFields``
                            field of a finding.
                          type: object
                        verificationState:
                          description: The rule action updates the ``VerificationState``
                            field of a finding.
                          enum:
                          - UNKNOWN
                          - TRUE_POSITIVE
                          - FALSE_POSITIVE
                          - BENIGN_POSITIVE
                          type: string
                        workflow:
                          description: The rule action will update the ``Workflow``
                            field of a finding.
                          properties:
                            status:
                              description: |-
                                The status of the investigation into the finding. The workflow status is specific to an individual finding. It
                                does not affect the generation of new findings. For example, setting the workflow status to ``SUPPRESSED`` or
                                ``RESOLVED`` does not prevent a new finding for the same issue. The allowed values are the following. +
                                ``NEW`` - The initial state of a finding, before it is reviewed. Securit...
                              enum:
                              - NEW
                              - NOTIFIED
                              - RESOLVED
                              - SUPPRESSED
                              type: string
                          required:
                          - status
                          type: object
                      type: object
                    type:
                      description: |-
                        Specifies the type of action that Security Hub CSPM takes when a finding matches the defined criteria of a
                        rule.
                      enum:
                      - FINDING_FIELDS_UPDATE
                      type: string
                  required:
                  - findingFieldsUpdate
                  - type
                  type: object
                type: array
              criteria:
                description: |-
                  A set of [Security Finding Format
                  (ASFF)](https://docs.aws.amazon.com/securityhub/latest/userguide/securityhub-findings-format.html) finding
                  field attributes and corresponding expected values that ASH uses to filter findings. If a rule is enabled and
                  a finding matches the criteria specified in this parameter, ASH applies the rule action to the finding.
                properties:
                  awsAccountId:
                    description: |-
                      The AWS-account ID in which a finding was generated. Array Members: Minimum number of 1 item. Maximum number
                      of 100 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  companyName:
                    description: |-
                      The name of the company for the product that generated the finding. For control-based findings, the company is
                      AWS. Array Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceAssociatedStandardsId:
                    description: |-
                      The unique identifier of a standard in which a control is enabled. This field consists of the resource portion
                      of the Amazon Resource Name (ARN) returned for a standard in the
                      [DescribeStandards](https://docs.aws.amazon.com/securityhub/1.0/APIReference/API_DescribeStandards.html) API
                      response. Array Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceSecurityControlId:
                    description: |-
                      The security control ID for which a finding was generated. Security control IDs are the same across standards.
                      Array Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceStatus:
                    description: |-
                      The result of a security check. This field is only used for findings generated from controls. Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  confidence:
                    description: |-
                      The likelihood that a finding accurately identifies the behavior or issue that it was intended to identify.
                      ``Confidence`` is scored on a 0–100 basis using a ratio scale. A value of ``0`` means 0 percent confidence,
                      and a value of ``100`` means 100 percent confidence. For example, a data exfiltration detection based on a
                      statistical deviation of network traffic has low confidence because an act...
                    items:
                      description: SecurityHubAutomationRuleNumberFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  createdAt:
                    description: |-
                      A timestamp that indicates when this finding record was created. For more information about the validation and
                      formatting of timestamp fields in ASHlong, see
                      [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleDateFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        dateRange:
                          description: A date range for the date filter.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          description: |-
                            A timestamp that provides the end date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                        start:
                          description: |-
                            A timestamp that provides the start date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                      type: object
                    type: array
                  criticality:
                    description: |-
                      The level of importance that is assigned to the resources that are associated with a finding. ``Criticality``
                      is scored on a 0–100 basis, using a ratio scale that supports only full integers. A score of ``0`` means that
                      the underlying resources have no criticality, and a score of ``100`` is reserved for the most critical
                      resources. For more information, see [Criticality](https://docs.aws.amazon...
                    items:
                      description: SecurityHubAutomationRuleNumberFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  description:
                    description: 'A finding''s description. Array Members: Minimum
                      number of 1 item. Maximum number of 20 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  firstObservedAt:
                    description: |-
                      A timestamp that indicates when the potential security issue captured by a finding was first observed by the
                      security findings product. For more information about the validation and formatting of timestamp fields in
                      ASHlong, see [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                      Array Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleDateFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        dateRange:
                          description: A date range for the date filter.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          description: |-
                            A timestamp that provides the end date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                        start:
                          description: |-
                            A timestamp that provides the start date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                      type: object
                    type: array
                  generatorId:
                    description: |-
                      The identifier for the solution-specific component that generated a finding. Array Members: Minimum number of
                      1 item. Maximum number of 100 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  id:
                    description: |-
                      The product-specific identifier for a finding. Array Members: Minimum number of 1 item. Maximum number of 20
                      items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  lastObservedAt:
                    description: |-
                      A timestamp that indicates when the security findings provider most recently observed a change in the resource
                      that is involved in the finding. For more information about the validation and formatting of timestamp fields
                      in ASHlong, see
                      [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members:
                      Minimum number of 1 item. Maximum number of 20 it...
                    items:
                      description: SecurityHubAutomationRuleDateFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        dateRange:
                          description: A date range for the date filter.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          description: |-
                            A timestamp that provides the end date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                        start:
                          description: |-
                            A timestamp that provides the start date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                      type: object
                    type: array
                  noteText:
                    description: |-
                      The text of a user-defined note that's added to a finding. Array Members: Minimum number of 1 item. Maximum
                      number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  noteUpdatedAt:
                    description: |-
                      The timestamp of when the note was updated. For more information about the validation and formatting of
                      timestamp fields in ASHlong, see
                      [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleDateFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        dateRange:
                          description: A date range for the date filter.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          description: |-
                            A timestamp that provides the end date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                        start:
                          description: |-
                            A timestamp that provides the start date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                      type: object
                    type: array
                  noteUpdatedBy:
                    description: 'The principal that created a note. Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  productArn:
                    description: |-
                      The Amazon Resource Name (ARN) for a third-party product that generated a finding in Security Hub CSPM. Array
                      Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  productName:
                    description: |-
                      Provides the name of the product that generated the finding. For control-based findings, the product name is
                      Security Hub CSPM. Array Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  recordState:
                    description: 'Provides the current state of a finding. Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  relatedFindingsId:
                    description: |-
                      The product-generated identifier for a related finding. Array Members: Minimum number of 1 item. Maximum
                      number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  relatedFindingsProductArn:
                    description: |-
                      The ARN for the product that generated a related finding. Array Members: Minimum number of 1 item. Maximum
                      number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceDetailsOther:
                    description: |-
                      Custom fields and values about the resource that a finding pertains to. Array Members: Minimum number of 1
                      item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleMapFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to the key value when filtering Security Hub CSPM findings with a map filter. To search
                            for values that have the filter value, use one of the following comparison operators: + To search for values
                            that include the filter value, use ``CONTAINS``. For example, for the ``ResourceTags`` field, the filter
                            ``Department CONTAINS Security`` matches findings that include the value...
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        key:
                          description: |-
                            The key of the map filter. For example, for ``ResourceTags``, ``Key`` identifies the name of the tag. For
                            ``UserDefinedFields``, ``Key`` is the name of the field.
                          type: string
                        value:
                          description: |-
                            The value for the key in the map filter. Filter values are case sensitive. For example, one of the values for
                            a tag called ``Department`` might be ``Security``. If you provide ``security`` as the filter value, then
                            there's no match.
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  resourceId:
                    description: |-
                      The identifier for the given resource type. For AWS resources that are identified by Amazon Resource Names
                      (ARNs), this is the ARN. For AWS resources that lack ARNs, this is the identifier as defined by the
                      AWS-service that created the resource. For non-AWS resources, this is a unique identifier that is associated
                      with the resource. Array Members: Minimum number of 1 item. Maximum number of 100...
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourcePartition:
                    description: |-
                      The partition in which the resource that the finding pertains to is located. A partition is a group of
                      AWS-Regions. Each AWS-account is scoped to one partition. Array Members: Minimum number of 1 item. Maximum
                      number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceRegion:
                    description: |-
                      The AWS-Region where the resource that a finding pertains to is located. Array Members: Minimum number of 1
                      item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceTags:
                    description: |-
                      A list of AWS tags associated with a resource at the time the finding was processed. Array Members: Minimum
                      number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleMapFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to the key value when filtering Security Hub CSPM findings with a map filter. To search
                            for values that have the filter value, use one of the following comparison operators: + To search for values
                            that include the filter value, use ``CONTAINS``. For example, for the ``ResourceTags`` field, the filter
                            ``Department CONTAINS Security`` matches findings that include the value...
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        key:
                          description: |-
                            The key of the map filter. For example, for ``ResourceTags``, ``Key`` identifies the name of the tag. For
                            ``UserDefinedFields``, ``Key`` is the name of the field.
                          type: string
                        value:
                          description: |-
                            The value for the key in the map filter. Filter values are case sensitive. For example, one of the values for
                            a tag called ``Department`` might be ``Security``. If you provide ``security`` as the filter value, then
                            there's no match.
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  resourceType:
                    description: 'A finding''s title. Array Members: Minimum number
                      of 1 item. Maximum number of 100 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  severityLabel:
                    description: 'The severity value of the finding. Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  sourceUrl:
                    description: |-
                      Provides a URL that links to a page about the current finding in the finding product. Array Members: Minimum
                      number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  title:
                    description: 'A finding''s title. Array Members: Minimum number
                      of 1 item. Maximum number of 100 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  type:
                    description: |-
                      One or more finding types in the format of namespace/category/classifier that classify a finding. For a list
                      of namespaces, classifiers, and categories, see [Types taxonomy for
                      ASFF](https://docs.aws.amazon.com/securityhub/latest/userguide/securityhub-findings-format-type-taxonomy.html)
                      in the *User Guide*. Array Members: Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  updatedAt:
                    description: |-
                      A timestamp that indicates when the finding record was most recently updated. For more information about the
                      validation and formatting of timestamp fields in ASHlong, see
                      [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps). Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleDateFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        dateRange:
                          description: A date range for the date filter.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          description: |-
                            A timestamp that provides the end date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                        start:
                          description: |-
                            A timestamp that provides the start date for the date filter. For more information about the validation and
                            formatting of timestamp fields in ASHlong, see
                            [Timestamps](https://docs.aws.amazon.com/securityhub/1.0/APIReference/Welcome.html#timestamps).
                          type: string
                      type: object
                    type: array
                  userDefinedFields:
                    description: |-
                      A list of user-defined name and value string pairs added to a finding. Array Members: Minimum number of 1
                      item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleMapFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to the key value when filtering Security Hub CSPM findings with a map filter. To search
                            for values that have the filter value, use one of the following comparison operators: + To search for values
                            that include the filter value, use ``CONTAINS``. For example, for the ``ResourceTags`` field, the filter
                            ``Department CONTAINS Security`` matches findings that include the value...
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        key:
                          description: |-
                            The key of the map filter. For example, for ``ResourceTags``, ``Key`` identifies the name of the tag. For
                            ``UserDefinedFields``, ``Key`` is the name of the field.
                          type: string
                        value:
                          description: |-
                            The value for the key in the map filter. Filter values are case sensitive. For example, one of the values for
                            a tag called ``Department`` might be ``Security``. If you provide ``security`` as the filter value, then
                            there's no match.
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  verificationState:
                    description: 'Provides the veracity of a finding. Array Members:
                      Minimum number of 1 item. Maximum number of 20 items.'
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  workflowStatus:
                    description: |-
                      Provides information about the status of the investigation into a finding. Array Members: Minimum number of 1
                      item. Maximum number of 20 items.
                    items:
                      description: SecurityHubAutomationRuleStringFilter is a nested
                        property type of AWS::SecurityHub::AutomationRule.
                      properties:
                        comparison:
                          description: |-
                            The condition to apply to a string value when filtering Security Hub CSPM findings. To search for values that
                            have the filter value, use one of the following comparison operators: + To search for values that include the
                            filter value, use ``CONTAINS``. For example, the filter ``Title CONTAINS CloudFront`` matches findings that
                            have a ``Title`` that includes the string CloudFront. + To search for...
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          - CONTAINS
                          - NOT_CONTAINS
                          type: string
                        value:
                          description: |-
                            The string filter value. Filter values are case sensitive. For example, the product name for control-based
                            findings is ``Security Hub CSPM``. If you provide ``security hub`` as the filter value, there's no match.
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                type: object
              description:
                description: A description of the rule.
                maxLength: 1024
                minLength: 1
                type: string
              isTerminal:
                description: |-
                  Specifies whether a rule is the last to be applied with respect to a finding that matches the rule criteria.
                  This is useful when a finding matches the criteria for multiple rules, and each rule has different actions. If
                  a rule is terminal, Security Hub CSPM applies the rule action to a finding that matches the rule criteria and
                  doesn't evaluate other rules for the finding. By default, a rule is...
                type: boolean
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              ruleName:
                description: The name of the rule.
                maxLength: 256
                minLength: 1
                type: string
              ruleOrder:
                description: |-
                  An integer ranging from 1 to 1000 that represents the order in which the rule action is applied to findings.
                  Security Hub CSPM applies rules with lower values for this parameter first.
                format: int64
                maximum: 1000
                minimum: 1
                type: integer
              ruleStatus:
                description: |-
                  Whether the rule is active after it is created. If this parameter is equal to ``ENABLED``, ASH applies the
                  rule to findings and finding updates after the rule is created.
                enum:
                - ENABLED
                - DISABLED
                type: string
              tags:
                additionalProperties:
                  type: string
                description: User-defined tags associated with an automation rule.
                type: object
            required:
            - actions
            - criteria
            - description
            - ruleName
            - ruleOrder
            type: object
          status:
            description: SecurityHubAutomationRuleStatus is the observed state of
              AWS::SecurityHub::AutomationRule.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              createdBy:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubautomationrulev2s.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubAutomationRuleV2
    listKind: SecurityHubAutomationRuleV2List
    plural: securityhubautomationrulev2s
    singular: securityhubautomationrulev2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubAutomationRuleV2 manages AWS::SecurityHub::AutomationRuleV2 through the AWS Cloud Control API.
          Resource schema for AWS::SecurityHub::AutomationRuleV2
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubAutomationRuleV2Spec is the desired state of AWS::SecurityHub::AutomationRuleV2.
            properties:
              actions:
                description: A list of actions to be performed when the rule criteria
                  is met
                items:
                  description: SecurityHubAutomationRuleV2AutomationRulesActionV2
                    is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                  properties:
                    externalIntegrationConfiguration:
                      description: SecurityHubAutomationRuleV2ExternalIntegrationConfiguration
                        is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                      properties:
                        connectorArn:
                          description: The ARN of the connector that establishes the
                            integration
                          type: string
                      type: object
                    findingFieldsUpdate:
                      description: SecurityHubAutomationRuleV2AutomationRulesFindingFieldsUpdateV2
                        is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                      properties:
                        comment:
                          description: Notes or contextual information for findings
                            that are modified by the automation rule
                          type: string
                        severityId:
                          description: The severity level to be assigned to findings
                            that match the automation rule criteria
                          format: int64
                          type: integer
                        statusId:
                          description: The status to be applied to findings that match
                            automation rule criteria
                          format: int64
                          type: integer
                      type: object
                    type:
                      description: The category of action to be executed by the automation
                        rule
                      enum:
                      - FINDING_FIELDS_UPDATE
                      - EXTERNAL_INTEGRATION
                      type: string
                  required:
                  - type
                  type: object
                type: array
              criteria:
                description: SecurityHubAutomationRuleV2Criteria is a nested property
                  type of AWS::SecurityHub::AutomationRuleV2.
                properties:
                  ocsfFindingCriteria:
                    description: SecurityHubAutomationRuleV2OcsfFindingFilters is
                      a nested property type of AWS::SecurityHub::AutomationRuleV2.
                    properties:
                      compositeFilters:
                        description: Enables the creation of complex filtering conditions
                          by combining filter
                        items:
                          description: SecurityHubAutomationRuleV2CompositeFilter
                            is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                          properties:
                            booleanFilters:
                              description: Enables filtering based on boolean field
                                values
                              items:
                                description: SecurityHubAutomationRuleV2OcsfBooleanFilter
                                  is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                properties:
                                  fieldName:
                                    description: The name of the field
                                    enum:
                                    - compliance.assessments.meets_criteria
                                    - vulnerabilities.is_exploit_available
                                    - vulnerabilities.is_fix_available
                                    type: string
                                  filter:
                                    description: SecurityHubAutomationRuleV2BooleanFilter
                                      is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                    properties:
                                      value:
                                        description: The value of the boolean
                                        type: boolean
                                    required:
                                    - value
                                    type: object
                                required:
                                - fieldName
                                - filter
                                type: object
                              type: array
                            dateFilters:
                              description: Enables filtering based on date and timestamp
                                fields
                              items:
                                description: SecurityHubAutomationRuleV2OcsfDateFilter
                                  is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                properties:
                                  fieldName:
                                    description: The name of the field
                                    enum:
                                    - finding_info.created_time_dt
                                    - finding_info.first_seen_time_dt
                                    - finding_info.last_seen_time_dt
                                    - finding_info.modified_time_dt
                                    type: string
                                  filter:
                                    description: SecurityHubAutomationRuleV2DateFilter
                                      is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                    properties:
                                      dateRange:
                                        description: SecurityHubAutomationRuleV2DateRange
                                          is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                        properties:
                                          unit:
                                            description: A date range unit for the
                                              date filter
                                            enum:
                                            - DAYS
                                            type: string
                                          value:
                                            description: A date range value for the
                                              date filter
                                            type: number
                                        required:
                                        - unit
                                        - value
                                        type: object
                                      end:
                                        type: string
                                      start:
                                        type: string
                                    type: object
                                required:
                                - fieldName
                                - filter
                                type: object
                              type: array
                            mapFilters:
                              description: Enables filtering based on map field value
                              items:
                                description: SecurityHubAutomationRuleV2OcsfMapFilter
                                  is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                properties:
                                  fieldName:
                                    description: The name of the field
                                    enum:
                                    - resources.tags
                                    type: string
                                  filter:
                                    description: SecurityHubAutomationRuleV2MapFilter
                                      is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                    properties:
                                      comparison:
                                        description: The condition to apply to the
                                          key value when filtering findings with a
                                          map filter
                                        enum:
                                        - EQUALS
                                        - NOT_EQUALS
                                        type: string
                                      key:
                                        description: The key of the map filter
                                        maxLength: 4096
                                        minLength: 1
                                        type: string
                                      value:
                                        description: The value for the key in the
                                          map filter
                                        maxLength: 4096
                                        minLength: 1
                                        type: string
                                    required:
                                    - comparison
                                    - key
                                    - value
                                    type: object
                                required:
                                - fieldName
                                - filter
                                type: object
                              type: array
                            numberFilters:
                              description: Enables filtering based on numerical field
                                values
                              items:
                                description: SecurityHubAutomationRuleV2OcsfNumberFilter
                                  is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                properties:
                                  fieldName:
                                    description: The name of the field
                                    enum:
                                    - activity_id
                                    - compliance.status_id
                                    - confidence_score
                                    - finding_info.related_events_count
                                    - vendor_attributes.severity_id
                                    type: string
                                  filter:
                                    description: SecurityHubAutomationRuleV2NumberFilter
                                      is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                    properties:
                                      eq:
                                        description: The equal-to condition to be
                                          applied to a single field when querying
                                          for findings
                                        type: number
                                      gte:
                                        description: The greater-than-equal condition
                                          to be applied to a single field when querying
                                          for findings
                                        type: number
                                      lte:
                                        description: The less-than-equal condition
                                          to be applied to a single field when querying
                                          for findings
                                        type: number
                                    type: object
                                required:
                                - fieldName
                                - filter
                                type: object
                              type: array
                            operator:
                              enum:
                              - AND
                              - OR
                              type: string
                            stringFilters:
                              description: Enables filtering based on string field
                                values
                              items:
                                description: SecurityHubAutomationRuleV2OcsfStringFilter
                                  is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                properties:
                                  fieldName:
                                    enum:
                                    - activity_name
                                    - cloud.account.name
                                    - cloud.account.uid
                                    - cloud.provider
                                    - cloud.region
                                    - compliance.assessments.category
                                    - compliance.assessments.name
                                    - compliance.control
                                    - compliance.status
                                    - compliance.standards
                                    - finding_info.desc
                                    - finding_info.src_url
                                    - finding_info.title
                                    - finding_info.types
                                    - finding_info.uid
                                    - finding_info.related_events.uid
                                    - finding_info.related_events.product.uid
                                    - finding_info.related_events.title
                                    - metadata.product.feature.uid
                                    - metadata.product.name
                                    - metadata.product.uid
                                    - metadata.product.vendor_name
                                    - remediation.desc
                                    - remediation.references
                                    - resources.cloud_partition
                                    - resources.name
                                    - resources.region
                                    - resources.type
                                    - resources.uid
                                    - vulnerabilities.fix_coverage
                                    - class_name
                                    - vendor_attributes.severity
                                    type: string
                                  filter:
                                    description: SecurityHubAutomationRuleV2StringFilter
                                      is a nested property type of AWS::SecurityHub::AutomationRuleV2.
                                    properties:
                                      comparison:
                                        description: The condition to apply to a string
                                          value when filtering findings
                                        enum:
                                        - EQUALS
                                        - PREFIX
                                        - NOT_EQUALS
                                        - PREFIX_NOT_EQUALS
                                        - CONTAINS
                                        type: string
                                      value:
                                        description: The string filter value
                                        maxLength: 4096
                                        minLength: 1
                                        type: string
                                    required:
                                    - comparison
                                    - value
                                    type: object
                                required:
                                - fieldName
                                - filter
                                type: object
                              type: array
                          type: object
                        type: array
                      compositeOperator:
                        enum:
                        - AND
                        - OR
                        type: string
                    type: object
                type: object
              description:
                description: A description of the automation rule
                maxLength: 256
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              ruleName:
                description: The name of the automation rule
                maxLength: 256
                minLength: 1
                type: string
              ruleOrder:
                description: The value for the rule priority
                type: number
              ruleStatus:
                description: The status of the automation rule
                enum:
                - ENABLED
                - DISABLED
                type: string
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - actions
            - criteria
            - description
            - ruleName
            - ruleOrder
            type: object
          status:
            description: SecurityHubAutomationRuleV2Status is the observed state of
              AWS::SecurityHub::AutomationRuleV2.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleArn:
                description: |-
                  The ARN of the automation rule
                  Read-only: reported by AWS after creation.
                type: string
              ruleId:
                description: |-
                  The ID of the automation rule
                  Read-only: reported by AWS after creation.
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubconfigurationpolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubConfigurationPolicy
    listKind: SecurityHubConfigurationPolicyList
    plural: securityhubconfigurationpolicies
    singular: securityhubconfigurationpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubConfigurationPolicy manages AWS::SecurityHub::ConfigurationPolicy through the AWS Cloud Control
          API. The AWS::SecurityHub::ConfigurationPolicy resource represents the Central Configuration Policy in your
          account.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubConfigurationPolicySpec is the desired state of
              AWS::SecurityHub::ConfigurationPolicy.
            properties:
              configurationPolicy:
                description: SecurityHubConfigurationPolicyPolicy is a nested property
                  type of AWS::SecurityHub::ConfigurationPolicy.
                properties:
                  securityHub:
                    description: SecurityHubConfigurationPolicySecurityHubPolicy is
                      a nested property type of AWS::SecurityHub::ConfigurationPolicy.
                    properties:
                      enabledStandardIdentifiers:
                        description: A list that defines which security standards
                          are enabled in the configuration policy.
                        items:
                          type: string
                        type: array
                      securityControlsConfiguration:
                        description: SecurityHubConfigurationPolicySecurityControlsConfiguration
                          is a nested property type of AWS::SecurityHub::ConfigurationPolicy.
                        properties:
                          disabledSecurityControlIdentifiers:
                            description: A list of security controls that are disabled
                              in the configuration policy
                            items:
                              type: string
                            type: array
                          enabledSecurityControlIdentifiers:
                            description: A list of security controls that are enabled
                              in the configuration policy.
                            items:
                              type: string
                            type: array
                          securityControlCustomParameters:
                            description: A list of security controls and control parameter
                              values that are included in a configuration policy.
                            items:
                              description: SecurityHubConfigurationPolicySecurityControlCustomParameter
                                is a nested property type of AWS::SecurityHub::ConfigurationPolicy.
                              properties:
                                parameters:
                                  additionalProperties:
                                    description: SecurityHubConfigurationPolicyParameterConfiguration
                                      is a nested property type of AWS::SecurityHub::ConfigurationPolicy.
                                    properties:
                                      value:
                                        description: SecurityHubConfigurationPolicyParameterValue
                                          is a nested property type of AWS::SecurityHub::ConfigurationPolicy.
                                        properties:
                                          boolean:
                                            description: A control parameter that
                                              is a boolean.
                                            type: boolean
                                          double:
                                            description: A control parameter that
                                              is a double.
                                            type: number
                                          enum:
                                            description: A control parameter that
                                              is an enum.
                                            maxLength: 2048
                                            type: string
                                          enumList:
                                            description: A control parameter that
                                              is a list of enums.
                                            items:
                                              type: string
                                            type: array
                                          integer:
                                            description: A control parameter that
                                              is an integer.
                                            format: int64
                                            type: integer
                                          integerList:
                                            description: A control parameter that
                                              is a list of integers.
                                            items:
                                              format: int64
                                              type: integer
                                            type: array
                                          string:
                                            description: A control parameter that
                                              is a string.
                                            maxLength: 2048
                                            type: string
                                          stringList:
                                            description: A control parameter that
                                              is a list of strings.
                                            items:
                                              type: string
                                            type: array
                                        type: object
                                      valueType:
                                        description: |-
                                          Identifies whether a control parameter uses a custom user-defined value or subscribes to the default AWS
                                          Security Hub behavior.
                                        enum:
                                        - DEFAULT
                                        - CUSTOM
                                        type: string
                                    required:
                                    - valueType
                                    type: object
                                  description: An object that specifies parameter
                                    values for a control in a configuration policy.
                                  type: object
                                securityControlId:
                                  description: The ID of the security control.
                                  maxLength: 2048
                                  type: string
                              type: object
                            type: array
                        type: object
                      serviceEnabled:
                        description: Indicates whether Security Hub is enabled in
                          the policy.
                        type: boolean
                    type: object
                type: object
              description:
                description: The description of the configuration policy.
                maxLength: 512
                type: string
              name:
                description: The name of the configuration policy.
                maxLength: 128
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - configurationPolicy
            - name
            type: object
          status:
            description: SecurityHubConfigurationPolicyStatus is the observed state
              of AWS::SecurityHub::ConfigurationPolicy.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the configuration policy.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The date and time, in UTC and ISO 8601 format.
                  Read-only: reported by AWS after creation.
                type: string
              id:
                description: |-
                  The universally unique identifier (UUID) of the configuration policy.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              serviceEnabled:
                description: |-
                  Indicates whether the service that the configuration policy applies to is enabled in the policy.
                  Read-only: reported by AWS after creation.
                type: boolean
              updatedAt:
                description: |-
                  The date and time, in UTC and ISO 8601 format.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubconnectors.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubConnector
    listKind: SecurityHubConnectorList
    plural: securityhubconnectors
    singular: securityhubconnector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubConnector manages AWS::SecurityHub::Connector through the AWS Cloud Control API. Creates a
          connector to a third-party cloud provider in Security Hub CSPM. A connector establishes a connection between
          Security Hub CSPM and a third-party cloud provider, enabling Security Hub CSPM to ingest security findings and
          resource data from the connected environment.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubConnectorSpec is the desired state of AWS::SecurityHub::Connector.
            properties:
              description:
                description: The description of the connector.
                type: string
              name:
                description: |-
                  The name of the connector.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              provider:
                description: SecurityHubConnectorProvider is a nested property type
                  of AWS::SecurityHub::Connector.
                properties:
                  azure:
                    description: SecurityHubConnectorAzureProviderConfiguration is
                      a nested property type of AWS::SecurityHub::Connector.
                    properties:
                      awsConfigConnectorArn:
                        description: The ARN of the multi-cloud configuration connector
                          used to establish the connection to Azure.
                        type: string
                      azureRegions:
                        description: The list of Azure regions to monitor.
                        items:
                          type: string
                        type: array
                      scopeConfiguration:
                        description: The scope configuration that defines which Azure
                          resources are monitored.
                        properties:
                          scopeType:
                            description: The type of scope. Valid values are ``tenant``
                              and ``subscription``.
                            enum:
                            - TENANT
                            - SUBSCRIPTION
                            type: string
                          scopeValues:
                            description: The list of scope values, such as subscription
                              IDs, when the scope type is ``subscription``.
                            items:
                              type: string
                            type: array
                        required:
                        - scopeType
                        type: object
                    required:
                    - awsConfigConnectorArn
                    - azureRegions
                    - scopeConfiguration
                    type: object
                required:
                - azure
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - name
            - provider
            type: object
          status:
            description: SecurityHubConnectorStatus is the observed state of AWS::SecurityHub::Connector.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectorArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              connectorId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              connectorStatus:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              createdBy:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              enablementStatus:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              issues:
                description: 'Read-only: reported by AWS after creation.'
                items:
                  description: SecurityHubConnectorHealthIssue is a nested property
                    type of AWS::SecurityHub::Connector.
                  properties:
                    code:
                      description: The error code that identifies the type of health
                        issue.
                      type: string
                    message:
                      description: A human-readable message that describes the health
                        issue.
                      type: string
                  required:
                  - code
                  - message
                  type: object
                type: array
              lastCheckedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              message:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubconnectorv2s.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubConnectorV2
    listKind: SecurityHubConnectorV2List
    plural: securityhubconnectorv2s
    singular: securityhubconnectorv2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubConnectorV2 manages AWS::SecurityHub::ConnectorV2 through the AWS Cloud Control API. Resource
          schema for AWS::SecurityHub::ConnectorV2
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubConnectorV2Spec is the desired state of AWS::SecurityHub::ConnectorV2.
            properties:
              description:
                description: A description of the connector
                maxLength: 256
                type: string
              kmsKeyArn:
                description: |-
                  The ARN of KMS key used for the connector
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              name:
                description: |-
                  The name of the connector
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              provider:
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - name
            - provider
            type: object
          status:
            description: SecurityHubConnectorV2Status is the observed state of AWS::SecurityHub::ConnectorV2.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectorArn:
                description: |-
                  The ARN of the connector
                  Read-only: reported by AWS after creation.
                type: string
              connectorId:
                description: |-
                  The ID of the connector
                  Read-only: reported by AWS after creation.
                type: string
              connectorStatus:
                description: |-
                  The status of the connector
                  Read-only: reported by AWS after creation.
                type: string
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              enablementStatus:
                description: |-
                  The enablement status of the connector
                  Read-only: reported by AWS after creation.
                type: string
              enablementStatusReason:
                description: |-
                  The reason for the enablement status of the connector
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              issues:
                description: |-
                  The list of health issues associated with the connector
                  Read-only: reported by AWS after creation.
                items:
                  description: SecurityHubConnectorV2HealthIssue is a nested property
                    type of AWS::SecurityHub::ConnectorV2.
                  properties:
                    code:
                      description: The code identifying the type of health issue
                      type: string
                    message:
                      description: The message describing the health issue
                      type: string
                  required:
                  - code
                  - message
                  type: object
                type: array
              lastCheckedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              message:
                description: |-
                  The message of the connector status change
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubdelegatedadmins.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubDelegatedAdmin
    listKind: SecurityHubDelegatedAdminList
    plural: securityhubdelegatedadmins
    singular: securityhubdelegatedadmin
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubDelegatedAdmin manages AWS::SecurityHub::DelegatedAdmin through the AWS Cloud Control API. The
          “AWS::SecurityHub::DelegatedAdmin“ resource designates the delegated ASHlong administrator account for an
          organization. You must enable the integration between ASH and AOlong before you can designate a delegated ASH
          administrator. Only the management account for an organization can design...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubDelegatedAdminSpec is the desired state of AWS::SecurityHub::DelegatedAdmin.
            properties:
              adminAccountId:
                description: |-
                  The AWS-account identifier of the account to designate as the Security Hub CSPM administrator account.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: adminAccountId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - adminAccountId
            type: object
          status:
            description: SecurityHubDelegatedAdminStatus is the observed state of
              AWS::SecurityHub::DelegatedAdmin.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              delegatedAdminIdentifier:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - ENABLED
                - DISABLE_IN_PROGRESS
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubfindingaggregators.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubFindingAggregator
    listKind: SecurityHubFindingAggregatorList
    plural: securityhubfindingaggregators
    singular: securityhubfindingaggregator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubFindingAggregator manages AWS::SecurityHub::FindingAggregator through the AWS Cloud Control API.
          The “AWS::SecurityHub::FindingAggregator“ resource enables cross-Region aggregation. When cross-Region
          aggregation is enabled, you can aggregate findings, finding updates, insights, control compliance statuses,
          and security scores from one or more linked Regions to a single aggregation ...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubFindingAggregatorSpec is the desired state of
              AWS::SecurityHub::FindingAggregator.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              regionLinkingMode:
                description: |-
                  Indicates whether to aggregate findings from all of the available Regions in the current partition. Also
                  determines whether to automatically aggregate findings from new Regions as Security Hub supports them and you
                  opt into them. The selected option also determines how to use the Regions provided in the Regions list. In
                  CFN, the options for this property are as follows: + ``ALL_REGIONS`` - Indi...
                enum:
                - ALL_REGIONS
                - ALL_REGIONS_EXCEPT_SPECIFIED
                - SPECIFIED_REGIONS
                type: string
              regions:
                description: |-
                  If ``RegionLinkingMode`` is ``ALL_REGIONS_EXCEPT_SPECIFIED``, then this is a space-separated list of Regions
                  that do not aggregate findings to the aggregation Region. If ``RegionLinkingMode`` is ``SPECIFIED_REGIONS``,
                  then this is a space-separated list of Regions that do aggregate findings to the aggregation Region.
                items:
                  type: string
                type: array
            required:
            - regionLinkingMode
            type: object
          status:
            description: SecurityHubFindingAggregatorStatus is the observed state
              of AWS::SecurityHub::FindingAggregator.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              findingAggregationRegion:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              findingAggregatorArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubhubv2s.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubHubV2
    listKind: SecurityHubHubV2List
    plural: securityhubhubv2s
    singular: securityhubhubv2
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubHubV2 manages AWS::SecurityHub::HubV2 through the AWS Cloud Control API. The
          AWS::SecurityHub::HubV2 resource represents the implementation of the AWS Security Hub V2 service in your
          account. Only one hubv2 resource can created in each region in which you enable Security Hub V2.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubHubV2Spec is the desired state of AWS::SecurityHub::HubV2.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            type: object
          status:
            description: SecurityHubHubV2Status is the observed state of AWS::SecurityHub::HubV2.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              hubV2Arn:
                description: |-
                  The Amazon Resource Name of the Security Hub V2 resource.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              subscribedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubinsights.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubInsight
    listKind: SecurityHubInsightList
    plural: securityhubinsights
    singular: securityhubinsight
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubInsight manages AWS::SecurityHub::Insight through the AWS Cloud Control API. The
          AWS::SecurityHub::Insight resource represents the AWS Security Hub Insight in your account. An AWS Security
          Hub insight is a collection of related findings.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubInsightSpec is the desired state of AWS::SecurityHub::Insight.
            properties:
              filters:
                description: One or more attributes used to filter the findings included
                  in the insight
                properties:
                  awsAccountId:
                    description: The AWS account ID in which a finding is generated.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  awsAccountName:
                    description: The name of the AWS account in which a finding is
                      generated.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  companyName:
                    description: The name of the findings provider (company) that
                      owns the solution (product) that generates findings.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceAssociatedStandardsId:
                    description: The unique identifier of a standard in which a control
                      is enabled.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceSecurityControlId:
                    description: The unique identifier of a control across standards.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceSecurityControlParametersName:
                    description: The name of a security control parameter.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceSecurityControlParametersValue:
                    description: The current value of a security control parameter.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  complianceStatus:
                    description: |-
                      Exclusive to findings that are generated as the result of a check run against a specific rule in a supported
                      standard.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  confidence:
                    description: A finding's confidence.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  createdAt:
                    description: |-
                      An ISO8601-formatted timestamp that indicates when the security findings provider captured the potential
                      security issue that a finding captured.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  criticality:
                    description: The level of importance assigned to the resources
                      associated with the finding.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  description:
                    description: A finding's description.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingProviderFieldsConfidence:
                    description: The finding provider value for the finding confidence.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  findingProviderFieldsCriticality:
                    description: The finding provider value for the level of importance
                      assigned to the resources associated with the findings.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  findingProviderFieldsRelatedFindingsId:
                    description: The finding identifier of a related finding that
                      is identified by the finding provider.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingProviderFieldsRelatedFindingsProductArn:
                    description: The ARN of the solution that generated a related
                      finding that is identified by the finding provider.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingProviderFieldsSeverityLabel:
                    description: The finding provider value for the severity label.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingProviderFieldsSeverityOriginal:
                    description: The finding provider's original value for the severity.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingProviderFieldsTypes:
                    description: One or more finding types that the finding provider
                      assigned to the finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  firstObservedAt:
                    description: |-
                      An ISO8601-formatted timestamp that indicates when the security findings provider first observed the potential
                      security issue that a finding captured.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  generatorId:
                    description: The identifier for the solution-specific component
                      (a discrete unit of logic) that generated a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  id:
                    description: The security findings provider-specific identifier
                      for a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  keyword:
                    description: A keyword for a finding.
                    items:
                      description: SecurityHubInsightKeywordFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        value:
                          description: A value for the keyword.
                          minLength: 1
                          type: string
                      required:
                      - value
                      type: object
                    type: array
                  lastObservedAt:
                    description: |-
                      An ISO8601-formatted timestamp that indicates when the security findings provider most recently observed the
                      potential security issue that a finding captured.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  malwareName:
                    description: The name of the malware that was observed.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  malwarePath:
                    description: The filesystem path of the malware that was observed.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  malwareState:
                    description: The state of the malware that was observed.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  malwareType:
                    description: The type of the malware that was observed.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  networkDestinationDomain:
                    description: The destination domain of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  networkDestinationIpV4:
                    description: The destination IPv4 address of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightIpFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        cidr:
                          description: A finding's CIDR value.
                          minLength: 1
                          type: string
                      required:
                      - cidr
                      type: object
                    type: array
                  networkDestinationIpV6:
                    description: The destination IPv6 address of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightIpFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        cidr:
                          description: A finding's CIDR value.
                          minLength: 1
                          type: string
                      required:
                      - cidr
                      type: object
                    type: array
                  networkDestinationPort:
                    description: The destination port of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  networkDirection:
                    description: Indicates the direction of network traffic associated
                      with a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  networkProtocol:
                    description: The protocol of network-related information about
                      a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  networkSourceDomain:
                    description: The source domain of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  networkSourceIpV4:
                    description: The source IPv4 address of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightIpFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        cidr:
                          description: A finding's CIDR value.
                          minLength: 1
                          type: string
                      required:
                      - cidr
                      type: object
                    type: array
                  networkSourceIpV6:
                    description: The source IPv6 address of network-related information
                      about a finding.
                    items:
                      description: SecurityHubInsightIpFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        cidr:
                          description: A finding's CIDR value.
                          minLength: 1
                          type: string
                      required:
                      - cidr
                      type: object
                    type: array
                  networkSourceMac:
                    description: The source media access control (MAC) address of
                      network-related information about a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  networkSourcePort:
                    description: The source port of network-related information about
                      a finding.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  noteText:
                    description: The text of a note.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  noteUpdatedAt:
                    description: The timestamp of when the note was updated.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  noteUpdatedBy:
                    description: The principal that created a note.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  processLaunchedAt:
                    description: A timestamp that identifies when the process was
                      launched.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  processName:
                    description: The name of the process.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  processParentPid:
                    description: The parent process ID.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  processPath:
                    description: The path to the process executable.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  processPid:
                    description: The process ID.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  processTerminatedAt:
                    description: A timestamp that identifies when the process was
                      terminated.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  productArn:
                    description: |-
                      The ARN generated by Security Hub that uniquely identifies a third-party company (security findings provider)
                      after this provider's product (solution that generates findings) is registered with Security Hub.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  productFields:
                    description: |-
                      A data type where security findings providers can include additional solution-specific details that aren't
                      part of the defined AwsSecurityFinding format.
                    items:
                      description: SecurityHubInsightMapFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          description: The condition to apply to the key value when
                            filtering Security Hub findings with a map filter.
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          type: string
                        key:
                          minLength: 1
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  productName:
                    description: The name of the solution (product) that generates
                      findings.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  recommendationText:
                    description: The recommendation of what to do about the issue
                      described in a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  recordState:
                    description: The updated record state for the finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  region:
                    description: The Region from which the finding was generated.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  relatedFindingsId:
                    description: The solution-generated identifier for a related finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  relatedFindingsProductArn:
                    description: The ARN of the solution that generated a related
                      finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceApplicationArn:
                    description: The ARN of the application that is related to a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceApplicationName:
                    description: The name of the application that is related to a
                      finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsEc2InstanceIamInstanceProfileArn:
                    description: The IAM profile ARN of the instance.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsEc2InstanceImageId:
                    description: The Amazon Machine Image (AMI) ID of the instance.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsEc2InstanceIpV4Addresses:
                    description: The IPv4 addresses associated with the instance.
                    items:
                      description: SecurityHubInsightIpFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        cidr:
                          description: A finding's CIDR value.
                          minLength: 1
                          type: string
                      required:
                      - cidr
                      type: object
                    type: array
                  resourceAwsEc2InstanceIpV6Addresses:
                    description: The IPv6 addresses associated with the instance.
                    items:
                      description: SecurityHubInsightIpFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        cidr:
                          description: A finding's CIDR value.
                          minLength: 1
                          type: string
                      required:
                      - cidr
                      type: object
                    type: array
                  resourceAwsEc2InstanceKeyName:
                    description: The key name associated with the instance.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsEc2InstanceLaunchedAt:
                    description: The date and time the instance was launched.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  resourceAwsEc2InstanceSubnetId:
                    description: The identifier of the subnet that the instance was
                      launched in.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsEc2InstanceType:
                    description: The instance type of the instance.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsEc2InstanceVpcId:
                    description: The identifier of the VPC that the instance was launched
                      in.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsIamAccessKeyCreatedAt:
                    description: The creation date/time of the IAM access key related
                      to a finding.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  resourceAwsIamAccessKeyPrincipalName:
                    description: The name of the principal that is associated with
                      an IAM access key.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsIamAccessKeyStatus:
                    description: The status of the IAM access key related to a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsIamAccessKeyUserName:
                    description: The user associated with the IAM access key related
                      to a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsIamUserUserName:
                    description: The name of an IAM user.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsS3BucketOwnerId:
                    description: The canonical user ID of the owner of the S3 bucket.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceAwsS3BucketOwnerName:
                    description: The display name of the owner of the S3 bucket.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceContainerImageId:
                    description: The identifier of the image related to a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceContainerImageName:
                    description: The name of the image related to a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceContainerLaunchedAt:
                    description: A timestamp that identifies when the container was
                      started.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  resourceContainerName:
                    description: The name of the container related to a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceDetailsOther:
                    description: The details of a resource that doesn't have a specific
                      subfield for the resource type defined.
                    items:
                      description: SecurityHubInsightMapFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          description: The condition to apply to the key value when
                            filtering Security Hub findings with a map filter.
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          type: string
                        key:
                          minLength: 1
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  resourceId:
                    description: The canonical identifier for the given resource type.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourcePartition:
                    description: The canonical AWS partition name that the Region
                      is assigned to.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceRegion:
                    description: The canonical AWS external Region name where this
                      resource is located.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceTags:
                    description: A list of AWS tags associated with a resource at
                      the time the finding was processed.
                    items:
                      description: SecurityHubInsightMapFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          description: The condition to apply to the key value when
                            filtering Security Hub findings with a map filter.
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          type: string
                        key:
                          minLength: 1
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  resourceType:
                    description: Specifies the type of the resource that details are
                      provided for.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  sample:
                    description: Indicates whether or not sample findings are included
                      in the filter results.
                    items:
                      description: SecurityHubInsightBooleanFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        value:
                          description: The value of the boolean.
                          type: boolean
                      required:
                      - value
                      type: object
                    type: array
                  severityLabel:
                    description: The label of a finding's severity.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  severityNormalized:
                    description: The normalized severity of a finding.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  severityProduct:
                    description: The native severity as defined by the security findings
                      provider's solution that generated the finding.
                    items:
                      description: SecurityHubInsightNumberFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        eq:
                          description: The equal-to condition to be applied to a single
                            field when querying for findings.
                          type: number
                        gte:
                          description: The greater-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                        lte:
                          description: The less-than-equal condition to be applied
                            to a single field when querying for findings.
                          type: number
                      type: object
                    type: array
                  sourceUrl:
                    description: A URL that links to a page about the current finding
                      in the security findings provider's solution.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  threatIntelIndicatorCategory:
                    description: The category of a threat intelligence indicator.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  threatIntelIndicatorLastObservedAt:
                    description: A timestamp that identifies the last observation
                      of a threat intelligence indicator.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  threatIntelIndicatorSource:
                    description: The source of the threat intelligence.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  threatIntelIndicatorSourceUrl:
                    description: The URL for more details from the source of the threat
                      intelligence.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  threatIntelIndicatorType:
                    description: The type of a threat intelligence indicator.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  threatIntelIndicatorValue:
                    description: The value of a threat intelligence indicator.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  title:
                    description: A finding's title.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  type:
                    description: A finding type in the format of namespace/category/classifier
                      that classifies a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  updatedAt:
                    description: |-
                      An ISO8601-formatted timestamp that indicates when the security findings provider last updated the finding
                      record.
                    items:
                      description: SecurityHubInsightDateFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        dateRange:
                          description: SecurityHubInsightDateRange is a nested property
                            type of AWS::SecurityHub::Insight.
                          properties:
                            unit:
                              description: A date range unit for the date filter.
                              enum:
                              - DAYS
                              type: string
                            value:
                              description: A date range value for the date filter.
                              type: number
                          required:
                          - unit
                          - value
                          type: object
                        end:
                          type: string
                        start:
                          type: string
                      type: object
                    type: array
                  userDefinedFields:
                    description: A list of name/value string pairs associated with
                      the finding.
                    items:
                      description: SecurityHubInsightMapFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          description: The condition to apply to the key value when
                            filtering Security Hub findings with a map filter.
                          enum:
                          - EQUALS
                          - NOT_EQUALS
                          type: string
                        key:
                          minLength: 1
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - key
                      - value
                      type: object
                    type: array
                  verificationState:
                    description: The veracity of a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  vulnerabilitiesExploitAvailable:
                    description: Indicates whether a software vulnerability in your
                      environment has a known exploit.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  vulnerabilitiesFixAvailable:
                    description: Indicates whether a vulnerability is fixed in a newer
                      version of the affected software packages.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  workflowState:
                    description: The workflow state of a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  workflowStatus:
                    description: The status of the investigation into a finding.
                    items:
                      description: SecurityHubInsightStringFilter is a nested property
                        type of AWS::SecurityHub::Insight.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          - PREFIX_NOT_EQUALS
                          type: string
                        value:
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                type: object
              groupByAttribute:
                description: The grouping attribute for the insight's findings
                minLength: 1
                type: string
              name:
                description: The name of a Security Hub insight
                maxLength: 128
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - filters
            - groupByAttribute
            - name
            type: object
          status:
            description: SecurityHubInsightStatus is the observed state of AWS::SecurityHub::Insight.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              insightArn:
                description: |-
                  The ARN of a Security Hub insight
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhuborganizationconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubOrganizationConfiguration
    listKind: SecurityHubOrganizationConfigurationList
    plural: securityhuborganizationconfigurations
    singular: securityhuborganizationconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubOrganizationConfiguration manages AWS::SecurityHub::OrganizationConfiguration through the AWS Cloud
          Control API. The AWS::SecurityHub::OrganizationConfiguration resource represents the configuration of your
          organization in Security Hub. Only the Security Hub administrator account can create Organization
          Configuration resource in each region and can opt-in to Central Configuration onl...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubOrganizationConfigurationSpec is the desired state
              of AWS::SecurityHub::OrganizationConfiguration.
            properties:
              autoEnable:
                description: Whether to automatically enable Security Hub in new member
                  accounts when they join the organization.
                type: boolean
              autoEnableStandards:
                description: |-
                  Whether to automatically enable Security Hub default standards in new member accounts when they join the
                  organization.
                enum:
                - DEFAULT
                - NONE
                type: string
              configurationType:
                description: Indicates whether the organization uses local or central
                  configuration.
                enum:
                - CENTRAL
                - LOCAL
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - autoEnable
            type: object
          status:
            description: SecurityHubOrganizationConfigurationStatus is the observed
              state of AWS::SecurityHub::OrganizationConfiguration.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              memberAccountLimitReached:
                description: |-
                  Whether the maximum number of allowed member accounts are already associated with the Security Hub
                  administrator account.
                  Read-only: reported by AWS after creation.
                type: boolean
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              organizationConfigurationIdentifier:
                description: |-
                  The identifier of the OrganizationConfiguration being created and assigned as the unique identifier.
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  Describes whether central configuration could be enabled as the ConfigurationType for the organization.
                  Read-only: reported by AWS after creation.
                enum:
                - PENDING
                - ENABLED
                - FAILED
                type: string
              statusMessage:
                description: |-
                  Provides an explanation if the value of Status is equal to FAILED when ConfigurationType is equal to CENTRAL.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubpolicyassociations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubPolicyAssociation
    listKind: SecurityHubPolicyAssociationList
    plural: securityhubpolicyassociations
    singular: securityhubpolicyassociation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubPolicyAssociation manages AWS::SecurityHub::PolicyAssociation through the AWS Cloud Control API.
          The AWS::SecurityHub::PolicyAssociation resource represents the AWS Security Hub Central Configuration Policy
          associations in your Target. Only the AWS Security Hub delegated administrator can create the resouce from the
          home region.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubPolicyAssociationSpec is the desired state of
              AWS::SecurityHub::PolicyAssociation.
            properties:
              configurationPolicyId:
                description: |-
                  The universally unique identifier (UUID) of the configuration policy or a value of SELF_MANAGED_SECURITY_HUB
                  for a self-managed configuration
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              targetId:
                description: |-
                  The identifier of the target account, organizational unit, or the root
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: targetId is immutable
                  rule: self == oldSelf
              targetType:
                description: |-
                  Indicates whether the target is an AWS account, organizational unit, or the organization root
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - ACCOUNT
                - ORGANIZATIONAL_UNIT
                - ROOT
                type: string
                x-kubernetes-validations:
                - message: targetType is immutable
                  rule: self == oldSelf
            required:
            - configurationPolicyId
            - targetId
            - targetType
            type: object
          status:
            description: SecurityHubPolicyAssociationStatus is the observed state
              of AWS::SecurityHub::PolicyAssociation.
            properties:
              associationIdentifier:
                description: |-
                  A unique identifier to indicates if the target has an association
                  Read-only: reported by AWS after creation.
                type: string
              associationStatus:
                description: |-
                  The current status of the association between the specified target and the configuration
                  Read-only: reported by AWS after creation.
                enum:
                - SUCCESS
                - PENDING
                - FAILED
                - DISABLED
                type: string
              associationStatusMessage:
                description: |-
                  An explanation for a FAILED value for AssociationStatus
                  Read-only: reported by AWS after creation.
                type: string
              associationType:
                description: |-
                  Indicates whether the association between the specified target and the configuration was directly applied by
                  the Security Hub delegated administrator or inherited from a parent
                  Read-only: reported by AWS after creation.
                enum:
                - APPLIED
                - INHERITED
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  The date and time, in UTC and ISO 8601 format, that the configuration policy association was last updated
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubproductsubscriptions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubProductSubscription
    listKind: SecurityHubProductSubscriptionList
    plural: securityhubproductsubscriptions
    singular: securityhubproductsubscription
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubProductSubscription manages AWS::SecurityHub::ProductSubscription through the AWS Cloud Control
          API. The AWS::SecurityHub::ProductSubscription resource represents a subscription to a service that is allowed
          to generate findings for your Security Hub account. One product subscription resource is created for each
          product enabled.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubProductSubscriptionSpec is the desired state of
              AWS::SecurityHub::ProductSubscription.
            properties:
              productArn:
                description: |-
                  The generic ARN of the product being subscribed to
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: productArn is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - productArn
            type: object
          status:
            description: SecurityHubProductSubscriptionStatus is the observed state
              of AWS::SecurityHub::ProductSubscription.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              productSubscriptionArn:
                description: |-
                  The ARN of the product subscription for the account
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: securityhubsecuritycontrols.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SecurityHubSecurityControl
    listKind: SecurityHubSecurityControlList
    plural: securityhubsecuritycontrols
    singular: securityhubsecuritycontrol
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SecurityHubSecurityControl manages AWS::SecurityHub::SecurityControl through the AWS Cloud Control API. A
          security control in Security Hub describes a security best practice related to a specific resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SecurityHubSecurityControlSpec is the desired state of AWS::SecurityHub::SecurityControl.
            properties:
              lastUpdateReason:
                description: |-
                  The most recent reason for updating the customizable properties of a security control. This differs from the
                  UpdateReason field of the BatchUpdateStandardsControlAssociations API, which tracks the reason for updating
                  the enablement status of a control. This field accepts alphanumeric characters in addition to white spaces,
                  dashes, and underscores.
                type: string
              parameters:
                additionalProperties:
                  description: SecurityHubSecurityControlParameterConfiguration is
                    a nested property type of AWS::SecurityHub::SecurityControl.
                  properties:
                    value:
                      description: SecurityHubSecurityControlParameterValue is a nested
                        property type of AWS::SecurityHub::SecurityControl.
                      properties:
                        boolean:
                          description: A control parameter that is a boolean.
                          type: boolean
                        double:
                          description: A control parameter that is a double.
                          type: number
                        enum:
                          description: A control parameter that is a enum.
                          type: string
                        enumList:
                          description: A control parameter that is a list of enums.
                          items:
                            type: string
                          type: array
                        integer:
                          description: A control parameter that is a integer.
                          format: int64
                          type: integer
                        integerList:
                          description: A control parameter that is a list of integers.
                          items:
                            format: int64
                            type: integer
                          type: array
                        string:
                          description: A control parameter that is a string.
                          type: string
                        stringList:
                          description: A control parameter that is a list of strings.
                          items:
                            type: string
                          type: array
                      type: object
                    valueType:
                      enum:
                      - DEFAULT
                      - CUSTOM
                      type: string
                  required:
                  - valueType
                  type: object
                description: |-
                  An object that identifies the name of a control parameter, its current value, and whether it has been
                  customized.
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              securityControlArn:
                description: |-
                  The Amazon Resource Name (ARN) for a security control across standards, such as
                  `arn:aws:securityhub:eu-central-1:123456789012:security-control/S3.1`. This parameter doesn't mention a
                  specific standard.
                type: string
              securityControlId:
                description: |-
                  The unique identifier of a security control across standards. Values for this field typically consist of an
                  AWS service name and a number, such as APIGateway.3.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
            required:
            - parameters
            type: object
          status:
            description: SecurityHubSecurityControlStatus is the observed state of
              AWS::SecurityHub::SecurityControl.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
