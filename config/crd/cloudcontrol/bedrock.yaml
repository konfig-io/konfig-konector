---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentaliases.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentAlias
    listKind: BedrockAgentAliasList
    plural: bedrockagentaliases
    singular: bedrockagentalias
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentAlias manages AWS::Bedrock::AgentAlias through the AWS Cloud Control API. Definition of
          AWS::Bedrock::AgentAlias Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentAliasSpec is the desired state of AWS::Bedrock::AgentAlias.
            properties:
              agentAliasName:
                description: Name for a resource.
                type: string
              agentId:
                description: |-
                  Identifier for a resource.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: agentId is immutable
                  rule: self == oldSelf
              description:
                description: Description of the Resource.
                maxLength: 200
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              routingConfiguration:
                description: Routing configuration for an Agent alias.
                items:
                  description: BedrockAgentAliasAgentAliasRoutingConfigurationListItem
                    is a nested property type of AWS::Bedrock::AgentAlias.
                  properties:
                    agentVersion:
                      description: Agent Version.
                      maxLength: 5
                      minLength: 1
                      type: string
                  required:
                  - agentVersion
                  type: object
                type: array
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - agentAliasName
            - agentId
            type: object
          status:
            description: BedrockAgentAliasStatus is the observed state of AWS::Bedrock::AgentAlias.
            properties:
              agentAliasArn:
                description: |-
                  Arn representation of the Agent Alias.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              agentAliasHistoryEvents:
                description: |-
                  The list of history events for an alias for an Agent.
                  Read-only: reported by AWS after creation.
                items:
                  description: BedrockAgentAliasAgentAliasHistoryEvent is a nested
                    property type of AWS::Bedrock::AgentAlias.
                  properties:
                    endDate:
                      description: Time Stamp.
                      type: string
                    routingConfiguration:
                      description: Routing configuration for an Agent alias.
                      items:
                        description: BedrockAgentAliasAgentAliasRoutingConfigurationListItem
                          is a nested property type of AWS::Bedrock::AgentAlias.
                        properties:
                          agentVersion:
                            description: Agent Version.
                            maxLength: 5
                            minLength: 1
                            type: string
                        required:
                        - agentVersion
                        type: object
                      type: array
                    startDate:
                      description: Time Stamp.
                      type: string
                  type: object
                type: array
              agentAliasId:
                description: |-
                  Id for an Agent Alias generated at the server side.
                  Read-only: reported by AWS after creation.
                maxLength: 10
                type: string
              agentAliasStatus:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - PREPARED
                - FAILED
                - UPDATING
                - DELETING
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagents.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgent
    listKind: BedrockAgentList
    plural: bedrockagents
    singular: bedrockagent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgent manages AWS::Bedrock::Agent through the AWS Cloud Control API. Definition of AWS::Bedrock::Agent
          Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentSpec is the desired state of AWS::Bedrock::Agent.
            properties:
              actionGroups:
                description: List of ActionGroups
                items:
                  description: BedrockAgentAgentActionGroup is a nested property type
                    of AWS::Bedrock::Agent.
                  properties:
                    actionGroupExecutor:
                      x-kubernetes-preserve-unknown-fields: true
                    actionGroupName:
                      description: Name of the action group
                      type: string
                    actionGroupState:
                      enum:
                      - ENABLED
                      - DISABLED
                      type: string
                    apiSchema:
                      x-kubernetes-preserve-unknown-fields: true
                    description:
                      description: Description of action group
                      maxLength: 200
                      type: string
                    functionSchema:
                      description: BedrockAgentFunctionSchema is a nested property
                        type of AWS::Bedrock::Agent.
                      properties:
                        functions:
                          description: List of Function definitions
                          items:
                            description: BedrockAgentFunction is a nested property
                              type of AWS::Bedrock::Agent.
                            properties:
                              description:
                                description: Description of function
                                maxLength: 1200
                                type: string
                              name:
                                description: Name for a resource.
                                type: string
                              parameters:
                                additionalProperties:
                                  description: BedrockAgentParameterDetail is a nested
                                    property type of AWS::Bedrock::Agent.
                                  properties:
                                    description:
                                      description: Description of function parameter.
                                      maxLength: 500
                                      type: string
                                    required:
                                      description: Information about if a parameter
                                        is required for function call. Default to
                                        false.
                                      type: boolean
                                    type:
                                      enum:
                                      - string
                                      - number
                                      - integer
                                      - boolean
                                      - array
                                      type: string
                                  required:
                                  - type
                                  type: object
                                type: object
                              requireConfirmation:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - name
                            type: object
                          type: array
                      required:
                      - functions
                      type: object
                    parentActionGroupSignature:
                      enum:
                      - AMAZON.UserInput
                      - AMAZON.CodeInterpreter
                      type: string
                    skipResourceInUseCheckOnDelete:
                      description: Specifies whether to allow deleting action group
                        while it is in use.
                      type: boolean
                  required:
                  - actionGroupName
                  type: object
                type: array
              agentCollaboration:
                enum:
                - DISABLED
                - SUPERVISOR
                - SUPERVISOR_ROUTER
                type: string
              agentCollaborators:
                description: List of Agent Collaborators
                items:
                  description: BedrockAgentAgentCollaborator is a nested property
                    type of AWS::Bedrock::Agent.
                  properties:
                    agentDescriptor:
                      description: Agent descriptor for agent collaborator
                      properties:
                        aliasArn:
                          description: Alias ARN for agent descriptor
                          type: string
                      type: object
                    collaborationInstruction:
                      description: Agent collaborator instruction
                      type: string
                    collaboratorName:
                      description: Agent collaborator name
                      type: string
                    relayConversationHistory:
                      enum:
                      - TO_COLLABORATOR
                      - DISABLED
                      type: string
                  required:
                  - agentDescriptor
                  - collaborationInstruction
                  - collaboratorName
                  type: object
                type: array
              agentName:
                description: Name for a resource.
                type: string
              agentResourceRoleArn:
                description: ARN of a IAM role.
                maxLength: 2048
                type: string
              autoPrepare:
                description: Specifies whether to automatically prepare after creating
                  or updating the agent.
                type: boolean
              customOrchestration:
                description: BedrockAgentCustomOrchestration is a nested property
                  type of AWS::Bedrock::Agent.
                properties:
                  executor:
                    description: BedrockAgentOrchestrationExecutor is a nested property
                      type of AWS::Bedrock::Agent.
                    properties:
                      lambda:
                        description: ARN of a Lambda.
                        maxLength: 2048
                        type: string
                    required:
                    - lambda
                    type: object
                type: object
              customerEncryptionKeyArn:
                description: A KMS key ARN
                maxLength: 2048
                type: string
              description:
                description: Description of the Resource.
                maxLength: 200
                type: string
              foundationModel:
                maxLength: 2048
                type: string
              guardrailConfiguration:
                description: BedrockAgentGuardrailConfiguration is a nested property
                  type of AWS::Bedrock::Agent.
                properties:
                  guardrailIdentifier:
                    description: Identifier for the guardrail, could be the id or
                      the arn
                    maxLength: 2048
                    type: string
                  guardrailVersion:
                    description: Version of the guardrail
                    type: string
                type: object
              idleSessionTTLInSeconds:
                description: Max Session Time.
                type: number
              instruction:
                description: Instruction for the agent.
                type: string
              knowledgeBases:
                description: List of Agent Knowledge Bases
                items:
                  description: BedrockAgentAgentKnowledgeBase is a nested property
                    type of AWS::Bedrock::Agent.
                  properties:
                    description:
                      description: Description of the Resource.
                      maxLength: 200
                      minLength: 1
                      type: string
                    knowledgeBaseId:
                      description: Identifier for a resource.
                      type: string
                    knowledgeBaseState:
                      enum:
                      - ENABLED
                      - DISABLED
                      type: string
                  required:
                  - description
                  - knowledgeBaseId
                  type: object
                type: array
              memoryConfiguration:
                description: BedrockAgentMemoryConfiguration is a nested property
                  type of AWS::Bedrock::Agent.
                properties:
                  enabledMemoryTypes:
                    items:
                      type: string
                    type: array
                  sessionSummaryConfiguration:
                    description: BedrockAgentSessionSummaryConfiguration is a nested
                      property type of AWS::Bedrock::Agent.
                    properties:
                      maxRecentSessions:
                        description: Maximum number of Sessions to Summarize
                        type: number
                    type: object
                  storageDays:
                    description: Maximum number of days to store session details
                    type: number
                type: object
              orchestrationType:
                enum:
                - DEFAULT
                - CUSTOM_ORCHESTRATION
                type: string
              promptOverrideConfiguration:
                description: BedrockAgentPromptOverrideConfiguration is a nested property
                  type of AWS::Bedrock::Agent.
                properties:
                  overrideLambda:
                    description: ARN of a Lambda.
                    maxLength: 2048
                    type: string
                  promptConfigurations:
                    description: List of BasePromptConfiguration
                    items:
                      description: BedrockAgentPromptConfiguration is a nested property
                        type of AWS::Bedrock::Agent.
                      properties:
                        additionalModelRequestFields:
                          x-kubernetes-preserve-unknown-fields: true
                        basePromptTemplate:
                          description: Base Prompt Template.
                          maxLength: 100000
                          type: string
                        foundationModel:
                          maxLength: 2048
                          type: string
                        inferenceConfiguration:
                          description: BedrockAgentInferenceConfiguration is a nested
                            property type of AWS::Bedrock::Agent.
                          properties:
                            maximumLength:
                              description: Maximum length of output
                              type: number
                            stopSequences:
                              description: List of stop sequences
                              items:
                                type: string
                              type: array
                            temperature:
                              description: Controls randomness, higher values increase
                                diversity
                              type: number
                            topK:
                              description: Sample from the k most likely next tokens
                              type: number
                            topP:
                              description: Cumulative probability cutoff for token
                                selection
                              type: number
                          type: object
                        parserMode:
                          enum:
                          - DEFAULT
                          - OVERRIDDEN
                          type: string
                        promptCreationMode:
                          enum:
                          - DEFAULT
                          - OVERRIDDEN
                          type: string
                        promptState:
                          enum:
                          - ENABLED
                          - DISABLED
                          type: string
                        promptType:
                          enum:
                          - PRE_PROCESSING
                          - ORCHESTRATION
                          - POST_PROCESSING
                          - ROUTING_CLASSIFIER
                          - MEMORY_SUMMARIZATION
                          - KNOWLEDGE_BASE_RESPONSE_GENERATION
                          type: string
                      type: object
                    type: array
                required:
                - promptConfigurations
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              skipResourceInUseCheckOnDelete:
                description: Specifies whether to allow deleting agent while it is
                  in use.
                type: boolean
              tags:
                additionalProperties:
                  type: string
                type: object
              testAliasTags:
                additionalProperties:
                  type: string
                type: object
            required:
            - agentName
            type: object
          status:
            description: BedrockAgentStatus is the observed state of AWS::Bedrock::Agent.
            properties:
              agentArn:
                description: |-
                  Arn representation of the Agent.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              agentId:
                description: |-
                  Identifier for a resource.
                  Read-only: reported by AWS after creation.
                type: string
              agentStatus:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - PREPARING
                - PREPARED
                - NOT_PREPARED
                - DELETING
                - FAILED
                - VERSIONING
                - UPDATING
                type: string
              agentVersion:
                description: |-
                  Draft Agent Version.
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              failureReasons:
                description: |-
                  Failure Reasons for Error.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              preparedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              recommendedActions:
                description: |-
                  The recommended actions users can take to resolve an error in failureReasons.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockapplicationinferenceprofiles.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockApplicationInferenceProfile
    listKind: BedrockApplicationInferenceProfileList
    plural: bedrockapplicationinferenceprofiles
    singular: bedrockapplicationinferenceprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockApplicationInferenceProfile manages AWS::Bedrock::ApplicationInferenceProfile through the AWS Cloud
          Control API. Definition of AWS::Bedrock::ApplicationInferenceProfile Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockApplicationInferenceProfileSpec is the desired state
              of AWS::Bedrock::ApplicationInferenceProfile.
            properties:
              description:
                description: |-
                  Description of the inference profile
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 200
                type: string
              inferenceProfileName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: inferenceProfileName is immutable
                  rule: self == oldSelf
              modelSource:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: List of Tags
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - inferenceProfileName
            type: object
          status:
            description: BedrockApplicationInferenceProfileStatus is the observed
              state of AWS::Bedrock::ApplicationInferenceProfile.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              inferenceProfileArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              inferenceProfileId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 64
                type: string
              inferenceProfileIdentifier:
                description: |-
                  Inference profile identifier. Supports both system-defined inference profile ids, and inference profile ARNs.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              lastSyncTime:
                format: date-time
                type: string
              models:
                description: |-
                  List of model configuration
                  Read-only: reported by AWS after creation.
                items:
                  description: BedrockApplicationInferenceProfileInferenceProfileModel
                    is a nested property type of AWS::Bedrock::ApplicationInferenceProfile.
                  properties:
                    modelArn:
                      description: ARN for Foundation Models in Bedrock. These models
                        can be used as base models for model customization jobs
                      type: string
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - ACTIVE
                type: string
              type:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - APPLICATION
                - SYSTEM_DEFINED
                type: string
              updatedAt:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockautomatedreasoningpolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAutomatedReasoningPolicy
    listKind: BedrockAutomatedReasoningPolicyList
    plural: bedrockautomatedreasoningpolicies
    singular: bedrockautomatedreasoningpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAutomatedReasoningPolicy manages AWS::Bedrock::AutomatedReasoningPolicy through the AWS Cloud Control
          API. Definition of AWS::Bedrock::AutomatedReasoningPolicy Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAutomatedReasoningPolicySpec is the desired state
              of AWS::Bedrock::AutomatedReasoningPolicy.
            properties:
              description:
                maxLength: 1024
                type: string
              forceDelete:
                type: boolean
              kmsKeyId:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 2048
                type: string
              name:
                maxLength: 256
                minLength: 1
                type: string
              policyDefinition:
                description: BedrockAutomatedReasoningPolicyPolicyDefinition is a
                  nested property type of AWS::Bedrock::AutomatedReasoningPolicy.
                properties:
                  rules:
                    description: The rules definition block of an AutomatedReasoningPolicyDefinition.
                    items:
                      description: BedrockAutomatedReasoningPolicyPolicyDefinitionRule
                        is a nested property type of AWS::Bedrock::AutomatedReasoningPolicy.
                      properties:
                        alternateExpression:
                          description: An alternate expression for this rule
                          maxLength: 2048
                          type: string
                        expression:
                          description: The SMT expression for this rule
                          maxLength: 2048
                          type: string
                        id:
                          description: A unique id within the PolicyDefinition
                          maxLength: 12
                          minLength: 12
                          type: string
                      required:
                      - expression
                      - id
                      type: object
                    type: array
                  types:
                    description: The types definition block of an AutomatedReasoningPolicyDefinition.
                    items:
                      description: BedrockAutomatedReasoningPolicyPolicyDefinitionType
                        is a nested property type of AWS::Bedrock::AutomatedReasoningPolicy.
                      properties:
                        description:
                          description: A natural language description of this type.
                          maxLength: 1024
                          type: string
                        name:
                          description: A name for this type.
                          maxLength: 64
                          minLength: 1
                          type: string
                        values:
                          description: A list of valid values for this type.
                          items:
                            description: BedrockAutomatedReasoningPolicyPolicyDefinitionTypeValue
                              is a nested property type of AWS::Bedrock::AutomatedReasoningPolicy.
                            properties:
                              description:
                                description: A natural language description of the
                                  type's value.
                                maxLength: 1024
                                type: string
                              value:
                                description: The value of the type value.
                                maxLength: 64
                                minLength: 1
                                type: string
                            required:
                            - value
                            type: object
                          type: array
                      required:
                      - name
                      - values
                      type: object
                    type: array
                  variables:
                    description: The variables definition block of an AutomatedReasoningPolicyDefinition.
                    items:
                      description: BedrockAutomatedReasoningPolicyPolicyDefinitionVariable
                        is a nested property type of AWS::Bedrock::AutomatedReasoningPolicy.
                      properties:
                        description:
                          description: A natural language description of this variable.
                          maxLength: 1024
                          type: string
                        name:
                          description: A name from this variable.
                          maxLength: 64
                          minLength: 1
                          type: string
                        type:
                          description: A type for this variable.
                          maxLength: 64
                          minLength: 1
                          type: string
                      required:
                      - description
                      - name
                      - type
                      type: object
                    type: array
                  version:
                    description: The policy format version.
                    type: string
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            description: BedrockAutomatedReasoningPolicyStatus is the observed state
              of AWS::Bedrock::AutomatedReasoningPolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              definitionHash:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 128
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              kmsKeyArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              policyArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              policyId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              version:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockautomatedreasoningpolicyversions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAutomatedReasoningPolicyVersion
    listKind: BedrockAutomatedReasoningPolicyVersionList
    plural: bedrockautomatedreasoningpolicyversions
    singular: bedrockautomatedreasoningpolicyversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAutomatedReasoningPolicyVersion manages AWS::Bedrock::AutomatedReasoningPolicyVersion through the AWS
          Cloud Control API. Definition of AWS::Bedrock::AutomatedReasoningPolicyVersion Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAutomatedReasoningPolicyVersionSpec is the desired
              state of AWS::Bedrock::AutomatedReasoningPolicyVersion.
            properties:
              lastUpdatedDefinitionHash:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 128
                type: string
              policyArn:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 2048
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: policyArn is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - policyArn
            type: object
          status:
            description: BedrockAutomatedReasoningPolicyVersionStatus is the observed
              state of AWS::Bedrock::AutomatedReasoningPolicyVersion.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              definitionHash:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 128
                type: string
              description:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 1024
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              name:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              policyId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              version:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 12
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockblueprints.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockBlueprint
    listKind: BedrockBlueprintList
    plural: bedrockblueprints
    singular: bedrockblueprint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockBlueprint manages AWS::Bedrock::Blueprint through the AWS Cloud Control API. Definition of
          AWS::Bedrock::Blueprint Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockBlueprintSpec is the desired state of AWS::Bedrock::Blueprint.
            properties:
              blueprintName:
                description: |-
                  Name of the Blueprint
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: blueprintName is immutable
                  rule: self == oldSelf
              kmsEncryptionContext:
                additionalProperties:
                  type: string
                description: KMS encryption context
                type: object
              kmsKeyId:
                description: KMS key identifier
                maxLength: 2048
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              schema:
                description: Schema of the blueprint
                x-kubernetes-preserve-unknown-fields: true
              tags:
                description: List of Tags
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              type:
                description: |-
                  Modality Type
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - DOCUMENT
                - IMAGE
                - AUDIO
                - VIDEO
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - blueprintName
            - schema
            - type
            type: object
          status:
            description: BedrockBlueprintStatus is the observed state of AWS::Bedrock::Blueprint.
            properties:
              blueprintArn:
                description: |-
                  ARN of a Blueprint
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              blueprintStage:
                description: |-
                  Stage of the Blueprint
                  Read-only: reported by AWS after creation.
                enum:
                - DEVELOPMENT
                - LIVE
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  Creation timestamp
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  Last modified timestamp
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockdataautomationlibraries.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockDataAutomationLibrary
    listKind: BedrockDataAutomationLibraryList
    plural: bedrockdataautomationlibraries
    singular: bedrockdataautomationlibrary
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockDataAutomationLibrary manages AWS::Bedrock::DataAutomationLibrary through the AWS Cloud Control API.
          Resource Type definition for AWS::Bedrock::DataAutomationLibrary
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockDataAutomationLibrarySpec is the desired state of
              AWS::Bedrock::DataAutomationLibrary.
            properties:
              encryptionConfiguration:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsEncryptionContext:
                    additionalProperties:
                      type: string
                    type: object
                  kmsKeyId:
                    description: KMS Key Identifier
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - kmsKeyId
                type: object
              libraryDescription:
                description: Description of the DataAutomationLibrary
                maxLength: 300
                type: string
              libraryName:
                description: |-
                  Name of the DataAutomationLibrary
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: libraryName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: List of tags
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - libraryName
            type: object
          status:
            description: BedrockDataAutomationLibraryStatus is the observed state
              of AWS::Bedrock::DataAutomationLibrary.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              entityTypes:
                description: |-
                  List of info for each entity type in the DataAutomationLibrary
                  Read-only: reported by AWS after creation.
                items:
                  description: BedrockDataAutomationLibraryEntityTypeInfo is a nested
                    property type of AWS::Bedrock::DataAutomationLibrary.
                  properties:
                    entityMetadata:
                      description: JSON string representing relevant metadata for
                        the entity type
                      type: string
                    entityType:
                      enum:
                      - VOCABULARY
                      type: string
                  required:
                  - entityType
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              libraryArn:
                description: |-
                  ARN generated at the server side when a DataAutomationLibrary is created
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - ACTIVE
                - DELETING
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockdataautomationprojects.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockDataAutomationProject
    listKind: BedrockDataAutomationProjectList
    plural: bedrockdataautomationprojects
    singular: bedrockdataautomationproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockDataAutomationProject manages AWS::Bedrock::DataAutomationProject through the AWS Cloud Control API.
          Definition of AWS::Bedrock::DataAutomationProject Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockDataAutomationProjectSpec is the desired state of
              AWS::Bedrock::DataAutomationProject.
            properties:
              customOutputConfiguration:
                description: BedrockDataAutomationProjectCustomOutputConfiguration
                  is a nested property type of AWS::Bedrock::DataAutomationProject.
                properties:
                  blueprints:
                    items:
                      description: BedrockDataAutomationProjectBlueprintItem is a
                        nested property type of AWS::Bedrock::DataAutomationProject.
                      properties:
                        blueprintArn:
                          description: ARN of a Blueprint
                          maxLength: 128
                          type: string
                        blueprintStage:
                          enum:
                          - DEVELOPMENT
                          - LIVE
                          type: string
                        blueprintVersion:
                          description: Blueprint Version
                          maxLength: 128
                          type: string
                      required:
                      - blueprintArn
                      type: object
                    type: array
                type: object
              kmsEncryptionContext:
                additionalProperties:
                  type: string
                description: KMS encryption context
                type: object
              kmsKeyId:
                description: KMS key identifier
                maxLength: 2048
                type: string
              overrideConfiguration:
                description: BedrockDataAutomationProjectOverrideConfiguration is
                  a nested property type of AWS::Bedrock::DataAutomationProject.
                properties:
                  audio:
                    description: BedrockDataAutomationProjectAudioOverrideConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      languageConfiguration:
                        description: BedrockDataAutomationProjectAudioLanguageConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          generativeOutputLanguage:
                            enum:
                            - DEFAULT
                            - EN
                            type: string
                          identifyMultipleLanguages:
                            type: boolean
                          inputLanguages:
                            items:
                              type: string
                            type: array
                        type: object
                      modalityProcessing:
                        description: BedrockDataAutomationProjectModalityProcessingConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                        type: object
                      sensitiveDataConfiguration:
                        description: BedrockDataAutomationProjectSensitiveDataConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          detectionMode:
                            enum:
                            - DETECTION
                            - DETECTION_AND_REDACTION
                            type: string
                          detectionScope:
                            items:
                              type: string
                            type: array
                          piiEntitiesConfiguration:
                            description: BedrockDataAutomationProjectPIIEntitiesConfiguration
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              piiEntityTypes:
                                items:
                                  type: string
                                type: array
                              redactionMaskMode:
                                enum:
                                - PII
                                - ENTITY_TYPE
                                type: string
                            type: object
                        type: object
                    type: object
                  document:
                    description: BedrockDataAutomationProjectDocumentOverrideConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      modalityProcessing:
                        description: BedrockDataAutomationProjectModalityProcessingConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                        type: object
                      sensitiveDataConfiguration:
                        description: BedrockDataAutomationProjectSensitiveDataConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          detectionMode:
                            enum:
                            - DETECTION
                            - DETECTION_AND_REDACTION
                            type: string
                          detectionScope:
                            items:
                              type: string
                            type: array
                          piiEntitiesConfiguration:
                            description: BedrockDataAutomationProjectPIIEntitiesConfiguration
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              piiEntityTypes:
                                items:
                                  type: string
                                type: array
                              redactionMaskMode:
                                enum:
                                - PII
                                - ENTITY_TYPE
                                type: string
                            type: object
                        type: object
                      splitter:
                        description: BedrockDataAutomationProjectSplitterConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                        type: object
                    type: object
                  image:
                    description: BedrockDataAutomationProjectImageOverrideConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      modalityProcessing:
                        description: BedrockDataAutomationProjectModalityProcessingConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                        type: object
                      sensitiveDataConfiguration:
                        description: BedrockDataAutomationProjectSensitiveDataConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          detectionMode:
                            enum:
                            - DETECTION
                            - DETECTION_AND_REDACTION
                            type: string
                          detectionScope:
                            items:
                              type: string
                            type: array
                          piiEntitiesConfiguration:
                            description: BedrockDataAutomationProjectPIIEntitiesConfiguration
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              piiEntityTypes:
                                items:
                                  type: string
                                type: array
                              redactionMaskMode:
                                enum:
                                - PII
                                - ENTITY_TYPE
                                type: string
                            type: object
                        type: object
                    type: object
                  modalityRouting:
                    description: BedrockDataAutomationProjectModalityRoutingConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      jpeg:
                        enum:
                        - DOCUMENT
                        - IMAGE
                        - VIDEO
                        - AUDIO
                        type: string
                      mov:
                        enum:
                        - DOCUMENT
                        - IMAGE
                        - VIDEO
                        - AUDIO
                        type: string
                      mp4:
                        enum:
                        - DOCUMENT
                        - IMAGE
                        - VIDEO
                        - AUDIO
                        type: string
                      png:
                        enum:
                        - DOCUMENT
                        - IMAGE
                        - VIDEO
                        - AUDIO
                        type: string
                    type: object
                  video:
                    description: BedrockDataAutomationProjectVideoOverrideConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      modalityProcessing:
                        description: BedrockDataAutomationProjectModalityProcessingConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                        type: object
                      sensitiveDataConfiguration:
                        description: BedrockDataAutomationProjectSensitiveDataConfiguration
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          detectionMode:
                            enum:
                            - DETECTION
                            - DETECTION_AND_REDACTION
                            type: string
                          detectionScope:
                            items:
                              type: string
                            type: array
                          piiEntitiesConfiguration:
                            description: BedrockDataAutomationProjectPIIEntitiesConfiguration
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              piiEntityTypes:
                                items:
                                  type: string
                                type: array
                              redactionMaskMode:
                                enum:
                                - PII
                                - ENTITY_TYPE
                                type: string
                            type: object
                        type: object
                    type: object
                type: object
              projectDescription:
                description: Description of the DataAutomationProject
                type: string
              projectName:
                description: |-
                  Name of the DataAutomationProject
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: projectName is immutable
                  rule: self == oldSelf
              projectType:
                description: |-
                  Type of the DataAutomationProject - Sync or Async
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - ASYNC
                - SYNC
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              standardOutputConfiguration:
                description: BedrockDataAutomationProjectStandardOutputConfiguration
                  is a nested property type of AWS::Bedrock::DataAutomationProject.
                properties:
                  audio:
                    description: BedrockDataAutomationProjectAudioStandardOutputConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      extraction:
                        description: BedrockDataAutomationProjectAudioStandardExtraction
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          category:
                            description: BedrockDataAutomationProjectAudioExtractionCategory
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              typeConfiguration:
                                description: BedrockDataAutomationProjectAudioExtractionCategoryTypeConfiguration
                                  is a nested property type of AWS::Bedrock::DataAutomationProject.
                                properties:
                                  transcript:
                                    description: BedrockDataAutomationProjectTranscriptConfiguration
                                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                                    properties:
                                      channelLabeling:
                                        description: BedrockDataAutomationProjectChannelLabelingConfiguration
                                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                                        properties:
                                          state:
                                            enum:
                                            - ENABLED
                                            - DISABLED
                                            type: string
                                        required:
                                        - state
                                        type: object
                                      speakerLabeling:
                                        description: BedrockDataAutomationProjectSpeakerLabelingConfiguration
                                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                                        properties:
                                          state:
                                            enum:
                                            - ENABLED
                                            - DISABLED
                                            type: string
                                        required:
                                        - state
                                        type: object
                                    type: object
                                type: object
                              types:
                                items:
                                  type: string
                                type: array
                            required:
                            - state
                            type: object
                        required:
                        - category
                        type: object
                      generativeField:
                        description: BedrockDataAutomationProjectAudioStandardGenerativeField
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                          types:
                            items:
                              type: string
                            type: array
                        required:
                        - state
                        type: object
                    type: object
                  document:
                    description: BedrockDataAutomationProjectDocumentStandardOutputConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      extraction:
                        description: BedrockDataAutomationProjectDocumentStandardExtraction
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          boundingBox:
                            description: BedrockDataAutomationProjectDocumentBoundingBox
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - state
                            type: object
                          granularity:
                            description: BedrockDataAutomationProjectDocumentExtractionGranularity
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              types:
                                items:
                                  type: string
                                type: array
                            type: object
                        required:
                        - boundingBox
                        - granularity
                        type: object
                      generativeField:
                        description: BedrockDataAutomationProjectDocumentStandardGenerativeField
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                        required:
                        - state
                        type: object
                      outputFormat:
                        description: BedrockDataAutomationProjectDocumentOutputFormat
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          additionalFileFormat:
                            description: BedrockDataAutomationProjectDocumentOutputAdditionalFileFormat
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - state
                            type: object
                          textFormat:
                            description: BedrockDataAutomationProjectDocumentOutputTextFormat
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              types:
                                items:
                                  type: string
                                type: array
                            type: object
                        required:
                        - additionalFileFormat
                        - textFormat
                        type: object
                    type: object
                  image:
                    description: BedrockDataAutomationProjectImageStandardOutputConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      extraction:
                        description: BedrockDataAutomationProjectImageStandardExtraction
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          boundingBox:
                            description: BedrockDataAutomationProjectImageBoundingBox
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - state
                            type: object
                          category:
                            description: BedrockDataAutomationProjectImageExtractionCategory
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              types:
                                items:
                                  type: string
                                type: array
                            required:
                            - state
                            type: object
                        required:
                        - boundingBox
                        - category
                        type: object
                      generativeField:
                        description: BedrockDataAutomationProjectImageStandardGenerativeField
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                          types:
                            items:
                              type: string
                            type: array
                        required:
                        - state
                        type: object
                    type: object
                  video:
                    description: BedrockDataAutomationProjectVideoStandardOutputConfiguration
                      is a nested property type of AWS::Bedrock::DataAutomationProject.
                    properties:
                      extraction:
                        description: BedrockDataAutomationProjectVideoStandardExtraction
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          boundingBox:
                            description: BedrockDataAutomationProjectVideoBoundingBox
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - state
                            type: object
                          category:
                            description: BedrockDataAutomationProjectVideoExtractionCategory
                              is a nested property type of AWS::Bedrock::DataAutomationProject.
                            properties:
                              state:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              types:
                                items:
                                  type: string
                                type: array
                            required:
                            - state
                            type: object
                        required:
                        - boundingBox
                        - category
                        type: object
                      generativeField:
                        description: BedrockDataAutomationProjectVideoStandardGenerativeField
                          is a nested property type of AWS::Bedrock::DataAutomationProject.
                        properties:
                          state:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                          types:
                            items:
                              type: string
                            type: array
                        required:
                        - state
                        type: object
                    type: object
                type: object
              tags:
                description: List of Tags
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - projectName
            type: object
          status:
            description: BedrockDataAutomationProjectStatus is the observed state
              of AWS::Bedrock::DataAutomationProject.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              projectArn:
                description: |-
                  ARN of a DataAutomationProject
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              projectStage:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - DEVELOPMENT
                - LIVE
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - COMPLETED
                - IN_PROGRESS
                - FAILED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockdatasources.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockDataSource
    listKind: BedrockDataSourceList
    plural: bedrockdatasources
    singular: bedrockdatasource
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockDataSource manages AWS::Bedrock::DataSource through the AWS Cloud Control API. Definition of
          AWS::Bedrock::DataSource Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockDataSourceSpec is the desired state of AWS::Bedrock::DataSource.
            properties:
              dataDeletionPolicy:
                enum:
                - RETAIN
                - DELETE
                type: string
              dataSourceConfiguration:
                description: BedrockDataSourceDataSourceConfiguration is a nested
                  property type of AWS::Bedrock::DataSource.
                properties:
                  confluenceConfiguration:
                    description: BedrockDataSourceConfluenceDataSourceConfiguration
                      is a nested property type of AWS::Bedrock::DataSource.
                    properties:
                      crawlerConfiguration:
                        description: BedrockDataSourceConfluenceCrawlerConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          filterConfiguration:
                            description: BedrockDataSourceCrawlFilterConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              patternObjectFilter:
                                description: BedrockDataSourcePatternObjectFilterConfiguration
                                  is a nested property type of AWS::Bedrock::DataSource.
                                properties:
                                  filters:
                                    items:
                                      description: BedrockDataSourcePatternObjectFilter
                                        is a nested property type of AWS::Bedrock::DataSource.
                                      properties:
                                        exclusionFilters:
                                          items:
                                            type: string
                                          type: array
                                        inclusionFilters:
                                          items:
                                            type: string
                                          type: array
                                        objectType:
                                          description: The supported object type or
                                            content type of the data source.
                                          maxLength: 50
                                          minLength: 1
                                          type: string
                                      required:
                                      - objectType
                                      type: object
                                    type: array
                                required:
                                - filters
                                type: object
                              type:
                                description: The crawl filter type.
                                enum:
                                - PATTERN
                                type: string
                            required:
                            - type
                            type: object
                        type: object
                      sourceConfiguration:
                        description: BedrockDataSourceConfluenceSourceConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          authType:
                            description: The supported authentication type to authenticate
                              and connect to your Confluence instance.
                            enum:
                            - BASIC
                            - OAUTH2_CLIENT_CREDENTIALS
                            type: string
                          credentialsSecretArn:
                            description: |-
                              The Amazon Resource Name of an AWS Secrets Manager secret that stores your authentication credentials for your
                              Confluence instance URL. For more information on the key-value pairs that must be included in your secret,
                              depending on your authentication type, see Confluence connection configuration.
                            type: string
                          hostType:
                            description: The supported host type, whether online/cloud
                              or server/on-premises.
                            enum:
                            - SAAS
                            type: string
                          hostUrl:
                            description: The Confluence host URL or instance URL.
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - authType
                        - credentialsSecretArn
                        - hostType
                        - hostUrl
                        type: object
                    required:
                    - sourceConfiguration
                    type: object
                  managedKnowledgeBaseConnectorConfiguration:
                    description: BedrockDataSourceManagedKnowledgeBaseConnectorConfiguration
                      is a nested property type of AWS::Bedrock::DataSource.
                    properties:
                      connectorParameters:
                        description: Connector-specific parameters.
                        x-kubernetes-preserve-unknown-fields: true
                      deletionProtectionConfiguration:
                        description: BedrockDataSourceDeletionProtectionConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          deletionProtectionStatus:
                            enum:
                            - ENABLED
                            - DISABLED
                            type: string
                          deletionProtectionThreshold:
                            description: Threshold for deletion protection.
                            format: int64
                            maximum: 100
                            minimum: 0
                            type: integer
                        required:
                        - deletionProtectionStatus
                        type: object
                      mediaExtractionConfiguration:
                        description: BedrockDataSourceMediaExtractionConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          audioExtractionConfiguration:
                            description: BedrockDataSourceAudioExtractionConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              audioExtractionStatus:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - audioExtractionStatus
                            type: object
                          imageExtractionConfiguration:
                            description: BedrockDataSourceImageExtractionConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              imageExtractionStatus:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - imageExtractionStatus
                            type: object
                          videoExtractionConfiguration:
                            description: BedrockDataSourceVideoExtractionConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              videoExtractionStatus:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                            required:
                            - videoExtractionStatus
                            type: object
                        type: object
                    type: object
                  s3Configuration:
                    description: BedrockDataSourceS3DataSourceConfiguration is a nested
                      property type of AWS::Bedrock::DataSource.
                    properties:
                      bucketArn:
                        description: The ARN of the bucket that contains the data
                          source.
                        maxLength: 2048
                        minLength: 1
                        type: string
                      bucketOwnerAccountId:
                        description: The account ID for the owner of the S3 bucket.
                        maxLength: 12
                        type: string
                      inclusionPrefixes:
                        description: A list of S3 prefixes that define the object
                          containing the data sources.
                        items:
                          type: string
                        type: array
                    required:
                    - bucketArn
                    type: object
                  salesforceConfiguration:
                    description: BedrockDataSourceSalesforceDataSourceConfiguration
                      is a nested property type of AWS::Bedrock::DataSource.
                    properties:
                      crawlerConfiguration:
                        description: BedrockDataSourceSalesforceCrawlerConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          filterConfiguration:
                            description: BedrockDataSourceCrawlFilterConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              patternObjectFilter:
                                description: BedrockDataSourcePatternObjectFilterConfiguration
                                  is a nested property type of AWS::Bedrock::DataSource.
                                properties:
                                  filters:
                                    items:
                                      description: BedrockDataSourcePatternObjectFilter
                                        is a nested property type of AWS::Bedrock::DataSource.
                                      properties:
                                        exclusionFilters:
                                          items:
                                            type: string
                                          type: array
                                        inclusionFilters:
                                          items:
                                            type: string
                                          type: array
                                        objectType:
                                          description: The supported object type or
                                            content type of the data source.
                                          maxLength: 50
                                          minLength: 1
                                          type: string
                                      required:
                                      - objectType
                                      type: object
                                    type: array
                                required:
                                - filters
                                type: object
                              type:
                                description: The crawl filter type.
                                enum:
                                - PATTERN
                                type: string
                            required:
                            - type
                            type: object
                        type: object
                      sourceConfiguration:
                        description: BedrockDataSourceSalesforceSourceConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          authType:
                            description: The supported authentication type to authenticate
                              and connect to your Salesforce instance.
                            enum:
                            - OAUTH2_CLIENT_CREDENTIALS
                            type: string
                          credentialsSecretArn:
                            description: |-
                              The Amazon Resource Name of an AWS Secrets Manager secret that stores your authentication credentials for your
                              Salesforce instance URL. For more information on the key-value pairs that must be included in your secret,
                              depending on your authentication type, see Salesforce connection configuration.
                            type: string
                          hostUrl:
                            description: The Salesforce host URL or instance URL.
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - authType
                        - credentialsSecretArn
                        - hostUrl
                        type: object
                    required:
                    - sourceConfiguration
                    type: object
                  sharePointConfiguration:
                    description: BedrockDataSourceSharePointDataSourceConfiguration
                      is a nested property type of AWS::Bedrock::DataSource.
                    properties:
                      crawlerConfiguration:
                        description: BedrockDataSourceSharePointCrawlerConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          filterConfiguration:
                            description: BedrockDataSourceCrawlFilterConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              patternObjectFilter:
                                description: BedrockDataSourcePatternObjectFilterConfiguration
                                  is a nested property type of AWS::Bedrock::DataSource.
                                properties:
                                  filters:
                                    items:
                                      description: BedrockDataSourcePatternObjectFilter
                                        is a nested property type of AWS::Bedrock::DataSource.
                                      properties:
                                        exclusionFilters:
                                          items:
                                            type: string
                                          type: array
                                        inclusionFilters:
                                          items:
                                            type: string
                                          type: array
                                        objectType:
                                          description: The supported object type or
                                            content type of the data source.
                                          maxLength: 50
                                          minLength: 1
                                          type: string
                                      required:
                                      - objectType
                                      type: object
                                    type: array
                                required:
                                - filters
                                type: object
                              type:
                                description: The crawl filter type.
                                enum:
                                - PATTERN
                                type: string
                            required:
                            - type
                            type: object
                        type: object
                      sourceConfiguration:
                        description: BedrockDataSourceSharePointSourceConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          authType:
                            description: The supported authentication type to authenticate
                              and connect to your SharePoint site/sites.
                            enum:
                            - OAUTH2_CLIENT_CREDENTIALS
                            - OAUTH2_SHAREPOINT_APP_ONLY_CLIENT_CREDENTIALS
                            type: string
                          credentialsSecretArn:
                            description: |-
                              The Amazon Resource Name of an AWS Secrets Manager secret that stores your authentication credentials for your
                              SharePoint site/sites. For more information on the key-value pairs that must be included in your secret,
                              depending on your authentication type, see SharePoint connection configuration.
                            type: string
                          domain:
                            description: The domain of your SharePoint instance or
                              site URL/URLs.
                            maxLength: 50
                            minLength: 1
                            type: string
                          hostType:
                            description: The supported host type, whether online/cloud
                              or server/on-premises.
                            enum:
                            - ONLINE
                            type: string
                          siteUrls:
                            description: A list of one or more SharePoint site URLs.
                            items:
                              type: string
                            type: array
                          tenantId:
                            description: The identifier of your Microsoft 365 tenant.
                            type: string
                        required:
                        - authType
                        - credentialsSecretArn
                        - domain
                        - hostType
                        - siteUrls
                        type: object
                    required:
                    - sourceConfiguration
                    type: object
                  type:
                    enum:
                    - S3
                    - CONFLUENCE
                    - SALESFORCE
                    - SHAREPOINT
                    - WEB
                    - CUSTOM
                    - REDSHIFT_METADATA
                    - MANAGED_KNOWLEDGE_BASE_CONNECTOR
                    type: string
                  webConfiguration:
                    description: BedrockDataSourceWebDataSourceConfiguration is a
                      nested property type of AWS::Bedrock::DataSource.
                    properties:
                      crawlerConfiguration:
                        description: BedrockDataSourceWebCrawlerConfiguration is a
                          nested property type of AWS::Bedrock::DataSource.
                        properties:
                          crawlerLimits:
                            description: BedrockDataSourceWebCrawlerLimits is a nested
                              property type of AWS::Bedrock::DataSource.
                            properties:
                              maxPages:
                                description: Maximum number of pages the crawler can
                                  crawl.
                                format: int64
                                minimum: 1
                                type: integer
                              rateLimit:
                                description: Rate of web URLs retrieved per minute.
                                format: int64
                                maximum: 300
                                minimum: 1
                                type: integer
                            type: object
                          exclusionFilters:
                            items:
                              type: string
                            type: array
                          inclusionFilters:
                            items:
                              type: string
                            type: array
                          scope:
                            enum:
                            - HOST_ONLY
                            - SUBDOMAINS
                            type: string
                          userAgent:
                            description: The suffix that will be included in the user
                              agent header.
                            maxLength: 40
                            type: string
                          userAgentHeader:
                            description: The full user agent header, including UUID
                              and suffix.
                            maxLength: 86
                            type: string
                        type: object
                      sourceConfiguration:
                        description: BedrockDataSourceWebSourceConfiguration is a
                          nested property type of AWS::Bedrock::DataSource.
                        properties:
                          urlConfiguration:
                            description: BedrockDataSourceUrlConfiguration is a nested
                              property type of AWS::Bedrock::DataSource.
                            properties:
                              seedUrls:
                                items:
                                  description: BedrockDataSourceSeedUrl is a nested
                                    property type of AWS::Bedrock::DataSource.
                                  properties:
                                    url:
                                      description: A web url.
                                      type: string
                                  required:
                                  - url
                                  type: object
                                type: array
                            required:
                            - seedUrls
                            type: object
                        required:
                        - urlConfiguration
                        type: object
                    required:
                    - sourceConfiguration
                    type: object
                required:
                - type
                type: object
              description:
                description: Description of the Resource.
                maxLength: 200
                type: string
              knowledgeBaseId:
                description: |-
                  The unique identifier of the knowledge base to which to add the data source.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: knowledgeBaseId is immutable
                  rule: self == oldSelf
              name:
                description: The name of the data source.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              serverSideEncryptionConfiguration:
                description: BedrockDataSourceServerSideEncryptionConfiguration is
                  a nested property type of AWS::Bedrock::DataSource.
                properties:
                  kmsKeyArn:
                    description: The ARN of the AWS KMS key used to encrypt the resource.
                    maxLength: 2048
                    type: string
                type: object
              vectorIngestionConfiguration:
                description: BedrockDataSourceVectorIngestionConfiguration is a nested
                  property type of AWS::Bedrock::DataSource.
                properties:
                  chunkingConfiguration:
                    description: BedrockDataSourceChunkingConfiguration is a nested
                      property type of AWS::Bedrock::DataSource.
                    properties:
                      chunkingStrategy:
                        enum:
                        - FIXED_SIZE
                        - NONE
                        - HIERARCHICAL
                        - SEMANTIC
                        type: string
                      fixedSizeChunkingConfiguration:
                        description: BedrockDataSourceFixedSizeChunkingConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          maxTokens:
                            description: The maximum number of tokens to include in
                              a chunk.
                            format: int64
                            minimum: 1
                            type: integer
                          overlapPercentage:
                            description: The percentage of overlap between adjacent
                              chunks of a data source.
                            format: int64
                            maximum: 99
                            minimum: 1
                            type: integer
                        required:
                        - maxTokens
                        - overlapPercentage
                        type: object
                      hierarchicalChunkingConfiguration:
                        description: BedrockDataSourceHierarchicalChunkingConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          levelConfigurations:
                            description: Token settings for each layer.
                            items:
                              description: BedrockDataSourceHierarchicalChunkingLevelConfiguration
                                is a nested property type of AWS::Bedrock::DataSource.
                              properties:
                                maxTokens:
                                  description: The maximum number of tokens that a
                                    chunk can contain in this layer.
                                  format: int64
                                  maximum: 8192
                                  minimum: 1
                                  type: integer
                              required:
                              - maxTokens
                              type: object
                            type: array
                          overlapTokens:
                            description: The number of tokens to repeat across chunks
                              in the same layer.
                            format: int64
                            minimum: 1
                            type: integer
                        required:
                        - levelConfigurations
                        - overlapTokens
                        type: object
                      semanticChunkingConfiguration:
                        description: BedrockDataSourceSemanticChunkingConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          breakpointPercentileThreshold:
                            description: The dissimilarity threshold for splitting
                              chunks.
                            format: int64
                            maximum: 99
                            minimum: 50
                            type: integer
                          bufferSize:
                            description: The buffer size.
                            format: int64
                            maximum: 1
                            minimum: 0
                            type: integer
                          maxTokens:
                            description: The maximum number of tokens that a chunk
                              can contain.
                            format: int64
                            minimum: 1
                            type: integer
                        required:
                        - breakpointPercentileThreshold
                        - bufferSize
                        - maxTokens
                        type: object
                    required:
                    - chunkingStrategy
                    type: object
                  contextEnrichmentConfiguration:
                    description: BedrockDataSourceContextEnrichmentConfiguration is
                      a nested property type of AWS::Bedrock::DataSource.
                    properties:
                      bedrockFoundationModelConfiguration:
                        description: BedrockDataSourceBedrockFoundationModelContextEnrichmentConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          enrichmentStrategyConfiguration:
                            description: BedrockDataSourceEnrichmentStrategyConfiguration
                              is a nested property type of AWS::Bedrock::DataSource.
                            properties:
                              method:
                                enum:
                                - CHUNK_ENTITY_EXTRACTION
                                type: string
                            required:
                            - method
                            type: object
                          modelArn:
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - enrichmentStrategyConfiguration
                        - modelArn
                        type: object
                      type:
                        enum:
                        - BEDROCK_FOUNDATION_MODEL
                        type: string
                    required:
                    - type
                    type: object
                  customTransformationConfiguration:
                    description: BedrockDataSourceCustomTransformationConfiguration
                      is a nested property type of AWS::Bedrock::DataSource.
                    properties:
                      intermediateStorage:
                        description: BedrockDataSourceIntermediateStorage is a nested
                          property type of AWS::Bedrock::DataSource.
                        properties:
                          s3Location:
                            description: BedrockDataSourceS3Location is a nested property
                              type of AWS::Bedrock::DataSource.
                            properties:
                              uri:
                                description: The location's URI
                                maxLength: 2048
                                minLength: 1
                                type: string
                            required:
                            - uri
                            type: object
                        required:
                        - s3Location
                        type: object
                      transformations:
                        description: A list of Lambda functions that process documents.
                        items:
                          description: BedrockDataSourceTransformation is a nested
                            property type of AWS::Bedrock::DataSource.
                          properties:
                            stepToApply:
                              description: When the service applies the transformation.
                              enum:
                              - POST_CHUNKING
                              type: string
                            transformationFunction:
                              description: BedrockDataSourceTransformationFunction
                                is a nested property type of AWS::Bedrock::DataSource.
                              properties:
                                transformationLambdaConfiguration:
                                  description: BedrockDataSourceTransformationLambdaConfiguration
                                    is a nested property type of AWS::Bedrock::DataSource.
                                  properties:
                                    lambdaArn:
                                      description: The function's ARN identifier.
                                      maxLength: 2048
                                      type: string
                                  required:
                                  - lambdaArn
                                  type: object
                              required:
                              - transformationLambdaConfiguration
                              type: object
                          required:
                          - stepToApply
                          - transformationFunction
                          type: object
                        type: array
                    required:
                    - intermediateStorage
                    - transformations
                    type: object
                  parsingConfiguration:
                    description: BedrockDataSourceParsingConfiguration is a nested
                      property type of AWS::Bedrock::DataSource.
                    properties:
                      bedrockDataAutomationConfiguration:
                        description: BedrockDataSourceBedrockDataAutomationConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          parsingModality:
                            enum:
                            - MULTIMODAL
                            type: string
                        type: object
                      bedrockFoundationModelConfiguration:
                        description: BedrockDataSourceBedrockFoundationModelConfiguration
                          is a nested property type of AWS::Bedrock::DataSource.
                        properties:
                          modelArn:
                            maxLength: 2048
                            minLength: 1
                            type: string
                          parsingModality:
                            enum:
                            - MULTIMODAL
                            type: string
                          parsingPrompt:
                            description: BedrockDataSourceParsingPrompt is a nested
                              property type of AWS::Bedrock::DataSource.
                            properties:
                              parsingPromptText:
                                description: Instructions for interpreting the contents
                                  of a document.
                                maxLength: 10000
                                minLength: 1
                                type: string
                            required:
                            - parsingPromptText
                            type: object
                        required:
                        - modelArn
                        type: object
                      parsingStrategy:
                        enum:
                        - BEDROCK_FOUNDATION_MODEL
                        - BEDROCK_DATA_AUTOMATION
                        - SMART_PARSING
                        type: string
                    required:
                    - parsingStrategy
                    type: object
                type: object
            required:
            - dataSourceConfiguration
            - knowledgeBaseId
            - name
            type: object
          status:
            description: BedrockDataSourceStatus is the observed state of AWS::Bedrock::DataSource.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The time at which the data source was created.
                  Read-only: reported by AWS after creation.
                type: string
              dataSourceId:
                description: |-
                  Identifier for a resource.
                  Read-only: reported by AWS after creation.
                type: string
              dataSourceStatus:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - AVAILABLE
                - DELETING
                - DELETE_UNSUCCESSFUL
                - CREATING
                - UPDATING
                - FAILED
                type: string
              failureReasons:
                description: |-
                  The details of the failure reasons related to the data source.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  The time at which the knowledge base was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockenforcedguardrailconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockEnforcedGuardrailConfiguration
    listKind: BedrockEnforcedGuardrailConfigurationList
    plural: bedrockenforcedguardrailconfigurations
    singular: bedrockenforcedguardrailconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockEnforcedGuardrailConfiguration manages AWS::Bedrock::EnforcedGuardrailConfiguration through the AWS
          Cloud Control API. Definition of AWS::Bedrock::EnforcedGuardrailConfiguration Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockEnforcedGuardrailConfigurationSpec is the desired
              state of AWS::Bedrock::EnforcedGuardrailConfiguration.
            properties:
              guardrailIdentifier:
                description: Identifier for the guardrail, could be the ID or the
                  ARN
                maxLength: 2048
                type: string
              guardrailVersion:
                description: Numerical guardrail version (not DRAFT)
                type: string
              modelEnforcement:
                description: BedrockEnforcedGuardrailConfigurationModelEnforcement
                  is a nested property type of AWS::Bedrock::EnforcedGuardrailConfiguration.
                properties:
                  excludedModels:
                    description: Models to exclude from enforcement. If a model is
                      in both lists, it is excluded
                    items:
                      type: string
                    type: array
                  includedModels:
                    description: Models to enforce the guardrail on
                    items:
                      type: string
                    type: array
                required:
                - excludedModels
                - includedModels
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              selectiveContentGuarding:
                description: BedrockEnforcedGuardrailConfigurationSelectiveContentGuarding
                  is a nested property type of AWS::Bedrock::EnforcedGuardrailConfiguration.
                properties:
                  messages:
                    description: Selective guarding mode for user messages
                    enum:
                    - SELECTIVE
                    - COMPREHENSIVE
                    type: string
                  system:
                    description: Selective guarding mode for system prompts
                    enum:
                    - SELECTIVE
                    - COMPREHENSIVE
                    type: string
                type: object
            required:
            - guardrailIdentifier
            - guardrailVersion
            type: object
          status:
            description: BedrockEnforcedGuardrailConfigurationStatus is the observed
              state of AWS::Bedrock::EnforcedGuardrailConfiguration.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              configId:
                description: |-
                  Unique ID for the account enforced configuration
                  Read-only: reported by AWS after creation.
                type: string
              createdAt:
                description: |-
                  Timestamp when the configuration was created
                  Read-only: reported by AWS after creation.
                type: string
              createdBy:
                description: |-
                  The ARN of the role used to create the configuration
                  Read-only: reported by AWS after creation.
                type: string
              guardrailArn:
                description: |-
                  ARN representation for the guardrail
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              guardrailId:
                description: |-
                  Unique ID for the guardrail
                  Read-only: reported by AWS after creation.
                maxLength: 64
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              owner:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - ACCOUNT
                - ORGANIZATION
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  Timestamp when the configuration was last updated
                  Read-only: reported by AWS after creation.
                type: string
              updatedBy:
                description: |-
                  The ARN of the role used to update the configuration
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockflowaliases.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockFlowAlias
    listKind: BedrockFlowAliasList
    plural: bedrockflowaliases
    singular: bedrockflowalias
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockFlowAlias manages AWS::Bedrock::FlowAlias through the AWS Cloud Control API. Definition of
          AWS::Bedrock::FlowAlias Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockFlowAliasSpec is the desired state of AWS::Bedrock::FlowAlias.
            properties:
              concurrencyConfiguration:
                description: BedrockFlowAliasFlowAliasConcurrencyConfiguration is
                  a nested property type of AWS::Bedrock::FlowAlias.
                properties:
                  maxConcurrency:
                    description: Number of nodes executed concurrently at a time
                    type: number
                  type:
                    enum:
                    - Automatic
                    - Manual
                    type: string
                required:
                - type
                type: object
              description:
                description: Description of the Resource.
                maxLength: 200
                type: string
              flowArn:
                description: |-
                  Arn representation of the Flow
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
                x-kubernetes-validations:
                - message: flowArn is immutable
                  rule: self == oldSelf
              name:
                description: Name for a resource.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              routingConfiguration:
                description: Routing configuration for a Flow alias.
                items:
                  description: BedrockFlowAliasFlowAliasRoutingConfigurationListItem
                    is a nested property type of AWS::Bedrock::FlowAlias.
                  properties:
                    flowVersion:
                      description: Version.
                      maxLength: 5
                      type: string
                  type: object
                type: array
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - flowArn
            - name
            - routingConfiguration
            type: object
          status:
            description: BedrockFlowAliasStatus is the observed state of AWS::Bedrock::FlowAlias.
            properties:
              arn:
                description: |-
                  Arn of the Flow Alias
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              flowId:
                description: |-
                  Identifier for a flow resource.
                  Read-only: reported by AWS after creation.
                type: string
              id:
                description: |-
                  Id for a Flow Alias generated at the server side.
                  Read-only: reported by AWS after creation.
                maxLength: 10
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockflows.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockFlow
    listKind: BedrockFlowList
    plural: bedrockflows
    singular: bedrockflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockFlow manages AWS::Bedrock::Flow through the AWS Cloud Control API. Definition of AWS::Bedrock::Flow
          Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockFlowSpec is the desired state of AWS::Bedrock::Flow.
            properties:
              customerEncryptionKeyArn:
                description: A KMS key ARN
                maxLength: 2048
                type: string
              definition:
                description: BedrockFlowFlowDefinition is a nested property type of
                  AWS::Bedrock::Flow.
                properties:
                  connections:
                    description: List of connections
                    items:
                      description: BedrockFlowFlowConnection is a nested property
                        type of AWS::Bedrock::Flow.
                      properties:
                        configuration:
                          x-kubernetes-preserve-unknown-fields: true
                        name:
                          description: Name of a connection in a flow
                          type: string
                        source:
                          description: Name of a node in a flow
                          type: string
                        target:
                          description: Name of a node in a flow
                          type: string
                        type:
                          enum:
                          - Data
                          - Conditional
                          type: string
                      required:
                      - name
                      - source
                      - target
                      - type
                      type: object
                    type: array
                  nodes:
                    description: List of nodes in a flow
                    items:
                      description: BedrockFlowFlowNode is a nested property type of
                        AWS::Bedrock::Flow.
                      properties:
                        configuration:
                          x-kubernetes-preserve-unknown-fields: true
                        inputs:
                          description: List of node inputs in a flow
                          items:
                            description: BedrockFlowFlowNodeInput is a nested property
                              type of AWS::Bedrock::Flow.
                            properties:
                              category:
                                enum:
                                - LoopCondition
                                - ReturnValueToLoopStart
                                - ExitLoop
                                type: string
                              expression:
                                description: Expression for a node input in a flow
                                maxLength: 64
                                minLength: 1
                                type: string
                              name:
                                description: Name of a node input in a flow
                                type: string
                              type:
                                enum:
                                - String
                                - Number
                                - Boolean
                                - Object
                                - Array
                                type: string
                            required:
                            - expression
                            - name
                            - type
                            type: object
                          type: array
                        name:
                          description: Name of a node in a flow
                          type: string
                        outputs:
                          description: List of node outputs in a flow
                          items:
                            description: BedrockFlowFlowNodeOutput is a nested property
                              type of AWS::Bedrock::Flow.
                            properties:
                              name:
                                description: Name of a node output in a flow
                                type: string
                              type:
                                enum:
                                - String
                                - Number
                                - Boolean
                                - Object
                                - Array
                                type: string
                            required:
                            - name
                            - type
                            type: object
                          type: array
                        type:
                          enum:
                          - Input
                          - Output
                          - KnowledgeBase
                          - Condition
                          - Lex
                          - Prompt
                          - LambdaFunction
                          - Agent
                          - Storage
                          - Retrieval
                          - Iterator
                          - Collector
                          - InlineCode
                          - Loop
                          - LoopInput
                          - LoopController
                          type: string
                      required:
                      - name
                      - type
                      type: object
                    type: array
                type: object
              definitionS3Location:
                description: BedrockFlowS3Location is a nested property type of AWS::Bedrock::Flow.
                properties:
                  bucket:
                    description: A bucket in S3
                    maxLength: 63
                    minLength: 3
                    type: string
                  key:
                    description: A object key in S3
                    maxLength: 1024
                    minLength: 1
                    type: string
                  version:
                    description: The version of the the S3 object to use
                    maxLength: 1024
                    type: string
                required:
                - bucket
                - key
                type: object
              definitionString:
                description: A JSON string containing a Definition with the same schema
                  as the Definition property of this resource
                maxLength: 512000
                type: string
              definitionSubstitutions:
                additionalProperties:
                  x-kubernetes-preserve-unknown-fields: true
                type: object
                x-kubernetes-preserve-unknown-fields: true
              description:
                description: Description of the flow
                maxLength: 200
                type: string
              executionRoleArn:
                description: ARN of a IAM role
                maxLength: 2048
                type: string
              name:
                description: Name for the flow
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              testAliasTags:
                additionalProperties:
                  type: string
                type: object
            required:
            - executionRoleArn
            - name
            type: object
          status:
            description: BedrockFlowStatus is the observed state of AWS::Bedrock::Flow.
            properties:
              arn:
                description: |-
                  Arn representation of the Flow
                  Read-only: reported by AWS after creation.
                maxLength: 1011
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              id:
                description: |-
                  Identifier for a Flow
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - Failed
                - Prepared
                - Preparing
                - NotPrepared
                type: string
              updatedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              validations:
                description: 'Read-only: reported by AWS after creation.'
                items:
                  description: BedrockFlowFlowValidation is a nested property type
                    of AWS::Bedrock::Flow.
                  properties:
                    message:
                      description: validation message
                      type: string
                  required:
                  - message
                  type: object
                type: array
              version:
                description: |-
                  Draft Version.
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockflowversions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockFlowVersion
    listKind: BedrockFlowVersionList
    plural: bedrockflowversions
    singular: bedrockflowversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockFlowVersion manages AWS::Bedrock::FlowVersion through the AWS Cloud Control API. Definition of
          AWS::Bedrock::FlowVersion Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockFlowVersionSpec is the desired state of AWS::Bedrock::FlowVersion.
            properties:
              description:
                description: |-
                  Description of the flow version
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 200
                type: string
              flowArn:
                description: |-
                  Arn representation of the Flow
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: flowArn is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - flowArn
            type: object
          status:
            description: BedrockFlowVersionStatus is the observed state of AWS::Bedrock::FlowVersion.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              customerEncryptionKeyArn:
                description: |-
                  A KMS key ARN
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              definition:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  connections:
                    description: List of connections
                    items:
                      description: BedrockFlowVersionFlowConnection is a nested property
                        type of AWS::Bedrock::FlowVersion.
                      properties:
                        configuration:
                          x-kubernetes-preserve-unknown-fields: true
                        name:
                          description: Name of a connection in a flow
                          type: string
                        source:
                          description: Name of a node in a flow
                          type: string
                        target:
                          description: Name of a node in a flow
                          type: string
                        type:
                          enum:
                          - Data
                          - Conditional
                          type: string
                      required:
                      - name
                      - source
                      - target
                      - type
                      type: object
                    type: array
                  nodes:
                    description: List of nodes in a flow
                    items:
                      description: BedrockFlowVersionFlowNode is a nested property
                        type of AWS::Bedrock::FlowVersion.
                      properties:
                        configuration:
                          x-kubernetes-preserve-unknown-fields: true
                        inputs:
                          description: List of node inputs in a flow
                          items:
                            description: BedrockFlowVersionFlowNodeInput is a nested
                              property type of AWS::Bedrock::FlowVersion.
                            properties:
                              expression:
                                description: Expression for a node input in a flow
                                maxLength: 64
                                minLength: 1
                                type: string
                              name:
                                description: Name of a node input in a flow
                                type: string
                              type:
                                enum:
                                - String
                                - Number
                                - Boolean
                                - Object
                                - Array
                                type: string
                            required:
                            - expression
                            - name
                            - type
                            type: object
                          type: array
                        name:
                          description: Name of a node in a flow
                          type: string
                        outputs:
                          description: List of node outputs in a flow
                          items:
                            description: BedrockFlowVersionFlowNodeOutput is a nested
                              property type of AWS::Bedrock::FlowVersion.
                            properties:
                              name:
                                description: Name of a node output in a flow
                                type: string
                              type:
                                enum:
                                - String
                                - Number
                                - Boolean
                                - Object
                                - Array
                                type: string
                            required:
                            - name
                            - type
                            type: object
                          type: array
                        type:
                          enum:
                          - Input
                          - Output
                          - KnowledgeBase
                          - Condition
                          - Lex
                          - Prompt
                          - LambdaFunction
                          - Agent
                          - Iterator
                          - Collector
                          - Storage
                          - Retrieval
                          - InlineCode
                          - Loop
                          - LoopInput
                          - LoopController
                          type: string
                      required:
                      - name
                      - type
                      type: object
                    type: array
                type: object
              executionRoleArn:
                description: |-
                  ARN of a IAM role
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              flowId:
                description: |-
                  Identifier for a Flow
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              name:
                description: |-
                  Name for the flow
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - Failed
                - Prepared
                - Preparing
                - NotPrepared
                type: string
              version:
                description: |-
                  Numerical Version.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockguardrails.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockGuardrail
    listKind: BedrockGuardrailList
    plural: bedrockguardrails
    singular: bedrockguardrail
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockGuardrail manages AWS::Bedrock::Guardrail through the AWS Cloud Control API. Definition of
          AWS::Bedrock::Guardrail Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockGuardrailSpec is the desired state of AWS::Bedrock::Guardrail.
            properties:
              automatedReasoningPolicyConfig:
                description: BedrockGuardrailAutomatedReasoningPolicyConfig is a nested
                  property type of AWS::Bedrock::Guardrail.
                properties:
                  confidenceThreshold:
                    description: The confidence threshold for triggering guardrail
                      actions based on Automated Reasoning policy violations.
                    type: number
                  policies:
                    description: The list of Automated Reasoning policy ARNs to include
                      in the guardrail configuration
                    items:
                      type: string
                    type: array
                required:
                - policies
                type: object
              blockedInputMessaging:
                description: Messaging for when violations are detected in text
                maxLength: 500
                minLength: 1
                type: string
              blockedOutputsMessaging:
                description: Messaging for when violations are detected in text
                maxLength: 500
                minLength: 1
                type: string
              contentPolicyConfig:
                description: BedrockGuardrailContentPolicyConfig is a nested property
                  type of AWS::Bedrock::Guardrail.
                properties:
                  contentFiltersTierConfig:
                    description: Guardrail tier config for content policy
                    properties:
                      tierName:
                        enum:
                        - CLASSIC
                        - STANDARD
                        type: string
                    required:
                    - tierName
                    type: object
                  filtersConfig:
                    description: List of content filter configs in content policy.
                    items:
                      description: BedrockGuardrailContentFilterConfig is a nested
                        property type of AWS::Bedrock::Guardrail.
                      properties:
                        inputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        inputEnabled:
                          type: boolean
                        inputModalities:
                          description: List of modalities
                          items:
                            type: string
                          type: array
                        inputStrength:
                          enum:
                          - NONE
                          - LOW
                          - MEDIUM
                          - HIGH
                          type: string
                        outputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        outputEnabled:
                          type: boolean
                        outputModalities:
                          description: List of modalities
                          items:
                            type: string
                          type: array
                        outputStrength:
                          enum:
                          - NONE
                          - LOW
                          - MEDIUM
                          - HIGH
                          type: string
                        type:
                          enum:
                          - SEXUAL
                          - VIOLENCE
                          - HATE
                          - INSULTS
                          - MISCONDUCT
                          - PROMPT_ATTACK
                          type: string
                      required:
                      - inputStrength
                      - outputStrength
                      - type
                      type: object
                    type: array
                required:
                - filtersConfig
                type: object
              contextualGroundingPolicyConfig:
                description: BedrockGuardrailContextualGroundingPolicyConfig is a
                  nested property type of AWS::Bedrock::Guardrail.
                properties:
                  filtersConfig:
                    description: List of contextual grounding filter configs.
                    items:
                      description: BedrockGuardrailContextualGroundingFilterConfig
                        is a nested property type of AWS::Bedrock::Guardrail.
                      properties:
                        action:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        enabled:
                          type: boolean
                        threshold:
                          description: The threshold for this filter.
                          type: number
                        type:
                          enum:
                          - GROUNDING
                          - RELEVANCE
                          type: string
                      required:
                      - threshold
                      - type
                      type: object
                    type: array
                required:
                - filtersConfig
                type: object
              crossRegionConfig:
                description: BedrockGuardrailGuardrailCrossRegionConfig is a nested
                  property type of AWS::Bedrock::Guardrail.
                properties:
                  guardrailProfileArn:
                    description: The Amazon Resource Name (ARN) of the guardrail profile
                    maxLength: 2048
                    minLength: 15
                    type: string
                required:
                - guardrailProfileArn
                type: object
              description:
                description: Description of the guardrail or its version
                maxLength: 200
                type: string
              kmsKeyArn:
                description: The KMS key with which the guardrail was encrypted at
                  rest
                maxLength: 2048
                type: string
              name:
                description: Name of the guardrail
                maxLength: 50
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              sensitiveInformationPolicyConfig:
                description: BedrockGuardrailSensitiveInformationPolicyConfig is a
                  nested property type of AWS::Bedrock::Guardrail.
                properties:
                  piiEntitiesConfig:
                    description: List of entities.
                    items:
                      description: BedrockGuardrailPiiEntityConfig is a nested property
                        type of AWS::Bedrock::Guardrail.
                      properties:
                        action:
                          enum:
                          - BLOCK
                          - ANONYMIZE
                          - NONE
                          type: string
                        inputAction:
                          enum:
                          - BLOCK
                          - ANONYMIZE
                          - NONE
                          type: string
                        inputEnabled:
                          type: boolean
                        outputAction:
                          enum:
                          - BLOCK
                          - ANONYMIZE
                          - NONE
                          type: string
                        outputEnabled:
                          type: boolean
                        type:
                          enum:
                          - ADDRESS
                          - AGE
                          - AWS_ACCESS_KEY
                          - AWS_SECRET_KEY
                          - CA_HEALTH_NUMBER
                          - CA_SOCIAL_INSURANCE_NUMBER
                          - CREDIT_DEBIT_CARD_CVV
                          - CREDIT_DEBIT_CARD_EXPIRY
                          - CREDIT_DEBIT_CARD_NUMBER
                          - DRIVER_ID
                          - EMAIL
                          - INTERNATIONAL_BANK_ACCOUNT_NUMBER
                          - IP_ADDRESS
                          - LICENSE_PLATE
                          - MAC_ADDRESS
                          - NAME
                          - PASSWORD
                          - PHONE
                          - PIN
                          - SWIFT_CODE
                          - UK_NATIONAL_HEALTH_SERVICE_NUMBER
                          - UK_NATIONAL_INSURANCE_NUMBER
                          - UK_UNIQUE_TAXPAYER_REFERENCE_NUMBER
                          - URL
                          - USERNAME
                          - US_BANK_ACCOUNT_NUMBER
                          - US_BANK_ROUTING_NUMBER
                          - US_INDIVIDUAL_TAX_IDENTIFICATION_NUMBER
                          - US_PASSPORT_NUMBER
                          - US_SOCIAL_SECURITY_NUMBER
                          - VEHICLE_IDENTIFICATION_NUMBER
                          type: string
                      required:
                      - action
                      - type
                      type: object
                    type: array
                  regexesConfig:
                    description: List of regex.
                    items:
                      description: BedrockGuardrailRegexConfig is a nested property
                        type of AWS::Bedrock::Guardrail.
                      properties:
                        action:
                          enum:
                          - BLOCK
                          - ANONYMIZE
                          - NONE
                          type: string
                        description:
                          description: The regex description.
                          maxLength: 1000
                          type: string
                        inputAction:
                          enum:
                          - BLOCK
                          - ANONYMIZE
                          - NONE
                          type: string
                        inputEnabled:
                          type: boolean
                        name:
                          description: The regex name.
                          maxLength: 100
                          minLength: 1
                          type: string
                        outputAction:
                          enum:
                          - BLOCK
                          - ANONYMIZE
                          - NONE
                          type: string
                        outputEnabled:
                          type: boolean
                        pattern:
                          description: The regex pattern.
                          minLength: 1
                          type: string
                      required:
                      - action
                      - name
                      - pattern
                      type: object
                    type: array
                type: object
              tags:
                description: List of Tags
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              topicPolicyConfig:
                description: BedrockGuardrailTopicPolicyConfig is a nested property
                  type of AWS::Bedrock::Guardrail.
                properties:
                  topicsConfig:
                    description: List of topic configs in topic policy.
                    items:
                      description: BedrockGuardrailTopicConfig is a nested property
                        type of AWS::Bedrock::Guardrail.
                      properties:
                        definition:
                          description: Definition of topic in topic policy
                          maxLength: 1000
                          minLength: 1
                          type: string
                        examples:
                          description: List of text examples
                          items:
                            type: string
                          type: array
                        inputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        inputEnabled:
                          type: boolean
                        name:
                          description: Name of topic in topic policy
                          maxLength: 100
                          minLength: 1
                          type: string
                        outputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        outputEnabled:
                          type: boolean
                        type:
                          enum:
                          - DENY
                          type: string
                      required:
                      - definition
                      - name
                      - type
                      type: object
                    type: array
                  topicsTierConfig:
                    description: Guardrail tier config for topic policy
                    properties:
                      tierName:
                        enum:
                        - CLASSIC
                        - STANDARD
                        type: string
                    required:
                    - tierName
                    type: object
                required:
                - topicsConfig
                type: object
              wordPolicyConfig:
                description: BedrockGuardrailWordPolicyConfig is a nested property
                  type of AWS::Bedrock::Guardrail.
                properties:
                  managedWordListsConfig:
                    description: A config for the list of managed words.
                    items:
                      description: BedrockGuardrailManagedWordsConfig is a nested
                        property type of AWS::Bedrock::Guardrail.
                      properties:
                        inputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        inputEnabled:
                          type: boolean
                        outputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        outputEnabled:
                          type: boolean
                        type:
                          enum:
                          - PROFANITY
                          type: string
                      required:
                      - type
                      type: object
                    type: array
                  wordsConfig:
                    description: List of custom word configs.
                    items:
                      description: BedrockGuardrailWordConfig is a nested property
                        type of AWS::Bedrock::Guardrail.
                      properties:
                        inputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        inputEnabled:
                          type: boolean
                        outputAction:
                          enum:
                          - BLOCK
                          - NONE
                          type: string
                        outputEnabled:
                          type: boolean
                        text:
                          description: The custom word text.
                          minLength: 1
                          type: string
                      required:
                      - text
                      type: object
                    type: array
                type: object
            required:
            - blockedInputMessaging
            - blockedOutputsMessaging
            - name
            type: object
          status:
            description: BedrockGuardrailStatus is the observed state of AWS::Bedrock::Guardrail.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              failureRecommendations:
                description: |-
                  List of failure recommendations
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              guardrailArn:
                description: |-
                  Arn representation for the guardrail
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              guardrailId:
                description: |-
                  Unique id for the guardrail
                  Read-only: reported by AWS after creation.
                maxLength: 64
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - UPDATING
                - VERSIONING
                - READY
                - FAILED
                - DELETING
                type: string
              statusReasons:
                description: |-
                  List of status reasons
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              updatedAt:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              version:
                description: |-
                  Guardrail version
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockguardrailversions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockGuardrailVersion
    listKind: BedrockGuardrailVersionList
    plural: bedrockguardrailversions
    singular: bedrockguardrailversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockGuardrailVersion manages AWS::Bedrock::GuardrailVersion through the AWS Cloud Control API. Definition
          of AWS::Bedrock::GuardrailVersion Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockGuardrailVersionSpec is the desired state of AWS::Bedrock::GuardrailVersion.
            properties:
              description:
                description: |-
                  Description of the Guardrail version
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 200
                type: string
              guardrailIdentifier:
                description: |-
                  Identifier (GuardrailId or GuardrailArn) for the guardrail
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
                x-kubernetes-validations:
                - message: guardrailIdentifier is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - guardrailIdentifier
            type: object
          status:
            description: BedrockGuardrailVersionStatus is the observed state of AWS::Bedrock::GuardrailVersion.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              guardrailArn:
                description: |-
                  Arn representation for the guardrail
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              guardrailId:
                description: |-
                  Unique id for the guardrail
                  Read-only: reported by AWS after creation.
                maxLength: 64
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              version:
                description: |-
                  Guardrail version
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockintelligentpromptrouters.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockIntelligentPromptRouter
    listKind: BedrockIntelligentPromptRouterList
    plural: bedrockintelligentpromptrouters
    singular: bedrockintelligentpromptrouter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockIntelligentPromptRouter manages AWS::Bedrock::IntelligentPromptRouter through the AWS Cloud Control
          API. Definition of AWS::Bedrock::IntelligentPromptRouter Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockIntelligentPromptRouterSpec is the desired state of
              AWS::Bedrock::IntelligentPromptRouter.
            properties:
              description:
                description: |-
                  Description of the Prompt Router.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 200
                type: string
              fallbackModel:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  modelArn:
                    description: Arn of underlying model which are added in the Prompt
                      Router.
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - modelArn
                type: object
              models:
                description: |-
                  List of model configuration
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: BedrockIntelligentPromptRouterPromptRouterTargetModel
                    is a nested property type of AWS::Bedrock::IntelligentPromptRouter.
                  properties:
                    modelArn:
                      description: Arn of underlying model which are added in the
                        Prompt Router.
                      maxLength: 2048
                      minLength: 1
                      type: string
                  required:
                  - modelArn
                  type: object
                type: array
              promptRouterName:
                description: |-
                  Name of the Prompt Router.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: promptRouterName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              routingCriteria:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  responseQualityDifference:
                    type: number
                required:
                - responseQualityDifference
                type: object
              tags:
                description: List of Tags
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - fallbackModel
            - models
            - promptRouterName
            - routingCriteria
            type: object
          status:
            description: BedrockIntelligentPromptRouterStatus is the observed state
              of AWS::Bedrock::IntelligentPromptRouter.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              promptRouterArn:
                description: |-
                  Arn of the Prompt Router.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - AVAILABLE
                type: string
              type:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - custom
                - default
                type: string
              updatedAt:
                description: |-
                  Time Stamp
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockknowledgebasepolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockKnowledgeBasePolicy
    listKind: BedrockKnowledgeBasePolicyList
    plural: bedrockknowledgebasepolicies
    singular: bedrockknowledgebasepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockKnowledgeBasePolicy manages AWS::Bedrock::KnowledgeBasePolicy through the AWS Cloud Control API.
          Definition of AWS::Bedrock::KnowledgeBasePolicy Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockKnowledgeBasePolicySpec is the desired state of AWS::Bedrock::KnowledgeBasePolicy.
            properties:
              knowledgeBaseId:
                description: |-
                  The unique identifier of the knowledge base
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 10
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: knowledgeBaseId is immutable
                  rule: self == oldSelf
              policyDocument:
                description: The IAM policy document defining access permissions for
                  the knowledge base
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - knowledgeBaseId
            - policyDocument
            type: object
          status:
            description: BedrockKnowledgeBasePolicyStatus is the observed state of
              AWS::Bedrock::KnowledgeBasePolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              revisionId:
                description: |-
                  The revision identifier for the policy, used for optimistic concurrency control
                  Read-only: reported by AWS after creation.
                maxLength: 255
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockknowledgebases.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockKnowledgeBase
    listKind: BedrockKnowledgeBaseList
    plural: bedrockknowledgebases
    singular: bedrockknowledgebase
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockKnowledgeBase manages AWS::Bedrock::KnowledgeBase through the AWS Cloud Control API. Definition of
          AWS::Bedrock::KnowledgeBase Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockKnowledgeBaseSpec is the desired state of AWS::Bedrock::KnowledgeBase.
            properties:
              description:
                description: Description of the Resource.
                maxLength: 200
                type: string
              knowledgeBaseConfiguration:
                description: BedrockKnowledgeBaseKnowledgeBaseConfiguration is a nested
                  property type of AWS::Bedrock::KnowledgeBase.
                properties:
                  kendraKnowledgeBaseConfiguration:
                    description: BedrockKnowledgeBaseKendraKnowledgeBaseConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      kendraIndexArn:
                        type: string
                    required:
                    - kendraIndexArn
                    type: object
                  managedKnowledgeBaseConfiguration:
                    description: BedrockKnowledgeBaseManagedKnowledgeBaseConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      embeddingModelArn:
                        description: The ARN of the model used to create vector embeddings
                          for the knowledge base.
                        maxLength: 2048
                        type: string
                      embeddingModelConfiguration:
                        description: BedrockKnowledgeBaseEmbeddingModelConfiguration
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          bedrockEmbeddingModelConfiguration:
                            description: BedrockKnowledgeBaseBedrockEmbeddingModelConfiguration
                              is a nested property type of AWS::Bedrock::KnowledgeBase.
                            properties:
                              audio:
                                items:
                                  description: BedrockKnowledgeBaseAudioConfiguration
                                    is a nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    segmentationConfiguration:
                                      description: BedrockKnowledgeBaseAudioSegmentationConfiguration
                                        is a nested property type of AWS::Bedrock::KnowledgeBase.
                                      properties:
                                        fixedLengthDuration:
                                          format: int64
                                          maximum: 30
                                          minimum: 1
                                          type: integer
                                      required:
                                      - fixedLengthDuration
                                      type: object
                                  required:
                                  - segmentationConfiguration
                                  type: object
                                type: array
                              dimensions:
                                description: The dimensions details for the vector
                                  configuration used on the Bedrock embeddings model.
                                format: int64
                                maximum: 4096
                                minimum: 0
                                type: integer
                              embeddingDataType:
                                description: The data type for the vectors when using
                                  a model to convert text into vector embeddings.
                                enum:
                                - FLOAT32
                                - BINARY
                                type: string
                              video:
                                items:
                                  description: BedrockKnowledgeBaseVideoConfiguration
                                    is a nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    segmentationConfiguration:
                                      description: BedrockKnowledgeBaseVideoSegmentationConfiguration
                                        is a nested property type of AWS::Bedrock::KnowledgeBase.
                                      properties:
                                        fixedLengthDuration:
                                          format: int64
                                          maximum: 30
                                          minimum: 1
                                          type: integer
                                      required:
                                      - fixedLengthDuration
                                      type: object
                                  required:
                                  - segmentationConfiguration
                                  type: object
                                type: array
                            type: object
                        type: object
                      embeddingModelType:
                        enum:
                        - CUSTOM
                        - MANAGED
                        type: string
                      serverSideEncryptionConfiguration:
                        description: BedrockKnowledgeBaseManagedKnowledgeBaseServerSideEncryptionConfiguration
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          kmsKeyArn:
                            description: The ARN of the AWS KMS key used to encrypt
                              the managed knowledge base.
                            maxLength: 2048
                            type: string
                        type: object
                    type: object
                  sqlKnowledgeBaseConfiguration:
                    description: BedrockKnowledgeBaseSqlKnowledgeBaseConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      redshiftConfiguration:
                        description: BedrockKnowledgeBaseRedshiftConfiguration is
                          a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          queryEngineConfiguration:
                            description: BedrockKnowledgeBaseRedshiftQueryEngineConfiguration
                              is a nested property type of AWS::Bedrock::KnowledgeBase.
                            properties:
                              provisionedConfiguration:
                                description: BedrockKnowledgeBaseRedshiftProvisionedConfiguration
                                  is a nested property type of AWS::Bedrock::KnowledgeBase.
                                properties:
                                  authConfiguration:
                                    description: BedrockKnowledgeBaseRedshiftProvisionedAuthConfiguration
                                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                                    properties:
                                      databaseUser:
                                        description: Redshift database user
                                        type: string
                                      type:
                                        enum:
                                        - IAM
                                        - USERNAME_PASSWORD
                                        - USERNAME
                                        type: string
                                      usernamePasswordSecretArn:
                                        type: string
                                    required:
                                    - type
                                    type: object
                                  clusterIdentifier:
                                    maxLength: 63
                                    minLength: 1
                                    type: string
                                required:
                                - authConfiguration
                                - clusterIdentifier
                                type: object
                              serverlessConfiguration:
                                description: BedrockKnowledgeBaseRedshiftServerlessConfiguration
                                  is a nested property type of AWS::Bedrock::KnowledgeBase.
                                properties:
                                  authConfiguration:
                                    description: BedrockKnowledgeBaseRedshiftServerlessAuthConfiguration
                                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                                    properties:
                                      type:
                                        enum:
                                        - IAM
                                        - USERNAME_PASSWORD
                                        type: string
                                      usernamePasswordSecretArn:
                                        type: string
                                    required:
                                    - type
                                    type: object
                                  workgroupArn:
                                    type: string
                                required:
                                - authConfiguration
                                - workgroupArn
                                type: object
                              type:
                                enum:
                                - SERVERLESS
                                - PROVISIONED
                                type: string
                            required:
                            - type
                            type: object
                          queryGenerationConfiguration:
                            description: BedrockKnowledgeBaseQueryGenerationConfiguration
                              is a nested property type of AWS::Bedrock::KnowledgeBase.
                            properties:
                              executionTimeoutSeconds:
                                format: int64
                                maximum: 200
                                minimum: 1
                                type: integer
                              generationContext:
                                description: BedrockKnowledgeBaseQueryGenerationContext
                                  is a nested property type of AWS::Bedrock::KnowledgeBase.
                                properties:
                                  curatedQueries:
                                    items:
                                      description: BedrockKnowledgeBaseCuratedQuery
                                        is a nested property type of AWS::Bedrock::KnowledgeBase.
                                      properties:
                                        naturalLanguage:
                                          maxLength: 1000
                                          minLength: 1
                                          type: string
                                        sql:
                                          maxLength: 1000
                                          minLength: 1
                                          type: string
                                      required:
                                      - naturalLanguage
                                      - sql
                                      type: object
                                    type: array
                                  tables:
                                    items:
                                      description: BedrockKnowledgeBaseQueryGenerationTable
                                        is a nested property type of AWS::Bedrock::KnowledgeBase.
                                      properties:
                                        columns:
                                          items:
                                            description: BedrockKnowledgeBaseQueryGenerationColumn
                                              is a nested property type of AWS::Bedrock::KnowledgeBase.
                                            properties:
                                              description:
                                                maxLength: 200
                                                type: string
                                              inclusion:
                                                enum:
                                                - INCLUDE
                                                - EXCLUDE
                                                type: string
                                              name:
                                                maxLength: 127
                                                type: string
                                            type: object
                                          type: array
                                        description:
                                          maxLength: 200
                                          type: string
                                        inclusion:
                                          enum:
                                          - INCLUDE
                                          - EXCLUDE
                                          type: string
                                        name:
                                          type: string
                                      required:
                                      - name
                                      type: object
                                    type: array
                                type: object
                            type: object
                          storageConfigurations:
                            items:
                              description: BedrockKnowledgeBaseRedshiftQueryEngineStorageConfiguration
                                is a nested property type of AWS::Bedrock::KnowledgeBase.
                              properties:
                                awsDataCatalogConfiguration:
                                  description: BedrockKnowledgeBaseRedshiftQueryEngineAwsDataCatalogStorageConfiguration
                                    is a nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    tableNames:
                                      items:
                                        type: string
                                      type: array
                                  required:
                                  - tableNames
                                  type: object
                                redshiftConfiguration:
                                  description: BedrockKnowledgeBaseRedshiftQueryEngineRedshiftStorageConfiguration
                                    is a nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    databaseName:
                                      maxLength: 200
                                      minLength: 1
                                      type: string
                                  required:
                                  - databaseName
                                  type: object
                                type:
                                  enum:
                                  - REDSHIFT
                                  - AWS_DATA_CATALOG
                                  type: string
                              required:
                              - type
                              type: object
                            type: array
                        required:
                        - queryEngineConfiguration
                        - storageConfigurations
                        type: object
                      type:
                        enum:
                        - REDSHIFT
                        type: string
                    required:
                    - type
                    type: object
                  type:
                    enum:
                    - VECTOR
                    - KENDRA
                    - SQL
                    - MANAGED
                    type: string
                  vectorKnowledgeBaseConfiguration:
                    description: BedrockKnowledgeBaseVectorKnowledgeBaseConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      embeddingModelArn:
                        description: The ARN of the model used to create vector embeddings
                          for the knowledge base.
                        maxLength: 2048
                        minLength: 20
                        type: string
                      embeddingModelConfiguration:
                        description: BedrockKnowledgeBaseEmbeddingModelConfiguration
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          bedrockEmbeddingModelConfiguration:
                            description: BedrockKnowledgeBaseBedrockEmbeddingModelConfiguration
                              is a nested property type of AWS::Bedrock::KnowledgeBase.
                            properties:
                              audio:
                                items:
                                  description: BedrockKnowledgeBaseAudioConfiguration
                                    is a nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    segmentationConfiguration:
                                      description: BedrockKnowledgeBaseAudioSegmentationConfiguration
                                        is a nested property type of AWS::Bedrock::KnowledgeBase.
                                      properties:
                                        fixedLengthDuration:
                                          format: int64
                                          maximum: 30
                                          minimum: 1
                                          type: integer
                                      required:
                                      - fixedLengthDuration
                                      type: object
                                  required:
                                  - segmentationConfiguration
                                  type: object
                                type: array
                              dimensions:
                                description: The dimensions details for the vector
                                  configuration used on the Bedrock embeddings model.
                                format: int64
                                maximum: 4096
                                minimum: 0
                                type: integer
                              embeddingDataType:
                                description: The data type for the vectors when using
                                  a model to convert text into vector embeddings.
                                enum:
                                - FLOAT32
                                - BINARY
                                type: string
                              video:
                                items:
                                  description: BedrockKnowledgeBaseVideoConfiguration
                                    is a nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    segmentationConfiguration:
                                      description: BedrockKnowledgeBaseVideoSegmentationConfiguration
                                        is a nested property type of AWS::Bedrock::KnowledgeBase.
                                      properties:
                                        fixedLengthDuration:
                                          format: int64
                                          maximum: 30
                                          minimum: 1
                                          type: integer
                                      required:
                                      - fixedLengthDuration
                                      type: object
                                  required:
                                  - segmentationConfiguration
                                  type: object
                                type: array
                            type: object
                        type: object
                      supplementalDataStorageConfiguration:
                        description: BedrockKnowledgeBaseSupplementalDataStorageConfiguration
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          supplementalDataStorageLocations:
                            items:
                              description: BedrockKnowledgeBaseSupplementalDataStorageLocation
                                is a nested property type of AWS::Bedrock::KnowledgeBase.
                              properties:
                                s3Location:
                                  description: BedrockKnowledgeBaseS3Location is a
                                    nested property type of AWS::Bedrock::KnowledgeBase.
                                  properties:
                                    uri:
                                      description: The location's URI
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - uri
                                  type: object
                                supplementalDataStorageLocationType:
                                  enum:
                                  - S3
                                  type: string
                              required:
                              - supplementalDataStorageLocationType
                              type: object
                            type: array
                        required:
                        - supplementalDataStorageLocations
                        type: object
                    required:
                    - embeddingModelArn
                    type: object
                required:
                - type
                type: object
              name:
                description: The name of the knowledge base.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The ARN of the IAM role with permissions to invoke API operations on the knowledge base. The ARN must begin
                  with AmazonBedrockExecutionRoleForKnowledgeBase_
                maxLength: 2048
                type: string
              storageConfiguration:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  mongoDbAtlasConfiguration:
                    description: BedrockKnowledgeBaseMongoDbAtlasConfiguration is
                      a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      collectionName:
                        description: Name of the collection within MongoDB Atlas.
                        maxLength: 63
                        type: string
                      credentialsSecretArn:
                        description: The ARN of the secret that you created in AWS
                          Secrets Manager that is linked to your Amazon Mongo database.
                        type: string
                      databaseName:
                        description: Name of the database within MongoDB Atlas.
                        maxLength: 63
                        type: string
                      endpoint:
                        description: MongoDB Atlas endpoint.
                        maxLength: 2048
                        type: string
                      endpointServiceName:
                        description: MongoDB Atlas endpoint service name.
                        maxLength: 255
                        type: string
                      fieldMapping:
                        description: BedrockKnowledgeBaseMongoDbAtlasFieldMapping
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          metadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores metadata about the vector store.
                            maxLength: 2048
                            type: string
                          textField:
                            description: |-
                              The name of the field in which Amazon Bedrock stores the raw text from your data. The text is split according
                              to the chunking strategy you choose.
                            maxLength: 2048
                            type: string
                          vectorField:
                            description: The name of the field in which Amazon Bedrock
                              stores the vector embeddings for your data sources.
                            maxLength: 2048
                            type: string
                        required:
                        - metadataField
                        - textField
                        - vectorField
                        type: object
                      textIndexName:
                        description: Name of a MongoDB Atlas text index.
                        maxLength: 2048
                        type: string
                      vectorIndexName:
                        description: Name of a MongoDB Atlas index.
                        maxLength: 2048
                        type: string
                    required:
                    - collectionName
                    - credentialsSecretArn
                    - databaseName
                    - endpoint
                    - fieldMapping
                    - vectorIndexName
                    type: object
                  neptuneAnalyticsConfiguration:
                    description: BedrockKnowledgeBaseNeptuneAnalyticsConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      fieldMapping:
                        description: BedrockKnowledgeBaseNeptuneAnalyticsFieldMapping
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          metadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores metadata about the vector store.
                            maxLength: 2048
                            type: string
                          textField:
                            description: |-
                              The name of the field in which Amazon Bedrock stores the raw text from your data. The text is split according
                              to the chunking strategy you choose.
                            maxLength: 2048
                            type: string
                        required:
                        - metadataField
                        - textField
                        type: object
                      graphArn:
                        description: ARN for Neptune Analytics graph database.
                        maxLength: 255
                        minLength: 1
                        type: string
                    required:
                    - fieldMapping
                    - graphArn
                    type: object
                  opensearchManagedClusterConfiguration:
                    description: BedrockKnowledgeBaseOpenSearchManagedClusterConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      domainArn:
                        description: The Amazon Resource Name (ARN) of the OpenSearch
                          domain.
                        maxLength: 2048
                        type: string
                      domainEndpoint:
                        description: The endpoint URL the OpenSearch domain.
                        maxLength: 2048
                        type: string
                      fieldMapping:
                        description: BedrockKnowledgeBaseOpenSearchManagedClusterFieldMapping
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          metadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores metadata about the vector store.
                            maxLength: 2048
                            type: string
                          textField:
                            description: |-
                              The name of the field in which Amazon Bedrock stores the raw text from your data. The text is split according
                              to the chunking strategy you choose.
                            maxLength: 2048
                            type: string
                          vectorField:
                            description: The name of the field in which Amazon Bedrock
                              stores the vector embeddings for your data sources.
                            maxLength: 2048
                            type: string
                        required:
                        - metadataField
                        - textField
                        - vectorField
                        type: object
                      vectorIndexName:
                        description: The name of the vector store.
                        maxLength: 2048
                        minLength: 1
                        type: string
                    required:
                    - domainArn
                    - domainEndpoint
                    - fieldMapping
                    - vectorIndexName
                    type: object
                  opensearchServerlessConfiguration:
                    description: BedrockKnowledgeBaseOpenSearchServerlessConfiguration
                      is a nested property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      collectionArn:
                        description: The ARN of the OpenSearch Service vector store.
                        maxLength: 2048
                        type: string
                      fieldMapping:
                        description: BedrockKnowledgeBaseOpenSearchServerlessFieldMapping
                          is a nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          metadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores metadata about the vector store.
                            maxLength: 2048
                            type: string
                          textField:
                            description: |-
                              The name of the field in which Amazon Bedrock stores the raw text from your data. The text is split according
                              to the chunking strategy you choose.
                            maxLength: 2048
                            type: string
                          vectorField:
                            description: The name of the field in which Amazon Bedrock
                              stores the vector embeddings for your data sources.
                            maxLength: 2048
                            type: string
                        required:
                        - metadataField
                        - textField
                        - vectorField
                        type: object
                      vectorIndexName:
                        description: The name of the vector store.
                        maxLength: 2048
                        type: string
                    required:
                    - collectionArn
                    - fieldMapping
                    - vectorIndexName
                    type: object
                  pineconeConfiguration:
                    description: BedrockKnowledgeBasePineconeConfiguration is a nested
                      property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      connectionString:
                        description: The endpoint URL for your index management page.
                        maxLength: 2048
                        type: string
                      credentialsSecretArn:
                        description: The ARN of the secret that you created in AWS
                          Secrets Manager that is linked to your Pinecone API key.
                        type: string
                      fieldMapping:
                        description: BedrockKnowledgeBasePineconeFieldMapping is a
                          nested property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          metadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores metadata about the vector store.
                            maxLength: 2048
                            type: string
                          textField:
                            description: |-
                              The name of the field in which Amazon Bedrock stores the raw text from your data. The text is split according
                              to the chunking strategy you choose.
                            maxLength: 2048
                            type: string
                        required:
                        - metadataField
                        - textField
                        type: object
                      namespace:
                        description: The namespace to be used to write new data to
                          your database.
                        maxLength: 2048
                        type: string
                    required:
                    - connectionString
                    - credentialsSecretArn
                    - fieldMapping
                    type: object
                  rdsConfiguration:
                    description: BedrockKnowledgeBaseRdsConfiguration is a nested
                      property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      credentialsSecretArn:
                        description: The ARN of the secret that you created in AWS
                          Secrets Manager that is linked to your Amazon RDS database.
                        type: string
                      databaseName:
                        description: The name of your Amazon RDS database.
                        maxLength: 63
                        type: string
                      fieldMapping:
                        description: BedrockKnowledgeBaseRdsFieldMapping is a nested
                          property type of AWS::Bedrock::KnowledgeBase.
                        properties:
                          customMetadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores custom metadata about the vector store.
                            maxLength: 63
                            type: string
                          metadataField:
                            description: The name of the field in which Amazon Bedrock
                              stores metadata about the vector store.
                            maxLength: 63
                            type: string
                          primaryKeyField:
                            description: The name of the field in which Amazon Bedrock
                              stores the ID for each entry.
                            maxLength: 63
                            type: string
                          textField:
                            description: |-
                              The name of the field in which Amazon Bedrock stores the raw text from your data. The text is split according
                              to the chunking strategy you choose.
                            maxLength: 63
                            type: string
                          vectorField:
                            description: The name of the field in which Amazon Bedrock
                              stores the vector embeddings for your data sources.
                            maxLength: 63
                            type: string
                        required:
                        - metadataField
                        - primaryKeyField
                        - textField
                        - vectorField
                        type: object
                      resourceArn:
                        description: The ARN of the vector store.
                        type: string
                      tableName:
                        description: The name of the table in the database.
                        maxLength: 63
                        type: string
                    required:
                    - credentialsSecretArn
                    - databaseName
                    - fieldMapping
                    - resourceArn
                    - tableName
                    type: object
                  s3VectorsConfiguration:
                    description: BedrockKnowledgeBaseS3VectorsConfiguration is a nested
                      property type of AWS::Bedrock::KnowledgeBase.
                    properties:
                      indexArn:
                        description: |-
                          The Amazon Resource Name (ARN) of the vector index used for the knowledge base. This ARN identifies the
                          specific vector index resource within Amazon Bedrock.
                        type: string
                      indexName:
                        description: |-
                          The name of the vector index used for the knowledge base. This name identifies the vector index within the
                          Amazon Bedrock service.
                        maxLength: 63
                        type: string
                      vectorBucketArn:
                        description: |-
                          The Amazon Resource Name (ARN) of the S3 bucket where vector embeddings are stored. This bucket contains the
                          vector data used by the knowledge base.
                        type: string
                    type: object
                  type:
                    enum:
                    - OPENSEARCH_SERVERLESS
                    - PINECONE
                    - RDS
                    - MONGO_DB_ATLAS
                    - NEPTUNE_ANALYTICS
                    - S3_VECTORS
                    - OPENSEARCH_MANAGED_CLUSTER
                    type: string
                required:
                - type
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - knowledgeBaseConfiguration
            - name
            - roleArn
            type: object
          status:
            description: BedrockKnowledgeBaseStatus is the observed state of AWS::Bedrock::KnowledgeBase.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The time at which the knowledge base was created.
                  Read-only: reported by AWS after creation.
                type: string
              failureReasons:
                description: |-
                  A list of reasons that the API operation on the knowledge base failed.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              knowledgeBaseArn:
                description: |-
                  The ARN of the knowledge base.
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              knowledgeBaseId:
                description: |-
                  The unique identifier of the knowledge base.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - ACTIVE
                - DELETING
                - UPDATING
                - FAILED
                - DELETE_UNSUCCESSFUL
                - UPDATE_UNSUCCESSFUL
                type: string
              updatedAt:
                description: |-
                  The time at which the knowledge base was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockprompts.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockPrompt
    listKind: BedrockPromptList
    plural: bedrockprompts
    singular: bedrockprompt
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockPrompt manages AWS::Bedrock::Prompt through the AWS Cloud Control API. Definition of
          AWS::Bedrock::Prompt Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockPromptSpec is the desired state of AWS::Bedrock::Prompt.
            properties:
              customerEncryptionKeyArn:
                description: A KMS key ARN
                maxLength: 2048
                type: string
              defaultVariant:
                description: Name for a variant.
                type: string
              description:
                description: Name for a prompt resource.
                maxLength: 200
                type: string
              name:
                description: Name for a prompt resource.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              variants:
                description: List of prompt variants
                items:
                  description: BedrockPromptPromptVariant is a nested property type
                    of AWS::Bedrock::Prompt.
                  properties:
                    additionalModelRequestFields:
                      x-kubernetes-preserve-unknown-fields: true
                    genAiResource:
                      x-kubernetes-preserve-unknown-fields: true
                    inferenceConfiguration:
                      x-kubernetes-preserve-unknown-fields: true
                    metadata:
                      items:
                        description: BedrockPromptPromptMetadataEntry is a nested
                          property type of AWS::Bedrock::Prompt.
                        properties:
                          key:
                            maxLength: 128
                            minLength: 1
                            type: string
                          value:
                            maxLength: 1024
                            minLength: 1
                            type: string
                        required:
                        - key
                        - value
                        type: object
                      type: array
                    modelId:
                      description: |-
                        ARN or Id of a Bedrock Foundational Model or Inference Profile, or the ARN of a imported model, or a
                        provisioned throughput ARN for custom models.
                      maxLength: 2048
                      type: string
                    name:
                      description: Name for a variant.
                      type: string
                    templateConfiguration:
                      x-kubernetes-preserve-unknown-fields: true
                    templateType:
                      enum:
                      - TEXT
                      - CHAT
                      type: string
                  required:
                  - name
                  - templateConfiguration
                  - templateType
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            description: BedrockPromptStatus is the observed state of AWS::Bedrock::Prompt.
            properties:
              arn:
                description: |-
                  ARN of a prompt resource possibly with a version
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              id:
                description: |-
                  Identifier for a Prompt
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              version:
                description: |-
                  Draft Version.
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockpromptversions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockPromptVersion
    listKind: BedrockPromptVersionList
    plural: bedrockpromptversions
    singular: bedrockpromptversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockPromptVersion manages AWS::Bedrock::PromptVersion through the AWS Cloud Control API. Definition of
          AWS::Bedrock::PromptVersion Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockPromptVersionSpec is the desired state of AWS::Bedrock::PromptVersion.
            properties:
              description:
                description: |-
                  Description for a prompt version resource.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 200
                type: string
              promptArn:
                description: |-
                  ARN of a prompt resource possibly with a version
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: promptArn is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: object
            required:
            - promptArn
            type: object
          status:
            description: BedrockPromptVersionStatus is the observed state of AWS::Bedrock::PromptVersion.
            properties:
              arn:
                description: |-
                  ARN of a prompt version resource
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              customerEncryptionKeyArn:
                description: |-
                  A KMS key ARN
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              defaultVariant:
                description: |-
                  Name for a variant.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              name:
                description: |-
                  Name for a prompt resource.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              promptId:
                description: |-
                  Identifier for a Prompt
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  Time Stamp.
                  Read-only: reported by AWS after creation.
                type: string
              variants:
                description: |-
                  List of prompt variants
                  Read-only: reported by AWS after creation.
                items:
                  description: BedrockPromptVersionPromptVariant is a nested property
                    type of AWS::Bedrock::PromptVersion.
                  properties:
                    additionalModelRequestFields:
                      x-kubernetes-preserve-unknown-fields: true
                    genAiResource:
                      x-kubernetes-preserve-unknown-fields: true
                    inferenceConfiguration:
                      x-kubernetes-preserve-unknown-fields: true
                    metadata:
                      items:
                        description: BedrockPromptVersionPromptMetadataEntry is a
                          nested property type of AWS::Bedrock::PromptVersion.
                        properties:
                          key:
                            maxLength: 128
                            minLength: 1
                            type: string
                          value:
                            maxLength: 1024
                            minLength: 1
                            type: string
                        required:
                        - key
                        - value
                        type: object
                      type: array
                    modelId:
                      description: |-
                        ARN or Id of a Bedrock Foundational Model or Inference Profile, or the ARN of a imported model, or a
                        provisioned throughput ARN for custom models.
                      maxLength: 2048
                      type: string
                    name:
                      description: Name for a variant.
                      type: string
                    templateConfiguration:
                      x-kubernetes-preserve-unknown-fields: true
                    templateType:
                      enum:
                      - TEXT
                      - CHAT
                      type: string
                  required:
                  - name
                  - templateConfiguration
                  - templateType
                  type: object
                type: array
              version:
                description: |-
                  Version.
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockresourcepolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockResourcePolicy
    listKind: BedrockResourcePolicyList
    plural: bedrockresourcepolicies
    singular: bedrockresourcepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockResourcePolicy manages AWS::Bedrock::ResourcePolicy through the AWS Cloud Control API. Definition of
          AWS::Bedrock::ResourcePolicy Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockResourcePolicySpec is the desired state of AWS::Bedrock::ResourcePolicy.
            properties:
              policyDocument:
                description: The IAM policy document defining access permissions for
                  the guardrail and guardrail profile resources
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              resourceArn:
                description: |-
                  The ARN of the Bedrock Guardrail or Guardrail Profile resource
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: resourceArn is immutable
                  rule: self == oldSelf
            required:
            - policyDocument
            - resourceArn
            type: object
          status:
            description: BedrockResourcePolicyStatus is the observed state of AWS::Bedrock::ResourcePolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrocksessions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockSession
    listKind: BedrockSessionList
    plural: bedrocksessions
    singular: bedrocksession
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockSession manages AWS::Bedrock::Session through the AWS Cloud Control API. Definition of
          AWS::Bedrock::Session Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockSessionSpec is the desired state of AWS::Bedrock::Session.
            properties:
              encryptionKeyArn:
                description: |-
                  The Amazon Resource Name (ARN) of the KMS key to use to encrypt the session data.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              sessionMetadata:
                additionalProperties:
                  type: string
                description: A map of key-value pairs containing attributes to be
                  persisted across the session.
                type: object
              tags:
                description: A list of tags associated with the session.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            type: object
          status:
            description: BedrockSessionStatus is the observed state of AWS::Bedrock::Session.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp for when the session was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  The timestamp for when the session was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              sessionArn:
                description: |-
                  The Amazon Resource Name (ARN) of the session.
                  Read-only: reported by AWS after creation.
                type: string
              sessionId:
                description: |-
                  The unique identifier of the session in UUID format.
                  Read-only: reported by AWS after creation.
                type: string
              sessionStatus:
                description: |-
                  The current status of the session.
                  Read-only: reported by AWS after creation.
                enum:
                - ACTIVE
                - EXPIRED
                - ENDED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
