---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: inspectorv2cisscanconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: InspectorV2CisScanConfiguration
    listKind: InspectorV2CisScanConfigurationList
    plural: inspectorv2cisscanconfigurations
    singular: inspectorv2cisscanconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InspectorV2CisScanConfiguration manages AWS::InspectorV2::CisScanConfiguration through the AWS Cloud Control
          API. CIS Scan Configuration resource schema
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InspectorV2CisScanConfigurationSpec is the desired state
              of AWS::InspectorV2::CisScanConfiguration.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              scanName:
                description: Name of the scan
                minLength: 1
                type: string
              schedule:
                x-kubernetes-preserve-unknown-fields: true
              securityLevel:
                enum:
                - LEVEL_1
                - LEVEL_2
                type: string
              tags:
                additionalProperties:
                  type: string
                type: object
              targets:
                x-kubernetes-preserve-unknown-fields: true
            required:
            - scanName
            - schedule
            - securityLevel
            - targets
            type: object
          status:
            description: InspectorV2CisScanConfigurationStatus is the observed state
              of AWS::InspectorV2::CisScanConfiguration.
            properties:
              arn:
                description: |-
                  CIS Scan configuration unique identifier
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: inspectorv2codesecurityintegrations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: InspectorV2CodeSecurityIntegration
    listKind: InspectorV2CodeSecurityIntegrationList
    plural: inspectorv2codesecurityintegrations
    singular: inspectorv2codesecurityintegration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InspectorV2CodeSecurityIntegration manages AWS::InspectorV2::CodeSecurityIntegration through the AWS Cloud
          Control API. Inspector CodeSecurityIntegration resource schema
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InspectorV2CodeSecurityIntegrationSpec is the desired state
              of AWS::InspectorV2::CodeSecurityIntegration.
            properties:
              createIntegrationDetails:
                description: |-
                  Create Integration Details
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  gitlabSelfManaged:
                    description: InspectorV2CodeSecurityIntegrationCreateGitLabSelfManagedIntegrationDetail
                      is a nested property type of AWS::InspectorV2::CodeSecurityIntegration.
                    properties:
                      accessToken:
                        type: string
                      instanceUrl:
                        type: string
                    required:
                    - accessToken
                    - instanceUrl
                    type: object
                required:
                - gitlabSelfManaged
                type: object
              name:
                description: Code Security Integration name
                maxLength: 60
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              type:
                description: Integration Type
                enum:
                - GITLAB_SELF_MANAGED
                - GITHUB
                type: string
              updateIntegrationDetails:
                description: Update Integration Details
                properties:
                  github:
                    description: InspectorV2CodeSecurityIntegrationUpdateGitHubIntegrationDetail
                      is a nested property type of AWS::InspectorV2::CodeSecurityIntegration.
                    properties:
                      code:
                        maxLength: 1024
                        minLength: 1
                        type: string
                      installationId:
                        maxLength: 1024
                        minLength: 1
                        type: string
                    required:
                    - code
                    - installationId
                    type: object
                  gitlabSelfManaged:
                    description: InspectorV2CodeSecurityIntegrationUpdateGitLabSelfManagedIntegrationDetail
                      is a nested property type of AWS::InspectorV2::CodeSecurityIntegration.
                    properties:
                      authCode:
                        maxLength: 1024
                        minLength: 1
                        type: string
                    required:
                    - authCode
                    type: object
                type: object
            type: object
          status:
            description: InspectorV2CodeSecurityIntegrationStatus is the observed
              state of AWS::InspectorV2::CodeSecurityIntegration.
            properties:
              arn:
                description: |-
                  Code Security Integration ARN
                  Read-only: reported by AWS after creation.
                type: string
              authorizationUrl:
                description: |-
                  Authorization URL for OAuth flow
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Creation timestamp
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  Last update timestamp
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  Integration Status
                  Read-only: reported by AWS after creation.
                enum:
                - PENDING
                - IN_PROGRESS
                - ACTIVE
                - INACTIVE
                - DISABLING
                type: string
              statusReason:
                description: |-
                  Reason for the current status
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: inspectorv2codesecurityscanconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: InspectorV2CodeSecurityScanConfiguration
    listKind: InspectorV2CodeSecurityScanConfigurationList
    plural: inspectorv2codesecurityscanconfigurations
    singular: inspectorv2codesecurityscanconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InspectorV2CodeSecurityScanConfiguration manages AWS::InspectorV2::CodeSecurityScanConfiguration through the
          AWS Cloud Control API. Inspector CodeSecurityScanConfiguration resource schema
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InspectorV2CodeSecurityScanConfigurationSpec is the desired
              state of AWS::InspectorV2::CodeSecurityScanConfiguration.
            properties:
              configuration:
                description: Code Security Scan Configuration
                properties:
                  continuousIntegrationScanConfiguration:
                    description: InspectorV2CodeSecurityScanConfigurationContinuousIntegrationScanConfiguration
                      is a nested property type of AWS::InspectorV2::CodeSecurityScanConfiguration.
                    properties:
                      supportedEvents:
                        items:
                          type: string
                        type: array
                    required:
                    - supportedEvents
                    type: object
                  periodicScanConfiguration:
                    description: InspectorV2CodeSecurityScanConfigurationPeriodicScanConfiguration
                      is a nested property type of AWS::InspectorV2::CodeSecurityScanConfiguration.
                    properties:
                      frequency:
                        enum:
                        - WEEKLY
                        - MONTHLY
                        - NEVER
                        type: string
                      frequencyExpression:
                        maxLength: 256
                        type: string
                    type: object
                  ruleSetCategories:
                    items:
                      type: string
                    type: array
                required:
                - ruleSetCategories
                type: object
              level:
                description: |-
                  Configuration Level
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - ORGANIZATION
                - ACCOUNT
                type: string
              name:
                description: |-
                  Code Security Scan Configuration name
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 60
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              scopeSettings:
                description: |-
                  Scope Settings
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  projectSelectionScope:
                    enum:
                    - ALL
                    type: string
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            type: object
          status:
            description: InspectorV2CodeSecurityScanConfigurationStatus is the observed
              state of AWS::InspectorV2::CodeSecurityScanConfiguration.
            properties:
              arn:
                description: |-
                  Code Security Scan Configuration ARN
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: inspectorv2connectors.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: InspectorV2Connector
    listKind: InspectorV2ConnectorList
    plural: inspectorv2connectors
    singular: inspectorv2connector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InspectorV2Connector manages AWS::InspectorV2::Connector through the AWS Cloud Control API. Creates and
          manages a multi-cloud connector for Amazon Inspector, enabling vulnerability scanning of Azure resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InspectorV2ConnectorSpec is the desired state of AWS::InspectorV2::Connector.
            properties:
              description:
                description: Optional description of the connector.
                maxLength: 200
                type: string
              name:
                description: |-
                  Display name for the connector.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 50
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              provider:
                description: |-
                  The cloud provider for this connector.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - AZURE
                type: string
                x-kubernetes-validations:
                - message: provider is immutable
                  rule: self == oldSelf
              providerConfiguration:
                description: Provider-specific configuration including regions and
                  scope.
                properties:
                  azure:
                    description: InspectorV2ConnectorAzureProviderConfiguration is
                      a nested property type of AWS::InspectorV2::Connector.
                    properties:
                      autoInstallVMScanner:
                        description: Whether to automatically install the VM scanner.
                          Defaults to true.
                        type: boolean
                      awsConfigConnectorArn:
                        description: The ARN of the AWS Config connector used for
                          Azure resource discovery.
                        maxLength: 512
                        minLength: 1
                        type: string
                      azureRegions:
                        description: List of Azure regions to scan.
                        items:
                          type: string
                        type: array
                      scopeConfiguration:
                        description: InspectorV2ConnectorAzureScopeConfigurationMap
                          is a nested property type of AWS::InspectorV2::Connector.
                        properties:
                          containerImageScanning:
                            description: InspectorV2ConnectorScopeConfiguration is
                              a nested property type of AWS::InspectorV2::Connector.
                            properties:
                              scopeType:
                                enum:
                                - TENANT
                                - SUBSCRIPTION
                                type: string
                              scopeValues:
                                description: List of subscription IDs. Empty for TENANT
                                  scope.
                                items:
                                  type: string
                                type: array
                              state:
                                enum:
                                - ACTIVE
                                - PENDING
                                - ERROR
                                - DISABLED
                                type: string
                              stateReason:
                                description: Reason for the current scope state.
                                type: string
                            required:
                            - scopeType
                            type: object
                          serverlessScanning:
                            description: InspectorV2ConnectorScopeConfiguration is
                              a nested property type of AWS::InspectorV2::Connector.
                            properties:
                              scopeType:
                                enum:
                                - TENANT
                                - SUBSCRIPTION
                                type: string
                              scopeValues:
                                description: List of subscription IDs. Empty for TENANT
                                  scope.
                                items:
                                  type: string
                                type: array
                              state:
                                enum:
                                - ACTIVE
                                - PENDING
                                - ERROR
                                - DISABLED
                                type: string
                              stateReason:
                                description: Reason for the current scope state.
                                type: string
                            required:
                            - scopeType
                            type: object
                          vmScanning:
                            description: InspectorV2ConnectorScopeConfiguration is
                              a nested property type of AWS::InspectorV2::Connector.
                            properties:
                              scopeType:
                                enum:
                                - TENANT
                                - SUBSCRIPTION
                                type: string
                              scopeValues:
                                description: List of subscription IDs. Empty for TENANT
                                  scope.
                                items:
                                  type: string
                                type: array
                              state:
                                enum:
                                - ACTIVE
                                - PENDING
                                - ERROR
                                - DISABLED
                                type: string
                              stateReason:
                                description: Reason for the current scope state.
                                type: string
                            required:
                            - scopeType
                            type: object
                        type: object
                    required:
                    - awsConfigConnectorArn
                    - azureRegions
                    - scopeConfiguration
                    type: object
                required:
                - azure
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Tags to apply to the connector.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - name
            - provider
            - providerConfiguration
            type: object
          status:
            description: InspectorV2ConnectorStatus is the observed state of AWS::InspectorV2::Connector.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectorArn:
                description: |-
                  The Amazon Resource Name (ARN) of the connector.
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              createdAt:
                description: |-
                  Timestamp when the connector was created.
                  Read-only: reported by AWS after creation.
                type: string
              enablementStatus:
                description: |-
                  The enablement status of the connector.
                  Read-only: reported by AWS after creation.
                enum:
                - ENABLED
                - PENDING_ENABLEMENT
                - FAILED_TO_ENABLE
                - PENDING_UPDATE
                - FAILED_TO_UPDATE
                - PENDING_DELETION
                - DELETED
                - FAILED_TO_DELETE
                type: string
              enablementStatusReason:
                description: |-
                  Reason for the current enablement status, if applicable.
                  Read-only: reported by AWS after creation.
                type: string
              health:
                description: |-
                  Health status of the connector.
                  Read-only: reported by AWS after creation.
                properties:
                  connectorStatus:
                    enum:
                    - CONNECTED
                    - DEGRADED
                    - FAILED_TO_CONNECT
                    - PENDING_AUTHORIZATION
                    - PENDING_CONFIGURATION
                    - UNKNOWN
                    type: string
                  lastCheckedAt:
                    type: string
                  message:
                    type: string
                type: object
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  Timestamp when the connector was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: inspectorv2filters.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: InspectorV2Filter
    listKind: InspectorV2FilterList
    plural: inspectorv2filters
    singular: inspectorv2filter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          InspectorV2Filter manages AWS::InspectorV2::Filter through the AWS Cloud Control API. Inspector Filter
          resource schema
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: InspectorV2FilterSpec is the desired state of AWS::InspectorV2::Filter.
            properties:
              description:
                description: Findings filter description.
                maxLength: 512
                type: string
              filterAction:
                description: Findings filter action.
                enum:
                - NONE
                - SUPPRESS
                type: string
              filterCriteria:
                description: Findings filter criteria.
                properties:
                  awsAccountId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  codeVulnerabilityDetectorName:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  codeVulnerabilityDetectorTags:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  codeVulnerabilityFilePath:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  componentId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  componentType:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ec2InstanceImageId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ec2InstanceSubnetId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ec2InstanceVpcId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ecrImageArchitecture:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ecrImageHash:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ecrImagePushedAt:
                    items:
                      description: InspectorV2FilterDateFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        endInclusive:
                          format: int64
                          type: integer
                        startInclusive:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  ecrImageRegistry:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ecrImageRepositoryName:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  ecrImageTags:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  epssScore:
                    items:
                      description: InspectorV2FilterNumberFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        lowerInclusive:
                          type: number
                        upperInclusive:
                          type: number
                      type: object
                    type: array
                  exploitAvailable:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingArn:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingStatus:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  findingType:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  firstObservedAt:
                    items:
                      description: InspectorV2FilterDateFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        endInclusive:
                          format: int64
                          type: integer
                        startInclusive:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  fixAvailable:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  inspectorScore:
                    items:
                      description: InspectorV2FilterNumberFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        lowerInclusive:
                          type: number
                        upperInclusive:
                          type: number
                      type: object
                    type: array
                  lambdaFunctionExecutionRoleArn:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  lambdaFunctionLastModifiedAt:
                    items:
                      description: InspectorV2FilterDateFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        endInclusive:
                          format: int64
                          type: integer
                        startInclusive:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  lambdaFunctionLayers:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  lambdaFunctionName:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  lambdaFunctionRuntime:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  lastObservedAt:
                    items:
                      description: InspectorV2FilterDateFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        endInclusive:
                          format: int64
                          type: integer
                        startInclusive:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  networkProtocol:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  portRange:
                    items:
                      description: InspectorV2FilterPortRangeFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        beginInclusive:
                          format: int64
                          maximum: 65535
                          minimum: 0
                          type: integer
                        endInclusive:
                          format: int64
                          maximum: 65535
                          minimum: 0
                          type: integer
                      type: object
                    type: array
                  relatedVulnerabilities:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  resourceTags:
                    items:
                      description: InspectorV2FilterMapFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          type: string
                        key:
                          maxLength: 128
                          type: string
                        value:
                          maxLength: 256
                          type: string
                      required:
                      - comparison
                      type: object
                    type: array
                  resourceType:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  severity:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  title:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  updatedAt:
                    items:
                      description: InspectorV2FilterDateFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        endInclusive:
                          format: int64
                          type: integer
                        startInclusive:
                          format: int64
                          type: integer
                      type: object
                    type: array
                  vendorSeverity:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  vulnerabilityId:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  vulnerabilitySource:
                    items:
                      description: InspectorV2FilterStringFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        comparison:
                          enum:
                          - EQUALS
                          - PREFIX
                          - NOT_EQUALS
                          type: string
                        value:
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - comparison
                      - value
                      type: object
                    type: array
                  vulnerablePackages:
                    items:
                      description: InspectorV2FilterPackageFilter is a nested property
                        type of AWS::InspectorV2::Filter.
                      properties:
                        architecture:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                        epoch:
                          description: InspectorV2FilterNumberFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            lowerInclusive:
                              type: number
                            upperInclusive:
                              type: number
                          type: object
                        filePath:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                        name:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                        release:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                        sourceLambdaLayerArn:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                        sourceLayerHash:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                        version:
                          description: InspectorV2FilterStringFilter is a nested property
                            type of AWS::InspectorV2::Filter.
                          properties:
                            comparison:
                              enum:
                              - EQUALS
                              - PREFIX
                              - NOT_EQUALS
                              type: string
                            value:
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - comparison
                          - value
                          type: object
                      type: object
                    type: array
                type: object
              name:
                description: Findings filter name.
                maxLength: 128
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - filterAction
            - filterCriteria
            - name
            type: object
          status:
            description: InspectorV2FilterStatus is the observed state of AWS::InspectorV2::Filter.
            properties:
              arn:
                description: |-
                  Findings filter ARN.
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
