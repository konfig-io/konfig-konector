---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: fisexperimenttemplates.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: FISExperimentTemplate
    listKind: FISExperimentTemplateList
    plural: fisexperimenttemplates
    singular: fisexperimenttemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FISExperimentTemplate manages AWS::FIS::ExperimentTemplate through the AWS Cloud Control API. Resource schema
          for AWS::FIS::ExperimentTemplate
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FISExperimentTemplateSpec is the desired state of AWS::FIS::ExperimentTemplate.
            properties:
              actions:
                additionalProperties:
                  description: FISExperimentTemplateExperimentTemplateAction is a
                    nested property type of AWS::FIS::ExperimentTemplate.
                  properties:
                    actionId:
                      maxLength: 64
                      type: string
                    description:
                      maxLength: 512
                      type: string
                    parameters:
                      additionalProperties:
                        type: string
                      description: The parameters for the action, if applicable.
                      type: object
                    startAfter:
                      items:
                        type: string
                      type: array
                    targets:
                      additionalProperties:
                        type: string
                      description: One or more targets for the action.
                      type: object
                  required:
                  - actionId
                  type: object
                type: object
              description:
                maxLength: 512
                type: string
              experimentOptions:
                description: FISExperimentTemplateExperimentTemplateExperimentOptions
                  is a nested property type of AWS::FIS::ExperimentTemplate.
                properties:
                  accountTargeting:
                    description: The account targeting setting for the experiment
                      template.
                    enum:
                    - multi-account
                    - single-account
                    type: string
                  emptyTargetResolutionMode:
                    description: The target resolution failure mode for the experiment
                      template.
                    enum:
                    - fail
                    - skip
                    type: string
                type: object
              experimentReportConfiguration:
                description: FISExperimentTemplateExperimentTemplateExperimentReportConfiguration
                  is a nested property type of AWS::FIS::ExperimentTemplate.
                properties:
                  dataSources:
                    description: FISExperimentTemplateExperimentTemplateExperimentReportConfigurationDataSources
                      is a nested property type of AWS::FIS::ExperimentTemplate.
                    properties:
                      cloudWatchDashboards:
                        items:
                          description: FISExperimentTemplateCloudWatchDashboard is
                            a nested property type of AWS::FIS::ExperimentTemplate.
                          properties:
                            dashboardIdentifier:
                              maxLength: 512
                              minLength: 1
                              type: string
                          required:
                          - dashboardIdentifier
                          type: object
                        type: array
                    type: object
                  outputs:
                    description: FISExperimentTemplateExperimentTemplateExperimentReportConfigurationOutputs
                      is a nested property type of AWS::FIS::ExperimentTemplate.
                    properties:
                      experimentReportS3Configuration:
                        description: FISExperimentTemplateExperimentTemplateExperimentReportConfigurationOutputsExperimentReportS3Configuration
                          is a nested property type of AWS::FIS::ExperimentTemplate.
                        properties:
                          bucketName:
                            maxLength: 63
                            minLength: 3
                            type: string
                          prefix:
                            maxLength: 256
                            type: string
                        required:
                        - bucketName
                        type: object
                    required:
                    - experimentReportS3Configuration
                    type: object
                  postExperimentDuration:
                    type: string
                  preExperimentDuration:
                    type: string
                required:
                - outputs
                type: object
              logConfiguration:
                description: FISExperimentTemplateExperimentTemplateLogConfiguration
                  is a nested property type of AWS::FIS::ExperimentTemplate.
                properties:
                  cloudWatchLogsConfiguration:
                    description: FISExperimentTemplateExperimentTemplateLogConfigurationCloudWatchLogsConfiguration
                      is a nested property type of AWS::FIS::ExperimentTemplate.
                    properties:
                      logGroupArn:
                        maxLength: 2048
                        minLength: 20
                        type: string
                    required:
                    - logGroupArn
                    type: object
                  logSchemaVersion:
                    format: int64
                    minimum: 1
                    type: integer
                  s3Configuration:
                    description: FISExperimentTemplateExperimentTemplateLogConfigurationS3Configuration
                      is a nested property type of AWS::FIS::ExperimentTemplate.
                    properties:
                      bucketName:
                        maxLength: 63
                        minLength: 3
                        type: string
                      prefix:
                        maxLength: 700
                        type: string
                    required:
                    - bucketName
                    type: object
                required:
                - logSchemaVersion
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                maxLength: 1224
                type: string
              stopConditions:
                items:
                  description: FISExperimentTemplateExperimentTemplateStopCondition
                    is a nested property type of AWS::FIS::ExperimentTemplate.
                  properties:
                    source:
                      maxLength: 64
                      type: string
                    value:
                      maxLength: 2048
                      type: string
                  required:
                  - source
                  type: object
                type: array
              tags:
                additionalProperties:
                  type: string
                type: object
              targets:
                additionalProperties:
                  description: FISExperimentTemplateExperimentTemplateTarget is a
                    nested property type of AWS::FIS::ExperimentTemplate.
                  properties:
                    filters:
                      items:
                        description: FISExperimentTemplateExperimentTemplateTargetFilter
                          is a nested property type of AWS::FIS::ExperimentTemplate.
                        properties:
                          path:
                            maxLength: 256
                            type: string
                          values:
                            items:
                              type: string
                            type: array
                        required:
                        - path
                        - values
                        type: object
                      type: array
                    parameters:
                      additionalProperties:
                        type: string
                      type: object
                    resourceArns:
                      items:
                        type: string
                      type: array
                    resourceTags:
                      additionalProperties:
                        type: string
                      type: object
                    resourceType:
                      maxLength: 64
                      type: string
                    selectionMode:
                      maxLength: 64
                      type: string
                  required:
                  - resourceType
                  - selectionMode
                  type: object
                type: object
            required:
            - description
            - roleArn
            - stopConditions
            - targets
            type: object
          status:
            description: FISExperimentTemplateStatus is the observed state of AWS::FIS::ExperimentTemplate.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              id:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: fistargetaccountconfigurations.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: FISTargetAccountConfiguration
    listKind: FISTargetAccountConfigurationList
    plural: fistargetaccountconfigurations
    singular: fistargetaccountconfiguration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          FISTargetAccountConfiguration manages AWS::FIS::TargetAccountConfiguration through the AWS Cloud Control API.
          CAUTION, DESTRUCTIVE SCOPE: fault injection into another account. This kind changes shared account, region or
          organization state (or carries credential material); it is provided for completeness and should not be used
          from application namespaces. Restrict it with AWSProvider.allowedName...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: FISTargetAccountConfigurationSpec is the desired state of
              AWS::FIS::TargetAccountConfiguration.
            properties:
              accountId:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 512
                type: string
                x-kubernetes-validations:
                - message: accountId is immutable
                  rule: self == oldSelf
              description:
                maxLength: 512
                type: string
              experimentTemplateId:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
                x-kubernetes-validations:
                - message: experimentTemplateId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                maxLength: 1224
                type: string
            required:
            - accountId
            - experimentTemplateId
            - roleArn
            type: object
          status:
            description: FISTargetAccountConfigurationStatus is the observed state
              of AWS::FIS::TargetAccountConfiguration.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
