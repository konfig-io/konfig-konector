---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: computeoptimizerautomationrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ComputeOptimizerAutomationRule
    listKind: ComputeOptimizerAutomationRuleList
    plural: computeoptimizerautomationrules
    singular: computeoptimizerautomationrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ComputeOptimizerAutomationRule manages AWS::ComputeOptimizer::AutomationRule through the AWS Cloud Control
          API. Creates an AWS Compute Optimizer automation rule that automatically implements recommended actions based
          on your defined criteria and schedule. Automation rules are global resources that manage automated actions
          across all AWS Regions where Compute Optimizer Automation is available. O...
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ComputeOptimizerAutomationRuleSpec is the desired state of
              AWS::ComputeOptimizer::AutomationRule.
            properties:
              criteria:
                description: Filter criteria that specify which recommended actions
                  qualify for implementation.
                properties:
                  ebsVolumeSizeInGib:
                    items:
                      description: ComputeOptimizerAutomationRuleIntegerCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            format: int64
                            type: integer
                          type: array
                      type: object
                    type: array
                  ebsVolumeType:
                    items:
                      description: ComputeOptimizerAutomationRuleStringCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  estimatedMonthlySavings:
                    items:
                      description: ComputeOptimizerAutomationRuleDoubleCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            type: number
                          type: array
                      type: object
                    type: array
                  lookBackPeriodInDays:
                    items:
                      description: ComputeOptimizerAutomationRuleIntegerCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            format: int64
                            type: integer
                          type: array
                      type: object
                    type: array
                  region:
                    items:
                      description: ComputeOptimizerAutomationRuleStringCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  resourceArn:
                    items:
                      description: ComputeOptimizerAutomationRuleStringCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  resourceTag:
                    items:
                      description: ComputeOptimizerAutomationRuleResourceTagsCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        key:
                          maxLength: 512
                          type: string
                        values:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                  restartNeeded:
                    items:
                      description: ComputeOptimizerAutomationRuleStringCriteriaCondition
                        is a nested property type of AWS::ComputeOptimizer::AutomationRule.
                      properties:
                        comparison:
                          enum:
                          - StringEquals
                          - StringNotEquals
                          - StringEqualsIgnoreCase
                          - StringNotEqualsIgnoreCase
                          - StringLike
                          - StringNotLike
                          - NumericEquals
                          - NumericNotEquals
                          - NumericLessThan
                          - NumericLessThanEquals
                          - NumericGreaterThan
                          - NumericGreaterThanEquals
                          type: string
                        values:
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              description:
                description: The description of the automation rule.
                maxLength: 1024
                type: string
              name:
                description: The name of the automation rule.
                maxLength: 128
                type: string
              organizationConfiguration:
                description: Organization configuration for organization rules, including
                  rule apply order and account scope.
                properties:
                  accountIds:
                    description: List of account IDs where the organization rule applies
                    items:
                      type: string
                    type: array
                  ruleApplyOrder:
                    description: When the rule should be applied relative to account
                      rules
                    enum:
                    - BeforeAccountRules
                    - AfterAccountRules
                    type: string
                type: object
              priority:
                description: Rule priority within its group
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              recommendedActionTypes:
                description: The types of recommended actions this rule will implement.
                items:
                  type: string
                type: array
              ruleType:
                description: The type of automation rule.
                enum:
                - AccountRule
                - OrganizationRule
                type: string
              schedule:
                description: The schedule configuration for when the rule runs.
                properties:
                  executionWindowInMinutes:
                    description: Execution window duration in minutes
                    format: int64
                    maximum: 1440
                    minimum: 60
                    type: integer
                  scheduleExpression:
                    description: Schedule expression (e.g., cron or rate expression)
                    type: string
                  scheduleExpressionTimezone:
                    description: IANA timezone identifier
                    type: string
                type: object
              status:
                description: The status of the automation rule.
                enum:
                - Active
                - Inactive
                type: string
              tags:
                description: Tags associated with the automation rule.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - name
            - recommendedActionTypes
            - ruleType
            - schedule
            - status
            type: object
          status:
            description: ComputeOptimizerAutomationRuleStatus is the observed state
              of AWS::ComputeOptimizer::AutomationRule.
            properties:
              accountId:
                description: |-
                  The AWS account ID that owns the automation rule.
                  Read-only: reported by AWS after creation.
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdTimestamp:
                description: |-
                  The timestamp when the automation rule was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedTimestamp:
                description: |-
                  The timestamp when the automation rule was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleArn:
                description: |-
                  The Amazon Resource Name (ARN) of the automation rule.
                  Read-only: reported by AWS after creation.
                type: string
              ruleId:
                description: |-
                  The unique identifier of the automation rule.
                  Read-only: reported by AWS after creation.
                type: string
              ruleRevision:
                description: |-
                  The revision number of the automation rule.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
