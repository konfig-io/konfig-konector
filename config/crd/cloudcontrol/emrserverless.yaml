---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: emrserverlessapplications.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: EMRServerlessApplication
    listKind: EMRServerlessApplicationList
    plural: emrserverlessapplications
    singular: emrserverlessapplication
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EMRServerlessApplication manages AWS::EMRServerless::Application through the AWS Cloud Control API. Resource
          schema for AWS::EMRServerless::Application Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EMRServerlessApplicationSpec is the desired state of AWS::EMRServerless::Application.
            properties:
              architecture:
                enum:
                - ARM64
                - X86_64
                type: string
              autoStartConfiguration:
                description: Configuration for Auto Start of Application.
                properties:
                  enabled:
                    description: If set to true, the Application will automatically
                      start. Defaults to true.
                    type: boolean
                type: object
              autoStopConfiguration:
                description: Configuration for Auto Stop of Application.
                properties:
                  enabled:
                    description: If set to true, the Application will automatically
                      stop after being idle. Defaults to true.
                    type: boolean
                  idleTimeoutMinutes:
                    description: |-
                      The amount of time [in minutes] to wait before auto stopping the Application when idle. Defaults to 15
                      minutes.
                    format: int64
                    type: integer
                type: object
              identityCenterConfiguration:
                description: |-
                  The IAM IdentityCenter configuration for trusted-identity-propagation on this application. Supported with
                  release labels emr-7.8.0 and above.
                properties:
                  identityCenterInstanceArn:
                    description: The IAM IdentityCenter instance arn
                    maxLength: 1024
                    type: string
                type: object
              imageConfiguration:
                description: EMRServerlessApplicationImageConfigurationInput is a
                  nested property type of AWS::EMRServerless::Application.
                properties:
                  imageUri:
                    description: |-
                      The URI of an image in the Amazon ECR registry. This field is required when you create a new application. If
                      you leave this field blank in an update, Amazon EMR will remove the image configuration.
                    maxLength: 1024
                    type: string
                type: object
              initialCapacity:
                description: Initial capacity initialized when an Application is started.
                items:
                  description: EMRServerlessApplicationInitialCapacityConfigKeyValuePair
                    is a nested property type of AWS::EMRServerless::Application.
                  properties:
                    key:
                      description: Worker type for an analytics framework.
                      maxLength: 50
                      minLength: 1
                      type: string
                    value:
                      description: EMRServerlessApplicationInitialCapacityConfig is
                        a nested property type of AWS::EMRServerless::Application.
                      properties:
                        workerConfiguration:
                          description: EMRServerlessApplicationWorkerConfiguration
                            is a nested property type of AWS::EMRServerless::Application.
                          properties:
                            cpu:
                              description: Per worker CPU resource. vCPU is the only
                                supported unit and specifying vCPU is optional.
                              maxLength: 15
                              minLength: 1
                              type: string
                            disk:
                              description: Per worker Disk resource. GB is the only
                                supported unit and specifying GB is optional
                              maxLength: 15
                              type: string
                            diskType:
                              description: |-
                                Per worker DiskType resource. Shuffle optimized and Standard are only supported types and specifying diskType
                                is optional
                              type: string
                            memory:
                              description: Per worker memory resource. GB is the only
                                supported unit and specifying GB is optional.
                              maxLength: 15
                              minLength: 1
                              type: string
                          required:
                          - cpu
                          - memory
                          type: object
                        workerCount:
                          description: |-
                            Initial count of workers to be initialized when an Application is started. This count will be continued to be
                            maintained until the Application is stopped
                          format: int64
                          maximum: 1000000
                          minimum: 1
                          type: integer
                      required:
                      - workerConfiguration
                      - workerCount
                      type: object
                  required:
                  - key
                  - value
                  type: object
                type: array
              interactiveConfiguration:
                description: EMRServerlessApplicationInteractiveConfiguration is a
                  nested property type of AWS::EMRServerless::Application.
                properties:
                  livyEndpointEnabled:
                    description: Enables an Apache Livy endpoint that you can connect
                      to and run interactive jobs
                    type: boolean
                  sessionEnabled:
                    description: Enables interactive sessions on the application
                    type: boolean
                  studioEnabled:
                    description: Enabled you to connect an Application to Amazon EMR
                      Studio to run interactive workloads in a notebook
                    type: boolean
                type: object
              maximumCapacity:
                description: |-
                  Maximum allowed cumulative resources for an Application. No new resources will be created once the limit is
                  hit.
                properties:
                  cpu:
                    description: Per worker CPU resource. vCPU is the only supported
                      unit and specifying vCPU is optional.
                    maxLength: 15
                    minLength: 1
                    type: string
                  disk:
                    description: Per worker Disk resource. GB is the only supported
                      unit and specifying GB is optional
                    maxLength: 15
                    type: string
                  memory:
                    description: Per worker memory resource. GB is the only supported
                      unit and specifying GB is optional.
                    maxLength: 15
                    minLength: 1
                    type: string
                required:
                - cpu
                - memory
                type: object
              monitoringConfiguration:
                description: EMRServerlessApplicationMonitoringConfiguration is a
                  nested property type of AWS::EMRServerless::Application.
                properties:
                  cloudWatchLoggingConfiguration:
                    description: CloudWatch logging configurations for a JobRun.
                    x-kubernetes-preserve-unknown-fields: true
                  managedPersistenceMonitoringConfiguration:
                    description: Managed log persistence configurations for a JobRun.
                    x-kubernetes-preserve-unknown-fields: true
                  prometheusMonitoringConfiguration:
                    description: Prometheus monitoring configurations for a JobRun.
                    x-kubernetes-preserve-unknown-fields: true
                  s3MonitoringConfiguration:
                    description: S3 monitoring configurations for a JobRun.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              name:
                description: |-
                  User friendly Application name.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                type: string
              networkConfiguration:
                description: Network Configuration for customer VPC connectivity.
                properties:
                  securityGroupIds:
                    description: The ID of the security groups in the VPC to which
                      you want to connect your job or application.
                    items:
                      type: string
                    type: array
                  subnetIds:
                    description: The ID of the subnets in the VPC to which you want
                      to connect your job or application.
                    items:
                      type: string
                    type: array
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              releaseLabel:
                description: EMR release label.
                maxLength: 64
                minLength: 1
                type: string
              runtimeConfiguration:
                items:
                  description: EMRServerlessApplicationConfigurationObject is a nested
                    property type of AWS::EMRServerless::Application.
                  properties:
                    classification:
                      description: String with a maximum length of 1024.
                      maxLength: 1024
                      minLength: 1
                      type: string
                    configurations:
                      items: {}
                      type: array
                    properties:
                      additionalProperties:
                        type: string
                      type: object
                  required:
                  - classification
                  type: object
                type: array
              schedulerConfiguration:
                description: |-
                  The scheduler configuration for batch and streaming jobs running on this application. Supported with release
                  labels emr-7.0.0 and above.
                properties:
                  maxConcurrentRuns:
                    description: |-
                      The maximum concurrent job runs on this application. If scheduler configuration is enabled on your
                      application, the default value is 15. The valid range is 1 to 1000.
                    format: int64
                    type: integer
                  queueTimeoutMinutes:
                    description: |-
                      The maximum duration in minutes for the job in QUEUED state. If scheduler configuration is enabled on your
                      application, the default value is 360 minutes (6 hours). The valid range is from 15 to 720.
                    format: int64
                    type: integer
                type: object
              tags:
                description: Tag map with key and value
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              type:
                description: |-
                  The type of the application
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
              workerTypeSpecifications:
                additionalProperties:
                  description: EMRServerlessApplicationWorkerTypeSpecificationInput
                    is a nested property type of AWS::EMRServerless::Application.
                  properties:
                    imageConfiguration:
                      description: EMRServerlessApplicationImageConfigurationInput
                        is a nested property type of AWS::EMRServerless::Application.
                      properties:
                        imageUri:
                          description: |-
                            The URI of an image in the Amazon ECR registry. This field is required when you create a new application. If
                            you leave this field blank in an update, Amazon EMR will remove the image configuration.
                          maxLength: 1024
                          type: string
                      type: object
                  type: object
                description: |-
                  The key-value pairs that specify worker type to WorkerTypeSpecificationInput. This parameter must contain all
                  valid worker types for a Spark or Hive application. Valid worker types include Driver and Executor for Spark
                  applications and HiveDriver and TezTask for Hive applications. You can either set image details in this
                  parameter for each worker type, or in imageConfiguration for all worker ty...
                type: object
            required:
            - releaseLabel
            - type
            type: object
          status:
            description: EMRServerlessApplicationStatus is the observed state of AWS::EMRServerless::Application.
            properties:
              applicationId:
                description: |-
                  The ID of the EMR Serverless Application.
                  Read-only: reported by AWS after creation.
                maxLength: 64
                type: string
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the EMR Serverless Application.
                  Read-only: reported by AWS after creation.
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
