---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: rbinrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: RbinRule
    listKind: RbinRuleList
    plural: rbinrules
    singular: rbinrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          RbinRule manages AWS::Rbin::Rule through the AWS Cloud Control API. Resource Type definition for
          AWS::Rbin::Rule
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RbinRuleSpec is the desired state of AWS::Rbin::Rule.
            properties:
              description:
                description: The description of the retention rule.
                maxLength: 255
                type: string
              excludeResourceTags:
                description: |-
                  Information about the exclude resource tags used to identify resources that are excluded by the retention
                  rule.
                items:
                  description: RbinRuleResourceTag is a nested property type of AWS::Rbin::Rule.
                  properties:
                    resourceTagKey:
                      description: The tag key of the resource.
                      maxLength: 128
                      minLength: 1
                      type: string
                    resourceTagValue:
                      description: The tag value of the resource
                      maxLength: 256
                      type: string
                  required:
                  - resourceTagKey
                  - resourceTagValue
                  type: object
                type: array
              lockConfiguration:
                description: Information about the retention rule lock configuration.
                properties:
                  unlockDelayUnit:
                    description: |-
                      The unit of time in which to measure the unlock delay. Currently, the unlock delay can be measure only in
                      days.
                    enum:
                    - DAYS
                    type: string
                  unlockDelayValue:
                    description: The unlock delay period, measured in the unit specified
                      for UnlockDelayUnit.
                    format: int64
                    maximum: 30
                    minimum: 7
                    type: integer
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              resourceTags:
                description: Information about the resource tags used to identify
                  resources that are retained by the retention rule.
                items:
                  description: RbinRuleResourceTag is a nested property type of AWS::Rbin::Rule.
                  properties:
                    resourceTagKey:
                      description: The tag key of the resource.
                      maxLength: 128
                      minLength: 1
                      type: string
                    resourceTagValue:
                      description: The tag value of the resource
                      maxLength: 256
                      type: string
                  required:
                  - resourceTagKey
                  - resourceTagValue
                  type: object
                type: array
              resourceType:
                description: |-
                  The resource type retained by the retention rule.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - EBS_SNAPSHOT
                - EC2_IMAGE
                - EBS_VOLUME
                type: string
                x-kubernetes-validations:
                - message: resourceType is immutable
                  rule: self == oldSelf
              retentionPeriod:
                description: Information about the retention period for which the
                  retention rule is to retain resources.
                properties:
                  retentionPeriodUnit:
                    description: The retention period unit of the rule
                    enum:
                    - DAYS
                    type: string
                  retentionPeriodValue:
                    description: The retention period value of the rule.
                    format: int64
                    maximum: 3650
                    minimum: 1
                    type: integer
                required:
                - retentionPeriodUnit
                - retentionPeriodValue
                type: object
              status:
                description: The state of the retention rule. Only retention rules
                  that are in the available state retain resources.
                type: string
              tags:
                description: Information about the tags assigned to the retention
                  rule.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - resourceType
            - retentionPeriod
            type: object
          status:
            description: RbinRuleStatus is the observed state of AWS::Rbin::Rule.
            properties:
              arn:
                description: |-
                  Rule Arn is unique for each rule.
                  Read-only: reported by AWS after creation.
                maxLength: 1011
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lockState:
                description: |-
                  The lock state for the retention rule.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
