---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: resiliencehubapps.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ResilienceHubApp
    listKind: ResilienceHubAppList
    plural: resiliencehubapps
    singular: resiliencehubapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ResilienceHubApp manages AWS::ResilienceHub::App through the AWS Cloud Control API. Resource Type Definition
          for AWS::ResilienceHub::App.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResilienceHubAppSpec is the desired state of AWS::ResilienceHub::App.
            properties:
              appAssessmentSchedule:
                description: Assessment execution schedule.
                enum:
                - Disabled
                - Daily
                type: string
              appTemplateBody:
                description: A string containing full ResilienceHub app template body.
                maxLength: 409600
                type: string
              description:
                description: App description.
                maxLength: 500
                type: string
              eventSubscriptions:
                description: The list of events you would like to subscribe and get
                  notification for.
                items:
                  description: ResilienceHubAppEventSubscription is a nested property
                    type of AWS::ResilienceHub::App.
                  properties:
                    eventType:
                      description: The type of event you would like to subscribe and
                        get notification for.
                      enum:
                      - ScheduledAssessmentFailure
                      - DriftDetected
                      type: string
                    name:
                      description: Unique name to identify an event subscription.
                      maxLength: 256
                      type: string
                    snsTopicArn:
                      description: Amazon Resource Name (ARN) of the Amazon Simple
                        Notification Service topic.
                      type: string
                  required:
                  - eventType
                  - name
                  type: object
                type: array
              name:
                description: |-
                  Name of the app.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              permissionModel:
                description: ResilienceHubAppPermissionModel is a nested property
                  type of AWS::ResilienceHub::App.
                properties:
                  crossAccountRoleArns:
                    description: |-
                      Defines a list of role Amazon Resource Names (ARNs) to be used in other accounts. These ARNs are used for
                      querying purposes while importing resources and assessing your application.
                    items:
                      type: string
                    type: array
                  invokerRoleName:
                    description: |-
                      Existing AWS IAM role name in the primary AWS account that will be assumed by AWS Resilience Hub Service
                      Principle to obtain a read-only access to your application resources while running an assessment.
                    type: string
                  type:
                    description: |-
                      Defines how AWS Resilience Hub scans your resources. It can scan for the resources by using a pre-existing
                      role in your AWS account, or by using the credentials of the current IAM user.
                    enum:
                    - LegacyIAMUser
                    - RoleBased
                    type: string
                required:
                - type
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              resiliencyPolicyArn:
                description: Amazon Resource Name (ARN) of the Resiliency Policy.
                type: string
              resourceMappings:
                description: An array of ResourceMapping objects.
                items:
                  description: ResilienceHubAppResourceMapping is a nested property
                    type of AWS::ResilienceHub::App.
                  properties:
                    eksSourceName:
                      type: string
                    logicalStackName:
                      type: string
                    mappingType:
                      type: string
                    physicalResourceId:
                      description: ResilienceHubAppPhysicalResourceId is a nested
                        property type of AWS::ResilienceHub::App.
                      properties:
                        awsAccountId:
                          type: string
                        awsRegion:
                          type: string
                        identifier:
                          maxLength: 255
                          minLength: 1
                          type: string
                        type:
                          type: string
                      required:
                      - identifier
                      - type
                      type: object
                    resourceName:
                      type: string
                    terraformSourceName:
                      type: string
                  required:
                  - mappingType
                  - physicalResourceId
                  type: object
                type: array
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - appTemplateBody
            - name
            - resourceMappings
            type: object
          status:
            description: ResilienceHubAppStatus is the observed state of AWS::ResilienceHub::App.
            properties:
              appArn:
                description: |-
                  Amazon Resource Name (ARN) of the App.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              driftStatus:
                description: |-
                  Indicates if compliance drifts (deviations) were detected while running an assessment for your application.
                  Read-only: reported by AWS after creation.
                enum:
                - NotChecked
                - NotDetected
                - Detected
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: resiliencehubresiliencypolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ResilienceHubResiliencyPolicy
    listKind: ResilienceHubResiliencyPolicyList
    plural: resiliencehubresiliencypolicies
    singular: resiliencehubresiliencypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ResilienceHubResiliencyPolicy manages AWS::ResilienceHub::ResiliencyPolicy through the AWS Cloud Control API.
          Resource Type Definition for Resiliency Policy.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ResilienceHubResiliencyPolicySpec is the desired state of
              AWS::ResilienceHub::ResiliencyPolicy.
            properties:
              dataLocationConstraint:
                description: Data Location Constraint of the Policy.
                enum:
                - AnyLocation
                - SameContinent
                - SameCountry
                type: string
              policy:
                description: ResilienceHubResiliencyPolicyPolicyMap is a nested property
                  type of AWS::ResilienceHub::ResiliencyPolicy.
                properties:
                  az:
                    description: ResilienceHubResiliencyPolicyFailurePolicy is a nested
                      property type of AWS::ResilienceHub::ResiliencyPolicy.
                    properties:
                      rpoInSecs:
                        description: RPO in seconds.
                        format: int64
                        type: integer
                      rtoInSecs:
                        description: RTO in seconds.
                        format: int64
                        type: integer
                    required:
                    - rpoInSecs
                    - rtoInSecs
                    type: object
                  hardware:
                    description: ResilienceHubResiliencyPolicyFailurePolicy is a nested
                      property type of AWS::ResilienceHub::ResiliencyPolicy.
                    properties:
                      rpoInSecs:
                        description: RPO in seconds.
                        format: int64
                        type: integer
                      rtoInSecs:
                        description: RTO in seconds.
                        format: int64
                        type: integer
                    required:
                    - rpoInSecs
                    - rtoInSecs
                    type: object
                  region:
                    description: ResilienceHubResiliencyPolicyFailurePolicy is a nested
                      property type of AWS::ResilienceHub::ResiliencyPolicy.
                    properties:
                      rpoInSecs:
                        description: RPO in seconds.
                        format: int64
                        type: integer
                      rtoInSecs:
                        description: RTO in seconds.
                        format: int64
                        type: integer
                    required:
                    - rpoInSecs
                    - rtoInSecs
                    type: object
                  software:
                    description: ResilienceHubResiliencyPolicyFailurePolicy is a nested
                      property type of AWS::ResilienceHub::ResiliencyPolicy.
                    properties:
                      rpoInSecs:
                        description: RPO in seconds.
                        format: int64
                        type: integer
                      rtoInSecs:
                        description: RTO in seconds.
                        format: int64
                        type: integer
                    required:
                    - rpoInSecs
                    - rtoInSecs
                    type: object
                required:
                - az
                - hardware
                - software
                type: object
              policyDescription:
                description: Description of Resiliency Policy.
                maxLength: 500
                type: string
              policyName:
                description: Name of Resiliency Policy.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
              tier:
                description: Resiliency Policy Tier.
                enum:
                - MissionCritical
                - Critical
                - Important
                - CoreServices
                - NonCritical
                type: string
            required:
            - policy
            - policyName
            - tier
            type: object
          status:
            description: ResilienceHubResiliencyPolicyStatus is the observed state
              of AWS::ResilienceHub::ResiliencyPolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              policyArn:
                description: |-
                  Amazon Resource Name (ARN) of the Resiliency Policy.
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
