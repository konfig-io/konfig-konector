---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cassandrakeyspaces.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: CassandraKeyspace
    listKind: CassandraKeyspaceList
    plural: cassandrakeyspaces
    singular: cassandrakeyspace
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CassandraKeyspace manages AWS::Cassandra::Keyspace through the AWS Cloud Control API. Resource schema for
          AWS::Cassandra::Keyspace
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CassandraKeyspaceSpec is the desired state of AWS::Cassandra::Keyspace.
            properties:
              clientSideTimestampsEnabled:
                description: |-
                  Indicates whether client-side timestamps are enabled (true) or disabled (false) for all tables in the
                  keyspace. To add a Region to a single-Region keyspace with at least one table, the value must be set to true.
                  After you enabled client-side timestamps for a table, you can’t disable it again.
                type: boolean
              keyspaceName:
                description: |-
                  Name for Cassandra keyspace
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              replicationSpecification:
                description: CassandraKeyspaceReplicationSpecification is a nested
                  property type of AWS::Cassandra::Keyspace.
                properties:
                  regionList:
                    items:
                      type: string
                    type: array
                  replicationStrategy:
                    enum:
                    - SINGLE_REGION
                    - MULTI_REGION
                    type: string
                type: object
              tags:
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            type: object
          status:
            description: CassandraKeyspaceStatus is the observed state of AWS::Cassandra::Keyspace.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cassandratables.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: CassandraTable
    listKind: CassandraTableList
    plural: cassandratables
    singular: cassandratable
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CassandraTable manages AWS::Cassandra::Table through the AWS Cloud Control API. Resource schema for
          AWS::Cassandra::Table
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CassandraTableSpec is the desired state of AWS::Cassandra::Table.
            properties:
              autoScalingSpecifications:
                description: CassandraTableAutoScalingSpecification is a nested property
                  type of AWS::Cassandra::Table.
                properties:
                  readCapacityAutoScaling:
                    description: CassandraTableAutoScalingSetting is a nested property
                      type of AWS::Cassandra::Table.
                    properties:
                      autoScalingDisabled:
                        type: boolean
                      maximumUnits:
                        format: int64
                        minimum: 1
                        type: integer
                      minimumUnits:
                        format: int64
                        minimum: 1
                        type: integer
                      scalingPolicy:
                        description: CassandraTableScalingPolicy is a nested property
                          type of AWS::Cassandra::Table.
                        properties:
                          targetTrackingScalingPolicyConfiguration:
                            description: CassandraTableTargetTrackingScalingPolicyConfiguration
                              is a nested property type of AWS::Cassandra::Table.
                            properties:
                              disableScaleIn:
                                type: boolean
                              scaleInCooldown:
                                format: int64
                                type: integer
                              scaleOutCooldown:
                                format: int64
                                type: integer
                              targetValue:
                                format: int64
                                type: integer
                            required:
                            - targetValue
                            type: object
                        type: object
                    type: object
                  writeCapacityAutoScaling:
                    description: CassandraTableAutoScalingSetting is a nested property
                      type of AWS::Cassandra::Table.
                    properties:
                      autoScalingDisabled:
                        type: boolean
                      maximumUnits:
                        format: int64
                        minimum: 1
                        type: integer
                      minimumUnits:
                        format: int64
                        minimum: 1
                        type: integer
                      scalingPolicy:
                        description: CassandraTableScalingPolicy is a nested property
                          type of AWS::Cassandra::Table.
                        properties:
                          targetTrackingScalingPolicyConfiguration:
                            description: CassandraTableTargetTrackingScalingPolicyConfiguration
                              is a nested property type of AWS::Cassandra::Table.
                            properties:
                              disableScaleIn:
                                type: boolean
                              scaleInCooldown:
                                format: int64
                                type: integer
                              scaleOutCooldown:
                                format: int64
                                type: integer
                              targetValue:
                                format: int64
                                type: integer
                            required:
                            - targetValue
                            type: object
                        type: object
                    type: object
                type: object
              billingMode:
                description: CassandraTableBillingMode is a nested property type of
                  AWS::Cassandra::Table.
                properties:
                  mode:
                    enum:
                    - PROVISIONED
                    - ON_DEMAND
                    type: string
                  provisionedThroughput:
                    description: CassandraTableProvisionedThroughput is a nested property
                      type of AWS::Cassandra::Table.
                    properties:
                      readCapacityUnits:
                        format: int64
                        minimum: 1
                        type: integer
                      writeCapacityUnits:
                        format: int64
                        minimum: 1
                        type: integer
                    required:
                    - readCapacityUnits
                    - writeCapacityUnits
                    type: object
                required:
                - mode
                type: object
              cdcSpecification:
                description: CassandraTableCdcSpecification is a nested property type
                  of AWS::Cassandra::Table.
                properties:
                  status:
                    enum:
                    - ENABLED
                    - DISABLED
                    type: string
                  tags:
                    description: An array of key-value pairs to apply to the CDC stream
                      resource
                    items:
                      description: CFNTag is the CloudFormation Tag shape shared by
                        generated kinds.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        value:
                          type: string
                      required:
                      - key
                      - value
                      type: object
                    type: array
                  viewType:
                    enum:
                    - NEW_IMAGE
                    - OLD_IMAGE
                    - KEYS_ONLY
                    - NEW_AND_OLD_IMAGES
                    type: string
                required:
                - status
                type: object
              clientSideTimestampsEnabled:
                description: |-
                  Indicates whether client side timestamps are enabled (true) or disabled (false) on the table. False by
                  default, once it is enabled it cannot be disabled again.
                  Create-only in AWS: changing it requires replacing the resource.
                type: boolean
              clusteringKeyColumns:
                description: |-
                  Clustering key columns of the table
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CassandraTableClusteringKeyColumn is a nested property
                    type of AWS::Cassandra::Table.
                  properties:
                    column:
                      description: CassandraTableColumn is a nested property type
                        of AWS::Cassandra::Table.
                      properties:
                        columnName:
                          type: string
                        columnType:
                          type: string
                      required:
                      - columnName
                      - columnType
                      type: object
                    orderBy:
                      enum:
                      - ASC
                      - DESC
                      type: string
                  required:
                  - column
                  type: object
                type: array
              defaultTimeToLive:
                description: |-
                  Default TTL (Time To Live) in seconds, where zero is disabled. If the value is greater than zero, TTL is
                  enabled for the entire table and an expiration timestamp is added to each column.
                format: int64
                minimum: 0
                type: integer
              encryptionSpecification:
                description: CassandraTableEncryptionSpecification is a nested property
                  type of AWS::Cassandra::Table.
                properties:
                  encryptionType:
                    enum:
                    - AWS_OWNED_KMS_KEY
                    - CUSTOMER_MANAGED_KMS_KEY
                    type: string
                  kmsKeyIdentifier:
                    type: string
                required:
                - encryptionType
                type: object
              keyspaceName:
                description: |-
                  Name for Cassandra keyspace
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: keyspaceName is immutable
                  rule: self == oldSelf
              partitionKeyColumns:
                description: |-
                  Partition key columns of the table
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CassandraTableColumn is a nested property type of AWS::Cassandra::Table.
                  properties:
                    columnName:
                      type: string
                    columnType:
                      type: string
                  required:
                  - columnName
                  - columnType
                  type: object
                type: array
              pointInTimeRecoveryEnabled:
                description: Indicates whether point in time recovery is enabled (true)
                  or disabled (false) on the table
                type: boolean
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              regularColumns:
                description: Non-key columns of the table
                items:
                  description: CassandraTableColumn is a nested property type of AWS::Cassandra::Table.
                  properties:
                    columnName:
                      type: string
                    columnType:
                      type: string
                  required:
                  - columnName
                  - columnType
                  type: object
                type: array
              replicaSpecifications:
                items:
                  description: CassandraTableReplicaSpecification is a nested property
                    type of AWS::Cassandra::Table.
                  properties:
                    readCapacityAutoScaling:
                      description: CassandraTableAutoScalingSetting is a nested property
                        type of AWS::Cassandra::Table.
                      properties:
                        autoScalingDisabled:
                          type: boolean
                        maximumUnits:
                          format: int64
                          minimum: 1
                          type: integer
                        minimumUnits:
                          format: int64
                          minimum: 1
                          type: integer
                        scalingPolicy:
                          description: CassandraTableScalingPolicy is a nested property
                            type of AWS::Cassandra::Table.
                          properties:
                            targetTrackingScalingPolicyConfiguration:
                              description: CassandraTableTargetTrackingScalingPolicyConfiguration
                                is a nested property type of AWS::Cassandra::Table.
                              properties:
                                disableScaleIn:
                                  type: boolean
                                scaleInCooldown:
                                  format: int64
                                  type: integer
                                scaleOutCooldown:
                                  format: int64
                                  type: integer
                                targetValue:
                                  format: int64
                                  type: integer
                              required:
                              - targetValue
                              type: object
                          type: object
                      type: object
                    readCapacityUnits:
                      format: int64
                      type: integer
                    region:
                      maxLength: 25
                      minLength: 2
                      type: string
                  required:
                  - region
                  type: object
                type: array
              tableName:
                description: |-
                  Name for Cassandra table
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              tags:
                description: An array of key-value pairs to apply to this resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              warmThroughput:
                description: CassandraTableWarmThroughput is a nested property type
                  of AWS::Cassandra::Table.
                properties:
                  readUnitsPerSecond:
                    format: int64
                    minimum: 1
                    type: integer
                  writeUnitsPerSecond:
                    format: int64
                    minimum: 1
                    type: integer
                type: object
            required:
            - keyspaceName
            - partitionKeyColumns
            type: object
          status:
            description: CassandraTableStatus is the observed state of AWS::Cassandra::Table.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cassandratypes.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: CassandraType
    listKind: CassandraTypeList
    plural: cassandratypes
    singular: cassandratype
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CassandraType manages AWS::Cassandra::Type through the AWS Cloud Control API. Resource schema for
          AWS::Cassandra::Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CassandraTypeSpec is the desired state of AWS::Cassandra::Type.
            properties:
              fields:
                description: |-
                  Field definitions of the User-Defined Type
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CassandraTypeField is a nested property type of AWS::Cassandra::Type.
                  properties:
                    fieldName:
                      type: string
                    fieldType:
                      type: string
                  required:
                  - fieldName
                  - fieldType
                  type: object
                type: array
              keyspaceName:
                description: |-
                  Name of the Keyspace which contains the User-Defined Type.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: keyspaceName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              typeName:
                description: |-
                  Name of the User-Defined Type.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: typeName is immutable
                  rule: self == oldSelf
            required:
            - fields
            - keyspaceName
            - typeName
            type: object
          status:
            description: CassandraTypeStatus is the observed state of AWS::Cassandra::Type.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              directParentTypes:
                description: |-
                  List of parent User-Defined Types that directly reference the User-Defined Type in their fields.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              directReferringTables:
                description: |-
                  List of Tables that directly reference the User-Defined Type in their columns.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              keyspaceArn:
                description: |-
                  ARN of the Keyspace which contains the User-Defined Type.
                  Read-only: reported by AWS after creation.
                type: string
              lastModifiedTimestamp:
                description: |-
                  Timestamp of the last time the User-Defined Type's meta data was modified.
                  Read-only: reported by AWS after creation.
                type: number
              lastSyncTime:
                format: date-time
                type: string
              maxNestingDepth:
                description: |-
                  Maximum nesting depth of the User-Defined Type across the field types.
                  Read-only: reported by AWS after creation.
                format: int64
                type: integer
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
