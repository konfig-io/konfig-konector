---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakeractions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerAction
    listKind: SageMakerActionList
    plural: sagemakeractions
    singular: sagemakeraction
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerAction manages AWS::SageMaker::Action through the AWS Cloud Control API. Resource type definition for
          AWS::SageMaker::Action. An action is a lineage tracking entity that represents an action or activity, such as
          a model deployment or an HPO job.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerActionSpec is the desired state of AWS::SageMaker::Action.
            properties:
              actionName:
                description: |-
                  The name of the action. Must be unique to your account in an AWS Region.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 120
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: actionName is immutable
                  rule: self == oldSelf
              actionType:
                description: |-
                  The action type.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                type: string
                x-kubernetes-validations:
                - message: actionType is immutable
                  rule: self == oldSelf
              description:
                description: The description of the action.
                maxLength: 3072
                type: string
              metadataProperties:
                description: |-
                  Metadata properties of the tracking entity, trial, or trial component.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  commitId:
                    description: The commit ID.
                    maxLength: 1024
                    type: string
                  generatedBy:
                    description: The entity this entity was generated by.
                    maxLength: 1024
                    type: string
                  projectId:
                    description: The project ID.
                    maxLength: 1024
                    type: string
                  repository:
                    description: The repository.
                    maxLength: 1024
                    type: string
                type: object
              properties:
                additionalProperties:
                  type: string
                description: A list of properties to add to the action.
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              source:
                description: |-
                  The source type, ID, and URI.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  sourceId:
                    description: The ID of the source.
                    maxLength: 256
                    type: string
                  sourceType:
                    description: The type of the source.
                    maxLength: 256
                    type: string
                  sourceUri:
                    description: The URI of the source.
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - sourceUri
                type: object
              status:
                description: The status of the action.
                enum:
                - Unknown
                - InProgress
                - Completed
                - Failed
                - Stopping
                - Stopped
                type: string
              tags:
                description: A list of tags to apply to the action.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - actionName
            - actionType
            - source
            type: object
          status:
            description: SageMakerActionStatus is the observed state of AWS::SageMaker::Action.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the action.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  When the action was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  When the action was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakeralgorithms.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerAlgorithm
    listKind: SageMakerAlgorithmList
    plural: sagemakeralgorithms
    singular: sagemakeralgorithm
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerAlgorithm manages AWS::SageMaker::Algorithm through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::Algorithm
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerAlgorithmSpec is the desired state of AWS::SageMaker::Algorithm.
            properties:
              algorithmDescription:
                description: |-
                  A description of the algorithm.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 1024
                type: string
              algorithmName:
                description: |-
                  The name of the algorithm.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: algorithmName is immutable
                  rule: self == oldSelf
              certifyForMarketplace:
                description: |-
                  Whether to certify the algorithm so that it can be listed in AWS Marketplace.
                  Create-only in AWS: changing it requires replacing the resource.
                type: boolean
              inferenceSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  containers:
                    items:
                      description: SageMakerAlgorithmModelPackageContainerDefinition
                        is a nested property type of AWS::SageMaker::Algorithm.
                      properties:
                        containerHostname:
                          maxLength: 63
                          type: string
                        environment:
                          additionalProperties:
                            type: string
                          type: object
                        framework:
                          type: string
                        frameworkVersion:
                          maxLength: 10
                          type: string
                        image:
                          maxLength: 255
                          type: string
                        imageDigest:
                          maxLength: 72
                          type: string
                        isCheckpoint:
                          type: boolean
                        modelInput:
                          description: SageMakerAlgorithmModelInput is a nested property
                            type of AWS::SageMaker::Algorithm.
                          properties:
                            dataInputConfig:
                              type: string
                          required:
                          - dataInputConfig
                          type: object
                        nearestModelName:
                          type: string
                      required:
                      - image
                      type: object
                    type: array
                  supportedContentTypes:
                    items:
                      type: string
                    type: array
                  supportedRealtimeInferenceInstanceTypes:
                    items:
                      type: string
                    type: array
                  supportedResponseMIMETypes:
                    items:
                      type: string
                    type: array
                  supportedTransformInstanceTypes:
                    items:
                      type: string
                    type: array
                required:
                - containers
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              trainingSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  metricDefinitions:
                    items:
                      description: SageMakerAlgorithmMetricDefinition is a nested
                        property type of AWS::SageMaker::Algorithm.
                      properties:
                        name:
                          maxLength: 255
                          minLength: 1
                          type: string
                        regex:
                          maxLength: 500
                          minLength: 1
                          type: string
                      required:
                      - name
                      - regex
                      type: object
                    type: array
                  supportedHyperParameters:
                    items:
                      description: SageMakerAlgorithmHyperParameterSpecification is
                        a nested property type of AWS::SageMaker::Algorithm.
                      properties:
                        defaultValue:
                          maxLength: 2500
                          type: string
                        description:
                          maxLength: 1024
                          type: string
                        isRequired:
                          type: boolean
                        isTunable:
                          type: boolean
                        name:
                          maxLength: 256
                          type: string
                        range:
                          description: SageMakerAlgorithmParameterRange is a nested
                            property type of AWS::SageMaker::Algorithm.
                          properties:
                            categoricalParameterRangeSpecification:
                              description: SageMakerAlgorithmCategoricalParameterRangeSpecification
                                is a nested property type of AWS::SageMaker::Algorithm.
                              properties:
                                values:
                                  items:
                                    type: string
                                  type: array
                              required:
                              - values
                              type: object
                            continuousParameterRangeSpecification:
                              description: SageMakerAlgorithmContinuousParameterRangeSpecification
                                is a nested property type of AWS::SageMaker::Algorithm.
                              properties:
                                maxValue:
                                  maxLength: 256
                                  type: string
                                minValue:
                                  maxLength: 256
                                  type: string
                              required:
                              - maxValue
                              - minValue
                              type: object
                            integerParameterRangeSpecification:
                              description: SageMakerAlgorithmIntegerParameterRangeSpecification
                                is a nested property type of AWS::SageMaker::Algorithm.
                              properties:
                                maxValue:
                                  maxLength: 256
                                  type: string
                                minValue:
                                  maxLength: 256
                                  type: string
                              required:
                              - maxValue
                              - minValue
                              type: object
                          type: object
                        type:
                          enum:
                          - Integer
                          - Continuous
                          - Categorical
                          - FreeText
                          type: string
                      required:
                      - name
                      - type
                      type: object
                    type: array
                  supportedTrainingInstanceTypes:
                    items:
                      type: string
                    type: array
                  supportedTuningJobObjectiveMetrics:
                    items:
                      description: SageMakerAlgorithmHyperParameterTuningJobObjective
                        is a nested property type of AWS::SageMaker::Algorithm.
                      properties:
                        metricName:
                          maxLength: 255
                          minLength: 1
                          type: string
                        type:
                          enum:
                          - Maximize
                          - Minimize
                          type: string
                      required:
                      - metricName
                      - type
                      type: object
                    type: array
                  supportsDistributedTraining:
                    type: boolean
                  trainingChannels:
                    items:
                      description: SageMakerAlgorithmChannelSpecification is a nested
                        property type of AWS::SageMaker::Algorithm.
                      properties:
                        description:
                          maxLength: 1024
                          type: string
                        isRequired:
                          type: boolean
                        name:
                          maxLength: 64
                          minLength: 1
                          type: string
                        supportedCompressionTypes:
                          items:
                            type: string
                          type: array
                        supportedContentTypes:
                          items:
                            type: string
                          type: array
                        supportedInputModes:
                          items:
                            type: string
                          type: array
                      required:
                      - name
                      - supportedContentTypes
                      - supportedInputModes
                      type: object
                    type: array
                  trainingImage:
                    maxLength: 255
                    type: string
                  trainingImageDigest:
                    maxLength: 72
                    type: string
                required:
                - supportedTrainingInstanceTypes
                - trainingChannels
                - trainingImage
                type: object
            required:
            - algorithmName
            - trainingSpecification
            type: object
          status:
            description: SageMakerAlgorithmStatus is the observed state of AWS::SageMaker::Algorithm.
            properties:
              algorithmArn:
                description: |-
                  The Amazon Resource Name (ARN) of the algorithm.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  A timestamp specifying when the algorithm was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerappimageconfigs.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerAppImageConfig
    listKind: SageMakerAppImageConfigList
    plural: sagemakerappimageconfigs
    singular: sagemakerappimageconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerAppImageConfig manages AWS::SageMaker::AppImageConfig through the AWS Cloud Control API. Resource
          Type definition for AWS::SageMaker::AppImageConfig
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerAppImageConfigSpec is the desired state of AWS::SageMaker::AppImageConfig.
            properties:
              appImageConfigName:
                description: |-
                  The Name of the AppImageConfig.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: appImageConfigName is immutable
                  rule: self == oldSelf
              codeEditorAppImageConfig:
                description: The CodeEditorAppImageConfig.
                properties:
                  containerConfig:
                    description: The container configuration for a SageMaker image.
                    properties:
                      containerArguments:
                        description: A list of arguments to apply to the container.
                        items:
                          type: string
                        type: array
                      containerEntrypoint:
                        description: The custom entry point to use on container.
                        items:
                          type: string
                        type: array
                      containerEnvironmentVariables:
                        description: A list of variables to apply to the custom container.
                        items:
                          description: CFNTag is the CloudFormation Tag shape shared
                            by generated kinds.
                          properties:
                            key:
                              minLength: 1
                              type: string
                            value:
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                    type: object
                type: object
              jupyterLabAppImageConfig:
                description: The JupyterLabAppImageConfig.
                properties:
                  containerConfig:
                    description: The container configuration for a SageMaker image.
                    properties:
                      containerArguments:
                        description: A list of arguments to apply to the container.
                        items:
                          type: string
                        type: array
                      containerEntrypoint:
                        description: The custom entry point to use on container.
                        items:
                          type: string
                        type: array
                      containerEnvironmentVariables:
                        description: A list of variables to apply to the custom container.
                        items:
                          description: CFNTag is the CloudFormation Tag shape shared
                            by generated kinds.
                          properties:
                            key:
                              minLength: 1
                              type: string
                            value:
                              type: string
                          required:
                          - key
                          - value
                          type: object
                        type: array
                    type: object
                type: object
              kernelGatewayImageConfig:
                description: The KernelGatewayImageConfig.
                properties:
                  fileSystemConfig:
                    description: The Amazon Elastic File System (EFS) storage configuration
                      for a SageMaker image.
                    properties:
                      defaultGid:
                        description: The default POSIX group ID (GID). If not specified,
                          defaults to 100.
                        format: int64
                        maximum: 65535
                        minimum: 0
                        type: integer
                      defaultUid:
                        description: The default POSIX user ID (UID). If not specified,
                          defaults to 1000.
                        format: int64
                        maximum: 65535
                        minimum: 0
                        type: integer
                      mountPath:
                        description: |-
                          The path within the image to mount the user's EFS home directory. The directory should be empty. If not
                          specified, defaults to /home/sagemaker-user.
                        maxLength: 1024
                        type: string
                    type: object
                  kernelSpecs:
                    description: The specification of the Jupyter kernels in the image.
                    items:
                      description: SageMakerAppImageConfigKernelSpec is a nested property
                        type of AWS::SageMaker::AppImageConfig.
                      properties:
                        displayName:
                          description: The display name of the kernel.
                          maxLength: 1024
                          type: string
                        name:
                          description: The name of the kernel.
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                required:
                - kernelSpecs
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: A list of tags to apply to the AppImageConfig.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - appImageConfigName
            type: object
          status:
            description: SageMakerAppImageConfigStatus is the observed state of AWS::SageMaker::AppImageConfig.
            properties:
              appImageConfigArn:
                description: |-
                  The Amazon Resource Name (ARN) of the AppImageConfig.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerapps.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerApp
    listKind: SageMakerAppList
    plural: sagemakerapps
    singular: sagemakerapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerApp manages AWS::SageMaker::App through the AWS Cloud Control API. Resource Type definition for
          AWS::SageMaker::App
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerAppSpec is the desired state of AWS::SageMaker::App.
            properties:
              appName:
                description: |-
                  The name of the app.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: appName is immutable
                  rule: self == oldSelf
              appType:
                description: |-
                  The type of app.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - JupyterServer
                - KernelGateway
                - RStudioServerPro
                - RSessionGateway
                - Canvas
                type: string
                x-kubernetes-validations:
                - message: appType is immutable
                  rule: self == oldSelf
              domainId:
                description: |-
                  The domain ID.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: domainId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              recoveryMode:
                description: Indicates whether the application is launched in recovery
                  mode.
                type: boolean
              resourceSpec:
                description: |-
                  The instance type and the Amazon Resource Name (ARN) of the SageMaker image created on the instance.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  instanceType:
                    description: The instance type that the image version runs on.
                    type: string
                  lifecycleConfigArn:
                    description: The Amazon Resource Name (ARN) of the Lifecycle Configuration
                      to attach to the Resource.
                    maxLength: 256
                    type: string
                  sageMakerImageArn:
                    description: The ARN of the SageMaker image that the image version
                      belongs to.
                    maxLength: 256
                    type: string
                  sageMakerImageVersionArn:
                    description: The ARN of the image version created on the instance.
                    maxLength: 256
                    type: string
                type: object
              tags:
                description: A list of tags to apply to the app.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              userProfileName:
                description: |-
                  The user profile name.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: userProfileName is immutable
                  rule: self == oldSelf
            required:
            - appName
            - appType
            - domainId
            - userProfileName
            type: object
          status:
            description: SageMakerAppStatus is the observed state of AWS::SageMaker::App.
            properties:
              appArn:
                description: |-
                  The Amazon Resource Name (ARN) of the app.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              builtInLifecycleConfigArn:
                description: |-
                  The lifecycle configuration that runs before the default lifecycle configuration.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerartifacts.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerArtifact
    listKind: SageMakerArtifactList
    plural: sagemakerartifacts
    singular: sagemakerartifact
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerArtifact manages AWS::SageMaker::Artifact through the AWS Cloud Control API. Resource type definition
          for AWS::SageMaker::Artifact. An artifact is a lineage tracking entity that represents a URI addressable
          object or data, such as an S3 URI of a dataset or the ECR registry path of an image.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerArtifactSpec is the desired state of AWS::SageMaker::Artifact.
            properties:
              artifactName:
                description: The name of the artifact. Must be unique to your account
                  in an AWS Region.
                maxLength: 120
                type: string
              artifactType:
                description: |-
                  The artifact type.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                type: string
                x-kubernetes-validations:
                - message: artifactType is immutable
                  rule: self == oldSelf
              metadataProperties:
                description: |-
                  Metadata properties of the tracking entity, trial, or trial component.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  commitId:
                    description: The commit ID.
                    maxLength: 1024
                    type: string
                  generatedBy:
                    description: The entity this entity was generated by.
                    maxLength: 1024
                    type: string
                  projectId:
                    description: The project ID.
                    maxLength: 1024
                    type: string
                  repository:
                    description: The repository.
                    maxLength: 1024
                    type: string
                type: object
              properties:
                additionalProperties:
                  type: string
                description: A list of properties to add to the artifact.
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              source:
                description: |-
                  The source of the artifact.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  sourceTypes:
                    description: A list of source types.
                    items:
                      description: SageMakerArtifactArtifactSourceType is a nested
                        property type of AWS::SageMaker::Artifact.
                      properties:
                        sourceIdType:
                          description: The type of ID.
                          enum:
                          - MD5Hash
                          - S3ETag
                          - S3Version
                          - Custom
                          type: string
                        value:
                          description: The ID.
                          maxLength: 256
                          type: string
                      required:
                      - sourceIdType
                      - value
                      type: object
                    type: array
                  sourceUri:
                    description: The URI of the source.
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - sourceUri
                type: object
              tags:
                description: A list of tags to apply to the artifact.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - artifactType
            - source
            type: object
          status:
            description: SageMakerArtifactStatus is the observed state of AWS::SageMaker::Artifact.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the artifact.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  When the artifact was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  When the artifact was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerclusters.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerCluster
    listKind: SageMakerClusterList
    plural: sagemakerclusters
    singular: sagemakercluster
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerCluster manages AWS::SageMaker::Cluster through the AWS Cloud Control API. Resource Type definition
          for AWS::SageMaker::Cluster
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerClusterSpec is the desired state of AWS::SageMaker::Cluster.
            properties:
              autoScaling:
                description: SageMakerClusterClusterAutoScalingConfig is a nested
                  property type of AWS::SageMaker::Cluster.
                properties:
                  autoScalerType:
                    description: The type of auto-scaler to use
                    enum:
                    - Karpenter
                    type: string
                  mode:
                    description: The auto-scaling mode for the cluster
                    enum:
                    - Enable
                    - Disable
                    type: string
                required:
                - mode
                type: object
              clusterName:
                description: |-
                  The name of the HyperPod Cluster.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                type: string
              clusterRole:
                description: The cluster role for the autoscaler to assume.
                maxLength: 2048
                type: string
              instanceGroups:
                items:
                  description: SageMakerClusterClusterInstanceGroup is a nested property
                    type of AWS::SageMaker::Cluster.
                  properties:
                    autoPatchConfig:
                      description: SageMakerClusterAutoPatchConfig is a nested property
                        type of AWS::SageMaker::Cluster.
                      properties:
                        deploymentConfig:
                          description: SageMakerClusterDeploymentConfig is a nested
                            property type of AWS::SageMaker::Cluster.
                          properties:
                            autoRollbackConfiguration:
                              items:
                                description: SageMakerClusterAlarmDetails is a nested
                                  property type of AWS::SageMaker::Cluster.
                                properties:
                                  alarmName:
                                    description: The name of the alarm.
                                    maxLength: 256
                                    minLength: 1
                                    type: string
                                required:
                                - alarmName
                                type: object
                              type: array
                            rollingUpdatePolicy:
                              description: SageMakerClusterRollingUpdatePolicy is
                                a nested property type of AWS::SageMaker::Cluster.
                              properties:
                                maximumBatchSize:
                                  description: SageMakerClusterCapacitySizeConfig
                                    is a nested property type of AWS::SageMaker::Cluster.
                                  properties:
                                    type:
                                      description: Specifies whether SageMaker should
                                        process the update by amount or percentage
                                        of instances.
                                      type: string
                                    value:
                                      description: Specifies the amount or percentage
                                        of instances SageMaker updates at a time.
                                      format: int64
                                      minimum: 1
                                      type: integer
                                  required:
                                  - type
                                  - value
                                  type: object
                                rollbackMaximumBatchSize:
                                  description: SageMakerClusterCapacitySizeConfig
                                    is a nested property type of AWS::SageMaker::Cluster.
                                  properties:
                                    type:
                                      description: Specifies whether SageMaker should
                                        process the update by amount or percentage
                                        of instances.
                                      type: string
                                    value:
                                      description: Specifies the amount or percentage
                                        of instances SageMaker updates at a time.
                                      format: int64
                                      minimum: 1
                                      type: integer
                                  required:
                                  - type
                                  - value
                                  type: object
                              required:
                              - maximumBatchSize
                              type: object
                            waitIntervalInSeconds:
                              description: The duration in seconds that SageMaker
                                waits before updating more instances in the cluster.
                              format: int64
                              maximum: 3600
                              minimum: 0
                              type: integer
                          type: object
                        patchSchedule:
                          description: SageMakerClusterPatchSchedule is a nested property
                            type of AWS::SageMaker::Cluster.
                          properties:
                            nextPatchDate:
                              description: The date and time of the next scheduled
                                patch, set by the system when a patch AMI is detected.
                              type: string
                          type: object
                        patchingStrategy:
                          description: |-
                            The patching strategy that determines when and how instances are patched. WhenIdle patches instances as they
                            become idle. WhenAllIdle patches all instances when they are all idle.
                          enum:
                          - WhenIdle
                          - WhenAllIdle
                          type: string
                      required:
                      - patchingStrategy
                      type: object
                    capacityRequirements:
                      description: SageMakerClusterClusterCapacityRequirements is
                        a nested property type of AWS::SageMaker::Cluster.
                      properties:
                        onDemand:
                          x-kubernetes-preserve-unknown-fields: true
                        spot:
                          x-kubernetes-preserve-unknown-fields: true
                      type: object
                    currentCount:
                      description: The number of instances that are currently in the
                        instance group of a SageMaker HyperPod cluster.
                      format: int64
                      minimum: 0
                      type: integer
                    executionRole:
                      maxLength: 2048
                      minLength: 20
                      type: string
                    imageId:
                      maxLength: 21
                      type: string
                    instanceCount:
                      description: The number of instances you specified to add to
                        the instance group of a SageMaker HyperPod cluster.
                      format: int64
                      minimum: 0
                      type: integer
                    instanceGroupName:
                      maxLength: 63
                      minLength: 1
                      type: string
                    instanceRequirements:
                      description: SageMakerClusterInstanceRequirements is a nested
                        property type of AWS::SageMaker::Cluster.
                      properties:
                        instanceTypes:
                          description: A list of instance types that can be used for
                            this instance group.
                          items:
                            type: string
                          type: array
                      required:
                      - instanceTypes
                      type: object
                    instanceStorageConfigs:
                      items:
                        x-kubernetes-preserve-unknown-fields: true
                      type: array
                    instanceType:
                      type: string
                    kubernetesConfig:
                      description: SageMakerClusterClusterKubernetesConfig is a nested
                        property type of AWS::SageMaker::Cluster.
                      properties:
                        labels:
                          additionalProperties:
                            type: string
                          type: object
                        taints:
                          items:
                            description: SageMakerClusterClusterKubernetesTaint is
                              a nested property type of AWS::SageMaker::Cluster.
                            properties:
                              effect:
                                description: The effect of the taint.
                                enum:
                                - NoSchedule
                                - PreferNoSchedule
                                - NoExecute
                                type: string
                              key:
                                description: The key of the taint.
                                type: string
                              value:
                                description: The value of the taint.
                                type: string
                            required:
                            - effect
                            - key
                            type: object
                          type: array
                      type: object
                    lifeCycleConfig:
                      description: SageMakerClusterClusterLifeCycleConfig is a nested
                        property type of AWS::SageMaker::Cluster.
                      properties:
                        onCreate:
                          description: |-
                            The file name of the entrypoint script of lifecycle scripts under SourceS3Uri. This entrypoint script runs
                            during cluster creation. Mutually exclusive with OnInitComplete.
                          maxLength: 128
                          type: string
                        onInitComplete:
                          description: |-
                            The file name of the extension script under SourceS3Uri. This script runs after HyperPod configures the
                            default software on the instance. Mutually exclusive with OnCreate.
                          maxLength: 128
                          type: string
                        sourceS3Uri:
                          description: An Amazon S3 bucket path where your lifecycle
                            scripts are stored.
                          maxLength: 1024
                          type: string
                      type: object
                    minInstanceCount:
                      description: |-
                        The minimum number of instances required for the instance group to be InService. MinInstanceCount must be less
                        than or equal to InstanceCount.
                      format: int64
                      minimum: 0
                      type: integer
                    networkInterface:
                      description: SageMakerClusterClusterNetworkInterface is a nested
                        property type of AWS::SageMaker::Cluster.
                      properties:
                        interfaceType:
                          description: The type of network interface.
                          enum:
                          - efa
                          - efa-only
                          type: string
                      required:
                      - interfaceType
                      type: object
                    onStartDeepHealthChecks:
                      items:
                        type: string
                      type: array
                    overrideVpcConfig:
                      description: SageMakerClusterVpcConfig is a nested property
                        type of AWS::SageMaker::Cluster.
                      properties:
                        securityGroupIds:
                          description: |-
                            The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                            in the Subnets field.
                          items:
                            type: string
                          type: array
                        subnets:
                          description: The ID of the subnets in the VPC to which you
                            want to connect your training job or model.
                          items:
                            type: string
                          type: array
                      required:
                      - securityGroupIds
                      - subnets
                      type: object
                    scheduledUpdateConfig:
                      description: SageMakerClusterScheduledUpdateConfig is a nested
                        property type of AWS::SageMaker::Cluster.
                      properties:
                        deploymentConfig:
                          description: SageMakerClusterDeploymentConfig is a nested
                            property type of AWS::SageMaker::Cluster.
                          properties:
                            autoRollbackConfiguration:
                              items:
                                description: SageMakerClusterAlarmDetails is a nested
                                  property type of AWS::SageMaker::Cluster.
                                properties:
                                  alarmName:
                                    description: The name of the alarm.
                                    maxLength: 256
                                    minLength: 1
                                    type: string
                                required:
                                - alarmName
                                type: object
                              type: array
                            rollingUpdatePolicy:
                              description: SageMakerClusterRollingUpdatePolicy is
                                a nested property type of AWS::SageMaker::Cluster.
                              properties:
                                maximumBatchSize:
                                  description: SageMakerClusterCapacitySizeConfig
                                    is a nested property type of AWS::SageMaker::Cluster.
                                  properties:
                                    type:
                                      description: Specifies whether SageMaker should
                                        process the update by amount or percentage
                                        of instances.
                                      type: string
                                    value:
                                      description: Specifies the amount or percentage
                                        of instances SageMaker updates at a time.
                                      format: int64
                                      minimum: 1
                                      type: integer
                                  required:
                                  - type
                                  - value
                                  type: object
                                rollbackMaximumBatchSize:
                                  description: SageMakerClusterCapacitySizeConfig
                                    is a nested property type of AWS::SageMaker::Cluster.
                                  properties:
                                    type:
                                      description: Specifies whether SageMaker should
                                        process the update by amount or percentage
                                        of instances.
                                      type: string
                                    value:
                                      description: Specifies the amount or percentage
                                        of instances SageMaker updates at a time.
                                      format: int64
                                      minimum: 1
                                      type: integer
                                  required:
                                  - type
                                  - value
                                  type: object
                              required:
                              - maximumBatchSize
                              type: object
                            waitIntervalInSeconds:
                              description: The duration in seconds that SageMaker
                                waits before updating more instances in the cluster.
                              format: int64
                              maximum: 3600
                              minimum: 0
                              type: integer
                          type: object
                        scheduleExpression:
                          description: A cron expression that specifies the schedule
                            that SageMaker follows when updating the AMI.
                          maxLength: 256
                          minLength: 1
                          type: string
                      required:
                      - scheduleExpression
                      type: object
                    slurmConfig:
                      description: SageMakerClusterClusterSlurmConfig is a nested
                        property type of AWS::SageMaker::Cluster.
                      properties:
                        nodeType:
                          description: The type of Slurm node for this instance group.
                          enum:
                          - Controller
                          - Login
                          - Compute
                          type: string
                        partitionNames:
                          description: The Slurm partitions that this instance group
                            belongs to. Maximum of 1 partition.
                          items:
                            type: string
                          type: array
                      required:
                      - nodeType
                      type: object
                    threadsPerCore:
                      description: |-
                        The number you specified to TreadsPerCore in CreateCluster for enabling or disabling multithreading. For
                        instance types that support multithreading, you can specify 1 for disabling multithreading and 2 for enabling
                        multithreading.
                      format: int64
                      maximum: 2
                      minimum: 1
                      type: integer
                    trainingPlanArn:
                      description: |-
                        The Amazon Resource Name (ARN) of the training plan to use for this cluster instance group. For more
                        information about how to reserve GPU capacity for your SageMaker HyperPod clusters using Amazon SageMaker
                        Training Plan, see CreateTrainingPlan.
                      maxLength: 2048
                      type: string
                  required:
                  - executionRole
                  - instanceCount
                  - instanceGroupName
                  type: object
                type: array
              nodeProvisioningMode:
                description: |-
                  Determines the scaling strategy for the SageMaker HyperPod cluster. When set to 'Continuous', enables
                  continuous scaling which dynamically manages node provisioning. If the parameter is omitted, uses the standard
                  scaling approach in previous release.
                enum:
                - Continuous
                type: string
              nodeRecovery:
                description: |-
                  If node auto-recovery is set to true, faulty nodes will be replaced or rebooted when a failure is detected. If
                  set to false, nodes will be labelled when a fault is detected.
                enum:
                - Automatic
                - None
                type: string
              orchestrator:
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              restrictedInstanceGroups:
                items:
                  description: SageMakerClusterClusterRestrictedInstanceGroup is a
                    nested property type of AWS::SageMaker::Cluster.
                  properties:
                    currentCount:
                      description: The number of instances that are currently in the
                        restricted instance group of a SageMaker HyperPod cluster.
                      format: int64
                      minimum: 0
                      type: integer
                    environmentConfig:
                      description: SageMakerClusterEnvironmentConfig is a nested property
                        type of AWS::SageMaker::Cluster.
                      properties:
                        fSxLustreConfig:
                          description: SageMakerClusterFSxLustreConfig is a nested
                            property type of AWS::SageMaker::Cluster.
                          properties:
                            perUnitStorageThroughput:
                              description: The throughput capacity of the FSx for
                                Lustre file system, measured in MB/s per TiB of storage.
                              format: int64
                              maximum: 1000
                              minimum: 125
                              type: integer
                            sizeInGiB:
                              description: The storage capacity of the FSx for Lustre
                                file system, specified in gibibytes (GiB).
                              format: int64
                              maximum: 100800
                              minimum: 1200
                              type: integer
                          required:
                          - perUnitStorageThroughput
                          - sizeInGiB
                          type: object
                      type: object
                    executionRole:
                      maxLength: 2048
                      minLength: 20
                      type: string
                    instanceCount:
                      description: The number of instances you specified to add to
                        the restricted instance group of a SageMaker HyperPod cluster.
                      format: int64
                      minimum: 0
                      type: integer
                    instanceGroupName:
                      maxLength: 63
                      minLength: 1
                      type: string
                    instanceStorageConfigs:
                      items:
                        x-kubernetes-preserve-unknown-fields: true
                      type: array
                    instanceType:
                      type: string
                    onStartDeepHealthChecks:
                      items:
                        type: string
                      type: array
                    overrideVpcConfig:
                      description: SageMakerClusterVpcConfig is a nested property
                        type of AWS::SageMaker::Cluster.
                      properties:
                        securityGroupIds:
                          description: |-
                            The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                            in the Subnets field.
                          items:
                            type: string
                          type: array
                        subnets:
                          description: The ID of the subnets in the VPC to which you
                            want to connect your training job or model.
                          items:
                            type: string
                          type: array
                      required:
                      - securityGroupIds
                      - subnets
                      type: object
                    threadsPerCore:
                      description: |-
                        The number you specified to TreadsPerCore in CreateCluster for enabling or disabling multithreading. For
                        instance types that support multithreading, you can specify 1 for disabling multithreading and 2 for enabling
                        multithreading.
                      format: int64
                      maximum: 2
                      minimum: 1
                      type: integer
                    trainingPlanArn:
                      description: |-
                        The Amazon Resource Name (ARN) of the training plan to use for this cluster restricted instance group. For
                        more information about how to reserve GPU capacity for your SageMaker HyperPod clusters using Amazon SageMaker
                        Training Plan, see CreateTrainingPlan.
                      maxLength: 2048
                      type: string
                  required:
                  - executionRole
                  - instanceCount
                  - instanceGroupName
                  - instanceType
                  type: object
                type: array
              restrictedInstanceGroupsConfig:
                description: SageMakerClusterRestrictedInstanceGroupsConfig is a nested
                  property type of AWS::SageMaker::Cluster.
                properties:
                  sharedEnvironmentConfig:
                    description: SageMakerClusterSharedEnvironmentConfig is a nested
                      property type of AWS::SageMaker::Cluster.
                    properties:
                      fSxLustreConfig:
                        description: SageMakerClusterFSxLustreConfig is a nested property
                          type of AWS::SageMaker::Cluster.
                        properties:
                          perUnitStorageThroughput:
                            description: The throughput capacity of the FSx for Lustre
                              file system, measured in MB/s per TiB of storage.
                            format: int64
                            maximum: 1000
                            minimum: 125
                            type: integer
                          sizeInGiB:
                            description: The storage capacity of the FSx for Lustre
                              file system, specified in gibibytes (GiB).
                            format: int64
                            maximum: 100800
                            minimum: 1200
                            type: integer
                        required:
                        - perUnitStorageThroughput
                        - sizeInGiB
                        type: object
                      fSxLustreDeletionPolicy:
                        description: |-
                          The deletion policy for the shared FSx Lustre file system. Keep retains the FSx when RIGs are deleted.
                          DeleteIfNotUsed deletes the FSx when no RIGs reference it.
                        enum:
                        - Keep
                        - DeleteIfNotUsed
                        type: string
                    required:
                    - fSxLustreDeletionPolicy
                    type: object
                required:
                - sharedEnvironmentConfig
                type: object
              tags:
                description: |-
                  Custom tags for managing the SageMaker HyperPod cluster as an AWS resource. You can add tags to your cluster
                  in the same way you add them in other AWS services that support tagging.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              tieredStorageConfig:
                description: SageMakerClusterTieredStorageConfig is a nested property
                  type of AWS::SageMaker::Cluster.
                properties:
                  instanceMemoryAllocationPercentage:
                    description: The percentage of instance memory to allocate for
                      tiered storage.
                    format: int64
                    type: integer
                  mode:
                    description: The mode of tiered storage.
                    enum:
                    - Enable
                    - Disable
                    type: string
                required:
                - mode
                type: object
              vpcConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  securityGroupIds:
                    description: |-
                      The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                      in the Subnets field.
                    items:
                      type: string
                    type: array
                  subnets:
                    description: The ID of the subnets in the VPC to which you want
                      to connect your training job or model.
                    items:
                      type: string
                    type: array
                required:
                - securityGroupIds
                - subnets
                type: object
            type: object
          status:
            description: SageMakerClusterStatus is the observed state of AWS::SageMaker::Cluster.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              clusterArn:
                description: |-
                  The Amazon Resource Name (ARN) of the HyperPod Cluster.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              clusterStatus:
                description: |-
                  The status of the HyperPod Cluster.
                  Read-only: reported by AWS after creation.
                enum:
                - Creating
                - Deleting
                - Failed
                - InService
                - RollingBack
                - SystemUpdating
                - Updating
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the HyperPod cluster was created.
                  Read-only: reported by AWS after creation.
                type: string
              failureMessage:
                description: |-
                  The failure message of the HyperPod Cluster.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakercontexts.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerContext
    listKind: SageMakerContextList
    plural: sagemakercontexts
    singular: sagemakercontext
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerContext manages AWS::SageMaker::Context through the AWS Cloud Control API. Resource type definition
          for AWS::SageMaker::Context. A context is a lineage tracking entity that represents a logical grouping of
          other tracking or experiment entities.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerContextSpec is the desired state of AWS::SageMaker::Context.
            properties:
              contextName:
                description: |-
                  The name of the context. Must be unique to your account in an AWS Region.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 120
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: contextName is immutable
                  rule: self == oldSelf
              contextType:
                description: |-
                  The context type.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                type: string
                x-kubernetes-validations:
                - message: contextType is immutable
                  rule: self == oldSelf
              description:
                description: The description of the context.
                maxLength: 3072
                type: string
              properties:
                additionalProperties:
                  type: string
                description: A list of properties to add to the context.
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              source:
                description: |-
                  The source type, ID, and URI.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  sourceId:
                    description: The ID of the source.
                    maxLength: 256
                    type: string
                  sourceType:
                    description: The type of the source.
                    maxLength: 256
                    type: string
                  sourceUri:
                    description: The URI of the source.
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - sourceUri
                type: object
              tags:
                description: A list of tags to apply to the context.
                items:
                  description: SageMakerContextTagsItem is a nested property type
                    of AWS::SageMaker::Context.
                  properties:
                    key:
                      description: The tag key.
                      maxLength: 128
                      minLength: 1
                      type: string
                    value:
                      description: The tag value.
                      maxLength: 256
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - contextName
            - contextType
            - source
            type: object
          status:
            description: SageMakerContextStatus is the observed state of AWS::SageMaker::Context.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the context.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  When the context was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  When the context was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerdataqualityjobdefinitions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerDataQualityJobDefinition
    listKind: SageMakerDataQualityJobDefinitionList
    plural: sagemakerdataqualityjobdefinitions
    singular: sagemakerdataqualityjobdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerDataQualityJobDefinition manages AWS::SageMaker::DataQualityJobDefinition through the AWS Cloud
          Control API. Resource Type definition for AWS::SageMaker::DataQualityJobDefinition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerDataQualityJobDefinitionSpec is the desired state
              of AWS::SageMaker::DataQualityJobDefinition.
            properties:
              dataQualityAppSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  containerArguments:
                    description: An array of arguments for the container used to run
                      the monitoring job.
                    items:
                      type: string
                    type: array
                  containerEntrypoint:
                    description: Specifies the entrypoint for a container used to
                      run the monitoring job.
                    items:
                      type: string
                    type: array
                  environment:
                    additionalProperties:
                      type: string
                    description: Sets the environment variables in the Docker container
                    type: object
                  imageUri:
                    description: The container image to be run by the monitoring job.
                    maxLength: 255
                    type: string
                  postAnalyticsProcessorSourceUri:
                    description: |-
                      An Amazon S3 URI to a script that is called after analysis has been performed. Applicable only for the
                      built-in (first party) containers.
                    maxLength: 1024
                    type: string
                  recordPreprocessorSourceUri:
                    description: |-
                      An Amazon S3 URI to a script that is called per row prior to running analysis. It can base64 decode the
                      payload and convert it into a flatted json so that the built-in container can use the converted data.
                      Applicable only for the built-in (first party) containers
                    maxLength: 1024
                    type: string
                required:
                - imageUri
                type: object
              dataQualityBaselineConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  baseliningJobName:
                    maxLength: 63
                    type: string
                  constraintsResource:
                    description: SageMakerDataQualityJobDefinitionConstraintsResource
                      is a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          The Amazon S3 URI for baseline constraint file in Amazon S3 that the current monitoring job should validated
                          against.
                        maxLength: 1024
                        type: string
                    type: object
                  statisticsResource:
                    description: SageMakerDataQualityJobDefinitionStatisticsResource
                      is a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          The Amazon S3 URI for the baseline statistics file in Amazon S3 that the current monitoring job should be
                          validated against.
                        maxLength: 1024
                        type: string
                    type: object
                type: object
              dataQualityJobInput:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  batchTransformInput:
                    description: SageMakerDataQualityJobDefinitionBatchTransformInput
                      is a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                    properties:
                      dataCapturedDestinationS3Uri:
                        description: A URI that identifies the Amazon S3 storage location
                          where Batch Transform Job captures data.
                        maxLength: 512
                        type: string
                      datasetFormat:
                        description: SageMakerDataQualityJobDefinitionDatasetFormat
                          is a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                        properties:
                          csv:
                            description: SageMakerDataQualityJobDefinitionCsv is a
                              nested property type of AWS::SageMaker::DataQualityJobDefinition.
                            properties:
                              header:
                                description: A boolean flag indicating if given CSV
                                  has header
                                type: boolean
                            type: object
                          json:
                            description: SageMakerDataQualityJobDefinitionJson is
                              a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                            properties:
                              line:
                                description: A boolean flag indicating if it is JSON
                                  line format
                                type: boolean
                            type: object
                          parquet:
                            type: boolean
                        type: object
                      excludeFeaturesAttribute:
                        description: Indexes or names of the features to be excluded
                          from analysis
                        maxLength: 100
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                    required:
                    - dataCapturedDestinationS3Uri
                    - datasetFormat
                    - localPath
                    type: object
                  endpointInput:
                    description: SageMakerDataQualityJobDefinitionEndpointInput is
                      a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                    properties:
                      endpointName:
                        maxLength: 63
                        type: string
                      excludeFeaturesAttribute:
                        description: Indexes or names of the features to be excluded
                          from analysis
                        maxLength: 100
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                    required:
                    - endpointName
                    - localPath
                    type: object
                type: object
              dataQualityJobOutputConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: |-
                      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest
                      using Amazon S3 server-side encryption.
                    maxLength: 2048
                    type: string
                  monitoringOutputs:
                    description: Monitoring outputs for monitoring jobs. This is where
                      the output of the periodic monitoring jobs is uploaded.
                    items:
                      description: SageMakerDataQualityJobDefinitionMonitoringOutput
                        is a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                      properties:
                        s3Output:
                          description: SageMakerDataQualityJobDefinitionS3Output is
                            a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                          properties:
                            localPath:
                              description: |-
                                The local path to the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring job.
                                LocalPath is an absolute path for the output data.
                              maxLength: 256
                              type: string
                            s3UploadMode:
                              description: Whether to upload the results of the monitoring
                                job continuously or after the job completes.
                              enum:
                              - Continuous
                              - EndOfJob
                              type: string
                            s3Uri:
                              description: |-
                                A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                                job.
                              maxLength: 512
                              type: string
                          required:
                          - localPath
                          - s3Uri
                          type: object
                      required:
                      - s3Output
                      type: object
                    type: array
                required:
                - monitoringOutputs
                type: object
              endpointName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobDefinitionName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobResources:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  clusterConfig:
                    description: SageMakerDataQualityJobDefinitionClusterConfig is
                      a nested property type of AWS::SageMaker::DataQualityJobDefinition.
                    properties:
                      instanceCount:
                        description: |-
                          The number of ML compute instances to use in the model monitoring job. For distributed processing jobs,
                          specify a value greater than 1. The default value is 1.
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      instanceType:
                        description: The ML compute instance type for the processing
                          job.
                        type: string
                      volumeKmsKeyId:
                        description: |-
                          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                          attached to the ML compute instance(s) that run the model monitoring job.
                        type: string
                      volumeSizeInGB:
                        description: |-
                          The size of the ML storage volume, in gigabytes, that you want to provision. You must specify sufficient ML
                          storage for your scenario.
                        format: int64
                        maximum: 16384
                        minimum: 1
                        type: integer
                    required:
                    - instanceCount
                    - instanceType
                    - volumeSizeInGB
                    type: object
                required:
                - clusterConfig
                type: object
              networkConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  enableInterContainerTrafficEncryption:
                    description: |-
                      Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt
                      communications. Encryption provides greater security for distributed processing jobs, but the processing might
                      take longer.
                    type: boolean
                  enableNetworkIsolation:
                    description: Whether to allow inbound and outbound network calls
                      to and from the containers used for the processing job.
                    type: boolean
                  vpcConfig:
                    description: SageMakerDataQualityJobDefinitionVpcConfig is a nested
                      property type of AWS::SageMaker::DataQualityJobDefinition.
                    properties:
                      securityGroupIds:
                        description: |-
                          The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                          in the Subnets field.
                        items:
                          type: string
                        type: array
                      subnets:
                        description: The ID of the subnets in the VPC to which you
                          want to connect to your monitoring jobs.
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroupIds
                    - subnets
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your
                  behalf.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              stoppingCondition:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  maxRuntimeInSeconds:
                    description: The maximum runtime allowed in seconds.
                    format: int64
                    maximum: 86400
                    minimum: 1
                    type: integer
                required:
                - maxRuntimeInSeconds
                type: object
              tags:
                description: |-
                  An array of key-value pairs to apply to this resource.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - dataQualityAppSpecification
            - dataQualityJobInput
            - dataQualityJobOutputConfig
            - jobResources
            - roleArn
            type: object
          status:
            description: SageMakerDataQualityJobDefinitionStatus is the observed state
              of AWS::SageMaker::DataQualityJobDefinition.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the job definition was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              jobDefinitionArn:
                description: |-
                  The Amazon Resource Name (ARN) of job definition.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerdevicefleets.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerDeviceFleet
    listKind: SageMakerDeviceFleetList
    plural: sagemakerdevicefleets
    singular: sagemakerdevicefleet
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerDeviceFleet manages AWS::SageMaker::DeviceFleet through the AWS Cloud Control API. Resource schema
          for AWS::SageMaker::DeviceFleet
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerDeviceFleetSpec is the desired state of AWS::SageMaker::DeviceFleet.
            properties:
              description:
                description: Description for the edge device fleet
                maxLength: 800
                type: string
              deviceFleetName:
                description: |-
                  The name of the edge device fleet
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: deviceFleetName is immutable
                  rule: self == oldSelf
              outputConfig:
                description: S3 bucket and an ecryption key id (if available) to store
                  outputs for the fleet
                properties:
                  kmsKeyId:
                    description: The KMS key id used for encryption on the S3 bucket
                    maxLength: 2048
                    type: string
                  s3OutputLocation:
                    description: The Amazon Simple Storage (S3) bucket URI
                    maxLength: 1024
                    type: string
                required:
                - s3OutputLocation
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: Role associated with the device fleet
                maxLength: 2048
                minLength: 20
                type: string
              tags:
                description: Associate tags with the resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - deviceFleetName
            - outputConfig
            - roleArn
            type: object
          status:
            description: SageMakerDeviceFleetStatus is the observed state of AWS::SageMaker::DeviceFleet.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerdevices.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerDevice
    listKind: SageMakerDeviceList
    plural: sagemakerdevices
    singular: sagemakerdevice
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerDevice manages AWS::SageMaker::Device through the AWS Cloud Control API. Resource schema for
          AWS::SageMaker::Device
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerDeviceSpec is the desired state of AWS::SageMaker::Device.
            properties:
              device:
                description: The Edge Device you want to register against a device
                  fleet
                properties:
                  description:
                    description: Description of the device
                    maxLength: 40
                    type: string
                  deviceName:
                    description: The name of the device
                    maxLength: 63
                    minLength: 1
                    type: string
                  iotThingName:
                    description: AWS Internet of Things (IoT) object name.
                    maxLength: 128
                    type: string
                required:
                - deviceName
                type: object
              deviceFleetName:
                description: The name of the edge device fleet
                maxLength: 63
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Associate tags with the resource
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - deviceFleetName
            type: object
          status:
            description: SageMakerDeviceStatus is the observed state of AWS::SageMaker::Device.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerdomains.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerDomain
    listKind: SageMakerDomainList
    plural: sagemakerdomains
    singular: sagemakerdomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerDomain manages AWS::SageMaker::Domain through the AWS Cloud Control API. Resource Type definition for
          AWS::SageMaker::Domain
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerDomainSpec is the desired state of AWS::SageMaker::Domain.
            properties:
              appNetworkAccessType:
                description: Specifies the VPC used for non-EFS traffic. The default
                  value is PublicInternetOnly.
                enum:
                - PublicInternetOnly
                - VpcOnly
                type: string
              appSecurityGroupManagement:
                description: |-
                  The entity that creates and manages the required security groups for inter-app communication in VPCOnly mode.
                  Required when CreateDomain.AppNetworkAccessType is VPCOnly and
                  DomainSettings.RStudioServerProDomainSettings.DomainExecutionRoleArn is provided.
                enum:
                - Service
                - Customer
                type: string
              authMode:
                description: |-
                  The mode of authentication that members use to access the domain.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - SSO
                - IAM
                type: string
                x-kubernetes-validations:
                - message: authMode is immutable
                  rule: self == oldSelf
              defaultSpaceSettings:
                description: The default space settings.
                properties:
                  customFileSystemConfigs:
                    items:
                      description: SageMakerDomainCustomFileSystemConfig is a nested
                        property type of AWS::SageMaker::Domain.
                      properties:
                        efsFileSystemConfig:
                          description: SageMakerDomainEFSFileSystemConfig is a nested
                            property type of AWS::SageMaker::Domain.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                            fileSystemPath:
                              maxLength: 256
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        fSxLustreFileSystemConfig:
                          description: SageMakerDomainFSxLustreFileSystemConfig is
                            a nested property type of AWS::SageMaker::Domain.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                            fileSystemPath:
                              maxLength: 256
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        s3FileSystemConfig:
                          description: SageMakerDomainS3FileSystemConfig is a nested
                            property type of AWS::SageMaker::Domain.
                          properties:
                            mountPath:
                              maxLength: 1024
                              type: string
                            s3Uri:
                              maxLength: 1024
                              type: string
                          type: object
                      type: object
                    type: array
                  customPosixUserConfig:
                    description: The Jupyter lab's custom posix user configurations.
                    properties:
                      gid:
                        format: int64
                        maximum: 4000000
                        minimum: 1001
                        type: integer
                      uid:
                        format: int64
                        maximum: 4000000
                        minimum: 10000
                        type: integer
                    required:
                    - gid
                    - uid
                    type: object
                  executionRole:
                    description: The execution role for the space.
                    maxLength: 2048
                    minLength: 20
                    type: string
                  jupyterLabAppSettings:
                    description: The Jupyter lab's app settings.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerDomainAppLifecycleManagement is a nested
                          property type of AWS::SageMaker::Domain.
                        properties:
                          idleSettings:
                            description: SageMakerDomainIdleSettings is a nested property
                              type of AWS::SageMaker::Domain.
                            properties:
                              idleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              lifecycleManagement:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              maxIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              minIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      builtInLifecycleConfigArn:
                        description: The lifecycle configuration that runs before
                          the default lifecycle configuration.
                        maxLength: 256
                        type: string
                      codeRepositories:
                        description: A list of CodeRepositories available for use
                          with JupyterLab apps.
                        items:
                          description: SageMakerDomainCodeRepository is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            repositoryUrl:
                              description: A CodeRepository (valid URL) to be used
                                within Jupyter's Git extension.
                              maxLength: 256
                              type: string
                          required:
                          - repositoryUrl
                          type: object
                        type: array
                      customImages:
                        description: A list of custom images for use for JupyterLab
                          apps.
                        items:
                          description: SageMakerDomainCustomImage is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          JupyterLab app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterLab apps.
                        items:
                          type: string
                        type: array
                    type: object
                  jupyterServerAppSettings:
                    description: The Jupyter server's app settings.
                    properties:
                      defaultResourceSpec:
                        description: SageMakerDomainResourceSpec is a nested property
                          type of AWS::SageMaker::Domain.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterServer apps.
                        items:
                          type: string
                        type: array
                    type: object
                  kernelGatewayAppSettings:
                    description: The kernel gateway app settings.
                    properties:
                      customImages:
                        description: A list of custom SageMaker images that are configured
                          to run as a KernelGateway app.
                        items:
                          description: SageMakerDomainCustomImage is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          KernelGateway app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with KernelGateway apps.
                        items:
                          type: string
                        type: array
                    type: object
                  securityGroups:
                    description: The security groups for the Amazon Virtual Private
                      Cloud (VPC) that Studio uses for communication.
                    items:
                      type: string
                    type: array
                  spaceStorageSettings:
                    description: The Jupyter lab's space storage settings.
                    properties:
                      defaultEbsStorageSettings:
                        description: SageMakerDomainDefaultEbsStorageSettings is a
                          nested property type of AWS::SageMaker::Domain.
                        properties:
                          defaultEbsVolumeSizeInGb:
                            description: Default size of the Amazon EBS volume in
                              Gb
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                          maximumEbsVolumeSizeInGb:
                            description: Maximum size of the Amazon EBS volume in
                              Gb. Must be greater than or equal to the DefaultEbsVolumeSizeInGb.
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                        required:
                        - defaultEbsVolumeSizeInGb
                        - maximumEbsVolumeSizeInGb
                        type: object
                    type: object
                required:
                - executionRole
                type: object
              defaultUserSettings:
                description: The default user settings.
                properties:
                  autoMountHomeEFS:
                    description: Indicates whether auto-mounting of an EFS volume
                      is supported for the user profile.
                    enum:
                    - Enabled
                    - Disabled
                    - DefaultAsDomain
                    type: string
                  codeEditorAppSettings:
                    description: SageMakerDomainCodeEditorAppSettings is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerDomainAppLifecycleManagement is a nested
                          property type of AWS::SageMaker::Domain.
                        properties:
                          idleSettings:
                            description: SageMakerDomainIdleSettings is a nested property
                              type of AWS::SageMaker::Domain.
                            properties:
                              idleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              lifecycleManagement:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              maxIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              minIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      builtInLifecycleConfigArn:
                        description: The lifecycle configuration that runs before
                          the default lifecycle configuration.
                        maxLength: 256
                        type: string
                      customImages:
                        description: A list of custom images for use for CodeEditor
                          apps.
                        items:
                          description: SageMakerDomainCustomImage is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          CodeEditor app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with CodeEditor apps.
                        items:
                          type: string
                        type: array
                    type: object
                  customFileSystemConfigs:
                    items:
                      description: SageMakerDomainCustomFileSystemConfig is a nested
                        property type of AWS::SageMaker::Domain.
                      properties:
                        efsFileSystemConfig:
                          description: SageMakerDomainEFSFileSystemConfig is a nested
                            property type of AWS::SageMaker::Domain.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                            fileSystemPath:
                              maxLength: 256
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        fSxLustreFileSystemConfig:
                          description: SageMakerDomainFSxLustreFileSystemConfig is
                            a nested property type of AWS::SageMaker::Domain.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                            fileSystemPath:
                              maxLength: 256
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        s3FileSystemConfig:
                          description: SageMakerDomainS3FileSystemConfig is a nested
                            property type of AWS::SageMaker::Domain.
                          properties:
                            mountPath:
                              maxLength: 1024
                              type: string
                            s3Uri:
                              maxLength: 1024
                              type: string
                          type: object
                      type: object
                    type: array
                  customPosixUserConfig:
                    description: SageMakerDomainCustomPosixUserConfig is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      gid:
                        format: int64
                        maximum: 4000000
                        minimum: 1001
                        type: integer
                      uid:
                        format: int64
                        maximum: 4000000
                        minimum: 10000
                        type: integer
                    required:
                    - gid
                    - uid
                    type: object
                  defaultLandingUri:
                    description: Defines which Amazon SageMaker application users
                      are directed to by default.
                    maxLength: 1023
                    type: string
                  executionRole:
                    description: The execution role for the user.
                    maxLength: 2048
                    minLength: 20
                    type: string
                  jupyterLabAppSettings:
                    description: SageMakerDomainJupyterLabAppSettings is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerDomainAppLifecycleManagement is a nested
                          property type of AWS::SageMaker::Domain.
                        properties:
                          idleSettings:
                            description: SageMakerDomainIdleSettings is a nested property
                              type of AWS::SageMaker::Domain.
                            properties:
                              idleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              lifecycleManagement:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              maxIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              minIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      builtInLifecycleConfigArn:
                        description: The lifecycle configuration that runs before
                          the default lifecycle configuration.
                        maxLength: 256
                        type: string
                      codeRepositories:
                        description: A list of CodeRepositories available for use
                          with JupyterLab apps.
                        items:
                          description: SageMakerDomainCodeRepository is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            repositoryUrl:
                              description: A CodeRepository (valid URL) to be used
                                within Jupyter's Git extension.
                              maxLength: 256
                              type: string
                          required:
                          - repositoryUrl
                          type: object
                        type: array
                      customImages:
                        description: A list of custom images for use for JupyterLab
                          apps.
                        items:
                          description: SageMakerDomainCustomImage is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          JupyterLab app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterLab apps.
                        items:
                          type: string
                        type: array
                    type: object
                  jupyterServerAppSettings:
                    description: The Jupyter server's app settings.
                    properties:
                      defaultResourceSpec:
                        description: SageMakerDomainResourceSpec is a nested property
                          type of AWS::SageMaker::Domain.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterServer apps.
                        items:
                          type: string
                        type: array
                    type: object
                  kernelGatewayAppSettings:
                    description: The kernel gateway app settings.
                    properties:
                      customImages:
                        description: A list of custom SageMaker images that are configured
                          to run as a KernelGateway app.
                        items:
                          description: SageMakerDomainCustomImage is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          KernelGateway app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with KernelGateway apps.
                        items:
                          type: string
                        type: array
                    type: object
                  rSessionAppSettings:
                    description: SageMakerDomainRSessionAppSettings is a nested property
                      type of AWS::SageMaker::Domain.
                    properties:
                      customImages:
                        description: A list of custom SageMaker images that are configured
                          to run as a KernelGateway app.
                        items:
                          description: SageMakerDomainCustomImage is a nested property
                            type of AWS::SageMaker::Domain.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: SageMakerDomainResourceSpec is a nested property
                          type of AWS::SageMaker::Domain.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                    type: object
                  rStudioServerProAppSettings:
                    description: SageMakerDomainRStudioServerProAppSettings is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      accessStatus:
                        description: Indicates whether the current user has access
                          to the RStudioServerPro app.
                        enum:
                        - ENABLED
                        - DISABLED
                        type: string
                      userGroup:
                        description: |-
                          The level of permissions that the user has within the RStudioServerPro app. This value defaults to User. The
                          Admin value allows the user access to the RStudio Administrative Dashboard.
                        enum:
                        - R_STUDIO_ADMIN
                        - R_STUDIO_USER
                        type: string
                    type: object
                  securityGroups:
                    description: The security groups for the Amazon Virtual Private
                      Cloud (VPC) that Studio uses for communication.
                    items:
                      type: string
                    type: array
                  sharingSettings:
                    description: The sharing settings.
                    properties:
                      notebookOutputOption:
                        description: Whether to include the notebook cell output when
                          sharing the notebook. The default is Disabled.
                        enum:
                        - Allowed
                        - Disabled
                        type: string
                      s3KmsKeyId:
                        description: |-
                          When NotebookOutputOption is Allowed, the AWS Key Management Service (KMS) encryption key ID used to encrypt
                          the notebook cell output in the Amazon S3 bucket.
                        maxLength: 2048
                        type: string
                      s3OutputPath:
                        description: When NotebookOutputOption is Allowed, the Amazon
                          S3 bucket used to store the shared notebook snapshots.
                        maxLength: 1024
                        type: string
                    type: object
                  spaceStorageSettings:
                    description: SageMakerDomainDefaultSpaceStorageSettings is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      defaultEbsStorageSettings:
                        description: SageMakerDomainDefaultEbsStorageSettings is a
                          nested property type of AWS::SageMaker::Domain.
                        properties:
                          defaultEbsVolumeSizeInGb:
                            description: Default size of the Amazon EBS volume in
                              Gb
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                          maximumEbsVolumeSizeInGb:
                            description: Maximum size of the Amazon EBS volume in
                              Gb. Must be greater than or equal to the DefaultEbsVolumeSizeInGb.
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                        required:
                        - defaultEbsVolumeSizeInGb
                        - maximumEbsVolumeSizeInGb
                        type: object
                    type: object
                  studioWebPortal:
                    description: Indicates whether the Studio experience is available
                      to users. If not, users cannot access Studio.
                    enum:
                    - ENABLED
                    - DISABLED
                    type: string
                  studioWebPortalSettings:
                    description: SageMakerDomainStudioWebPortalSettings is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      hiddenAppTypes:
                        description: Applications supported in Studio that are hidden
                          from the Studio left navigation pane.
                        items:
                          type: string
                        type: array
                      hiddenInstanceTypes:
                        description: The instance types you are hiding from the Studio
                          user interface.
                        items:
                          type: string
                        type: array
                      hiddenMlTools:
                        description: The machine learning tools that are hidden from
                          the Studio left navigation pane.
                        items:
                          type: string
                        type: array
                      hiddenSageMakerImageVersionAliases:
                        description: The version aliases you are hiding from the Studio
                          user interface.
                        items:
                          description: SageMakerDomainHiddenSageMakerImage is a nested
                            property type of AWS::SageMaker::Domain.
                          properties:
                            sageMakerImageName:
                              description: The SageMaker image name that you are hiding
                                from the Studio user interface.
                              enum:
                              - sagemaker_distribution
                              type: string
                            versionAliases:
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                required:
                - executionRole
                type: object
              domainName:
                description: |-
                  A name for the domain.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                type: string
                x-kubernetes-validations:
                - message: domainName is immutable
                  rule: self == oldSelf
              domainSettings:
                description: SageMakerDomainDomainSettings is a nested property type
                  of AWS::SageMaker::Domain.
                properties:
                  dockerSettings:
                    description: SageMakerDomainDockerSettings is a nested property
                      type of AWS::SageMaker::Domain.
                    properties:
                      enableDockerAccess:
                        description: The flag to enable/disable docker-proxy server
                        enum:
                        - ENABLED
                        - DISABLED
                        type: string
                      vpcOnlyTrustedAccounts:
                        description: A list of account id's that would be used to
                          pull images from in VpcOnly mode
                        items:
                          type: string
                        type: array
                    type: object
                  executionRoleIdentityConfig:
                    description: |-
                      The configuration for attaching a SageMaker user profile name to the execution role as a sts:SourceIdentity
                      key.
                    enum:
                    - USER_PROFILE_NAME
                    - DISABLED
                    type: string
                  ipAddressType:
                    enum:
                    - IPV4
                    - DUALSTACK
                    type: string
                  rStudioServerProDomainSettings:
                    description: SageMakerDomainRStudioServerProDomainSettings is
                      a nested property type of AWS::SageMaker::Domain.
                    properties:
                      defaultResourceSpec:
                        description: SageMakerDomainResourceSpec is a nested property
                          type of AWS::SageMaker::Domain.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The Amazon Resource Name (ARN) of the SageMaker
                              image that the image version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The Amazon Resource Name (ARN) of the image
                              version created on the instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      domainExecutionRoleArn:
                        description: The ARN of the execution role for the RStudioServerPro
                          Domain-level app.
                        maxLength: 2048
                        minLength: 20
                        type: string
                      rStudioConnectUrl:
                        description: A URL pointing to an RStudio Connect server.
                        type: string
                      rStudioPackageManagerUrl:
                        description: A URL pointing to an RStudio Package Manager
                          server.
                        type: string
                    required:
                    - domainExecutionRoleArn
                    type: object
                  securityGroupIds:
                    description: |-
                      The security groups for the Amazon Virtual Private Cloud that the Domain uses for communication between
                      Domain-level apps and user apps.
                    items:
                      type: string
                    type: array
                  unifiedStudioSettings:
                    description: SageMakerDomainUnifiedStudioSettings is a nested
                      property type of AWS::SageMaker::Domain.
                    properties:
                      domainAccountId:
                        description: |-
                          The ID of the AWS account that has the Amazon SageMaker Unified Studio domain. The default value, if you don't
                          specify an ID, is the ID of the account that has the Amazon SageMaker AI domain.
                        maxLength: 12
                        type: string
                      domainId:
                        description: The ID of the Amazon SageMaker Unified Studio
                          domain associated with this domain.
                        maxLength: 36
                        type: string
                      domainRegion:
                        description: |-
                          The AWS Region where the domain is located in Amazon SageMaker Unified Studio. The default value, if you don't
                          specify a Region, is the Region where the Amazon SageMaker AI domain is located.
                        type: string
                      environmentId:
                        description: The ID of the environment that Amazon SageMaker
                          Unified Studio associates with the domain.
                        maxLength: 36
                        type: string
                      projectId:
                        description: The ID of the Amazon SageMaker Unified Studio
                          project that corresponds to the domain.
                        type: string
                      projectS3Path:
                        description: |-
                          The location where Amazon S3 stores temporary execution data and other artifacts for the project that
                          corresponds to the domain.
                        maxLength: 100
                        type: string
                      studioWebPortalAccess:
                        description: |-
                          Sets whether you can access the domain in Amazon SageMaker Studio: ENABLED You can access the domain in Amazon
                          SageMaker Studio. If you migrate the domain to Amazon SageMaker Unified Studio, you can access it in both
                          studio interfaces. DISABLED You can't access the domain in Amazon SageMaker Studio. If you migrate the domain
                          to Amazon SageMaker Unified Studio, you can access it only in that stu...
                        enum:
                        - ENABLED
                        - DISABLED
                        type: string
                    type: object
                type: object
              homeEfsFileSystemCreation:
                description: |-
                  Indicates whether a home EFS file system is created for the domain. Set to Disabled to skip EFS creation and
                  reduce domain creation time.
                enum:
                - Enabled
                - Disabled
                type: string
              kmsKeyId:
                description: |-
                  SageMaker uses AWS KMS to encrypt the EFS volume attached to the domain with an AWS managed customer master
                  key (CMK) by default.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              subnetIds:
                description: The VPC subnets that Studio uses for communication.
                items:
                  type: string
                type: array
              tagPropagation:
                description: |-
                  Indicates whether the tags added to Domain, User Profile and Space entity is propagated to all SageMaker
                  resources.
                enum:
                - ENABLED
                - DISABLED
                type: string
              tags:
                description: A list of tags to apply to the user profile.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              vpcId:
                description: The ID of the Amazon Virtual Private Cloud (VPC) that
                  Studio uses for communication.
                maxLength: 32
                type: string
            required:
            - authMode
            - defaultUserSettings
            - domainName
            type: object
          status:
            description: SageMakerDomainStatus is the observed state of AWS::SageMaker::Domain.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              domainArn:
                description: |-
                  The Amazon Resource Name (ARN) of the created domain.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              domainId:
                description: |-
                  The domain name.
                  Read-only: reported by AWS after creation.
                maxLength: 63
                type: string
              homeEfsFileSystemId:
                description: |-
                  The ID of the Amazon Elastic File System (EFS) managed by this Domain.
                  Read-only: reported by AWS after creation.
                maxLength: 32
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              securityGroupIdForDomainBoundary:
                description: |-
                  The ID of the security group that authorizes traffic between the RSessionGateway apps and the RStudioServerPro
                  app.
                  Read-only: reported by AWS after creation.
                maxLength: 32
                type: string
              singleSignOnApplicationArn:
                description: |-
                  The ARN of the application managed by SageMaker in IAM Identity Center. This value is only returned for
                  domains created after October 1, 2023.
                  Read-only: reported by AWS after creation.
                type: string
              singleSignOnManagedApplicationInstanceId:
                description: |-
                  The SSO managed application instance ID.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              url:
                description: |-
                  The URL to the created domain.
                  Read-only: reported by AWS after creation.
                maxLength: 1024
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerendpointconfigs.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerEndpointConfig
    listKind: SageMakerEndpointConfigList
    plural: sagemakerendpointconfigs
    singular: sagemakerendpointconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerEndpointConfig manages AWS::SageMaker::EndpointConfig through the AWS Cloud Control API. Resource
          Type definition for AWS::SageMaker::EndpointConfig
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerEndpointConfigSpec is the desired state of AWS::SageMaker::EndpointConfig.
            properties:
              asyncInferenceConfig:
                description: |-
                  Specifies configuration for how an endpoint performs asynchronous inference.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  clientConfig:
                    description: |-
                      Configures the behavior of the client used by SageMaker to interact with the model container during
                      asynchronous inference.
                    properties:
                      maxConcurrentInvocationsPerInstance:
                        description: |-
                          The maximum number of concurrent requests sent by the SageMaker client to the model container. If no value is
                          provided, SageMaker will choose an optimal value for you.
                        format: int64
                        minimum: 1
                        type: integer
                    type: object
                  outputConfig:
                    description: Specifies the configuration for asynchronous inference
                      invocation outputs.
                    properties:
                      kmsKeyId:
                        description: |-
                          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the asynchronous inference
                          output in Amazon S3.
                        type: string
                      notificationConfig:
                        description: Specifies the configuration for notifications
                          of inference results for asynchronous inference.
                        properties:
                          errorTopic:
                            description: |-
                              Amazon SNS topic to post a notification to when an inference fails. If no topic is provided, no notification
                              is sent on failure.
                            type: string
                          includeInferenceResponseIn:
                            description: The Amazon SNS topics where you want the
                              inference response to be included.
                            items:
                              type: string
                            type: array
                          successTopic:
                            description: |-
                              Amazon SNS topic to post a notification to when an inference completes successfully. If no topic is provided,
                              no notification is sent on success.
                            type: string
                        type: object
                      s3FailurePath:
                        description: The Amazon S3 location to upload failure inference
                          responses to.
                        type: string
                      s3OutputPath:
                        description: The Amazon S3 location to upload inference responses
                          to.
                        type: string
                    type: object
                required:
                - outputConfig
                type: object
              dataCaptureConfig:
                description: |-
                  Specifies how to capture endpoint data for model monitor. The data capture configuration applies to all
                  production variants hosted at the endpoint.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  captureContentTypeHeader:
                    description: A list of the JSON and CSV content type that the
                      endpoint captures.
                    properties:
                      csvContentTypes:
                        description: |-
                          A list of the CSV content types of the data that the endpoint captures. For the endpoint to capture the data,
                          you must also specify the content type when you invoke the endpoint.
                        items:
                          type: string
                        type: array
                      jsonContentTypes:
                        description: |-
                          A list of the JSON content types of the data that the endpoint captures. For the endpoint to capture the data,
                          you must also specify the content type when you invoke the endpoint.
                        items:
                          type: string
                        type: array
                    type: object
                  captureOptions:
                    description: Specifies whether the endpoint captures input data
                      to your model, output data from your model, or both.
                    items:
                      description: SageMakerEndpointConfigCaptureOption is a nested
                        property type of AWS::SageMaker::EndpointConfig.
                      properties:
                        captureMode:
                          description: Specifies whether the endpoint captures input
                            data or output data.
                          type: string
                      required:
                      - captureMode
                      type: object
                    type: array
                  destinationS3Uri:
                    description: The S3 bucket where model monitor stores captured
                      data.
                    type: string
                  enableCapture:
                    description: Set to True to enable data capture.
                    type: boolean
                  initialSamplingPercentage:
                    description: The percentage of data to capture.
                    format: int64
                    maximum: 100
                    minimum: 0
                    type: integer
                  kmsKeyId:
                    description: |-
                      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the captured data at rest
                      using Amazon S3 server-side encryption.
                    type: string
                required:
                - captureOptions
                - destinationS3Uri
                - initialSamplingPercentage
                type: object
              enableNetworkIsolation:
                description: |-
                  Sets whether all model containers deployed to the endpoint are isolated. If they are, no inbound or outbound
                  network calls can be made to or from the model containers.
                  Create-only in AWS: changing it requires replacing the resource.
                type: boolean
              endpointConfigName:
                description: |-
                  The name of the endpoint configuration.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                type: string
              executionRoleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker AI can assume to perform actions on your
                  behalf.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              explainerConfig:
                description: |-
                  A parameter to activate explainers.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  clarifyExplainerConfig:
                    description: A member of ExplainerConfig that contains configuration
                      parameters for the SageMaker Clarify explainer.
                    properties:
                      enableExplanations:
                        description: A JMESPath boolean expression used to filter
                          which records to explain. Explanations are activated by
                          default.
                        maxLength: 64
                        type: string
                      inferenceConfig:
                        description: The inference configuration parameter for the
                          model container.
                        properties:
                          contentTemplate:
                            description: A template string used to format a JSON record
                              into an acceptable model container input.
                            maxLength: 64
                            type: string
                          featureHeaders:
                            description: |-
                              The names of the features. If provided, these are included in the endpoint response payload to help
                              readability of the InvokeEndpoint output.
                            items:
                              type: string
                            type: array
                          featureTypes:
                            description: |-
                              A list of data types of the features (optional). Applicable only to NLP explainability. If provided,
                              FeatureTypes must have at least one 'text' string (for example, ['text']). If FeatureTypes is not provided,
                              the explainer infers the feature types based on the baseline data.
                            items:
                              type: string
                            type: array
                          featuresAttribute:
                            description: Provides the JMESPath expression to extract
                              the features from a model container input in JSON Lines
                              format.
                            maxLength: 64
                            type: string
                          labelAttribute:
                            description: A JMESPath expression used to locate the
                              list of label headers in the model container output.
                            maxLength: 64
                            type: string
                          labelHeaders:
                            description: |-
                              For multiclass classification problems, the label headers are the names of the classes. Otherwise, the label
                              header is the name of the predicted label.
                            items:
                              type: string
                            type: array
                          labelIndex:
                            description: |-
                              A zero-based index used to extract a label header or list of label headers from model container output in CSV
                              format.
                            format: int64
                            minimum: 0
                            type: integer
                          maxPayloadInMB:
                            description: |-
                              The maximum payload size (MB) allowed of a request from the explainer to the model container. Defaults to 6
                              MB.
                            format: int64
                            maximum: 25
                            minimum: 1
                            type: integer
                          maxRecordCount:
                            description: |-
                              The maximum number of records in a request that the model container can process when querying the model
                              container for the predictions of a synthetic dataset. A record is a unit of input data that inference can be
                              made on, for example, a single line in CSV data.
                            format: int64
                            minimum: 1
                            type: integer
                          probabilityAttribute:
                            description: |-
                              A JMESPath expression used to extract the probability (or score) from the model container output if the model
                              container is in JSON Lines format.
                            maxLength: 64
                            type: string
                          probabilityIndex:
                            description: |-
                              A zero-based index used to extract a probability value (score) or list from model container output in CSV
                              format. If this value is not provided, the entire model container output will be treated as a probability
                              value (score) or list.
                            format: int64
                            minimum: 0
                            type: integer
                        type: object
                      shapConfig:
                        description: The configuration for SHAP analysis.
                        properties:
                          numberOfSamples:
                            description: The number of samples to be used for analysis
                              by the Kernal SHAP algorithm.
                            format: int64
                            minimum: 1
                            type: integer
                          seed:
                            description: |-
                              The starting value used to initialize the random number generator in the explainer. Provide a value for this
                              parameter to obtain a deterministic SHAP result.
                            format: int64
                            type: integer
                          shapBaselineConfig:
                            description: The configuration for the SHAP baseline of
                              the Kernal SHAP algorithm.
                            properties:
                              mimeType:
                                description: The MIME type of the baseline data. Choose
                                  from 'text/csv' or 'application/jsonlines'. Defaults
                                  to 'text/csv'.
                                maxLength: 255
                                type: string
                              shapBaseline:
                                description: |-
                                  The inline SHAP baseline data in string format. ShapBaseline can have one or multiple records to be used as
                                  the baseline dataset. The format of the SHAP baseline file should be the same format as the training dataset.
                                maxLength: 4096
                                type: string
                              shapBaselineUri:
                                description: |-
                                  The uniform resource identifier (URI) of the S3 bucket where the SHAP baseline file is stored. The format of
                                  the SHAP baseline file should be the same format as the format of the training dataset.
                                maxLength: 1024
                                type: string
                            type: object
                          textConfig:
                            description: |-
                              A parameter that indicates if text features are treated as text and explanations are provided for individual
                              units of text. Required for natural language processing (NLP) explainability only.
                            properties:
                              granularity:
                                description: |-
                                  The unit of granularity for the analysis of text features. For example, if the unit is 'token', then each
                                  token (like a word in English) of the text is treated as a feature. SHAP values are computed for each
                                  unit/feature.
                                type: string
                              language:
                                description: Specifies the language of the text features
                                  in ISO 639-1 or ISO 639-3 code of a supported language.
                                type: string
                            required:
                            - granularity
                            - language
                            type: object
                          useLogit:
                            description: |-
                              A Boolean toggle to indicate if you want to use the logit function (true) or log-odds units (false) for model
                              predictions. Defaults to false.
                            type: boolean
                        required:
                        - shapBaselineConfig
                        type: object
                    required:
                    - shapConfig
                    type: object
                type: object
              kmsKeyId:
                description: |-
                  The Amazon Resource Name (ARN) of an AWS Key Management Service key that Amazon SageMaker uses to encrypt data
                  on the storage volume attached to the ML compute instance that hosts the endpoint.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              metricsConfig:
                description: |-
                  Specifies the metrics that the endpoint publishes to Amazon CloudWatch, the frequency of publication, and
                  whether to enable enhanced or detailed observability metrics.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  enableDetailedObservability:
                    description: |-
                      Specifies whether to enable detailed observability for the endpoint. When set to true, the endpoint publishes
                      container-level inference metrics, per-GPU metrics, per-instance host metrics, and inference component
                      placement metrics.
                    type: boolean
                  enableEnhancedMetrics:
                    description: |-
                      Specifies whether to enable enhanced metrics for the endpoint. Enhanced metrics provide utilization and
                      invocation data at instance and container granularity.
                    type: boolean
                  metricPublishFrequencyInSeconds:
                    description: |-
                      The interval, in seconds, at which the endpoint publishes metrics to Amazon CloudWatch. Valid values are 10,
                      30, 60, 120, 180, 240, and 300. The default is 60.
                    format: int64
                    type: integer
                type: object
              productionVariants:
                description: |-
                  A list of ProductionVariant objects, one for each model that you want to host at this endpoint.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: SageMakerEndpointConfigProductionVariant is a nested
                    property type of AWS::SageMaker::EndpointConfig.
                  properties:
                    capacityReservationConfig:
                      description: Settings for the capacity reservation for the compute
                        instances that SageMaker AI reserves for an endpoint.
                      properties:
                        capacityReservationPreference:
                          description: Options that you can choose for the capacity
                            reservation.
                          type: string
                        mlReservationArn:
                          description: |-
                            The Amazon Resource Name (ARN) that uniquely identifies the ML capacity reservation that SageMaker AI applies
                            when it deploys the endpoint.
                          type: string
                      type: object
                    containerStartupHealthCheckTimeoutInSeconds:
                      description: The timeout value, in seconds, for your inference
                        container to pass health check by SageMaker Hosting.
                      format: int64
                      maximum: 3600
                      minimum: 60
                      type: integer
                    coreDumpConfig:
                      description: Specifies configuration for a core dump from the
                        model container when the process crashes.
                      properties:
                        destinationS3Uri:
                          description: The Amazon S3 bucket to send the core dump
                            to.
                          type: string
                        kmsKeyId:
                          description: |-
                            The AWS Key Management Service (AWS KMS) key that SageMaker uses to encrypt the core dump data at rest using
                            Amazon S3 server-side encryption. If you use a KMS key ID or an alias of your KMS key, the SageMaker execution
                            role must include permissions to call kms:Encrypt.
                          type: string
                      required:
                      - destinationS3Uri
                      type: object
                    enableSSMAccess:
                      description: |-
                        You can use this parameter to turn on native AWS Systems Manager (SSM) access for a production variant behind
                        an endpoint. By default, SSM access is disabled for all production variants behind an endpoint.
                      type: boolean
                    inferenceAmiVersion:
                      description: |-
                        Specifies an option from a collection of preconfigured Amazon Machine Image (AMI) images. Each image is
                        configured by AWS with a set of software and driver versions. AWS optimizes these configurations for different
                        machine learning workloads. By selecting an AMI version, you can ensure that your inference environment is
                        compatible with specific software requirements, such as CUDA driver version...
                      type: string
                    initialInstanceCount:
                      description: Number of instances to launch initially.
                      format: int64
                      minimum: 1
                      type: integer
                    initialVariantWeight:
                      description: |-
                        Determines initial traffic distribution among all of the models that you specify in the endpoint
                        configuration.
                      type: number
                    instancePools:
                      description: |-
                        A list of instance pools for the production variant. Each instance pool specifies an instance type and its
                        priority for provisioning. Use instance pools to configure heterogeneous endpoints that deploy models across
                        multiple instance types.
                      items:
                        description: SageMakerEndpointConfigInstancePool is a nested
                          property type of AWS::SageMaker::EndpointConfig.
                        properties:
                          instanceType:
                            description: The ML compute instance type for the instance
                              pool.
                            type: string
                          modelNameOverride:
                            description: |-
                              The name of a SageMaker model to use for this instance pool instead of the model specified for the production
                              variant. Use this to deploy a different model optimized for the instance type in this pool.
                            type: string
                          priority:
                            description: |-
                              The priority for the instance pool. SageMaker attempts to provision instances in order of priority, starting
                              with the lowest value. If instances for a higher-priority pool are unavailable, SageMaker attempts to
                              provision from the next pool. Valid values: 1 to 5, where 1 is the highest priority.
                            format: int64
                            maximum: 5
                            minimum: 1
                            type: integer
                        required:
                        - instanceType
                        - priority
                        type: object
                      type: array
                    instanceType:
                      description: The ML compute instance type.
                      type: string
                    managedInstanceScaling:
                      description: SageMakerEndpointConfigManagedInstanceScaling is
                        a nested property type of AWS::SageMaker::EndpointConfig.
                      properties:
                        maxInstanceCount:
                          description: |-
                            The maximum number of instances that the endpoint can provision when it scales up to accommodate an increase
                            in traffic.
                          format: int64
                          type: integer
                        minInstanceCount:
                          description: |-
                            The minimum number of instances that the endpoint must retain when it scales down to accommodate a decrease in
                            traffic.
                          format: int64
                          type: integer
                        scaleInPolicy:
                          description: Configures the scale-in behavior for managed
                            instance scaling.
                          properties:
                            cooldownInMinutes:
                              description: |-
                                The cooldown period, in minutes, after the last endpoint operation before the endpoint evaluates consolidation
                                scale-in opportunities. Valid values are 5 to 1440. The default is 20.
                              format: int64
                              type: integer
                            maximumStepSize:
                              description: |-
                                The maximum number of instances that the endpoint can terminate at a time during a consolidation scale-in
                                operation. Valid values are 1 to 100. The default is 1.
                              format: int64
                              type: integer
                            strategy:
                              description: |-
                                The strategy for scaling in instances. IDLE_RELEASE releases instances that have no hosted inference component
                                copies. CONSOLIDATION consolidates inference component copies onto fewer instances to release more instances.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - strategy
                          type: object
                        status:
                          description: Indicates whether managed instance scaling
                            is enabled.
                          maxLength: 10
                          type: string
                      type: object
                    modelDataDownloadTimeoutInSeconds:
                      description: |-
                        The timeout value, in seconds, to download and extract the model that you want to host from Amazon S3 to the
                        individual inference instance associated with this production variant.
                      format: int64
                      maximum: 3600
                      minimum: 60
                      type: integer
                    modelName:
                      description: The name of the model that you want to host. This
                        is the name that you specified when creating the model.
                      maxLength: 63
                      type: string
                    routingConfig:
                      description: Settings that control how the endpoint routes incoming
                        traffic to the instances that the endpoint hosts.
                      properties:
                        prefixAwareRoutingConfig:
                          description: |-
                            The configuration for prefix-aware routing. Specify this property only when you set RoutingStrategy to
                            PREFIX_AWARE.
                          properties:
                            concurrencyThreshold:
                              description: |-
                                The maximum number of in-flight requests on the target instance before the endpoint routes to another
                                instance. Required when RoutingStrategy is PREFIX_AWARE. Valid values are 1 to 1024.
                              format: int64
                              type: integer
                            prefixLength:
                              description: |-
                                The maximum length of the prefix used for routing decisions. Required when RoutingStrategy is PREFIX_AWARE.
                                Valid values are 1024 to 65536.
                              format: int64
                              type: integer
                          type: object
                        routingStrategy:
                          description: Sets how the endpoint routes incoming traffic.
                          maxLength: 63
                          type: string
                      type: object
                    serverlessConfig:
                      description: |-
                        The serverless configuration for an endpoint. Specifies a serverless endpoint configuration instead of an
                        instance-based endpoint configuration.
                      properties:
                        maxConcurrency:
                          description: The maximum number of concurrent invocations
                            your serverless endpoint can process.
                          format: int64
                          type: integer
                        memorySizeInMB:
                          description: |-
                            The memory size of your serverless endpoint. Valid values are in 1 GB increments: 1024 MB, 2048 MB, 3072 MB,
                            4096 MB, 5120 MB, or 6144 MB.
                          format: int64
                          type: integer
                        provisionedConcurrency:
                          description: |-
                            The amount of provisioned concurrency to allocate for the serverless endpoint. Should be less than or equal to
                            MaxConcurrency.
                          format: int64
                          type: integer
                      required:
                      - maxConcurrency
                      - memorySizeInMB
                      type: object
                    variantInstanceProvisionTimeoutInSeconds:
                      description: |-
                        The timeout value, in seconds, for provisioning instances for the production variant. When SageMaker
                        encounters an insufficient capacity error while provisioning instances, it retries with the next instance pool
                        (if configured) or waits until the timeout expires. This timeout applies only to capacity provisioning and
                        does not include the time for model download or container startup.
                      format: int64
                      maximum: 3600
                      minimum: 300
                      type: integer
                    variantName:
                      description: The name of the production variant.
                      maxLength: 63
                      minLength: 1
                      type: string
                    volumeSizeInGB:
                      description: |-
                        The size, in GB, of the ML storage volume attached to individual inference instance associated with the
                        production variant. Currently only Amazon EBS gp2 storage volumes are supported.
                      format: int64
                      maximum: 512
                      minimum: 1
                      type: integer
                  required:
                  - variantName
                  type: object
                type: array
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              shadowProductionVariants:
                description: |-
                  Array of ProductionVariant objects. There is one for each model that you want to host at this endpoint in
                  shadow mode with production traffic replicated from the model specified on ProductionVariants. If you use this
                  field, you can only specify one variant for ProductionVariants and one variant for ShadowProductionVariants.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: SageMakerEndpointConfigProductionVariant is a nested
                    property type of AWS::SageMaker::EndpointConfig.
                  properties:
                    capacityReservationConfig:
                      description: Settings for the capacity reservation for the compute
                        instances that SageMaker AI reserves for an endpoint.
                      properties:
                        capacityReservationPreference:
                          description: Options that you can choose for the capacity
                            reservation.
                          type: string
                        mlReservationArn:
                          description: |-
                            The Amazon Resource Name (ARN) that uniquely identifies the ML capacity reservation that SageMaker AI applies
                            when it deploys the endpoint.
                          type: string
                      type: object
                    containerStartupHealthCheckTimeoutInSeconds:
                      description: The timeout value, in seconds, for your inference
                        container to pass health check by SageMaker Hosting.
                      format: int64
                      maximum: 3600
                      minimum: 60
                      type: integer
                    coreDumpConfig:
                      description: Specifies configuration for a core dump from the
                        model container when the process crashes.
                      properties:
                        destinationS3Uri:
                          description: The Amazon S3 bucket to send the core dump
                            to.
                          type: string
                        kmsKeyId:
                          description: |-
                            The AWS Key Management Service (AWS KMS) key that SageMaker uses to encrypt the core dump data at rest using
                            Amazon S3 server-side encryption. If you use a KMS key ID or an alias of your KMS key, the SageMaker execution
                            role must include permissions to call kms:Encrypt.
                          type: string
                      required:
                      - destinationS3Uri
                      type: object
                    enableSSMAccess:
                      description: |-
                        You can use this parameter to turn on native AWS Systems Manager (SSM) access for a production variant behind
                        an endpoint. By default, SSM access is disabled for all production variants behind an endpoint.
                      type: boolean
                    inferenceAmiVersion:
                      description: |-
                        Specifies an option from a collection of preconfigured Amazon Machine Image (AMI) images. Each image is
                        configured by AWS with a set of software and driver versions. AWS optimizes these configurations for different
                        machine learning workloads. By selecting an AMI version, you can ensure that your inference environment is
                        compatible with specific software requirements, such as CUDA driver version...
                      type: string
                    initialInstanceCount:
                      description: Number of instances to launch initially.
                      format: int64
                      minimum: 1
                      type: integer
                    initialVariantWeight:
                      description: |-
                        Determines initial traffic distribution among all of the models that you specify in the endpoint
                        configuration.
                      type: number
                    instancePools:
                      description: |-
                        A list of instance pools for the production variant. Each instance pool specifies an instance type and its
                        priority for provisioning. Use instance pools to configure heterogeneous endpoints that deploy models across
                        multiple instance types.
                      items:
                        description: SageMakerEndpointConfigInstancePool is a nested
                          property type of AWS::SageMaker::EndpointConfig.
                        properties:
                          instanceType:
                            description: The ML compute instance type for the instance
                              pool.
                            type: string
                          modelNameOverride:
                            description: |-
                              The name of a SageMaker model to use for this instance pool instead of the model specified for the production
                              variant. Use this to deploy a different model optimized for the instance type in this pool.
                            type: string
                          priority:
                            description: |-
                              The priority for the instance pool. SageMaker attempts to provision instances in order of priority, starting
                              with the lowest value. If instances for a higher-priority pool are unavailable, SageMaker attempts to
                              provision from the next pool. Valid values: 1 to 5, where 1 is the highest priority.
                            format: int64
                            maximum: 5
                            minimum: 1
                            type: integer
                        required:
                        - instanceType
                        - priority
                        type: object
                      type: array
                    instanceType:
                      description: The ML compute instance type.
                      type: string
                    managedInstanceScaling:
                      description: SageMakerEndpointConfigManagedInstanceScaling is
                        a nested property type of AWS::SageMaker::EndpointConfig.
                      properties:
                        maxInstanceCount:
                          description: |-
                            The maximum number of instances that the endpoint can provision when it scales up to accommodate an increase
                            in traffic.
                          format: int64
                          type: integer
                        minInstanceCount:
                          description: |-
                            The minimum number of instances that the endpoint must retain when it scales down to accommodate a decrease in
                            traffic.
                          format: int64
                          type: integer
                        scaleInPolicy:
                          description: Configures the scale-in behavior for managed
                            instance scaling.
                          properties:
                            cooldownInMinutes:
                              description: |-
                                The cooldown period, in minutes, after the last endpoint operation before the endpoint evaluates consolidation
                                scale-in opportunities. Valid values are 5 to 1440. The default is 20.
                              format: int64
                              type: integer
                            maximumStepSize:
                              description: |-
                                The maximum number of instances that the endpoint can terminate at a time during a consolidation scale-in
                                operation. Valid values are 1 to 100. The default is 1.
                              format: int64
                              type: integer
                            strategy:
                              description: |-
                                The strategy for scaling in instances. IDLE_RELEASE releases instances that have no hosted inference component
                                copies. CONSOLIDATION consolidates inference component copies onto fewer instances to release more instances.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - strategy
                          type: object
                        status:
                          description: Indicates whether managed instance scaling
                            is enabled.
                          maxLength: 10
                          type: string
                      type: object
                    modelDataDownloadTimeoutInSeconds:
                      description: |-
                        The timeout value, in seconds, to download and extract the model that you want to host from Amazon S3 to the
                        individual inference instance associated with this production variant.
                      format: int64
                      maximum: 3600
                      minimum: 60
                      type: integer
                    modelName:
                      description: The name of the model that you want to host. This
                        is the name that you specified when creating the model.
                      maxLength: 63
                      type: string
                    routingConfig:
                      description: Settings that control how the endpoint routes incoming
                        traffic to the instances that the endpoint hosts.
                      properties:
                        prefixAwareRoutingConfig:
                          description: |-
                            The configuration for prefix-aware routing. Specify this property only when you set RoutingStrategy to
                            PREFIX_AWARE.
                          properties:
                            concurrencyThreshold:
                              description: |-
                                The maximum number of in-flight requests on the target instance before the endpoint routes to another
                                instance. Required when RoutingStrategy is PREFIX_AWARE. Valid values are 1 to 1024.
                              format: int64
                              type: integer
                            prefixLength:
                              description: |-
                                The maximum length of the prefix used for routing decisions. Required when RoutingStrategy is PREFIX_AWARE.
                                Valid values are 1024 to 65536.
                              format: int64
                              type: integer
                          type: object
                        routingStrategy:
                          description: Sets how the endpoint routes incoming traffic.
                          maxLength: 63
                          type: string
                      type: object
                    serverlessConfig:
                      description: |-
                        The serverless configuration for an endpoint. Specifies a serverless endpoint configuration instead of an
                        instance-based endpoint configuration.
                      properties:
                        maxConcurrency:
                          description: The maximum number of concurrent invocations
                            your serverless endpoint can process.
                          format: int64
                          type: integer
                        memorySizeInMB:
                          description: |-
                            The memory size of your serverless endpoint. Valid values are in 1 GB increments: 1024 MB, 2048 MB, 3072 MB,
                            4096 MB, 5120 MB, or 6144 MB.
                          format: int64
                          type: integer
                        provisionedConcurrency:
                          description: |-
                            The amount of provisioned concurrency to allocate for the serverless endpoint. Should be less than or equal to
                            MaxConcurrency.
                          format: int64
                          type: integer
                      required:
                      - maxConcurrency
                      - memorySizeInMB
                      type: object
                    variantInstanceProvisionTimeoutInSeconds:
                      description: |-
                        The timeout value, in seconds, for provisioning instances for the production variant. When SageMaker
                        encounters an insufficient capacity error while provisioning instances, it retries with the next instance pool
                        (if configured) or waits until the timeout expires. This timeout applies only to capacity provisioning and
                        does not include the time for model download or container startup.
                      format: int64
                      maximum: 3600
                      minimum: 300
                      type: integer
                    variantName:
                      description: The name of the production variant.
                      maxLength: 63
                      minLength: 1
                      type: string
                    volumeSizeInGB:
                      description: |-
                        The size, in GB, of the ML storage volume attached to individual inference instance associated with the
                        production variant. Currently only Amazon EBS gp2 storage volumes are supported.
                      format: int64
                      maximum: 512
                      minimum: 1
                      type: integer
                  required:
                  - variantName
                  type: object
                type: array
              tags:
                description: A list of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              vpcConfig:
                description: |-
                  Specifies an Amazon Virtual Private Cloud (VPC) that your SageMaker jobs, hosted models, and compute resources
                  have access to. You can control access to and from your resources by configuring a VPC.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  securityGroupIds:
                    description: |-
                      The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                      in the Subnets field.
                    items:
                      type: string
                    type: array
                  subnets:
                    description: The ID of the subnets in the VPC to which you want
                      to connect your training job or model.
                    items:
                      type: string
                    type: array
                required:
                - securityGroupIds
                - subnets
                type: object
            required:
            - productionVariants
            type: object
          status:
            description: SageMakerEndpointConfigStatus is the observed state of AWS::SageMaker::EndpointConfig.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpointConfigArn:
                description: |-
                  The Amazon Resource Name (ARN) of the endpoint configuration.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerendpoints.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerEndpoint
    listKind: SageMakerEndpointList
    plural: sagemakerendpoints
    singular: sagemakerendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerEndpoint manages AWS::SageMaker::Endpoint through the AWS Cloud Control API. Resource Type definition
          for AWS::SageMaker::Endpoint
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerEndpointSpec is the desired state of AWS::SageMaker::Endpoint.
            properties:
              deploymentConfig:
                description: Specifies deployment configuration for updating the SageMaker
                  endpoint. Includes rollback and update policies.
                properties:
                  autoRollbackConfiguration:
                    description: Configuration for automatic rollback if an error
                      occurs during deployment.
                    properties:
                      alarms:
                        description: |-
                          List of CloudWatch alarms to monitor during the deployment. If any alarm goes off, the deployment is rolled
                          back.
                        items:
                          description: SageMakerEndpointAlarm is a nested property
                            type of AWS::SageMaker::Endpoint.
                          properties:
                            alarmName:
                              description: The name of the CloudWatch alarm.
                              maxLength: 255
                              minLength: 1
                              type: string
                          required:
                          - alarmName
                          type: object
                        type: array
                    required:
                    - alarms
                    type: object
                  blueGreenUpdatePolicy:
                    description: Configuration for blue-green update deployment policies.
                    properties:
                      maximumExecutionTimeoutInSeconds:
                        description: The maximum time allowed for the blue/green update,
                          in seconds.
                        format: int64
                        maximum: 28800
                        minimum: 600
                        type: integer
                      terminationWaitInSeconds:
                        description: The wait time before terminating the old endpoint
                          during a blue/green deployment.
                        format: int64
                        maximum: 3600
                        minimum: 0
                        type: integer
                      trafficRoutingConfiguration:
                        description: The traffic routing configuration for the blue/green
                          deployment.
                        properties:
                          canarySize:
                            description: Specifies the size of the canary traffic
                              in a canary deployment.
                            properties:
                              type:
                                description: Specifies whether the `Value` is an instance
                                  count or a capacity unit.
                                type: string
                              value:
                                description: The value representing either the number
                                  of instances or the number of capacity units.
                                format: int64
                                minimum: 1
                                type: integer
                            required:
                            - type
                            - value
                            type: object
                          linearStepSize:
                            description: Specifies the step size for linear traffic
                              routing.
                            properties:
                              type:
                                description: Specifies whether the `Value` is an instance
                                  count or a capacity unit.
                                type: string
                              value:
                                description: The value representing either the number
                                  of instances or the number of capacity units.
                                format: int64
                                minimum: 1
                                type: integer
                            required:
                            - type
                            - value
                            type: object
                          type:
                            description: Specifies the type of traffic routing (e.g.,
                              'AllAtOnce', 'Canary', 'Linear').
                            type: string
                          waitIntervalInSeconds:
                            description: Specifies the wait interval between traffic
                              shifts, in seconds.
                            format: int64
                            maximum: 3600
                            minimum: 0
                            type: integer
                        required:
                        - type
                        type: object
                    required:
                    - trafficRoutingConfiguration
                    type: object
                  rollingUpdatePolicy:
                    description: Configuration for rolling update deployment policies.
                    properties:
                      maximumBatchSize:
                        description: Specifies the maximum batch size for each rolling
                          update.
                        properties:
                          type:
                            description: Specifies whether the `Value` is an instance
                              count or a capacity unit.
                            type: string
                          value:
                            description: The value representing either the number
                              of instances or the number of capacity units.
                            format: int64
                            minimum: 1
                            type: integer
                        required:
                        - type
                        - value
                        type: object
                      maximumExecutionTimeoutInSeconds:
                        description: The maximum time allowed for the rolling update,
                          in seconds.
                        format: int64
                        maximum: 28800
                        minimum: 600
                        type: integer
                      rollbackMaximumBatchSize:
                        description: The maximum batch size for rollback during an
                          update failure.
                        properties:
                          type:
                            description: Specifies whether the `Value` is an instance
                              count or a capacity unit.
                            type: string
                          value:
                            description: The value representing either the number
                              of instances or the number of capacity units.
                            format: int64
                            minimum: 1
                            type: integer
                        required:
                        - type
                        - value
                        type: object
                      waitIntervalInSeconds:
                        description: The time to wait between steps during the rolling
                          update, in seconds.
                        format: int64
                        maximum: 3600
                        minimum: 0
                        type: integer
                    required:
                    - maximumBatchSize
                    - waitIntervalInSeconds
                    type: object
                type: object
              endpointConfigName:
                description: The name of the endpoint configuration for the SageMaker
                  endpoint. This is a required property.
                maxLength: 64
                minLength: 1
                type: string
              endpointName:
                description: |-
                  The name of the SageMaker endpoint. This name must be unique within an AWS Region.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                type: string
              excludeRetainedVariantProperties:
                description: Specifies a list of variant properties that you want
                  to exclude when updating an endpoint.
                items:
                  description: SageMakerEndpointVariantProperty is a nested property
                    type of AWS::SageMaker::Endpoint.
                  properties:
                    variantPropertyType:
                      description: The type of variant property (e.g., 'DesiredInstanceCount',
                        'DesiredWeight', 'DataCaptureConfig').
                      type: string
                  type: object
                type: array
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              retainAllVariantProperties:
                description: When set to true, retains all variant properties for
                  an endpoint when it is updated.
                type: boolean
              retainDeploymentConfig:
                description: When set to true, retains the deployment configuration
                  during endpoint updates.
                type: boolean
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - endpointConfigName
            type: object
          status:
            description: SageMakerEndpointStatus is the observed state of AWS::SageMaker::Endpoint.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              endpointArn:
                description: |-
                  The Amazon Resource Name (ARN) of the endpoint.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerexperiments.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerExperiment
    listKind: SageMakerExperimentList
    plural: sagemakerexperiments
    singular: sagemakerexperiment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerExperiment manages AWS::SageMaker::Experiment through the AWS Cloud Control API. Resource type
          definition for AWS::SageMaker::Experiment. An experiment is a collection of related trials used to organize
          and track machine learning workflows.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerExperimentSpec is the desired state of AWS::SageMaker::Experiment.
            properties:
              description:
                description: The description of the experiment.
                maxLength: 3072
                type: string
              displayName:
                description: The name of the experiment as displayed. The name does
                  not need to be unique.
                maxLength: 120
                type: string
              experimentName:
                description: |-
                  The name of the experiment. Must be unique in your AWS account and is not case-sensitive.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 120
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: experimentName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: A list of tags to associate with the experiment.
                items:
                  description: SageMakerExperimentTagsItem is a nested property type
                    of AWS::SageMaker::Experiment.
                  properties:
                    key:
                      description: The tag key.
                      maxLength: 128
                      minLength: 1
                      type: string
                    value:
                      description: The tag value.
                      maxLength: 256
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - experimentName
            type: object
          status:
            description: SageMakerExperimentStatus is the observed state of AWS::SageMaker::Experiment.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the experiment.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  When the experiment was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  When the experiment was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerfeaturegroups.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerFeatureGroup
    listKind: SageMakerFeatureGroupList
    plural: sagemakerfeaturegroups
    singular: sagemakerfeaturegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerFeatureGroup manages AWS::SageMaker::FeatureGroup through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::FeatureGroup
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerFeatureGroupSpec is the desired state of AWS::SageMaker::FeatureGroup.
            properties:
              description:
                description: |-
                  Description about the FeatureGroup.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                type: string
              eventTimeFeatureName:
                description: |-
                  The Event Time Feature Name.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: eventTimeFeatureName is immutable
                  rule: self == oldSelf
              featureDefinitions:
                description: An Array of Feature Definition
                items:
                  description: SageMakerFeatureGroupFeatureDefinition is a nested
                    property type of AWS::SageMaker::FeatureGroup.
                  properties:
                    featureName:
                      maxLength: 64
                      minLength: 1
                      type: string
                    featureType:
                      enum:
                      - Integral
                      - Fractional
                      - String
                      type: string
                  required:
                  - featureName
                  - featureType
                  type: object
                type: array
              featureGroupName:
                description: |-
                  The Name of the FeatureGroup.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: featureGroupName is immutable
                  rule: self == oldSelf
              offlineStoreConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  dataCatalogConfig:
                    description: SageMakerFeatureGroupDataCatalogConfig is a nested
                      property type of AWS::SageMaker::FeatureGroup.
                    properties:
                      catalog:
                        maxLength: 255
                        minLength: 1
                        type: string
                      database:
                        maxLength: 255
                        minLength: 1
                        type: string
                      tableName:
                        maxLength: 255
                        minLength: 1
                        type: string
                    required:
                    - catalog
                    - database
                    - tableName
                    type: object
                  disableGlueTableCreation:
                    type: boolean
                  s3StorageConfig:
                    description: SageMakerFeatureGroupS3StorageConfig is a nested
                      property type of AWS::SageMaker::FeatureGroup.
                    properties:
                      kmsKeyId:
                        maxLength: 2048
                        type: string
                      s3Uri:
                        maxLength: 1024
                        type: string
                    required:
                    - s3Uri
                    type: object
                  tableFormat:
                    enum:
                    - Iceberg
                    - Glue
                    type: string
                required:
                - s3StorageConfig
                type: object
              onlineStoreConfig:
                description: SageMakerFeatureGroupOnlineStoreConfig is a nested property
                  type of AWS::SageMaker::FeatureGroup.
                properties:
                  enableOnlineStore:
                    type: boolean
                  securityConfig:
                    description: SageMakerFeatureGroupOnlineStoreSecurityConfig is
                      a nested property type of AWS::SageMaker::FeatureGroup.
                    properties:
                      kmsKeyId:
                        maxLength: 2048
                        type: string
                    type: object
                  storageType:
                    enum:
                    - Standard
                    - InMemory
                    type: string
                  ttlDuration:
                    description: SageMakerFeatureGroupTtlDuration is a nested property
                      type of AWS::SageMaker::FeatureGroup.
                    properties:
                      unit:
                        enum:
                        - Seconds
                        - Minutes
                        - Hours
                        - Days
                        - Weeks
                        type: string
                      value:
                        format: int64
                        type: integer
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              recordIdentifierFeatureName:
                description: |-
                  The Record Identifier Feature Name.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: recordIdentifierFeatureName is immutable
                  rule: self == oldSelf
              roleArn:
                description: |-
                  Role Arn
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              tags:
                description: |-
                  An array of key-value pair to apply to this resource.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              throughputConfig:
                description: SageMakerFeatureGroupThroughputConfig is a nested property
                  type of AWS::SageMaker::FeatureGroup.
                properties:
                  provisionedReadCapacityUnits:
                    description: |-
                      For provisioned feature groups with online store enabled, this indicates the read throughput you are billed
                      for and can consume without throttling.
                    format: int64
                    type: integer
                  provisionedWriteCapacityUnits:
                    description: |-
                      For provisioned feature groups, this indicates the write throughput you are billed for and can consume without
                      throttling.
                    format: int64
                    type: integer
                  throughputMode:
                    enum:
                    - OnDemand
                    - Provisioned
                    type: string
                required:
                - throughputMode
                type: object
            required:
            - eventTimeFeatureName
            - featureDefinitions
            - featureGroupName
            - recordIdentifierFeatureName
            type: object
          status:
            description: SageMakerFeatureGroupStatus is the observed state of AWS::SageMaker::FeatureGroup.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  A timestamp of FeatureGroup creation time.
                  Read-only: reported by AWS after creation.
                type: string
              featureGroupStatus:
                description: |-
                  The status of the feature group.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerhubs.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerHub
    listKind: SageMakerHubList
    plural: sagemakerhubs
    singular: sagemakerhub
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerHub manages AWS::SageMaker::Hub through the AWS Cloud Control API. Resource type definition for
          AWS::SageMaker::Hub
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerHubSpec is the desired state of AWS::SageMaker::Hub.
            properties:
              hubDescription:
                description: A description of the hub.
                maxLength: 1023
                type: string
              hubDisplayName:
                description: The display name of the hub.
                maxLength: 255
                type: string
              hubName:
                description: |-
                  The name of the hub.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                type: string
                x-kubernetes-validations:
                - message: hubName is immutable
                  rule: self == oldSelf
              hubSearchKeywords:
                description: The searchable keywords for the hub.
                items:
                  type: string
                type: array
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              s3StorageConfig:
                description: |-
                  The Amazon S3 storage configuration for the hub.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  s3OutputPath:
                    description: The Amazon S3 bucket prefix for hosting hub content.
                    maxLength: 1024
                    type: string
                type: object
              tags:
                description: Tags to associate with the hub.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - hubDescription
            - hubName
            type: object
          status:
            description: SageMakerHubStatus is the observed state of AWS::SageMaker::Hub.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The date and time that the hub was created.
                  Read-only: reported by AWS after creation.
                type: string
              hubArn:
                description: |-
                  The Amazon Resource Name (ARN) of the hub.
                  Read-only: reported by AWS after creation.
                maxLength: 255
                type: string
              hubStatus:
                description: |-
                  The status of the hub.
                  Read-only: reported by AWS after creation.
                enum:
                - InService
                - Creating
                - Updating
                - Deleting
                - CreateFailed
                - UpdateFailed
                - DeleteFailed
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  The date and time that the hub was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerhumantaskuis.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerHumanTaskUi
    listKind: SageMakerHumanTaskUiList
    plural: sagemakerhumantaskuis
    singular: sagemakerhumantaskui
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerHumanTaskUi manages AWS::SageMaker::HumanTaskUi through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::HumanTaskUi
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerHumanTaskUiSpec is the desired state of AWS::SageMaker::HumanTaskUi.
            properties:
              humanTaskUiName:
                description: |-
                  The name of the human task user interface.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: humanTaskUiName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: |-
                  An array of key-value pairs that contain metadata to help you categorize and organize a human review workflow
                  user interface.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              uiTemplate:
                description: |-
                  The Liquid template for the worker user interface.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  content:
                    description: The content of the Liquid template for the worker
                      user interface.
                    maxLength: 128000
                    minLength: 1
                    type: string
                required:
                - content
                type: object
            required:
            - humanTaskUiName
            type: object
          status:
            description: SageMakerHumanTaskUiStatus is the observed state of AWS::SageMaker::HumanTaskUi.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The timestamp when the human task user interface was created.
                  Read-only: reported by AWS after creation.
                type: string
              humanTaskUiArn:
                description: |-
                  The Amazon Resource Name (ARN) of the human task user interface.
                  Read-only: reported by AWS after creation.
                maxLength: 1024
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerimages.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerImage
    listKind: SageMakerImageList
    plural: sagemakerimages
    singular: sagemakerimage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerImage manages AWS::SageMaker::Image through the AWS Cloud Control API. Resource Type definition for
          AWS::SageMaker::Image
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerImageSpec is the desired state of AWS::SageMaker::Image.
            properties:
              imageDescription:
                maxLength: 512
                type: string
              imageDisplayName:
                maxLength: 128
                type: string
              imageName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: imageName is immutable
                  rule: self == oldSelf
              imageRoleArn:
                maxLength: 256
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - imageName
            - imageRoleArn
            type: object
          status:
            description: SageMakerImageStatus is the observed state of AWS::SageMaker::Image.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              imageArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerimageversions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerImageVersion
    listKind: SageMakerImageVersionList
    plural: sagemakerimageversions
    singular: sagemakerimageversion
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerImageVersion manages AWS::SageMaker::ImageVersion through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::ImageVersion
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerImageVersionSpec is the desired state of AWS::SageMaker::ImageVersion.
            properties:
              alias:
                maxLength: 128
                type: string
              aliases:
                items:
                  type: string
                type: array
              baseImage:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 255
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: baseImage is immutable
                  rule: self == oldSelf
              horovod:
                type: boolean
              imageName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: imageName is immutable
                  rule: self == oldSelf
              jobType:
                enum:
                - TRAINING
                - INFERENCE
                - NOTEBOOK_KERNEL
                type: string
              mlFramework:
                maxLength: 128
                type: string
              processor:
                enum:
                - CPU
                - GPU
                type: string
              programmingLang:
                maxLength: 128
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              releaseNotes:
                maxLength: 255
                type: string
              vendorGuidance:
                enum:
                - NOT_PROVIDED
                - STABLE
                - TO_BE_ARCHIVED
                - ARCHIVED
                type: string
            required:
            - baseImage
            - imageName
            type: object
          status:
            description: SageMakerImageVersionStatus is the observed state of AWS::SageMaker::ImageVersion.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              containerImage:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 255
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              imageArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              imageVersionArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              version:
                description: 'Read-only: reported by AWS after creation.'
                format: int64
                minimum: 1
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerinferencecomponents.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerInferenceComponent
    listKind: SageMakerInferenceComponentList
    plural: sagemakerinferencecomponents
    singular: sagemakerinferencecomponent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerInferenceComponent manages AWS::SageMaker::InferenceComponent through the AWS Cloud Control API.
          Resource Type definition for AWS::SageMaker::InferenceComponent
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerInferenceComponentSpec is the desired state of AWS::SageMaker::InferenceComponent.
            properties:
              deploymentConfig:
                description: SageMakerInferenceComponentInferenceComponentDeploymentConfig
                  is a nested property type of AWS::SageMaker::InferenceComponent.
                properties:
                  autoRollbackConfiguration:
                    description: SageMakerInferenceComponentAutoRollbackConfiguration
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      alarms:
                        items:
                          description: SageMakerInferenceComponentAlarm is a nested
                            property type of AWS::SageMaker::InferenceComponent.
                          properties:
                            alarmName:
                              maxLength: 255
                              minLength: 1
                              type: string
                          required:
                          - alarmName
                          type: object
                        type: array
                    required:
                    - alarms
                    type: object
                  rollingUpdatePolicy:
                    description: SageMakerInferenceComponentInferenceComponentRollingUpdatePolicy
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      maximumBatchSize:
                        description: SageMakerInferenceComponentInferenceComponentCapacitySize
                          is a nested property type of AWS::SageMaker::InferenceComponent.
                        properties:
                          type:
                            enum:
                            - COPY_COUNT
                            - CAPACITY_PERCENT
                            type: string
                          value:
                            format: int64
                            type: integer
                        required:
                        - type
                        - value
                        type: object
                      maximumExecutionTimeoutInSeconds:
                        format: int64
                        maximum: 28800
                        minimum: 600
                        type: integer
                      rollbackMaximumBatchSize:
                        description: SageMakerInferenceComponentInferenceComponentCapacitySize
                          is a nested property type of AWS::SageMaker::InferenceComponent.
                        properties:
                          type:
                            enum:
                            - COPY_COUNT
                            - CAPACITY_PERCENT
                            type: string
                          value:
                            format: int64
                            type: integer
                        required:
                        - type
                        - value
                        type: object
                      waitIntervalInSeconds:
                        format: int64
                        maximum: 3600
                        minimum: 0
                        type: integer
                    type: object
                type: object
              endpointArn:
                maxLength: 256
                type: string
              endpointName:
                maxLength: 63
                type: string
              inferenceComponentName:
                maxLength: 63
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              runtimeConfig:
                description: SageMakerInferenceComponentInferenceComponentRuntimeConfig
                  is a nested property type of AWS::SageMaker::InferenceComponent.
                properties:
                  copyCount:
                    format: int64
                    minimum: 0
                    type: integer
                  currentCopyCount:
                    format: int64
                    minimum: 0
                    type: integer
                  desiredCopyCount:
                    format: int64
                    minimum: 0
                    type: integer
                  placementStatus:
                    description: The placement status of the inference component across
                      instance types
                    items:
                      description: SageMakerInferenceComponentInferenceComponentPlacementStatus
                        is a nested property type of AWS::SageMaker::InferenceComponent.
                      properties:
                        currentCopyCount:
                          format: int64
                          minimum: 0
                          type: integer
                        instanceType:
                          maxLength: 64
                          type: string
                      required:
                      - currentCopyCount
                      - instanceType
                      type: object
                    type: array
                type: object
              specification:
                description: SageMakerInferenceComponentInferenceComponentSpecification
                  is a nested property type of AWS::SageMaker::InferenceComponent.
                properties:
                  baseInferenceComponentName:
                    maxLength: 63
                    type: string
                  computeResourceRequirements:
                    description: SageMakerInferenceComponentInferenceComponentComputeResourceRequirements
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      maxMemoryRequiredInMb:
                        format: int64
                        minimum: 128
                        type: integer
                      minMemoryRequiredInMb:
                        format: int64
                        minimum: 128
                        type: integer
                      numberOfAcceleratorDevicesRequired:
                        type: number
                      numberOfCpuCoresRequired:
                        type: number
                    type: object
                  container:
                    description: SageMakerInferenceComponentInferenceComponentContainerSpecification
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      artifactUrl:
                        maxLength: 1024
                        type: string
                      containerMetricsConfig:
                        description: SageMakerInferenceComponentContainerMetricsConfig
                          is a nested property type of AWS::SageMaker::InferenceComponent.
                        properties:
                          metricsEndpoints:
                            items:
                              description: SageMakerInferenceComponentMetricsEndpoint
                                is a nested property type of AWS::SageMaker::InferenceComponent.
                              properties:
                                metricPublishFrequencyInSeconds:
                                  description: |-
                                    The interval, in seconds, at which container metrics scraped from the endpoint are published to Amazon
                                    CloudWatch. Valid values per the SageMaker API Reference are 10, 30, 60, 120, 180, 240 and 300; the service
                                    validates the value.
                                  format: int64
                                  maximum: 300
                                  minimum: 10
                                  type: integer
                                metricsEndpointPath:
                                  description: The path to the Prometheus formatted
                                    metrics endpoint exposed by the container
                                  maxLength: 256
                                  type: string
                              required:
                              - metricsEndpointPath
                              type: object
                            type: array
                        required:
                        - metricsEndpoints
                        type: object
                      deployedImage:
                        description: SageMakerInferenceComponentDeployedImage is a
                          nested property type of AWS::SageMaker::InferenceComponent.
                        properties:
                          resolutionTime:
                            type: string
                          resolvedImage:
                            maxLength: 255
                            type: string
                          specifiedImage:
                            maxLength: 255
                            type: string
                        type: object
                      environment:
                        additionalProperties:
                          type: string
                        type: object
                      image:
                        maxLength: 255
                        type: string
                    type: object
                  currentDataCacheConfig:
                    description: |-
                      The data caching configuration actually in effect, including a value the service chose rather than the
                      template: SageMaker enables caching automatically on instance types with more than 232 GiB of local NVMe
                      storage, whether or not DataCacheConfig was set. Returned by Describe and not settable; set DataCacheConfig
                      instead.
                    properties:
                      enableCaching:
                        description: |-
                          Whether the endpoint caches the model artifacts and container image on each instance it provisions for the
                          inference component
                        type: boolean
                    required:
                    - enableCaching
                    type: object
                  dataCacheConfig:
                    description: SageMakerInferenceComponentInferenceComponentDataCacheConfig
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      enableCaching:
                        description: |-
                          Whether the endpoint caches the model artifacts and container image on each instance it provisions for the
                          inference component
                        type: boolean
                    required:
                    - enableCaching
                    type: object
                  modelName:
                    maxLength: 63
                    type: string
                  schedulingConfig:
                    description: SageMakerInferenceComponentInferenceComponentSchedulingConfig
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      availabilityZoneBalance:
                        description: SageMakerInferenceComponentInferenceComponentAvailabilityZoneBalance
                          is a nested property type of AWS::SageMaker::InferenceComponent.
                        properties:
                          enforcementMode:
                            enum:
                            - PERMISSIVE
                            type: string
                          maxImbalance:
                            description: The maximum allowed difference in the number
                              of inference component copies between any two Availability
                              Zones
                            format: int64
                            maximum: 100
                            minimum: 0
                            type: integer
                        required:
                        - enforcementMode
                        type: object
                      placementStrategy:
                        enum:
                        - SPREAD
                        - BINPACK
                        type: string
                    required:
                    - availabilityZoneBalance
                    - placementStrategy
                    type: object
                  startupParameters:
                    description: SageMakerInferenceComponentInferenceComponentStartupParameters
                      is a nested property type of AWS::SageMaker::InferenceComponent.
                    properties:
                      containerStartupHealthCheckTimeoutInSeconds:
                        format: int64
                        maximum: 3600
                        minimum: 60
                        type: integer
                      modelDataDownloadTimeoutInSeconds:
                        format: int64
                        maximum: 3600
                        minimum: 60
                        type: integer
                    type: object
                type: object
              specifications:
                items:
                  description: SageMakerInferenceComponentInferenceComponentSpecificationForInstanceType
                    is a nested property type of AWS::SageMaker::InferenceComponent.
                  properties:
                    computeResourceRequirements:
                      description: SageMakerInferenceComponentInferenceComponentComputeResourceRequirements
                        is a nested property type of AWS::SageMaker::InferenceComponent.
                      properties:
                        maxMemoryRequiredInMb:
                          format: int64
                          minimum: 128
                          type: integer
                        minMemoryRequiredInMb:
                          format: int64
                          minimum: 128
                          type: integer
                        numberOfAcceleratorDevicesRequired:
                          type: number
                        numberOfCpuCoresRequired:
                          type: number
                      type: object
                    container:
                      description: SageMakerInferenceComponentInferenceComponentContainerSpecificationForInstanceType
                        is a nested property type of AWS::SageMaker::InferenceComponent.
                      properties:
                        artifactUrl:
                          maxLength: 1024
                          type: string
                        containerMetricsConfig:
                          description: SageMakerInferenceComponentContainerMetricsConfig
                            is a nested property type of AWS::SageMaker::InferenceComponent.
                          properties:
                            metricsEndpoints:
                              items:
                                description: SageMakerInferenceComponentMetricsEndpoint
                                  is a nested property type of AWS::SageMaker::InferenceComponent.
                                properties:
                                  metricPublishFrequencyInSeconds:
                                    description: |-
                                      The interval, in seconds, at which container metrics scraped from the endpoint are published to Amazon
                                      CloudWatch. Valid values per the SageMaker API Reference are 10, 30, 60, 120, 180, 240 and 300; the service
                                      validates the value.
                                    format: int64
                                    maximum: 300
                                    minimum: 10
                                    type: integer
                                  metricsEndpointPath:
                                    description: The path to the Prometheus formatted
                                      metrics endpoint exposed by the container
                                    maxLength: 256
                                    type: string
                                required:
                                - metricsEndpointPath
                                type: object
                              type: array
                          required:
                          - metricsEndpoints
                          type: object
                        environment:
                          additionalProperties:
                            type: string
                          type: object
                        image:
                          maxLength: 255
                          type: string
                      type: object
                    currentDataCacheConfig:
                      description: |-
                        The data caching configuration actually in effect for this instance type, including a value the service chose
                        rather than the template: SageMaker enables caching automatically on instance types with more than 232 GiB of
                        local NVMe storage, whether or not DataCacheConfig was set. Returned by Describe and not settable; set
                        DataCacheConfig instead.
                      properties:
                        enableCaching:
                          description: |-
                            Whether the endpoint caches the model artifacts and container image on each instance it provisions for the
                            inference component
                          type: boolean
                      required:
                      - enableCaching
                      type: object
                    dataCacheConfig:
                      description: SageMakerInferenceComponentInferenceComponentDataCacheConfig
                        is a nested property type of AWS::SageMaker::InferenceComponent.
                      properties:
                        enableCaching:
                          description: |-
                            Whether the endpoint caches the model artifacts and container image on each instance it provisions for the
                            inference component
                          type: boolean
                      required:
                      - enableCaching
                      type: object
                    instanceType:
                      maxLength: 64
                      type: string
                    modelName:
                      maxLength: 63
                      type: string
                    schedulingConfig:
                      description: SageMakerInferenceComponentInferenceComponentSchedulingConfig
                        is a nested property type of AWS::SageMaker::InferenceComponent.
                      properties:
                        availabilityZoneBalance:
                          description: SageMakerInferenceComponentInferenceComponentAvailabilityZoneBalance
                            is a nested property type of AWS::SageMaker::InferenceComponent.
                          properties:
                            enforcementMode:
                              enum:
                              - PERMISSIVE
                              type: string
                            maxImbalance:
                              description: The maximum allowed difference in the number
                                of inference component copies between any two Availability
                                Zones
                              format: int64
                              maximum: 100
                              minimum: 0
                              type: integer
                          required:
                          - enforcementMode
                          type: object
                        placementStrategy:
                          enum:
                          - SPREAD
                          - BINPACK
                          type: string
                      required:
                      - availabilityZoneBalance
                      - placementStrategy
                      type: object
                    startupParameters:
                      description: SageMakerInferenceComponentInferenceComponentStartupParameters
                        is a nested property type of AWS::SageMaker::InferenceComponent.
                      properties:
                        containerStartupHealthCheckTimeoutInSeconds:
                          format: int64
                          maximum: 3600
                          minimum: 60
                          type: integer
                        modelDataDownloadTimeoutInSeconds:
                          format: int64
                          maximum: 3600
                          minimum: 60
                          type: integer
                      type: object
                  required:
                  - instanceType
                  type: object
                type: array
              tags:
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              variantName:
                maxLength: 63
                type: string
            required:
            - endpointName
            type: object
          status:
            description: SageMakerInferenceComponentStatus is the observed state of
              AWS::SageMaker::InferenceComponent.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              failureReason:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 63
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              inferenceComponentArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              inferenceComponentStatus:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - InService
                - Creating
                - Updating
                - Failed
                - Deleting
                type: string
              lastModifiedTime:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerinferenceexperiments.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerInferenceExperiment
    listKind: SageMakerInferenceExperimentList
    plural: sagemakerinferenceexperiments
    singular: sagemakerinferenceexperiment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerInferenceExperiment manages AWS::SageMaker::InferenceExperiment through the AWS Cloud Control API.
          Resource Type definition for AWS::SageMaker::InferenceExperiment
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerInferenceExperimentSpec is the desired state of
              AWS::SageMaker::InferenceExperiment.
            properties:
              dataStorageConfig:
                description: SageMakerInferenceExperimentDataStorageConfig is a nested
                  property type of AWS::SageMaker::InferenceExperiment.
                properties:
                  contentType:
                    description: SageMakerInferenceExperimentCaptureContentTypeHeader
                      is a nested property type of AWS::SageMaker::InferenceExperiment.
                    properties:
                      csvContentTypes:
                        description: The list of all content type headers that SageMaker
                          will treat as CSV and capture accordingly.
                        items:
                          type: string
                        type: array
                      jsonContentTypes:
                        description: The list of all content type headers that SageMaker
                          will treat as JSON and capture accordingly.
                        items:
                          type: string
                        type: array
                    type: object
                  destination:
                    description: The Amazon S3 bucket where the inference request
                      and response data is stored.
                    maxLength: 512
                    type: string
                  kmsKey:
                    description: |-
                      The AWS Key Management Service key that Amazon SageMaker uses to encrypt captured data at rest using Amazon S3
                      server-side encryption.
                    maxLength: 2048
                    type: string
                required:
                - destination
                type: object
              description:
                description: The description of the inference experiment.
                maxLength: 1024
                type: string
              desiredState:
                description: The desired state of the experiment after starting or
                  stopping operation.
                enum:
                - Running
                - Completed
                - Cancelled
                type: string
              endpointName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
                x-kubernetes-validations:
                - message: endpointName is immutable
                  rule: self == oldSelf
              kmsKey:
                description: |-
                  The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                  attached to the ML compute instance that hosts the endpoint.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              modelVariants:
                description: |-
                  An array of ModelVariantConfig objects. Each ModelVariantConfig object in the array describes the
                  infrastructure configuration for the corresponding variant.
                items:
                  description: SageMakerInferenceExperimentModelVariantConfig is a
                    nested property type of AWS::SageMaker::InferenceExperiment.
                  properties:
                    infrastructureConfig:
                      description: SageMakerInferenceExperimentModelInfrastructureConfig
                        is a nested property type of AWS::SageMaker::InferenceExperiment.
                      properties:
                        infrastructureType:
                          description: The type of the inference experiment that you
                            want to run.
                          enum:
                          - RealTimeInference
                          type: string
                        realTimeInferenceConfig:
                          description: SageMakerInferenceExperimentRealTimeInferenceConfig
                            is a nested property type of AWS::SageMaker::InferenceExperiment.
                          properties:
                            instanceCount:
                              description: The number of instances of the type specified
                                by InstanceType.
                              format: int64
                              type: integer
                            instanceType:
                              description: The instance type the model is deployed
                                to.
                              type: string
                          required:
                          - instanceCount
                          - instanceType
                          type: object
                      required:
                      - infrastructureType
                      - realTimeInferenceConfig
                      type: object
                    modelName:
                      description: The name of the Amazon SageMaker Model entity.
                      maxLength: 63
                      type: string
                    variantName:
                      description: The name of the variant.
                      maxLength: 63
                      type: string
                  required:
                  - infrastructureConfig
                  - modelName
                  - variantName
                  type: object
                type: array
              name:
                description: |-
                  The name for the inference experiment.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 120
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to access model artifacts and
                  container images, and manage Amazon SageMaker Inference endpoints for model deployment.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              schedule:
                description: SageMakerInferenceExperimentInferenceExperimentSchedule
                  is a nested property type of AWS::SageMaker::InferenceExperiment.
                properties:
                  endTime:
                    description: The timestamp at which the inference experiment ended
                      or will end.
                    type: string
                  startTime:
                    description: The timestamp at which the inference experiment started
                      or will start.
                    type: string
                type: object
              shadowModeConfig:
                description: SageMakerInferenceExperimentShadowModeConfig is a nested
                  property type of AWS::SageMaker::InferenceExperiment.
                properties:
                  shadowModelVariants:
                    description: List of shadow variant configurations.
                    items:
                      description: SageMakerInferenceExperimentShadowModelVariantConfig
                        is a nested property type of AWS::SageMaker::InferenceExperiment.
                      properties:
                        samplingPercentage:
                          description: |-
                            The percentage of inference requests that Amazon SageMaker replicates from the production variant to the
                            shadow variant.
                          format: int64
                          maximum: 100
                          type: integer
                        shadowModelVariantName:
                          description: The name of the shadow variant.
                          maxLength: 63
                          type: string
                      required:
                      - samplingPercentage
                      - shadowModelVariantName
                      type: object
                    type: array
                  sourceModelVariantName:
                    description: The name of the production variant, which takes all
                      the inference requests.
                    maxLength: 63
                    type: string
                required:
                - shadowModelVariants
                - sourceModelVariantName
                type: object
              statusReason:
                description: |-
                  The error message or client-specified reason from the StopInferenceExperiment API, that explains the status of
                  the inference experiment.
                maxLength: 1024
                type: string
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              type:
                description: |-
                  The type of the inference experiment that you want to run.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - ShadowMode
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - endpointName
            - modelVariants
            - name
            - roleArn
            - type
            type: object
          status:
            description: SageMakerInferenceExperimentStatus is the observed state
              of AWS::SageMaker::InferenceExperiment.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the inference experiment.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The timestamp at which you created the inference experiment.
                  Read-only: reported by AWS after creation.
                type: string
              endpointMetadata:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  endpointConfigName:
                    description: The name of the endpoint configuration.
                    maxLength: 63
                    type: string
                  endpointName:
                    maxLength: 63
                    type: string
                  endpointStatus:
                    description: The status of the endpoint. For possible values of
                      the status of an endpoint.
                    enum:
                    - Creating
                    - Updating
                    - SystemUpdating
                    - RollingBack
                    - InService
                    - OutOfService
                    - Deleting
                    - Failed
                    type: string
                required:
                - endpointName
                type: object
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  The timestamp at which you last modified the inference experiment.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The status of the inference experiment.
                  Read-only: reported by AWS after creation.
                enum:
                - Creating
                - Created
                - Updating
                - Starting
                - Stopping
                - Running
                - Completed
                - Cancelled
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermlflowapps.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerMlflowApp
    listKind: SageMakerMlflowAppList
    plural: sagemakermlflowapps
    singular: sagemakermlflowapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerMlflowApp manages AWS::SageMaker::MlflowApp through the AWS Cloud Control API. Resource type
          definition for AWS::SageMaker::MlflowApp
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerMlflowAppSpec is the desired state of AWS::SageMaker::MlflowApp.
            properties:
              artifactStoreUri:
                description: The S3 URI for a general purpose bucket to use as the
                  MLflow App artifact store.
                maxLength: 1024
                type: string
              modelRegistrationMode:
                description: Whether to enable or disable automatic registration of
                  new MLflow models to the SageMaker Model Registry.
                enum:
                - AutoModelRegistrationEnabled
                - AutoModelRegistrationDisabled
                type: string
              name:
                description: |-
                  The name of the MLflow App.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) for an IAM role in your account that the MLflow App uses to access the artifact
                  store in Amazon S3.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              tags:
                description: Tags to associate with the MLflow App.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              weeklyMaintenanceWindowStart:
                description: |-
                  The day and time of the week in Coordinated Universal Time (UTC) 24-hour standard time that weekly maintenance
                  updates are scheduled. For example: Tue:03:30.
                maxLength: 9
                type: string
            required:
            - artifactStoreUri
            - name
            - roleArn
            type: object
          status:
            description: SageMakerMlflowAppStatus is the observed state of AWS::SageMaker::MlflowApp.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the MLflow App.
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The date and time that the MLflow App was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  The date and time that the MLflow App was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              mlflowAppId:
                description: |-
                  The server-generated identifier of the MLflow App.
                  Read-only: reported by AWS after creation.
                type: string
              mlflowVersion:
                description: |-
                  The MLflow version used by the MLflow App.
                  Read-only: reported by AWS after creation.
                maxLength: 16
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The status of the MLflow App.
                  Read-only: reported by AWS after creation.
                enum:
                - Creating
                - Created
                - CreateFailed
                - Updating
                - Updated
                - UpdateFailed
                - Deleting
                - DeleteFailed
                - Deleted
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermlflowtrackingservers.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerMlflowTrackingServer
    listKind: SageMakerMlflowTrackingServerList
    plural: sagemakermlflowtrackingservers
    singular: sagemakermlflowtrackingserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerMlflowTrackingServer manages AWS::SageMaker::MlflowTrackingServer through the AWS Cloud Control API.
          Resource Type definition for AWS::SageMaker::MlflowTrackingServer
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerMlflowTrackingServerSpec is the desired state of
              AWS::SageMaker::MlflowTrackingServer.
            properties:
              artifactStoreUri:
                description: The Amazon S3 URI for MLFlow Tracking Server artifacts.
                maxLength: 2048
                minLength: 1
                type: string
              automaticModelRegistration:
                description: A flag to enable Automatic SageMaker Model Registration.
                type: boolean
              mlflowVersion:
                description: The MLFlow Version used on the MLFlow Tracking Server.
                maxLength: 32
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that enables Amazon SageMaker to perform tasks on behalf of the
                  customer.
                maxLength: 2048
                minLength: 20
                type: string
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              trackingServerName:
                description: |-
                  The name of the MLFlow Tracking Server.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: trackingServerName is immutable
                  rule: self == oldSelf
              trackingServerSize:
                description: The size of the MLFlow Tracking Server.
                enum:
                - Small
                - Medium
                - Large
                type: string
              weeklyMaintenanceWindowStart:
                description: The start of the time window for maintenance of the MLFlow
                  Tracking Server in UTC time.
                maxLength: 9
                type: string
            required:
            - artifactStoreUri
            - roleArn
            - trackingServerName
            type: object
          status:
            description: SageMakerMlflowTrackingServerStatus is the observed state
              of AWS::SageMaker::MlflowTrackingServer.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              trackingServerArn:
                description: |-
                  The Amazon Resource Name (ARN) of the MLFlow Tracking Server.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodelbiasjobdefinitions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModelBiasJobDefinition
    listKind: SageMakerModelBiasJobDefinitionList
    plural: sagemakermodelbiasjobdefinitions
    singular: sagemakermodelbiasjobdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModelBiasJobDefinition manages AWS::SageMaker::ModelBiasJobDefinition through the AWS Cloud Control
          API. Resource Type definition for AWS::SageMaker::ModelBiasJobDefinition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelBiasJobDefinitionSpec is the desired state
              of AWS::SageMaker::ModelBiasJobDefinition.
            properties:
              endpointName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobDefinitionName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobResources:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  clusterConfig:
                    description: SageMakerModelBiasJobDefinitionClusterConfig is a
                      nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                    properties:
                      instanceCount:
                        description: |-
                          The number of ML compute instances to use in the model monitoring job. For distributed processing jobs,
                          specify a value greater than 1. The default value is 1.
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      instanceType:
                        description: The ML compute instance type for the processing
                          job.
                        type: string
                      volumeKmsKeyId:
                        description: |-
                          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                          attached to the ML compute instance(s) that run the model monitoring job.
                        type: string
                      volumeSizeInGB:
                        description: |-
                          The size of the ML storage volume, in gigabytes, that you want to provision. You must specify sufficient ML
                          storage for your scenario.
                        format: int64
                        maximum: 16384
                        minimum: 1
                        type: integer
                    required:
                    - instanceCount
                    - instanceType
                    - volumeSizeInGB
                    type: object
                required:
                - clusterConfig
                type: object
              modelBiasAppSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  configUri:
                    description: The S3 URI to an analysis configuration file
                    maxLength: 1024
                    type: string
                  environment:
                    additionalProperties:
                      type: string
                    description: Sets the environment variables in the Docker container
                    type: object
                  imageUri:
                    description: The container image to be run by the monitoring job.
                    maxLength: 255
                    type: string
                required:
                - configUri
                - imageUri
                type: object
              modelBiasBaselineConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  baseliningJobName:
                    maxLength: 63
                    type: string
                  constraintsResource:
                    description: SageMakerModelBiasJobDefinitionConstraintsResource
                      is a nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          The Amazon S3 URI for baseline constraint file in Amazon S3 that the current monitoring job should validated
                          against.
                        maxLength: 1024
                        type: string
                    type: object
                type: object
              modelBiasJobInput:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  batchTransformInput:
                    description: SageMakerModelBiasJobDefinitionBatchTransformInput
                      is a nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                    properties:
                      dataCapturedDestinationS3Uri:
                        description: A URI that identifies the Amazon S3 storage location
                          where Batch Transform Job captures data.
                        maxLength: 512
                        type: string
                      datasetFormat:
                        description: SageMakerModelBiasJobDefinitionDatasetFormat
                          is a nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                        properties:
                          csv:
                            description: SageMakerModelBiasJobDefinitionCsv is a nested
                              property type of AWS::SageMaker::ModelBiasJobDefinition.
                            properties:
                              header:
                                description: A boolean flag indicating if given CSV
                                  has header
                                type: boolean
                            type: object
                          json:
                            description: SageMakerModelBiasJobDefinitionJson is a
                              nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                            properties:
                              line:
                                description: A boolean flag indicating if it is JSON
                                  line format
                                type: boolean
                            type: object
                          parquet:
                            type: boolean
                        type: object
                      endTimeOffset:
                        description: Monitoring end time offset, e.g. PT0H
                        maxLength: 15
                        type: string
                      featuresAttribute:
                        description: JSONpath to locate features in JSONlines dataset
                        maxLength: 256
                        type: string
                      inferenceAttribute:
                        description: Index or JSONpath to locate predicted label(s)
                        maxLength: 256
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      probabilityAttribute:
                        description: Index or JSONpath to locate probabilities
                        maxLength: 256
                        type: string
                      probabilityThresholdAttribute:
                        type: number
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                      startTimeOffset:
                        description: Monitoring start time offset, e.g. -PT1H
                        maxLength: 15
                        type: string
                    required:
                    - dataCapturedDestinationS3Uri
                    - datasetFormat
                    - localPath
                    type: object
                  endpointInput:
                    description: SageMakerModelBiasJobDefinitionEndpointInput is a
                      nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                    properties:
                      endTimeOffset:
                        description: Monitoring end time offset, e.g. PT0H
                        maxLength: 15
                        type: string
                      endpointName:
                        maxLength: 63
                        type: string
                      featuresAttribute:
                        description: JSONpath to locate features in JSONlines dataset
                        maxLength: 256
                        type: string
                      inferenceAttribute:
                        description: Index or JSONpath to locate predicted label(s)
                        maxLength: 256
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      probabilityAttribute:
                        description: Index or JSONpath to locate probabilities
                        maxLength: 256
                        type: string
                      probabilityThresholdAttribute:
                        type: number
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                      startTimeOffset:
                        description: Monitoring start time offset, e.g. -PT1H
                        maxLength: 15
                        type: string
                    required:
                    - endpointName
                    - localPath
                    type: object
                  groundTruthS3Input:
                    description: SageMakerModelBiasJobDefinitionMonitoringGroundTruthS3Input
                      is a nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                          job.
                        maxLength: 512
                        type: string
                    required:
                    - s3Uri
                    type: object
                required:
                - groundTruthS3Input
                type: object
              modelBiasJobOutputConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: |-
                      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest
                      using Amazon S3 server-side encryption.
                    maxLength: 2048
                    type: string
                  monitoringOutputs:
                    description: Monitoring outputs for monitoring jobs. This is where
                      the output of the periodic monitoring jobs is uploaded.
                    items:
                      description: SageMakerModelBiasJobDefinitionMonitoringOutput
                        is a nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                      properties:
                        s3Output:
                          description: SageMakerModelBiasJobDefinitionS3Output is
                            a nested property type of AWS::SageMaker::ModelBiasJobDefinition.
                          properties:
                            localPath:
                              description: |-
                                The local path to the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring job.
                                LocalPath is an absolute path for the output data.
                              maxLength: 256
                              type: string
                            s3UploadMode:
                              description: Whether to upload the results of the monitoring
                                job continuously or after the job completes.
                              enum:
                              - Continuous
                              - EndOfJob
                              type: string
                            s3Uri:
                              description: |-
                                A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                                job.
                              maxLength: 512
                              type: string
                          required:
                          - localPath
                          - s3Uri
                          type: object
                      required:
                      - s3Output
                      type: object
                    type: array
                required:
                - monitoringOutputs
                type: object
              networkConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  enableInterContainerTrafficEncryption:
                    description: |-
                      Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt
                      communications. Encryption provides greater security for distributed processing jobs, but the processing might
                      take longer.
                    type: boolean
                  enableNetworkIsolation:
                    description: Whether to allow inbound and outbound network calls
                      to and from the containers used for the processing job.
                    type: boolean
                  vpcConfig:
                    description: SageMakerModelBiasJobDefinitionVpcConfig is a nested
                      property type of AWS::SageMaker::ModelBiasJobDefinition.
                    properties:
                      securityGroupIds:
                        description: |-
                          The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                          in the Subnets field.
                        items:
                          type: string
                        type: array
                      subnets:
                        description: The ID of the subnets in the VPC to which you
                          want to connect to your monitoring jobs.
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroupIds
                    - subnets
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your
                  behalf.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              stoppingCondition:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  maxRuntimeInSeconds:
                    description: The maximum runtime allowed in seconds.
                    format: int64
                    maximum: 86400
                    minimum: 1
                    type: integer
                required:
                - maxRuntimeInSeconds
                type: object
              tags:
                description: |-
                  An array of key-value pairs to apply to this resource.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - jobResources
            - modelBiasAppSpecification
            - modelBiasJobInput
            - modelBiasJobOutputConfig
            - roleArn
            type: object
          status:
            description: SageMakerModelBiasJobDefinitionStatus is the observed state
              of AWS::SageMaker::ModelBiasJobDefinition.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the job definition was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              jobDefinitionArn:
                description: |-
                  The Amazon Resource Name (ARN) of job definition.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodelcards.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModelCard
    listKind: SageMakerModelCardList
    plural: sagemakermodelcards
    singular: sagemakermodelcard
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModelCard manages AWS::SageMaker::ModelCard through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::ModelCard.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelCardSpec is the desired state of AWS::SageMaker::ModelCard.
            properties:
              content:
                description: SageMakerModelCardContent is a nested property type of
                  AWS::SageMaker::ModelCard.
                properties:
                  additionalInformation:
                    description: SageMakerModelCardAdditionalInformation is a nested
                      property type of AWS::SageMaker::ModelCard.
                    properties:
                      caveatsAndRecommendations:
                        description: Caveats and recommendations for people who might
                          use this model in their applications.
                        maxLength: 2048
                        type: string
                      customDetails:
                        additionalProperties:
                          type: string
                        description: customer details.
                        type: object
                      ethicalConsiderations:
                        description: Any ethical considerations that the author wants
                          to provide.
                        maxLength: 2048
                        type: string
                    type: object
                  businessDetails:
                    description: SageMakerModelCardBusinessDetails is a nested property
                      type of AWS::SageMaker::ModelCard.
                    properties:
                      businessProblem:
                        description: What business problem does the model solve?
                        maxLength: 2048
                        type: string
                      businessStakeholders:
                        description: Business stakeholders.
                        maxLength: 2048
                        type: string
                      lineOfBusiness:
                        description: Line of business.
                        maxLength: 2048
                        type: string
                    type: object
                  evaluationDetails:
                    items:
                      description: SageMakerModelCardEvaluationDetail is a nested
                        property type of AWS::SageMaker::ModelCard.
                      properties:
                        datasets:
                          items:
                            type: string
                          type: array
                        evaluationJobArn:
                          maxLength: 256
                          type: string
                        evaluationObservation:
                          maxLength: 2096
                          type: string
                        metadata:
                          additionalProperties:
                            type: string
                          description: additional attributes associated with the evaluation
                            results.
                          type: object
                        metricGroups:
                          items:
                            description: SageMakerModelCardMetricGroup is a nested
                              property type of AWS::SageMaker::ModelCard.
                            properties:
                              metricData:
                                items:
                                  x-kubernetes-preserve-unknown-fields: true
                                type: array
                              name:
                                type: string
                            required:
                            - metricData
                            - name
                            type: object
                          type: array
                        name:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  intendedUses:
                    description: SageMakerModelCardIntendedUses is a nested property
                      type of AWS::SageMaker::ModelCard.
                    properties:
                      explanationsForRiskRating:
                        maxLength: 2048
                        type: string
                      factorsAffectingModelEfficiency:
                        maxLength: 2048
                        type: string
                      intendedUses:
                        description: intended use cases.
                        maxLength: 2048
                        type: string
                      purposeOfModel:
                        description: Why the model was developed?
                        maxLength: 2048
                        type: string
                      riskRating:
                        enum:
                        - High
                        - Medium
                        - Low
                        - Unknown
                        type: string
                    type: object
                  modelOverview:
                    description: SageMakerModelCardModelOverview is a nested property
                      type of AWS::SageMaker::ModelCard.
                    properties:
                      algorithmType:
                        description: Algorithm used to solve the problem.
                        maxLength: 1024
                        type: string
                      inferenceEnvironment:
                        description: Overview about the inference.
                        properties:
                          containerImage:
                            description: SageMaker inference image uri.
                            items:
                              type: string
                            type: array
                        type: object
                      modelArtifact:
                        description: Location of the model artifact.
                        items:
                          type: string
                        type: array
                      modelCreator:
                        description: Creator of model.
                        maxLength: 1024
                        type: string
                      modelDescription:
                        description: description of model.
                        maxLength: 1024
                        type: string
                      modelId:
                        description: SageMaker Model Arn or Non SageMaker Model id.
                        maxLength: 1024
                        type: string
                      modelName:
                        description: Name of the model.
                        maxLength: 1024
                        type: string
                      modelOwner:
                        description: Owner of model.
                        maxLength: 1024
                        type: string
                      modelVersion:
                        description: Version of the model.
                        type: number
                      problemType:
                        description: Problem being solved with the model.
                        maxLength: 1024
                        type: string
                    type: object
                  modelPackageDetails:
                    description: SageMakerModelCardModelPackageDetails is a nested
                      property type of AWS::SageMaker::ModelCard.
                    properties:
                      approvalDescription:
                        description: A description provided for the model approval
                        maxLength: 1024
                        type: string
                      createdBy:
                        description: Information about the user who created model
                          package.
                        properties:
                          userProfileName:
                            description: The name of the user's profile in Studio
                            maxLength: 63
                            type: string
                        type: object
                      domain:
                        description: |-
                          The machine learning domain of the model package you specified. Common machine learning domains include
                          computer vision and natural language processing.
                        type: string
                      inferenceSpecification:
                        description: Details about inference jobs that can be run
                          with models based on this model package.
                        properties:
                          containers:
                            description: Contains inference related information which
                              were used to create model package.
                            items:
                              description: SageMakerModelCardContainer is a nested
                                property type of AWS::SageMaker::ModelCard.
                              properties:
                                image:
                                  description: |-
                                    Inference environment path. The Amazon EC2 Container Registry (Amazon ECR) path where inference code is
                                    stored.
                                  maxLength: 255
                                  type: string
                                modelDataUrl:
                                  description: The Amazon S3 path where the model
                                    artifacts, which result from model training, are
                                    stored.
                                  maxLength: 1024
                                  type: string
                                nearestModelName:
                                  description: |-
                                    The name of a pre-trained machine learning benchmarked by Amazon SageMaker Inference Recommender model that
                                    matches your model.
                                  type: string
                              required:
                              - image
                              type: object
                            type: array
                        required:
                        - containers
                        type: object
                      modelApprovalStatus:
                        description: Current approval status of model package
                        enum:
                        - Approved
                        - Rejected
                        - PendingManualApproval
                        type: string
                      modelPackageArn:
                        description: The Amazon Resource Name (ARN) of the model package
                        maxLength: 2048
                        type: string
                      modelPackageDescription:
                        description: A brief summary of the model package
                        maxLength: 1024
                        type: string
                      modelPackageGroupName:
                        description: If the model is a versioned model, the name of
                          the model group that the versioned model belongs to.
                        maxLength: 63
                        type: string
                      modelPackageName:
                        description: Name of the model package
                        maxLength: 63
                        type: string
                      modelPackageStatus:
                        description: Current status of model package
                        enum:
                        - Pending
                        - InProgress
                        - Completed
                        - Failed
                        - Deleting
                        type: string
                      modelPackageVersion:
                        description: Version of the model package
                        type: number
                      sourceAlgorithms:
                        description: A list of algorithms that were used to create
                          a model package.
                        items:
                          description: SageMakerModelCardSourceAlgorithm is a nested
                            property type of AWS::SageMaker::ModelCard.
                          properties:
                            algorithmName:
                              description: |-
                                The name of an algorithm that was used to create the model package. The algorithm must be either an algorithm
                                resource in your SageMaker account or an algorithm in AWS Marketplace that you are subscribed to.
                              maxLength: 170
                              type: string
                            modelDataUrl:
                              description: The Amazon S3 path where the model artifacts,
                                which result from model training, are stored.
                              maxLength: 1024
                              type: string
                          required:
                          - algorithmName
                          type: object
                        type: array
                      task:
                        description: |-
                          The machine learning task you specified that your model package accomplishes. Common machine learning tasks
                          include object detection and image classification.
                        type: string
                    type: object
                  trainingDetails:
                    description: SageMakerModelCardTrainingDetails is a nested property
                      type of AWS::SageMaker::ModelCard.
                    properties:
                      objectiveFunction:
                        description: SageMakerModelCardObjectiveFunction is a nested
                          property type of AWS::SageMaker::ModelCard.
                        properties:
                          function:
                            description: objective function that training job is optimized
                              for.
                            properties:
                              condition:
                                maxLength: 63
                                type: string
                              facet:
                                maxLength: 63
                                type: string
                              function:
                                enum:
                                - Maximize
                                - Minimize
                                type: string
                            type: object
                          notes:
                            maxLength: 1024
                            type: string
                        type: object
                      trainingJobDetails:
                        description: SageMakerModelCardTrainingDetailsTrainingJobDetails
                          is a nested property type of AWS::SageMaker::ModelCard.
                        properties:
                          hyperParameters:
                            items:
                              description: SageMakerModelCardTrainingHyperParameter
                                is a nested property type of AWS::SageMaker::ModelCard.
                              properties:
                                name:
                                  type: string
                                value:
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          trainingArn:
                            description: SageMaker Training job arn.
                            maxLength: 1024
                            type: string
                          trainingDatasets:
                            description: Location of the model datasets.
                            items:
                              type: string
                            type: array
                          trainingEnvironment:
                            description: SageMakerModelCardTrainingDetailsTrainingJobDetailsTrainingEnvironment
                              is a nested property type of AWS::SageMaker::ModelCard.
                            properties:
                              containerImage:
                                description: SageMaker training image uri.
                                items:
                                  type: string
                                type: array
                            type: object
                          trainingMetrics:
                            items:
                              description: SageMakerModelCardTrainingMetric is a nested
                                property type of AWS::SageMaker::ModelCard.
                              properties:
                                name:
                                  type: string
                                notes:
                                  maxLength: 1024
                                  type: string
                                value:
                                  type: number
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          userProvidedHyperParameters:
                            items:
                              description: SageMakerModelCardTrainingHyperParameter
                                is a nested property type of AWS::SageMaker::ModelCard.
                              properties:
                                name:
                                  type: string
                                value:
                                  type: string
                              required:
                              - name
                              - value
                              type: object
                            type: array
                          userProvidedTrainingMetrics:
                            items:
                              description: SageMakerModelCardTrainingMetric is a nested
                                property type of AWS::SageMaker::ModelCard.
                              properties:
                                name:
                                  type: string
                                notes:
                                  maxLength: 1024
                                  type: string
                                value:
                                  type: number
                              required:
                              - name
                              - value
                              type: object
                            type: array
                        type: object
                      trainingObservations:
                        maxLength: 1024
                        type: string
                    type: object
                type: object
              createdBy:
                description: |-
                  Information about the user who created or modified an experiment, trial, trial component, lineage group,
                  project, or model card.
                properties:
                  domainId:
                    description: The domain associated with the user.
                    type: string
                  userProfileArn:
                    description: The Amazon Resource Name (ARN) of the user's profile.
                    type: string
                  userProfileName:
                    description: The name of the user's profile.
                    type: string
                type: object
              lastModifiedBy:
                description: |-
                  Information about the user who created or modified an experiment, trial, trial component, lineage group,
                  project, or model card.
                properties:
                  domainId:
                    description: The domain associated with the user.
                    type: string
                  userProfileArn:
                    description: The Amazon Resource Name (ARN) of the user's profile.
                    type: string
                  userProfileName:
                    description: The name of the user's profile.
                    type: string
                type: object
              modelCardName:
                description: |-
                  The unique name of the model card.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                type: string
                x-kubernetes-validations:
                - message: modelCardName is immutable
                  rule: self == oldSelf
              modelCardStatus:
                description: |-
                  The approval status of the model card within your organization. Different organizations might have different
                  criteria for model card review and approval.
                enum:
                - Draft
                - PendingReview
                - Approved
                - Archived
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              securityConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: A Key Management Service key ID to use for encrypting
                      a model card.
                    maxLength: 2048
                    type: string
                type: object
              tags:
                description: Key-value pairs used to manage metadata for model cards.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - content
            - modelCardName
            - modelCardStatus
            type: object
          status:
            description: SageMakerModelCardStatus is the observed state of AWS::SageMaker::ModelCard.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The date and time the model card was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  The date and time the model card was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              modelCardArn:
                description: |-
                  The Amazon Resource Name (ARN) of the successfully created model card.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              modelCardProcessingStatus:
                description: |-
                  The processing status of model card deletion. The ModelCardProcessingStatus updates throughout the different
                  deletion steps.
                  Read-only: reported by AWS after creation.
                enum:
                - UnsetValue
                - DeleteInProgress
                - DeletePending
                - ContentDeleted
                - ExportJobsDeleted
                - DeleteCompleted
                - DeleteFailed
                type: string
              modelCardVersion:
                description: |-
                  A version of the model card.
                  Read-only: reported by AWS after creation.
                format: int64
                minimum: 1
                type: integer
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodelexplainabilityjobdefinitions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModelExplainabilityJobDefinition
    listKind: SageMakerModelExplainabilityJobDefinitionList
    plural: sagemakermodelexplainabilityjobdefinitions
    singular: sagemakermodelexplainabilityjobdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModelExplainabilityJobDefinition manages AWS::SageMaker::ModelExplainabilityJobDefinition through the
          AWS Cloud Control API. Resource Type definition for AWS::SageMaker::ModelExplainabilityJobDefinition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelExplainabilityJobDefinitionSpec is the desired
              state of AWS::SageMaker::ModelExplainabilityJobDefinition.
            properties:
              endpointName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobDefinitionName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobResources:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  clusterConfig:
                    description: SageMakerModelExplainabilityJobDefinitionClusterConfig
                      is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                    properties:
                      instanceCount:
                        description: |-
                          The number of ML compute instances to use in the model monitoring job. For distributed processing jobs,
                          specify a value greater than 1. The default value is 1.
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      instanceType:
                        description: The ML compute instance type for the processing
                          job.
                        type: string
                      volumeKmsKeyId:
                        description: |-
                          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                          attached to the ML compute instance(s) that run the model monitoring job.
                        type: string
                      volumeSizeInGB:
                        description: |-
                          The size of the ML storage volume, in gigabytes, that you want to provision. You must specify sufficient ML
                          storage for your scenario.
                        format: int64
                        maximum: 16384
                        minimum: 1
                        type: integer
                    required:
                    - instanceCount
                    - instanceType
                    - volumeSizeInGB
                    type: object
                required:
                - clusterConfig
                type: object
              modelExplainabilityAppSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  configUri:
                    description: The S3 URI to an analysis configuration file
                    maxLength: 1024
                    type: string
                  environment:
                    additionalProperties:
                      type: string
                    description: Sets the environment variables in the Docker container
                    type: object
                  imageUri:
                    description: The container image to be run by the monitoring job.
                    maxLength: 255
                    type: string
                required:
                - configUri
                - imageUri
                type: object
              modelExplainabilityBaselineConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  baseliningJobName:
                    maxLength: 63
                    type: string
                  constraintsResource:
                    description: SageMakerModelExplainabilityJobDefinitionConstraintsResource
                      is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          The Amazon S3 URI for baseline constraint file in Amazon S3 that the current monitoring job should validated
                          against.
                        maxLength: 1024
                        type: string
                    type: object
                type: object
              modelExplainabilityJobInput:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  batchTransformInput:
                    description: SageMakerModelExplainabilityJobDefinitionBatchTransformInput
                      is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                    properties:
                      dataCapturedDestinationS3Uri:
                        description: A URI that identifies the Amazon S3 storage location
                          where Batch Transform Job captures data.
                        maxLength: 512
                        type: string
                      datasetFormat:
                        description: SageMakerModelExplainabilityJobDefinitionDatasetFormat
                          is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                        properties:
                          csv:
                            description: SageMakerModelExplainabilityJobDefinitionCsv
                              is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                            properties:
                              header:
                                description: A boolean flag indicating if given CSV
                                  has header
                                type: boolean
                            type: object
                          json:
                            description: SageMakerModelExplainabilityJobDefinitionJson
                              is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                            properties:
                              line:
                                description: A boolean flag indicating if it is JSON
                                  line format
                                type: boolean
                            type: object
                          parquet:
                            type: boolean
                        type: object
                      featuresAttribute:
                        description: JSONpath to locate features in JSONlines dataset
                        maxLength: 256
                        type: string
                      inferenceAttribute:
                        description: Index or JSONpath to locate predicted label(s)
                        maxLength: 256
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      probabilityAttribute:
                        description: Index or JSONpath to locate probabilities
                        maxLength: 256
                        type: string
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                    required:
                    - dataCapturedDestinationS3Uri
                    - datasetFormat
                    - localPath
                    type: object
                  endpointInput:
                    description: SageMakerModelExplainabilityJobDefinitionEndpointInput
                      is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                    properties:
                      endpointName:
                        maxLength: 63
                        type: string
                      featuresAttribute:
                        description: JSONpath to locate features in JSONlines dataset
                        maxLength: 256
                        type: string
                      inferenceAttribute:
                        description: Index or JSONpath to locate predicted label(s)
                        maxLength: 256
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      probabilityAttribute:
                        description: Index or JSONpath to locate probabilities
                        maxLength: 256
                        type: string
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                    required:
                    - endpointName
                    - localPath
                    type: object
                type: object
              modelExplainabilityJobOutputConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: |-
                      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest
                      using Amazon S3 server-side encryption.
                    maxLength: 2048
                    type: string
                  monitoringOutputs:
                    description: Monitoring outputs for monitoring jobs. This is where
                      the output of the periodic monitoring jobs is uploaded.
                    items:
                      description: SageMakerModelExplainabilityJobDefinitionMonitoringOutput
                        is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                      properties:
                        s3Output:
                          description: SageMakerModelExplainabilityJobDefinitionS3Output
                            is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                          properties:
                            localPath:
                              description: |-
                                The local path to the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring job.
                                LocalPath is an absolute path for the output data.
                              maxLength: 256
                              type: string
                            s3UploadMode:
                              description: Whether to upload the results of the monitoring
                                job continuously or after the job completes.
                              enum:
                              - Continuous
                              - EndOfJob
                              type: string
                            s3Uri:
                              description: |-
                                A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                                job.
                              maxLength: 512
                              type: string
                          required:
                          - localPath
                          - s3Uri
                          type: object
                      required:
                      - s3Output
                      type: object
                    type: array
                required:
                - monitoringOutputs
                type: object
              networkConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  enableInterContainerTrafficEncryption:
                    description: |-
                      Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt
                      communications. Encryption provides greater security for distributed processing jobs, but the processing might
                      take longer.
                    type: boolean
                  enableNetworkIsolation:
                    description: Whether to allow inbound and outbound network calls
                      to and from the containers used for the processing job.
                    type: boolean
                  vpcConfig:
                    description: SageMakerModelExplainabilityJobDefinitionVpcConfig
                      is a nested property type of AWS::SageMaker::ModelExplainabilityJobDefinition.
                    properties:
                      securityGroupIds:
                        description: |-
                          The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                          in the Subnets field.
                        items:
                          type: string
                        type: array
                      subnets:
                        description: The ID of the subnets in the VPC to which you
                          want to connect to your monitoring jobs.
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroupIds
                    - subnets
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your
                  behalf.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              stoppingCondition:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  maxRuntimeInSeconds:
                    description: The maximum runtime allowed in seconds.
                    format: int64
                    maximum: 86400
                    minimum: 1
                    type: integer
                required:
                - maxRuntimeInSeconds
                type: object
              tags:
                description: |-
                  An array of key-value pairs to apply to this resource.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - jobResources
            - modelExplainabilityAppSpecification
            - modelExplainabilityJobInput
            - modelExplainabilityJobOutputConfig
            - roleArn
            type: object
          status:
            description: SageMakerModelExplainabilityJobDefinitionStatus is the observed
              state of AWS::SageMaker::ModelExplainabilityJobDefinition.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the job definition was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              jobDefinitionArn:
                description: |-
                  The Amazon Resource Name (ARN) of job definition.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodelpackagegroups.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModelPackageGroup
    listKind: SageMakerModelPackageGroupList
    plural: sagemakermodelpackagegroups
    singular: sagemakermodelpackagegroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModelPackageGroup manages AWS::SageMaker::ModelPackageGroup through the AWS Cloud Control API.
          Resource Type definition for AWS::SageMaker::ModelPackageGroup
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelPackageGroupSpec is the desired state of AWS::SageMaker::ModelPackageGroup.
            properties:
              modelPackageGroupDescription:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 1024
                type: string
              modelPackageGroupName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
                x-kubernetes-validations:
                - message: modelPackageGroupName is immutable
                  rule: self == oldSelf
              modelPackageGroupPolicy:
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - modelPackageGroupName
            type: object
          status:
            description: SageMakerModelPackageGroupStatus is the observed state of
              AWS::SageMaker::ModelPackageGroup.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the model package group was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              modelPackageGroupArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 256
                type: string
              modelPackageGroupStatus:
                description: |-
                  The status of a modelpackage group job.
                  Read-only: reported by AWS after creation.
                enum:
                - Pending
                - InProgress
                - Completed
                - Failed
                - Deleting
                - DeleteFailed
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodelpackages.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModelPackage
    listKind: SageMakerModelPackageList
    plural: sagemakermodelpackages
    singular: sagemakermodelpackage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModelPackage manages AWS::SageMaker::ModelPackage through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::ModelPackage
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelPackageSpec is the desired state of AWS::SageMaker::ModelPackage.
            properties:
              additionalInferenceSpecifications:
                items:
                  description: SageMakerModelPackageAdditionalInferenceSpecificationDefinition
                    is a nested property type of AWS::SageMaker::ModelPackage.
                  properties:
                    containers:
                      description: The Amazon ECR registry path of the Docker image
                        that contains the inference code.
                      items:
                        description: SageMakerModelPackageModelPackageContainerDefinition
                          is a nested property type of AWS::SageMaker::ModelPackage.
                        properties:
                          containerHostname:
                            description: The DNS host name for the Docker container.
                            maxLength: 63
                            type: string
                          environment:
                            additionalProperties:
                              type: string
                            type: object
                          framework:
                            description: The machine learning framework of the model
                              package container image.
                            type: string
                          frameworkVersion:
                            description: The framework version of the Model Package
                              Container Image.
                            maxLength: 10
                            type: string
                          image:
                            description: The Amazon EC2 Container Registry (Amazon
                              ECR) path where inference code is stored.
                            maxLength: 255
                            minLength: 1
                            type: string
                          imageDigest:
                            description: An MD5 hash of the training algorithm that
                              identifies the Docker image used for training.
                            maxLength: 72
                            type: string
                          modelDataSource:
                            description: SageMakerModelPackageModelDataSource is a
                              nested property type of AWS::SageMaker::ModelPackage.
                            properties:
                              s3DataSource:
                                description: SageMakerModelPackageS3ModelDataSource
                                  is a nested property type of AWS::SageMaker::ModelPackage.
                                properties:
                                  compressionType:
                                    description: Specifies how the ML model data is
                                      prepared.
                                    enum:
                                    - None
                                    - Gzip
                                    type: string
                                  modelAccessConfig:
                                    description: SageMakerModelPackageModelAccessConfig
                                      is a nested property type of AWS::SageMaker::ModelPackage.
                                    properties:
                                      acceptEula:
                                        description: Specifies agreement to the model
                                          end-user license agreement (EULA).
                                        type: boolean
                                    required:
                                    - acceptEula
                                    type: object
                                  s3DataType:
                                    description: Specifies the type of ML model data
                                      to deploy.
                                    enum:
                                    - S3Prefix
                                    - S3Object
                                    type: string
                                  s3Uri:
                                    description: Specifies the S3 path of ML model
                                      data to deploy.
                                    maxLength: 1024
                                    type: string
                                required:
                                - compressionType
                                - s3DataType
                                - s3Uri
                                type: object
                            type: object
                          modelDataUrl:
                            description: A structure with Model Input details.
                            maxLength: 1024
                            type: string
                          modelInput:
                            description: SageMakerModelPackageModelPackageContainerDefinitionModelInput
                              is a nested property type of AWS::SageMaker::ModelPackage.
                            properties:
                              dataInputConfig:
                                description: The input configuration object for the
                                  model.
                                maxLength: 1024
                                minLength: 1
                                type: string
                            required:
                            - dataInputConfig
                            type: object
                          nearestModelName:
                            description: |-
                              The name of a pre-trained machine learning benchmarked by Amazon SageMaker Inference Recommender model that
                              matches your model.
                            type: string
                        required:
                        - image
                        type: object
                      type: array
                    description:
                      description: A description of the additional Inference specification.
                      maxLength: 1024
                      type: string
                    name:
                      description: |-
                        A unique name to identify the additional inference specification. The name must be unique within the list of
                        your additional inference specifications for a particular model package.
                      maxLength: 63
                      minLength: 1
                      type: string
                    supportedContentTypes:
                      description: The supported MIME types for the input data.
                      items:
                        type: string
                      type: array
                    supportedRealtimeInferenceInstanceTypes:
                      description: A list of the instance types that are used to generate
                        inferences in real-time
                      items:
                        type: string
                      type: array
                    supportedResponseMIMETypes:
                      description: The supported MIME types for the output data.
                      items:
                        type: string
                      type: array
                    supportedTransformInstanceTypes:
                      description: A list of the instance types on which a transformation
                        job can be run or on which an endpoint can be deployed.
                      items:
                        type: string
                      type: array
                  required:
                  - containers
                  - name
                  type: object
                type: array
              additionalInferenceSpecificationsToAdd:
                items:
                  description: SageMakerModelPackageAdditionalInferenceSpecificationDefinition
                    is a nested property type of AWS::SageMaker::ModelPackage.
                  properties:
                    containers:
                      description: The Amazon ECR registry path of the Docker image
                        that contains the inference code.
                      items:
                        description: SageMakerModelPackageModelPackageContainerDefinition
                          is a nested property type of AWS::SageMaker::ModelPackage.
                        properties:
                          containerHostname:
                            description: The DNS host name for the Docker container.
                            maxLength: 63
                            type: string
                          environment:
                            additionalProperties:
                              type: string
                            type: object
                          framework:
                            description: The machine learning framework of the model
                              package container image.
                            type: string
                          frameworkVersion:
                            description: The framework version of the Model Package
                              Container Image.
                            maxLength: 10
                            type: string
                          image:
                            description: The Amazon EC2 Container Registry (Amazon
                              ECR) path where inference code is stored.
                            maxLength: 255
                            minLength: 1
                            type: string
                          imageDigest:
                            description: An MD5 hash of the training algorithm that
                              identifies the Docker image used for training.
                            maxLength: 72
                            type: string
                          modelDataSource:
                            description: SageMakerModelPackageModelDataSource is a
                              nested property type of AWS::SageMaker::ModelPackage.
                            properties:
                              s3DataSource:
                                description: SageMakerModelPackageS3ModelDataSource
                                  is a nested property type of AWS::SageMaker::ModelPackage.
                                properties:
                                  compressionType:
                                    description: Specifies how the ML model data is
                                      prepared.
                                    enum:
                                    - None
                                    - Gzip
                                    type: string
                                  modelAccessConfig:
                                    description: SageMakerModelPackageModelAccessConfig
                                      is a nested property type of AWS::SageMaker::ModelPackage.
                                    properties:
                                      acceptEula:
                                        description: Specifies agreement to the model
                                          end-user license agreement (EULA).
                                        type: boolean
                                    required:
                                    - acceptEula
                                    type: object
                                  s3DataType:
                                    description: Specifies the type of ML model data
                                      to deploy.
                                    enum:
                                    - S3Prefix
                                    - S3Object
                                    type: string
                                  s3Uri:
                                    description: Specifies the S3 path of ML model
                                      data to deploy.
                                    maxLength: 1024
                                    type: string
                                required:
                                - compressionType
                                - s3DataType
                                - s3Uri
                                type: object
                            type: object
                          modelDataUrl:
                            description: A structure with Model Input details.
                            maxLength: 1024
                            type: string
                          modelInput:
                            description: SageMakerModelPackageModelPackageContainerDefinitionModelInput
                              is a nested property type of AWS::SageMaker::ModelPackage.
                            properties:
                              dataInputConfig:
                                description: The input configuration object for the
                                  model.
                                maxLength: 1024
                                minLength: 1
                                type: string
                            required:
                            - dataInputConfig
                            type: object
                          nearestModelName:
                            description: |-
                              The name of a pre-trained machine learning benchmarked by Amazon SageMaker Inference Recommender model that
                              matches your model.
                            type: string
                        required:
                        - image
                        type: object
                      type: array
                    description:
                      description: A description of the additional Inference specification.
                      maxLength: 1024
                      type: string
                    name:
                      description: |-
                        A unique name to identify the additional inference specification. The name must be unique within the list of
                        your additional inference specifications for a particular model package.
                      maxLength: 63
                      minLength: 1
                      type: string
                    supportedContentTypes:
                      description: The supported MIME types for the input data.
                      items:
                        type: string
                      type: array
                    supportedRealtimeInferenceInstanceTypes:
                      description: A list of the instance types that are used to generate
                        inferences in real-time
                      items:
                        type: string
                      type: array
                    supportedResponseMIMETypes:
                      description: The supported MIME types for the output data.
                      items:
                        type: string
                      type: array
                    supportedTransformInstanceTypes:
                      description: A list of the instance types on which a transformation
                        job can be run or on which an endpoint can be deployed.
                      items:
                        type: string
                      type: array
                  required:
                  - containers
                  - name
                  type: object
                type: array
              approvalDescription:
                maxLength: 1024
                type: string
              certifyForMarketplace:
                type: boolean
              clientToken:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 36
                type: string
              customerMetadataProperties:
                additionalProperties:
                  type: string
                type: object
              domain:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              driftCheckBaselines:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  bias:
                    description: SageMakerModelPackageDriftCheckBias is a nested property
                      type of AWS::SageMaker::ModelPackage.
                    properties:
                      configFile:
                        description: SageMakerModelPackageFileSource is a nested property
                          type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the file source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the file source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the file source.
                            maxLength: 1024
                            type: string
                        required:
                        - s3Uri
                        type: object
                      postTrainingConstraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      preTrainingConstraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                  explainability:
                    description: SageMakerModelPackageDriftCheckExplainability is
                      a nested property type of AWS::SageMaker::ModelPackage.
                    properties:
                      configFile:
                        description: SageMakerModelPackageFileSource is a nested property
                          type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the file source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the file source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the file source.
                            maxLength: 1024
                            type: string
                        required:
                        - s3Uri
                        type: object
                      constraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                  modelDataQuality:
                    description: SageMakerModelPackageDriftCheckModelDataQuality is
                      a nested property type of AWS::SageMaker::ModelPackage.
                    properties:
                      constraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      statistics:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                  modelQuality:
                    description: SageMakerModelPackageDriftCheckModelQuality is a
                      nested property type of AWS::SageMaker::ModelPackage.
                    properties:
                      constraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      statistics:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                type: object
              inferenceSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  containers:
                    description: The Amazon ECR registry path of the Docker image
                      that contains the inference code.
                    items:
                      description: SageMakerModelPackageModelPackageContainerDefinition
                        is a nested property type of AWS::SageMaker::ModelPackage.
                      properties:
                        containerHostname:
                          description: The DNS host name for the Docker container.
                          maxLength: 63
                          type: string
                        environment:
                          additionalProperties:
                            type: string
                          type: object
                        framework:
                          description: The machine learning framework of the model
                            package container image.
                          type: string
                        frameworkVersion:
                          description: The framework version of the Model Package
                            Container Image.
                          maxLength: 10
                          type: string
                        image:
                          description: The Amazon EC2 Container Registry (Amazon ECR)
                            path where inference code is stored.
                          maxLength: 255
                          minLength: 1
                          type: string
                        imageDigest:
                          description: An MD5 hash of the training algorithm that
                            identifies the Docker image used for training.
                          maxLength: 72
                          type: string
                        modelDataSource:
                          description: SageMakerModelPackageModelDataSource is a nested
                            property type of AWS::SageMaker::ModelPackage.
                          properties:
                            s3DataSource:
                              description: SageMakerModelPackageS3ModelDataSource
                                is a nested property type of AWS::SageMaker::ModelPackage.
                              properties:
                                compressionType:
                                  description: Specifies how the ML model data is
                                    prepared.
                                  enum:
                                  - None
                                  - Gzip
                                  type: string
                                modelAccessConfig:
                                  description: SageMakerModelPackageModelAccessConfig
                                    is a nested property type of AWS::SageMaker::ModelPackage.
                                  properties:
                                    acceptEula:
                                      description: Specifies agreement to the model
                                        end-user license agreement (EULA).
                                      type: boolean
                                  required:
                                  - acceptEula
                                  type: object
                                s3DataType:
                                  description: Specifies the type of ML model data
                                    to deploy.
                                  enum:
                                  - S3Prefix
                                  - S3Object
                                  type: string
                                s3Uri:
                                  description: Specifies the S3 path of ML model data
                                    to deploy.
                                  maxLength: 1024
                                  type: string
                              required:
                              - compressionType
                              - s3DataType
                              - s3Uri
                              type: object
                          type: object
                        modelDataUrl:
                          description: A structure with Model Input details.
                          maxLength: 1024
                          type: string
                        modelInput:
                          description: SageMakerModelPackageModelPackageContainerDefinitionModelInput
                            is a nested property type of AWS::SageMaker::ModelPackage.
                          properties:
                            dataInputConfig:
                              description: The input configuration object for the
                                model.
                              maxLength: 1024
                              minLength: 1
                              type: string
                          required:
                          - dataInputConfig
                          type: object
                        nearestModelName:
                          description: |-
                            The name of a pre-trained machine learning benchmarked by Amazon SageMaker Inference Recommender model that
                            matches your model.
                          type: string
                      required:
                      - image
                      type: object
                    type: array
                  supportedContentTypes:
                    description: The supported MIME types for the input data.
                    items:
                      type: string
                    type: array
                  supportedRealtimeInferenceInstanceTypes:
                    description: A list of the instance types that are used to generate
                      inferences in real-time
                    items:
                      type: string
                    type: array
                  supportedResponseMIMETypes:
                    description: The supported MIME types for the output data.
                    items:
                      type: string
                    type: array
                  supportedTransformInstanceTypes:
                    description: A list of the instance types on which a transformation
                      job can be run or on which an endpoint can be deployed.
                    items:
                      type: string
                    type: array
                required:
                - containers
                - supportedContentTypes
                - supportedResponseMIMETypes
                type: object
              metadataProperties:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  commitId:
                    description: The commit ID.
                    maxLength: 1024
                    type: string
                  generatedBy:
                    description: The entity this entity was generated by.
                    maxLength: 1024
                    type: string
                  projectId:
                    description: The project ID metadata.
                    maxLength: 1024
                    type: string
                  repository:
                    description: The repository metadata.
                    maxLength: 1024
                    type: string
                type: object
              modelApprovalStatus:
                enum:
                - Approved
                - Rejected
                - PendingManualApproval
                type: string
              modelCard:
                description: SageMakerModelPackageModelCard is a nested property type
                  of AWS::SageMaker::ModelPackage.
                properties:
                  modelCardContent:
                    description: The content of the model card.
                    maxLength: 100000
                    type: string
                  modelCardStatus:
                    description: The approval status of the model card within your
                      organization.
                    enum:
                    - Draft
                    - PendingReview
                    - Approved
                    - Archived
                    type: string
                required:
                - modelCardContent
                - modelCardStatus
                type: object
              modelMetrics:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  bias:
                    description: SageMakerModelPackageBias is a nested property type
                      of AWS::SageMaker::ModelPackage.
                    properties:
                      postTrainingReport:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      preTrainingReport:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      report:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                  explainability:
                    description: SageMakerModelPackageExplainability is a nested property
                      type of AWS::SageMaker::ModelPackage.
                    properties:
                      report:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                  modelDataQuality:
                    description: SageMakerModelPackageModelDataQuality is a nested
                      property type of AWS::SageMaker::ModelPackage.
                    properties:
                      constraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      statistics:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                  modelQuality:
                    description: SageMakerModelPackageModelQuality is a nested property
                      type of AWS::SageMaker::ModelPackage.
                    properties:
                      constraints:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                      statistics:
                        description: SageMakerModelPackageMetricsSource is a nested
                          property type of AWS::SageMaker::ModelPackage.
                        properties:
                          contentDigest:
                            description: The digest of the metric source.
                            maxLength: 72
                            type: string
                          contentType:
                            description: The type of content stored in the metric
                              source.
                            maxLength: 256
                            type: string
                          s3Uri:
                            description: The Amazon S3 URI for the metric source.
                            maxLength: 1024
                            type: string
                        required:
                        - contentType
                        - s3Uri
                        type: object
                    type: object
                type: object
              modelPackageDescription:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 1024
                type: string
              modelPackageGroupName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 170
                type: string
              modelPackageName:
                type: string
              modelPackageStatusDetails:
                description: SageMakerModelPackageModelPackageStatusDetails is a nested
                  property type of AWS::SageMaker::ModelPackage.
                properties:
                  validationStatuses:
                    items:
                      description: SageMakerModelPackageModelPackageStatusItem is
                        a nested property type of AWS::SageMaker::ModelPackage.
                      properties:
                        failureReason:
                          description: If the overall status is Failed, the reason
                            for the failure.
                          type: string
                        name:
                          description: The name of the model package for which the
                            overall status is being reported.
                          maxLength: 63
                          minLength: 1
                          type: string
                        status:
                          description: The current status.
                          enum:
                          - NotStarted
                          - Failed
                          - InProgress
                          - Completed
                          type: string
                      required:
                      - name
                      - status
                      type: object
                    type: array
                type: object
              modelPackageVersion:
                format: int64
                minimum: 1
                type: integer
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              samplePayloadUrl:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 1024
                type: string
              securityConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: The AWS KMS Key ID (KMSKeyId) used for encryption
                      of model package information.
                    maxLength: 2048
                    type: string
                required:
                - kmsKeyId
                type: object
              skipModelValidation:
                enum:
                - None
                - All
                type: string
              sourceAlgorithmSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  sourceAlgorithms:
                    description: A list of algorithms that were used to create a model
                      package.
                    items:
                      description: SageMakerModelPackageSourceAlgorithm is a nested
                        property type of AWS::SageMaker::ModelPackage.
                      properties:
                        algorithmName:
                          description: |-
                            The name of an algorithm that was used to create the model package. The algorithm must be either an algorithm
                            resource in your Amazon SageMaker account or an algorithm in AWS Marketplace that you are subscribed to.
                          maxLength: 170
                          minLength: 1
                          type: string
                        modelDataUrl:
                          description: |-
                            The Amazon S3 path where the model artifacts, which result from model training, are stored. This path must
                            point to a single gzip compressed tar archive (.tar.gz suffix).
                          maxLength: 1024
                          type: string
                      required:
                      - algorithmName
                      type: object
                    type: array
                required:
                - sourceAlgorithms
                type: object
              sourceUri:
                maxLength: 1024
                type: string
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              task:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              validationSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  validationProfiles:
                    items:
                      description: SageMakerModelPackageValidationProfile is a nested
                        property type of AWS::SageMaker::ModelPackage.
                      properties:
                        profileName:
                          description: The name of the profile for the model package.
                          maxLength: 63
                          minLength: 1
                          type: string
                        transformJobDefinition:
                          description: SageMakerModelPackageTransformJobDefinition
                            is a nested property type of AWS::SageMaker::ModelPackage.
                          properties:
                            batchStrategy:
                              description: A string that determines the number of
                                records included in a single mini-batch.
                              enum:
                              - MultiRecord
                              - SingleRecord
                              type: string
                            environment:
                              additionalProperties:
                                type: string
                              type: object
                            maxConcurrentTransforms:
                              description: |-
                                The maximum number of parallel requests that can be sent to each instance in a transform job. The default
                                value is 1.
                              format: int64
                              minimum: 0
                              type: integer
                            maxPayloadInMB:
                              description: The maximum payload size allowed, in MB.
                                A payload is the data portion of a record (without
                                metadata).
                              format: int64
                              minimum: 0
                              type: integer
                            transformInput:
                              description: SageMakerModelPackageTransformInput is
                                a nested property type of AWS::SageMaker::ModelPackage.
                              properties:
                                compressionType:
                                  description: |-
                                    If your transform data is compressed, specify the compression type. Amazon SageMaker automatically
                                    decompresses the data for the transform job accordingly. The default value is None.
                                  enum:
                                  - None
                                  - Gzip
                                  type: string
                                contentType:
                                  description: |-
                                    The multipurpose internet mail extension (MIME) type of the data. Amazon SageMaker uses the MIME type with
                                    each http call to transfer data to the transform job.
                                  maxLength: 256
                                  type: string
                                dataSource:
                                  description: SageMakerModelPackageDataSource is
                                    a nested property type of AWS::SageMaker::ModelPackage.
                                  properties:
                                    s3DataSource:
                                      description: SageMakerModelPackageS3DataSource
                                        is a nested property type of AWS::SageMaker::ModelPackage.
                                      properties:
                                        s3DataType:
                                          description: The S3 Data Source Type
                                          enum:
                                          - ManifestFile
                                          - S3Prefix
                                          - AugmentedManifestFile
                                          type: string
                                        s3Uri:
                                          description: Depending on the value specified
                                            for the S3DataType, identifies either
                                            a key name prefix or a manifest.
                                          maxLength: 1024
                                          type: string
                                      required:
                                      - s3DataType
                                      - s3Uri
                                      type: object
                                  required:
                                  - s3DataSource
                                  type: object
                                splitType:
                                  description: The method to use to split the transform
                                    job's data files into smaller batches.
                                  enum:
                                  - None
                                  - TFRecord
                                  - Line
                                  - RecordIO
                                  type: string
                              required:
                              - dataSource
                              type: object
                            transformOutput:
                              description: SageMakerModelPackageTransformOutput is
                                a nested property type of AWS::SageMaker::ModelPackage.
                              properties:
                                accept:
                                  description: |-
                                    The MIME type used to specify the output data. Amazon SageMaker uses the MIME type with each http call to
                                    transfer data from the transform job.
                                  maxLength: 256
                                  type: string
                                assembleWith:
                                  description: Defines how to assemble the results
                                    of the transform job as a single S3 object.
                                  enum:
                                  - None
                                  - Line
                                  type: string
                                kmsKeyId:
                                  description: |-
                                    The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest
                                    using Amazon S3 server-side encryption.
                                  maxLength: 2048
                                  type: string
                                s3OutputPath:
                                  description: The Amazon S3 path where you want Amazon
                                    SageMaker to store the results of the transform
                                    job.
                                  maxLength: 1024
                                  type: string
                              required:
                              - s3OutputPath
                              type: object
                            transformResources:
                              description: SageMakerModelPackageTransformResources
                                is a nested property type of AWS::SageMaker::ModelPackage.
                              properties:
                                instanceCount:
                                  description: |-
                                    The number of ML compute instances to use in the transform job. For distributed transform jobs, specify a
                                    value greater than 1. The default value is 1.
                                  format: int64
                                  minimum: 1
                                  type: integer
                                instanceType:
                                  description: The ML compute instance type for the
                                    transform job.
                                  type: string
                                volumeKmsKeyId:
                                  description: |-
                                    The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt model data on the storage
                                    volume attached to the ML compute instance(s) that run the batch transform job.
                                  maxLength: 2048
                                  type: string
                              required:
                              - instanceCount
                              - instanceType
                              type: object
                          required:
                          - transformInput
                          - transformOutput
                          - transformResources
                          type: object
                      required:
                      - profileName
                      - transformJobDefinition
                      type: object
                    type: array
                  validationRole:
                    description: The IAM roles to be used for the validation of the
                      model package.
                    maxLength: 2048
                    minLength: 20
                    type: string
                required:
                - validationProfiles
                - validationRole
                type: object
            type: object
          status:
            description: SageMakerModelPackageStatus is the observed state of AWS::SageMaker::ModelPackage.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSyncTime:
                format: date-time
                type: string
              modelPackageArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              modelPackageStatus:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - Pending
                - Deleting
                - InProgress
                - Completed
                - Failed
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodelqualityjobdefinitions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModelQualityJobDefinition
    listKind: SageMakerModelQualityJobDefinitionList
    plural: sagemakermodelqualityjobdefinitions
    singular: sagemakermodelqualityjobdefinition
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModelQualityJobDefinition manages AWS::SageMaker::ModelQualityJobDefinition through the AWS Cloud
          Control API. Resource Type definition for AWS::SageMaker::ModelQualityJobDefinition
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelQualityJobDefinitionSpec is the desired state
              of AWS::SageMaker::ModelQualityJobDefinition.
            properties:
              endpointName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobDefinitionName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
              jobResources:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  clusterConfig:
                    description: SageMakerModelQualityJobDefinitionClusterConfig is
                      a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                    properties:
                      instanceCount:
                        description: |-
                          The number of ML compute instances to use in the model monitoring job. For distributed processing jobs,
                          specify a value greater than 1. The default value is 1.
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      instanceType:
                        description: The ML compute instance type for the processing
                          job.
                        type: string
                      volumeKmsKeyId:
                        description: |-
                          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                          attached to the ML compute instance(s) that run the model monitoring job.
                        type: string
                      volumeSizeInGB:
                        description: |-
                          The size of the ML storage volume, in gigabytes, that you want to provision. You must specify sufficient ML
                          storage for your scenario.
                        format: int64
                        maximum: 16384
                        minimum: 1
                        type: integer
                    required:
                    - instanceCount
                    - instanceType
                    - volumeSizeInGB
                    type: object
                required:
                - clusterConfig
                type: object
              modelQualityAppSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  containerArguments:
                    description: An array of arguments for the container used to run
                      the monitoring job.
                    items:
                      type: string
                    type: array
                  containerEntrypoint:
                    description: Specifies the entrypoint for a container used to
                      run the monitoring job.
                    items:
                      type: string
                    type: array
                  environment:
                    additionalProperties:
                      type: string
                    description: Sets the environment variables in the Docker container
                    type: object
                  imageUri:
                    description: The container image to be run by the monitoring job.
                    maxLength: 255
                    type: string
                  postAnalyticsProcessorSourceUri:
                    description: |-
                      An Amazon S3 URI to a script that is called after analysis has been performed. Applicable only for the
                      built-in (first party) containers.
                    maxLength: 1024
                    type: string
                  problemType:
                    enum:
                    - BinaryClassification
                    - MulticlassClassification
                    - Regression
                    type: string
                  recordPreprocessorSourceUri:
                    description: |-
                      An Amazon S3 URI to a script that is called per row prior to running analysis. It can base64 decode the
                      payload and convert it into a flatted json so that the built-in container can use the converted data.
                      Applicable only for the built-in (first party) containers
                    maxLength: 1024
                    type: string
                required:
                - imageUri
                - problemType
                type: object
              modelQualityBaselineConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  baseliningJobName:
                    maxLength: 63
                    type: string
                  constraintsResource:
                    description: SageMakerModelQualityJobDefinitionConstraintsResource
                      is a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          The Amazon S3 URI for baseline constraint file in Amazon S3 that the current monitoring job should validated
                          against.
                        maxLength: 1024
                        type: string
                    type: object
                type: object
              modelQualityJobInput:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  batchTransformInput:
                    description: SageMakerModelQualityJobDefinitionBatchTransformInput
                      is a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                    properties:
                      dataCapturedDestinationS3Uri:
                        description: A URI that identifies the Amazon S3 storage location
                          where Batch Transform Job captures data.
                        maxLength: 512
                        type: string
                      datasetFormat:
                        description: SageMakerModelQualityJobDefinitionDatasetFormat
                          is a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                        properties:
                          csv:
                            description: SageMakerModelQualityJobDefinitionCsv is
                              a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                            properties:
                              header:
                                description: A boolean flag indicating if given CSV
                                  has header
                                type: boolean
                            type: object
                          json:
                            description: SageMakerModelQualityJobDefinitionJson is
                              a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                            properties:
                              line:
                                description: A boolean flag indicating if it is JSON
                                  line format
                                type: boolean
                            type: object
                          parquet:
                            type: boolean
                        type: object
                      endTimeOffset:
                        description: Monitoring end time offset, e.g. PT0H
                        maxLength: 15
                        type: string
                      inferenceAttribute:
                        description: Index or JSONpath to locate predicted label(s)
                        maxLength: 256
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      probabilityAttribute:
                        description: Index or JSONpath to locate probabilities
                        maxLength: 256
                        type: string
                      probabilityThresholdAttribute:
                        type: number
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                      startTimeOffset:
                        description: Monitoring start time offset, e.g. -PT1H
                        maxLength: 15
                        type: string
                    required:
                    - dataCapturedDestinationS3Uri
                    - datasetFormat
                    - localPath
                    type: object
                  endpointInput:
                    description: SageMakerModelQualityJobDefinitionEndpointInput is
                      a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                    properties:
                      endTimeOffset:
                        description: Monitoring end time offset, e.g. PT0H
                        maxLength: 15
                        type: string
                      endpointName:
                        maxLength: 63
                        type: string
                      inferenceAttribute:
                        description: Index or JSONpath to locate predicted label(s)
                        maxLength: 256
                        type: string
                      localPath:
                        description: Path to the filesystem where the endpoint data
                          is available to the container.
                        maxLength: 256
                        type: string
                      probabilityAttribute:
                        description: Index or JSONpath to locate probabilities
                        maxLength: 256
                        type: string
                      probabilityThresholdAttribute:
                        type: number
                      s3DataDistributionType:
                        description: |-
                          Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                          FullyReplicated
                        enum:
                        - FullyReplicated
                        - ShardedByS3Key
                        type: string
                      s3InputMode:
                        description: |-
                          Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                          recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                        enum:
                        - Pipe
                        - File
                        type: string
                      startTimeOffset:
                        description: Monitoring start time offset, e.g. -PT1H
                        maxLength: 15
                        type: string
                    required:
                    - endpointName
                    - localPath
                    type: object
                  groundTruthS3Input:
                    description: SageMakerModelQualityJobDefinitionMonitoringGroundTruthS3Input
                      is a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                    properties:
                      s3Uri:
                        description: |-
                          A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                          job.
                        maxLength: 512
                        type: string
                    required:
                    - s3Uri
                    type: object
                required:
                - groundTruthS3Input
                type: object
              modelQualityJobOutputConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: |-
                      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest
                      using Amazon S3 server-side encryption.
                    maxLength: 2048
                    type: string
                  monitoringOutputs:
                    description: Monitoring outputs for monitoring jobs. This is where
                      the output of the periodic monitoring jobs is uploaded.
                    items:
                      description: SageMakerModelQualityJobDefinitionMonitoringOutput
                        is a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                      properties:
                        s3Output:
                          description: SageMakerModelQualityJobDefinitionS3Output
                            is a nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                          properties:
                            localPath:
                              description: |-
                                The local path to the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring job.
                                LocalPath is an absolute path for the output data.
                              maxLength: 256
                              type: string
                            s3UploadMode:
                              description: Whether to upload the results of the monitoring
                                job continuously or after the job completes.
                              enum:
                              - Continuous
                              - EndOfJob
                              type: string
                            s3Uri:
                              description: |-
                                A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                                job.
                              maxLength: 512
                              type: string
                          required:
                          - localPath
                          - s3Uri
                          type: object
                      required:
                      - s3Output
                      type: object
                    type: array
                required:
                - monitoringOutputs
                type: object
              networkConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  enableInterContainerTrafficEncryption:
                    description: |-
                      Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt
                      communications. Encryption provides greater security for distributed processing jobs, but the processing might
                      take longer.
                    type: boolean
                  enableNetworkIsolation:
                    description: Whether to allow inbound and outbound network calls
                      to and from the containers used for the processing job.
                    type: boolean
                  vpcConfig:
                    description: SageMakerModelQualityJobDefinitionVpcConfig is a
                      nested property type of AWS::SageMaker::ModelQualityJobDefinition.
                    properties:
                      securityGroupIds:
                        description: |-
                          The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                          in the Subnets field.
                        items:
                          type: string
                        type: array
                      subnets:
                        description: The ID of the subnets in the VPC to which you
                          want to connect to your monitoring jobs.
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroupIds
                    - subnets
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your
                  behalf.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              stoppingCondition:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  maxRuntimeInSeconds:
                    description: The maximum runtime allowed in seconds.
                    format: int64
                    maximum: 86400
                    minimum: 1
                    type: integer
                required:
                - maxRuntimeInSeconds
                type: object
              tags:
                description: |-
                  An array of key-value pairs to apply to this resource.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - jobResources
            - modelQualityAppSpecification
            - modelQualityJobInput
            - modelQualityJobOutputConfig
            - roleArn
            type: object
          status:
            description: SageMakerModelQualityJobDefinitionStatus is the observed
              state of AWS::SageMaker::ModelQualityJobDefinition.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the job definition was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              jobDefinitionArn:
                description: |-
                  The Amazon Resource Name (ARN) of job definition.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermodels.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerModel
    listKind: SageMakerModelList
    plural: sagemakermodels
    singular: sagemakermodel
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerModel manages AWS::SageMaker::Model through the AWS Cloud Control API. Resource type definition for
          AWS::SageMaker::Model
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerModelSpec is the desired state of AWS::SageMaker::Model.
            properties:
              containers:
                description: |-
                  Specifies the containers in the inference pipeline.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: SageMakerModelContainerDefinition is a nested property
                    type of AWS::SageMaker::Model.
                  properties:
                    containerHostname:
                      description: |-
                        This parameter is ignored for models that contain only a PrimaryContainer. When a ContainerDefinition is part
                        of an inference pipeline, the value of the parameter uniquely identifies the container for the purposes of
                        logging and metrics. For information, see [Use Logs and Metrics to Monitor an Inference
                        Pipeline](https://docs.aws.amazon.com/sagemaker/latest/dg/inference-pipeline-logs-metrics.ht...
                      type: string
                    environment:
                      description: |-
                        The environment variables to set in the Docker container. Don't include any sensitive data in your environment
                        variables. The maximum length of each key and value in the Environment map is 1024 bytes. The maximum length
                        of all keys and values in the map, combined, is 32 KB. If you pass multiple containers to a CreateModel
                        request, then the maximum length of all of their maps, combined, is also ...
                      x-kubernetes-preserve-unknown-fields: true
                    image:
                      description: |-
                        The path where inference code is stored. This can be either in Amazon EC2 Container Registry or in a Docker
                        registry that is accessible from the same VPC that you configure for your endpoint. If you are using your own
                        custom algorithm instead of an algorithm provided by SageMaker, the inference code must meet SageMaker
                        requirements. SageMaker supports both registry/repository[:tag] and registry...
                      type: string
                    imageConfig:
                      description: SageMakerModelImageConfig is a nested property
                        type of AWS::SageMaker::Model.
                      properties:
                        repositoryAccessMode:
                          description: |-
                            Set this to one of the following values: Platform - The model image is hosted in Amazon ECR. Vpc - The model
                            image is hosted in a private Docker registry in your VPC.
                          enum:
                          - Platform
                          - Vpc
                          type: string
                        repositoryAuthConfig:
                          description: SageMakerModelRepositoryAuthConfig is a nested
                            property type of AWS::SageMaker::Model.
                          properties:
                            repositoryCredentialsProviderArn:
                              description: |-
                                The Amazon Resource Name (ARN) of an AWS Lambda function that provides credentials to authenticate to the
                                private Docker registry where your model image is hosted. For information about how to create an AWS Lambda
                                function, see [Create a Lambda function with the
                                console](https://docs.aws.amazon.com/lambda/latest/dg/getting-started-create-function.html) in the AWS Lambda
                                Developer Guide
                              type: string
                          required:
                          - repositoryCredentialsProviderArn
                          type: object
                      required:
                      - repositoryAccessMode
                      type: object
                    inferenceSpecificationName:
                      description: The inference specification name in the model package
                        version.
                      type: string
                    mode:
                      description: Whether the container hosts a single model or multiple
                        models.
                      enum:
                      - SingleModel
                      - MultiModel
                      type: string
                    modelDataSource:
                      description: SageMakerModelModelDataSource is a nested property
                        type of AWS::SageMaker::Model.
                      properties:
                        s3DataSource:
                          description: SageMakerModelS3DataSource is a nested property
                            type of AWS::SageMaker::Model.
                          properties:
                            compressionType:
                              description: Specifies how the ML model data is prepared.
                              enum:
                              - None
                              - Gzip
                              type: string
                            hubAccessConfig:
                              description: SageMakerModelHubAccessConfig is a nested
                                property type of AWS::SageMaker::Model.
                              properties:
                                hubContentArn:
                                  description: The ARN of the hub content for which
                                    deployment access is allowed.
                                  type: string
                              required:
                              - hubContentArn
                              type: object
                            modelAccessConfig:
                              description: SageMakerModelModelAccessConfig is a nested
                                property type of AWS::SageMaker::Model.
                              properties:
                                acceptEula:
                                  description: |-
                                    Specifies agreement to the model end-user license agreement (EULA). The `AcceptEula` value must be explicitly
                                    defined as `True` in order to accept the EULA that this model requires. You are responsible for reviewing and
                                    complying with any applicable license terms and making sure they are acceptable for your use case before
                                    downloading or using a model.
                                  type: boolean
                              required:
                              - acceptEula
                              type: object
                            s3DataType:
                              description: Specifies the type of ML model data to
                                deploy.
                              enum:
                              - S3Prefix
                              - S3Object
                              type: string
                            s3Uri:
                              description: Specifies the S3 path of ML model data
                                to deploy.
                              type: string
                          required:
                          - compressionType
                          - s3DataType
                          - s3Uri
                          type: object
                      required:
                      - s3DataSource
                      type: object
                    modelDataUrl:
                      description: |-
                        The S3 path where the model artifacts, which result from model training, are stored. This path must point to a
                        single gzip compressed tar archive (.tar.gz suffix). The S3 path is required for SageMaker built-in
                        algorithms, but not if you use your own algorithms. For more information on built-in algorithms, see [Common
                        Parameters](https://docs.aws.amazon.com/sagemaker/latest/dg/sagemaker-algo-do...
                      type: string
                    modelPackageName:
                      description: The name or Amazon Resource Name (ARN) of the model
                        package to use to create the model.
                      type: string
                    multiModelConfig:
                      description: SageMakerModelMultiModelConfig is a nested property
                        type of AWS::SageMaker::Model.
                      properties:
                        modelCacheSetting:
                          description: |-
                            Whether to cache models for a multi-model endpoint. By default, multi-model endpoints cache models so that a
                            model does not have to be loaded into memory each time it is invoked. Some use cases do not benefit from model
                            caching. For example, if an endpoint hosts a large number of models that are each invoked infrequently, the
                            endpoint might perform better if you disable model caching. To disabl...
                          enum:
                          - Enabled
                          - Disabled
                          type: string
                      type: object
                  type: object
                type: array
              enableNetworkIsolation:
                description: |-
                  Isolates the model container. No inbound or outbound network calls can be made to or from the model container.
                  Create-only in AWS: changing it requires replacing the resource.
                type: boolean
              executionRoleArn:
                description: |-
                  The Amazon Resource Name (ARN) of the IAM role that you specified for the model.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              inferenceExecutionConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  mode:
                    description: How containers in a multi-container are run.
                    enum:
                    - Serial
                    - Direct
                    type: string
                required:
                - mode
                type: object
              modelName:
                description: |-
                  The name of the new model.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              primaryContainer:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  containerHostname:
                    description: |-
                      This parameter is ignored for models that contain only a PrimaryContainer. When a ContainerDefinition is part
                      of an inference pipeline, the value of the parameter uniquely identifies the container for the purposes of
                      logging and metrics. For information, see [Use Logs and Metrics to Monitor an Inference
                      Pipeline](https://docs.aws.amazon.com/sagemaker/latest/dg/inference-pipeline-logs-metrics.ht...
                    type: string
                  environment:
                    description: |-
                      The environment variables to set in the Docker container. Don't include any sensitive data in your environment
                      variables. The maximum length of each key and value in the Environment map is 1024 bytes. The maximum length
                      of all keys and values in the map, combined, is 32 KB. If you pass multiple containers to a CreateModel
                      request, then the maximum length of all of their maps, combined, is also ...
                    x-kubernetes-preserve-unknown-fields: true
                  image:
                    description: |-
                      The path where inference code is stored. This can be either in Amazon EC2 Container Registry or in a Docker
                      registry that is accessible from the same VPC that you configure for your endpoint. If you are using your own
                      custom algorithm instead of an algorithm provided by SageMaker, the inference code must meet SageMaker
                      requirements. SageMaker supports both registry/repository[:tag] and registry...
                    type: string
                  imageConfig:
                    description: SageMakerModelImageConfig is a nested property type
                      of AWS::SageMaker::Model.
                    properties:
                      repositoryAccessMode:
                        description: |-
                          Set this to one of the following values: Platform - The model image is hosted in Amazon ECR. Vpc - The model
                          image is hosted in a private Docker registry in your VPC.
                        enum:
                        - Platform
                        - Vpc
                        type: string
                      repositoryAuthConfig:
                        description: SageMakerModelRepositoryAuthConfig is a nested
                          property type of AWS::SageMaker::Model.
                        properties:
                          repositoryCredentialsProviderArn:
                            description: |-
                              The Amazon Resource Name (ARN) of an AWS Lambda function that provides credentials to authenticate to the
                              private Docker registry where your model image is hosted. For information about how to create an AWS Lambda
                              function, see [Create a Lambda function with the
                              console](https://docs.aws.amazon.com/lambda/latest/dg/getting-started-create-function.html) in the AWS Lambda
                              Developer Guide
                            type: string
                        required:
                        - repositoryCredentialsProviderArn
                        type: object
                    required:
                    - repositoryAccessMode
                    type: object
                  inferenceSpecificationName:
                    description: The inference specification name in the model package
                      version.
                    type: string
                  mode:
                    description: Whether the container hosts a single model or multiple
                      models.
                    enum:
                    - SingleModel
                    - MultiModel
                    type: string
                  modelDataSource:
                    description: SageMakerModelModelDataSource is a nested property
                      type of AWS::SageMaker::Model.
                    properties:
                      s3DataSource:
                        description: SageMakerModelS3DataSource is a nested property
                          type of AWS::SageMaker::Model.
                        properties:
                          compressionType:
                            description: Specifies how the ML model data is prepared.
                            enum:
                            - None
                            - Gzip
                            type: string
                          hubAccessConfig:
                            description: SageMakerModelHubAccessConfig is a nested
                              property type of AWS::SageMaker::Model.
                            properties:
                              hubContentArn:
                                description: The ARN of the hub content for which
                                  deployment access is allowed.
                                type: string
                            required:
                            - hubContentArn
                            type: object
                          modelAccessConfig:
                            description: SageMakerModelModelAccessConfig is a nested
                              property type of AWS::SageMaker::Model.
                            properties:
                              acceptEula:
                                description: |-
                                  Specifies agreement to the model end-user license agreement (EULA). The `AcceptEula` value must be explicitly
                                  defined as `True` in order to accept the EULA that this model requires. You are responsible for reviewing and
                                  complying with any applicable license terms and making sure they are acceptable for your use case before
                                  downloading or using a model.
                                type: boolean
                            required:
                            - acceptEula
                            type: object
                          s3DataType:
                            description: Specifies the type of ML model data to deploy.
                            enum:
                            - S3Prefix
                            - S3Object
                            type: string
                          s3Uri:
                            description: Specifies the S3 path of ML model data to
                              deploy.
                            type: string
                        required:
                        - compressionType
                        - s3DataType
                        - s3Uri
                        type: object
                    required:
                    - s3DataSource
                    type: object
                  modelDataUrl:
                    description: |-
                      The S3 path where the model artifacts, which result from model training, are stored. This path must point to a
                      single gzip compressed tar archive (.tar.gz suffix). The S3 path is required for SageMaker built-in
                      algorithms, but not if you use your own algorithms. For more information on built-in algorithms, see [Common
                      Parameters](https://docs.aws.amazon.com/sagemaker/latest/dg/sagemaker-algo-do...
                    type: string
                  modelPackageName:
                    description: The name or Amazon Resource Name (ARN) of the model
                      package to use to create the model.
                    type: string
                  multiModelConfig:
                    description: SageMakerModelMultiModelConfig is a nested property
                      type of AWS::SageMaker::Model.
                    properties:
                      modelCacheSetting:
                        description: |-
                          Whether to cache models for a multi-model endpoint. By default, multi-model endpoints cache models so that a
                          model does not have to be loaded into memory each time it is invoked. Some use cases do not benefit from model
                          caching. For example, if an endpoint hosts a large number of models that are each invoked infrequently, the
                          endpoint might perform better if you disable model caching. To disabl...
                        enum:
                        - Enabled
                        - Disabled
                        type: string
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: |-
                  An array of key-value pairs. You can use tags to categorize your AWS resources in different ways, for example,
                  by purpose, owner, or environment. For more information, see [Tagging AWS
                  Resources](https://docs.aws.amazon.com/general/latest/gr/aws_tagging.html).
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              vpcConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  securityGroupIds:
                    description: |-
                      The VPC security group IDs, in the form `sg-xxxxxxxx`. Specify the security groups for the VPC that is
                      specified in the `Subnets` field.
                    items:
                      type: string
                    type: array
                  subnets:
                    description: |-
                      The ID of the subnets in the VPC to which you want to connect your training job or model. For information
                      about the availability of specific instance types, see [Supported Instance Types and Availability
                      Zones](https://docs.aws.amazon.com/sagemaker/latest/dg/instance-types-az.html).
                    items:
                      type: string
                    type: array
                required:
                - securityGroupIds
                - subnets
                type: object
            type: object
          status:
            description: SageMakerModelStatus is the observed state of AWS::SageMaker::Model.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              modelArn:
                description: |-
                  The Amazon Resource Name (ARN) of the model.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakermonitoringschedules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerMonitoringSchedule
    listKind: SageMakerMonitoringScheduleList
    plural: sagemakermonitoringschedules
    singular: sagemakermonitoringschedule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerMonitoringSchedule manages AWS::SageMaker::MonitoringSchedule through the AWS Cloud Control API.
          Resource Type definition for AWS::SageMaker::MonitoringSchedule
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerMonitoringScheduleSpec is the desired state of AWS::SageMaker::MonitoringSchedule.
            properties:
              endpointName:
                maxLength: 63
                type: string
              failureReason:
                description: Contains the reason a monitoring job failed, if it failed.
                maxLength: 1024
                type: string
              lastMonitoringExecutionSummary:
                description: Describes metadata on the last execution to run, if there
                  was one.
                properties:
                  creationTime:
                    description: The time at which the monitoring job was created.
                    type: string
                  endpointName:
                    maxLength: 63
                    type: string
                  failureReason:
                    description: Contains the reason a monitoring job failed, if it
                      failed.
                    maxLength: 1024
                    type: string
                  lastModifiedTime:
                    description: A timestamp that indicates the last time the monitoring
                      job was modified.
                    type: string
                  monitoringExecutionStatus:
                    description: The status of the monitoring job.
                    enum:
                    - Pending
                    - Completed
                    - CompletedWithViolations
                    - InProgress
                    - Failed
                    - Stopping
                    - Stopped
                    type: string
                  monitoringScheduleName:
                    maxLength: 63
                    type: string
                  processingJobArn:
                    description: The Amazon Resource Name (ARN) of the monitoring
                      job.
                    maxLength: 256
                    type: string
                  scheduledTime:
                    description: The time the monitoring job was scheduled.
                    type: string
                required:
                - creationTime
                - lastModifiedTime
                - monitoringExecutionStatus
                - monitoringScheduleName
                - scheduledTime
                type: object
              monitoringScheduleConfig:
                description: SageMakerMonitoringScheduleMonitoringScheduleConfig is
                  a nested property type of AWS::SageMaker::MonitoringSchedule.
                properties:
                  monitoringJobDefinition:
                    description: SageMakerMonitoringScheduleMonitoringJobDefinition
                      is a nested property type of AWS::SageMaker::MonitoringSchedule.
                    properties:
                      baselineConfig:
                        description: SageMakerMonitoringScheduleBaselineConfig is
                          a nested property type of AWS::SageMaker::MonitoringSchedule.
                        properties:
                          constraintsResource:
                            description: SageMakerMonitoringScheduleConstraintsResource
                              is a nested property type of AWS::SageMaker::MonitoringSchedule.
                            properties:
                              s3Uri:
                                description: |-
                                  The Amazon S3 URI for baseline constraint file in Amazon S3 that the current monitoring job should validated
                                  against.
                                maxLength: 1024
                                type: string
                            type: object
                          statisticsResource:
                            description: SageMakerMonitoringScheduleStatisticsResource
                              is a nested property type of AWS::SageMaker::MonitoringSchedule.
                            properties:
                              s3Uri:
                                description: |-
                                  The Amazon S3 URI for the baseline statistics file in Amazon S3 that the current monitoring job should be
                                  validated against.
                                maxLength: 1024
                                type: string
                            type: object
                        type: object
                      environment:
                        additionalProperties:
                          type: string
                        description: Sets the environment variables in the Docker
                          container
                        type: object
                      monitoringAppSpecification:
                        description: SageMakerMonitoringScheduleMonitoringAppSpecification
                          is a nested property type of AWS::SageMaker::MonitoringSchedule.
                        properties:
                          containerArguments:
                            description: An array of arguments for the container used
                              to run the monitoring job.
                            items:
                              type: string
                            type: array
                          containerEntrypoint:
                            description: Specifies the entrypoint for a container
                              used to run the monitoring job.
                            items:
                              type: string
                            type: array
                          imageUri:
                            description: The container image to be run by the monitoring
                              job.
                            maxLength: 255
                            type: string
                          postAnalyticsProcessorSourceUri:
                            description: |-
                              An Amazon S3 URI to a script that is called after analysis has been performed. Applicable only for the
                              built-in (first party) containers.
                            maxLength: 1024
                            type: string
                          recordPreprocessorSourceUri:
                            description: |-
                              An Amazon S3 URI to a script that is called per row prior to running analysis. It can base64 decode the
                              payload and convert it into a flatted json so that the built-in container can use the converted data.
                              Applicable only for the built-in (first party) containers
                            maxLength: 1024
                            type: string
                        required:
                        - imageUri
                        type: object
                      monitoringInputs:
                        items:
                          description: SageMakerMonitoringScheduleMonitoringInput
                            is a nested property type of AWS::SageMaker::MonitoringSchedule.
                          properties:
                            batchTransformInput:
                              description: SageMakerMonitoringScheduleBatchTransformInput
                                is a nested property type of AWS::SageMaker::MonitoringSchedule.
                              properties:
                                dataCapturedDestinationS3Uri:
                                  description: A URI that identifies the Amazon S3
                                    storage location where Batch Transform Job captures
                                    data.
                                  maxLength: 512
                                  type: string
                                datasetFormat:
                                  description: SageMakerMonitoringScheduleDatasetFormat
                                    is a nested property type of AWS::SageMaker::MonitoringSchedule.
                                  properties:
                                    csv:
                                      description: SageMakerMonitoringScheduleCsv
                                        is a nested property type of AWS::SageMaker::MonitoringSchedule.
                                      properties:
                                        header:
                                          description: A boolean flag indicating if
                                            given CSV has header
                                          type: boolean
                                      type: object
                                    json:
                                      description: SageMakerMonitoringScheduleJson
                                        is a nested property type of AWS::SageMaker::MonitoringSchedule.
                                      properties:
                                        line:
                                          description: A boolean flag indicating if
                                            it is JSON line format
                                          type: boolean
                                      type: object
                                    parquet:
                                      type: boolean
                                  type: object
                                excludeFeaturesAttribute:
                                  description: Indexes or names of the features to
                                    be excluded from analysis
                                  maxLength: 100
                                  type: string
                                localPath:
                                  description: Path to the filesystem where the endpoint
                                    data is available to the container.
                                  maxLength: 256
                                  type: string
                                s3DataDistributionType:
                                  description: |-
                                    Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                                    FullyReplicated
                                  enum:
                                  - FullyReplicated
                                  - ShardedByS3Key
                                  type: string
                                s3InputMode:
                                  description: |-
                                    Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                                    recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                                  enum:
                                  - Pipe
                                  - File
                                  type: string
                              required:
                              - dataCapturedDestinationS3Uri
                              - datasetFormat
                              - localPath
                              type: object
                            endpointInput:
                              description: SageMakerMonitoringScheduleEndpointInput
                                is a nested property type of AWS::SageMaker::MonitoringSchedule.
                              properties:
                                endpointName:
                                  maxLength: 63
                                  type: string
                                excludeFeaturesAttribute:
                                  description: Indexes or names of the features to
                                    be excluded from analysis
                                  maxLength: 100
                                  type: string
                                localPath:
                                  description: Path to the filesystem where the endpoint
                                    data is available to the container.
                                  maxLength: 256
                                  type: string
                                s3DataDistributionType:
                                  description: |-
                                    Whether input data distributed in Amazon S3 is fully replicated or sharded by an S3 key. Defauts to
                                    FullyReplicated
                                  enum:
                                  - FullyReplicated
                                  - ShardedByS3Key
                                  type: string
                                s3InputMode:
                                  description: |-
                                    Whether the Pipe or File is used as the input mode for transfering data for the monitoring job. Pipe mode is
                                    recommended for large datasets. File mode is useful for small files that fit in memory. Defaults to File.
                                  enum:
                                  - Pipe
                                  - File
                                  type: string
                              required:
                              - endpointName
                              - localPath
                              type: object
                          type: object
                        type: array
                      monitoringOutputConfig:
                        description: SageMakerMonitoringScheduleMonitoringOutputConfig
                          is a nested property type of AWS::SageMaker::MonitoringSchedule.
                        properties:
                          kmsKeyId:
                            description: |-
                              The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the model artifacts at rest
                              using Amazon S3 server-side encryption.
                            maxLength: 2048
                            type: string
                          monitoringOutputs:
                            description: Monitoring outputs for monitoring jobs. This
                              is where the output of the periodic monitoring jobs
                              is uploaded.
                            items:
                              description: SageMakerMonitoringScheduleMonitoringOutput
                                is a nested property type of AWS::SageMaker::MonitoringSchedule.
                              properties:
                                s3Output:
                                  description: SageMakerMonitoringScheduleS3Output
                                    is a nested property type of AWS::SageMaker::MonitoringSchedule.
                                  properties:
                                    localPath:
                                      description: |-
                                        The local path to the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring job.
                                        LocalPath is an absolute path for the output data.
                                      maxLength: 256
                                      type: string
                                    s3UploadMode:
                                      description: Whether to upload the results of
                                        the monitoring job continuously or after the
                                        job completes.
                                      enum:
                                      - Continuous
                                      - EndOfJob
                                      type: string
                                    s3Uri:
                                      description: |-
                                        A URI that identifies the Amazon S3 storage location where Amazon SageMaker saves the results of a monitoring
                                        job.
                                      maxLength: 512
                                      type: string
                                  required:
                                  - localPath
                                  - s3Uri
                                  type: object
                              required:
                              - s3Output
                              type: object
                            type: array
                        required:
                        - monitoringOutputs
                        type: object
                      monitoringResources:
                        description: SageMakerMonitoringScheduleMonitoringResources
                          is a nested property type of AWS::SageMaker::MonitoringSchedule.
                        properties:
                          clusterConfig:
                            description: SageMakerMonitoringScheduleClusterConfig
                              is a nested property type of AWS::SageMaker::MonitoringSchedule.
                            properties:
                              instanceCount:
                                description: |-
                                  The number of ML compute instances to use in the model monitoring job. For distributed processing jobs,
                                  specify a value greater than 1. The default value is 1.
                                format: int64
                                maximum: 100
                                minimum: 1
                                type: integer
                              instanceType:
                                description: The ML compute instance type for the
                                  processing job.
                                type: string
                              volumeKmsKeyId:
                                description: |-
                                  The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                                  attached to the ML compute instance(s) that run the model monitoring job.
                                type: string
                              volumeSizeInGB:
                                description: |-
                                  The size of the ML storage volume, in gigabytes, that you want to provision. You must specify sufficient ML
                                  storage for your scenario.
                                format: int64
                                maximum: 16384
                                minimum: 1
                                type: integer
                            required:
                            - instanceCount
                            - instanceType
                            - volumeSizeInGB
                            type: object
                        required:
                        - clusterConfig
                        type: object
                      networkConfig:
                        description: SageMakerMonitoringScheduleNetworkConfig is a
                          nested property type of AWS::SageMaker::MonitoringSchedule.
                        properties:
                          enableInterContainerTrafficEncryption:
                            description: |-
                              Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt
                              communications. Encryption provides greater security for distributed processing jobs, but the processing might
                              take longer.
                            type: boolean
                          enableNetworkIsolation:
                            description: Whether to allow inbound and outbound network
                              calls to and from the containers used for the processing
                              job.
                            type: boolean
                          vpcConfig:
                            description: SageMakerMonitoringScheduleVpcConfig is a
                              nested property type of AWS::SageMaker::MonitoringSchedule.
                            properties:
                              securityGroupIds:
                                description: |-
                                  The VPC security group IDs, in the form sg-xxxxxxxx. Specify the security groups for the VPC that is specified
                                  in the Subnets field.
                                items:
                                  type: string
                                type: array
                              subnets:
                                description: The ID of the subnets in the VPC to which
                                  you want to connect to your monitoring jobs.
                                items:
                                  type: string
                                type: array
                            required:
                            - securityGroupIds
                            - subnets
                            type: object
                        type: object
                      roleArn:
                        description: |-
                          The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your
                          behalf.
                        maxLength: 2048
                        minLength: 20
                        type: string
                      stoppingCondition:
                        description: SageMakerMonitoringScheduleStoppingCondition
                          is a nested property type of AWS::SageMaker::MonitoringSchedule.
                        properties:
                          maxRuntimeInSeconds:
                            description: The maximum runtime allowed in seconds.
                            format: int64
                            maximum: 86400
                            minimum: 1
                            type: integer
                        required:
                        - maxRuntimeInSeconds
                        type: object
                    required:
                    - monitoringAppSpecification
                    - monitoringInputs
                    - monitoringOutputConfig
                    - monitoringResources
                    - roleArn
                    type: object
                  monitoringJobDefinitionName:
                    description: Name of the job definition
                    maxLength: 63
                    type: string
                  monitoringType:
                    enum:
                    - DataQuality
                    - ModelQuality
                    - ModelBias
                    - ModelExplainability
                    type: string
                  scheduleConfig:
                    description: SageMakerMonitoringScheduleScheduleConfig is a nested
                      property type of AWS::SageMaker::MonitoringSchedule.
                    properties:
                      dataAnalysisEndTime:
                        description: Data Analysis end time, e.g. PT0H
                        maxLength: 15
                        type: string
                      dataAnalysisStartTime:
                        description: Data Analysis start time, e.g. -PT1H
                        maxLength: 15
                        type: string
                      scheduleExpression:
                        description: A cron expression or 'NOW' that describes details
                          about the monitoring schedule.
                        maxLength: 256
                        minLength: 1
                        type: string
                    required:
                    - scheduleExpression
                    type: object
                type: object
              monitoringScheduleName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 63
                type: string
                x-kubernetes-validations:
                - message: monitoringScheduleName is immutable
                  rule: self == oldSelf
              monitoringScheduleStatus:
                description: The status of a schedule job.
                enum:
                - Pending
                - Failed
                - Scheduled
                - Stopped
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - monitoringScheduleConfig
            - monitoringScheduleName
            type: object
          status:
            description: SageMakerMonitoringScheduleStatus is the observed state of
              AWS::SageMaker::MonitoringSchedule.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the schedule was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  A timestamp that indicates the last time the monitoring job was modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              monitoringScheduleArn:
                description: |-
                  The Amazon Resource Name (ARN) of the monitoring schedule.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerpartnerapps.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerPartnerApp
    listKind: SageMakerPartnerAppList
    plural: sagemakerpartnerapps
    singular: sagemakerpartnerapp
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerPartnerApp manages AWS::SageMaker::PartnerApp through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::PartnerApp
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerPartnerAppSpec is the desired state of AWS::SageMaker::PartnerApp.
            properties:
              appVersion:
                description: The version of the PartnerApp.
                maxLength: 64
                type: string
              applicationConfig:
                description: A collection of settings that specify the maintenance
                  schedule for the PartnerApp.
                properties:
                  adminUsers:
                    description: A list of users with administrator privileges for
                      the PartnerApp.
                    items:
                      type: string
                    type: array
                  arguments:
                    additionalProperties:
                      type: string
                    description: A list of arguments to pass to the PartnerApp.
                    type: object
                type: object
              authType:
                description: |-
                  The Auth type of PartnerApp.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - IAM
                type: string
                x-kubernetes-validations:
                - message: authType is immutable
                  rule: self == oldSelf
              clientToken:
                description: The client token for the PartnerApp.
                maxLength: 36
                type: string
              enableAutoMinorVersionUpgrade:
                description: Enables automatic minor version upgrades for the PartnerApp.
                type: boolean
              enableIamSessionBasedIdentity:
                description: Enables IAM Session based Identity for PartnerApp.
                type: boolean
              executionRoleArn:
                description: |-
                  The execution role for the user.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: executionRoleArn is immutable
                  rule: self == oldSelf
              kmsKeyId:
                description: |-
                  The AWS KMS customer managed key used to encrypt the data associated with the PartnerApp.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              maintenanceConfig:
                description: A collection of settings that specify the maintenance
                  schedule for the PartnerApp.
                properties:
                  maintenanceWindowStart:
                    description: The maintenance window start day and time for the
                      PartnerApp.
                    maxLength: 9
                    type: string
                required:
                - maintenanceWindowStart
                type: object
              name:
                description: |-
                  A name for the PartnerApp.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: A list of tags to apply to the PartnerApp.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              tier:
                description: The tier of the PartnerApp.
                maxLength: 64
                minLength: 1
                type: string
              type:
                description: |-
                  The type of PartnerApp.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - lakera-guard
                - comet
                - deepchecks-llm-evaluation
                - fiddler
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - authType
            - executionRoleArn
            - name
            - tier
            - type
            type: object
          status:
            description: SageMakerPartnerAppStatus is the observed state of AWS::SageMaker::PartnerApp.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the created PartnerApp.
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              baseUrl:
                description: |-
                  The AppServerUrl based on app and account-info.
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentVersionEolDate:
                description: |-
                  The end-of-life date for the current version of the PartnerApp.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerpipelines.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerPipeline
    listKind: SageMakerPipelineList
    plural: sagemakerpipelines
    singular: sagemakerpipeline
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerPipeline manages AWS::SageMaker::Pipeline through the AWS Cloud Control API. Resource Type definition
          for AWS::SageMaker::Pipeline
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerPipelineSpec is the desired state of AWS::SageMaker::Pipeline.
            properties:
              parallelismConfiguration:
                description: SageMakerPipelineParallelismConfiguration is a nested
                  property type of AWS::SageMaker::Pipeline.
                properties:
                  maxParallelExecutionSteps:
                    description: Maximum parallel execution steps
                    format: int64
                    minimum: 1
                    type: integer
                required:
                - maxParallelExecutionSteps
                type: object
              pipelineDefinition:
                x-kubernetes-preserve-unknown-fields: true
              pipelineDescription:
                description: The description of the Pipeline.
                maxLength: 3072
                type: string
              pipelineDisplayName:
                description: The display name of the Pipeline.
                maxLength: 256
                type: string
              pipelineName:
                description: |-
                  The name of the Pipeline.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: pipelineName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: Role Arn
                maxLength: 2048
                minLength: 20
                type: string
              tags:
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - pipelineDefinition
            - pipelineName
            - roleArn
            type: object
          status:
            description: SageMakerPipelineStatus is the observed state of AWS::SageMaker::Pipeline.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerprocessingjobs.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerProcessingJob
    listKind: SageMakerProcessingJobList
    plural: sagemakerprocessingjobs
    singular: sagemakerprocessingjob
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerProcessingJob manages AWS::SageMaker::ProcessingJob through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::ProcessingJob
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerProcessingJobSpec is the desired state of AWS::SageMaker::ProcessingJob.
            properties:
              appSpecification:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  containerArguments:
                    description: The arguments for a container used to run a processing
                      job.
                    items:
                      type: string
                    type: array
                  containerEntrypoint:
                    description: The entrypoint for a container used to run a processing
                      job.
                    items:
                      type: string
                    type: array
                  imageUri:
                    description: The container image to be run by the processing job.
                    maxLength: 256
                    type: string
                required:
                - imageUri
                type: object
              environment:
                additionalProperties:
                  type: string
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: object
              experimentConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  experimentName:
                    description: The name of an existing experiment to associate with
                      the trial component.
                    maxLength: 120
                    type: string
                  runName:
                    description: The name of the experiment run to associate with
                      the trial component.
                    maxLength: 120
                    type: string
                  trialComponentDisplayName:
                    description: |-
                      The display name for the trial component. If this key isn't specified, the display name is the trial component
                      name.
                    maxLength: 120
                    type: string
                  trialName:
                    description: The name of an existing trial to associate the trial
                      component with. If not specified, a new trial is created.
                    maxLength: 120
                    type: string
                type: object
              networkConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  enableInterContainerTrafficEncryption:
                    description: |-
                      Whether to encrypt all communications between distributed processing jobs. Choose True to encrypt
                      communications. Encryption provides greater security for distributed processing jobs, but the processing might
                      take longer.
                    type: boolean
                  enableNetworkIsolation:
                    description: Whether to allow inbound and outbound network calls
                      to and from the containers used for the processing job.
                    type: boolean
                  vpcConfig:
                    description: SageMakerProcessingJobVpcConfig is a nested property
                      type of AWS::SageMaker::ProcessingJob.
                    properties:
                      securityGroupIds:
                        description: |-
                          The VPC security group IDs, in the form 'sg-xxxxxxxx'. Specify the security groups for the VPC that is
                          specified in the 'Subnets' field.
                        items:
                          type: string
                        type: array
                      subnets:
                        description: |-
                          The ID of the subnets in the VPC to which you want to connect your training job or model. For information
                          about the availability of specific instance types, see
                          https://docs.aws.amazon.com/sagemaker/latest/dg/regions-quotas.html
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroupIds
                    - subnets
                    type: object
                type: object
              processingInputs:
                description: |-
                  An array of inputs configuring the data to download into the processing container.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: SageMakerProcessingJobProcessingInputsObject is a nested
                    property type of AWS::SageMaker::ProcessingJob.
                  properties:
                    appManaged:
                      description: |-
                        When True, input operations such as data download are managed natively by the processing job application. When
                        False (default), input operations are managed by Amazon SageMaker.
                      type: boolean
                    datasetDefinition:
                      description: SageMakerProcessingJobDatasetDefinition is a nested
                        property type of AWS::SageMaker::ProcessingJob.
                      properties:
                        athenaDatasetDefinition:
                          description: SageMakerProcessingJobAthenaDatasetDefinition
                            is a nested property type of AWS::SageMaker::ProcessingJob.
                          properties:
                            catalog:
                              description: The name of the data catalog used in Athena
                                query execution.
                              maxLength: 256
                              type: string
                            database:
                              description: The name of the database used in the Athena
                                query execution.
                              maxLength: 255
                              type: string
                            kmsKeyId:
                              description: |-
                                The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data generated from an
                                Athena query execution.
                              maxLength: 2048
                              type: string
                            outputCompression:
                              description: The compression used for Athena query results.
                              enum:
                              - GZIP
                              - SNAPPY
                              - ZLIB
                              type: string
                            outputFormat:
                              description: The data storage format for Athena query
                                results.
                              enum:
                              - PARQUET
                              - AVRO
                              - ORC
                              - JSON
                              - TEXTFILE
                              type: string
                            outputS3Uri:
                              description: The location in Amazon S3 where Athena
                                query results are stored.
                              maxLength: 1024
                              type: string
                            queryString:
                              description: The SQL query statements, to be executed.
                              maxLength: 4096
                              type: string
                            workGroup:
                              description: The name of the workgroup in which the
                                Athena query is being started.
                              maxLength: 128
                              type: string
                          required:
                          - catalog
                          - database
                          - outputFormat
                          - outputS3Uri
                          - queryString
                          type: object
                        dataDistributionType:
                          description: Whether the generated dataset is FullyReplicated
                            or ShardedByS3Key (default).
                          enum:
                          - FullyReplicated
                          - ShardedByS3Key
                          type: string
                        inputMode:
                          description: |-
                            Whether to use File or Pipe input mode. In File (default) mode, Amazon SageMaker copies the data from the
                            input source onto the local Amazon Elastic Block Store (Amazon EBS) volumes before starting your training
                            algorithm. This is the most commonly used input mode. In Pipe mode, Amazon SageMaker streams input data from
                            the source directly to your algorithm without using the EBS volume.
                          enum:
                          - File
                          - Pipe
                          type: string
                        localPath:
                          description: |-
                            The local path where you want Amazon SageMaker to download the Dataset Definition inputs to run a processing
                            job. LocalPath is an absolute path to the input data. This is a required parameter when AppManaged is False
                            (default).
                          maxLength: 256
                          type: string
                        redshiftDatasetDefinition:
                          description: SageMakerProcessingJobRedshiftDatasetDefinition
                            is a nested property type of AWS::SageMaker::ProcessingJob.
                          properties:
                            clusterId:
                              description: The Redshift cluster Identifier.
                              maxLength: 63
                              type: string
                            clusterRoleArn:
                              description: The IAM role attached to your Redshift
                                cluster that Amazon SageMaker uses to generate datasets.
                              maxLength: 2048
                              minLength: 20
                              type: string
                            database:
                              description: The name of the Redshift database used
                                in Redshift query execution.
                              maxLength: 64
                              type: string
                            dbUser:
                              description: The database user name used in Redshift
                                query execution.
                              maxLength: 128
                              type: string
                            kmsKeyId:
                              description: |-
                                The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data from a Redshift
                                execution.
                              maxLength: 2048
                              type: string
                            outputCompression:
                              description: The compression used for Redshift query
                                results.
                              enum:
                              - None
                              - GZIP
                              - SNAPPY
                              - ZSTD
                              - BZIP2
                              type: string
                            outputFormat:
                              description: The data storage format for Redshift query
                                results.
                              enum:
                              - PARQUET
                              - CSV
                              type: string
                            outputS3Uri:
                              description: The location in Amazon S3 where the Redshift
                                query results are stored.
                              maxLength: 1024
                              type: string
                            queryString:
                              description: The SQL query statements to be executed.
                              maxLength: 4096
                              type: string
                          required:
                          - clusterId
                          - clusterRoleArn
                          - database
                          - dbUser
                          - outputFormat
                          - outputS3Uri
                          - queryString
                          type: object
                      type: object
                    inputName:
                      description: The name for the processing job input.
                      type: string
                    s3Input:
                      description: SageMakerProcessingJobS3Input is a nested property
                        type of AWS::SageMaker::ProcessingJob.
                      properties:
                        localPath:
                          description: |-
                            The local path in your container where you want Amazon SageMaker to write input data to. `LocalPath` is an
                            absolute path to the input data and must begin with `/opt/ml/processing/`. LocalPath is a required parameter
                            when `AppManaged` is `False` (default).
                          maxLength: 256
                          type: string
                        s3CompressionType:
                          description: |-
                            Whether to GZIP-decompress the data in Amazon S3 as it is streamed into the processing container. `Gzip` can
                            only be used when `Pipe` mode is specified as the `S3InputMode`. In `Pipe` mode, Amazon SageMaker streams
                            input data from the source directly to your container without using the EBS volume.
                          enum:
                          - None
                          - Gzip
                          type: string
                        s3DataDistributionType:
                          description: |-
                            Whether to distribute the data from Amazon S3 to all processing instances with `FullyReplicated`, or whether
                            the data from Amazon S3 is shared by Amazon S3 key, downloading one shard of data to each processing instance.
                          enum:
                          - FullyReplicated
                          - ShardedByS3Key
                          type: string
                        s3DataType:
                          description: |-
                            Whether you use an S3Prefix or a ManifestFile for the data type. If you choose S3Prefix, S3Uri identifies a
                            key name prefix. Amazon SageMaker uses all objects with the specified key name prefix for the processing job.
                            If you choose ManifestFile, S3Uri identifies an object that is a manifest file containing a list of object
                            keys that you want Amazon SageMaker to use for the processing job.
                          enum:
                          - ManifestFile
                          - S3Prefix
                          type: string
                        s3InputMode:
                          description: |-
                            Whether to use File or Pipe input mode. In File mode, Amazon SageMaker copies the data from the input source
                            onto the local ML storage volume before starting your processing container. This is the most commonly used
                            input mode. In Pipe mode, Amazon SageMaker streams input data from the source directly to your processing
                            container into named pipes without using the ML storage volume.
                          enum:
                          - File
                          - Pipe
                          type: string
                        s3Uri:
                          description: The URI of the Amazon S3 prefix Amazon SageMaker
                            downloads data required to run a processing job.
                          maxLength: 1024
                          type: string
                      required:
                      - s3DataType
                      - s3Uri
                      type: object
                  required:
                  - inputName
                  type: object
                type: array
              processingJobName:
                description: |-
                  The name of the processing job. The name must be unique within an AWS Region in the AWS account.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                type: string
              processingOutputConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  kmsKeyId:
                    description: |-
                      The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt the processing job output.
                      KmsKeyId can be an ID of a KMS key, ARN of a KMS key, or alias of a KMS key. The KmsKeyId is applied to all
                      outputs.
                    maxLength: 2048
                    type: string
                  outputs:
                    description: An array of outputs configuring the data to upload
                      from the processing container.
                    items:
                      description: SageMakerProcessingJobProcessingOutputsObject is
                        a nested property type of AWS::SageMaker::ProcessingJob.
                      properties:
                        appManaged:
                          description: |-
                            When True, output operations such as data upload are managed natively by the processing job application. When
                            False (default), output operations are managed by Amazon SageMaker.
                          type: boolean
                        featureStoreOutput:
                          description: SageMakerProcessingJobFeatureStoreOutput is
                            a nested property type of AWS::SageMaker::ProcessingJob.
                          properties:
                            featureGroupName:
                              description: |-
                                The name of the Amazon SageMaker FeatureGroup to use as the destination for processing job output. Note that
                                your processing script is responsible for putting records into your Feature Store.
                              maxLength: 64
                              type: string
                          required:
                          - featureGroupName
                          type: object
                        outputName:
                          description: The name for the processing job output.
                          type: string
                        s3Output:
                          description: SageMakerProcessingJobS3Output is a nested
                            property type of AWS::SageMaker::ProcessingJob.
                          properties:
                            localPath:
                              description: |-
                                The local path of a directory where you want Amazon SageMaker to upload its contents to Amazon S3. LocalPath
                                is an absolute path to a directory containing output files. This directory will be created by the platform and
                                exist when your container's entrypoint is invoked.
                              maxLength: 256
                              type: string
                            s3UploadMode:
                              description: Whether to upload the results of the processing
                                job continuously or after the job completes.
                              enum:
                              - Continuous
                              - EndOfJob
                              type: string
                            s3Uri:
                              description: |-
                                A URI that identifies the Amazon S3 bucket where you want Amazon SageMaker to save the results of a processing
                                job.
                              maxLength: 1024
                              type: string
                          required:
                          - s3UploadMode
                          - s3Uri
                          type: object
                      required:
                      - outputName
                      type: object
                    type: array
                required:
                - outputs
                type: object
              processingResources:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  clusterConfig:
                    description: SageMakerProcessingJobClusterConfig is a nested property
                      type of AWS::SageMaker::ProcessingJob.
                    properties:
                      instanceCount:
                        description: |-
                          The number of ML compute instances to use in the processing job. For distributed processing jobs, specify a
                          value greater than 1. The default value is 1.
                        format: int64
                        maximum: 100
                        minimum: 1
                        type: integer
                      instanceType:
                        description: The ML compute instance type for the processing
                          job.
                        type: string
                      volumeKmsKeyId:
                        description: |-
                          The AWS Key Management Service (AWS KMS) key that Amazon SageMaker uses to encrypt data on the storage volume
                          attached to the ML compute instance(s) that run the processing job.
                        maxLength: 2048
                        type: string
                      volumeSizeInGB:
                        description: |-
                          The size of the ML storage volume in gigabytes that you want to provision. You must specify sufficient ML
                          storage for your scenario.
                        format: int64
                        maximum: 16384
                        minimum: 1
                        type: integer
                    required:
                    - instanceCount
                    - instanceType
                    - volumeSizeInGB
                    type: object
                required:
                - clusterConfig
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: |-
                  The Amazon Resource Name (ARN) of an IAM role that Amazon SageMaker can assume to perform tasks on your
                  behalf.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: roleArn is immutable
                  rule: self == oldSelf
              stoppingCondition:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  maxRuntimeInSeconds:
                    description: Specifies the maximum runtime in seconds.
                    format: int64
                    maximum: 777600
                    minimum: 1
                    type: integer
                required:
                - maxRuntimeInSeconds
                type: object
              tags:
                description: |-
                  (Optional) An array of key-value pairs. For more information, see Using Cost Allocation
                  Tags(https://docs.aws.amazon.com/awsaccountbilling/latest/aboutv2/cost-alloc-tags.html#allocation-whatURL) in
                  the AWS Billing and Cost Management User Guide.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - appSpecification
            - processingResources
            - roleArn
            type: object
          status:
            description: SageMakerProcessingJobStatus is the observed state of AWS::SageMaker::ProcessingJob.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              autoMLJobArn:
                description: |-
                  The ARN of an AutoML job associated with this processing job.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the processing job was created.
                  Read-only: reported by AWS after creation.
                type: string
              exitMessage:
                description: |-
                  An optional string, up to one KB in size, that contains metadata from the processing container when the
                  processing job exits.
                  Read-only: reported by AWS after creation.
                maxLength: 1024
                type: string
              failureReason:
                description: |-
                  A string, up to one KB in size, that contains the reason a processing job failed, if it failed.
                  Read-only: reported by AWS after creation.
                maxLength: 1024
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  The time at which the processing job was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              monitoringScheduleArn:
                description: |-
                  The ARN of a monitoring schedule for an endpoint associated with this processing job.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              processingEndTime:
                description: |-
                  The time at which the processing job completed.
                  Read-only: reported by AWS after creation.
                type: string
              processingJobArn:
                description: |-
                  The Amazon Resource Name (ARN) of the processing job.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              processingJobStatus:
                description: |-
                  Provides the status of a processing job.
                  Read-only: reported by AWS after creation.
                enum:
                - Completed
                - InProgress
                - Stopping
                - Stopped
                - Failed
                type: string
              processingStartTime:
                description: |-
                  The time at which the processing job started.
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              trainingJobArn:
                description: |-
                  The ARN of a training job associated with this processing job
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerprojects.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerProject
    listKind: SageMakerProjectList
    plural: sagemakerprojects
    singular: sagemakerproject
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerProject manages AWS::SageMaker::Project through the AWS Cloud Control API. Resource Type definition
          for AWS::SageMaker::Project
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerProjectSpec is the desired state of AWS::SageMaker::Project.
            properties:
              projectDescription:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 1024
                type: string
              projectName:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                maxLength: 32
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: projectName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              serviceCatalogProvisionedProductDetails:
                description: Provisioned ServiceCatalog Details
                properties:
                  provisionedProductId:
                    maxLength: 100
                    type: string
                  provisionedProductStatusMessage:
                    type: string
                type: object
              serviceCatalogProvisioningDetails:
                description: |-
                  Input ServiceCatalog Provisioning Details
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  pathId:
                    maxLength: 100
                    type: string
                  productId:
                    maxLength: 100
                    type: string
                  provisioningArtifactId:
                    maxLength: 100
                    type: string
                  provisioningParameters:
                    description: Parameters specified by the administrator that are
                      required for provisioning the product.
                    items:
                      description: CFNTag is the CloudFormation Tag shape shared by
                        generated kinds.
                      properties:
                        key:
                          minLength: 1
                          type: string
                        value:
                          type: string
                      required:
                      - key
                      - value
                      type: object
                    type: array
                required:
                - productId
                type: object
              tags:
                description: |-
                  An array of key-value pairs to apply to this resource.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              templateProviderDetails:
                description: |-
                  An array of template providers associated with the project.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: SageMakerProjectTemplateProviderDetail is a nested
                    property type of AWS::SageMaker::Project.
                  properties:
                    cfnTemplateProviderDetail:
                      description: SageMakerProjectCfnTemplateProviderDetail is a
                        nested property type of AWS::SageMaker::Project.
                      properties:
                        parameters:
                          items:
                            description: CFNTag is the CloudFormation Tag shape shared
                              by generated kinds.
                            properties:
                              key:
                                minLength: 1
                                type: string
                              value:
                                type: string
                            required:
                            - key
                            - value
                            type: object
                          type: array
                        roleARN:
                          description: The Amazon Resource Name (ARN) of the IAM role
                            used by the template provider.
                          maxLength: 2048
                          type: string
                        templateName:
                          description: The name of the template used for the project.
                          maxLength: 32
                          minLength: 1
                          type: string
                        templateURL:
                          description: The URL of the CloudFormation template.
                          maxLength: 1024
                          minLength: 1
                          type: string
                      required:
                      - templateName
                      - templateURL
                      type: object
                  type: object
                type: array
            required:
            - projectName
            type: object
          status:
            description: SageMakerProjectStatus is the observed state of AWS::SageMaker::Project.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  The time at which the project was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              projectArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              projectId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 20
                type: string
              projectStatus:
                description: |-
                  The status of a project.
                  Read-only: reported by AWS after creation.
                enum:
                - Pending
                - CreateInProgress
                - CreateCompleted
                - CreateFailed
                - DeleteInProgress
                - DeleteFailed
                - DeleteCompleted
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerspaces.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerSpace
    listKind: SageMakerSpaceList
    plural: sagemakerspaces
    singular: sagemakerspace
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerSpace manages AWS::SageMaker::Space through the AWS Cloud Control API. Resource Type definition for
          AWS::SageMaker::Space
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerSpaceSpec is the desired state of AWS::SageMaker::Space.
            properties:
              domainId:
                description: |-
                  The ID of the associated Domain.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: domainId is immutable
                  rule: self == oldSelf
              ownershipSettings:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  ownerUserProfileName:
                    maxLength: 63
                    type: string
                required:
                - ownerUserProfileName
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              spaceDisplayName:
                maxLength: 64
                type: string
              spaceName:
                description: |-
                  A name for the Space.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: spaceName is immutable
                  rule: self == oldSelf
              spaceSettings:
                description: A collection of settings.
                properties:
                  appType:
                    enum:
                    - JupyterServer
                    - KernelGateway
                    - TensorBoard
                    - RStudioServerPro
                    - RSessionGateway
                    - JupyterLab
                    - CodeEditor
                    type: string
                  codeEditorAppSettings:
                    description: The CodeEditor app settings.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerSpaceSpaceAppLifecycleManagement is
                          a nested property type of AWS::SageMaker::Space.
                        properties:
                          idleSettings:
                            description: SageMakerSpaceSpaceIdleSettings is a nested
                              property type of AWS::SageMaker::Space.
                            properties:
                              idleTimeoutInMinutes:
                                description: The space idle timeout value set in minutes
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      defaultResourceSpec:
                        description: SageMakerSpaceResourceSpec is a nested property
                          type of AWS::SageMaker::Space.
                        properties:
                          instanceType:
                            description: The instance type that the image version
                              runs on.
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                        type: object
                    type: object
                  customFileSystems:
                    items:
                      description: SageMakerSpaceCustomFileSystem is a nested property
                        type of AWS::SageMaker::Space.
                      properties:
                        efsFileSystem:
                          description: SageMakerSpaceEFSFileSystem is a nested property
                            type of AWS::SageMaker::Space.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        fSxLustreFileSystem:
                          description: SageMakerSpaceFSxLustreFileSystem is a nested
                            property type of AWS::SageMaker::Space.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        s3FileSystem:
                          description: SageMakerSpaceS3FileSystem is a nested property
                            type of AWS::SageMaker::Space.
                          properties:
                            s3Uri:
                              maxLength: 1024
                              type: string
                          type: object
                      type: object
                    type: array
                  jupyterLabAppSettings:
                    description: The JupyterLab app settings.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerSpaceSpaceAppLifecycleManagement is
                          a nested property type of AWS::SageMaker::Space.
                        properties:
                          idleSettings:
                            description: SageMakerSpaceSpaceIdleSettings is a nested
                              property type of AWS::SageMaker::Space.
                            properties:
                              idleTimeoutInMinutes:
                                description: The space idle timeout value set in minutes
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      codeRepositories:
                        description: A list of CodeRepositories available for use
                          with JupyterLab apps.
                        items:
                          description: SageMakerSpaceCodeRepository is a nested property
                            type of AWS::SageMaker::Space.
                          properties:
                            repositoryUrl:
                              description: A CodeRepository (valid URL) to be used
                                within Jupyter's Git extension.
                              maxLength: 256
                              type: string
                          required:
                          - repositoryUrl
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: SageMakerSpaceResourceSpec is a nested property
                          type of AWS::SageMaker::Space.
                        properties:
                          instanceType:
                            description: The instance type that the image version
                              runs on.
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                        type: object
                    type: object
                  jupyterServerAppSettings:
                    description: The Jupyter server's app settings.
                    properties:
                      defaultResourceSpec:
                        description: SageMakerSpaceResourceSpec is a nested property
                          type of AWS::SageMaker::Space.
                        properties:
                          instanceType:
                            description: The instance type that the image version
                              runs on.
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterServer apps.
                        items:
                          type: string
                        type: array
                    type: object
                  kernelGatewayAppSettings:
                    description: The kernel gateway app settings.
                    properties:
                      customImages:
                        description: A list of custom SageMaker images that are configured
                          to run as a KernelGateway app.
                        items:
                          description: SageMakerSpaceCustomImage is a nested property
                            type of AWS::SageMaker::Space.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          KernelGateway app.
                        properties:
                          instanceType:
                            description: The instance type that the image version
                              runs on.
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with KernelGateway apps.
                        items:
                          type: string
                        type: array
                    type: object
                  remoteAccess:
                    description: This is a flag used to indicate if remote access
                      is enabled.
                    enum:
                    - ENABLED
                    - DISABLED
                    type: string
                  spaceManagedResources:
                    description: This is a flag used to indicate if space managed
                      resources needs to be created.
                    enum:
                    - ENABLED
                    - DISABLED
                    type: string
                  spaceStorageSettings:
                    description: Default storage settings for a space.
                    properties:
                      ebsStorageSettings:
                        description: SageMakerSpaceEbsStorageSettings is a nested
                          property type of AWS::SageMaker::Space.
                        properties:
                          ebsVolumeSizeInGb:
                            description: Size of the Amazon EBS volume in Gb
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                        required:
                        - ebsVolumeSizeInGb
                        type: object
                    type: object
                type: object
              spaceSharingSettings:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  sharingType:
                    enum:
                    - Private
                    - Shared
                    type: string
                required:
                - sharingType
                type: object
              tags:
                description: A list of tags to apply to the space.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - domainId
            - spaceName
            type: object
          status:
            description: SageMakerSpaceStatus is the observed state of AWS::SageMaker::Space.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              spaceArn:
                description: |-
                  The space Amazon Resource Name (ARN).
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              url:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 1024
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerstudiolifecycleconfigs.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerStudioLifecycleConfig
    listKind: SageMakerStudioLifecycleConfigList
    plural: sagemakerstudiolifecycleconfigs
    singular: sagemakerstudiolifecycleconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerStudioLifecycleConfig manages AWS::SageMaker::StudioLifecycleConfig through the AWS Cloud Control
          API. Resource Type definition for AWS::SageMaker::StudioLifecycleConfig
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerStudioLifecycleConfigSpec is the desired state of
              AWS::SageMaker::StudioLifecycleConfig.
            properties:
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              studioLifecycleConfigAppType:
                description: |-
                  The App type that the Lifecycle Configuration is attached to.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - JupyterServer
                - KernelGateway
                - CodeEditor
                - JupyterLab
                type: string
                x-kubernetes-validations:
                - message: studioLifecycleConfigAppType is immutable
                  rule: self == oldSelf
              studioLifecycleConfigContent:
                description: |-
                  The content of your Amazon SageMaker Studio Lifecycle Configuration script. This content must be base64
                  encoded.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 16384
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: studioLifecycleConfigContent is immutable
                  rule: self == oldSelf
              studioLifecycleConfigName:
                description: |-
                  The name of the Amazon SageMaker Studio Lifecycle Configuration.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: studioLifecycleConfigName is immutable
                  rule: self == oldSelf
              tags:
                description: |-
                  Tags to be associated with the Lifecycle Configuration. Each tag consists of a key and an optional value. Tag
                  keys must be unique per resource. Tags are searchable using the Search API.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - studioLifecycleConfigAppType
            - studioLifecycleConfigContent
            - studioLifecycleConfigName
            type: object
          status:
            description: SageMakerStudioLifecycleConfigStatus is the observed state
              of AWS::SageMaker::StudioLifecycleConfig.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              studioLifecycleConfigArn:
                description: |-
                  The Amazon Resource Name (ARN) of the Lifecycle Configuration.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakertrialcomponents.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerTrialComponent
    listKind: SageMakerTrialComponentList
    plural: sagemakertrialcomponents
    singular: sagemakertrialcomponent
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerTrialComponent manages AWS::SageMaker::TrialComponent through the AWS Cloud Control API. Resource
          type definition for AWS::SageMaker::TrialComponent. A trial component is a stage of a machine learning trial,
          such as a preprocessing job, training job, or batch transform job.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerTrialComponentSpec is the desired state of AWS::SageMaker::TrialComponent.
            properties:
              displayName:
                description: The name of the component as displayed. If DisplayName
                  isn't specified, TrialComponentName is displayed.
                maxLength: 120
                type: string
              inputArtifacts:
                additionalProperties:
                  description: SageMakerTrialComponentTrialComponentArtifact is a
                    nested property type of AWS::SageMaker::TrialComponent.
                  properties:
                    mediaType:
                      description: The media type of the artifact.
                      maxLength: 64
                      type: string
                    value:
                      description: The location of the artifact.
                      maxLength: 2048
                      type: string
                  required:
                  - value
                  type: object
                description: The input artifacts for the component.
                type: object
              metadataProperties:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  commitId:
                    description: The commit ID.
                    maxLength: 1024
                    type: string
                  generatedBy:
                    description: The entity this entity was generated by.
                    maxLength: 1024
                    type: string
                  projectId:
                    description: The project ID.
                    maxLength: 1024
                    type: string
                  repository:
                    description: The repository.
                    maxLength: 1024
                    type: string
                type: object
              outputArtifacts:
                additionalProperties:
                  description: SageMakerTrialComponentTrialComponentArtifact is a
                    nested property type of AWS::SageMaker::TrialComponent.
                  properties:
                    mediaType:
                      description: The media type of the artifact.
                      maxLength: 64
                      type: string
                    value:
                      description: The location of the artifact.
                      maxLength: 2048
                      type: string
                  required:
                  - value
                  type: object
                description: The output artifacts for the component.
                type: object
              parameters:
                additionalProperties:
                  description: SageMakerTrialComponentTrialComponentParameterValue
                    is a nested property type of AWS::SageMaker::TrialComponent.
                  properties:
                    numberValue:
                      description: The numeric value of a numeric hyperparameter.
                      type: number
                    stringValue:
                      description: The string value of a categorical hyperparameter.
                      maxLength: 2500
                      type: string
                  type: object
                description: The hyperparameters for the component.
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              status:
                description: SageMakerTrialComponentTrialComponentStatus is a nested
                  property type of AWS::SageMaker::TrialComponent.
                properties:
                  message:
                    description: If the component failed, a message describing why.
                    maxLength: 1024
                    type: string
                  primaryStatus:
                    description: The status of the trial component.
                    enum:
                    - InProgress
                    - Completed
                    - Failed
                    - Stopping
                    - Stopped
                    type: string
                type: object
              tags:
                description: A list of tags to associate with the trial component.
                items:
                  description: SageMakerTrialComponentTagsItem is a nested property
                    type of AWS::SageMaker::TrialComponent.
                  properties:
                    key:
                      description: The tag key.
                      maxLength: 128
                      minLength: 1
                      type: string
                    value:
                      description: The tag value.
                      maxLength: 256
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              trialComponentName:
                description: |-
                  The name of the trial component. Must be unique in your AWS account and is not case-sensitive.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 120
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: trialComponentName is immutable
                  rule: self == oldSelf
            required:
            - trialComponentName
            type: object
          status:
            description: SageMakerTrialComponentStatus is the observed state of AWS::SageMaker::TrialComponent.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              creationTime:
                description: |-
                  When the trial component was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastModifiedTime:
                description: |-
                  When the trial component was last modified.
                  Read-only: reported by AWS after creation.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lineageGroupArn:
                description: |-
                  The Amazon Resource Name (ARN) of the lineage group.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              trialComponentArn:
                description: |-
                  The Amazon Resource Name (ARN) of the trial component.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakeruserprofiles.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerUserProfile
    listKind: SageMakerUserProfileList
    plural: sagemakeruserprofiles
    singular: sagemakeruserprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerUserProfile manages AWS::SageMaker::UserProfile through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::UserProfile
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerUserProfileSpec is the desired state of AWS::SageMaker::UserProfile.
            properties:
              domainId:
                description: |-
                  The ID of the associated Domain.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: domainId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              singleSignOnUserIdentifier:
                description: |-
                  A specifier for the type of value specified in SingleSignOnUserValue. Currently, the only supported value is
                  "UserName". If the Domain's AuthMode is SSO, this field is required. If the Domain's AuthMode is not SSO, this
                  field cannot be specified.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              singleSignOnUserValue:
                description: |-
                  The username of the associated AWS Single Sign-On User for this UserProfile. If the Domain's AuthMode is SSO,
                  this field is required, and must match a valid username of a user in your directory. If the Domain's AuthMode
                  is not SSO, this field cannot be specified.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 256
                type: string
              tags:
                description: A list of tags to apply to the user profile.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              userProfileName:
                description: |-
                  A name for the UserProfile.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: userProfileName is immutable
                  rule: self == oldSelf
              userSettings:
                description: A collection of settings.
                properties:
                  autoMountHomeEFS:
                    description: Indicates whether auto-mounting of an EFS volume
                      is supported for the user profile.
                    enum:
                    - Enabled
                    - Disabled
                    - DefaultAsDomain
                    type: string
                  codeEditorAppSettings:
                    description: SageMakerUserProfileCodeEditorAppSettings is a nested
                      property type of AWS::SageMaker::UserProfile.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerUserProfileAppLifecycleManagement is
                          a nested property type of AWS::SageMaker::UserProfile.
                        properties:
                          idleSettings:
                            description: SageMakerUserProfileIdleSettings is a nested
                              property type of AWS::SageMaker::UserProfile.
                            properties:
                              idleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              lifecycleManagement:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              maxIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              minIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      builtInLifecycleConfigArn:
                        description: The lifecycle configuration that runs before
                          the default lifecycle configuration.
                        maxLength: 256
                        type: string
                      customImages:
                        description: A list of custom images for use for CodeEditor
                          apps.
                        items:
                          description: SageMakerUserProfileCustomImage is a nested
                            property type of AWS::SageMaker::UserProfile.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          CodeEditor app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with CodeEditor apps.
                        items:
                          type: string
                        type: array
                    type: object
                  customFileSystemConfigs:
                    items:
                      description: SageMakerUserProfileCustomFileSystemConfig is a
                        nested property type of AWS::SageMaker::UserProfile.
                      properties:
                        efsFileSystemConfig:
                          description: SageMakerUserProfileEFSFileSystemConfig is
                            a nested property type of AWS::SageMaker::UserProfile.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                            fileSystemPath:
                              maxLength: 256
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        fSxLustreFileSystemConfig:
                          description: SageMakerUserProfileFSxLustreFileSystemConfig
                            is a nested property type of AWS::SageMaker::UserProfile.
                          properties:
                            fileSystemId:
                              maxLength: 21
                              minLength: 11
                              type: string
                            fileSystemPath:
                              maxLength: 256
                              type: string
                          required:
                          - fileSystemId
                          type: object
                        s3FileSystemConfig:
                          description: SageMakerUserProfileS3FileSystemConfig is a
                            nested property type of AWS::SageMaker::UserProfile.
                          properties:
                            mountPath:
                              maxLength: 1024
                              type: string
                            s3Uri:
                              maxLength: 1024
                              type: string
                          type: object
                      type: object
                    type: array
                  customPosixUserConfig:
                    description: SageMakerUserProfileCustomPosixUserConfig is a nested
                      property type of AWS::SageMaker::UserProfile.
                    properties:
                      gid:
                        format: int64
                        maximum: 4000000
                        minimum: 1001
                        type: integer
                      uid:
                        format: int64
                        maximum: 4000000
                        minimum: 10000
                        type: integer
                    required:
                    - gid
                    - uid
                    type: object
                  defaultLandingUri:
                    description: Defines which Amazon SageMaker application users
                      are directed to by default.
                    maxLength: 1023
                    type: string
                  executionRole:
                    description: The user profile Amazon Resource Name (ARN).
                    maxLength: 2048
                    type: string
                  jupyterLabAppSettings:
                    description: SageMakerUserProfileJupyterLabAppSettings is a nested
                      property type of AWS::SageMaker::UserProfile.
                    properties:
                      appLifecycleManagement:
                        description: SageMakerUserProfileAppLifecycleManagement is
                          a nested property type of AWS::SageMaker::UserProfile.
                        properties:
                          idleSettings:
                            description: SageMakerUserProfileIdleSettings is a nested
                              property type of AWS::SageMaker::UserProfile.
                            properties:
                              idleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              lifecycleManagement:
                                enum:
                                - ENABLED
                                - DISABLED
                                type: string
                              maxIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                              minIdleTimeoutInMinutes:
                                format: int64
                                maximum: 525600
                                minimum: 60
                                type: integer
                            type: object
                        type: object
                      builtInLifecycleConfigArn:
                        description: The lifecycle configuration that runs before
                          the default lifecycle configuration.
                        maxLength: 256
                        type: string
                      codeRepositories:
                        description: A list of CodeRepositories available for use
                          with JupyterLab apps.
                        items:
                          description: SageMakerUserProfileCodeRepository is a nested
                            property type of AWS::SageMaker::UserProfile.
                          properties:
                            repositoryUrl:
                              description: A CodeRepository (valid URL) to be used
                                within Jupyter's Git extension.
                              maxLength: 256
                              type: string
                          required:
                          - repositoryUrl
                          type: object
                        type: array
                      customImages:
                        description: A list of custom images available for use for
                          JupyterLab apps
                        items:
                          description: SageMakerUserProfileCustomImage is a nested
                            property type of AWS::SageMaker::UserProfile.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          JupyterLab app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      emrSettings:
                        description: The configuration parameters for EMR settings.
                        properties:
                          assumableRoleArns:
                            description: An array of Amazon Resource Names (ARNs)
                              of the IAM roles that the execution role of SageMaker
                              can assume.
                            items:
                              type: string
                            type: array
                          executionRoleArns:
                            description: An array of ARNs of IAM roles used by EMR
                              cluster instances or job execution environments.
                            items:
                              type: string
                            type: array
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterLab apps.
                        items:
                          type: string
                        type: array
                    type: object
                  jupyterServerAppSettings:
                    description: The Jupyter server's app settings.
                    properties:
                      defaultResourceSpec:
                        description: SageMakerUserProfileResourceSpec is a nested
                          property type of AWS::SageMaker::UserProfile.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with JupyterServer apps.
                        items:
                          type: string
                        type: array
                    type: object
                  kernelGatewayAppSettings:
                    description: The kernel gateway app settings.
                    properties:
                      customImages:
                        description: A list of custom SageMaker images that are configured
                          to run as a KernelGateway app.
                        items:
                          description: SageMakerUserProfileCustomImage is a nested
                            property type of AWS::SageMaker::UserProfile.
                          properties:
                            appImageConfigName:
                              description: The Name of the AppImageConfig.
                              maxLength: 63
                              type: string
                            imageName:
                              description: The name of the CustomImage. Must be unique
                                to your account.
                              maxLength: 63
                              type: string
                            imageVersionNumber:
                              description: The version number of the CustomImage.
                              format: int64
                              minimum: 0
                              type: integer
                          required:
                          - appImageConfigName
                          - imageName
                          type: object
                        type: array
                      defaultResourceSpec:
                        description: |-
                          The default instance type and the Amazon Resource Name (ARN) of the default SageMaker image used by the
                          KernelGateway app.
                        properties:
                          instanceType:
                            type: string
                          lifecycleConfigArn:
                            description: The Amazon Resource Name (ARN) of the Lifecycle
                              Configuration to attach to the Resource.
                            maxLength: 256
                            type: string
                          sageMakerImageArn:
                            description: The ARN of the SageMaker image that the image
                              version belongs to.
                            maxLength: 256
                            type: string
                          sageMakerImageVersionArn:
                            description: The ARN of the image version created on the
                              instance.
                            maxLength: 256
                            type: string
                          trainingPlanArn:
                            description: The Amazon Resource Name (ARN) of the training
                              plan to use for the ResourceSpec.
                            maxLength: 2048
                            type: string
                        type: object
                      lifecycleConfigArns:
                        description: A list of LifecycleConfigArns available for use
                          with KernelGateway apps.
                        items:
                          type: string
                        type: array
                    type: object
                  rStudioServerProAppSettings:
                    description: SageMakerUserProfileRStudioServerProAppSettings is
                      a nested property type of AWS::SageMaker::UserProfile.
                    properties:
                      accessStatus:
                        description: Indicates whether the current user has access
                          to the RStudioServerPro app.
                        enum:
                        - ENABLED
                        - DISABLED
                        type: string
                      userGroup:
                        description: |-
                          The level of permissions that the user has within the RStudioServerPro app. This value defaults to User. The
                          Admin value allows the user access to the RStudio Administrative Dashboard.
                        enum:
                        - R_STUDIO_ADMIN
                        - R_STUDIO_USER
                        type: string
                    type: object
                  securityGroups:
                    description: The security groups for the Amazon Virtual Private
                      Cloud (VPC) that Studio uses for communication.
                    items:
                      type: string
                    type: array
                  sharingSettings:
                    description: The sharing settings.
                    properties:
                      notebookOutputOption:
                        description: Whether to include the notebook cell output when
                          sharing the notebook. The default is Disabled.
                        enum:
                        - Allowed
                        - Disabled
                        type: string
                      s3KmsKeyId:
                        description: |-
                          When NotebookOutputOption is Allowed, the AWS Key Management Service (KMS) encryption key ID used to encrypt
                          the notebook cell output in the Amazon S3 bucket.
                        maxLength: 2048
                        type: string
                      s3OutputPath:
                        description: When NotebookOutputOption is Allowed, the Amazon
                          S3 bucket used to store the shared notebook snapshots.
                        maxLength: 1024
                        type: string
                    type: object
                  spaceStorageSettings:
                    description: SageMakerUserProfileDefaultSpaceStorageSettings is
                      a nested property type of AWS::SageMaker::UserProfile.
                    properties:
                      defaultEbsStorageSettings:
                        description: SageMakerUserProfileDefaultEbsStorageSettings
                          is a nested property type of AWS::SageMaker::UserProfile.
                        properties:
                          defaultEbsVolumeSizeInGb:
                            description: Default size of the Amazon EBS volume in
                              Gb
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                          maximumEbsVolumeSizeInGb:
                            description: Maximum size of the Amazon EBS volume in
                              Gb. Must be greater than or equal to the DefaultEbsVolumeSizeInGb.
                            format: int64
                            maximum: 16384
                            minimum: 5
                            type: integer
                        required:
                        - defaultEbsVolumeSizeInGb
                        - maximumEbsVolumeSizeInGb
                        type: object
                    type: object
                  studioWebPortal:
                    description: Indicates whether the Studio experience is available
                      to users. If not, users cannot access Studio.
                    enum:
                    - ENABLED
                    - DISABLED
                    type: string
                  studioWebPortalSettings:
                    description: SageMakerUserProfileStudioWebPortalSettings is a
                      nested property type of AWS::SageMaker::UserProfile.
                    properties:
                      hiddenAppTypes:
                        description: Applications supported in Studio that are hidden
                          from the Studio left navigation pane.
                        items:
                          type: string
                        type: array
                      hiddenInstanceTypes:
                        description: The instance types you are hiding from the Studio
                          user interface.
                        items:
                          type: string
                        type: array
                      hiddenMlTools:
                        description: The machine learning tools that are hidden from
                          the Studio left navigation pane.
                        items:
                          type: string
                        type: array
                      hiddenSageMakerImageVersionAliases:
                        description: The version aliases you are hiding from the Studio
                          user interface.
                        items:
                          description: SageMakerUserProfileHiddenSageMakerImage is
                            a nested property type of AWS::SageMaker::UserProfile.
                          properties:
                            sageMakerImageName:
                              description: The SageMaker image name that you are hiding
                                from the Studio user interface.
                              enum:
                              - sagemaker_distribution
                              type: string
                            versionAliases:
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                type: object
            required:
            - domainId
            - userProfileName
            type: object
          status:
            description: SageMakerUserProfileStatus is the observed state of AWS::SageMaker::UserProfile.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              userProfileArn:
                description: |-
                  The user profile Amazon Resource Name (ARN).
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sagemakerworkforces.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SageMakerWorkforce
    listKind: SageMakerWorkforceList
    plural: sagemakerworkforces
    singular: sagemakerworkforce
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          SageMakerWorkforce manages AWS::SageMaker::Workforce through the AWS Cloud Control API. Resource Type
          definition for AWS::SageMaker::Workforce. Use to create a private workforce that you can use to label your
          training data using Amazon SageMaker Ground Truth.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SageMakerWorkforceSpec is the desired state of AWS::SageMaker::Workforce.
            properties:
              cognitoConfig:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  clientId:
                    description: The client ID for your Amazon Cognito user pool.
                    type: string
                  userPool:
                    description: The ID for your Amazon Cognito user pool.
                    type: string
                required:
                - clientId
                - userPool
                type: object
              ipAddressType:
                description: The IP address type for the workforce. IPv4 only or dualstack
                  (IPv4 and IPv6).
                enum:
                - ipv4
                - dualstack
                type: string
              oidcConfig:
                description: SageMakerWorkforceOidcConfig is a nested property type
                  of AWS::SageMaker::Workforce.
                properties:
                  authenticationRequestExtraParams:
                    additionalProperties:
                      type: string
                    description: A string to string map of identifiers specific to
                      the custom identity provider (IdP) being used.
                    type: object
                  authorizationEndpoint:
                    description: The OIDC IdP authorization endpoint used to configure
                      your private workforce.
                    maxLength: 500
                    type: string
                  clientId:
                    description: The OIDC IdP client ID used to configure your private
                      workforce.
                    maxLength: 1024
                    type: string
                  clientSecret:
                    description: The OIDC IdP client secret used to configure your
                      private workforce.
                    maxLength: 1024
                    type: string
                  issuer:
                    description: The OIDC IdP issuer used to configure your private
                      workforce.
                    maxLength: 500
                    type: string
                  jwksUri:
                    description: The OIDC IdP JSON Web Key Set (Jwks) URI used to
                      configure your private workforce.
                    maxLength: 500
                    type: string
                  logoutEndpoint:
                    description: The OIDC IdP logout endpoint used to configure your
                      private workforce.
                    maxLength: 500
                    type: string
                  scope:
                    description: |-
                      An array of string identifiers used to refer to the specific pieces of user data or claims that the client
                      application wants to access.
                    maxLength: 500
                    type: string
                  tokenEndpoint:
                    description: The OIDC IdP token endpoint used to configure your
                      private workforce.
                    maxLength: 500
                    type: string
                  userInfoEndpoint:
                    description: The OIDC IdP user info endpoint used to configure
                      your private workforce.
                    maxLength: 500
                    type: string
                required:
                - authorizationEndpoint
                - clientId
                - issuer
                - jwksUri
                - logoutEndpoint
                - tokenEndpoint
                - userInfoEndpoint
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              sourceIpConfig:
                description: SageMakerWorkforceSourceIpConfig is a nested property
                  type of AWS::SageMaker::Workforce.
                properties:
                  cidrs:
                    description: A list of one to ten Classless Inter-Domain Routing
                      (CIDR) values.
                    items:
                      type: string
                    type: array
                required:
                - cidrs
                type: object
              tags:
                description: An array of key-value pairs.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              workforceName:
                description: |-
                  The name of the private workforce.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 63
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: workforceName is immutable
                  rule: self == oldSelf
              workforceVpcConfig:
                description: SageMakerWorkforceWorkforceVpcConfigRequest is a nested
                  property type of AWS::SageMaker::Workforce.
                properties:
                  securityGroupIds:
                    description: The VPC security group IDs.
                    items:
                      type: string
                    type: array
                  subnets:
                    description: The VPC subnets.
                    items:
                      type: string
                    type: array
                  vpcId:
                    description: The ID of the VPC.
                    type: string
                type: object
            required:
            - workforceName
            type: object
          status:
            description: SageMakerWorkforceStatus is the observed state of AWS::SageMaker::Workforce.
            properties:
              attempt:
                description: |-
                  Attempt counts failed create/delete requests; it salts the Cloud Control
                  idempotency token so a retry is not served the cached failure.
                format: int32
                type: integer
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              subDomain:
                description: |-
                  The subdomain for your OIDC Identity Provider.
                  Read-only: reported by AWS after creation.
                type: string
              workforceArn:
                description: |-
                  The Amazon Resource Name (ARN) of the private workforce.
                  Read-only: reported by AWS after creation.
                maxLength: 256
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
