---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreapikeycredentialproviders.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreApiKeyCredentialProvider
    listKind: BedrockAgentCoreApiKeyCredentialProviderList
    plural: bedrockagentcoreapikeycredentialproviders
    singular: bedrockagentcoreapikeycredentialprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreApiKeyCredentialProvider manages AWS::BedrockAgentCore::ApiKeyCredentialProvider through the
          AWS Cloud Control API. Resource Type definition for AWS::BedrockAgentCore::ApiKeyCredentialProvider
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreApiKeyCredentialProviderSpec is the desired
              state of AWS::BedrockAgentCore::ApiKeyCredentialProvider.
            properties:
              apiKey:
                description: The API key to use for authentication
                maxLength: 65536
                type: string
              apiKeySecretConfig:
                description: Configuration for a customer-provided secret containing
                  the API key
                properties:
                  jsonKey:
                    description: The JSON key within the secret that contains the
                      credential value
                    maxLength: 128
                    minLength: 1
                    type: string
                  secretId:
                    description: The ID or ARN of the secret in AWS Secrets Manager
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - jsonKey
                - secretId
                type: object
              apiKeySecretSource:
                description: |-
                  The source of the API key secret. Use MANAGED for service-managed secrets or EXTERNAL for customer-provided
                  secrets.
                enum:
                - MANAGED
                - EXTERNAL
                type: string
              name:
                description: |-
                  The name of the API key credential provider
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Tags to assign to the API key credential provider
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            description: BedrockAgentCoreApiKeyCredentialProviderStatus is the observed
              state of AWS::BedrockAgentCore::ApiKeyCredentialProvider.
            properties:
              apiKeySecretArn:
                description: |-
                  The ARN of the API key secret in AWS Secrets Manager
                  Read-only: reported by AWS after creation.
                properties:
                  secretArn:
                    description: The ARN of the secret in AWS Secrets Manager
                    type: string
                required:
                - secretArn
                type: object
              apiKeySecretJsonKey:
                description: |-
                  The JSON key within the secret that contains the API key value
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdTime:
                description: |-
                  The timestamp when the credential provider was created
                  Read-only: reported by AWS after creation.
                type: string
              credentialProviderArn:
                description: |-
                  The Amazon Resource Name (ARN) of the API key credential provider
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedTime:
                description: |-
                  The timestamp when the credential provider was last updated
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorebrowsercustoms.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreBrowserCustom
    listKind: BedrockAgentCoreBrowserCustomList
    plural: bedrockagentcorebrowsercustoms
    singular: bedrockagentcorebrowsercustom
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreBrowserCustom manages AWS::BedrockAgentCore::BrowserCustom through the AWS Cloud Control API.
          Resource definition for AWS::BedrockAgentCore::BrowserCustom
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreBrowserCustomSpec is the desired state of
              AWS::BedrockAgentCore::BrowserCustom.
            properties:
              browserSigning:
                description: |-
                  Browser signing configuration.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  enabled:
                    type: boolean
                type: object
              certificates:
                description: |-
                  List of root CA certificates in PEM format.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: BedrockAgentCoreBrowserCustomCertificate is a nested
                    property type of AWS::BedrockAgentCore::BrowserCustom.
                  properties:
                    certificateLocation:
                      description: Certificate location in Secrets Manager.
                      properties:
                        secretArn:
                          description: Secrets Manager secret ARN.
                          type: string
                      required:
                      - secretArn
                      type: object
                  required:
                  - certificateLocation
                  type: object
                type: array
              description:
                description: |-
                  The description of the browser.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              enterprisePolicies:
                description: |-
                  A list of enterprise policy files for the browser.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: BedrockAgentCoreBrowserCustomBrowserEnterprisePolicy
                    is a nested property type of AWS::BedrockAgentCore::BrowserCustom.
                  properties:
                    location:
                      description: The S3 location of the enterprise policy file.
                      properties:
                        bucket:
                          type: string
                        prefix:
                          minLength: 1
                          type: string
                      required:
                      - bucket
                      - prefix
                      type: object
                    type:
                      description: The type of browser enterprise policy.
                      enum:
                      - MANAGED
                      - RECOMMENDED
                      type: string
                  required:
                  - location
                  - type
                  type: object
                type: array
              executionRoleArn:
                description: |-
                  The Amazon Resource Name (ARN) of the IAM role that the browser uses to access resources.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              name:
                description: |-
                  The name of the browser.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              networkConfiguration:
                description: |-
                  Network configuration for browser.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  networkMode:
                    enum:
                    - PUBLIC
                    - VPC
                    type: string
                  vpcConfig:
                    description: BedrockAgentCoreBrowserCustomVpcConfig is a nested
                      property type of AWS::BedrockAgentCore::BrowserCustom.
                    properties:
                      securityGroups:
                        items:
                          type: string
                        type: array
                      subnets:
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroups
                    - subnets
                    type: object
                required:
                - networkMode
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              recordingConfig:
                description: |-
                  Recording configuration for browser.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  enabled:
                    type: boolean
                  s3Location:
                    description: BedrockAgentCoreBrowserCustomS3Location is a nested
                      property type of AWS::BedrockAgentCore::BrowserCustom.
                    properties:
                      bucket:
                        type: string
                      prefix:
                        minLength: 1
                        type: string
                    required:
                    - bucket
                    - prefix
                    type: object
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - name
            - networkConfiguration
            type: object
          status:
            description: BedrockAgentCoreBrowserCustomStatus is the observed state
              of AWS::BedrockAgentCore::BrowserCustom.
            properties:
              browserArn:
                description: |-
                  The ARN of a Browser resource.
                  Read-only: reported by AWS after creation.
                type: string
              browserId:
                description: |-
                  The id of the browser.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Timestamp when the browser was created.
                  Read-only: reported by AWS after creation.
                type: string
              failureReason:
                description: |-
                  The reason for failure if the browser creation or operation failed.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  Timestamp when the browser was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  Status of browser.
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - READY
                - DELETING
                - DELETE_FAILED
                - DELETED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorebrowserprofiles.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreBrowserProfile
    listKind: BedrockAgentCoreBrowserProfileList
    plural: bedrockagentcorebrowserprofiles
    singular: bedrockagentcorebrowserprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreBrowserProfile manages AWS::BedrockAgentCore::BrowserProfile through the AWS Cloud Control
          API. Resource definition for AWS::BedrockAgentCore::BrowserProfile
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreBrowserProfileSpec is the desired state of
              AWS::BedrockAgentCore::BrowserProfile.
            properties:
              description:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              name:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: A map of tag keys and values.
                type: object
            required:
            - name
            type: object
          status:
            description: BedrockAgentCoreBrowserProfileStatus is the observed state
              of AWS::BedrockAgentCore::BrowserProfile.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSavedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSavedBrowserId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSavedBrowserSessionId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              profileArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              profileId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - READY
                - DELETING
                - DELETED
                - SAVING
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorecapacityproviders.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreCapacityProvider
    listKind: BedrockAgentCoreCapacityProviderList
    plural: bedrockagentcorecapacityproviders
    singular: bedrockagentcorecapacityprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreCapacityProvider manages AWS::BedrockAgentCore::CapacityProvider through the AWS Cloud Control
          API. Resource Type definition for AWS::BedrockAgentCore::CapacityProvider. A capacity provider defines the
          compute resources (EC2) used to run Amazon Bedrock AgentCore agent runtimes.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreCapacityProviderSpec is the desired state
              of AWS::BedrockAgentCore::CapacityProvider.
            properties:
              computeConfiguration:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  ec2Configuration:
                    description: BedrockAgentCoreCapacityProviderEc2Configuration
                      is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                    properties:
                      launchTemplateSource:
                        description: BedrockAgentCoreCapacityProviderLaunchTemplateSource
                          is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                        properties:
                          launchParameters:
                            description: BedrockAgentCoreCapacityProviderLaunchParameters
                              is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                            properties:
                              capacityReservationSpecification:
                                description: BedrockAgentCoreCapacityProviderCapacityReservationSpecification
                                  is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                                properties:
                                  capacityReservationPreference:
                                    description: Indicates the instance's Capacity
                                      Reservation preferences.
                                    enum:
                                    - capacity-reservations-only
                                    - open
                                    - none
                                    type: string
                                  capacityReservationTarget:
                                    description: BedrockAgentCoreCapacityProviderCapacityReservationTarget
                                      is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                                    properties:
                                      capacityReservationId:
                                        description: The ID of the Capacity Reservation
                                          in which to run the instance.
                                        maxLength: 255
                                        type: string
                                      capacityReservationResourceGroupArn:
                                        description: The ARN of the Capacity Reservation
                                          resource group in which to run the instance.
                                        maxLength: 2048
                                        type: string
                                    type: object
                                type: object
                              ephemeralVolumes:
                                description: The block device mapping for ephemeral
                                  (instance store) volumes.
                                items:
                                  description: BedrockAgentCoreCapacityProviderEphemeralBlockDeviceMapping
                                    is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                                  properties:
                                    deviceName:
                                      description: The device name (for example, /dev/sdh
                                        or xvdh).
                                      maxLength: 255
                                      type: string
                                    ebs:
                                      description: BedrockAgentCoreCapacityProviderEphemeralEBSVolumeConfiguration
                                        is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                                      properties:
                                        ebsCardIndex:
                                          description: The index of the EBS card.
                                            Applies to instances with multiple EBS
                                            cards.
                                          format: int64
                                          minimum: 0
                                          type: integer
                                        encrypted:
                                          description: Indicates whether the EBS volume
                                            is encrypted.
                                          type: boolean
                                        iops:
                                          description: The number of I/O operations
                                            per second (IOPS).
                                          format: int64
                                          maximum: 256000
                                          minimum: 100
                                          type: integer
                                        kmsKeyId:
                                          description: Identifier of the customer
                                            managed KMS key to use for EBS encryption.
                                          maxLength: 2048
                                          type: string
                                        snapshotId:
                                          description: The ID of the snapshot.
                                          maxLength: 64
                                          type: string
                                        throughput:
                                          description: The throughput to provision
                                            for a gp3 volume, in MiB/s.
                                          format: int64
                                          maximum: 2000
                                          minimum: 125
                                          type: integer
                                        volumeInitializationRate:
                                          description: |-
                                            The rate at which the volume is initialized after creation, in MiB/s. Supported only for volumes created from
                                            snapshots. If the snapshot is enabled for fast snapshot restore and a volume initialization rate is also
                                            specified, the volume is initialized at the specified rate instead of by fast snapshot restore. Valid range:
                                            100-300 MiB/s.
                                          format: int64
                                          maximum: 300
                                          minimum: 100
                                          type: integer
                                        volumeSize:
                                          description: The size of the volume, in
                                            GiBs.
                                          format: int64
                                          maximum: 65536
                                          minimum: 1
                                          type: integer
                                        volumeType:
                                          description: The volume type. Defaults to
                                            gp3 if not specified.
                                          enum:
                                          - standard
                                          - io1
                                          - io2
                                          - gp2
                                          - sc1
                                          - st1
                                          - gp3
                                          type: string
                                      type: object
                                    virtualName:
                                      description: The virtual device name (ephemeralN).
                                      maxLength: 255
                                      type: string
                                  type: object
                                type: array
                              instanceProfileArn:
                                description: The ARN of the IAM instance profile to
                                  associate with launched instances.
                                maxLength: 2048
                                type: string
                              instanceRequirements:
                                description: BedrockAgentCoreCapacityProviderInstanceRequirements
                                  is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                                properties:
                                  allowedInstanceTypes:
                                    description: List of allowed instance types.
                                    items:
                                      type: string
                                    type: array
                                required:
                                - allowedInstanceTypes
                                type: object
                              licenseSpecifications:
                                description: The license configurations.
                                items:
                                  description: BedrockAgentCoreCapacityProviderLicenseSpecification
                                    is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                                  properties:
                                    licenseConfigurationArn:
                                      description: The ARN of the license configuration.
                                      maxLength: 2048
                                      minLength: 1
                                      type: string
                                  required:
                                  - licenseConfigurationArn
                                  type: object
                                type: array
                              monitoring:
                                description: The monitoring level for the instance.
                                enum:
                                - BASIC
                                - DETAILED
                                type: string
                              operatingSystem:
                                description: The operating system and CPU architecture
                                  for the instances.
                                enum:
                                - LINUX_X86_64
                                - LINUX_ARM64
                                type: string
                              propagatedTags:
                                additionalProperties:
                                  type: string
                                description: |-
                                  Tags to apply to all EC2 resources (instances, volumes, and network interfaces) created by this capacity
                                  provider.
                                type: object
                              sshKeyName:
                                description: The name of the SSH key pair to configure
                                  on instances for SSH connectivity.
                                maxLength: 255
                                type: string
                            required:
                            - instanceRequirements
                            - operatingSystem
                            type: object
                        required:
                        - launchParameters
                        type: object
                      lifecycleConfiguration:
                        description: BedrockAgentCoreCapacityProviderInstanceLifecycleConfiguration
                          is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                        properties:
                          idleInstanceTimeout:
                            description: The number of seconds an instance can remain
                              idle before it is stopped.
                            format: int64
                            maximum: 1209600
                            minimum: 60
                            type: integer
                          maxLifetime:
                            description: |-
                              Maximum lifetime for the instance in seconds. Once reached, instances will be automatically terminated
                              regardless of activity. Default: 28800 seconds (8 hours). Maximum: 1209600 seconds (14 days).
                            format: int64
                            maximum: 1209600
                            minimum: 60
                            type: integer
                        type: object
                      rootVolume:
                        description: BedrockAgentCoreCapacityProviderRootVolumeConfiguration
                          is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                        properties:
                          encrypted:
                            description: |-
                              Indicates whether the EBS volume is encrypted. Encrypted volumes can only be attached to instances that
                              support Amazon EBS encryption. If you are creating a volume from a snapshot, you can't specify an encryption
                              value.
                            type: boolean
                          freeSpaceGiB:
                            description: |-
                              The free space guaranteed on the root volume, in GiB. The service adds the operating system overhead on top of
                              this value. Defaults to 8 GiB. The maximum is below the 65,536 GiB gp3 ceiling because the service adds the
                              AMI size bucket on top of this value, and the resulting total must still be a provisionable gp3 volume.
                            format: int64
                            maximum: 65000
                            minimum: 2
                            type: integer
                          iops:
                            description: The number of IOPS to provision. Only valid
                              for gp3, io1, and io2 volumes.
                            format: int64
                            maximum: 256000
                            minimum: 100
                            type: integer
                          kmsKeyId:
                            description: Identifier of the customer managed KMS key
                              to use for EBS encryption.
                            maxLength: 2048
                            type: string
                          throughput:
                            description: 'The throughput to provision for a gp3 volume,
                              in MiB/s. Valid range: 125-2000 MiB/s.'
                            format: int64
                            maximum: 2000
                            minimum: 125
                            type: integer
                          volumeType:
                            description: The EBS volume type. Defaults to gp3 if not
                              specified.
                            enum:
                            - standard
                            - io1
                            - io2
                            - gp2
                            - sc1
                            - st1
                            - gp3
                            type: string
                        type: object
                      volumes:
                        description: Named persistent EBS volumes for this capacity
                          provider.
                        items:
                          description: BedrockAgentCoreCapacityProviderVolumeConfiguration
                            is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                          properties:
                            ebsConfiguration:
                              description: BedrockAgentCoreCapacityProviderEbsVolumeConfiguration
                                is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                              properties:
                                encrypted:
                                  description: Whether to encrypt the volume. Defaults
                                    to true.
                                  type: boolean
                                iops:
                                  description: The number of IOPS to provision. Only
                                    valid for gp3, io1, and io2 volumes.
                                  format: int64
                                  maximum: 256000
                                  minimum: 100
                                  type: integer
                                kmsKeyId:
                                  description: Identifier of the KMS key to use for
                                    encryption.
                                  maxLength: 2048
                                  type: string
                                name:
                                  description: The logical name of the volume, used
                                    to reference it when mounting.
                                  maxLength: 48
                                  minLength: 1
                                  type: string
                                sizeGiB:
                                  description: The size of the volume in GiB.
                                  format: int64
                                  maximum: 65536
                                  minimum: 1
                                  type: integer
                                snapshotId:
                                  description: Optional EBS snapshot ID to initialize
                                    the volume from.
                                  maxLength: 64
                                  type: string
                                throughput:
                                  description: The throughput in MiB/s. Only valid
                                    for gp3 volumes.
                                  format: int64
                                  maximum: 2000
                                  minimum: 125
                                  type: integer
                                volumeType:
                                  description: The EBS volume type. Defaults to gp3
                                    if not specified.
                                  enum:
                                  - standard
                                  - io1
                                  - io2
                                  - gp2
                                  - sc1
                                  - st1
                                  - gp3
                                  type: string
                              required:
                              - name
                              - sizeGiB
                              type: object
                          required:
                          - ebsConfiguration
                          type: object
                        type: array
                      vpcConfiguration:
                        description: BedrockAgentCoreCapacityProviderVpcConfiguration
                          is a nested property type of AWS::BedrockAgentCore::CapacityProvider.
                        properties:
                          securityGroups:
                            description: The IDs of the security groups to associate
                              with the instances.
                            items:
                              type: string
                            type: array
                          subnets:
                            description: The IDs of the subnets in which to launch
                              instances.
                            items:
                              type: string
                            type: array
                        required:
                        - securityGroups
                        - subnets
                        type: object
                    required:
                    - launchTemplateSource
                    - vpcConfiguration
                    type: object
                required:
                - ec2Configuration
                type: object
              description:
                description: An optional description of the capacity provider.
                maxLength: 4096
                type: string
              name:
                description: |-
                  The name of the capacity provider.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 48
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              permissionsConfiguration:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  capacityProviderOperatorRoleArn:
                    description: The ARN of the IAM role that operators use to manage
                      the capacity provider.
                    maxLength: 2048
                    minLength: 1
                    type: string
                required:
                - capacityProviderOperatorRoleArn
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to the capacity
                  provider.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - computeConfiguration
            - name
            - permissionsConfiguration
            type: object
          status:
            description: BedrockAgentCoreCapacityProviderStatus is the observed state
              of AWS::BedrockAgentCore::CapacityProvider.
            properties:
              arn:
                description: |-
                  The ARN of the capacity provider.
                  Read-only: reported by AWS after creation.
                type: string
              capacityProviderId:
                description: |-
                  The unique identifier of the capacity provider.
                  Read-only: reported by AWS after creation.
                maxLength: 59
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the capacity provider was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  The timestamp when the capacity provider was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the capacity provider.
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - READY
                - DELETING
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorecodeinterpretercustoms.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreCodeInterpreterCustom
    listKind: BedrockAgentCoreCodeInterpreterCustomList
    plural: bedrockagentcorecodeinterpretercustoms
    singular: bedrockagentcorecodeinterpretercustom
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreCodeInterpreterCustom manages AWS::BedrockAgentCore::CodeInterpreterCustom through the AWS
          Cloud Control API. Resource definition for AWS::BedrockAgentCore::CodeInterpreterCustom
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreCodeInterpreterCustomSpec is the desired
              state of AWS::BedrockAgentCore::CodeInterpreterCustom.
            properties:
              certificates:
                description: |-
                  List of root CA certificates in PEM format.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  description: BedrockAgentCoreCodeInterpreterCustomCertificate is
                    a nested property type of AWS::BedrockAgentCore::CodeInterpreterCustom.
                  properties:
                    certificateLocation:
                      description: Certificate location in Secrets Manager.
                      properties:
                        secretArn:
                          description: Secrets Manager secret ARN.
                          type: string
                      required:
                      - secretArn
                      type: object
                  required:
                  - certificateLocation
                  type: object
                type: array
              description:
                description: |-
                  The description of the code interpreter.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              executionRoleArn:
                description: |-
                  The ARN of the IAM role that the code interpreter uses to access resources.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
              name:
                description: |-
                  The name of the code interpreter.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              networkConfiguration:
                description: |-
                  Network configuration for code interpreter.
                  Create-only in AWS: changing it requires replacing the resource.
                properties:
                  networkMode:
                    enum:
                    - PUBLIC
                    - SANDBOX
                    - VPC
                    type: string
                  vpcConfig:
                    description: BedrockAgentCoreCodeInterpreterCustomVpcConfig is
                      a nested property type of AWS::BedrockAgentCore::CodeInterpreterCustom.
                    properties:
                      securityGroups:
                        items:
                          type: string
                        type: array
                      subnets:
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroups
                    - subnets
                    type: object
                required:
                - networkMode
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - name
            - networkConfiguration
            type: object
          status:
            description: BedrockAgentCoreCodeInterpreterCustomStatus is the observed
              state of AWS::BedrockAgentCore::CodeInterpreterCustom.
            properties:
              codeInterpreterArn:
                description: |-
                  The ARN of a CodeInterpreter resource.
                  Read-only: reported by AWS after creation.
                type: string
              codeInterpreterId:
                description: |-
                  The id of the code interpreter.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Timestamp when the code interpreter was created.
                  Read-only: reported by AWS after creation.
                type: string
              failureReason:
                description: |-
                  The reason for failure if the code interpreter creation or operation failed.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  Timestamp when the code interpreter was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  Status of code interpreter.
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - READY
                - DELETING
                - DELETE_FAILED
                - DELETED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreconfigurationbundles.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreConfigurationBundle
    listKind: BedrockAgentCoreConfigurationBundleList
    plural: bedrockagentcoreconfigurationbundles
    singular: bedrockagentcoreconfigurationbundle
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreConfigurationBundle manages AWS::BedrockAgentCore::ConfigurationBundle through the AWS Cloud
          Control API. Definition of AWS::BedrockAgentCore::ConfigurationBundle Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreConfigurationBundleSpec is the desired state
              of AWS::BedrockAgentCore::ConfigurationBundle.
            properties:
              branchName:
                description: The branch name for version tracking.
                maxLength: 128
                type: string
              bundleName:
                description: |-
                  The name for the configuration bundle. Names must be unique within your account.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: bundleName is immutable
                  rule: self == oldSelf
              commitMessage:
                description: A commit message describing the version of the configuration
                  bundle.
                maxLength: 500
                type: string
              components:
                additionalProperties:
                  description: BedrockAgentCoreConfigurationBundleComponentConfiguration
                    is a nested property type of AWS::BedrockAgentCore::ConfigurationBundle.
                  properties:
                    configuration:
                      description: The configuration values as a flexible JSON document.
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - configuration
                  type: object
                description: A map of component identifiers to their configurations.
                type: object
              createdBy:
                description: BedrockAgentCoreConfigurationBundleVersionCreatedBySource
                  is a nested property type of AWS::BedrockAgentCore::ConfigurationBundle.
                properties:
                  arn:
                    description: The Amazon Resource Name (ARN) of the source, if
                      applicable.
                    type: string
                  name:
                    description: The name of the source (for example, user, optimization-job,
                      or system).
                    type: string
                required:
                - name
                type: object
              description:
                description: The description for the configuration bundle.
                maxLength: 500
                type: string
              kmsKeyArn:
                description: The ARN of the KMS key used to encrypt component configurations.
                maxLength: 2048
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Tags to assign to the configuration bundle.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - bundleName
            - components
            type: object
          status:
            description: BedrockAgentCoreConfigurationBundleStatus is the observed
              state of AWS::BedrockAgentCore::ConfigurationBundle.
            properties:
              bundleArn:
                description: |-
                  The Amazon Resource Name (ARN) of the configuration bundle.
                  Read-only: reported by AWS after creation.
                type: string
              bundleId:
                description: |-
                  The unique identifier of the configuration bundle.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the configuration bundle was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lineageMetadata:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  branchName:
                    description: The branch name for this version.
                    maxLength: 128
                    type: string
                  commitMessage:
                    description: A commit message describing the changes in this version.
                    maxLength: 500
                    type: string
                  createdBy:
                    description: BedrockAgentCoreConfigurationBundleVersionCreatedBySource
                      is a nested property type of AWS::BedrockAgentCore::ConfigurationBundle.
                    properties:
                      arn:
                        description: The Amazon Resource Name (ARN) of the source,
                          if applicable.
                        type: string
                      name:
                        description: The name of the source (for example, user, optimization-job,
                          or system).
                        type: string
                    required:
                    - name
                    type: object
                  parentVersionIds:
                    description: A list of parent version identifiers.
                    items:
                      type: string
                    type: array
                type: object
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              updatedAt:
                description: |-
                  The timestamp when the configuration bundle was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              versionId:
                description: |-
                  The version identifier of the configuration bundle.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoredatasets.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreDataset
    listKind: BedrockAgentCoreDatasetList
    plural: bedrockagentcoredatasets
    singular: bedrockagentcoredataset
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreDataset manages AWS::BedrockAgentCore::Dataset through the AWS Cloud Control API. Definition
          of AWS::BedrockAgentCore::Dataset Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreDatasetSpec is the desired state of AWS::BedrockAgentCore::Dataset.
            properties:
              datasetName:
                description: |-
                  Human-readable name for the dataset. Unique within the account (case-insensitive). Immutable after creation.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: datasetName is immutable
                  rule: self == oldSelf
              description:
                description: A description of the dataset.
                maxLength: 200
                type: string
              kmsKeyArn:
                description: |-
                  Optional AWS KMS key ARN for SSE-KMS on service S3 writes.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              schemaType:
                description: |-
                  Versioned schema type governing the structure of examples. Immutable after creation.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - AGENTCORE_EVALUATION_PREDEFINED_V1
                - AGENTCORE_EVALUATION_SIMULATED_V1
                type: string
                x-kubernetes-validations:
                - message: schemaType is immutable
                  rule: self == oldSelf
              source:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  inlineExamples:
                    description: BedrockAgentCoreDatasetInlineExamplesSource is a
                      nested property type of AWS::BedrockAgentCore::Dataset.
                    properties:
                      examples:
                        description: Examples to add. Each example is a free-form
                          JSON document validated against the declared schemaType.
                        items:
                          x-kubernetes-preserve-unknown-fields: true
                        type: array
                    required:
                    - examples
                    type: object
                  s3Source:
                    description: BedrockAgentCoreDatasetS3Source is a nested property
                      type of AWS::BedrockAgentCore::Dataset.
                    properties:
                      s3Uri:
                        description: S3 URI of the JSONL file (e.g. s3://my-bucket/path/to/examples.jsonl).
                        type: string
                    required:
                    - s3Uri
                    type: object
                type: object
              tags:
                description: A list of tags to assign to the dataset.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - datasetName
            - schemaType
            type: object
          status:
            description: BedrockAgentCoreDatasetStatus is the observed state of AWS::BedrockAgentCore::Dataset.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the dataset was created.
                  Read-only: reported by AWS after creation.
                type: string
              datasetArn:
                description: |-
                  The Amazon Resource Name (ARN) of the dataset.
                  Read-only: reported by AWS after creation.
                type: string
              datasetId:
                description: |-
                  The unique identifier of the dataset.
                  Read-only: reported by AWS after creation.
                type: string
              exampleCount:
                description: |-
                  The number of examples in the dataset DRAFT.
                  Read-only: reported by AWS after creation.
                format: int64
                type: integer
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the dataset.
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - UPDATING
                - DELETING
                - ACTIVE
                - CREATE_FAILED
                - UPDATE_FAILED
                - DELETE_FAILED
                type: string
              updatedAt:
                description: |-
                  The timestamp when the dataset was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreevaluators.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreEvaluator
    listKind: BedrockAgentCoreEvaluatorList
    plural: bedrockagentcoreevaluators
    singular: bedrockagentcoreevaluator
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreEvaluator manages AWS::BedrockAgentCore::Evaluator through the AWS Cloud Control API. Resource
          Type definition for AWS::BedrockAgentCore::Evaluator - Creates a custom evaluator for agent quality assessment
          using LLM-as-a-Judge configurations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreEvaluatorSpec is the desired state of AWS::BedrockAgentCore::Evaluator.
            properties:
              description:
                description: The description of the evaluator.
                maxLength: 200
                type: string
              evaluatorConfig:
                description: The configuration for the evaluator.
                properties:
                  codeBased:
                    description: BedrockAgentCoreEvaluatorCodeBasedEvaluatorConfig
                      is a nested property type of AWS::BedrockAgentCore::Evaluator.
                    properties:
                      lambdaConfig:
                        description: BedrockAgentCoreEvaluatorLambdaEvaluatorConfig
                          is a nested property type of AWS::BedrockAgentCore::Evaluator.
                        properties:
                          lambdaArn:
                            description: The ARN of the Lambda function used for evaluation.
                            type: string
                          lambdaTimeoutInSeconds:
                            description: The timeout in seconds for the Lambda function
                              invocation.
                            format: int64
                            maximum: 300
                            minimum: 1
                            type: integer
                        required:
                        - lambdaArn
                        type: object
                    required:
                    - lambdaConfig
                    type: object
                  llmAsAJudge:
                    description: BedrockAgentCoreEvaluatorLlmAsAJudgeEvaluatorConfig
                      is a nested property type of AWS::BedrockAgentCore::Evaluator.
                    properties:
                      instructions:
                        description: The evaluation instructions that guide the language
                          model in assessing agent performance.
                        type: string
                      modelConfig:
                        description: BedrockAgentCoreEvaluatorEvaluatorModelConfig
                          is a nested property type of AWS::BedrockAgentCore::Evaluator.
                        properties:
                          bedrockEvaluatorModelConfig:
                            description: BedrockAgentCoreEvaluatorBedrockEvaluatorModelConfig
                              is a nested property type of AWS::BedrockAgentCore::Evaluator.
                            properties:
                              additionalModelRequestFields:
                                description: Additional model-specific request fields.
                                x-kubernetes-preserve-unknown-fields: true
                              inferenceConfig:
                                description: BedrockAgentCoreEvaluatorInferenceConfiguration
                                  is a nested property type of AWS::BedrockAgentCore::Evaluator.
                                properties:
                                  maxTokens:
                                    description: The maximum number of tokens to generate
                                      in the model response.
                                    format: int64
                                    minimum: 1
                                    type: integer
                                  temperature:
                                    description: The temperature value that controls
                                      randomness in the model's responses.
                                    type: number
                                  topP:
                                    description: The top-p sampling parameter that
                                      controls the diversity of the model's responses.
                                    type: number
                                type: object
                              modelId:
                                description: The identifier of the Amazon Bedrock
                                  model to use for evaluation.
                                type: string
                            required:
                            - modelId
                            type: object
                          responsesEvaluatorModelConfig:
                            description: BedrockAgentCoreEvaluatorOpenResponsesEvaluatorModelConfig
                              is a nested property type of AWS::BedrockAgentCore::Evaluator.
                            properties:
                              maxOutputTokens:
                                description: The maximum number of output tokens to
                                  generate, including visible output and reasoning
                                  tokens.
                                format: int64
                                minimum: 1
                                type: integer
                              modelId:
                                description: The identifier of the model to use for
                                  evaluation.
                                type: string
                              reasoning:
                                description: BedrockAgentCoreEvaluatorReasoningConfiguration
                                  is a nested property type of AWS::BedrockAgentCore::Evaluator.
                                properties:
                                  effort:
                                    description: The level of reasoning effort the
                                      model applies.
                                    maxLength: 64
                                    type: string
                                type: object
                              temperature:
                                description: The sampling temperature between 0 and
                                  2.
                                type: number
                              topP:
                                description: The nucleus sampling probability mass
                                  between 0 and 1.
                                type: number
                            required:
                            - modelId
                            type: object
                        type: object
                      ratingScale:
                        description: BedrockAgentCoreEvaluatorRatingScale is a nested
                          property type of AWS::BedrockAgentCore::Evaluator.
                        properties:
                          categorical:
                            items:
                              description: BedrockAgentCoreEvaluatorCategoricalScaleDefinition
                                is a nested property type of AWS::BedrockAgentCore::Evaluator.
                              properties:
                                definition:
                                  description: The description that explains what
                                    this categorical rating represents.
                                  type: string
                                label:
                                  description: The label of this categorical rating
                                    option.
                                  maxLength: 100
                                  minLength: 1
                                  type: string
                              required:
                              - definition
                              - label
                              type: object
                            type: array
                          numerical:
                            items:
                              description: BedrockAgentCoreEvaluatorNumericalScaleDefinition
                                is a nested property type of AWS::BedrockAgentCore::Evaluator.
                              properties:
                                definition:
                                  description: The description that explains what
                                    this numerical rating represents.
                                  type: string
                                label:
                                  description: The label that describes this numerical
                                    rating option.
                                  maxLength: 100
                                  minLength: 1
                                  type: string
                                value:
                                  description: The numerical value for this rating
                                    scale option.
                                  type: number
                              required:
                              - definition
                              - label
                              - value
                              type: object
                            type: array
                        type: object
                    required:
                    - instructions
                    - modelConfig
                    - ratingScale
                    type: object
                type: object
              evaluatorName:
                description: |-
                  The name of the evaluator. Must be unique within your account.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: evaluatorName is immutable
                  rule: self == oldSelf
              kmsKeyArn:
                description: The ARN of the KMS key used to encrypt evaluator data.
                maxLength: 2048
                type: string
              level:
                description: The evaluation level that determines the scope of evaluation.
                enum:
                - TOOL_CALL
                - TRACE
                - SESSION
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: A list of tags to assign to the evaluator.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - evaluatorConfig
            - evaluatorName
            - level
            type: object
          status:
            description: BedrockAgentCoreEvaluatorStatus is the observed state of
              AWS::BedrockAgentCore::Evaluator.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the evaluator was created.
                  Read-only: reported by AWS after creation.
                type: string
              evaluatorArn:
                description: |-
                  The Amazon Resource Name (ARN) of the evaluator.
                  Read-only: reported by AWS after creation.
                type: string
              evaluatorId:
                description: |-
                  The unique identifier of the evaluator.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the evaluator.
                  Read-only: reported by AWS after creation.
                enum:
                - ACTIVE
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - DELETING
                type: string
              updatedAt:
                description: |-
                  The timestamp when the evaluator was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoregatewayratelimits.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreGatewayRateLimit
    listKind: BedrockAgentCoreGatewayRateLimitList
    plural: bedrockagentcoregatewayratelimits
    singular: bedrockagentcoregatewayratelimit
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreGatewayRateLimit manages AWS::BedrockAgentCore::GatewayRateLimit through the AWS Cloud Control
          API. Definition of AWS::BedrockAgentCore::GatewayRateLimit Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreGatewayRateLimitSpec is the desired state
              of AWS::BedrockAgentCore::GatewayRateLimit.
            properties:
              description:
                description: Optional human-readable description for this limit.
                maxLength: 512
                type: string
              dimensionKeys:
                description: |-
                  Ordered list of dimension names defining the scope of this limit. Unique per gateway — no two limits can share
                  the same dimensionKeys.
                  Create-only in AWS: changing it requires replacing the resource.
                items:
                  type: string
                type: array
              entries:
                description: Rule entries mapping dimension values to rate configurations.
                items:
                  description: BedrockAgentCoreGatewayRateLimitLimitEntry is a nested
                    property type of AWS::BedrockAgentCore::GatewayRateLimit.
                  properties:
                    connections:
                      description: Connection rate limits (per second only). Limited
                        to 1 entry for now. — P2
                      items:
                        description: BedrockAgentCoreGatewayRateLimitRateConfig is
                          a nested property type of AWS::BedrockAgentCore::GatewayRateLimit.
                        properties:
                          period:
                            enum:
                            - second
                            - minute
                            type: string
                          rate:
                            type: number
                        required:
                        - period
                        - rate
                        type: object
                      type: array
                    dimensions:
                      additionalProperties:
                        type: string
                      type: object
                    requests:
                      description: Request rate limits (RPS or RPM). Limited to 1
                        entry for now.
                      items:
                        description: BedrockAgentCoreGatewayRateLimitRateConfig is
                          a nested property type of AWS::BedrockAgentCore::GatewayRateLimit.
                        properties:
                          period:
                            enum:
                            - second
                            - minute
                            type: string
                          rate:
                            type: number
                        required:
                        - period
                        - rate
                        type: object
                      type: array
                    tokens:
                      description: Token rate limits (TPM). Limited to 1 entry for
                        now. — P1
                      items:
                        description: BedrockAgentCoreGatewayRateLimitRateConfig is
                          a nested property type of AWS::BedrockAgentCore::GatewayRateLimit.
                        properties:
                          period:
                            enum:
                            - second
                            - minute
                            type: string
                          rate:
                            type: number
                        required:
                        - period
                        - rate
                        type: object
                      type: array
                  required:
                  - dimensions
                  type: object
                type: array
              gatewayIdentifier:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              rateLimitId:
                description: |-
                  Limit identifier. Optional on Create (system-generates if not provided by customer). Always present in
                  responses.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 64
                type: string
            required:
            - dimensionKeys
            - entries
            type: object
          status:
            description: BedrockAgentCoreGatewayRateLimitStatus is the observed state
              of AWS::BedrockAgentCore::GatewayRateLimit.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - ACTIVE
                - UPDATING
                - DELETING
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoregatewayrules.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreGatewayRule
    listKind: BedrockAgentCoreGatewayRuleList
    plural: bedrockagentcoregatewayrules
    singular: bedrockagentcoregatewayrule
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreGatewayRule manages AWS::BedrockAgentCore::GatewayRule through the AWS Cloud Control API.
          Resource Type definition for AWS::BedrockAgentCore::GatewayRule
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreGatewayRuleSpec is the desired state of AWS::BedrockAgentCore::GatewayRule.
            properties:
              actions:
                items:
                  x-kubernetes-preserve-unknown-fields: true
                type: array
              conditions:
                items:
                  x-kubernetes-preserve-unknown-fields: true
                type: array
              description:
                maxLength: 256
                type: string
              gatewayIdentifier:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              priority:
                type: number
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
            required:
            - actions
            - priority
            type: object
          status:
            description: BedrockAgentCoreGatewayRuleStatus is the observed state of
              AWS::BedrockAgentCore::GatewayRule.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              gatewayArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              ruleId:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 36
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - ACTIVE
                - UPDATING
                - DELETING
                type: string
              system:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  managedBy:
                    type: string
                required:
                - managedBy
                type: object
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoregateways.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreGateway
    listKind: BedrockAgentCoreGatewayList
    plural: bedrockagentcoregateways
    singular: bedrockagentcoregateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreGateway manages AWS::BedrockAgentCore::Gateway through the AWS Cloud Control API. Definition
          of AWS::BedrockAgentCore::Gateway Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreGatewaySpec is the desired state of AWS::BedrockAgentCore::Gateway.
            properties:
              authorizerConfiguration:
                x-kubernetes-preserve-unknown-fields: true
              authorizerType:
                enum:
                - CUSTOM_JWT
                - AWS_IAM
                - NONE
                - AUTHENTICATE_ONLY
                type: string
              description:
                maxLength: 200
                type: string
              exceptionLevel:
                enum:
                - DEBUG
                type: string
              interceptorConfigurations:
                items:
                  description: BedrockAgentCoreGatewayGatewayInterceptorConfiguration
                    is a nested property type of AWS::BedrockAgentCore::Gateway.
                  properties:
                    inputConfiguration:
                      description: BedrockAgentCoreGatewayInterceptorInputConfiguration
                        is a nested property type of AWS::BedrockAgentCore::Gateway.
                      properties:
                        passRequestHeaders:
                          type: boolean
                        payloadFilter:
                          description: BedrockAgentCoreGatewayInterceptorPayloadFilter
                            is a nested property type of AWS::BedrockAgentCore::Gateway.
                          properties:
                            exclude:
                              items:
                                x-kubernetes-preserve-unknown-fields: true
                              type: array
                          required:
                          - exclude
                          type: object
                      required:
                      - passRequestHeaders
                      type: object
                    interceptionPoints:
                      items:
                        type: string
                      type: array
                    interceptor:
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - interceptionPoints
                  - interceptor
                  type: object
                type: array
              kmsKeyArn:
                maxLength: 2048
                type: string
              name:
                type: string
              policyEngineConfiguration:
                description: BedrockAgentCoreGatewayGatewayPolicyEngineConfiguration
                  is a nested property type of AWS::BedrockAgentCore::Gateway.
                properties:
                  arn:
                    maxLength: 170
                    minLength: 1
                    type: string
                  mode:
                    enum:
                    - LOG_ONLY
                    - ENFORCE
                    type: string
                required:
                - arn
                - mode
                type: object
              protocolConfiguration:
                x-kubernetes-preserve-unknown-fields: true
              protocolType:
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                maxLength: 2048
                minLength: 1
                type: string
              tags:
                additionalProperties:
                  type: string
                type: object
              wafConfiguration:
                description: BedrockAgentCoreGatewayWafConfiguration is a nested property
                  type of AWS::BedrockAgentCore::Gateway.
                properties:
                  failureMode:
                    enum:
                    - FAIL_CLOSE
                    - FAIL_OPEN
                    type: string
                type: object
            required:
            - authorizerType
            - name
            - roleArn
            type: object
          status:
            description: BedrockAgentCoreGatewayStatus is the observed state of AWS::BedrockAgentCore::Gateway.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              gatewayArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              gatewayIdentifier:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              gatewayUrl:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 1024
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - UPDATING
                - UPDATE_UNSUCCESSFUL
                - DELETING
                - READY
                - FAILED
                type: string
              statusReasons:
                description: 'Read-only: reported by AWS after creation.'
                items:
                  type: string
                type: array
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              webAclArn:
                description: 'Read-only: reported by AWS after creation.'
                maxLength: 2048
                type: string
              workloadIdentityDetails:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  workloadIdentityArn:
                    maxLength: 1024
                    minLength: 1
                    type: string
                required:
                - workloadIdentityArn
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoregatewaytargets.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreGatewayTarget
    listKind: BedrockAgentCoreGatewayTargetList
    plural: bedrockagentcoregatewaytargets
    singular: bedrockagentcoregatewaytarget
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreGatewayTarget manages AWS::BedrockAgentCore::GatewayTarget through the AWS Cloud Control API.
          Definition of AWS::BedrockAgentCore::GatewayTarget Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreGatewayTargetSpec is the desired state of
              AWS::BedrockAgentCore::GatewayTarget.
            properties:
              credentialProviderConfigurations:
                items:
                  description: BedrockAgentCoreGatewayTargetCredentialProviderConfiguration
                    is a nested property type of AWS::BedrockAgentCore::GatewayTarget.
                  properties:
                    credentialProvider:
                      x-kubernetes-preserve-unknown-fields: true
                    credentialProviderType:
                      enum:
                      - GATEWAY_IAM_ROLE
                      - OAUTH
                      - API_KEY
                      - CALLER_IAM_CREDENTIALS
                      - JWT_PASSTHROUGH
                      type: string
                  required:
                  - credentialProviderType
                  type: object
                type: array
              description:
                maxLength: 200
                type: string
              gatewayIdentifier:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              metadataConfiguration:
                description: BedrockAgentCoreGatewayTargetMetadataConfiguration is
                  a nested property type of AWS::BedrockAgentCore::GatewayTarget.
                properties:
                  allowedQueryParameters:
                    items:
                      type: string
                    type: array
                  allowedRequestHeaders:
                    items:
                      type: string
                    type: array
                  allowedResponseHeaders:
                    items:
                      type: string
                    type: array
                type: object
              name:
                type: string
              privateEndpoint:
                x-kubernetes-preserve-unknown-fields: true
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              targetConfiguration:
                x-kubernetes-preserve-unknown-fields: true
            required:
            - targetConfiguration
            type: object
          status:
            description: BedrockAgentCoreGatewayTargetStatus is the observed state
              of AWS::BedrockAgentCore::GatewayTarget.
            properties:
              authorizationData:
                description: 'Read-only: reported by AWS after creation.'
                x-kubernetes-preserve-unknown-fields: true
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              gatewayArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastSynchronizedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              privateEndpointManagedResources:
                description: 'Read-only: reported by AWS after creation.'
                items:
                  description: BedrockAgentCoreGatewayTargetManagedResourceDetails
                    is a nested property type of AWS::BedrockAgentCore::GatewayTarget.
                  properties:
                    domain:
                      type: string
                    resourceAssociationArn:
                      type: string
                    resourceGatewayArn:
                      type: string
                  type: object
                type: array
              protocolType:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - MCP
                - HTTP
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - UPDATING
                - UPDATE_UNSUCCESSFUL
                - DELETING
                - READY
                - FAILED
                - SYNCHRONIZING
                - SYNCHRONIZE_UNSUCCESSFUL
                - CREATE_PENDING_AUTH
                - UPDATE_PENDING_AUTH
                - SYNCHRONIZE_PENDING_AUTH
                type: string
              statusReasons:
                description: 'Read-only: reported by AWS after creation.'
                items:
                  type: string
                type: array
              targetId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreharnessendpoints.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreHarnessEndpoint
    listKind: BedrockAgentCoreHarnessEndpointList
    plural: bedrockagentcoreharnessendpoints
    singular: bedrockagentcoreharnessendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreHarnessEndpoint manages AWS::BedrockAgentCore::HarnessEndpoint through the AWS Cloud Control
          API. Resource Type definition for AWS::BedrockAgentCore::HarnessEndpoint - a named, stable reference to a
          specific version of a Harness that callers invoke, allowing the underlying version to be updated without
          changing how the agent is invoked.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreHarnessEndpointSpec is the desired state
              of AWS::BedrockAgentCore::HarnessEndpoint.
            properties:
              description:
                description: The description of the endpoint.
                maxLength: 256
                type: string
              endpointName:
                description: |-
                  The name of the endpoint. Must start with a letter and contain only alphanumeric characters and underscores.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: endpointName is immutable
                  rule: self == oldSelf
              harnessId:
                description: |-
                  The ID of the harness that the endpoint belongs to.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: harnessId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Tags to apply to the harness endpoint resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              targetVersion:
                description: The harness version that the endpoint points to and serves
                  invocations from.
                maxLength: 5
                type: string
            required:
            - endpointName
            - harnessId
            type: object
          status:
            description: BedrockAgentCoreHarnessEndpointStatus is the observed state
              of AWS::BedrockAgentCore::HarnessEndpoint.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the endpoint.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the endpoint was created.
                  Read-only: reported by AWS after creation.
                type: string
              harnessName:
                description: |-
                  The name of the harness that the endpoint belongs to.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              liveVersion:
                description: |-
                  The harness version that the endpoint is currently serving.
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - READY
                - DELETING
                - DELETE_FAILED
                type: string
              updatedAt:
                description: |-
                  The timestamp when the endpoint was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreharnesses.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreHarness
    listKind: BedrockAgentCoreHarnessList
    plural: bedrockagentcoreharnesses
    singular: bedrockagentcoreharness
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreHarness manages AWS::BedrockAgentCore::Harness through the AWS Cloud Control API. Resource
          Type definition for AWS::BedrockAgentCore::Harness - a managed agentic loop service that provides a turnkey
          solution for running stateful, tool-equipped AI agents.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreHarnessSpec is the desired state of AWS::BedrockAgentCore::Harness.
            properties:
              allowedTools:
                description: The tools that the agent is allowed to use.
                items:
                  type: string
                type: array
              authorizerConfiguration:
                description: The inbound authorization configuration for authenticating
                  incoming requests.
                properties:
                  customJWTAuthorizer:
                    description: BedrockAgentCoreHarnessCustomJWTAuthorizerConfiguration
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      allowedAudience:
                        items:
                          type: string
                        type: array
                      allowedClients:
                        items:
                          type: string
                        type: array
                      allowedScopes:
                        items:
                          type: string
                        type: array
                      customClaims:
                        items:
                          description: BedrockAgentCoreHarnessCustomClaimValidationType
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            authorizingClaimMatchValue:
                              description: BedrockAgentCoreHarnessAuthorizingClaimMatchValueType
                                is a nested property type of AWS::BedrockAgentCore::Harness.
                              properties:
                                claimMatchOperator:
                                  enum:
                                  - EQUALS
                                  - CONTAINS
                                  - CONTAINS_ANY
                                  type: string
                                claimMatchValue:
                                  description: BedrockAgentCoreHarnessClaimMatchValueType
                                    is a nested property type of AWS::BedrockAgentCore::Harness.
                                  properties:
                                    matchValueString:
                                      maxLength: 255
                                      type: string
                                    matchValueStringList:
                                      items:
                                        type: string
                                      type: array
                                  type: object
                              required:
                              - claimMatchOperator
                              - claimMatchValue
                              type: object
                            inboundTokenClaimName:
                              maxLength: 255
                              minLength: 1
                              type: string
                            inboundTokenClaimValueType:
                              enum:
                              - STRING
                              - STRING_ARRAY
                              type: string
                          required:
                          - authorizingClaimMatchValue
                          - inboundTokenClaimName
                          - inboundTokenClaimValueType
                          type: object
                        type: array
                      discoveryUrl:
                        type: string
                      privateEndpoint:
                        description: BedrockAgentCoreHarnessPrivateEndpoint is a nested
                          property type of AWS::BedrockAgentCore::Harness.
                        properties:
                          managedVpcResource:
                            description: BedrockAgentCoreHarnessManagedVpcResource
                              is a nested property type of AWS::BedrockAgentCore::Harness.
                            properties:
                              endpointIpAddressType:
                                enum:
                                - IPV4
                                - IPV6
                                type: string
                              routingDomain:
                                maxLength: 255
                                type: string
                              securityGroupIds:
                                items:
                                  type: string
                                type: array
                              subnetIds:
                                items:
                                  type: string
                                type: array
                              tags:
                                additionalProperties:
                                  type: string
                                type: object
                              vpcIdentifier:
                                type: string
                            required:
                            - endpointIpAddressType
                            - subnetIds
                            - vpcIdentifier
                            type: object
                          selfManagedLatticeResource:
                            description: BedrockAgentCoreHarnessSelfManagedLatticeResource
                              is a nested property type of AWS::BedrockAgentCore::Harness.
                            properties:
                              resourceConfigurationIdentifier:
                                maxLength: 2048
                                minLength: 20
                                type: string
                            required:
                            - resourceConfigurationIdentifier
                            type: object
                        type: object
                      privateEndpointOverrides:
                        items:
                          description: BedrockAgentCoreHarnessPrivateEndpointOverride
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            domain:
                              maxLength: 253
                              minLength: 1
                              type: string
                            privateEndpoint:
                              description: BedrockAgentCoreHarnessPrivateEndpoint
                                is a nested property type of AWS::BedrockAgentCore::Harness.
                              properties:
                                managedVpcResource:
                                  description: BedrockAgentCoreHarnessManagedVpcResource
                                    is a nested property type of AWS::BedrockAgentCore::Harness.
                                  properties:
                                    endpointIpAddressType:
                                      enum:
                                      - IPV4
                                      - IPV6
                                      type: string
                                    routingDomain:
                                      maxLength: 255
                                      type: string
                                    securityGroupIds:
                                      items:
                                        type: string
                                      type: array
                                    subnetIds:
                                      items:
                                        type: string
                                      type: array
                                    tags:
                                      additionalProperties:
                                        type: string
                                      type: object
                                    vpcIdentifier:
                                      type: string
                                  required:
                                  - endpointIpAddressType
                                  - subnetIds
                                  - vpcIdentifier
                                  type: object
                                selfManagedLatticeResource:
                                  description: BedrockAgentCoreHarnessSelfManagedLatticeResource
                                    is a nested property type of AWS::BedrockAgentCore::Harness.
                                  properties:
                                    resourceConfigurationIdentifier:
                                      maxLength: 2048
                                      minLength: 20
                                      type: string
                                  required:
                                  - resourceConfigurationIdentifier
                                  type: object
                              type: object
                          required:
                          - domain
                          - privateEndpoint
                          type: object
                        type: array
                    required:
                    - discoveryUrl
                    type: object
                type: object
              environment:
                description: The compute environment configuration for the harness,
                  including underlying runtime information.
                properties:
                  agentCoreRuntimeEnvironment:
                    description: BedrockAgentCoreHarnessHarnessAgentCoreRuntimeEnvironment
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      agentRuntimeArn:
                        description: The ARN of the underlying AgentCore Runtime.
                        maxLength: 1011
                        type: string
                      agentRuntimeId:
                        description: The ID of the underlying AgentCore Runtime.
                        type: string
                      agentRuntimeName:
                        description: The name of the underlying AgentCore Runtime.
                        type: string
                      filesystemConfigurations:
                        items:
                          description: BedrockAgentCoreHarnessFilesystemConfiguration
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            efsAccessPoint:
                              description: BedrockAgentCoreHarnessEfsAccessPointConfiguration
                                is a nested property type of AWS::BedrockAgentCore::Harness.
                              properties:
                                accessPointArn:
                                  maxLength: 128
                                  type: string
                                mountPath:
                                  maxLength: 200
                                  minLength: 6
                                  type: string
                              required:
                              - accessPointArn
                              - mountPath
                              type: object
                            s3FilesAccessPoint:
                              description: BedrockAgentCoreHarnessS3FilesAccessPointConfiguration
                                is a nested property type of AWS::BedrockAgentCore::Harness.
                              properties:
                                accessPointArn:
                                  maxLength: 256
                                  type: string
                                mountPath:
                                  maxLength: 200
                                  minLength: 6
                                  type: string
                              required:
                              - accessPointArn
                              - mountPath
                              type: object
                            sessionStorage:
                              description: BedrockAgentCoreHarnessSessionStorageConfiguration
                                is a nested property type of AWS::BedrockAgentCore::Harness.
                              properties:
                                mountPath:
                                  maxLength: 200
                                  minLength: 6
                                  type: string
                              required:
                              - mountPath
                              type: object
                          type: object
                        type: array
                      lifecycleConfiguration:
                        description: BedrockAgentCoreHarnessLifecycleConfiguration
                          is a nested property type of AWS::BedrockAgentCore::Harness.
                        properties:
                          idleRuntimeSessionTimeout:
                            format: int64
                            maximum: 28800
                            minimum: 60
                            type: integer
                          maxLifetime:
                            format: int64
                            maximum: 28800
                            minimum: 60
                            type: integer
                        type: object
                      networkConfiguration:
                        description: BedrockAgentCoreHarnessNetworkConfiguration is
                          a nested property type of AWS::BedrockAgentCore::Harness.
                        properties:
                          networkMode:
                            enum:
                            - PUBLIC
                            - VPC
                            type: string
                          networkModeConfig:
                            description: BedrockAgentCoreHarnessVpcConfig is a nested
                              property type of AWS::BedrockAgentCore::Harness.
                            properties:
                              securityGroups:
                                items:
                                  type: string
                                type: array
                              subnets:
                                items:
                                  type: string
                                type: array
                            required:
                            - securityGroups
                            - subnets
                            type: object
                        required:
                        - networkMode
                        type: object
                    type: object
                type: object
              environmentArtifact:
                description: The environment artifact for the harness, such as a custom
                  container image.
                properties:
                  containerConfiguration:
                    description: BedrockAgentCoreHarnessContainerConfiguration is
                      a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      containerUri:
                        description: The ECR URI of the container.
                        maxLength: 1024
                        minLength: 1
                        type: string
                    required:
                    - containerUri
                    type: object
                type: object
              environmentVariables:
                additionalProperties:
                  type: string
                description: Environment variables to set in the harness runtime environment.
                type: object
              executionRoleArn:
                description: The ARN of the IAM role that the harness assumes when
                  running.
                maxLength: 2048
                minLength: 1
                type: string
              harnessName:
                description: |-
                  The name of the harness.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: harnessName is immutable
                  rule: self == oldSelf
              maxIterations:
                description: The maximum number of iterations the agent loop can execute
                  per invocation.
                format: int64
                type: integer
              maxTokens:
                description: The maximum number of tokens the agent can generate per
                  iteration.
                format: int64
                type: integer
              memory:
                description: The AgentCore Memory configuration for persisting conversation
                  context.
                properties:
                  agentCoreMemoryConfiguration:
                    description: BedrockAgentCoreHarnessHarnessAgentCoreMemoryConfiguration
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      actorId:
                        type: string
                      arn:
                        type: string
                      messagesCount:
                        format: int64
                        type: integer
                      retrievalConfig:
                        additionalProperties:
                          description: BedrockAgentCoreHarnessHarnessAgentCoreMemoryRetrievalConfig
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            relevanceScore:
                              description: |-
                                Minimum relevance score for retrieved memories. Typed as both number and string because CloudFormation
                                marshals scalars nested in dynamic-key (patternProperties) maps as strings, while direct API/CDK callers send
                                a JSON number; both forms must validate.
                              x-kubernetes-preserve-unknown-fields: true
                            strategyId:
                              type: string
                            topK:
                              description: |-
                                Maximum number of memory records to retrieve. Typed as both integer and string because CloudFormation marshals
                                scalars nested in dynamic-key (patternProperties) maps as strings, while direct API/CDK callers send a JSON
                                integer; both forms must validate.
                              x-kubernetes-preserve-unknown-fields: true
                          type: object
                        type: object
                    required:
                    - arn
                    type: object
                  disabled:
                    x-kubernetes-preserve-unknown-fields: true
                  managedMemoryConfiguration:
                    description: BedrockAgentCoreHarnessHarnessManagedMemoryConfiguration
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      arn:
                        description: The ARN of the managed memory resource. Read-only,
                          populated by the service.
                        type: string
                      encryptionKeyArn:
                        description: Customer-managed KMS key ARN. Defaults to AWS-owned
                          key. Not updatable after creation.
                        type: string
                      eventExpiryDuration:
                        description: Event retention in days. Defaults to 30.
                        format: int64
                        maximum: 365
                        minimum: 3
                        type: integer
                      strategies:
                        description: Strategy types to enable. Defaults to [SEMANTIC,
                          SUMMARIZATION].
                        items:
                          type: string
                        type: array
                    type: object
                type: object
              model:
                description: The model configuration for the harness.
                properties:
                  bedrockModelConfig:
                    description: BedrockAgentCoreHarnessHarnessBedrockModelConfig
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      additionalParams:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        description: Provider-specific parameters passed through to
                          the model provider unchanged.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      apiFormat:
                        enum:
                        - converse_stream
                        - responses
                        - chat_completions
                        type: string
                      maxTokens:
                        format: int64
                        minimum: 1
                        type: integer
                      modelId:
                        type: string
                      temperature:
                        type: number
                      topP:
                        type: number
                    required:
                    - modelId
                    type: object
                  geminiModelConfig:
                    description: BedrockAgentCoreHarnessHarnessGeminiModelConfig is
                      a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      apiKeyArn:
                        type: string
                      maxTokens:
                        format: int64
                        minimum: 1
                        type: integer
                      modelId:
                        type: string
                      temperature:
                        type: number
                      topK:
                        format: int64
                        maximum: 500
                        minimum: 0
                        type: integer
                      topP:
                        type: number
                    required:
                    - apiKeyArn
                    - modelId
                    type: object
                  liteLlmModelConfig:
                    description: BedrockAgentCoreHarnessHarnessLiteLlmModelConfig
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      additionalParams:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        description: Provider-specific parameters passed through to
                          LiteLLM unchanged.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      apiBase:
                        maxLength: 16383
                        type: string
                      apiKeyArn:
                        type: string
                      maxTokens:
                        format: int64
                        minimum: 1
                        type: integer
                      modelId:
                        type: string
                      temperature:
                        type: number
                      topP:
                        type: number
                    required:
                    - modelId
                    type: object
                  openAiModelConfig:
                    description: BedrockAgentCoreHarnessHarnessOpenAiModelConfig is
                      a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      additionalParams:
                        additionalProperties:
                          x-kubernetes-preserve-unknown-fields: true
                        description: Provider-specific parameters passed through to
                          the model provider unchanged.
                        type: object
                        x-kubernetes-preserve-unknown-fields: true
                      apiFormat:
                        enum:
                        - chat_completions
                        - responses
                        type: string
                      apiKeyArn:
                        type: string
                      maxTokens:
                        format: int64
                        minimum: 1
                        type: integer
                      modelId:
                        type: string
                      temperature:
                        type: number
                      topP:
                        type: number
                    required:
                    - apiKeyArn
                    - modelId
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              skills:
                description: The skills available to the agent.
                items:
                  description: BedrockAgentCoreHarnessHarnessSkill is a nested property
                    type of AWS::BedrockAgentCore::Harness.
                  properties:
                    awsSkills:
                      description: BedrockAgentCoreHarnessHarnessSkillAwsSkillsSource
                        is a nested property type of AWS::BedrockAgentCore::Harness.
                      properties:
                        paths:
                          description: Optionally filter allowed skills with glob
                            syntax, e.g., ['core-skills/*'].
                          items:
                            type: string
                          type: array
                      type: object
                    git:
                      description: BedrockAgentCoreHarnessHarnessSkillGitSource is
                        a nested property type of AWS::BedrockAgentCore::Harness.
                      properties:
                        auth:
                          description: BedrockAgentCoreHarnessHarnessSkillGitAuth
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            credentialArn:
                              description: The ARN of the credential in AgentCore
                                Identity containing the password or personal access
                                token.
                              type: string
                            username:
                              description: Username for authentication. Defaults to
                                'oauth2' if not specified.
                              type: string
                          required:
                          - credentialArn
                          type: object
                        path:
                          description: Subdirectory within the repository containing
                            the skill.
                          type: string
                        url:
                          description: The HTTPS URL of the git repository.
                          minLength: 8
                          type: string
                      required:
                      - url
                      type: object
                    path:
                      description: The filesystem path to the skill definition.
                      type: string
                    s3:
                      description: BedrockAgentCoreHarnessHarnessSkillS3Source is
                        a nested property type of AWS::BedrockAgentCore::Harness.
                      properties:
                        uri:
                          description: The S3 URI pointing to the skill directory
                            (e.g., s3://bucket/skills/my-skill/).
                          minLength: 5
                          type: string
                      required:
                      - uri
                      type: object
                  type: object
                type: array
              systemPrompt:
                description: The system prompt that defines the agent's behavior.
                items:
                  description: BedrockAgentCoreHarnessHarnessSystemContentBlock is
                    a nested property type of AWS::BedrockAgentCore::Harness.
                  properties:
                    text:
                      description: The text content of the system prompt block.
                      minLength: 1
                      type: string
                  required:
                  - text
                  type: object
                type: array
              tags:
                description: Tags to apply to the harness resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
              timeoutSeconds:
                description: The maximum duration in seconds for the agent loop execution
                  per invocation.
                format: int64
                type: integer
              tools:
                description: The tools available to the agent.
                items:
                  description: BedrockAgentCoreHarnessHarnessTool is a nested property
                    type of AWS::BedrockAgentCore::Harness.
                  properties:
                    config:
                      description: BedrockAgentCoreHarnessHarnessToolConfiguration
                        is a nested property type of AWS::BedrockAgentCore::Harness.
                      properties:
                        agentCoreBrowser:
                          description: BedrockAgentCoreHarnessHarnessAgentCoreBrowserConfig
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            browserArn:
                              type: string
                          type: object
                        agentCoreCodeInterpreter:
                          description: BedrockAgentCoreHarnessHarnessAgentCoreCodeInterpreterConfig
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            codeInterpreterArn:
                              type: string
                          type: object
                        agentCoreGateway:
                          description: BedrockAgentCoreHarnessHarnessAgentCoreGatewayConfig
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            gatewayArn:
                              type: string
                            outboundAuth:
                              description: BedrockAgentCoreHarnessHarnessGatewayOutboundAuth
                                is a nested property type of AWS::BedrockAgentCore::Harness.
                              properties:
                                awsIam:
                                  x-kubernetes-preserve-unknown-fields: true
                                none:
                                  x-kubernetes-preserve-unknown-fields: true
                                oauth:
                                  description: BedrockAgentCoreHarnessOAuthCredentialProvider
                                    is a nested property type of AWS::BedrockAgentCore::Harness.
                                  properties:
                                    customParameters:
                                      additionalProperties:
                                        type: string
                                      type: object
                                    defaultReturnUrl:
                                      maxLength: 2048
                                      type: string
                                    grantType:
                                      enum:
                                      - CLIENT_CREDENTIALS
                                      - AUTHORIZATION_CODE
                                      type: string
                                    providerArn:
                                      type: string
                                    scopes:
                                      items:
                                        type: string
                                      type: array
                                  required:
                                  - providerArn
                                  - scopes
                                  type: object
                              type: object
                          required:
                          - gatewayArn
                          type: object
                        inlineFunction:
                          description: BedrockAgentCoreHarnessHarnessInlineFunctionConfig
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            description:
                              maxLength: 4096
                              minLength: 1
                              type: string
                            inputSchema:
                              description: JSON Schema describing the tool's input
                                parameters.
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - description
                          - inputSchema
                          type: object
                        remoteMcp:
                          description: BedrockAgentCoreHarnessHarnessRemoteMcpConfig
                            is a nested property type of AWS::BedrockAgentCore::Harness.
                          properties:
                            headers:
                              additionalProperties:
                                type: string
                              type: object
                            url:
                              maxLength: 16383
                              minLength: 1
                              type: string
                          required:
                          - url
                          type: object
                      type: object
                    name:
                      maxLength: 64
                      type: string
                    type:
                      enum:
                      - remote_mcp
                      - agentcore_browser
                      - agentcore_gateway
                      - inline_function
                      - agentcore_code_interpreter
                      type: string
                  required:
                  - type
                  type: object
                type: array
              truncation:
                description: The truncation configuration for managing conversation
                  context.
                properties:
                  config:
                    description: BedrockAgentCoreHarnessHarnessTruncationStrategyConfiguration
                      is a nested property type of AWS::BedrockAgentCore::Harness.
                    properties:
                      slidingWindow:
                        description: BedrockAgentCoreHarnessHarnessSlidingWindowConfiguration
                          is a nested property type of AWS::BedrockAgentCore::Harness.
                        properties:
                          messagesCount:
                            format: int64
                            type: integer
                        type: object
                      summarization:
                        description: BedrockAgentCoreHarnessHarnessSummarizationConfiguration
                          is a nested property type of AWS::BedrockAgentCore::Harness.
                        properties:
                          preserveRecentMessages:
                            format: int64
                            type: integer
                          summarizationSystemPrompt:
                            type: string
                          summaryRatio:
                            type: number
                        type: object
                    type: object
                  strategy:
                    enum:
                    - sliding_window
                    - summarization
                    - none
                    type: string
                required:
                - strategy
                type: object
            required:
            - executionRoleArn
            - harnessName
            - model
            type: object
          status:
            description: BedrockAgentCoreHarnessStatus is the observed state of AWS::BedrockAgentCore::Harness.
            properties:
              arn:
                description: |-
                  The Amazon Resource Name (ARN) of the harness.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the harness was created.
                  Read-only: reported by AWS after creation.
                type: string
              harnessId:
                description: |-
                  The unique identifier of the harness.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the harness.
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - READY
                - DELETING
                - DELETE_FAILED
                type: string
              updatedAt:
                description: |-
                  The timestamp when the harness was last updated.
                  Read-only: reported by AWS after creation.
                type: string
              version:
                description: |-
                  The version of the harness. Incremented on every successful update.
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorememories.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreMemory
    listKind: BedrockAgentCoreMemoryList
    plural: bedrockagentcorememories
    singular: bedrockagentcorememory
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreMemory manages AWS::BedrockAgentCore::Memory through the AWS Cloud Control API. Resource Type
          definition for AWS::BedrockAgentCore::Memory
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreMemorySpec is the desired state of AWS::BedrockAgentCore::Memory.
            properties:
              description:
                type: string
              encryptionKeyArn:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
              eventExpiryDuration:
                description: Duration in days until memory events expire
                format: int64
                maximum: 365
                minimum: 3
                type: integer
              indexedKeys:
                items:
                  description: BedrockAgentCoreMemoryIndexedKey is a nested property
                    type of AWS::BedrockAgentCore::Memory.
                  properties:
                    key:
                      maxLength: 128
                      minLength: 1
                      type: string
                    type:
                      enum:
                      - STRING
                      - STRINGLIST
                      - NUMBER
                      type: string
                  required:
                  - key
                  - type
                  type: object
                type: array
              memoryExecutionRoleArn:
                type: string
              memoryStrategies:
                items:
                  description: BedrockAgentCoreMemoryMemoryStrategy is a nested property
                    type of AWS::BedrockAgentCore::Memory.
                  properties:
                    customMemoryStrategy:
                      description: BedrockAgentCoreMemoryCustomMemoryStrategy is a
                        nested property type of AWS::BedrockAgentCore::Memory.
                      properties:
                        configuration:
                          description: BedrockAgentCoreMemoryCustomConfigurationInput
                            is a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            episodicOverride:
                              description: BedrockAgentCoreMemoryEpisodicOverride
                                is a nested property type of AWS::BedrockAgentCore::Memory.
                              properties:
                                consolidation:
                                  description: BedrockAgentCoreMemoryEpisodicOverrideConsolidationConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                                extraction:
                                  description: BedrockAgentCoreMemoryEpisodicOverrideExtractionConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                                reflection:
                                  description: BedrockAgentCoreMemoryEpisodicOverrideReflectionConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    memoryRecordSchema:
                                      description: BedrockAgentCoreMemoryMemoryRecordSchema
                                        is a nested property type of AWS::BedrockAgentCore::Memory.
                                      properties:
                                        metadataSchema:
                                          items:
                                            description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              extractionConfig:
                                                description: BedrockAgentCoreMemoryExtractionConfig
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  llmExtractionConfig:
                                                    description: BedrockAgentCoreMemoryLlmExtractionConfig
                                                      is a nested property type of
                                                      AWS::BedrockAgentCore::Memory.
                                                    properties:
                                                      definition:
                                                        maxLength: 1000
                                                        minLength: 1
                                                        type: string
                                                      llmExtractionInstruction:
                                                        maxLength: 1000
                                                        type: string
                                                      validation:
                                                        description: BedrockAgentCoreMemoryValidation
                                                          is a nested property type
                                                          of AWS::BedrockAgentCore::Memory.
                                                        properties:
                                                          numberValidation:
                                                            description: BedrockAgentCoreMemoryNumberValidation
                                                              is a nested property
                                                              type of AWS::BedrockAgentCore::Memory.
                                                            properties:
                                                              maxValue:
                                                                type: number
                                                              minValue:
                                                                type: number
                                                            type: object
                                                          stringListValidation:
                                                            description: BedrockAgentCoreMemoryStringListValidation
                                                              is a nested property
                                                              type of AWS::BedrockAgentCore::Memory.
                                                            properties:
                                                              allowedValues:
                                                                items:
                                                                  type: string
                                                                type: array
                                                              maxItems:
                                                                format: int64
                                                                maximum: 5
                                                                minimum: 1
                                                                type: integer
                                                            type: object
                                                          stringValidation:
                                                            description: BedrockAgentCoreMemoryStringValidation
                                                              is a nested property
                                                              type of AWS::BedrockAgentCore::Memory.
                                                            properties:
                                                              allowedValues:
                                                                items:
                                                                  type: string
                                                                type: array
                                                            required:
                                                            - allowedValues
                                                            type: object
                                                        type: object
                                                    required:
                                                    - definition
                                                    type: object
                                                type: object
                                              extractionType:
                                                enum:
                                                - LLM_INFERRED
                                                - STRICTLY_CONSISTENT
                                                type: string
                                              key:
                                                maxLength: 128
                                                minLength: 1
                                                type: string
                                              type:
                                                enum:
                                                - STRING
                                                - STRINGLIST
                                                - NUMBER
                                                type: string
                                            required:
                                            - key
                                            type: object
                                          type: array
                                      type: object
                                    modelId:
                                      type: string
                                    namespaceTemplates:
                                      items:
                                        type: string
                                      type: array
                                    namespaces:
                                      items:
                                        type: string
                                      type: array
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                              type: object
                            selfManagedConfiguration:
                              description: BedrockAgentCoreMemorySelfManagedConfiguration
                                is a nested property type of AWS::BedrockAgentCore::Memory.
                              properties:
                                historicalContextWindowSize:
                                  format: int64
                                  maximum: 50
                                  minimum: 0
                                  type: integer
                                invocationConfiguration:
                                  description: BedrockAgentCoreMemoryInvocationConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    payloadDeliveryBucketName:
                                      type: string
                                    topicArn:
                                      type: string
                                  type: object
                                triggerConditions:
                                  items:
                                    description: BedrockAgentCoreMemoryTriggerConditionInput
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      messageBasedTrigger:
                                        description: BedrockAgentCoreMemoryMessageBasedTriggerInput
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          messageCount:
                                            format: int64
                                            maximum: 50
                                            minimum: 1
                                            type: integer
                                        type: object
                                      timeBasedTrigger:
                                        description: BedrockAgentCoreMemoryTimeBasedTriggerInput
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          idleSessionTimeout:
                                            format: int64
                                            maximum: 3000
                                            minimum: 10
                                            type: integer
                                        type: object
                                      tokenBasedTrigger:
                                        description: BedrockAgentCoreMemoryTokenBasedTriggerInput
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          tokenCount:
                                            format: int64
                                            maximum: 500000
                                            minimum: 100
                                            type: integer
                                        type: object
                                    type: object
                                  type: array
                              type: object
                            semanticOverride:
                              description: BedrockAgentCoreMemorySemanticOverride
                                is a nested property type of AWS::BedrockAgentCore::Memory.
                              properties:
                                consolidation:
                                  description: BedrockAgentCoreMemorySemanticOverrideConsolidationConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                                extraction:
                                  description: BedrockAgentCoreMemorySemanticOverrideExtractionConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                              type: object
                            summaryOverride:
                              description: BedrockAgentCoreMemorySummaryOverride is
                                a nested property type of AWS::BedrockAgentCore::Memory.
                              properties:
                                consolidation:
                                  description: BedrockAgentCoreMemorySummaryOverrideConsolidationConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                              type: object
                            userPreferenceOverride:
                              description: BedrockAgentCoreMemoryUserPreferenceOverride
                                is a nested property type of AWS::BedrockAgentCore::Memory.
                              properties:
                                consolidation:
                                  description: BedrockAgentCoreMemoryUserPreferenceOverrideConsolidationConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                                extraction:
                                  description: BedrockAgentCoreMemoryUserPreferenceOverrideExtractionConfigurationInput
                                    is a nested property type of AWS::BedrockAgentCore::Memory.
                                  properties:
                                    appendToPrompt:
                                      maxLength: 30000
                                      minLength: 1
                                      type: string
                                    modelId:
                                      type: string
                                  required:
                                  - appendToPrompt
                                  - modelId
                                  type: object
                              type: object
                          type: object
                        createdAt:
                          description: Creation timestamp of the memory strategy
                          type: string
                        description:
                          type: string
                        memoryRecordSchema:
                          description: BedrockAgentCoreMemoryMemoryRecordSchema is
                            a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            metadataSchema:
                              items:
                                description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                properties:
                                  extractionConfig:
                                    description: BedrockAgentCoreMemoryExtractionConfig
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      llmExtractionConfig:
                                        description: BedrockAgentCoreMemoryLlmExtractionConfig
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          definition:
                                            maxLength: 1000
                                            minLength: 1
                                            type: string
                                          llmExtractionInstruction:
                                            maxLength: 1000
                                            type: string
                                          validation:
                                            description: BedrockAgentCoreMemoryValidation
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              numberValidation:
                                                description: BedrockAgentCoreMemoryNumberValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  maxValue:
                                                    type: number
                                                  minValue:
                                                    type: number
                                                type: object
                                              stringListValidation:
                                                description: BedrockAgentCoreMemoryStringListValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                  maxItems:
                                                    format: int64
                                                    maximum: 5
                                                    minimum: 1
                                                    type: integer
                                                type: object
                                              stringValidation:
                                                description: BedrockAgentCoreMemoryStringValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                required:
                                                - allowedValues
                                                type: object
                                            type: object
                                        required:
                                        - definition
                                        type: object
                                    type: object
                                  extractionType:
                                    enum:
                                    - LLM_INFERRED
                                    - STRICTLY_CONSISTENT
                                    type: string
                                  key:
                                    maxLength: 128
                                    minLength: 1
                                    type: string
                                  type:
                                    enum:
                                    - STRING
                                    - STRINGLIST
                                    - NUMBER
                                    type: string
                                required:
                                - key
                                type: object
                              type: array
                          type: object
                        name:
                          type: string
                        namespaceTemplates:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                        status:
                          description: Status of the memory strategy
                          enum:
                          - CREATING
                          - ACTIVE
                          - DELETING
                          - FAILED
                          type: string
                        strategyId:
                          description: Unique identifier for the memory strategy
                          type: string
                        type:
                          description: Type of memory strategy
                          enum:
                          - SEMANTIC
                          - SUMMARIZATION
                          - USER_PREFERENCE
                          - CUSTOM
                          - EPISODIC
                          type: string
                        updatedAt:
                          description: Last update timestamp of the memory strategy
                          type: string
                      required:
                      - name
                      type: object
                    episodicMemoryStrategy:
                      description: BedrockAgentCoreMemoryEpisodicMemoryStrategy is
                        a nested property type of AWS::BedrockAgentCore::Memory.
                      properties:
                        createdAt:
                          description: Creation timestamp of the memory strategy
                          type: string
                        description:
                          type: string
                        memoryRecordSchema:
                          description: BedrockAgentCoreMemoryMemoryRecordSchema is
                            a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            metadataSchema:
                              items:
                                description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                properties:
                                  extractionConfig:
                                    description: BedrockAgentCoreMemoryExtractionConfig
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      llmExtractionConfig:
                                        description: BedrockAgentCoreMemoryLlmExtractionConfig
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          definition:
                                            maxLength: 1000
                                            minLength: 1
                                            type: string
                                          llmExtractionInstruction:
                                            maxLength: 1000
                                            type: string
                                          validation:
                                            description: BedrockAgentCoreMemoryValidation
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              numberValidation:
                                                description: BedrockAgentCoreMemoryNumberValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  maxValue:
                                                    type: number
                                                  minValue:
                                                    type: number
                                                type: object
                                              stringListValidation:
                                                description: BedrockAgentCoreMemoryStringListValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                  maxItems:
                                                    format: int64
                                                    maximum: 5
                                                    minimum: 1
                                                    type: integer
                                                type: object
                                              stringValidation:
                                                description: BedrockAgentCoreMemoryStringValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                required:
                                                - allowedValues
                                                type: object
                                            type: object
                                        required:
                                        - definition
                                        type: object
                                    type: object
                                  extractionType:
                                    enum:
                                    - LLM_INFERRED
                                    - STRICTLY_CONSISTENT
                                    type: string
                                  key:
                                    maxLength: 128
                                    minLength: 1
                                    type: string
                                  type:
                                    enum:
                                    - STRING
                                    - STRINGLIST
                                    - NUMBER
                                    type: string
                                required:
                                - key
                                type: object
                              type: array
                          type: object
                        name:
                          type: string
                        namespaceTemplates:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                        reflectionConfiguration:
                          description: BedrockAgentCoreMemoryEpisodicReflectionConfigurationInput
                            is a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            memoryRecordSchema:
                              description: BedrockAgentCoreMemoryMemoryRecordSchema
                                is a nested property type of AWS::BedrockAgentCore::Memory.
                              properties:
                                metadataSchema:
                                  items:
                                    description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      extractionConfig:
                                        description: BedrockAgentCoreMemoryExtractionConfig
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          llmExtractionConfig:
                                            description: BedrockAgentCoreMemoryLlmExtractionConfig
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              definition:
                                                maxLength: 1000
                                                minLength: 1
                                                type: string
                                              llmExtractionInstruction:
                                                maxLength: 1000
                                                type: string
                                              validation:
                                                description: BedrockAgentCoreMemoryValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  numberValidation:
                                                    description: BedrockAgentCoreMemoryNumberValidation
                                                      is a nested property type of
                                                      AWS::BedrockAgentCore::Memory.
                                                    properties:
                                                      maxValue:
                                                        type: number
                                                      minValue:
                                                        type: number
                                                    type: object
                                                  stringListValidation:
                                                    description: BedrockAgentCoreMemoryStringListValidation
                                                      is a nested property type of
                                                      AWS::BedrockAgentCore::Memory.
                                                    properties:
                                                      allowedValues:
                                                        items:
                                                          type: string
                                                        type: array
                                                      maxItems:
                                                        format: int64
                                                        maximum: 5
                                                        minimum: 1
                                                        type: integer
                                                    type: object
                                                  stringValidation:
                                                    description: BedrockAgentCoreMemoryStringValidation
                                                      is a nested property type of
                                                      AWS::BedrockAgentCore::Memory.
                                                    properties:
                                                      allowedValues:
                                                        items:
                                                          type: string
                                                        type: array
                                                    required:
                                                    - allowedValues
                                                    type: object
                                                type: object
                                            required:
                                            - definition
                                            type: object
                                        type: object
                                      extractionType:
                                        enum:
                                        - LLM_INFERRED
                                        - STRICTLY_CONSISTENT
                                        type: string
                                      key:
                                        maxLength: 128
                                        minLength: 1
                                        type: string
                                      type:
                                        enum:
                                        - STRING
                                        - STRINGLIST
                                        - NUMBER
                                        type: string
                                    required:
                                    - key
                                    type: object
                                  type: array
                              type: object
                            namespaceTemplates:
                              items:
                                type: string
                              type: array
                            namespaces:
                              items:
                                type: string
                              type: array
                          type: object
                        status:
                          description: Status of the memory strategy
                          enum:
                          - CREATING
                          - ACTIVE
                          - DELETING
                          - FAILED
                          type: string
                        strategyId:
                          description: Unique identifier for the memory strategy
                          type: string
                        type:
                          description: Type of memory strategy
                          enum:
                          - SEMANTIC
                          - SUMMARIZATION
                          - USER_PREFERENCE
                          - CUSTOM
                          - EPISODIC
                          type: string
                        updatedAt:
                          description: Last update timestamp of the memory strategy
                          type: string
                      required:
                      - name
                      type: object
                    semanticMemoryStrategy:
                      description: BedrockAgentCoreMemorySemanticMemoryStrategy is
                        a nested property type of AWS::BedrockAgentCore::Memory.
                      properties:
                        createdAt:
                          description: Creation timestamp of the memory strategy
                          type: string
                        description:
                          type: string
                        memoryRecordSchema:
                          description: BedrockAgentCoreMemoryMemoryRecordSchema is
                            a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            metadataSchema:
                              items:
                                description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                properties:
                                  extractionConfig:
                                    description: BedrockAgentCoreMemoryExtractionConfig
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      llmExtractionConfig:
                                        description: BedrockAgentCoreMemoryLlmExtractionConfig
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          definition:
                                            maxLength: 1000
                                            minLength: 1
                                            type: string
                                          llmExtractionInstruction:
                                            maxLength: 1000
                                            type: string
                                          validation:
                                            description: BedrockAgentCoreMemoryValidation
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              numberValidation:
                                                description: BedrockAgentCoreMemoryNumberValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  maxValue:
                                                    type: number
                                                  minValue:
                                                    type: number
                                                type: object
                                              stringListValidation:
                                                description: BedrockAgentCoreMemoryStringListValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                  maxItems:
                                                    format: int64
                                                    maximum: 5
                                                    minimum: 1
                                                    type: integer
                                                type: object
                                              stringValidation:
                                                description: BedrockAgentCoreMemoryStringValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                required:
                                                - allowedValues
                                                type: object
                                            type: object
                                        required:
                                        - definition
                                        type: object
                                    type: object
                                  extractionType:
                                    enum:
                                    - LLM_INFERRED
                                    - STRICTLY_CONSISTENT
                                    type: string
                                  key:
                                    maxLength: 128
                                    minLength: 1
                                    type: string
                                  type:
                                    enum:
                                    - STRING
                                    - STRINGLIST
                                    - NUMBER
                                    type: string
                                required:
                                - key
                                type: object
                              type: array
                          type: object
                        name:
                          type: string
                        namespaceTemplates:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                        status:
                          description: Status of the memory strategy
                          enum:
                          - CREATING
                          - ACTIVE
                          - DELETING
                          - FAILED
                          type: string
                        strategyId:
                          description: Unique identifier for the memory strategy
                          type: string
                        type:
                          description: Type of memory strategy
                          enum:
                          - SEMANTIC
                          - SUMMARIZATION
                          - USER_PREFERENCE
                          - CUSTOM
                          - EPISODIC
                          type: string
                        updatedAt:
                          description: Last update timestamp of the memory strategy
                          type: string
                      required:
                      - name
                      type: object
                    summaryMemoryStrategy:
                      description: BedrockAgentCoreMemorySummaryMemoryStrategy is
                        a nested property type of AWS::BedrockAgentCore::Memory.
                      properties:
                        createdAt:
                          description: Creation timestamp of the memory strategy
                          type: string
                        description:
                          type: string
                        memoryRecordSchema:
                          description: BedrockAgentCoreMemoryMemoryRecordSchema is
                            a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            metadataSchema:
                              items:
                                description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                properties:
                                  extractionConfig:
                                    description: BedrockAgentCoreMemoryExtractionConfig
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      llmExtractionConfig:
                                        description: BedrockAgentCoreMemoryLlmExtractionConfig
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          definition:
                                            maxLength: 1000
                                            minLength: 1
                                            type: string
                                          llmExtractionInstruction:
                                            maxLength: 1000
                                            type: string
                                          validation:
                                            description: BedrockAgentCoreMemoryValidation
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              numberValidation:
                                                description: BedrockAgentCoreMemoryNumberValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  maxValue:
                                                    type: number
                                                  minValue:
                                                    type: number
                                                type: object
                                              stringListValidation:
                                                description: BedrockAgentCoreMemoryStringListValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                  maxItems:
                                                    format: int64
                                                    maximum: 5
                                                    minimum: 1
                                                    type: integer
                                                type: object
                                              stringValidation:
                                                description: BedrockAgentCoreMemoryStringValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                required:
                                                - allowedValues
                                                type: object
                                            type: object
                                        required:
                                        - definition
                                        type: object
                                    type: object
                                  extractionType:
                                    enum:
                                    - LLM_INFERRED
                                    - STRICTLY_CONSISTENT
                                    type: string
                                  key:
                                    maxLength: 128
                                    minLength: 1
                                    type: string
                                  type:
                                    enum:
                                    - STRING
                                    - STRINGLIST
                                    - NUMBER
                                    type: string
                                required:
                                - key
                                type: object
                              type: array
                          type: object
                        name:
                          type: string
                        namespaceTemplates:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                        status:
                          description: Status of the memory strategy
                          enum:
                          - CREATING
                          - ACTIVE
                          - DELETING
                          - FAILED
                          type: string
                        strategyId:
                          description: Unique identifier for the memory strategy
                          type: string
                        type:
                          description: Type of memory strategy
                          enum:
                          - SEMANTIC
                          - SUMMARIZATION
                          - USER_PREFERENCE
                          - CUSTOM
                          - EPISODIC
                          type: string
                        updatedAt:
                          description: Last update timestamp of the memory strategy
                          type: string
                      required:
                      - name
                      type: object
                    userPreferenceMemoryStrategy:
                      description: BedrockAgentCoreMemoryUserPreferenceMemoryStrategy
                        is a nested property type of AWS::BedrockAgentCore::Memory.
                      properties:
                        createdAt:
                          description: Creation timestamp of the memory strategy
                          type: string
                        description:
                          type: string
                        memoryRecordSchema:
                          description: BedrockAgentCoreMemoryMemoryRecordSchema is
                            a nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            metadataSchema:
                              items:
                                description: BedrockAgentCoreMemoryMetadataSchemaEntry
                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                properties:
                                  extractionConfig:
                                    description: BedrockAgentCoreMemoryExtractionConfig
                                      is a nested property type of AWS::BedrockAgentCore::Memory.
                                    properties:
                                      llmExtractionConfig:
                                        description: BedrockAgentCoreMemoryLlmExtractionConfig
                                          is a nested property type of AWS::BedrockAgentCore::Memory.
                                        properties:
                                          definition:
                                            maxLength: 1000
                                            minLength: 1
                                            type: string
                                          llmExtractionInstruction:
                                            maxLength: 1000
                                            type: string
                                          validation:
                                            description: BedrockAgentCoreMemoryValidation
                                              is a nested property type of AWS::BedrockAgentCore::Memory.
                                            properties:
                                              numberValidation:
                                                description: BedrockAgentCoreMemoryNumberValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  maxValue:
                                                    type: number
                                                  minValue:
                                                    type: number
                                                type: object
                                              stringListValidation:
                                                description: BedrockAgentCoreMemoryStringListValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                  maxItems:
                                                    format: int64
                                                    maximum: 5
                                                    minimum: 1
                                                    type: integer
                                                type: object
                                              stringValidation:
                                                description: BedrockAgentCoreMemoryStringValidation
                                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                                properties:
                                                  allowedValues:
                                                    items:
                                                      type: string
                                                    type: array
                                                required:
                                                - allowedValues
                                                type: object
                                            type: object
                                        required:
                                        - definition
                                        type: object
                                    type: object
                                  extractionType:
                                    enum:
                                    - LLM_INFERRED
                                    - STRICTLY_CONSISTENT
                                    type: string
                                  key:
                                    maxLength: 128
                                    minLength: 1
                                    type: string
                                  type:
                                    enum:
                                    - STRING
                                    - STRINGLIST
                                    - NUMBER
                                    type: string
                                required:
                                - key
                                type: object
                              type: array
                          type: object
                        name:
                          type: string
                        namespaceTemplates:
                          items:
                            type: string
                          type: array
                        namespaces:
                          items:
                            type: string
                          type: array
                        status:
                          description: Status of the memory strategy
                          enum:
                          - CREATING
                          - ACTIVE
                          - DELETING
                          - FAILED
                          type: string
                        strategyId:
                          description: Unique identifier for the memory strategy
                          type: string
                        type:
                          description: Type of memory strategy
                          enum:
                          - SEMANTIC
                          - SUMMARIZATION
                          - USER_PREFERENCE
                          - CUSTOM
                          - EPISODIC
                          type: string
                        updatedAt:
                          description: Last update timestamp of the memory strategy
                          type: string
                      required:
                      - name
                      type: object
                  type: object
                type: array
              name:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              streamDeliveryResources:
                description: BedrockAgentCoreMemoryStreamDeliveryResources is a nested
                  property type of AWS::BedrockAgentCore::Memory.
                properties:
                  resources:
                    items:
                      description: BedrockAgentCoreMemoryStreamDeliveryResource is
                        a nested property type of AWS::BedrockAgentCore::Memory.
                      properties:
                        kinesis:
                          description: BedrockAgentCoreMemoryKinesisResource is a
                            nested property type of AWS::BedrockAgentCore::Memory.
                          properties:
                            contentConfigurations:
                              items:
                                description: BedrockAgentCoreMemoryContentConfiguration
                                  is a nested property type of AWS::BedrockAgentCore::Memory.
                                properties:
                                  level:
                                    description: The level of content detail to deliver
                                    enum:
                                    - METADATA_ONLY
                                    - FULL_CONTENT
                                    type: string
                                  type:
                                    description: The type of content to deliver
                                    enum:
                                    - MEMORY_RECORDS
                                    type: string
                                required:
                                - type
                                type: object
                              type: array
                            dataStreamArn:
                              type: string
                          required:
                          - contentConfigurations
                          - dataStreamArn
                          type: object
                      type: object
                    type: array
                required:
                - resources
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - eventExpiryDuration
            - name
            type: object
          status:
            description: BedrockAgentCoreMemoryStatus is the observed state of AWS::BedrockAgentCore::Memory.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              failureReason:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              memoryArn:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              memoryId:
                description: 'Read-only: reported by AWS after creation.'
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - ACTIVE
                - FAILED
                - DELETING
                type: string
              updatedAt:
                description: 'Read-only: reported by AWS after creation.'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreoauth2credentialproviders.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreOAuth2CredentialProvider
    listKind: BedrockAgentCoreOAuth2CredentialProviderList
    plural: bedrockagentcoreoauth2credentialproviders
    singular: bedrockagentcoreoauth2credentialprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreOAuth2CredentialProvider manages AWS::BedrockAgentCore::OAuth2CredentialProvider through the
          AWS Cloud Control API. Resource Type definition for AWS::BedrockAgentCore::OAuth2CredentialProvider
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreOAuth2CredentialProviderSpec is the desired
              state of AWS::BedrockAgentCore::OAuth2CredentialProvider.
            properties:
              credentialProviderVendor:
                description: |-
                  The vendor of the OAuth2 credential provider
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - GoogleOauth2
                - GithubOauth2
                - SlackOauth2
                - SalesforceOauth2
                - MicrosoftOauth2
                - CustomOauth2
                - AtlassianOauth2
                - LinkedinOauth2
                - XOauth2
                - OktaOauth2
                - OneLoginOauth2
                - PingOneOauth2
                - FacebookOauth2
                - YandexOauth2
                - RedditOauth2
                - ZoomOauth2
                - TwitchOauth2
                - SpotifyOauth2
                - DropboxOauth2
                - NotionOauth2
                - HubspotOauth2
                - CyberArkOauth2
                - FusionAuthOauth2
                - Auth0Oauth2
                - CognitoOauth2
                type: string
                x-kubernetes-validations:
                - message: credentialProviderVendor is immutable
                  rule: self == oldSelf
              name:
                description: |-
                  The name of the OAuth2 credential provider
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              oauth2ProviderConfigInput:
                description: The configuration settings for the OAuth2 provider
                properties:
                  atlassianOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderAtlassianOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - clientId
                    type: object
                  customOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderCustomOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientAuthenticationMethod:
                        description: The client authentication method to use when
                          authenticating with the token endpoint
                        enum:
                        - CLIENT_SECRET_BASIC
                        - CLIENT_SECRET_POST
                        - AWS_IAM_ID_TOKEN_JWT
                        - PRIVATE_KEY_JWT
                        type: string
                      clientId:
                        description: The client ID for the custom OAuth2 provider
                        maxLength: 256
                        type: string
                      clientSecret:
                        description: The client secret for the custom OAuth2 provider
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        description: The source of the client secret
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      oauthDiscovery:
                        description: BedrockAgentCoreOAuth2CredentialProviderOauth2Discovery
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          authorizationServerMetadata:
                            description: BedrockAgentCoreOAuth2CredentialProviderOauth2AuthorizationServerMetadata
                              is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                            properties:
                              authorizationEndpoint:
                                description: The authorization endpoint URL
                                type: string
                              issuer:
                                description: The issuer URL for the OAuth2 authorization
                                  server
                                type: string
                              responseTypes:
                                description: The supported response types
                                items:
                                  type: string
                                type: array
                              tokenEndpoint:
                                description: The token endpoint URL
                                type: string
                            required:
                            - authorizationEndpoint
                            - issuer
                            - tokenEndpoint
                            type: object
                          discoveryUrl:
                            description: The discovery URL for the OAuth2 provider
                            type: string
                        type: object
                      onBehalfOfTokenExchangeConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderOnBehalfOfTokenExchangeConfig
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          grantType:
                            description: The grant type for on-behalf-of token exchange
                            enum:
                            - TOKEN_EXCHANGE
                            - JWT_AUTHORIZATION_GRANT
                            type: string
                          tokenExchangeGrantTypeConfig:
                            description: BedrockAgentCoreOAuth2CredentialProviderTokenExchangeGrantTypeConfig
                              is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                            properties:
                              actorTokenContent:
                                description: The actor token content type
                                enum:
                                - NONE
                                - M2M
                                - AWS_IAM_ID_TOKEN_JWT
                                type: string
                              actorTokenScopes:
                                description: The actor token scopes. Only valid when
                                  ActorTokenContent is M2M.
                                items:
                                  type: string
                                type: array
                            required:
                            - actorTokenContent
                            type: object
                        required:
                        - grantType
                        type: object
                      privateEndpoint:
                        description: BedrockAgentCoreOAuth2CredentialProviderPrivateEndpoint
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          managedVpcResource:
                            description: BedrockAgentCoreOAuth2CredentialProviderManagedVpcResource
                              is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                            properties:
                              endpointIpAddressType:
                                description: The IP address type for the resource
                                  configuration endpoint
                                enum:
                                - IPV4
                                - IPV6
                                type: string
                              routingDomain:
                                description: An intermediate publicly resolvable domain
                                  used as the VPC Lattice resource configuration endpoint
                                maxLength: 255
                                type: string
                              securityGroupIds:
                                description: The security group IDs to associate with
                                  the VPC Lattice resource gateway
                                items:
                                  type: string
                                type: array
                              subnetIds:
                                description: The subnet IDs within the VPC where the
                                  VPC Lattice resource gateway is placed
                                items:
                                  type: string
                                type: array
                              tags:
                                additionalProperties:
                                  type: string
                                description: Tags to apply to the managed VPC Lattice
                                  resource gateway
                                type: object
                              vpcIdentifier:
                                description: The ID of the VPC that contains your
                                  private resource
                                type: string
                            required:
                            - endpointIpAddressType
                            - subnetIds
                            - vpcIdentifier
                            type: object
                          selfManagedLatticeResource:
                            description: BedrockAgentCoreOAuth2CredentialProviderSelfManagedLatticeResource
                              is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                            properties:
                              resourceConfigurationIdentifier:
                                description: The ARN or ID of the VPC Lattice resource
                                  configuration
                                maxLength: 2048
                                minLength: 20
                                type: string
                            required:
                            - resourceConfigurationIdentifier
                            type: object
                        type: object
                      privateEndpointOverrides:
                        description: |-
                          A list of private endpoint overrides. Each override maps a specific domain to a private endpoint, enabling
                          secure connectivity through VPC Lattice resource configurations.
                        items:
                          description: BedrockAgentCoreOAuth2CredentialProviderPrivateEndpointOverride
                            is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                          properties:
                            domain:
                              description: The domain to override with a private endpoint
                              maxLength: 253
                              minLength: 1
                              type: string
                            privateEndpoint:
                              description: BedrockAgentCoreOAuth2CredentialProviderPrivateEndpoint
                                is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                              properties:
                                managedVpcResource:
                                  description: BedrockAgentCoreOAuth2CredentialProviderManagedVpcResource
                                    is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                                  properties:
                                    endpointIpAddressType:
                                      description: The IP address type for the resource
                                        configuration endpoint
                                      enum:
                                      - IPV4
                                      - IPV6
                                      type: string
                                    routingDomain:
                                      description: An intermediate publicly resolvable
                                        domain used as the VPC Lattice resource configuration
                                        endpoint
                                      maxLength: 255
                                      type: string
                                    securityGroupIds:
                                      description: The security group IDs to associate
                                        with the VPC Lattice resource gateway
                                      items:
                                        type: string
                                      type: array
                                    subnetIds:
                                      description: The subnet IDs within the VPC where
                                        the VPC Lattice resource gateway is placed
                                      items:
                                        type: string
                                      type: array
                                    tags:
                                      additionalProperties:
                                        type: string
                                      description: Tags to apply to the managed VPC
                                        Lattice resource gateway
                                      type: object
                                    vpcIdentifier:
                                      description: The ID of the VPC that contains
                                        your private resource
                                      type: string
                                  required:
                                  - endpointIpAddressType
                                  - subnetIds
                                  - vpcIdentifier
                                  type: object
                                selfManagedLatticeResource:
                                  description: BedrockAgentCoreOAuth2CredentialProviderSelfManagedLatticeResource
                                    is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                                  properties:
                                    resourceConfigurationIdentifier:
                                      description: The ARN or ID of the VPC Lattice
                                        resource configuration
                                      maxLength: 2048
                                      minLength: 20
                                      type: string
                                  required:
                                  - resourceConfigurationIdentifier
                                  type: object
                              type: object
                          required:
                          - domain
                          - privateEndpoint
                          type: object
                        type: array
                      privateKeyJwtConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderPrivateKeyJwtConfig
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          additionalHeaderClaims:
                            additionalProperties:
                              type: string
                            type: object
                          additionalPayloadClaims:
                            additionalProperties:
                              type: string
                            type: object
                          privateKeySource:
                            description: BedrockAgentCoreOAuth2CredentialProviderPrivateKeySource
                              is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                            properties:
                              kmsKeySource:
                                description: BedrockAgentCoreOAuth2CredentialProviderKmsKeySourceType
                                  is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                                properties:
                                  kmsKeyArn:
                                    description: The Amazon Resource Name (ARN) of
                                      the KMS key used to sign the JWT client assertion
                                    maxLength: 2048
                                    minLength: 1
                                    type: string
                                required:
                                - kmsKeyArn
                                type: object
                            type: object
                          signingAlgorithm:
                            description: The algorithm used to sign the JWT client
                              assertion
                            enum:
                            - RS256
                            - PS256
                            - ES256
                            type: string
                        type: object
                    required:
                    - oauthDiscovery
                    type: object
                  githubOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderGithubOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - clientId
                    type: object
                  googleOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderGoogleOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - clientId
                    type: object
                  includedOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderIncludedOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      authorizationEndpoint:
                        description: OAuth2 authorization endpoint for your isolated
                          OAuth2 application tenant
                        type: string
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      issuer:
                        description: Token issuer of your isolated OAuth2 application
                          tenant
                        type: string
                      tokenEndpoint:
                        description: OAuth2 token endpoint for your isolated OAuth2
                          application tenant
                        type: string
                    required:
                    - clientId
                    type: object
                  linkedinOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderLinkedinOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - clientId
                    type: object
                  microsoftOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderMicrosoftOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      tenantId:
                        description: The Microsoft Entra ID tenant ID
                        maxLength: 2048
                        type: string
                    required:
                    - clientId
                    type: object
                  salesforceOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderSalesforceOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - clientId
                    type: object
                  slackOauth2ProviderConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderSlackOauth2ProviderConfigInput
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      clientId:
                        maxLength: 256
                        minLength: 1
                        type: string
                      clientSecret:
                        maxLength: 2048
                        type: string
                      clientSecretConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      clientSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - clientId
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Tags to assign to the OAuth2 credential provider
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - credentialProviderVendor
            - name
            type: object
          status:
            description: BedrockAgentCoreOAuth2CredentialProviderStatus is the observed
              state of AWS::BedrockAgentCore::OAuth2CredentialProvider.
            properties:
              callbackUrl:
                description: |-
                  The callback URL for the OAuth2 authorization flow
                  Read-only: reported by AWS after creation.
                type: string
              clientSecretArn:
                description: |-
                  The ARN of the client secret in AWS Secrets Manager
                  Read-only: reported by AWS after creation.
                properties:
                  secretArn:
                    description: The ARN of the secret in AWS Secrets Manager
                    type: string
                required:
                - secretArn
                type: object
              clientSecretJsonKey:
                description: |-
                  The JSON key within the secret that contains the client secret value
                  Read-only: reported by AWS after creation.
                maxLength: 128
                type: string
              clientSecretSource:
                description: |-
                  The source of the client secret
                  Read-only: reported by AWS after creation.
                enum:
                - MANAGED
                - EXTERNAL
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdTime:
                description: |-
                  The timestamp when the credential provider was created
                  Read-only: reported by AWS after creation.
                type: string
              credentialProviderArn:
                description: |-
                  The Amazon Resource Name (ARN) of the OAuth2 credential provider
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedTime:
                description: |-
                  The timestamp when the credential provider was last updated
                  Read-only: reported by AWS after creation.
                type: string
              oauth2ProviderConfigOutput:
                description: |-
                  The output configuration for the OAuth2 provider
                  Read-only: reported by AWS after creation.
                properties:
                  clientAuthenticationMethod:
                    description: The client authentication method used when authenticating
                      with the token endpoint
                    enum:
                    - CLIENT_SECRET_BASIC
                    - CLIENT_SECRET_POST
                    - AWS_IAM_ID_TOKEN_JWT
                    - PRIVATE_KEY_JWT
                    type: string
                  clientId:
                    maxLength: 256
                    type: string
                  oauthDiscovery:
                    description: BedrockAgentCoreOAuth2CredentialProviderOauth2Discovery
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      authorizationServerMetadata:
                        description: BedrockAgentCoreOAuth2CredentialProviderOauth2AuthorizationServerMetadata
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          authorizationEndpoint:
                            description: The authorization endpoint URL
                            type: string
                          issuer:
                            description: The issuer URL for the OAuth2 authorization
                              server
                            type: string
                          responseTypes:
                            description: The supported response types
                            items:
                              type: string
                            type: array
                          tokenEndpoint:
                            description: The token endpoint URL
                            type: string
                        required:
                        - authorizationEndpoint
                        - issuer
                        - tokenEndpoint
                        type: object
                      discoveryUrl:
                        description: The discovery URL for the OAuth2 provider
                        type: string
                    type: object
                  onBehalfOfTokenExchangeConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderOnBehalfOfTokenExchangeConfig
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      grantType:
                        description: The grant type for on-behalf-of token exchange
                        enum:
                        - TOKEN_EXCHANGE
                        - JWT_AUTHORIZATION_GRANT
                        type: string
                      tokenExchangeGrantTypeConfig:
                        description: BedrockAgentCoreOAuth2CredentialProviderTokenExchangeGrantTypeConfig
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          actorTokenContent:
                            description: The actor token content type
                            enum:
                            - NONE
                            - M2M
                            - AWS_IAM_ID_TOKEN_JWT
                            type: string
                          actorTokenScopes:
                            description: The actor token scopes. Only valid when ActorTokenContent
                              is M2M.
                            items:
                              type: string
                            type: array
                        required:
                        - actorTokenContent
                        type: object
                    required:
                    - grantType
                    type: object
                  privateEndpoint:
                    description: BedrockAgentCoreOAuth2CredentialProviderPrivateEndpoint
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      managedVpcResource:
                        description: BedrockAgentCoreOAuth2CredentialProviderManagedVpcResource
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          endpointIpAddressType:
                            description: The IP address type for the resource configuration
                              endpoint
                            enum:
                            - IPV4
                            - IPV6
                            type: string
                          routingDomain:
                            description: An intermediate publicly resolvable domain
                              used as the VPC Lattice resource configuration endpoint
                            maxLength: 255
                            type: string
                          securityGroupIds:
                            description: The security group IDs to associate with
                              the VPC Lattice resource gateway
                            items:
                              type: string
                            type: array
                          subnetIds:
                            description: The subnet IDs within the VPC where the VPC
                              Lattice resource gateway is placed
                            items:
                              type: string
                            type: array
                          tags:
                            additionalProperties:
                              type: string
                            description: Tags to apply to the managed VPC Lattice
                              resource gateway
                            type: object
                          vpcIdentifier:
                            description: The ID of the VPC that contains your private
                              resource
                            type: string
                        required:
                        - endpointIpAddressType
                        - subnetIds
                        - vpcIdentifier
                        type: object
                      selfManagedLatticeResource:
                        description: BedrockAgentCoreOAuth2CredentialProviderSelfManagedLatticeResource
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          resourceConfigurationIdentifier:
                            description: The ARN or ID of the VPC Lattice resource
                              configuration
                            maxLength: 2048
                            minLength: 20
                            type: string
                        required:
                        - resourceConfigurationIdentifier
                        type: object
                    type: object
                  privateEndpointOverrides:
                    description: |-
                      The list of private endpoint overrides for the OAuth2 provider. Each override maps a specific domain to a
                      private endpoint, enabling secure connectivity through VPC Lattice resource configurations.
                    items:
                      description: BedrockAgentCoreOAuth2CredentialProviderPrivateEndpointOverride
                        is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                      properties:
                        domain:
                          description: The domain to override with a private endpoint
                          maxLength: 253
                          minLength: 1
                          type: string
                        privateEndpoint:
                          description: BedrockAgentCoreOAuth2CredentialProviderPrivateEndpoint
                            is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                          properties:
                            managedVpcResource:
                              description: BedrockAgentCoreOAuth2CredentialProviderManagedVpcResource
                                is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                              properties:
                                endpointIpAddressType:
                                  description: The IP address type for the resource
                                    configuration endpoint
                                  enum:
                                  - IPV4
                                  - IPV6
                                  type: string
                                routingDomain:
                                  description: An intermediate publicly resolvable
                                    domain used as the VPC Lattice resource configuration
                                    endpoint
                                  maxLength: 255
                                  type: string
                                securityGroupIds:
                                  description: The security group IDs to associate
                                    with the VPC Lattice resource gateway
                                  items:
                                    type: string
                                  type: array
                                subnetIds:
                                  description: The subnet IDs within the VPC where
                                    the VPC Lattice resource gateway is placed
                                  items:
                                    type: string
                                  type: array
                                tags:
                                  additionalProperties:
                                    type: string
                                  description: Tags to apply to the managed VPC Lattice
                                    resource gateway
                                  type: object
                                vpcIdentifier:
                                  description: The ID of the VPC that contains your
                                    private resource
                                  type: string
                              required:
                              - endpointIpAddressType
                              - subnetIds
                              - vpcIdentifier
                              type: object
                            selfManagedLatticeResource:
                              description: BedrockAgentCoreOAuth2CredentialProviderSelfManagedLatticeResource
                                is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                              properties:
                                resourceConfigurationIdentifier:
                                  description: The ARN or ID of the VPC Lattice resource
                                    configuration
                                  maxLength: 2048
                                  minLength: 20
                                  type: string
                              required:
                              - resourceConfigurationIdentifier
                              type: object
                          type: object
                      required:
                      - domain
                      - privateEndpoint
                      type: object
                    type: array
                  privateKeyJwtConfig:
                    description: BedrockAgentCoreOAuth2CredentialProviderPrivateKeyJwtConfig
                      is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                    properties:
                      additionalHeaderClaims:
                        additionalProperties:
                          type: string
                        type: object
                      additionalPayloadClaims:
                        additionalProperties:
                          type: string
                        type: object
                      privateKeySource:
                        description: BedrockAgentCoreOAuth2CredentialProviderPrivateKeySource
                          is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                        properties:
                          kmsKeySource:
                            description: BedrockAgentCoreOAuth2CredentialProviderKmsKeySourceType
                              is a nested property type of AWS::BedrockAgentCore::OAuth2CredentialProvider.
                            properties:
                              kmsKeyArn:
                                description: The Amazon Resource Name (ARN) of the
                                  KMS key used to sign the JWT client assertion
                                maxLength: 2048
                                minLength: 1
                                type: string
                            required:
                            - kmsKeyArn
                            type: object
                        type: object
                      signingAlgorithm:
                        description: The algorithm used to sign the JWT client assertion
                        enum:
                        - RS256
                        - PS256
                        - ES256
                        type: string
                    type: object
                type: object
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the OAuth2 credential provider
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - READY
                - DELETING
                - DELETE_FAILED
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreonlineevaluationconfigs.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreOnlineEvaluationConfig
    listKind: BedrockAgentCoreOnlineEvaluationConfigList
    plural: bedrockagentcoreonlineevaluationconfigs
    singular: bedrockagentcoreonlineevaluationconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreOnlineEvaluationConfig manages AWS::BedrockAgentCore::OnlineEvaluationConfig through the AWS
          Cloud Control API. Resource Type definition for AWS::BedrockAgentCore::OnlineEvaluationConfig - Creates an
          online evaluation configuration for continuous monitoring of agent performance.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreOnlineEvaluationConfigSpec is the desired
              state of AWS::BedrockAgentCore::OnlineEvaluationConfig.
            properties:
              clusteringConfig:
                description: The configuration for clustering analysis of evaluation
                  results.
                properties:
                  frequencies:
                    description: The list of frequencies at which clustering reports
                      are generated.
                    items:
                      type: string
                    type: array
                required:
                - frequencies
                type: object
              dataSourceConfig:
                description: The data source configuration that specifies CloudWatch
                  log groups and service names to monitor.
                properties:
                  cloudWatchLogs:
                    description: BedrockAgentCoreOnlineEvaluationConfigCloudWatchLogsInputConfig
                      is a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                    properties:
                      logGroupNames:
                        description: The list of CloudWatch log group names to monitor
                          for agent traces.
                        items:
                          type: string
                        type: array
                      serviceNames:
                        description: The list of service names to filter traces within
                          the specified log groups.
                        items:
                          type: string
                        type: array
                    required:
                    - logGroupNames
                    - serviceNames
                    type: object
                required:
                - cloudWatchLogs
                type: object
              description:
                description: The description of the online evaluation configuration.
                maxLength: 200
                type: string
              evaluationExecutionRoleArn:
                description: The Amazon Resource Name (ARN) of the IAM role that grants
                  permissions for evaluation.
                maxLength: 2048
                minLength: 1
                type: string
              evaluators:
                description: The list of evaluators to apply during online evaluation.
                items:
                  description: BedrockAgentCoreOnlineEvaluationConfigEvaluatorReference
                    is a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                  properties:
                    evaluatorId:
                      description: The unique identifier of the evaluator.
                      type: string
                  required:
                  - evaluatorId
                  type: object
                type: array
              executionStatus:
                description: The execution status indicating whether the online evaluation
                  is currently running.
                enum:
                - ENABLED
                - DISABLED
                type: string
              insights:
                description: The list of insights to enable for failure analysis.
                items:
                  description: BedrockAgentCoreOnlineEvaluationConfigInsight is a
                    nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                  properties:
                    insightId:
                      description: The unique identifier of the insight.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - insightId
                  type: object
                type: array
              onlineEvaluationConfigName:
                description: |-
                  The name of the online evaluation configuration. Must be unique within your account.
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: onlineEvaluationConfigName is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              rule:
                description: The evaluation rule that defines sampling configuration,
                  filters, and session detection settings.
                properties:
                  filters:
                    description: The list of filters that determine which agent traces
                      should be included in the evaluation.
                    items:
                      description: BedrockAgentCoreOnlineEvaluationConfigFilter is
                        a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                      properties:
                        key:
                          description: The key or field name to filter on within the
                            agent trace data.
                          maxLength: 256
                          minLength: 1
                          type: string
                        operator:
                          description: The comparison operator to use for filtering.
                          enum:
                          - Equals
                          - NotEquals
                          - GreaterThan
                          - LessThan
                          - GreaterThanOrEqual
                          - LessThanOrEqual
                          - Contains
                          - NotContains
                          type: string
                        value:
                          description: BedrockAgentCoreOnlineEvaluationConfigFilterValue
                            is a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                          properties:
                            booleanValue:
                              description: The boolean value for true/false filtering
                                conditions.
                              type: boolean
                            doubleValue:
                              description: The numeric value for numerical filtering.
                              type: number
                            stringValue:
                              description: The string value for text-based filtering.
                              maxLength: 1024
                              type: string
                          type: object
                      required:
                      - key
                      - operator
                      - value
                      type: object
                    type: array
                  samplingConfig:
                    description: BedrockAgentCoreOnlineEvaluationConfigSamplingConfig
                      is a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                    properties:
                      samplingPercentage:
                        description: The percentage of agent traces to sample for
                          evaluation.
                        type: number
                    required:
                    - samplingPercentage
                    type: object
                  sessionConfig:
                    description: BedrockAgentCoreOnlineEvaluationConfigSessionConfig
                      is a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                    properties:
                      sessionTimeoutMinutes:
                        description: The number of minutes of inactivity after which
                          an agent session is considered complete.
                        format: int64
                        maximum: 1440
                        minimum: 1
                        type: integer
                    required:
                    - sessionTimeoutMinutes
                    type: object
                required:
                - samplingConfig
                type: object
              tags:
                description: A list of tags to assign to the online evaluation configuration.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - dataSourceConfig
            - evaluationExecutionRoleArn
            - onlineEvaluationConfigName
            - rule
            type: object
          status:
            description: BedrockAgentCoreOnlineEvaluationConfigStatus is the observed
              state of AWS::BedrockAgentCore::OnlineEvaluationConfig.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the online evaluation configuration was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              onlineEvaluationConfigArn:
                description: |-
                  The Amazon Resource Name (ARN) of the online evaluation configuration.
                  Read-only: reported by AWS after creation.
                type: string
              onlineEvaluationConfigId:
                description: |-
                  The unique identifier of the online evaluation configuration.
                  Read-only: reported by AWS after creation.
                type: string
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              outputConfig:
                description: |-
                  The configuration that specifies where evaluation results should be written.
                  Read-only: reported by AWS after creation.
                properties:
                  cloudWatchConfig:
                    description: BedrockAgentCoreOnlineEvaluationConfigCloudWatchOutputConfig
                      is a nested property type of AWS::BedrockAgentCore::OnlineEvaluationConfig.
                    properties:
                      logGroupName:
                        description: The CloudWatch log group name for evaluation
                          results.
                        type: string
                    type: object
                type: object
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The status of the online evaluation configuration.
                  Read-only: reported by AWS after creation.
                enum:
                - ACTIVE
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - DELETING
                type: string
              updatedAt:
                description: |-
                  The timestamp when the online evaluation configuration was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorepaymentconnectors.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCorePaymentConnector
    listKind: BedrockAgentCorePaymentConnectorList
    plural: bedrockagentcorepaymentconnectors
    singular: bedrockagentcorepaymentconnector
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCorePaymentConnector manages AWS::BedrockAgentCore::PaymentConnector through the AWS Cloud Control
          API. Resource Type definition for AWS::BedrockAgentCore::PaymentConnector
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCorePaymentConnectorSpec is the desired state
              of AWS::BedrockAgentCore::PaymentConnector.
            properties:
              connectorName:
                description: |-
                  The name of the payment connector
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: connectorName is immutable
                  rule: self == oldSelf
              connectorType:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                enum:
                - CoinbaseCDP
                - StripePrivy
                type: string
                x-kubernetes-validations:
                - message: connectorType is immutable
                  rule: self == oldSelf
              credentialProviderConfigurations:
                description: |-
                  The credential provider configurations for the connector. Required when ProvisionMode is MANUAL or not
                  specified. Empty for QUICK_CREATE until provisioning completes.
                items:
                  description: BedrockAgentCorePaymentConnectorCredentialsProviderConfiguration
                    is a nested property type of AWS::BedrockAgentCore::PaymentConnector.
                  properties:
                    coinbaseCDP:
                      description: BedrockAgentCorePaymentConnectorPaymentCredentialProviderConfiguration
                        is a nested property type of AWS::BedrockAgentCore::PaymentConnector.
                      properties:
                        credentialProviderArn:
                          description: The ARN of the payment credential provider
                          type: string
                      required:
                      - credentialProviderArn
                      type: object
                    stripePrivy:
                      description: BedrockAgentCorePaymentConnectorPaymentCredentialProviderConfiguration
                        is a nested property type of AWS::BedrockAgentCore::PaymentConnector.
                      properties:
                        credentialProviderArn:
                          description: The ARN of the payment credential provider
                          type: string
                      required:
                      - credentialProviderArn
                      type: object
                  type: object
                type: array
              description:
                description: A description of the payment connector
                maxLength: 4096
                type: string
              paymentManagerId:
                description: |-
                  The identifier of the parent payment manager
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: paymentManagerId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              provisionMode:
                description: |-
                  The provision mode for creating the connector. MANUAL requires CredentialProviderConfigurations; QUICK_CREATE
                  orchestrates OAuth consent and credential provisioning.
                  Create-only in AWS: changing it requires replacing the resource.
                enum:
                - MANUAL
                - QUICK_CREATE
                type: string
            required:
            - connectorName
            - connectorType
            - paymentManagerId
            type: object
          status:
            description: BedrockAgentCorePaymentConnectorStatus is the observed state
              of AWS::BedrockAgentCore::PaymentConnector.
            properties:
              authorizationUrl:
                description: |-
                  The URL the user must open to complete OAuth consent. Only present when ConnectorStatus is
                  PENDING_AUTHENTICATION.
                  Read-only: reported by AWS after creation.
                maxLength: 4096
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connectorCreatedAt:
                description: |-
                  The timestamp when the connector was created
                  Read-only: reported by AWS after creation.
                type: string
              connectorLastUpdatedAt:
                description: |-
                  The timestamp when the connector was last updated
                  Read-only: reported by AWS after creation.
                type: string
              connectorStatus:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - UPDATING
                - DELETING
                - READY
                - CREATE_FAILED
                - UPDATE_FAILED
                - DELETE_FAILED
                - AWS_MARKETPLACE_SUBSCRIPTION_REQUIRED
                - PENDING_AUTHENTICATION
                - PROVISIONING
                - AUTHENTICATION_EXPIRED
                - AUTHENTICATION_FAILED
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              paymentConnectorArn:
                description: |-
                  Synthetic ARN for the payment connector (used for engine resolution)
                  Read-only: reported by AWS after creation.
                type: string
              paymentConnectorId:
                description: |-
                  The unique identifier for the payment connector
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorepaymentcredentialproviders.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCorePaymentCredentialProvider
    listKind: BedrockAgentCorePaymentCredentialProviderList
    plural: bedrockagentcorepaymentcredentialproviders
    singular: bedrockagentcorepaymentcredentialprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCorePaymentCredentialProvider manages AWS::BedrockAgentCore::PaymentCredentialProvider through the
          AWS Cloud Control API. Resource Type definition for AWS::BedrockAgentCore::PaymentCredentialProvider
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCorePaymentCredentialProviderSpec is the desired
              state of AWS::BedrockAgentCore::PaymentCredentialProvider.
            properties:
              credentialProviderVendor:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                enum:
                - CoinbaseCDP
                - StripePrivy
                type: string
                x-kubernetes-validations:
                - message: credentialProviderVendor is immutable
                  rule: self == oldSelf
              name:
                description: |-
                  Unique name for the payment credential provider
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 128
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerConfigurationInput:
                description: BedrockAgentCorePaymentCredentialProviderPaymentProviderConfigurationInput
                  is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                properties:
                  coinbaseCdpConfiguration:
                    description: BedrockAgentCorePaymentCredentialProviderCoinbaseCdpConfigurationInput
                      is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                    properties:
                      apiKeyId:
                        description: The Coinbase CDP API key ID
                        maxLength: 512
                        minLength: 1
                        type: string
                      apiKeySecret:
                        description: The Coinbase CDP API key secret
                        maxLength: 2048
                        type: string
                      apiKeySecretConfig:
                        description: BedrockAgentCorePaymentCredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      apiKeySecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      walletSecret:
                        description: The Coinbase CDP wallet secret
                        maxLength: 2048
                        type: string
                      walletSecretConfig:
                        description: BedrockAgentCorePaymentCredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      walletSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - apiKeyId
                    type: object
                  stripePrivyConfiguration:
                    description: BedrockAgentCorePaymentCredentialProviderStripePrivyConfigurationInput
                      is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                    properties:
                      appId:
                        description: The app ID provided by Privy
                        maxLength: 512
                        minLength: 1
                        type: string
                      appSecret:
                        description: The app secret provided by Privy
                        maxLength: 2048
                        type: string
                      appSecretConfig:
                        description: BedrockAgentCorePaymentCredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      appSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      authorizationId:
                        description: The authorization ID for the Stripe Privy integration
                        maxLength: 512
                        minLength: 1
                        type: string
                      authorizationPrivateKey:
                        description: The authorization private key for the Stripe
                          Privy integration
                        maxLength: 4096
                        type: string
                      authorizationPrivateKeyConfig:
                        description: BedrockAgentCorePaymentCredentialProviderSecretReference
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          jsonKey:
                            description: The JSON key within the secret that contains
                              the credential value
                            maxLength: 128
                            minLength: 1
                            type: string
                          secretId:
                            description: The ID or ARN of the secret in AWS Secrets
                              Manager
                            maxLength: 2048
                            minLength: 1
                            type: string
                        required:
                        - jsonKey
                        - secretId
                        type: object
                      authorizationPrivateKeySource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - appId
                    - authorizationId
                    type: object
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: Tags for the payment credential provider
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - credentialProviderVendor
            - name
            type: object
          status:
            description: BedrockAgentCorePaymentCredentialProviderStatus is the observed
              state of AWS::BedrockAgentCore::PaymentCredentialProvider.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdTime:
                description: |-
                  The timestamp when the credential provider was created
                  Read-only: reported by AWS after creation.
                type: string
              credentialProviderArn:
                description: |-
                  The Amazon Resource Name (ARN) of the payment credential provider
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedTime:
                description: |-
                  The timestamp when the credential provider was last updated
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              providerConfigurationOutput:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  coinbaseCdpConfiguration:
                    description: BedrockAgentCorePaymentCredentialProviderCoinbaseCdpConfigurationOutput
                      is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                    properties:
                      apiKeyId:
                        description: The Coinbase CDP API key ID
                        maxLength: 512
                        minLength: 1
                        type: string
                      apiKeySecretArn:
                        description: BedrockAgentCorePaymentCredentialProviderSecretInfo
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          secretArn:
                            description: The ARN of the secret in AWS Secrets Manager
                            type: string
                        required:
                        - secretArn
                        type: object
                      apiKeySecretJsonKey:
                        description: The JSON key within the secret that contains
                          the API key secret value
                        maxLength: 128
                        type: string
                      apiKeySecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      walletSecretArn:
                        description: BedrockAgentCorePaymentCredentialProviderSecretInfo
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          secretArn:
                            description: The ARN of the secret in AWS Secrets Manager
                            type: string
                        required:
                        - secretArn
                        type: object
                      walletSecretJsonKey:
                        description: The JSON key within the secret that contains
                          the wallet secret value
                        maxLength: 128
                        type: string
                      walletSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - apiKeyId
                    - apiKeySecretArn
                    type: object
                  stripePrivyConfiguration:
                    description: BedrockAgentCorePaymentCredentialProviderStripePrivyConfigurationOutput
                      is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                    properties:
                      appId:
                        description: The app ID provided by Privy
                        maxLength: 512
                        minLength: 1
                        type: string
                      appSecretArn:
                        description: BedrockAgentCorePaymentCredentialProviderSecretInfo
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          secretArn:
                            description: The ARN of the secret in AWS Secrets Manager
                            type: string
                        required:
                        - secretArn
                        type: object
                      appSecretJsonKey:
                        description: The JSON key within the secret that contains
                          the app secret value
                        maxLength: 128
                        type: string
                      appSecretSource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                      authorizationId:
                        description: The authorization ID for the Stripe Privy integration
                        maxLength: 512
                        minLength: 1
                        type: string
                      authorizationPrivateKeyArn:
                        description: BedrockAgentCorePaymentCredentialProviderSecretInfo
                          is a nested property type of AWS::BedrockAgentCore::PaymentCredentialProvider.
                        properties:
                          secretArn:
                            description: The ARN of the secret in AWS Secrets Manager
                            type: string
                        required:
                        - secretArn
                        type: object
                      authorizationPrivateKeyJsonKey:
                        description: The JSON key within the secret that contains
                          the authorization private key value
                        maxLength: 128
                        type: string
                      authorizationPrivateKeySource:
                        enum:
                        - MANAGED
                        - EXTERNAL
                        type: string
                    required:
                    - appId
                    - appSecretArn
                    - authorizationId
                    - authorizationPrivateKeyArn
                    type: object
                type: object
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorepaymentmanagers.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCorePaymentManager
    listKind: BedrockAgentCorePaymentManagerList
    plural: bedrockagentcorepaymentmanagers
    singular: bedrockagentcorepaymentmanager
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCorePaymentManager manages AWS::BedrockAgentCore::PaymentManager through the AWS Cloud Control
          API. Resource Type definition for AWS::BedrockAgentCore::PaymentManager
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCorePaymentManagerSpec is the desired state of
              AWS::BedrockAgentCore::PaymentManager.
            properties:
              authorizerConfiguration:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                properties:
                  customJWTAuthorizer:
                    description: BedrockAgentCorePaymentManagerCustomJWTAuthorizerConfiguration
                      is a nested property type of AWS::BedrockAgentCore::PaymentManager.
                    properties:
                      allowedAudience:
                        items:
                          type: string
                        type: array
                      allowedClients:
                        items:
                          type: string
                        type: array
                      allowedScopes:
                        items:
                          type: string
                        type: array
                      customClaims:
                        items:
                          description: BedrockAgentCorePaymentManagerCustomClaimValidationType
                            is a nested property type of AWS::BedrockAgentCore::PaymentManager.
                          properties:
                            authorizingClaimMatchValue:
                              description: BedrockAgentCorePaymentManagerAuthorizingClaimMatchValueType
                                is a nested property type of AWS::BedrockAgentCore::PaymentManager.
                              properties:
                                claimMatchOperator:
                                  enum:
                                  - EQUALS
                                  - CONTAINS
                                  - CONTAINS_ANY
                                  type: string
                                claimMatchValue:
                                  description: BedrockAgentCorePaymentManagerClaimMatchValueType
                                    is a nested property type of AWS::BedrockAgentCore::PaymentManager.
                                  properties:
                                    matchValueString:
                                      maxLength: 255
                                      type: string
                                    matchValueStringList:
                                      items:
                                        type: string
                                      type: array
                                  type: object
                              required:
                              - claimMatchOperator
                              - claimMatchValue
                              type: object
                            inboundTokenClaimName:
                              maxLength: 255
                              minLength: 1
                              type: string
                            inboundTokenClaimValueType:
                              enum:
                              - STRING
                              - STRING_ARRAY
                              type: string
                          required:
                          - authorizingClaimMatchValue
                          - inboundTokenClaimName
                          - inboundTokenClaimValueType
                          type: object
                        type: array
                      discoveryUrl:
                        type: string
                    required:
                    - discoveryUrl
                    type: object
                required:
                - customJWTAuthorizer
                type: object
              authorizerType:
                description: 'Create-only in AWS: changing it requires replacing the
                  resource.'
                enum:
                - CUSTOM_JWT
                - AWS_IAM
                type: string
                x-kubernetes-validations:
                - message: authorizerType is immutable
                  rule: self == oldSelf
              description:
                description: A description of the payment manager
                maxLength: 4096
                type: string
              name:
                description: |-
                  The name of the payment manager
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              roleArn:
                description: The ARN of the IAM role for the payment manager
                maxLength: 2048
                minLength: 1
                type: string
              tags:
                description: Tags to assign to the payment manager
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - authorizerType
            - name
            - roleArn
            type: object
          status:
            description: BedrockAgentCorePaymentManagerStatus is the observed state
              of AWS::BedrockAgentCore::PaymentManager.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the payment manager was created
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  The timestamp when the payment manager was last updated
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              paymentManagerArn:
                description: |-
                  The Amazon Resource Name (ARN) of the payment manager
                  Read-only: reported by AWS after creation.
                type: string
              paymentManagerId:
                description: |-
                  The unique identifier for the payment manager
                  Read-only: reported by AWS after creation.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: 'Read-only: reported by AWS after creation.'
                enum:
                - CREATING
                - UPDATING
                - DELETING
                - READY
                - CREATE_FAILED
                - UPDATE_FAILED
                - DELETE_FAILED
                type: string
              workloadIdentityDetails:
                description: 'Read-only: reported by AWS after creation.'
                properties:
                  workloadIdentityArn:
                    maxLength: 1024
                    minLength: 1
                    type: string
                required:
                - workloadIdentityArn
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorepolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCorePolicy
    listKind: BedrockAgentCorePolicyList
    plural: bedrockagentcorepolicies
    singular: bedrockagentcorepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCorePolicy manages AWS::BedrockAgentCore::Policy through the AWS Cloud Control API. Resource Type
          definition for AWS::BedrockAgentCore::Policy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCorePolicySpec is the desired state of AWS::BedrockAgentCore::Policy.
            properties:
              definition:
                description: BedrockAgentCorePolicyPolicyDefinition is a nested property
                  type of AWS::BedrockAgentCore::Policy.
                properties:
                  cedar:
                    description: BedrockAgentCorePolicyCedarPolicy is a nested property
                      type of AWS::BedrockAgentCore::Policy.
                    properties:
                      statement:
                        description: The Cedar policy statement that defines the authorization
                          logic.
                        maxLength: 10000
                        minLength: 35
                        type: string
                    required:
                    - statement
                    type: object
                  policy:
                    description: BedrockAgentCorePolicyPolicyStatement is a nested
                      property type of AWS::BedrockAgentCore::Policy.
                    properties:
                      statement:
                        description: The policy statement.
                        maxLength: 10000
                        minLength: 35
                        type: string
                    required:
                    - statement
                    type: object
                type: object
              description:
                description: A human-readable description of the policy's purpose
                  and functionality.
                maxLength: 4096
                type: string
              enforcementMode:
                enum:
                - ACTIVE
                - LOG_ONLY
                type: string
              name:
                description: |-
                  The customer-assigned immutable name for the policy. Must be unique within the policy engine.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 48
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              policyEngineId:
                description: |-
                  The identifier of the policy engine which contains this policy.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 59
                minLength: 12
                type: string
                x-kubernetes-validations:
                - message: policyEngineId is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              validationMode:
                enum:
                - FAIL_ON_ANY_FINDINGS
                - IGNORE_ALL_FINDINGS
                type: string
            required:
            - definition
            - name
            - policyEngineId
            type: object
          status:
            description: BedrockAgentCorePolicyStatus is the observed state of AWS::BedrockAgentCore::Policy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the policy was created.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              policyArn:
                description: |-
                  The Amazon Resource Name (ARN) of the policy.
                  Read-only: reported by AWS after creation.
                maxLength: 203
                type: string
              policyId:
                description: |-
                  The unique identifier for the policy.
                  Read-only: reported by AWS after creation.
                maxLength: 59
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the policy.
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - ACTIVE
                - UPDATING
                - DELETING
                - CREATE_FAILED
                - UPDATE_FAILED
                - DELETE_FAILED
                type: string
              statusReasons:
                description: |-
                  Additional information about the policy status.
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              updatedAt:
                description: |-
                  The timestamp when the policy was last updated.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcorepolicyengines.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCorePolicyEngine
    listKind: BedrockAgentCorePolicyEngineList
    plural: bedrockagentcorepolicyengines
    singular: bedrockagentcorepolicyengine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCorePolicyEngine manages AWS::BedrockAgentCore::PolicyEngine through the AWS Cloud Control API.
          Resource Type definition for AWS::BedrockAgentCore::PolicyEngine
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCorePolicyEngineSpec is the desired state of
              AWS::BedrockAgentCore::PolicyEngine.
            properties:
              description:
                description: A human-readable description of the policy engine's purpose
                  and scope
                maxLength: 4096
                type: string
              encryptionKeyArn:
                description: |-
                  The ARN of the KMS key used to encrypt the policy engine data
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 2048
                type: string
              name:
                description: |-
                  The customer-assigned immutable name for the policy engine
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 48
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: A list of tags to assign to the policy engine.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            description: BedrockAgentCorePolicyEngineStatus is the observed state
              of AWS::BedrockAgentCore::PolicyEngine.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the policy engine was created
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              policyEngineArn:
                description: |-
                  The Amazon Resource Name (ARN) of the policy engine
                  Read-only: reported by AWS after creation.
                maxLength: 136
                type: string
              policyEngineId:
                description: |-
                  The unique identifier for the policy engine
                  Read-only: reported by AWS after creation.
                maxLength: 59
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The current status of the policy engine
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - ACTIVE
                - UPDATING
                - DELETING
                - CREATE_FAILED
                - UPDATE_FAILED
                - DELETE_FAILED
                type: string
              statusReasons:
                description: |-
                  Additional information about the policy engine status
                  Read-only: reported by AWS after creation.
                items:
                  type: string
                type: array
              updatedAt:
                description: |-
                  The timestamp when the policy engine was last updated
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreresourcepolicies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreResourcePolicy
    listKind: BedrockAgentCoreResourcePolicyList
    plural: bedrockagentcoreresourcepolicies
    singular: bedrockagentcoreresourcepolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreResourcePolicy manages AWS::BedrockAgentCore::ResourcePolicy through the AWS Cloud Control
          API. Resource Type definition for AWS::BedrockAgentCore::ResourcePolicy
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreResourcePolicySpec is the desired state of
              AWS::BedrockAgentCore::ResourcePolicy.
            properties:
              policy:
                description: The resource policy to create or update.
                maxLength: 20480
                minLength: 1
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              resourceArn:
                description: |-
                  The Amazon Resource Name (ARN) of the resource for which to create or update the resource policy.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 1011
                minLength: 20
                type: string
                x-kubernetes-validations:
                - message: resourceArn is immutable
                  rule: self == oldSelf
            required:
            - policy
            - resourceArn
            type: object
          status:
            description: BedrockAgentCoreResourcePolicyStatus is the observed state
              of AWS::BedrockAgentCore::ResourcePolicy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreruntimeendpoints.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreRuntimeEndpoint
    listKind: BedrockAgentCoreRuntimeEndpointList
    plural: bedrockagentcoreruntimeendpoints
    singular: bedrockagentcoreruntimeendpoint
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreRuntimeEndpoint manages AWS::BedrockAgentCore::RuntimeEndpoint through the AWS Cloud Control
          API. Resource definition for AWS::BedrockAgentCore::RuntimeEndpoint
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreRuntimeEndpointSpec is the desired state
              of AWS::BedrockAgentCore::RuntimeEndpoint.
            properties:
              agentRuntimeId:
                description: |-
                  The ID of the parent Agent Runtime (required for creation)
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: agentRuntimeId is immutable
                  rule: self == oldSelf
              agentRuntimeVersion:
                description: The version of the AgentCore Runtime to use for the endpoint.
                type: string
              description:
                description: The description of the AgentCore Runtime endpoint.
                maxLength: 256
                type: string
              name:
                description: |-
                  The name of the Agent Runtime Endpoint
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 48
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - agentRuntimeId
            - name
            type: object
          status:
            description: BedrockAgentCoreRuntimeEndpointStatus is the observed state
              of AWS::BedrockAgentCore::RuntimeEndpoint.
            properties:
              agentRuntimeArn:
                description: |-
                  The ARN of the Agent Runtime
                  Read-only: reported by AWS after creation.
                type: string
              agentRuntimeEndpointArn:
                description: |-
                  The Amazon Resource Name (ARN) of the AgentCore Runtime.
                  Read-only: reported by AWS after creation.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  The timestamp when the Agent Runtime Endpoint was created
                  Read-only: reported by AWS after creation.
                type: string
              failureReason:
                description: |-
                  The reason for failure if the endpoint is in a failed state
                  Read-only: reported by AWS after creation.
                type: string
              id:
                description: |-
                  The unique identifier of the AgentCore Runtime endpoint.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  The timestamp when the Agent Runtime Endpoint was last updated
                  Read-only: reported by AWS after creation.
                type: string
              liveVersion:
                description: |-
                  The Live version of the Agent Runtime
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  The status of the Agent Runtime Endpoint
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - READY
                - DELETING
                type: string
              targetVersion:
                description: |-
                  The target version of the AgentCore Runtime for the endpoint.
                  Read-only: reported by AWS after creation.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreruntimes.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreRuntime
    listKind: BedrockAgentCoreRuntimeList
    plural: bedrockagentcoreruntimes
    singular: bedrockagentcoreruntime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreRuntime manages AWS::BedrockAgentCore::Runtime through the AWS Cloud Control API. Resource
          Type definition for AWS::BedrockAgentCore::Runtime
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreRuntimeSpec is the desired state of AWS::BedrockAgentCore::Runtime.
            properties:
              agentRuntimeArtifact:
                description: The artifact of the agent
                properties:
                  codeConfiguration:
                    description: BedrockAgentCoreRuntimeCodeConfiguration is a nested
                      property type of AWS::BedrockAgentCore::Runtime.
                    properties:
                      code:
                        description: BedrockAgentCoreRuntimeCode is a nested property
                          type of AWS::BedrockAgentCore::Runtime.
                        properties:
                          s3:
                            description: BedrockAgentCoreRuntimeS3Location is a nested
                              property type of AWS::BedrockAgentCore::Runtime.
                            properties:
                              bucket:
                                description: S3 bucket name
                                type: string
                              prefix:
                                description: S3 object key prefix
                                maxLength: 1024
                                minLength: 1
                                type: string
                              versionId:
                                description: S3 object version ID
                                maxLength: 1024
                                type: string
                            required:
                            - bucket
                            - prefix
                            type: object
                        type: object
                      entryPoint:
                        items:
                          type: string
                        type: array
                      runtime:
                        enum:
                        - PYTHON_3_10
                        - PYTHON_3_11
                        - PYTHON_3_12
                        - PYTHON_3_13
                        - PYTHON_3_14
                        - NODE_22
                        type: string
                    required:
                    - code
                    - entryPoint
                    - runtime
                    type: object
                  containerConfiguration:
                    description: BedrockAgentCoreRuntimeContainerConfiguration is
                      a nested property type of AWS::BedrockAgentCore::Runtime.
                    properties:
                      containerUri:
                        maxLength: 1024
                        minLength: 1
                        type: string
                    required:
                    - containerUri
                    type: object
                type: object
              agentRuntimeName:
                description: |-
                  Name for a resource
                  Create-only in AWS: changing it requires replacing the resource.
                type: string
                x-kubernetes-validations:
                - message: agentRuntimeName is immutable
                  rule: self == oldSelf
              authorizerConfiguration:
                description: Authorizer configuration for the agent runtime
                properties:
                  customJWTAuthorizer:
                    description: BedrockAgentCoreRuntimeCustomJWTAuthorizerConfiguration
                      is a nested property type of AWS::BedrockAgentCore::Runtime.
                    properties:
                      allowedAudience:
                        items:
                          type: string
                        type: array
                      allowedClients:
                        items:
                          type: string
                        type: array
                      allowedScopes:
                        items:
                          type: string
                        type: array
                      allowedWorkloadConfiguration:
                        description: BedrockAgentCoreRuntimeAllowedWorkloadConfiguration
                          is a nested property type of AWS::BedrockAgentCore::Runtime.
                        properties:
                          hostingEnvironments:
                            items:
                              description: BedrockAgentCoreRuntimeHostingEnvironment
                                is a nested property type of AWS::BedrockAgentCore::Runtime.
                              properties:
                                arn:
                                  description: The ARN of the bedrock-agentcore hosting
                                    environment
                                  maxLength: 1011
                                  minLength: 20
                                  type: string
                              required:
                              - arn
                              type: object
                            type: array
                          workloadIdentities:
                            items:
                              type: string
                            type: array
                        type: object
                      customClaims:
                        items:
                          description: BedrockAgentCoreRuntimeCustomClaimValidationType
                            is a nested property type of AWS::BedrockAgentCore::Runtime.
                          properties:
                            authorizingClaimMatchValue:
                              description: BedrockAgentCoreRuntimeAuthorizingClaimMatchValueType
                                is a nested property type of AWS::BedrockAgentCore::Runtime.
                              properties:
                                claimMatchOperator:
                                  enum:
                                  - EQUALS
                                  - CONTAINS
                                  - CONTAINS_ANY
                                  type: string
                                claimMatchValue:
                                  description: BedrockAgentCoreRuntimeClaimMatchValueType
                                    is a nested property type of AWS::BedrockAgentCore::Runtime.
                                  properties:
                                    matchValueString:
                                      type: string
                                    matchValueStringList:
                                      items:
                                        type: string
                                      type: array
                                  type: object
                              required:
                              - claimMatchOperator
                              - claimMatchValue
                              type: object
                            inboundTokenClaimName:
                              type: string
                            inboundTokenClaimValueType:
                              enum:
                              - STRING
                              - STRING_ARRAY
                              type: string
                          required:
                          - authorizingClaimMatchValue
                          - inboundTokenClaimName
                          - inboundTokenClaimValueType
                          type: object
                        type: array
                      discoveryUrl:
                        type: string
                      privateEndpoint:
                        x-kubernetes-preserve-unknown-fields: true
                      privateEndpointOverrides:
                        items:
                          description: BedrockAgentCoreRuntimePrivateEndpointOverride
                            is a nested property type of AWS::BedrockAgentCore::Runtime.
                          properties:
                            domain:
                              description: The domain to override
                              maxLength: 253
                              minLength: 1
                              type: string
                            privateEndpoint:
                              x-kubernetes-preserve-unknown-fields: true
                          required:
                          - domain
                          - privateEndpoint
                          type: object
                        type: array
                    required:
                    - discoveryUrl
                    type: object
                type: object
              capacityProviderConfiguration:
                description: Capacity provider configuration for the agent runtime
                properties:
                  capacityProviderArn:
                    maxLength: 2048
                    type: string
                required:
                - capacityProviderArn
                type: object
              description:
                description: Description of the resource
                maxLength: 1200
                type: string
              environmentVariables:
                additionalProperties:
                  type: string
                description: Environment variables for the agent runtime
                type: object
              filesystemConfigurations:
                description: Filesystem configurations for the agent runtime
                items:
                  description: BedrockAgentCoreRuntimeFilesystemConfiguration is a
                    nested property type of AWS::BedrockAgentCore::Runtime.
                  properties:
                    capacityProviderVolume:
                      description: BedrockAgentCoreRuntimeCapacityProviderVolumeConfiguration
                        is a nested property type of AWS::BedrockAgentCore::Runtime.
                      properties:
                        mountPath:
                          maxLength: 200
                          minLength: 6
                          type: string
                        volumeName:
                          maxLength: 48
                          minLength: 1
                          type: string
                      required:
                      - mountPath
                      - volumeName
                      type: object
                    efsAccessPoint:
                      description: BedrockAgentCoreRuntimeEfsAccessPointConfiguration
                        is a nested property type of AWS::BedrockAgentCore::Runtime.
                      properties:
                        accessPointArn:
                          maxLength: 128
                          type: string
                        mountPath:
                          maxLength: 200
                          minLength: 6
                          type: string
                      required:
                      - accessPointArn
                      - mountPath
                      type: object
                    s3FilesAccessPoint:
                      description: BedrockAgentCoreRuntimeS3FilesAccessPointConfiguration
                        is a nested property type of AWS::BedrockAgentCore::Runtime.
                      properties:
                        accessPointArn:
                          maxLength: 256
                          type: string
                        mountPath:
                          maxLength: 200
                          minLength: 6
                          type: string
                      required:
                      - accessPointArn
                      - mountPath
                      type: object
                    sessionStorage:
                      description: BedrockAgentCoreRuntimeSessionStorageConfiguration
                        is a nested property type of AWS::BedrockAgentCore::Runtime.
                      properties:
                        mountPath:
                          maxLength: 200
                          minLength: 6
                          type: string
                      required:
                      - mountPath
                      type: object
                  type: object
                type: array
              lifecycleConfiguration:
                description: Lifecycle configuration for managing runtime sessions
                properties:
                  idleRuntimeSessionTimeout:
                    description: Timeout in seconds for idle runtime sessions
                    format: int64
                    maximum: 1209600
                    minimum: 60
                    type: integer
                  maxLifetime:
                    description: Maximum lifetime in seconds for runtime sessions
                    format: int64
                    maximum: 1209600
                    minimum: 60
                    type: integer
                type: object
              networkConfiguration:
                description: Network access configuration for the Agent
                properties:
                  networkMode:
                    enum:
                    - PUBLIC
                    - VPC
                    type: string
                  networkModeConfig:
                    description: BedrockAgentCoreRuntimeVpcConfig is a nested property
                      type of AWS::BedrockAgentCore::Runtime.
                    properties:
                      securityGroups:
                        items:
                          type: string
                        type: array
                      subnets:
                        items:
                          type: string
                        type: array
                    required:
                    - securityGroups
                    - subnets
                    type: object
                required:
                - networkMode
                type: object
              protocolConfiguration:
                description: Protocol configuration for the agent runtime
                enum:
                - MCP
                - HTTP
                - A2A
                - AGUI
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              requestHeaderConfiguration:
                description: Configuration for HTTP request headers
                properties:
                  requestHeaderAllowlist:
                    items:
                      type: string
                    type: array
                type: object
              roleArn:
                description: Amazon Resource Name (ARN) of an IAM role
                type: string
              tags:
                additionalProperties:
                  type: string
                type: object
            required:
            - agentRuntimeArtifact
            - agentRuntimeName
            - roleArn
            type: object
          status:
            description: BedrockAgentCoreRuntimeStatus is the observed state of AWS::BedrockAgentCore::Runtime.
            properties:
              agentRuntimeArn:
                description: |-
                  The Amazon Resource Name(ARN) that uniquely identifies the Agent
                  Read-only: reported by AWS after creation.
                maxLength: 2048
                type: string
              agentRuntimeId:
                description: |-
                  Identifier for a resource
                  Read-only: reported by AWS after creation.
                type: string
              agentRuntimeVersion:
                description: |-
                  Version of the Agent
                  Read-only: reported by AWS after creation.
                maxLength: 5
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdAt:
                description: |-
                  Timestamp when the Agent was created
                  Read-only: reported by AWS after creation.
                type: string
              failureReason:
                description: |-
                  The reason for failure if the agent is in a failed state.
                  Read-only: reported by AWS after creation.
                type: string
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedAt:
                description: |-
                  When resource was last updated
                  Read-only: reported by AWS after creation.
                type: string
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              status:
                description: |-
                  Current status of the agent
                  Read-only: reported by AWS after creation.
                enum:
                - CREATING
                - CREATE_FAILED
                - UPDATING
                - UPDATE_FAILED
                - READY
                - DELETING
                type: string
              workloadIdentityDetails:
                description: |-
                  Workload identity details for the agent
                  Read-only: reported by AWS after creation.
                properties:
                  workloadIdentityArn:
                    maxLength: 1024
                    minLength: 1
                    type: string
                required:
                - workloadIdentityArn
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: bedrockagentcoreworkloadidentities.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: BedrockAgentCoreWorkloadIdentity
    listKind: BedrockAgentCoreWorkloadIdentityList
    plural: bedrockagentcoreworkloadidentities
    singular: bedrockagentcoreworkloadidentity
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.identifier
      name: Identifier
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BedrockAgentCoreWorkloadIdentity manages AWS::BedrockAgentCore::WorkloadIdentity through the AWS Cloud Control
          API. Definition of AWS::BedrockAgentCore::WorkloadIdentity Resource Type
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BedrockAgentCoreWorkloadIdentitySpec is the desired state
              of AWS::BedrockAgentCore::WorkloadIdentity.
            properties:
              allowedResourceOauth2ReturnUrls:
                description: The list of allowed OAuth2 return URLs for resources
                  associated with this workload identity.
                items:
                  type: string
                type: array
              name:
                description: |-
                  The name of the workload identity. The name must be unique within your account.
                  Create-only in AWS: changing it requires replacing the resource.
                maxLength: 255
                minLength: 3
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  default provider, then the operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                description: An array of key-value pairs to apply to this resource.
                items:
                  description: CFNTag is the CloudFormation Tag shape shared by generated
                    kinds.
                  properties:
                    key:
                      minLength: 1
                      type: string
                    value:
                      type: string
                  required:
                  - key
                  - value
                  type: object
                type: array
            required:
            - name
            type: object
          status:
            description: BedrockAgentCoreWorkloadIdentityStatus is the observed state
              of AWS::BedrockAgentCore::WorkloadIdentity.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              createdTime:
                description: |-
                  The timestamp when the workload identity was created.
                  Read-only: reported by AWS after creation.
                type: number
              identifier:
                description: Identifier is the Cloud Control primary identifier of
                  the resource.
                type: string
              lastSyncTime:
                format: date-time
                type: string
              lastUpdatedTime:
                description: |-
                  The timestamp when the workload identity was last updated.
                  Read-only: reported by AWS after creation.
                type: number
              observedGeneration:
                format: int64
                type: integer
              operation:
                description: Operation is the in-flight operation (CREATE, UPDATE,
                  DELETE).
                type: string
              operationStatus:
                description: OperationStatus is the last reported operation status.
                type: string
              requestToken:
                description: RequestToken tracks an in-flight asynchronous operation.
                type: string
              workloadIdentityArn:
                description: |-
                  The Amazon Resource Name (ARN) of the workload identity.
                  Read-only: reported by AWS after creation.
                maxLength: 1024
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
