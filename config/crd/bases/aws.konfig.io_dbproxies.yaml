---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: dbproxies.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: DBProxy
    listKind: DBProxyList
    plural: dbproxies
    singular: dbproxy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.dbProxyArn
      name: ARN
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DBProxy is the Schema for managing RDS DB Proxies.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DBProxySpec defines the desired state of an RDS Proxy.
            properties:
              auth:
                description: Auth is the authorization configuration for the proxy.
                items:
                  description: DBProxyAuthConfig defines an authentication configuration
                    for an RDS Proxy.
                  properties:
                    authScheme:
                      description: AuthScheme is the authentication scheme (SECRETS).
                      type: string
                    description:
                      description: Description is an optional description of this
                        auth config.
                      type: string
                    iamAuth:
                      description: IAMAuth enables or disables IAM authentication.
                      enum:
                      - DISABLED
                      - REQUIRED
                      - ALLOWED
                      type: string
                    secretArn:
                      description: SecretARN is the ARN of the Secrets Manager secret
                        holding the credentials.
                      type: string
                  type: object
                minItems: 1
                type: array
              dbProxyName:
                description: DBProxyName is the name of the proxy. Immutable after
                  creation.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: dbProxyName is immutable
                  rule: self == oldSelf
              debugLogging:
                description: DebugLogging enables enhanced logging for the proxy.
                type: boolean
              engineFamily:
                description: EngineFamily is the database engine family (MYSQL or
                  POSTGRESQL).
                enum:
                - MYSQL
                - POSTGRESQL
                - SQLSERVER
                type: string
                x-kubernetes-validations:
                - message: engineFamily is immutable
                  rule: self == oldSelf
              idleClientTimeout:
                description: IdleClientTimeout is the number of seconds a connection
                  can be inactive before being closed.
                format: int32
                type: integer
              requireTls:
                description: RequireTLS requires TLS for all connections to the proxy.
                type: boolean
              roleArn:
                description: RoleARN is the ARN of the IAM role the proxy uses to
                  access Secrets Manager.
                minLength: 1
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are AWS resource tags to apply.
                type: object
              vpcSecurityGroupIds:
                description: VPCSecurityGroupIDs are optional security groups for
                  the proxy.
                items:
                  type: string
                type: array
              vpcSubnetIds:
                description: VPCSubnetIDs are the VPC subnet IDs for the proxy.
                items:
                  type: string
                minItems: 1
                type: array
            required:
            - auth
            - dbProxyName
            - engineFamily
            - roleArn
            - vpcSubnetIds
            type: object
          status:
            description: DBProxyStatus defines the observed state of DBProxy.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dbProxyArn:
                description: DBProxyARN is the ARN of the RDS proxy.
                type: string
              endpoint:
                description: Endpoint is the proxy endpoint for client connections.
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
              status:
                description: Status is the current state of the proxy.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
