---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: gluetriggers.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: GlueTrigger
    listKind: GlueTriggerList
    plural: gluetriggers
    singular: gluetrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.triggerName
      name: Trigger
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GlueTrigger is the Schema for managing Glue triggers.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GlueTriggerSpec defines the desired state of a Glue trigger.
            properties:
              actions:
                description: Actions are initiated when the trigger fires.
                items:
                  description: GlueTriggerAction is one action initiated by a trigger.
                  properties:
                    arguments:
                      additionalProperties:
                        type: string
                      description: |-
                        Arguments are job arguments that replace the job's default arguments
                        for this run.
                      type: object
                    jobName:
                      description: JobName is the name of the Glue job to run.
                      minLength: 1
                      type: string
                  required:
                  - jobName
                  type: object
                minItems: 1
                type: array
              description:
                description: Description is a description of the trigger.
                type: string
              name:
                description: Name is the name of the trigger. Immutable after creation.
                maxLength: 255
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: name is immutable
                  rule: self == oldSelf
              predicate:
                description: Predicate defines when a CONDITIONAL trigger fires.
                properties:
                  conditions:
                    description: Conditions determine when the trigger fires.
                    items:
                      description: GlueTriggerCondition is one condition of a CONDITIONAL
                        trigger's predicate.
                      properties:
                        crawlerName:
                          description: CrawlerName is the crawler this condition applies
                            to.
                          type: string
                        jobName:
                          description: JobName is the job whose runs this condition
                            applies to.
                          type: string
                        logicalOperator:
                          description: LogicalOperator is the comparison operator;
                            only EQUALS is supported.
                          enum:
                          - EQUALS
                          type: string
                        state:
                          description: |-
                            State is the job/crawler state the trigger listens for
                            (SUCCEEDED, STOPPED, FAILED, TIMEOUT for jobs).
                          type: string
                      type: object
                    type: array
                  logical:
                    description: 'Logical is how multiple conditions combine: AND
                      or ANY.'
                    enum:
                    - AND
                    - ANY
                    type: string
                type: object
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              schedule:
                description: |-
                  Schedule is a cron expression for SCHEDULED triggers,
                  e.g. cron(15 12 * * ? *).
                type: string
              startOnCreation:
                description: StartOnCreation activates SCHEDULED/CONDITIONAL triggers
                  when created.
                type: boolean
              tags:
                additionalProperties:
                  type: string
                description: Tags are AWS resource tags to apply at creation.
                type: object
              type:
                description: Type is the trigger type. Immutable after creation.
                enum:
                - SCHEDULED
                - CONDITIONAL
                - ON_DEMAND
                type: string
                x-kubernetes-validations:
                - message: type is immutable
                  rule: self == oldSelf
            required:
            - actions
            - name
            - type
            type: object
          status:
            description: GlueTriggerStatus defines the observed state of GlueTrigger.
            properties:
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
              state:
                description: State is the current trigger state reported by AWS.
                type: string
              triggerName:
                description: TriggerName is the name of the trigger in AWS.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
