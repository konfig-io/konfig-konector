---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: elasticachereplicationgroups.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: ElastiCacheReplicationGroup
    listKind: ElastiCacheReplicationGroupList
    plural: elasticachereplicationgroups
    singular: elasticachereplicationgroup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.primaryEndpoint
      name: Primary-Endpoint
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ElastiCacheReplicationGroup is the Schema for managing ElastiCache
          Replication Groups.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ElastiCacheReplicationGroupSpec defines the desired state
              of an ElastiCache Replication Group.
            properties:
              atRestEncryption:
                description: AtRestEncryption enables encryption at rest.
                type: boolean
              authToken:
                description: |-
                  AuthToken is the literal auth token value.
                  Deprecated: use authTokenRef instead — literal tokens are readable by
                  anyone with get access on the CR and are stored unencrypted in etcd.
                type: string
              authTokenRef:
                description: |-
                  AuthTokenRef references a Kubernetes Secret containing the auth token
                  for transit-encrypted clusters.
                properties:
                  key:
                    description: Key is the key within the Secret whose value to use.
                    type: string
                  name:
                    description: Name of the Kubernetes Secret.
                    type: string
                required:
                - key
                - name
                type: object
              automaticFailover:
                description: AutomaticFailover enables automatic failover to a read
                  replica on primary failure.
                type: boolean
              cacheNodeType:
                description: CacheNodeType is the instance type (e.g. cache.t3.micro).
                minLength: 1
                type: string
              description:
                description: Description is a human-readable description of the replication
                  group.
                minLength: 1
                type: string
              engine:
                description: 'Engine is the cache engine: redis or valkey.'
                enum:
                - redis
                - valkey
                type: string
                x-kubernetes-validations:
                - message: engine is immutable
                  rule: self == oldSelf
              engineVersion:
                description: EngineVersion is the cache engine version (e.g. "7.0").
                type: string
              numCacheClusters:
                description: NumCacheClusters is the number of cache clusters (nodes)
                  in the replication group.
                format: int32
                maximum: 6
                minimum: 1
                type: integer
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              replicationGroupId:
                description: ReplicationGroupID is the identifier for the replication
                  group. Immutable after creation.
                maxLength: 40
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: replicationGroupId is immutable
                  rule: self == oldSelf
              securityGroupRefs:
                description: SecurityGroupRefs is the list of security group references.
                items:
                  description: SecurityGroupRef references either a managed SecurityGroup
                    CR or a direct Security Group ID.
                  properties:
                    id:
                      description: ID is a direct AWS Security Group ID (e.g. sg-0abc1234).
                      type: string
                    name:
                      description: Name of a SecurityGroup CR in the same namespace.
                      type: string
                  type: object
                type: array
              snapshotRetentionLimit:
                description: SnapshotRetentionLimit is the number of days to retain
                  automatic snapshots.
                format: int32
                maximum: 35
                minimum: 0
                type: integer
              subnetGroupRef:
                description: SubnetGroupRef is the name of an ElastiCacheSubnetGroup
                  CR in the same namespace.
                type: string
              tags:
                additionalProperties:
                  type: string
                description: Tags are AWS resource tags to apply.
                type: object
              transitEncryption:
                description: TransitEncryption enables in-transit encryption (TLS).
                type: boolean
            required:
            - cacheNodeType
            - description
            - engine
            - replicationGroupId
            type: object
          status:
            description: ElastiCacheReplicationGroupStatus defines the observed state
              of ElastiCacheReplicationGroup.
            properties:
              arn:
                description: ARN is the Amazon Resource Name of the replication group.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
              primaryEndpoint:
                description: PrimaryEndpoint is the primary node endpoint.
                type: string
              readerEndpoint:
                description: ReaderEndpoint is the reader endpoint for read replicas.
                type: string
              status:
                description: Status is the current replication group status.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
