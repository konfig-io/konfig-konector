# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/aws.konfig.io_iamroles.yaml
- bases/aws.konfig.io_iampolicies.yaml
- bases/aws.konfig.io_iampolicyattachments.yaml
- bases/aws.konfig.io_iamrolepolicies.yaml
- bases/aws.konfig.io_podidentityassociations.yaml
- bases/aws.konfig.io_hostedzones.yaml
- bases/aws.konfig.io_recordsets.yaml
- bases/aws.konfig.io_healthchecks.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- path: patches/webhook_in_iamroles.yaml
#- path: patches/webhook_in_iampolicies.yaml
#- path: patches/webhook_in_iampolicyattachments.yaml
#- path: patches/webhook_in_iamrolepolicies.yaml
#- path: patches/webhook_in_podidentityassociations.yaml
#- path: patches/webhook_in_hostedzones.yaml
#- path: patches/webhook_in_recordsets.yaml
#- path: patches/webhook_in_healthchecks.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- path: patches/cainjection_in_iamroles.yaml
#- path: patches/cainjection_in_iampolicies.yaml
#- path: patches/cainjection_in_iampolicyattachments.yaml
#- path: patches/cainjection_in_iamrolepolicies.yaml
#- path: patches/cainjection_in_podidentityassociations.yaml
#- path: patches/cainjection_in_hostedzones.yaml
#- path: patches/cainjection_in_recordsets.yaml
#- path: patches/cainjection_in_healthchecks.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.

#configurations:
#- kustomizeconfig.yaml
