# All resources are created in the release namespace. Values are for the
# scratch account used in e2e; override accountId/region for another account.
accountId: "367165277836"
region: us-east-1
amiId: ami-081b0a6eac00b4f53
# Name of an AWSProvider to scope every smoke resource to (multi-account
# proof: all 93 kinds reconcile under the provider's assumed role). Empty
# uses the operator's own credentials.
providerRef: ""
