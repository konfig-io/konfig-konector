---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: sqsqueues.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: SQSQueue
    listKind: SQSQueueList
    plural: sqsqueues
    singular: sqsqueue
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.queueArn
      name: Queue-ARN
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SQSQueue is the Schema for managing SQS Queues.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SQSQueueSpec defines the desired state of an SQS Queue.
            properties:
              delaySeconds:
                description: DelaySeconds is the time (seconds) messages are delayed
                  before delivery.
                format: int32
                maximum: 900
                minimum: 0
                type: integer
              fifo:
                description: FIFO enables FIFO queue ordering and exactly-once processing.
                type: boolean
              kmsKeyId:
                description: KMSKeyID is the KMS key ARN or alias for SSE-KMS encryption.
                type: string
              messageRetentionPeriod:
                description: MessageRetentionPeriod is how long (seconds) messages
                  are retained.
                format: int32
                maximum: 1209600
                minimum: 60
                type: integer
              policy:
                description: Policy is a JSON resource policy to attach to the queue.
                type: string
              queueName:
                description: |-
                  QueueName is the name of the queue. Immutable after creation.
                  FIFO queues must end with ".fifo".
                maxLength: 80
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: queueName is immutable
                  rule: self == oldSelf
              receiveMessageWaitTime:
                description: ReceiveMessageWaitTime enables long polling when > 0
                  (seconds).
                format: int32
                maximum: 20
                minimum: 0
                type: integer
              redrivePolicy:
                description: RedrivePolicy configures a dead-letter queue.
                properties:
                  deadLetterQueueRef:
                    description: DeadLetterQueueRef is the name of an SQSQueue CR
                      in the same namespace.
                    type: string
                  maxReceiveCount:
                    description: |-
                      MaxReceiveCount is the number of times a message can be received before
                      it is sent to the dead-letter queue.
                    format: int32
                    minimum: 1
                    type: integer
                required:
                - deadLetterQueueRef
                - maxReceiveCount
                type: object
              tags:
                additionalProperties:
                  type: string
                description: Tags are AWS resource tags to apply.
                type: object
              visibilityTimeout:
                description: VisibilityTimeout is the duration (seconds) that a received
                  message is hidden.
                format: int32
                maximum: 43200
                minimum: 0
                type: integer
            required:
            - queueName
            type: object
          status:
            description: SQSQueueStatus defines the observed state of SQSQueue.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
              queueArn:
                description: QueueARN is the ARN of the SQS queue.
                type: string
              queueUrl:
                description: QueueURL is the URL of the SQS queue.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
