---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: userpoolclients.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: UserPoolClient
    listKind: UserPoolClientList
    plural: userpoolclients
    singular: userpoolclient
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.clientId
      name: Client-ID
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: UserPoolClient is the Schema for managing Cognito User Pool App
          Clients.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: UserPoolClientSpec defines the desired state of a Cognito
              User Pool Client.
            properties:
              accessTokenValidity:
                description: AccessTokenValidity is the access token validity in minutes
                  (1-86400).
                format: int32
                type: integer
              allowedOAuthFlows:
                description: AllowedOAuthFlows are the allowed OAuth flows.
                items:
                  type: string
                type: array
              allowedOAuthScopes:
                description: AllowedOAuthScopes are the allowed OAuth scopes.
                items:
                  type: string
                type: array
              callbackUrls:
                description: CallbackURLs are the allowed callback URLs.
                items:
                  type: string
                type: array
              clientName:
                description: ClientName is the name of the app client.
                maxLength: 128
                minLength: 1
                type: string
              explicitAuthFlows:
                description: ExplicitAuthFlows are the authentication flows to allow.
                items:
                  type: string
                type: array
              generateSecret:
                description: GenerateSecret generates a client secret.
                type: boolean
              idTokenValidity:
                description: IdTokenValidity is the ID token validity in minutes (1-86400).
                format: int32
                type: integer
              logoutUrls:
                description: LogoutURLs are the allowed sign-out URLs.
                items:
                  type: string
                type: array
              refreshTokenValidity:
                description: RefreshTokenValidity is the refresh token validity in
                  days (1-3650).
                format: int32
                type: integer
              supportedIdentityProviders:
                description: SupportedIdentityProviders are the supported identity
                  providers.
                items:
                  type: string
                type: array
              userPoolRef:
                description: UserPoolRef references the user pool.
                properties:
                  name:
                    description: Name of a UserPool CR in the same namespace.
                    type: string
                  userPoolId:
                    description: UserPoolID is a direct AWS Cognito user pool ID.
                    type: string
                type: object
            required:
            - clientName
            - userPoolRef
            type: object
          status:
            description: UserPoolClientStatus defines the observed state of UserPoolClient.
            properties:
              clientId:
                description: ClientID is the AWS Cognito app client ID.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
