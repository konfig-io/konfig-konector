---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cloudfrontdistributions.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: CloudFrontDistribution
    listKind: CloudFrontDistributionList
    plural: cloudfrontdistributions
    singular: cloudfrontdistribution
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.distributionId
      name: ID
      type: string
    - jsonPath: .status.domainName
      name: Domain
      type: string
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudFrontDistribution is the Schema for managing CloudFront
          distributions.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudFrontDistributionSpec defines the desired state of a
              CloudFront Distribution.
            properties:
              aliases:
                description: Aliases are the CNAMEs (alternate domain names) for this
                  distribution.
                items:
                  type: string
                type: array
              comment:
                description: Comment is a description for the distribution.
                type: string
              defaultCacheBehavior:
                description: DefaultCacheBehavior is the default cache behavior configuration.
                properties:
                  allowedMethods:
                    description: AllowedMethods are the HTTP methods CloudFront processes
                      and forwards to the origin.
                    items:
                      type: string
                    type: array
                  cachePolicyId:
                    description: CachePolicyID is the ID of the cache policy attached
                      to the default cache behavior.
                    type: string
                  targetOriginId:
                    description: TargetOriginID specifies the value of the ID for
                      the origin that CloudFront routes requests to.
                    type: string
                  viewerProtocolPolicy:
                    description: ViewerProtocolPolicy defines how you want CloudFront
                      to serve requests.
                    enum:
                    - allow-all
                    - https-only
                    - redirect-to-https
                    type: string
                required:
                - targetOriginId
                - viewerProtocolPolicy
                type: object
              enabled:
                description: Enabled controls whether the distribution is enabled.
                type: boolean
              origins:
                description: Origins is the list of origins for this distribution.
                items:
                  description: CFOrigin defines a CloudFront distribution origin.
                  properties:
                    domainName:
                      description: DomainName is the DNS domain name of the origin.
                      type: string
                    id:
                      description: ID is the unique identifier for the origin.
                      type: string
                    originPath:
                      description: OriginPath is an optional element that causes CloudFront
                        to request content from a specific directory.
                      type: string
                  required:
                  - domainName
                  - id
                  type: object
                minItems: 1
                type: array
              priceClass:
                description: PriceClass is the price class for this distribution.
                enum:
                - PriceClass_100
                - PriceClass_200
                - PriceClass_All
                type: string
              providerRef:
                description: |-
                  ProviderRef selects the AWSProvider (account/region) this resource is
                  reconciled against. Defaults to the namespace annotation, then the
                  operator's own credentials.
                properties:
                  name:
                    description: Name of a cluster-scoped AWSProvider.
                    minLength: 1
                    type: string
                  region:
                    description: Region overrides the provider's region for this resource
                      only.
                    type: string
                required:
                - name
                type: object
              tags:
                additionalProperties:
                  type: string
                description: Tags are metadata tags for the distribution.
                type: object
            required:
            - defaultCacheBehavior
            - origins
            type: object
          status:
            description: CloudFrontDistributionStatus defines the observed state of
              CloudFrontDistribution.
            properties:
              awsProvider:
                description: AWSProvider confirms the account and region this resource
                  was reconciled against.
                properties:
                  accountId:
                    description: AccountID the resource lives in.
                    type: string
                  name:
                    description: Name of the AWSProvider used; empty when the operator's
                      own credentials were used.
                    type: string
                  region:
                    description: Region the resource lives in (empty for global services).
                    type: string
                type: object
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              distributionArn:
                description: DistributionARN is the ARN of the distribution.
                type: string
              distributionId:
                description: DistributionID is the ID of the distribution.
                type: string
              domainName:
                description: DomainName is the CloudFront domain name for the distribution.
                type: string
              etag:
                description: ETag is the ETag for the distribution (required for updates
                  and deletes).
                type: string
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
              status:
                description: Status is the current deployment status of the distribution.
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
