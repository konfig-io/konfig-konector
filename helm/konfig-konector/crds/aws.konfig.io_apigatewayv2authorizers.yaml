---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: apigatewayv2authorizers.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: APIGatewayV2Authorizer
    listKind: APIGatewayV2AuthorizerList
    plural: apigatewayv2authorizers
    singular: apigatewayv2authorizer
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.authorizerId
      name: Authorizer-ID
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: APIGatewayV2Authorizer is the Schema for managing API Gateway
          v2 authorizers.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: APIGatewayV2AuthorizerSpec defines the desired state of an
              API Gateway v2 authorizer.
            properties:
              apiRef:
                description: APIRef references the APIGatewayV2API this authorizer
                  belongs to.
                properties:
                  apiId:
                    description: APIID is a direct AWS API Gateway API ID, bypassing
                      CR lookup.
                    type: string
                  name:
                    description: Name of the API CR in the same namespace.
                    type: string
                type: object
              authorizerCredentialsArn:
                description: |-
                  AuthorizerCredentialsARN is the IAM role for API Gateway to invoke the
                  authorizer. REQUEST only.
                type: string
              authorizerPayloadFormatVersion:
                description: AuthorizerPayloadFormatVersion is 1.0 or 2.0. REQUEST
                  only.
                enum:
                - "1.0"
                - "2.0"
                type: string
              authorizerResultTtlInSeconds:
                description: AuthorizerResultTTLInSeconds is how long authorizer results
                  are cached.
                format: int32
                maximum: 3600
                minimum: 0
                type: integer
              authorizerType:
                description: AuthorizerType is JWT (HTTP APIs) or REQUEST (Lambda
                  authorizer).
                enum:
                - JWT
                - REQUEST
                type: string
              authorizerUri:
                description: |-
                  AuthorizerURI is the Lambda authorizer invocation URI. Prefer
                  FunctionRef; if both are set, AuthorizerURI wins. REQUEST only.
                type: string
              enableSimpleResponses:
                description: EnableSimpleResponses lets a REQUEST authorizer return
                  a boolean response.
                type: boolean
              functionRef:
                description: |-
                  FunctionRef references a LambdaFunction CR whose ARN is turned into the
                  authorizer URI. REQUEST only.
                properties:
                  functionName:
                    description: FunctionName is a direct AWS Lambda function name
                      or ARN.
                    type: string
                  name:
                    description: Name of a LambdaFunction CR in the same namespace.
                    type: string
                type: object
              identitySource:
                description: |-
                  IdentitySource is the list of identity sources
                  (e.g. "$request.header.Authorization").
                items:
                  type: string
                type: array
              jwtConfiguration:
                description: JWTConfiguration is required when authorizerType is JWT.
                properties:
                  audience:
                    description: Audience is the list of intended recipients of the
                      JWT.
                    items:
                      type: string
                    type: array
                  issuer:
                    description: |-
                      Issuer is the base domain of the identity provider that issues JWTs
                      (e.g. "https://cognito-idp.us-east-1.amazonaws.com/us-east-1_abc123").
                    type: string
                required:
                - issuer
                type: object
              name:
                description: Name of the authorizer.
                minLength: 1
                type: string
            required:
            - apiRef
            - authorizerType
            - name
            type: object
          status:
            description: APIGatewayV2AuthorizerStatus defines the observed state of
              APIGatewayV2Authorizer.
            properties:
              apiId:
                description: APIID is the resolved API identifier the authorizer was
                  created in.
                type: string
              authorizerId:
                description: AuthorizerID is the authorizer identifier.
                type: string
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
