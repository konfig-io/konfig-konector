---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: s3bucketnotifications.aws.konfig.io
spec:
  group: aws.konfig.io
  names:
    kind: S3BucketNotification
    listKind: S3BucketNotificationList
    plural: s3bucketnotifications
    singular: s3bucketnotification
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.bucketName
      name: Bucket
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: S3BucketNotification is the Schema for managing S3 bucket notification
          configurations.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: S3BucketNotificationSpec defines the desired state of S3BucketNotification.
            properties:
              bucketName:
                description: BucketName is the name of the S3 bucket.
                minLength: 1
                type: string
                x-kubernetes-validations:
                - message: bucketName is immutable
                  rule: self == oldSelf
              eventBridgeEnabled:
                description: EventBridgeEnabled enables delivery of all events to
                  Amazon EventBridge.
                type: boolean
              lambdaFunctionConfigurations:
                description: LambdaFunctionConfigurations are Lambda function notification
                  configurations.
                items:
                  description: S3LambdaNotificationConfig defines a Lambda function
                    notification.
                  properties:
                    events:
                      description: Events is the list of S3 events that trigger this
                        notification.
                      items:
                        type: string
                      minItems: 1
                      type: array
                    id:
                      description: ID is an optional unique identifier for this configuration.
                      type: string
                    lambdaFunctionArn:
                      description: LambdaFunctionARN is the ARN of the Lambda function.
                      minLength: 1
                      type: string
                  required:
                  - events
                  - lambdaFunctionArn
                  type: object
                type: array
              queueConfigurations:
                description: QueueConfigurations are SQS queue notification configurations.
                items:
                  description: S3QueueNotificationConfig defines an SQS queue notification.
                  properties:
                    events:
                      description: Events is the list of S3 events that trigger this
                        notification.
                      items:
                        type: string
                      minItems: 1
                      type: array
                    id:
                      description: ID is an optional unique identifier for this configuration.
                      type: string
                    queueArn:
                      description: QueueARN is the ARN of the SQS queue.
                      minLength: 1
                      type: string
                  required:
                  - events
                  - queueArn
                  type: object
                type: array
              topicConfigurations:
                description: TopicConfigurations are SNS topic notification configurations.
                items:
                  description: S3TopicNotificationConfig defines an SNS topic notification.
                  properties:
                    events:
                      description: Events is the list of S3 events that trigger this
                        notification.
                      items:
                        type: string
                      minItems: 1
                      type: array
                    id:
                      description: ID is an optional unique identifier for this configuration.
                      type: string
                    topicArn:
                      description: TopicARN is the ARN of the SNS topic.
                      minLength: 1
                      type: string
                  required:
                  - events
                  - topicArn
                  type: object
                type: array
            required:
            - bucketName
            type: object
          status:
            description: S3BucketNotificationStatus defines the observed state of
              S3BucketNotification.
            properties:
              conditions:
                description: Conditions describe the current state of the resource.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastSyncTime:
                description: LastSyncTime is when the resource was last successfully
                  reconciled.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent .metadata.generation
                  reconciled.
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
